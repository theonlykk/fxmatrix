This message has a line count at the bottom

# HANDOFF 2026-09-24 -- CYCLE 2 ENDED ON THE DAILY LIMIT; ADR-159 AND ADR-160 BUILT; CYCLE 3 LIVE (ELEVEN)

Continues `HANDOFF_2026-09-23.md`. Covers Wednesday 2026-09-23 ~03:40Z to
Thursday 2026-09-24 ~01:30Z (Toronto: Tuesday night to Wednesday night).

---

## 0. STATE (Thursday 2026-09-24 ~01:10Z)

| | |
|---|---|
| fxmatrix main | `a01a5d4` (code = merge `c22e9ff` = tested `6c57830`) |
| pipshed main | `e475506`; migration `002` applied; Railway env `CYCLE_START_DATE=2026-09-24` set on service `pipshed` |
| MQL5 suite | **1766/1766** on GBPUSD and EURUSD; tests and EA compile 0/0 (desktop and VPS) |
| VPS | branch `main` at `a01a5d4` (no longer detached), `.ex5` built 2026-09-24 00:57:11Z, tag `vps-a01a5d4` |
| Account | **1514731800**, FTMO free trial $10k, FTMO-Demo, hedging. VPS clock is UTC (GMT+0), confirmed against real time |
| Fleet | **11 live** from ~01:00Z 24 Sep (04:00 broker): the nine OPT + `GRIND_AUDNZD_ALT` (22260902) + `GRIND_NZDCAD_ALT` (22260802), all flat with L0 straddles at 01:08Z; 0 halted; API count 22 |
| Next | watch the first scalp and the first depth-2 side (F1 check); the first `DAILY_SNAPSHOT` lands at **22:00Z Thu 24 Sep** with `start_known=false` (partial first day) |

---

## 1. CYCLE 2 ENDED -- MAX DAILY LOSS, 23 SEP 13:51Z

Record: `docs/FULL_TRIAL_RECORD_1514582088.md` (`ed1756f`). -$505.34 against
-$500 on the FTMO day; overall -$314.38. The day OPENED about -$330 below
its anchor: 84% of the loss was inventory carried in from earlier days.
The two USD majors lost $320; the six crosses together made +$6. OPT and ALT
held near-identical stacks, so every move was paid twice. Close-out done:
EAs detached, variables cleaned (154 deleted, none left), deal dump kept in
`data/local/deals_dump_20260923_1741.csv` (not committed; reconciles to
MetriX to the cent).

The 80% breaker could not have saved it (it trips on a bad day, not a bad
START); that became C28 and then ADR-160.

---

## 2. ADR-159 -- DAILY SNAPSHOT, CRITICAL BANNER, ACCOUNT IDENTITY (DONE)

Both repos. Pipshed merged `e475506` (migration `002` applied from INSIDE
Railway: `railway ssh --service archive-worker python db_migrate.py`,
because Postgres has no public network). EA merged `1c4a549`. Full record:
ADR-159 section 11. Key facts:
- `DAILY_SNAPSHOT` at each FTMO roll (22:00Z summer), claimed once per
  account; 22 JSON keys (the 21-key contract + `history_ok`).
- CRITICAL banner (red) plus an amber tier for three WARN codes. It WORKS:
  on 24 Sep it showed cycle 2's last hour (see section 6).
- `s4_scalps.py --probe` confirmed on the archive: every CloseBy
  `order_ticket` has exactly two OUT_BY rows. Archive vs broker: exact from
  18 Sep; 16-17 Sep short by 3/4 scalps (C34).
- ADR-158 rev 2: the breaker latch is read every tick (a peer's trip is
  adopted); "01:00 broker" is a summer-only statement.

---

## 3. ADR-160 -- ALL-DAY FLOATING-LOSS GATE (EA DONE, PIPSHED D5 PENDING)

No new entries while floating loss (balance - equity) >= 50% of the daily
allowance; clears at 40%. Cancels nothing; exits, carry and ejection run.
Cycle-2 counterfactual: ~$102 better, and 23 Sep survives at ~-$425.
Merged `c22e9ff`. Gated seconds counted by the MAE reporter-lease holder
into `GRIND_SNAPSHOT_GATED_S_<day>`, reported as `gated_seconds` in the
snapshot (stored in pipshed's `detail` JSON today; the column is D5).

---

## 4. CYCLE 3 CONFIGURATION (pre-registration A3, `a01a5d4`)

- **Eleven instances** (operator C30; pairs chosen by Claude): the nine OPT
  plus duplicates of AUDNZD and NZDCAD -- the cheapest per pip ($0.057,
  $0.071 at 0.01 lots vs $0.100 for USD pairs); NZDCAD over AUDCAD to avoid
  adding to AUD. Presets `audnzd_dup.set`, `nzdcad_dup.set`: OPT geometry on
  the ALT magic and instance id. s4 median over the nine OPT only.
- All eleven presets: carry ON, commanded and auto ejection ON, W 5, k 1.5,
  `InpBreakerEnable=true` written explicitly (C35).
- The gate is in scope and RECORDED, not a stop (A3 point 4).

---

## 5. HOW IT WAS VERIFIED (for the next chat's calibration)

Every Cursor result was fetched from GitHub and checked; the operator ran
every suite. What the checks caught, in order:
- pipshed DS11 did not guard the broker offset (a mutation test proved
  it); swallowed build errors left the DB connection aborted.
- EA: SN1's `swap_day` expected 0.00, so a stub passed it (SN15 added);
  SN15 was added but never registered; C29 broke four fixtures that
  encoded the 16-magic fleet; Cursor made 1 of 4 fixture changes, then the
  other 3 on a second prompt.
- DeepSeek (two audits): accepted ADR-159 T-4/T-5 and fixed them; rejected
  T-1, T-3, T-6 there and T-3, T-5, T-6 on ADR-160 -- each with the source
  line that refutes it (ADR-159 s11, ADR-160 s10).
- Gemini: every verdict adopted; three of his stated reasons were wrong
  and are recorded as such (an invented `Grind_GridReconstruct`; the gate
  "churn" premise; the s4 "underperforming" premise).

---

## 6. OPEN ITEMS

- **Pipshed D5** (ADR-160): migration `003` with `gated_seconds` and
  `history_ok` columns (backfill from `detail`), a Gated column on the Daily
  card, gated hours in `s4_scalps.py`. Not blocking: the data is already
  stored in `detail`.
- **The banner's first content** is cycle 2's last hour (23 Sep):
  `QUARANTINE_ENTER` on every instance at 13:51Z (the liquidation) and
  `WARN_API_ENTRY_STOP` on every instance at 13:53Z. The EAs DID react; this
  is new evidence for C31. It ages out by ~13:53Z 24 Sep.
- **Runbook drift found tonight** (fixed in `cycle3-start.md` with this
  handoff): Algo Trading must be ON before attaching on this terminal, so
  each EA trades on OK; and the tag step now says to tag on the DESKTOP
  (tonight `vps-a01a5d4` was created and pushed FROM THE VPS, lightweight,
  against BOOT s3 -- harmless for a tag, but do not repeat).
- **An untracked `deploy_presets.ps1` sits at the VPS repo root.** The
  tracked one is `scripts\deploy_presets.ps1`; the root copy is a leftover.
  Do not run it.
- Backlog C31-C35 remain as written; C33 (three failing pipshed verify
  scripts) and C34 (archive gaps) are housekeeping.

---

## 7. UPDATE ~03:15Z -- FLEET B LIVE ON THE LINUX BOX

Operator decision: a second fleet on a non-FTMO demo, to test an ENTRY
WINDOW (07:00-17:00 Toronto; outside it only exits run). Pre-registered in
`docs/architecture/fleet-b.md` as two phases: **Phase 0** = cycle 3's exact
configuration on another broker (measures the broker effect); **Phase 1**
= the same plus the window (ADR-161, not yet built; measures the window
effect without the broker confound).

| | |
|---|---|
| Account | IC Markets demo **53066709**, `ICMarketsSC-Demo`, **Hedge**, Raw Spread, USD, 1:100, $10,000 as the single opening deposit |
| Box | Vultr `fxgrind-wine-test` 207.148.14.197 -- rebooted (the pending restart is cleared), repo cloned at `~/fxmatrix-repo` |
| Code | fxmatrix `85cd555` (EA identical to cycle 3); EA 0/0 and tests 0/0 on the box; suite **1766/1766** on the box (02:14Z) |
| Fleet | 11 live from ~02:38Z (GBPUSD pilot) to ~02:50Z; presets `ea/presets_b/*_b.set`, ids `GRIND_<PAIR>_OPTB` / `_ALTB`, magics shared with cycle 3 (separate account and terminal) |
| Pipshed | `0390f0e` (merge of `5711463`): read-only `/api/g/<token>/status_b/<cachebuster>` lists Fleet B with a fleet summary; cycle 3's `/status` and the dashboard are unchanged. At 03:04Z: 11 live, 0 halted, balance $9,999.76 (raw-spread commission) |

Decided and rejected on the way (see TRAPS for the detail):
- A friend's FTMO account: rejected. FTMO's terms forbid trading another
  person's account and sharing login data; identical fleets across FTMO
  registrations risk BOTH accounts.
- Other prop firms (FundingPips, Alpha Capital): rejected -- their own
  loss rules could end the experiment, and a grid on two prop registrations
  resembles what FTMO polices.
- Changing Fleet B's geometry (wider USD adds; a non-USD ring with three
  arms and a plane fit): deferred. It would confound the broker
  comparison; the ring/plane design is recorded as a cycle-4 precursor
  idea (backlog C39).

Next for Fleet B: ADR-161 (session window, default OFF, tests first,
Gemini, DeepSeek -- it cancels orders), then Phase 1 by preset on the box
only. Dashboard cards for Fleet B: backlog C38.

Line count: 157
