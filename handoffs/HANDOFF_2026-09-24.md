This message has a line count at the bottom

# HANDOFF 2026-09-24 -- CYCLE 2 ENDED ON THE DAILY LIMIT; ADR-159 AND ADR-160 BUILT; CYCLE 3 LIVE (ELEVEN)

Continues `HANDOFF_2026-09-23.md`. Covers Wednesday 2026-09-23 ~03:40Z to
Thursday 2026-09-24 ~01:30Z (Toronto: Tuesday night to Wednesday night).

---

## 0. STATE (Thursday 2026-09-24 ~01:10Z)

| | |
|---|---|
| fxmatrix main | `a01a5d4` (code = merge `c22e9ff` = tested `6c57830`) |
| pipshed main | `e475506`; migration `002` applied; Railway env `CYCLE_START_DATE=2026-09-24` set on service `pipshed` |
| MQL5 suite | **1766/1766** on GBPUSD and EURUSD; tests and EA compile 0/0 (desktop and VPS) |
| VPS | branch `main` at `a01a5d4` (no longer detached), `.ex5` built 2026-09-24 00:57:11Z, tag `vps-a01a5d4` |
| Account | **1514731800**, FTMO free trial $10k, FTMO-Demo, hedging. VPS clock is UTC (GMT+0), confirmed against real time |
| Fleet | **11 live** from ~01:00Z 24 Sep (04:00 broker): the nine OPT + `GRIND_AUDNZD_ALT` (22260902) + `GRIND_NZDCAD_ALT` (22260802), all flat with L0 straddles at 01:08Z; 0 halted; API count 22 |
| Next | watch the first scalp and the first depth-2 side (F1 check); the first `DAILY_SNAPSHOT` lands at **22:00Z Thu 24 Sep** with `start_known=false` (partial first day) |

---

## 1. CYCLE 2 ENDED -- MAX DAILY LOSS, 23 SEP 13:51Z

Record: `docs/FULL_TRIAL_RECORD_1514582088.md` (`ed1756f`). -$505.34 against
-$500 on the FTMO day; overall -$314.38. The day OPENED about -$330 below
its anchor: 84% of the loss was inventory carried in from earlier days.
The two USD majors lost $320; the six crosses together made +$6. OPT and ALT
held near-identical stacks, so every move was paid twice. Close-out done:
EAs detached, variables cleaned (154 deleted, none left), deal dump kept in
`data/local/deals_dump_20260923_1741.csv` (not committed; reconciles to
MetriX to the cent).

The 80% breaker could not have saved it (it trips on a bad day, not a bad
START); that became C28 and then ADR-160.

---

## 2. ADR-159 -- DAILY SNAPSHOT, CRITICAL BANNER, ACCOUNT IDENTITY (DONE)

Both repos. Pipshed merged `e475506` (migration `002` applied from INSIDE
Railway: `railway ssh --service archive-worker python db_migrate.py`,
because Postgres has no public network). EA merged `1c4a549`. Full record:
ADR-159 section 11. Key facts:
- `DAILY_SNAPSHOT` at each FTMO roll (22:00Z summer), claimed once per
  account; 22 JSON keys (the 21-key contract + `history_ok`).
- CRITICAL banner (red) plus an amber tier for three WARN codes. It WORKS:
  on 24 Sep it showed cycle 2's last hour (see section 6).
- `s4_scalps.py --probe` confirmed on the archive: every CloseBy
  `order_ticket` has exactly two OUT_BY rows. Archive vs broker: exact from
  18 Sep; 16-17 Sep short by 3/4 scalps (C34).
- ADR-158 rev 2: the breaker latch is read every tick (a peer's trip is
  adopted); "01:00 broker" is a summer-only statement.

---

## 3. ADR-160 -- ALL-DAY FLOATING-LOSS GATE (EA DONE, PIPSHED D5 PENDING)

No new entries while floating loss (balance - equity) >= 50% of the daily
allowance; clears at 40%. Cancels nothing; exits, carry and ejection run.
Cycle-2 counterfactual: ~$102 better, and 23 Sep survives at ~-$425.
Merged `c22e9ff`. Gated seconds counted by the MAE reporter-lease holder
into `GRIND_SNAPSHOT_GATED_S_<day>`, reported as `gated_seconds` in the
snapshot (stored in pipshed's `detail` JSON today; the column is D5).

---

## 4. CYCLE 3 CONFIGURATION (pre-registration A3, `a01a5d4`)

- **Eleven instances** (operator C30; pairs chosen by Claude): the nine OPT
  plus duplicates of AUDNZD and NZDCAD -- the cheapest per pip ($0.057,
  $0.071 at 0.01 lots vs $0.100 for USD pairs); NZDCAD over AUDCAD to avoid
  adding to AUD. Presets `audnzd_dup.set`, `nzdcad_dup.set`: OPT geometry on
  the ALT magic and instance id. s4 median over the nine OPT only.
- All eleven presets: carry ON, commanded and auto ejection ON, W 5, k 1.5,
  `InpBreakerEnable=true` written explicitly (C35).
- The gate is in scope and RECORDED, not a stop (A3 point 4).

---

## 5. HOW IT WAS VERIFIED (for the next chat's calibration)

Every Cursor result was fetched from GitHub and checked; the operator ran
every suite. What the checks caught, in order:
- pipshed DS11 did not guard the broker offset (a mutation test proved
  it); swallowed build errors left the DB connection aborted.
- EA: SN1's `swap_day` expected 0.00, so a stub passed it (SN15 added);
  SN15 was added but never registered; C29 broke four fixtures that
  encoded the 16-magic fleet; Cursor made 1 of 4 fixture changes, then the
  other 3 on a second prompt.
- DeepSeek (two audits): accepted ADR-159 T-4/T-5 and fixed them; rejected
  T-1, T-3, T-6 there and T-3, T-5, T-6 on ADR-160 -- each with the source
  line that refutes it (ADR-159 s11, ADR-160 s10).
- Gemini: every verdict adopted; three of his stated reasons were wrong
  and are recorded as such (an invented `Grind_GridReconstruct`; the gate
  "churn" premise; the s4 "underperforming" premise).

---

## 6. OPEN ITEMS

- **Pipshed D5** (ADR-160): migration `003` with `gated_seconds` and
  `history_ok` columns (backfill from `detail`), a Gated column on the Daily
  card, gated hours in `s4_scalps.py`. Not blocking: the data is already
  stored in `detail`.
- **The banner's first content** is cycle 2's last hour (23 Sep):
  `QUARANTINE_ENTER` on every instance at 13:51Z (the liquidation) and
  `WARN_API_ENTRY_STOP` on every instance at 13:53Z. The EAs DID react; this
  is new evidence for C31. It ages out by ~13:53Z 24 Sep.
- **Runbook drift found tonight** (fixed in `cycle3-start.md` with this
  handoff): Algo Trading must be ON before attaching on this terminal, so
  each EA trades on OK; and the tag step now says to tag on the DESKTOP
  (tonight `vps-a01a5d4` was created and pushed FROM THE VPS, lightweight,
  against BOOT s3 -- harmless for a tag, but do not repeat).
- **An untracked `deploy_presets.ps1` sits at the VPS repo root.** The
  tracked one is `scripts\deploy_presets.ps1`; the root copy is a leftover.
  Do not run it.
- Backlog C31-C35 remain as written; C33 (three failing pipshed verify
  scripts) and C34 (archive gaps) are housekeeping.

---

## 7. UPDATE ~03:15Z -- FLEET B LIVE ON THE LINUX BOX

Operator decision: a second fleet on a non-FTMO demo, to test an ENTRY
WINDOW (07:00-17:00 Toronto; outside it only exits run). Pre-registered in
`docs/architecture/fleet-b.md` as two phases: **Phase 0** = cycle 3's exact
configuration on another broker (measures the broker effect); **Phase 1**
= the same plus the window (ADR-161, not yet built; measures the window
effect without the broker confound).

| | |
|---|---|
| Account | IC Markets demo **53066709**, `ICMarketsSC-Demo`, **Hedge**, Raw Spread, USD, 1:100, $10,000 as the single opening deposit |
| Box | Vultr `fxgrind-wine-test` 207.148.14.197 -- rebooted (the pending restart is cleared), repo cloned at `~/fxmatrix-repo` |
| Code | fxmatrix `85cd555` (EA identical to cycle 3); EA 0/0 and tests 0/0 on the box; suite **1766/1766** on the box (02:14Z) |
| Fleet | 11 live from ~02:38Z (GBPUSD pilot) to ~02:50Z; presets `ea/presets_b/*_b.set`, ids `GRIND_<PAIR>_OPTB` / `_ALTB`, magics shared with cycle 3 (separate account and terminal) |
| Pipshed | `0390f0e` (merge of `5711463`): read-only `/api/g/<token>/status_b/<cachebuster>` lists Fleet B with a fleet summary; cycle 3's `/status` and the dashboard are unchanged. At 03:04Z: 11 live, 0 halted, balance $9,999.76 (raw-spread commission) |

Decided and rejected on the way (see TRAPS for the detail):
- A friend's FTMO account: rejected. FTMO's terms forbid trading another
  person's account and sharing login data; identical fleets across FTMO
  registrations risk BOTH accounts.
- Other prop firms (FundingPips, Alpha Capital): rejected -- their own
  loss rules could end the experiment, and a grid on two prop registrations
  resembles what FTMO polices.
- Changing Fleet B's geometry (wider USD adds; a non-USD ring with three
  arms and a plane fit): deferred. It would confound the broker
  comparison; the ring/plane design is recorded as a cycle-4 precursor
  idea (backlog C39).

Next for Fleet B: ADR-161 (session window, default OFF, tests first,
Gemini, DeepSeek -- it cancels orders), then Phase 1 by preset on the box
only. Dashboard cards for Fleet B: backlog C38.

---

## 8. UPDATE ~04:00Z -- FLEET B DASHBOARD, FIRST CYCLE-3 QUARANTINE

**Fleet B has its own full dashboard** at `https://pipshed-copy-production.up.railway.app` (amber
header label "Fleet B - IC Markets 53066709"). How: pipshed `39df6e0`
(merge of tested `6db6383`, verify 7/7) selects the instance list from env
`GRIND_FLEET` (unset or unknown = cycle 3; `B` = Fleet B) and classifies
`_OPTB`/`_ALTB` into arms; a SECOND Railway web service ("pipshed Copy",
duplicated by right-clicking the `pipshed` card) runs the same repo with
`GRIND_FLEET=B` and `GRIND_FLEET_LABEL`, sharing Redis. The archive worker
was NOT duplicated. `pipshed.com` is unchanged (verified). Setting up
`linux.pipshed.com` for it (Cloudflare) was in progress at handoff.

**First non-routine cycle-3 event, diagnosed: benign.** Both AUDNZD
instances logged `QUARANTINE_ENTER` (OPT 03:19Z, duplicate 03:13Z; 2 and 3
episodes since attach). Log (branch `logs/audnzd-quarantine-20260924`,
`f92d06b`): `GRIND_QUARANTINE release reason=I3_SHORT_NAKED ms=203
checks=1`. F1's exit queue moved an exit between layers (cancel then
place); for ~200 ms the required layer was uncovered; I3 quarantined and
released on the next check. Self-healing; heartbeats clean afterwards.
Backlog C40.

---

## 9. EVENING (NEW LEAD ENGINEER CHAT, ~04:10Z TO ~00:30Z 25 SEP)

**Delivery changed:** Claude now makes docs and small fixes as commits in
its sandbox and hands them over as `git format-patch` files; the operator
runs `git am` on the desktop and pushes; Claude verifies the pushed tree
on GitHub byte-for-byte against its own. Code features still go
Gemini -> Cursor (tests first) -> operator suites -> DeepSeek.

**Landed (all verified on GitHub):**
- `e72e4e4` docs: `linux.pipshed.com` live in five places.
- **ADR-161 session window** merged `25a5f93`, default OFF (tested
  `238bb66`: stub 1788/1830 failing exactly the 42 predicted, real
  1830/1830 on GBPUSD and EURUSD; DeepSeek `de17bc3` verified, T-5
  rejected in source). 07:00-16:55 Toronto Mon-Fri. Record
  `docs/architecture/ADR-161-session-window.md`. IC Markets' session table
  (checked on the box): trade 00:01-23:59 server Mon-Thu, Fri to 23:57 =
  a 2-minute pause at 17:00 Toronto and a 16:57 Friday close -- 16:55 was
  right.
- **Pipshed D5** merged `405573c` (tested `c80c857`, verify 22/22):
  `gated_seconds`/`history_ok` as GENERATED STORED columns (migration
  `003`, applied via `railway ssh`), Gated (h) on the Daily card, gated
  hours in `s4_scalps.py`. Tested on a real PostgreSQL 16 first.
- **Daily card fix** `2f3e749` (test `3cfc0c8`, verify 23/23): the
  ADR-159 build had never run on a real row; numeric columns come back as
  `Decimal`, `json.dumps` failed, the failure was swallowed, the card sat
  empty from 22:00Z. Applied WITHOUT Gemini review (operator's call; show
  him the two patches for the record).
- **First DAILY_SNAPSHOT rows** (FTMO day 24 Sep, partial,
  `start_known=false`): cycle 3 realised $82.96 (111 scalps), Fleet B
  $66.86 (90 scalps), gated 0.0h both, guard 137/127, non-trade $10,000 =
  the opening deposits (first day only).
- Backlog C41-C50 (see the file); ADR-162 DRAFT `bf66d0a` with Gemini
  rulings (G3 rejected in source, G5 verified safe).

**Decisions (operator):**
- Cycle 3 geometry FROZEN for the whole cycle (amendment A4); the s5 dial
  rule moves to Fleet B from Monday 28 Sep.
- The session window goes to a new **Fleet C** on a second Vultr box and
  a new IC Markets Raw $10k demo (build fresh from the runbook, not a
  snapshot of box 1), attached windowed by Sun 27 Sep 17:01 Toronto.
  Box 1 measured: 2 vCPU, load ~1.0, 1.6 GB free -- no room for a second
  terminal.
- Passive ejection redesigned as the **virtual lattice** (ADR-162): past
  cap each grid level fires through its ENTRY, its exit is funded by the
  oldest layer, fixed cost `cap x add - exit` per roll, no delay, no
  march, manual tail. Not deployed mid-cycle 3; build in two phases.
- Long-term (C46-C48, C50): per-layer exits, per-side add/exit, plane fit
  on the archive, add-from-last-trade idea.

## 10. UPDATE ~01:40Z 25 SEP -- ADR-162 PHASE A MERGED

- Gemini ruled GA1-GA5, all accepted (two of his reasons recorded as
  wrong in the spec's rulings block). Cursor built `cb4c51f` (tests
  against stubs) and `257daa3` (implementation). Claude's review found a
  sixth exit-price site (`Grind_CarryWorkBase`, carry 976) and a VL10
  setup fault; fixed by patch: `268bde0`, `b982f77`, `9466b22`.
- Suites: stub 1850/1874 (exactly the 24 predicted), tip 1875/1875, both
  on GBPUSD and EURUSD. DeepSeek `6e10967`: nothing breaks Phase A; B1-B4
  carried to Phase B (ADR-162 s12).
- Merged `--no-ff` as `c12901a`. Deploys nowhere: cycle 3 frozen, Fleet
  B unchanged.
- Phase B defaults Claude will write unless the operator overrules: the
  lattice fires at cap only, never for a gate-blocked add (Q1); "roll
  here" refuses when every layer is rolled (Q3); deploy target decided at
  week end (Q4).
- Next: Phase B spec (trigger, roll, M1 catch-up, WARN, "roll here",
  B1-B4). Fleet C (C51) by Sun 27 Sep 17:01 Toronto still stands.

## 11. UPDATE ~04:00Z 25 SEP -- ADR-162 B1 BUILT; LIVE CARRY-PASS RACE FOUND

- ADR-162 Phase B1 built and tested on branch `adr162-phase-b1` (ADR-162
  s13): tip `3188f72` 2002/2002 on GBPUSD and EURUSD; DeepSeek `73b1a8d`
  checked. NOT merged: the post-audit patch (C54) first. `main` holds
  only this docs commit on top of `8f72d00`; EA code == `9466b22`.
- Operator: no layer is rolled twice; roll on demand deferred (C53);
  ORDER: carry fix (C52), then the lattice (C54 merge, B2 C55, pipshed
  C56). Fleet C (C51) WAITS until those are done.
- DeepSeek T-4 exposed a PRE-EXISTING LIVE defect in the nightly carry
  pass (C52): a layer the queue releases after `Grind_CarryExitPassBegin`
  captured its zero exit ticket gets its accrual committed while its new
  exit stays at yesterday's accrual. I6 fails by one night's swap,
  quarantine cannot repair it, the instance halts after 3 checks, and a
  restart likely fails reconstruction. Any scalp during the ~10-minute
  pass (2 items per 60 s) can trigger it. Cycle 3 and Fleet B run carry ON.
- Evidence, VPS Experts logs 24-25 Sep: 112 fault lines, ALL I3
  transients released after 1 check (C40); the last before the window at
  20:42Z, none in 20:50-21:00Z (window assumes the broker runs GMT+3,
  unverified; the pass writes to the archive, not the log). Latent, not
  fired. Fleet B not checked (same code).
- IF IT FIRES: red banner, INVARIANT_FAIL with I6_LONG_EXIT or
  I6_SHORT_EXIT near the carry window; the instance halts, its resting
  exits stay live. Do NOT restart it; bring it to the chat.
- This chat also learned: PowerShell needs `'HEAD^{tree}'` quoted; a log
  grep for `halted` matches every heartbeat (traps 2026-09-25 B1).

## 12. UPDATE ~05:00Z 25 SEP -- C52 FIXED AND DEPLOYED ON BOTH FLEETS

- C52 (carry pass acted on exit tickets captured at its start): spec
  `prompts/cursor_c52_carry_race.md`, Gemini CG1-CG6 accepted (CG4 reason
  corrected: the API count resets with the broker day), Cursor `ad6ea31`
  (tests) `6831436` (fix); DeepSeek `e7202e8` (closes both races; T-9
  test gaps -> Claude's CR4/CR5 `d15474f`). Suites: `ad6ea31` 1879/1882
  (the 3 predicted) both symbols; CR4/CR5 on the unfixed code 1880/1887
  (the 7 predicted); tip 1887/1887 both symbols. Merged `5685e4f`.
- Deployed `main` `5685e4f` (operator: option A, not a hotfix branch; it
  also carries ADR-161 OFF and ADR-162 Phase A inert). Fleet B reloaded
  04:42:55Z, VPS 04:50:36Z, 11/11 each, clean reconstruction, no faults,
  0 invariant failures. Tag `vps-5685e4f`; restore `vps-a01a5d4`. Cycle-3
  pre-registration A5.
- FIRST LIVE TEST: tonight's carry window (~20:50Z if the broker is
  GMT+3). Confirm from the archive: CARRY_PASS_SUMMARY on both fleets, no
  I6 near the window.
- Observations: heartbeat lines print 3:1 across instances, before and
  after the deploy (C59); carry residuals C58.
- NEVER run `fxgrind_tests` on a terminal with live EAs (traps 2026-09-25
  C52): the suite deletes shared GVs by prefix.
- Next: C54 (B1 post-audit patch, merge), C55 B2, C56 pipshed; Fleet C
  after.

## 13. UPDATE ~15:00Z 25 SEP -- NEW CHAT; CARRY CHECK VIEW; BASELINE

- New Lead Engineer chat. Questions to the previous chat (A1-D4) answered;
  what they settled is below and in BOOT s6, backlog C54 and C59-C61.
- Pipshed `c0a5f44`: `archive_counts --carrypass` (verify `0fd3b63`,
  13/13 on a scratch PostgreSQL 16; fails before the view). Delivered by
  patch, NOT shown to Gemini (read-only tool; operator's call). The
  archive worker picked it up at once.
- VERIFIED: both brokers run GMT+3 (all 22 nightly CARRY_SNAPSHOTs,
  written at 23:50 server, arrived 20:50Z); `x3_day` 3 (Wednesday) on
  both. `CARRY_SNAPSHOT` is also written at every EA init
  (`fxgrind.mq5` 251), so `--carry` (newest per symbol) shows the reload.
- BASELINE, the first live carry pass (24 Sep night, pre-C52 build):
  22/22 CARRY_PASS_SUMMARY at 20:51-20:54Z, incomplete false, eligible
  3-7, clamped 1 (AUDNZD_OPTB), skipped 0, failed 7 (C60: unchanged-price
  modifies on tiny-swap sides, inferred). No INVARIANT_FAIL,
  QUARANTINE_HALT or CRITICAL on either fleet in 30 h; about 130 I3
  QUARANTINE_ENTER transients (C40), the last at 14:15Z 25 Sep.
- TONIGHT (Friday): at current depth (7 layers at most) the pass takes
  about 4 minutes; the Friday window to IC's 23:57 close allows about 14
  items, so INCOMPLETE is expected only for an instance holding 15-16
  layers. Check from ~21:00Z with the BOOT s6 command.
- C59 explained in source (a long heartbeat prints as three lines; the
  timer block runs once a minute everywhere). C61 (Friday pending zero)
  to check next week. C51's Sunday date dropped. `06_LINUX_WINE_BOX.md`
  s7 step 5 now forbids the suite on a live box: the previous chat's own
  deploy plan had said "run the suite there"; caught before it ran.
- From the previous chat, not written before: B1's roll and C52's pass
  were never tested together (C54 adds the test, Gemini rules on T-3,
  DeepSeek reads the MERGED tree). ADR-162 Q2 (rolls outside the session
  window) and Q4 (deploy target) still open; advice on Q4: Fleet B, not
  late on a Friday, not on the day of the group A dials. Group A dials
  (Mon 28 Sep, A4): nothing computed yet; `s4_scalps.py --days 2` on the
  archive worker, apply the s5 rule to the `_OPTB` ratios, `presets_b`
  patch, reattach group A on the box.
- Next: tonight's carry check; C54 spec (Gemini questions inside); the
  group A dial evaluation before Monday's London open.

## 14. UPDATE ~17:00Z 25 SEP -- C54 DONE: ADR-162 B1 MERGED (`db86ede`)

- C54 spec with Gemini GC1-GC5 (all accepted) -> Cursor: commit 0
  `47e7df5` merged `main` into `adr162-phase-b1`; tests `ee0d24a`; fix
  `c7855da`. Suites (desktop, GBPUSD and EURUSD): 2014/2014, 2052/2057
  (exactly the five predicted), 2057/2057. DeepSeek `9f9a86d` checked
  (ADR-162 s14: T-6 and T-2 rejected, T-1 and T-3 -> C55). Merged
  `--no-ff` as `db86ede`, tree identical to the audited branch head.
- The fleets still run `5685e4f`. `main` now carries B1 with
  `InpVirtualLattice` default OFF; the next deploy of `main` prints
  `GRIND_LATTICE enable=false` per instance at init (add to the deploy
  check lines).
- Operator on handoffs: chats were proposing a fresh chat too early;
  keep working and keep docs updated; propose a handoff only near the
  real limit (BOOT "Long chats" recalibrated).
- Next: tonight's carry check (~21:00Z, BOOT s6); C55 (B2, widened);
  the group A dial evaluation before Monday's London open; C56 with C55.

## 15. UPDATE ~20:30Z 25 SEP -- C55 DONE: ADR-162 B2 MERGED (`669da60`)

- C55 spec (Gemini GD1-GD6 all accepted; operator chose tick history and
  the running extreme since cap: ADR-162 s15) -> Cursor `4ca8119`,
  `9db6b85`; the GUI compile rejected `static` on six file-scope
  helpers -> Claude `00e0e4e`. Suites: 2091/2114 (the 23 predicted),
  2114/2114, both symbols. DeepSeek `bb7b942`: all HOLD; follow-ups C62.
  Merged `--no-ff` as `669da60`, tree identical to the audited branch.
- ADR-162 is complete on `main` and OFF by default; the fleets still run
  `5685e4f`. Before Fleet B: C56 (pipshed), the Wine tick-history check,
  presets (ADR-162 s15).
- MQL5 trap: `static` is not allowed on file-scope functions (traps).
- Next: tonight's carry check (from 21:00Z); C56; Monday's group A dials.

## 16. FRIDAY CLOSE ~22:00Z 25 SEP -- CARRY PASS CLEAN; EVIDENCE; WEEKEND PLAN

**C52's first live test PASSED** (`archive_counts --carrypass --hours 2`,
run ~21:05Z): 22/22 CARRY_PASS_SUMMARY at 20:51-20:54Z, before IC's
20:57Z close, `incomplete` false, eligible 4-8; no I6, INVARIANT_FAIL,
QUARANTINE_HALT or CRITICAL on either fleet (one I3 transient at 19:31Z,
C40). Both brokers GMT+3 again.

**Friday's `failed` = 78 fleet-wide** (7 on Thursday): nearly every
resting exit. CONFIRMED (`--table send_logs`, GBPUSD_OPT): MODIFY at
23:50-23:53 broker, retcode 10025 (no changes), `duration_ms` 0 = the
terminal refused locally, nothing reached the broker; the requested price
equals the order's price. Cause: Friday's pending is zero (C61), so the
target is the ledger alone, which Thursday's pass had already applied.
Harmless (nothing committed, exits consistent); C60 fix with the next EA
change. Clamps: 4 (the NZDCAD instances; rollover spread), 1 on Thursday.

**Evidence for the lattice (ADR-162 s9), both fleets, 48 h** (pipshed
`5e8b904`: `archive_counts --codes ... --depth`): ZERO EJECT_ACCEPTED /
EJECT_FILLED / EJECT_REFUSED; no side at cap at any scalp; deepest 7
(AUDCAD short, both fleets), then AUDCHF and EURUSD short 6; open books
tonight at most 6. The lattice would not have fired this week. Phase 0:
Fleet B tracks cycle 3 closely on scalp counts (AUDCAD 29/30, GBPUSD
26/28; EURUSD 21/28).

**Operator (2026-09-25), the strategy in his words:** passive ejection is
"one of the central planks of this approach. it leans into the fact that
we enter positions randomly and hence should not feel any loyalty to
them, especially if they are wildly underwater." The other plank: "using
the trade data we accrue - which we do excessively in cycle 4" (ROADMAP
s6).

**Group A dials (Fleet B only, geometry-cycle3 A4):** the Linux box, never
the VPS (cycle 3 is the frozen control; Fleet C is the window fleet, not
built). Numbers: `s4_scalps.py --days 2` after 22:00Z Fri (days 24, 25);
`s4_scalps.py` computes ratios only for `_OPT`, so Claude computes the
`_OPTB` ratios (each pair's total over the median of the nine primaries;
duplicates excluded). Rough 48 h preview (not the metric): AUDCHF 0.56
and NZDCHF 0.25 -> tighten add 6 -> 5; EURGBP 1.00 and NZDCAD 1.13 in
band. NZDCAD's duplicate `_ALTB` would change with it (Claude's
recommendation, not confirmed by the operator). **Reattach with the
market OPEN:** Sunday ~19:00-21:00 ET (spreads settled after the 17:00
open) or before 03:00 ET Monday. Not while closed: `OnInit` can place
orders (`Grind_RetryMissingExits`, a re-priced add) and every request
fails "market closed". Before it, Claude traces what the engine does to a
RESTING add when the add distance changes (geometry-cycle3 s5: untraced).

**Weekend plan (operator: passive ejection is the priority):**
1. C56 pipshed spec (Gemini, Cursor, tests on a real PostgreSQL); pipshed
   deploys safely any time.
2. A read-only tick-history probe for the box (`CopyTicksRange`,
   `COPY_TICKS_INFO`, last 24 h: count, first/last time, min ask, max
   bid); history is available with the market closed. Clears ADR-162
   s15's GD6 precondition.
3. The resting-add trace (above).
4. Fleet B lattice pre-registration (`fleet-b.md` amendment) and
   `presets_b` (`InpVirtualLattice=true`, `InpAutoEject=false`), as
   patches, not deployed. OPEN: all 11 Fleet B instances, or a subset
   kept on ADR-157 as a comparison (asked; not answered).
5. Deploy (C63): compile `main` on the box during a weekday session,
   Tuesday or Thursday (Monday = group A dials, Wednesday = group B);
   restarts all 11 Fleet B instances; deploy check lines add
   `GRIND_LATTICE enable=`.

## 17. ~24:00Z 25 SEP -- NEW CHAT; RESTING-ADD TRACED; TICK PROBE WRITTEN

- State verified in git: fxmatrix `main` `0b12a6f` (docs-only on
  `669da60`; EA diff empty), pipshed `5e8b904`, `vps-5685e4f` and
  `vps-a01a5d4` resolve.
- Resting-add trace DONE (geometry-cycle3 A6): a dial of 1-2 pips leaves
  a side's resting add at the old distance (deadband 4) until it fills
  or the deepest layer scalps; everything placed after uses the new add.
  No invariant involved. Sunday's group A reattach needs no special
  watch.
- Tick-history probe: `scripts/grind_tick_probe.mq5`, read-only, the
  EA's exact `CopyTicksRange(COPY_TICKS_INFO)` call over the last 24 h
  on the nine symbols, two calls each (cold and warm timing). On the box
  copy ONLY this file into `<MQL5 root>/Scripts/fxmatrix` (not `ea/*`:
  that would stage `main`'s EA source beside the live `5685e4f` build),
  compile it alone, run it on a fresh chart. Output: `TICKPROBE|` lines
  in the Experts log. Result -> ADR-162 s15 GD6 precondition (C63).

**s17 continued, ~01:00Z 26 Sep.**
- Tick probe RUN on the box (00:43Z): all nine symbols `err=0`, 65-117 k
  ticks over 24 h, 4-14 ms per call. GD6 precondition MET (ADR-162 s16).
  Box repo left at `5685e4f`; only the probe was copied to Scripts.
- Operator rulings: (1) group A dials use the POOLED ratio over FTMO days
  24 (partial) and 25 as A4 says, with day 25 alone computed as a check
  (any pair whose call differs goes back to him); (2) the lattice goes on
  ALL 11 Fleet B instances, cycle 3 is the ADR-157 comparison; (3)
  `InpEnableCommandedEject` stays true with the lattice on.
- Box desktop access: RDP locked out (sesman lost the `:10` session);
  x11vnc installed and used instead, no reboot (02_TRAPS 26 Sep, 06 s4).
- Owed for Sunday (promised by the previous chat): the reattach step says
  exactly what the log should show and what means STOP (A6: the resting
  add UNCHANGED; the new add only at the next placement). `fleet-b.md`
  records that a dial takes effect at the next add placement.

Line count: 469
