This message has a line count at the bottom

# HANDOFF 2026-09-17b -- DEFENSIVE MODE: 20 DEEP LAYERS CLOSED, MAJORS CAPPED AT 8, NZD ALT ARMS BACK

Continues `HANDOFF_2026-09-17.md` (ADR-151 deploy, state at 02:53Z). Same
night, same chat. No code changed; everything here is operator action on the
live account plus design discussion. All times UTC.

---

## 0. STATE AT 03:54Z

| | |
|---|---|
| fxmatrix main | `2b570a3` + this addendum commit. EA source unchanged since `c39fb84` |
| VPS build | `c39fb84` (ADR-151 Phase A) |
| Fleet | **16 attached, 0 halted** (status c23, 03:54:15Z). NZDCHF not attached (ADR-146) |
| Book | AccountLimits 03:54:25Z: **70 positions + 82 orders = 152** |
| Guard | ~152 + ~31 resting entries = ~183 <= 194: entries flowing |
| Majors | GBPUSD OPT/ALT, EURUSD OPT/ALT at **`InpMaxLayers` = 8 set in the EA dialog only**. Repo presets still say 12 (s4) |
| USD long layers | 21 (GBPUSD ALT 8, OPT 6; EURUSD OPT 4, ALT 3) = ~$2.10/pip at 0.01 |
| Open MTM | ~ -$188 fleet (status c20, 03:39Z) |

## 1. WHY -- DRAWDOWN, NOT MECHANICS

Pipshed summary ~03:20Z (before the closes): **Equity 9,827.39, Balance
10,228.61**, open MTM -381.66, financing -19.56. Cycle to date +235.96 net.

FTMO 2-step rules as published: max daily loss 5% of initial balance, measured
from the midnight CE(S)T balance, closed + floating; max loss 10%. Assuming a
$10,000 initial balance and a midnight balance ~10,228, the daily floor is
~9,728 and **headroom was ~$100**. Sensitivity was ~$4 per pip of USD strength
(40 long USD-pair layers), i.e. ~25 pips from breach. Next reset 22:00Z.
**None of this was verified in MetriX** -- initial balance, the recorded
midnight balance and whether the demo follows Challenge rules are assumptions.

Operator decision: close the deepest underwater layers on the four USD-pair
instances and cap them at 8. Closing does not change today's daily-loss figure
(floating already counts); it halves the per-pip sensitivity (~48 pips of
headroom instead of ~25).

## 2. THE CLOSES (operator, manual, 03:22-03:38Z)

Procedure per instance: detach -> delete own ENT orders -> close L00-L04 (all
held layers, no exit orders) -> reattach with `InpMaxLayers` 8 and key ->
status read. Loss column is the MTM change between reads, NOT the realised
figure; the MT5 History tab holds the actuals.

| Instance (magic) | ENT deleted | Positions closed | ~Loss |
|---|---|---|---|
| GBPUSD ALT (22260102) | 544240791 | 541060808, 541999470, 542056165, 543250220, 543261883 | -59 |
| GBPUSD OPT (22260101) | 544286642, 544286410 | 540374044, 541050954, 542106297, 543251889, 543312365 | -63 |
| EURUSD OPT (22260201) | 544294765, 544286416 | 541056374, 541143859, 541935535, 543875516, 543876862 | -48 |
| EURUSD ALT (22260202) | 544285101, 544285000 | 541057335, 541144297, 541940108, 543236894, 543420521 | -48 |

Total ~ -$218. **Manual closes do not enter EA realised P&L, `scalp_history`
or the pipshed summary.**

Reattach results (status c17-c20), all `recon_ok true`, cap 8:
- GBPUSD ALT: 7 longs L05-L11; L12 add then filled 03:28:20 (1.33712), exit
  187 ms later, L09 exit trimmed; now **8/8, adds blocked by cap**.
- GBPUSD OPT: 6 longs L05-L10, exits L10/L09/L08.
- EURUSD OPT: 4 longs L05-L08, exits L08/L07/L06.
- EURUSD ALT: 3 longs L05-L07, all with exits.

**Code facts verified before closing** (fxmatrix @ c39fb84): layer-index gaps
are valid (`Grind_ReconLayerIndicesValid` rejects only negative/duplicate);
next add index = max + 1 (`Grind_SideNextIndex`); cap is by depth count.
A reattach at cap 8 with more than 8 layers on a side trips I7 (not
quarantinable), so close BEFORE reattaching.

## 3. NZD ALT ARMS REATTACHED (operator decision)

Claude flagged: ALT calibration-only exception (ADR-150) never ruled; adds a
third pair on CAD and AUD, against the disjoint-ring direction (s5). Capacity
objection withdrawn once the book was ~148.

- **NZDCAD ALT (22260802)**: L0s placed 03:44:58Z (0.80002 / 0.80103).
  `TelemetryAPIKey` was wrong (RDP copy-paste broken): Experts showed
  `grind telemetry dropped status=401` on push and action. **The EA traded
  blind to pipshed until the key was re-entered after an RDP restart;** those
  two sends are missing from the archive. Live at c22 (03:50:54Z).
- **AUDNZD ALT (22260902)**: attached 03:52:47Z, L0s 1.24072 / 1.24213,
  telemetry fine first time. Heartbeat carries `api_counter_broken:false`.

## 4. OPEN ITEMS CREATED TONIGHT -- DO FIRST NEXT SESSION

  1. **Commit the cap to the presets.** `ea/presets/gbpusd_opt.set`,
     `gbpusd_alt.set`, `eurusd_opt.set`, `eurusd_alt.set`: `InpMaxLayers=12`
     -> `8`. Until then any preset reload silently restores 12. Presets only;
     no compile. (This is the "majors 12 -> 8" follow-on in practice; the I7
     migration trap is already passed for these four.)
  2. **Rotate `TelemetryAPIKey`** -- visible in full in two screenshots.
  3. **Confirm the daily-loss position in MetriX** and record the real limits.
  4. Pipshed status: pass through `api_counter_broken` (the EA emits it).
  5. Record realised P&L of the 20 closes from MT5 History.
  6. Still not exercised live: held-exit release after a front exit nets;
     hold-cancel race; QUARANTINE pairs on unwind.

## 5. DESIGN DIRECTION DISCUSSED -- NOT DECIDED, NEEDS DEEPSEEK + GEMINI

- **Keep OPT/ALT slot strings.** Renaming (e.g. CHOP/STRESS) breaks comment
  parsing and reconstruction on a non-flat book; labels only, or a migration
  ADR on a flat book.
- **OPT = uniform spacing, ALT = progressive widening** (e.g. 10, 16, 22...).
  Blocked today by the fatal `add == 2 x width` rule
  (`Grind_ValidateAddWidthRelationship`); needs a per-arm spacing mode, add
  target change, and the simulator change. ALT also differs in exit_pips, so
  a comparison confounds two variables unless exits are aligned.
- **Disjoint rings instead of a GV exposure cap:** each currency in exactly 2
  pairs x 2 arms. EUR/GBP/USD and AUD/CAD/CHF already comply; the NZD
  extension breaks it (CAD and AUD each in 3 pairs). Per-currency bound at
  P=8: 2 x 2 x 8 = 32 layers. Two rings at P=8 = 12 instances; one-sided worst
  case 12 x (8+2+1+1) = 144 vs 196. Open: retire NZD arms, or a third disjoint
  triangle. Tonight's reattach of NZD ALT arms goes the other way.
- **Operator target: never more than 16 layers per pair** (GBPUSD held 23 at
  03:14Z). Rings alone do not give it; P=8 per arm does.
- **300 s pacing gate on adds.** Evidence: FOMC minute fills 21 s (GBPUSD ALT
  L07->L08), 44 s (GBPUSD OPT L07->L08), 56 s (EURUSD OPT L03->L04); the rest
  of the grind was minutes-hours apart. One FOMC, motivating not proving.
  ARCHITECT s12 lists it as a concept; no code exists in fxgrind.
- **Leg cap (ADR-149)** exists with thresholds 0.0; operator: build, do not
  deploy -- likely superseded by rings.
- **Add clamp after long pause** (pre-ADR-151) re-adds at market immediately
  on reattach; seen on EURUSD ALT/OPT, GBPUSD OPT/ALT, AUDCHF OPT.

Line count: 127
