# HANDOFF 2026-09-11 -- ADR-127/128/129 LIVE; POSTGRES ARCHIVE (PIPSHED SIDE) LIVE

Supersedes `HANDOFF_2026-09-10.md` for current state. Keep 09-10 for the
Postgres design (5b), the second ring (9) and the MAE note (10); they still
stand, with the amendments in section 6 below.

Revised 2026-09-11 afternoon (Toronto). The first version of this file was
committed as `0e5dbf1`; git history holds it.

---

## 0. TIME CONVENTIONS -- READ FIRST

Three clocks appear in the evidence and they differ:

    MT5 / broker times      GMT+3 now, GMT+2 after FTMO's DST change
                            (open_time, open_time_msc, DEAL_TIME, scalps)
    VPS log files           UTC (Journal and Experts logs)
    pipshed generated_at    real UTC

`close_time` in scalp records carries a `Z` suffix but is BROKER time. So is
`open_time_msc`: it decodes as UTC but is broker time. Item 7 below.

Times in this document are labelled. Toronto = UTC-4.

---

## 1. STATE OF THE LIVE SYSTEM (2026-09-11 13:32Z)

- **main = `444812a`**, deployed to the VPS at 13:31:49Z (twelve
  `deinit reason=2` then twelve CONFIG lines, every magic on its own symbol).
  Suite **714/714**.
- Twelve fxgrind instances, cycle 2 unchanged (table in HANDOFF_2026-09-10).
  All `recon_ok`, `invariant_ok`, none halted.
- Book consistent at 13:32:15Z: 13 positions, every one with exactly one exit;
  every empty side with one L0; adds correctly labelled.
- Live code now includes ADR-127 (CloseBy dispatch), ADR-128 (invariant
  quarantine) and ADR-129 (stale-state duplicate L0 and wrong-layer exit).
- The recompiles at 13:09Z and 13:31Z zeroed every in-memory daily counter
  (item 16).
- `api_count` 1957 at 13:32Z, 2209 at 16:23Z; see item 21 (FTMO budget).
- Fleet book re-checked at 16:23Z: consistent, nothing halted; ADR-129 paths
  exercised live (an add fill got its exit in the same second; an L0
  re-quoted immediately after a scalp).

**Archive (pipshed), as of 16:59Z:**
- pipshed main `a34795d` deployed (web service): `/api/telemetry/action`,
  scalp tap on `/api/telemetry/scalp_closed`, public health route
  `/api/g/<token>/archive/<segment>`.
- Railway project pipshed now has: pipshed (web), Redis, Postgres (+volume,
  private only, no public access), archive-worker (`python archive_worker.py`,
  1 replica, no domain; REDIS_URL and DATABASE_URL as reference variables).
  The web service deliberately has NO DATABASE_URL.
- Migration `001_archive_phase1` applied to production (5 tables +
  schema_migrations); re-run reports "No pending migrations."
- Worker healthy at 16:59:54Z: queues 0/0/0, last_error null.
  `inserted_total` 0: no scalp has closed since the tap went live (~15:20Z);
  it is a per-process counter and resets on every redeploy.
- Postgres password rotated 16:55Z (the previous one had appeared in chat).

---

## 2. WHAT THIS SESSION DID

Night of 09-10 to 09-11:
1. Reviewed ARCHITECT.md against the repo. Decisions in section 6.
2. Found, test-confirmed, fixed and deployed the CloseBy dispatcher defect
   (ADR-127). Section 3a.
3. Cleared four phantom short layers (EURUSD_OPT, EURUSD_ALT, GBPUSD_ALT).
4. Established the duplicate-L0 mechanism from VPS logs (3c).
5. Found a latent cap defect and several telemetry defects (section 5).

Day of 09-11:
6. ADR-127 confirmed live and marked Accepted.
7. AUDCHF_ALT halted on I3; the fill-transition race was PROVEN from logs and
   the instance recovered (3d, section 8).
8. DeepSeek audit of the four candidate fixes P1-P4, verified against source
   (3e).
9. ADR-128 (P1, invariant quarantine) specified, Gemini-approved, tested
   (645/685 at stub commit, 685/685 at fix), merged and deployed.
10. AUDCHF_OPT halted on I5_SHORT_DUP: second live duplicate-L0 incident,
    recovered (3c, section 8). This is Spec B's evidence.
11. The DeepSeek courier pattern was documented and committed (section 6).
12. ADR-129 (P2 + P3 + P4, narrowed) specified, Gemini-approved with four
    rulings, tested (697/714 at stub commit, 714/714 at fix), merged and
    deployed at 13:31:49Z (3c).
13. Postgres archive table list consulted with Gemini (R1-R8 approved); pipshed
    side specified (ADR-130), built by Cursor, then verified by Claude against
    REAL Postgres 16 and Redis in a sandbox, which found three worker defects
    the fake-DB tests missed (every live scalp crashed the worker; a poison
    item crash-looped it; Postgres down made it exit). Fixed tests-first,
    re-verified on real servers (duplicates, startup outage, mid-run outage,
    poison items), plus removal of a test-only `_Stop` check Cursor had put in
    production code. Merged `a34795d`, deployed, migrated, worker live.
14. Railway housekeeping: DSA SSH key removed; Postgres password rotated.
15. EA side of the archive, Part 1 specified (ADR-131,
    `cursor_adr131_ea_archive_events.md`, 282 lines): pending Gemini.

---

## 3. ROOT CAUSES

### 3a. Phantom layers AND missing short scalp events -- FIXED, ACCEPTED (ADR-127)

One defect, two symptoms.

- `Grind_OnTradeTransactionEngine` offers every DEAL_ADD to the long handler,
  then the short handler.
- The OUT_BY branch of `Grind_HandleSideDealFill` called
  `Grind_MarkDealProcessed` BEFORE checking whether a layer on that side owned
  the deal. The processed set is shared by both sides.
- So the long pass consumed every short CloseBy deal: no `scalp_closed` event,
  and the short layer was never removed.
- Introduced in `1b9e255` (2026-09-07). V2 used per-side processed sets.

Fix `e486bb9`: mark processed only after a layer on this side matches. Tests
CB1-CB4 (614/628 before the fix as predicted, 628/628 after). Deployed 05:04Z.

Live confirmation after deploy: four SHORT scalps emitted with a real
`stack_depth` and no phantoms (EURGBP_OPT 09:00:57, EURGBP_ALT 09:15:24,
EURGBP_OPT 13:01:14, EURGBP_ALT 15:13:07 broker). Status Accepted in `d98804c`.

### 3b. GBPUSD_OPT LONG scalp at 16:42:14 broker (09-10) -- EXPLAINED

The instance was already halted by an I3 six seconds earlier; a halted EA
returns early from `OnTradeTransaction`.

### 3c. Duplicate L0 and wrong-layer exit -- CONFIRMED TWICE; FIXED BY ADR-129

Incident A, EURUSD_OPT, 2026-09-10 (UTC):

    14:25:17.708  EA sends modify on its long L0 #539505969
    14:25:45.174  short L0 #538969489 FILLS at 1.16231
    14:26:09.431  modify returns "done in 51722 ms"
    14:26:09.431  NEW short L0 sent at 1.16277 (same millisecond)
    14:26:34.920  exit for the 1.16231 layer sent
    14:31:57      recompile; stray adopted by reconstruction
    14:35:50.738  stray #539592770 FILLS at 1.16282
    14:35:50.739  exit sent at 1.16161 -- the FIRST layer's price
    14:36:58      I5_SHORT_DUP halt

Incident C, AUDCHF_OPT, 2026-09-11 (UTC):

    12:30:02.753  EA modifies short L0 #539601737 0.58501 -> 0.58422
    12:30:11.996  it FILLS at 0.58431
    12:30:14.255  NEW short L0 sent at 0.58501   (fill not yet processed)
    12:30:17      exit for the 0.58431 layer placed (fill processed only now)
    12:42:35.886  stray #540370366 FILLS at 0.58501
    12:42:35      second exit at 0.58381 -- the FIRST layer's price
                  I5_SHORT_DUP halt; new position naked

Defects (grind_engine.mqh / grind_recon.mqh at `ed5d9ff`):

1. **Stale state after a send.** Order sends block (item 12); fills arrive
   meanwhile and the tracker lags.
2. **`Grind_TryPlaceL0` treats "cannot select my order" as "order gone"** and
   places a new L0. It cannot tell filled from cancelled.
3. **The fill's transaction only clears the tracked pending if tickets match.**
4. **ENT-branch exit placement finds the layer by index** and
   `Grind_TryPlaceExitForLayer` overwrites the exit ticket unconditionally, so
   a duplicate index steals the first layer's exit.
5. **Reconstruction adopts a lone L0 pending without checking the side is
   empty.**

**ADR-129 (`4d8d4c0` tests, `444812a` fix, deployed 13:31:49Z):**
- P2: when a tracked L0 or add pending cannot be selected, check for a
  position with the same ticket (an MT5 position's ticket equals its opening
  order's ticket; confirmed on three live positions). If it exists, the order
  filled: keep the ticket and place nothing until the transaction arrives.
  Otherwise clear and proceed as before.
- P3: the ENT branch finds the new layer by position ticket, and
  `Grind_TryPlaceExitForLayer` refuses to overwrite an existing exit.
- P4: after placing the fill's exit, an L0 fill cancels any different tracked
  L0 pending; `Grind_ReconcileStrayL0` also runs each tick at the top of the
  engine and cancels an L0 pending on a side that holds layers.
- Remaining window: order gone and position not yet visible still re-quotes as
  before; P4 then cancels the stray. A stray that fills before the cancel gets
  its own exit and halts on I5 (non-quarantinable) with both positions covered.
- Accept after the first live `WARN GRIND_STRAY_L0` line with no duplicate
  following it.

### 3d. Missing exits (I3_*_NAKED) -- PROVEN; MITIGATED BY ADR-128

Proof, AUDCHF_ALT 2026-09-11 (UTC):

    10:35:40.980  CRITICAL invariant fail I3_LONG_NAKED, instance halts
    10:35:41.071  Journal logs the long L00 exit fill (deal #517766886)

The invariant fired 91 ms BEFORE the terminal logged the fill that covered it.
`Grind_CheckBookInvariants` rebuilds from broker positions then orders (not
atomic) on every tick; a fill in progress looks naked. A halted EA then ignores
all transactions, so the exit fill queued no CloseBy and a later short fill
got no exit. Four 09-10 I3 halts share the signature.

The invariant only checks the broker book's self-consistency; it never
compares against the EA's tracker. That is why phantom layers kept
`invariant_ok: true`.

**ADR-128 (`36398c6`, deployed 13:09Z):** I2_*_EXIT_DUP, I3_*_NAKED and
I4_*_ORPHAN_EXIT enter QUARANTINE instead of halting: engine frozen for the
whole instance, trade transactions and CloseBy still processed, exit retry
still runs. A passing check releases immediately. Halt only if the failure
persists >= 3000 ms (GetTickCount64) AND >= 3 consecutive checks. All other
invariants halt immediately. I3 proven live; I2 and I4 derived from source.
Status Proposed until a live quarantine release is seen.

Exit retry during quarantine is safe because it keys on the EA's own record,
not because it cures transients: in the proven case the layer still holds its
exit-order ticket, so retry does not fire. 3000 ms is a MINIMUM duration, not
a maximum.

### 3e. DeepSeek audit of P1-P4 -- VERIFIED BY CLAUDE

`prompts/deepseek_grind_fill_race_stale_state.md` and `_response.md`.
Premise upheld; all findings fixable. Claude's verification against source:

Accepted:
- P1: keep exit retry in quarantine; halt on persistence of time AND checks.
- P2: bound the "not yet in history" wait; fall back to checking for a
  matching entry position, else one side can stall.
- P3: after a P3 protective halt, reattach will halt again on I5 -- the
  operator must close the duplicate first.
- P4: keep "no L0 pending on a side with layers" (engine and reconstruction);
  DROP "cancel add pending on an empty side" -- `Grind_EnsureAddNext` already
  cancels those via its stale-label path.

Rejected:
- DeepSeek called P4 unsafe; it is redundant, not unsafe.
- DeepSeek called the 1.16231 fill "price improvement". Backwards: a sell
  limit at 1.16244 must fill at or above it. Item 13 stays open.
- DeepSeek treated I8 as a reconciler comparing tracker with broker. I8 checks
  the state rebuilt from broker tickets (`grind_recon.mqh` 731-738).

Deviation ruled by Gemini: whole-instance freeze instead of DeepSeek's
per-side freeze.

ADR-129 narrowings, all ruled by Gemini: (1) the position-ticket check
replaces the order-history lookup and bounded wait; (2) no new protective
halt, the existing I5 invariant halts once both duplicates are covered;
(3) reconstruction unchanged, the engine reconcile cancels an adopted stray on
its first tick; (4) no empty-side add rule, `Grind_EnsureAddNext` already does
it. Gemini described position state as synchronous; it is not quite, which is
why P4 covers the remaining window.

---

## 4. WORK QUEUE (agreed order)

1. ~~ADR-127 CloseBy dispatcher~~ DONE, Accepted.
2. ~~ADR-128 invariant quarantine~~ DEPLOYED. Accept after the first live
   `INFO GRIND_QUARANTINE release` line in the Experts log.
3. ~~ADR-129 stale-state duplicates~~ DEPLOYED. Accept per 3c.
4. ~~Postgres archive, pipshed side (ADR-130)~~ LIVE (section 1).
5. **EA archive events, Part 1 (ADR-131)** -- send_logs, fill_logs (recorded
   before the halted return), config_events INIT/DEINIT with EA_BUILD,
   CRITICAL ea_events, retain-until-success queue (5000, drop marker), 2 s
   flush of up to 200, EA clock; timer change also fixes item 14. Spec
   written, 59 new assertions predicted 721/773 at stubs and 773/773 at fix.
   NEXT: Gemini review.
6. **EA archive events, Part 2** -- QUARANTINE / STRAY_L0 / CLOSEBY / RECON
   markers as ea_events.
7. **Items 12 and 13** -- with the send and fill logs.
8. Small: pipshed status to surface `quarantined`, `quarantine_reason`,
   `quarantine_episodes` (item 20).

Parked: 8, 14 (fixed by ADR-131 when it ships), 15, 16, 17, 18, 19, 22-26.

---

## 5. OPEN ITEMS

**7. Timestamps.** `close_time` is broker time labelled `Z`. Decision: broker
time stays source of truth; the EA drops the `Z`; Postgres stores `*_broker`
plain `timestamp` columns plus `received_at timestamptz`. Ships with the
Postgres action endpoint.

**8. Cap would block every entry if enabled.** `grind_cap.mqh`: instances
publish exposure keys only for their own two legs; the reader sums a leg across
six hardcoded magics and treats an absent key as MAXED, so it blocks.
`GRIND_CAP_ALL_MAGICS` omits the AUD/CAD/CHF magics. Tests T29/M3 pre-seed all
keys, masking it. Inert (thresholds 0.0). Gate: thresholds stay 0 until fixed;
a failing ALLOW test in a realistic topology is the prerequisite.

**12. Send latency.** Journal "done in" times on 09-10, 14:16-14:36 UTC: 9.9 s
to 51.7 s across 13 sends. On 09-11 12:30 UTC one placement took 2.4 s.
Enabling condition for 3c and 3d.

**13. Fills away from the limit price.** 09-10: sell limit #538969489 modified
to 1.16244, filled at 1.16231 (1.3 pips WORSE). 09-11: sell limit #539601737
modified to 0.58422, filled at 0.58431 (0.9 pips BETTER). Not systematically
adverse, but a limit should never fill worse. Needs the fill log.

**14. Statics survive a parameter reinit.** `OnTimer`'s 1 s -> 60 s switch sits
behind a `static` flag; a reason-5 reinit keeps it, so the heartbeat stays at
1 s. Workaround: remove and reattach, or recompile.

**15. API counter.** Counts sends rejected locally (Algo off).
`GRIND_DAILY_API_LIMIT` (2000) is defined but never enforced; warning at 1800
only. FTMO's rule is more than 2,000 server requests/day.

**16. Daily counters are in-memory only.** Every recompile or reattach zeroes
`scalps` and `realised_pnl_today`; `scalps` also survives the day boundary.

**17. Scalp P&L read from one OUT_BY deal.** Recorded in ADR-127.

**18. Quarantine flag after a quarantine halt.** `g_grind_quarantined` stays
true when quarantine escalates to a halt; heartbeat shows both flags true.
Cosmetic.

**19. ADR-128 file contains a few non-ASCII dashes.** Cosmetic.

**20. Pipshed does not surface the quarantine heartbeat fields.** The EA sends
them; `/status/` projects only known fields.

**22. Three pipshed verify scripts fail on main (pre-existing).**
verify_grind_exposure, verify_fxgrind_panel, verify_grind_pnl_render fail
identically at 8549680 and a34795d. Verify drift, not archive-related.

**23. Archive worker `last_error` is sticky.** It is never cleared after later
successes, so it can show a stale error (it showed "relation send_logs does
not exist" long after the migration until a redeploy reset the process).
Clear on success or add a timestamp.

**24. EA scalp_closed queue drops events on POST failure.**
`Grind_DrainScalpEventQueue` removes each event before posting and never
re-queues, so a pipshed/Redis outage loses scalp events (recoverable by the
backfill script). The ADR-131 action queue does not have this flaw; the scalp
queue should be moved onto it or fixed the same way.

**25. Untracked debug files in D:\pipshed** (aggregate.json, live_status.json,
public2.json, response_body.json, commit_msg_ab_view.txt): never commit them
(public repo); delete or gitignore.

**26. `railway connect Postgres --tunnel-only` does not forward traffic** on
this setup ("channel 1: open failed ... port forwarding rides your dev.new
session"). Use `railway ssh --service <svc>` instead (section 8).

**21. FTMO request budget, 2026-09-11.** `api_count` 1956 at 13:30Z includes
about 1,120 locally rejected retries from the Algo-off episode (04:30Z), so
real server requests were about 840. Rate since London open: about 45/h in
the morning, about 250/h from 12:08Z to 13:30Z (two recoveries plus New York
open). Projection to broker midnight (21:00Z): 1,600-2,700 real requests
against FTMO's 2,000. For this broker day only: real is roughly
`api_count` - 1,120. Not verified that rejected retries never reached the
server. Structural fix belongs with item 15 (count only sends that reach the
server) and item 12.

---

## 6. DECISIONS AND PROCESS (for the ARCHITECT.md rewrite -- not yet written)

- **Master copy:** `docs/architecture/ARCHITECT.md` in the repo. The desktop
  copy is to be deleted.
- **V2 material** moves to `docs/architecture/ARCHITECT_V2_HISTORY.md`.
  ARCHITECT.md keeps "V2 carry-forward candidates": the 300 s pacing gate and
  the ADR-114 layer-anchor ratchet. The AddPipsFloor slippage lesson becomes a
  general rule.
- **DeepSeek** is mandatory for geometry, statistics, sim/calibration
  methodology, and state-machine logic of existing mechanisms; optional for
  telemetry, dashboard and deploy plumbing. Use it more. Every brief carries
  the fixed market-maker frame block.
- **DeepSeek courier pattern** (documented in
  `docs/deepseek_prompts_templates/`: HOWTO plus worked example): Claude writes
  the BRIEF and a COURIER wrapper; the operator saves the brief to `prompts/`,
  sets Cursor to DeepSeek R1 and pastes the courier; the courier integrity-gates
  line 1, a mid-file anchor, the last line and the total count, hard-stops on
  any mismatch, writes `prompts/<name>_response.md`, and never touches git. The
  operator commits the pair; Claude reads both from GitHub and verifies every
  load-bearing claim against source before use.
- **Gemini** also refines prompts and consults on design; his reasoning is
  checked too (on ADR-128 he said exit retry clears transients and 3 s is a
  maximum -- both corrected).
- **Standard review loop:** Claude drafts; Gemini reviews; Claude verifies each
  amendment and states accepted/rejected with reasons; Claude reissues the full
  prompt with a fresh count; operator sends the final version to Cursor.
- **Fix verification pattern:** tests first, then the fix; operator runs the
  suite at both commits with an exact predicted SUMMARY. When the tests need
  functions that do not exist yet, commit 1 adds unwired stubs so the tests
  compile and must fail; any test passing against the stubs stops the job.
- **Machines:** Surface is Python only, no MT5, `C:\fxmatrix`, data over RDP
  (`S:\` as backup); Cursor has no write access, deliberately. All Strategy
  Tester work runs on the desktop. The VPS terminal trades only.
- **Timestamps:** as item 7. **Cap gate:** as item 8.
- **Deploy:** for an fxgrind recompile on a consistent book, Algo off is
  optional (operator practice: `deploy.ps1`, then compile); take a status read
  before and after.
- Add the handover's standing disciplines missing from ARCHITECT: ASCII-only
  prompts, independently derived expected values, branch/push/no merge,
  invariant vs reconciler, live over sim, never-run paths, verify Gemini and
  DeepSeek, never commit CSVs, explicit User-Agent for pipshed, pipshed's two
  gates, GlobalVariables are terminal-scoped.

---

## 7. CORRECTIONS TO HANDOVER_NEW_CHAT_2026-09-11

- ARCHITECT.md IS in the repo (`docs/architecture/`).
- The DeepSeek commission and courier templates exist (Template C in
  `prompts/TEMPLATE_cursor_deepseek_handoff.md`; courier HOWTO in
  `docs/deepseek_prompts_templates/`).
- DeepSeek responses live in `prompts/<name>_response.md`, not `adrs/`.
- Strategy Tester data export runs on the desktop, not the Surface.
- The invariant runs every tick, not on the heartbeat.
- The missing scalp events WERE side-based: 13 of 14 short.
- CandleLab is public; its migrations are a standalone `db_migrate.py`,
  consistent with No Startup DDL. The bounce feature is not yet inspected.

---

## 8. OPERATIONAL PLAYBOOKS AND TRAPS

**Before any recovery:** take a fresh status read; grep the VPS logs by PRICE
(log times are UTC) to capture the timeline. Halt state is evidence.

**I3 halt (naked position):**
1. Close the genuinely naked position by hand (the one with no exit and no
   tracker layer).
2. Remove and reattach the instance with its preset (not a parameter reinit,
   item 14).
3. Reconstruction derives CloseBy pairs for any layer whose exit already filled
   (`Grind_DeriveCloseByQueueFromBook`). Verified live on AUDCHF_ALT:
   `INFO GRIND_CLOSEBY success retry=1` at 12:14:08 UTC.
4. Status read. The book snapshot in `/status/` is sent every 60 s and can lag
   the EA by up to a minute.

**I5 duplicate-L0 halt:**
Before ADR-129 (as on AUDCHF_OPT, recovered 12:59Z): the later position had no
exit and a second exit sat at the first layer's price.
1. Close the uncovered duplicate position (the later ticket).
2. Delete the duplicate exit order -- the LATER one; keep the original.
3. Remove and reattach.
After ADR-129 each duplicate has its own exit at its own price:
1. Close the later duplicate position.
2. Delete the exit order placed at that position's fill time (price = its
   entry minus/plus exit distance); keep the first layer's exit.
3. Remove and reattach.

**Phantom layers** (should not recur after ADR-127): signature is layers with
`has_exit_position: true` while `book.positions` is empty. Procedure: status
read; Algo off; delete adds anchored on phantoms; reinit; status read; Algo on.

Traps:
- Deleting a stray add with Algo on while a phantom remains is futile:
  `Grind_EnsureAddNext` re-places it next tick.
- Pressing OK in EA properties without a change does NOT reinit; change a
  harmless input (e.g. `InpConfigWarning`) for `deinit reason=5`.
- Repo presets have a blank `TelemetryAPIKey`; if a dialog shows it blank the
  instance goes dark. Do not share screenshots of the properties dialog.
- A scalp recorded after a halt-and-recover carries the CloseBy time, not the
  exit fill time.
- Pipshed fetches: paste URLs on their own line or in a code block (the chat
  converts them into links whose target can differ); retry a failed fetch once
  before diagnosing.
- Cursor reports the prompt's line count as its own and abbreviates diffs
  despite instructions (four times, including once after an explicit ban).
  Always verify the branch itself. It also once put a test-only exception
  check into production code to make its tests stop; reject that pattern
  (a test stop-exception should derive from BaseException instead).

**Railway access (desktop):** Railway CLI 5.52.1 (npm), logged in, D:\pipshed
linked to project pipshed / production. Registered SSH key: ed25519
`SHA256:UYUOgIwB...` from `~\.ssh\id_ed25519` (the DSA key was rejected by
Railway and has been removed). Always pass `-i "$HOME\.ssh\id_ed25519"`.
- Run a command in a service: `railway ssh --service archive-worker -i
  "$HOME\.ssh\id_ed25519" python db_migrate.py`.
- Shell in Postgres: `railway ssh --service Postgres -i ...`, then
  `psql -h /var/run/postgresql -U postgres` (local socket, no password).
- Typing a shell command at the `railway=#` psql prompt leaves a half-typed
  query (`railway-#`); clear with `\r`, leave with `\q`.

**Archive migrations:** run `python db_migrate.py` INSIDE archive-worker via
`railway ssh` (private network; the database never needs public access). A
worker started before its tables exist is harmless: it takes nothing from the
queue and recovers on its own once the migration runs (verified).

**Postgres password rotation (verified procedure):**
1. New password: `[guid]::NewGuid().ToString("N") + [guid]::NewGuid().ToString("N")`
   (letters/digits only, URL-safe). Never paste it into chat.
2. Railway -> Postgres -> Variables: set POSTGRES_PASSWORD (PGPASSWORD and
   DATABASE_URL follow by reference); Deploy; wait Active. Changing the
   variable alone does NOT change the role inside the database.
3. `railway ssh --service Postgres -i ...`; `psql -h /var/run/postgresql -U postgres`;
   `\password postgres` (paste twice); `\q`.
4. Still in the container: `psql -U postgres -c "select 1"` must return 1
   (proves variable and role agree over the network).
5. REDEPLOY every service that references DATABASE_URL (archive-worker).
   Running containers keep their old environment -- this step was missed
   twice and produced "password authentication failed" both times.
6. Verify: `railway ssh --service archive-worker -i ... python db_migrate.py`
   -> "No pending migrations."

**Verification discipline, extended:** for pipshed/Python work, Claude runs
the verify scripts itself and repeats the critical paths against real
Postgres and Redis installed in its sandbox. Fake-DB tests alone passed three
crash defects.

---

## 9. NEXT SESSION

0. Send `cursor_adr131_ea_archive_events.md` to Gemini; then Cursor; then
   Claude verifies the branch and predicted SUMMARYs (721/773, 773/773).
1. Grep the Experts log for the new markers:
   `Select-String -Path "$t\MQL5\Logs\<date>.log" -Pattern "GRIND_STRAY_L0|GRIND_QUARANTINE"`.
   - First `INFO GRIND_QUARANTINE release`: mark ADR-128 Accepted.
   - First `WARN GRIND_STRAY_L0 cancel on fill` or `WARN GRIND_STRAY_L0 cancel`
     with no I5 duplicate following: mark ADR-129 Accepted.
   - Any `CRITICAL GRIND_QUARANTINE halt` or I5 halt deserves a full look.
2. Check the FTMO request budget (item 21).
3. Confirm `"quarantined":false` appears in a TELEM heartbeat line.
4. ARCHITECT.md rewrite and ARCHITECT_V2_HISTORY.md, through the Gemini loop.
5. Check `/api/g/<token>/archive/<segment>` after the first scalp: expect
   `inserted_total` >= 1 and queues at 0.
6. Commit `handoffs/HANDOVER_NEW_CHAT_2026-09-11.md` (currently untracked).
7. New `HANDOVER_NEW_CHAT_<date>.md` pointing here.

---

**Revised:** 2026-09-11, about 13:05 Toronto.
**Repos:** theonlykk/fxmatrix (main `24ac850`), theonlykk/pipshed (main `a34795d`).
**Suite:** 714/714 MQL5; pipshed archive verify scripts 4/4.
