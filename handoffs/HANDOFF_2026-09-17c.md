This message has a line count at the bottom

# HANDOFF 2026-09-17c -- CARRY: PRINCIPLE RESTATED, LEDGER CONFIRMS X3, PHASE A LEAK

Continues `HANDOFF_2026-09-17b.md`. Short, late-night record so nothing is
re-derived tomorrow. No code or preset changed.

---

## 1. THE PRINCIPLE IS SETTLED -- DO NOT REOPEN IT

ADR-135b (Context item 3, "Economics"): exit limits are fixed at fill time
while swap accrues on the position, so a 5-pip exit on a layer that paid 1.5
pips nets 3.5. **Exits MUST move by accrued carry to preserve the financial
terms of the trade.** It is not a bet on whether price reaches the moved
target; leaving the exit where it is silently changes what the trade earns.
ARCHITECT s1: carry rules apply to basis-anchored orders (exits) only; adds and
L0s never move for carry.

The parameters were chosen with carry priced in: rollover carry was wired into
the simulator and sweep aggregation at `725391f` ("Wire rollover carry into
simulator and sweep aggregation"), and `fleet_carry_v2_2026_09_13` (234 cells)
was the first sweep to price financing (HANDOFF_2026-09-13 s6/6a). Harvest and
exit geometry therefore ASSUME exits that recover carry.

**Claude error, 2026-09-17 ~04:30Z:** framed shifting as optional ("shift
exits vs don't shift vs carry-aware geometry", "fill less often"), re-opening a
decided principle. Recorded in TRAPS.

## 2. WHAT IS TRUE RIGHT NOW -- A KNOWN ECONOMIC LEAK

ADR-151 Phase A requires `InpEnableCarryPass=false` (fxgrind.mq5 ~96, OnInit
FATAL otherwise); all 18 presets are false. So since the ADR-151 deploy
(2026-09-17 02:15:57Z) **no exit recovers carry**: every layer held across a
rollover exits on changed financial terms, and the live fleet no longer matches
the geometry the carry-priced sweep selected.

**Claude error, same night:** told Gemini "carry is off, so there is no risk in
waiting" for Phase B, and Gemini approved deferring Phase B on that basis.
That premise is wrong: carry off is a continuous P&L leak and a divergence from
the calibrated model. **Phase B priority must be re-decided with this
correction, and Gemini told.**

Scale at the 2026-09-17 ~03:20Z summary: financing accrued -$19.56 against
cycle scalp income +$235.96 net.

## 3. NEW LEDGER CONFIRMATION (MT5 History, account 1514582088)

Twenty 0.01-lot LONG layers closed manually 2026-09-17 ~06:22-06:38 broker
(HANDOFF_17b s2), extracted by Cursor from
`D:\fxmatrix\temp\ReportHistory-1514582088.xlsx`. All 20 found; Positions swap
equals sum of Deals swap per ticket; entry deals carry 0.

| Group (open day, broker) | Swap each | Ratio to MON |
|---|---|---|
| GBPUSD OPT/ALT MON (5 charged days if x3 on Wed->Thu) | -0.25 | 1.00 |
| GBPUSD OPT/ALT TUE | -0.21 | 0.84 |
| GBPUSD OPT/ALT WED | -0.16 | 0.64 |
| EURUSD OPT/ALT MON | -0.44 | 1.00 |
| EURUSD OPT/ALT WED | -0.26 | 0.59 |

Predicted ratios: x3 on the Wed->Thu broker midnight 0.80 (TUE) / 0.60 (WED);
x3 on Tue->Wed 0.80 / 0.20; x3 on Thu->Fri 0.67 / 0.33. **Confirms x3 on the
Wed->Thu roll**, independently agreeing with the 135a snapshot finding of
2026-09-16 (ADR-135b Context). Per charged day, 0.01 lot long: GBPUSD
~ -$0.05, EURUSD ~ -$0.09 (~0.5 and ~0.9 pip). A week held (7 charged days):
~3.5 and ~6.2 pips -- the size of one exit.

Not yet cross-checked: the live `SYMBOL_SWAP_ROLLOVER3DAYS` value in a
`CARRY_SNAPSHOT` event (code applies x3 when the pass weekday equals it).

## 4. FOR TOMORROW

  1. Correct the record with Gemini (s2): Phase B deferral was approved on a
     false premise.
  2. Re-decide Phase B sequencing: carry-aware targets from the swap ledger at
     release; reconstruction of carry-shifted held targets; promotion deadband
     (Gemini Q1: 10). Design already sketched in
     `MEMO_2026-09-16_order_purgatory.md` s4.1, s4.5, s4.6.
  3. Tension to resolve, not assume: Phase B builds on Phase A paths (held-exit
     release, hold-cancel race) that have not yet run live.
  4. Cross-check `SYMBOL_SWAP_ROLLOVER3DAYS` in a `CARRY_SNAPSHOT`.

Line count: 84
