This message has a line count at the bottom

# HANDOFF 2026-09-21 -- WEDNESDAY READY; ROLLS MEASURED; EJECTION AND BREAKER DESIGNED

Continues `HANDOFF_2026-09-20.md`. Covers Sunday night (21:52 ET) through
Monday afternoon. Times UTC unless marked broker (UTC+3).

---

## 0. STATE (Monday ~19:45Z)

| | |
|---|---|
| fxmatrix main | `9f43bbd` |
| pipshed main | `5153977` (ladder in price order, deployed to Railway) |
| VPS running | `5454358`, DETACHED HEAD, 16 instances, cycle-2 presets. **Nothing new deployed** |
| Account | demo 1514582088, $10k. MetriX: daily limit $500, **worst day -$420 (FOMC night)**, ~$217 of today's limit used by mid-session, $877 above the $9,000 max-loss floor |
| Book | MTM about -$240, concentrated in old one-sided stacks (AUDCAD, GBPUSD FOMC longs, CADCHF) |
| Suite | `main` **1387/1387**, verified on GBPUSD and AUDCAD charts |
| **Cycle 3** | **Wednesday 2026-09-23, new account, flat book.** Runbook ready |

---

## 1. DONE TODAY

**ADR-153 merged** (`01b71d8`): add/width lock gone, stranded is a free
preset parameter, the recentre stops at the 1,800 API soft-warn.

**Suite fixed** (`29df88f`): IV5/EF3 failed for a week because twelve
tests place exits on the 1001 fixture WITHOUT seeding the market, so the
LIVE chart price decided whether an exit clamped, and a clamp left a
shift + release flag for 1001 that nobody deleted. Shared test reset now
clears carry state. (Last night's "live swap rates" diagnosis was wrong.)

**Cycle 3 defined** (`bd76f14`):
- `docs/architecture/geometry-cycle3.md` -- the pre-registration: 9 pairs,
  ONE arm each (OPT magic), cap 8, lots 0.01; metric = scalps/day divided
  by the fleet median, band 0.7-1.3; staggered mid-week changes (GBPUSD
  fixed; group A Friday, group B Tuesday); add/width/stranded/deadband may
  change mid-cycle, **exit and cap may not**.
- `docs/architecture/ADR-154-nzdchf-reinstated.md` -- NZDCHF back at width 3.
- Nine `ea/presets/*_opt.set` (add/exit/width/stranded): GBPUSD 10/10/5/10,
  EURUSD 8/10/7/14, EURGBP 4/5/3/8, AUDCAD 7/10/5/10, AUDCHF 6/10/5/10,
  CADCHF 6/10/5/10, NZDCAD 10/10/5/10, AUDNZD 10/10/7/14, NZDCHF 6/10/3/8.
- **Lots 0.01 everywhere**: the EA assumes full fills (C14); 0.01 is the
  broker minimum so no partial is possible.

**Wednesday runbook** (`a5867b1`, `d312840`):
`docs/runbooks/account-switch-2026-09-23.md` -- close out, switch, restart
terminal (clears temporary GlobalVariables), run
`scripts/grind_gv_clean.mq5` (merged `18aeab5`; deletes the ten persistent
prefixes; aborts if a reporter heartbeat exists), deploy, attach nine,
first-hour checks including F1's first live barbell.

**Manual rolls** (`docs/runbooks/manual-roll.md`, `roll-log.md`): four on
AUDCAD, deepest short closed at market, engine re-quotes the next add near
market. Two within the trigger (all-in -$7.23 OPT, -$7.42 ALT), two
consecutive ones OUTSIDE it (-$6.33, -$6.32 price). Both sides re-capped
within hours -- AUD in a macro uptrend (RBA hike bets vs BoC on hold).
Operator view: closing was right -- the loss was already in MTM, the
layers could not earn, and they blocked the slots that do.

**ADR-155 commanded passive ejection** (`fb0d252`, Proposed rev 3,
Gemini + DeepSeek reviewed): a script writes `GRIND_EJECT_<magic>`; the EA
moves the DEEPEST layer's exit to the best passive price at market (never
a market order), stored in `GRIND_EJECT_OFFSET_<pos>`, read by the exit
formula AND I6. DeepSeek found the carry pass bypasses the formula --
ejected layers must skip it. Full table of seven touch points in the ADR.

**pipshed**: `--l0churn` view (`36a4ade`); layer table in price order
with a Gap column and pending adds/L0s as rows (`5153977`).

---

## 2. WEDNESDAY

Follow `docs/runbooks/account-switch-2026-09-23.md` top to bottom. Traps
it exists for:
- presets in `MQL5\Presets` must match what is attached, or a reattach
  halts on I6 (AUDCAD ALT did, twice today, on exit 7 vs 10)
- **read every input back before OK** -- loading a preset silently resets
  typed values
- never close a position under a running EA (quarantine, then halt)
- the suite runs from `MQL5\Scripts\`; `desktop_sync.ps1` is at repo ROOT
  and does not copy the repo's `scripts\` folder

**Watch in week one:** RBA decision 29 Sep (three AUD pairs in cycle 3);
MetriX daily headroom -- nothing in the EA enforces it yet.

---

## 3. AFTER WEDNESDAY, IN ORDER

1. **C17 account daily-loss circuit breaker.** Portfolio-level ONLY
   (operator: no per-instance budgets -- the pairs exist to diversify).
   At ~80% of the FTMO daily limit, every instance stops NEW entries;
   exits keep working; reset at midnight CE(S)T. `GRIND_MAE_DAILY_LOSS_FRAC`
   today only REPORTS. **First confirm how FTMO computes "today's
   permitted loss"** (start-of-day balance vs equity, higher of).
2. **C15 commanded ejection** -- Cursor spec from ADR-155 rev 3.
3. **C16 retrace study**; week-one measurements C3 (exit), C10 (carry cost).
4. Roll log 24-hour follow-ups for the four rolls.

**FTMO rules that shape the roadmap** (in `07_ROADMAP.md`): 2,000
requests/day, 200 pending; **never run the same pair on two accounts**
(opposite positions across linked accounts are forbidden); same strategy
across own accounts capped at $400k; a funded account would need to be
Swing (news windows).

---

## 4. OPEN ITEMS FOR THE OPERATOR

- Commit this handoff and the BOOT update.
- Roll log: close prices and swap for tickets 544092579 and 542332509.
- Tomorrow: the rolls' 24-hour columns.

## 5. LEARNED

In `02_TRAPS.md`: check the suite TOTAL; Cursor's self-reported hashes and
line counts are wrong after an amend; run `main` before blaming a branch;
a passing test may pass for the wrong reason; an ADR inequality is not
automatically a safety rule; name the TYPE of direction arguments; a
restart clears only TEMPORARY GlobalVariables, and each terminal has its
own store. Cursor repeatedly leaves the repo on its branch -- check
`git branch --show-current` before every commit.

Line count: 128
