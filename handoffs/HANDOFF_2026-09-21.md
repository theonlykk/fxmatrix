This message has a line count at the bottom

# HANDOFF 2026-09-21 -- ADR-153 MERGED; FLEET RESHAPED TO 9 PAIRS x 1 ARM

Continues `HANDOFF_2026-09-20.md`. Session ran Sunday 21:52 ET to Monday
~01:15 ET. All times UTC unless marked broker (UTC+3).

---

## 0. STATE

| | |
|---|---|
| fxmatrix main | `10af8da` |
| pipshed main | `36a4ade` (`--l0churn` view merged) |
| VPS running | `5454358`, DETACHED HEAD, unchanged. **ADR-153 is merged but NOT deployed** |
| Fleet 05:10Z | 16 live, 0 halted, all `recon_ok` / `invariant_ok` |
| Book | 104 positions, 60 orders, 30 resting entries. **Guard 194** |
| Today | 19 scalps, 9.95 USD realised. MTM -207 (was -186 at 23:13Z) |
| Capped | **AUDCAD OPT and ALT both short 8/8** -- AUD rallied vs CAD overnight |
| API | 228 of 2,000 (shared counter) |
| Suite | `main` 1377/1380 after merge; the 3 failures are pre-existing (backlog C12) |

Stale-offset fix: every rotation since the open priced exactly on formula;
fleet-wide penetration 0.0, reverts 0.

---

## 1. ADR-153 -- DONE

`docs/architecture/ADR-153-geometry-independence.md`, **Accepted**, merged
`01b71d8`. Rev 3:

1. `add == 2 x width` is gone. OnInit keeps a typo guard only:
   `0.5 <= add/width <= 4.0`.
2. **`InpStrandedThreshPips` is a free design parameter.** No floor. The
   rescue design (`2 x width`) and a live-quoting design (`stranded ~
   width`, cadence set by deadband) are both presets.
3. Fatal checks are sanity only: every geometry input > 0, deadband >= 0.
4. **`Grind_TryRecenterOppositeL0` stops at the 1,800 soft-warn level**;
   entries and adds keep 1,900 (Gemini: re-quotes earn nothing).

Also fixed on the way: the recentre read its price with raw
`OrderGetDouble`, so `Test_PO4_...` passed for the wrong reason; now via
`Grind_OrderGetPriceOpen`, with a negative-twin test (PO4b).

Path: rev 1 -> Gemini -> DeepSeek (10 findings, "do not implement") ->
rev 2 -> rev 3 (floor dropped as a preference, API gate added) ->
tests-first implementation -> DeepSeek on the branch -> follow-up (`add >
exit` test, G6/G7) -> merge.

**Evidence that drove it** (`pipshed scripts/archive_counts.py
--l0churn`): EURGBP ALT made **139 L0 re-quotes in 7h36m** on 13/14-Sep,
48% of its API requests over nine days. The re-quote threshold on mid
drift is `max(deadband, stranded - width)`; the churn is mostly time spent
one-sided, not geometry.

---

## 2. FLEET DECISION FOR WEDNESDAY (A3)

**Nine pairs, ONE arm each, cap 8.** GBPUSD, EURUSD, EURGBP, plus the
complete AUD/CAD/CHF/NZD block: AUDCAD, AUDCHF, AUDNZD, CADCHF, NZDCAD,
NZDCHF. Each of AUD/CAD/CHF/NZD appears 3 times, USD/EUR/GBP twice; CHF
instances fall from 4 to 3.

Modelled from the 10-18 Sep fills (depth replayed per side, scaled by the
add change): **peak ~144 slots, median ~112**. For contrast, 16 instances
on the cycle-3 geometry peaked at 251.

Why one arm: the objective (even pips per pair) is cross-sectional;
volume is high-count; a staggered mid-week schedule (half the pairs
changed one day, half another) gives each pair a same-week control.
**Exit and cap are fixed for the cycle** (I6/I7 halt on change); add,
width, stranded and deadband are reconstruction-safe mid-week dials.

**Target: even pips per pair.** Operator accepts that AUD/CAD/CHF/NZD see
more aggregate activity; adjust toward two-thirds only if they run heavy.

Starting geometry (add / exit / width / stranded):

| pair | setting |
|---|---|
| GBPUSD | 10 / 10 / 5 / 10 |
| EURUSD | 8 / 10 / 7 / 14 |
| EURGBP | 4 / **?** / 3 / 8 |
| AUDCAD | 7 / 10 / 5 / 10 |
| AUDCHF | 6 / 10 / 5 / 10 |
| CADCHF | 6 / 10 / 5 / 10 |
| NZDCAD | 10 / 10 / 5 / 10 |
| AUDNZD | 10 / 10 / 7 / 14 |
| NZDCHF | 6 / 10 / 3 / 8 |

---

## 3. NEXT SESSION, IN ORDER

1. **Operator: EURGBP's exit** -- 5 (fills), 8 (middle), 11 (replay).
2. **Operator: lots (A2)** -- 0.01 everywhere, or 0.02 on the low
   pip-value pairs (AUDCAD, NZDCAD, AUDNZD) per Gemini's dollars ruling.
3. **Cycle-3 revision doc**: the table above, the pre-registered metric
   (scalps/day as a ratio to the fleet median, target band), the stagger
   schedule, and what a mid-week change may and may not touch.
4. **ADR-154: NZDCHF.** ADR-146 rejected a width-7 geometry on a
   simulator tail gate that the live CHF pairs also fail; width 3 survives
   60-74%. Supersede its D1.
5. **Presets** for the nine, then A5 (account id in telemetry), A7 (GV
   clean-up list), A9 (deploy sequence).
6. **Watch AUDCAD**: both arms capped short, 16 layers.

---

## 4. LEARNED

In `02_TRAPS.md` (ADR-153 section): check the suite TOTAL, not just the
FAIL rows (a stale compile of `main` gave identical FAIL rows); Cursor's
self-reported hashes and line counts are wrong after an amend; run `main`
before blaming a branch; write the negative twin of a passing test; an
inequality in an ADR is not automatically a safety rule; name the type of
every direction argument.

Process changes in `05_GIT_AS_TRANSPORT.md`: Gemini briefs stay in chat,
rulings go in the repo.

---

## 5. COMMITS

fxmatrix: `63b98fb` ADR-153 rev 3; `da7c790` spec; `53d7564` DeepSeek
review 1; `807c794` follow-up spec; `01b71d8` merge; `10af8da` backlog +
traps. pipshed: `400d089` spec v2; `36a4ade` merge `--l0churn`.

Line count: 133
