This message has a line count at the bottom

# BACKLOG -- OPEN WORK ITEMS

Standing list of what is open. **Not state** (that is `01_BOOT.md`), **not
direction** (`07_ROADMAP.md`), **not this session's plan** (the NEXT SESSION
block of the newest handoff, which picks items from here and orders them).

Rules: an item stays until it ships or is explicitly dropped. Each one says
what it is, why it is blocked or not, and where the evidence lives. Delete
when done -- the handoff records that it happened.

Last reviewed 2026-09-21 19:40Z.

---

## A. BEFORE WEDNESDAY (new demo account, flat book)

| # | Item | Status |
|---|---|---|
| A2 | **Lots: DECIDED 2026-09-21 -- 0.01 on every instance.** Overrides Gemini's dollars ruling: 0.01 is the broker minimum so no partial fill is possible, and the EA assumes full fills (C14) | decided |
| A3 | **Fleet: DECIDED -- nine pairs, one arm, cap 8.** Written up as the cycle-3 pre-registration, `docs/architecture/geometry-cycle3.md` | superseded 2026-09-23: eleven instances (C30, pre-registration A3) |
| A4 | **Presets: WRITTEN 2026-09-21** -- nine `ea/presets/*_opt.set` (ALT presets untouched, unattached). EURGBP exit 5 (fills). Deploy with A9  **Still to do before the attach (2026-09-23):** the files have NO line for `InpEnableCommandedEject`, `InpAutoEject`, the W and k inputs, and set `InpEnableCarryPass=false`; A2 point 2 needs those lines ADDED (ejections on, W 5, k 1.5, carry on), byte-checked, and no preset may set `InpBreakerEnable=false` (C35) | done 2026-09-23: eleven presets (nine OPT + `audnzd_dup`, `nzdcad_dup`) with the A2 point-2 lines and explicit breaker |
| A5 | **Account identity in telemetry and archive.** Without it the two cycles' daily totals mix. Cheapest while there is one account | done (ADR-159: `account_login` in `DAILY_SNAPSHOT`, heartbeat and `scalp_closed`; session view `session_accounts`) |
| A9 | **Deploy sequence: WRITTEN** -- `docs/runbooks/account-close-out.md` + `docs/runbooks/cycle3-start.md`. Includes the F1 first-live check (B1) | ready |

## B. WEDNESDAY, OPTIONAL BUT CHEAP

| # | Item | Status |
|---|---|---|
| B1 | **F1 (barbell) on the flat account.** Merged already; a flat book has nothing to fail reconstruction, so it deploys cleanly and is the groundwork for ejection. Empty-book test not written; ADR-156 tests cover reconstruction | ready |
| B2 | **DECIDED 2026-09-22: carry ON for cycle 3.** Carry shifts EVERY exit, ejected and purgatory-held included; an exit that ignores accrued swap locks in a loss. The accrual-before-modify halt path was fixed first (ADR-155 part A). C25 DONE (`f870598`): the guard is gone; set `InpEnableCarryPass=true` in the nine cycle-3 presets with the pre-registration amendment | decided |

## C. AFTER WEDNESDAY

| # | Item | Status |
|---|---|---|
| C2 | **DONE -- ADR-156, merged `3f72b9f` (2026-09-22).** Startup tolerates a missing required exit, places it, OnTick stays strict. Suite 1432/1432; DeepSeek audit `5e31430`; Gemini ruling `prompts/gemini_adr156_ruling.md` | done |
| C3 | **Second exit study** on the new account, using a difference-from-reference selection rule (the flaw in `prompts/exit_counterfactual_results.md` s6). Needs a week of fills | waiting on data |
| C4 | **Carry-skewed quoting.** Asymmetric L0 (e.g. mid -2 / mid +8) to prefer the positive-carry side. Breakeven is 2-3 nights held against a 3-pip skew, most holds are hours, and a fleet-wide skew becomes a carry trade. **First: split realised pips by side per pair, after swap, and see whether there is anything to capture** | analysis first |
| C6 | **Linux box qualification.** Run `fxgrind_tests` there (Strategy Tester); whitelist the pipshed URL and prove telemetry; systemd service so the terminal survives a reboot; watch for Wine crashes. Only then consider moving a fleet to it. `OrderSend` under Wine stays unproven until a live instance runs | partly done |
| C7 | **Monitoring for N accounts.** One pass/fail across accounts: anything halted, any account near its loss limit, any book near 200, any API count near cap, anything stopped reporting. See `07_ROADMAP.md` s4 | not started |
| C12 | **Twelve tests price against the LIVE chart.** T45, T46, T46b, T46c, T57b, S1, S1b, S3, S4, S5, S6, Q10 reach exit placement on the 1001 fixture without `Grind_MarketTestSeed`. Their own assertions pass either way and the carry leak they caused is fixed (`29df88f`), but they still read live quotes. Seed the market in each | hygiene |
| C17 | **DONE -- ADR-158 account daily-loss breaker (`8df6ffa`).** FTMO 2-Step rule: equity >= balance at 00:00 CE(S)T - $500. Day from GMT (EU DST), anchor from deal history (all types; mid-day deposit anchors to itself), allowance 5% of first deposit, 80% soft trip latched per FTMO day (shared GV), blocks + cancels entries; exits/carry/ejection stay on; pre-midnight entry halt at 23:00 CE(S)T when floating >= 50%. Suite 1646/1646. Deferred: hard level, market orders, pre-midnight trimming | done |
| C15 | **DONE -- ADR-155 commanded passive ejection, merged `95c89c6` (part A `b5a9e20`).** Operator script `scripts/grind_eject.mq5`; EA moves the deepest exit to the best passive price; `InpEnableCommandedEject` default false. Gemini-reviewed; DeepSeek audit `prompts/deepseek_adr155b_audit_response.md`; suite 1538/1538. POLICY (when to eject) still open: pre-register it for cycle 3 | done |
| C16 | **Retrace study** -- from the M1 bid/ask data and fills: for each capped side, how often a retrace of `X` pips came within 24h vs how often price returned to the deepest layer's exit. Turns roll vs eject-and-wait into a number. Operator insight 2026-09-21: with add `a` < exit `X`, a retrace earns ~`X/a` pips per pip beyond the first `X` -- favours rolling | not started |
| C14 | **Partial-fill handling -- prerequisite for any lot above 0.01.** The EA reads filled volume only to LOG it (`grind_engine.mqh:1651`) and places each exit for `InpLots`. A partial fill leaves an exit sized for volume that never filled; CloseBy then nets part of it and leaves an unowned opposite position, and the second partial deal's handling is untraced. Operator wants to scale (e.g. 0.1 on a 100k account), so this gates growth. Tied to the API budget: handling partials costs requests | not started |
| C9 | **Rotate `TelemetryAPIKey`.** It appeared in a chat screenshot (not public). Deferred from Wednesday: rotating means touching every chart's inputs. Do it at a reattach that is happening anyway. **The separate, larger exposure is the pipshed READ token, which is in every handoff in a PUBLIC repo** -- see the standing question in `NEW_CHAT_PROMPT.md` | deferred |
| C10 | **Carry cost of cycle 3.** Tighter grids hold more layers, so the nightly swap bill rises, and that is NOT in the pips-per-day figures cycle 3 was chosen on. Measure after a week of the new geometry | waiting on data |
| C18 | **Quarantine escalates while the retry is blocked** (DeepSeek T-1 on ADR-156). Checks count even when `Grind_GuardsAllowTrading` is false, so ticks during a close-only window halt an instance that cannot repair itself. Smallest fix: count a check only when a retry was allowed. Quarantine-wide, so its own ADR (Gemini ruling item 4) | not started |
| C19 | **Pipshed does not render `CRITICAL_*` events.** They are stored in `ea_events` but no dashboard view shows them, so `STARTUP_EXIT_SHORTFALL_SIDE` is visible only in the Experts tab or an archive query. **In ADR-159 (rev 2 `600fc8d`): red CRITICAL banner plus an amber allow-listed WARN tier, last 24 h** | done (ADR-159, pipshed `e475506`) |
| C20 | **DONE -- `tools/r1_audit/r1_audit.py` in the repo (`e9d8d24`).** Per-audit JSON config in `prompts/`, hard-coded `AUDIT_TASK` deleted, key from `D:\candlelab\.env`, `.env` gitignored; old local copy renamed `.retired` | done |
| C21 | **Guard ceiling rations entries by tick speed.** At `positions + orders + resting ENT > 194` every entry is blocked; each scalp frees room for about one entry, and the fastest-ticking instance takes it (2026-09-22: GBPUSD OPT took AUDCAD ALT's freed slot in 0.3 s). Safe (exits unaffected) but unfair. First: show the guard total in pipshed (`g_grind_last_guard_total` exists in the EA); watch it on the 9-instance account before designing any allocation | not started |
| C22 | **Score and compare pairs in USD, not only pips.** At 0.01 lots one pip is worth $0.057 (AUDNZD) to $0.134 (EURGBP), 2.3x apart (derived from the 2026-09-22 13:04Z book; last scalps confirm to the cent). Equal pips on AUDNZD and GBPUSD made about half the dollars. Judge geometry per pair in pips; judge contribution, risk and the C17 breaker in USD. For cycle 4: R2 scores USD per scalp. With lots fixed at 0.01, the dollar lever is pair SELECTION. Sizing lots by pip value is OUT while fills must stay atomic (see C23) | not started |
| C23 | **Assess partial-fill risk above 0.01 lots.** 0.01 is the minimum lot, so a fill is all or nothing, and the engine assumes it. Question: how often does a resting limit of 0.02 or 0.10 fill partially on this broker? Evidence first: the symbol's filling mode (`SYMBOL_FILLING_MODE`) and the order's `ORDER_FILLING_*`, then a small sample of deals whose volume is below the order volume. If partials are very rare, design a contingency that cannot threaten the API budget (one bounded action per partial -- for example keep the filled part as a smaller layer, or close the remainder once -- never a retry loop). Unlocks sizing by pip value (C22) | not started |
| C24 | **Daily close snapshot in pipshed: spread capture vs inventory P&L.** Realised P&L counts only winners (no stops), so it cannot show whether the system makes money. One row per day at a fixed time (broker day end or the FTMO reset C17 settles on): equity, balance, realised that day, change in open MTM, swap charged, layers per side per instance, guard total. The daily equation is spread capture (realised) minus adverse selection (MTM change: layers pile up because price moves against the book). 2026-09-22: ~+$80 realised on ~210 scalps, while one hour showed +$7.85 realised vs -$28.55 MTM. Cycle 2 all-in: equity 9,853 vs 10,000 start (-1.5%); closed about +$169, open book about -$316. Before/after measure for C15 ejection. **In ADR-159 (rev 2 `600fc8d`) as `DAILY_SNAPSHOT` at each FTMO roll; the row's day-start equity minus balance is exactly the carried MTM that C28 needs** | done (ADR-159: pipshed `e475506`, EA `1c4a549`) |
| C25 | **DONE -- ADR-151 Phase B closed, carry unblocked (`f870598`).** Items 1-2 verified, 3 moot under the barbell; the carry-pass clamp now sets the release marker (decision 3); the OnInit guard is removed. DeepSeek T-3 widened by Claude: the EA never flushed GlobalVariables, so a crash after the carry pass would halt every carry-shifted instance on I6 -- fixed with a dirty flag flushed every 1 s and on deinit. Suite 1561/1561; audit `prompts/deepseek_c25_audit_response.md` | done |
| C26 | **Carry clamps at rollover spread are never re-priced.** The carry pass (23:50-00:00 broker) has NO spread threshold (`Grind_CarrySessionReady` checks only tick validity, freshness and FULL trade mode). When the carry-adjusted exit lands inside a widened rollover spread it is CLAMPED to ask/bid + min passive distance -- safe since C25 (shift + release marker) -- but it stays there until the next night's pass; nothing re-prices it when the spread normalises. The layers most affected are those nearest the market, about to scalp: they sit roughly one rollover spread further away for most of a day. A performance cost, not a safety issue. Options: skip a symbol while spread exceeds a multiple of normal and retry within the window; or re-price exits carrying a release marker once the spread normalises (more robust). **First measure** in cycle 3's first week: clamps per pass are already counted (`g_grind_carry_exit_clamped`) | waiting on data |
| C27 | **DONE -- ADR-157 automatic passive ejection trigger (`ce6e985`).** Fires at cap when stable (no new extreme in W of 2W minutes, wall-clock window), spread <= k x hourly mean, trailing an orphaned ejected exit; shared accept path with the manual command; `InpAutoEject` default false, W 5, k 1.5. DeepSeek: five fixes incl. a retry storm and a carry-pass race that also affected the manual command. Suite 1619/1619. POLICY tuning (W, k, tie rule) comes from the demo | done |
| C28 | **Carried open MTM against the FTMO daily limit.** Cycle 2 ended on the Max Daily Loss (-$505.34 vs -$500) with 84% of the day's loss in positions carried in from earlier days: the day OPENED at about -$330 of its $500. FTMO counts the daily limit from the day-start BALANCE with equity including open MTM, so displacement is charged to whichever day it is observed on. The ADR-158 breaker cannot help much with that: blocking entries from its ~09:17Z trip saves only ~$19 (day ~-$486); no entries all day still gives ~-$425 (`docs/FULL_TRIAL_RECORD_1514582088.md` s3). It nearly happened a week earlier: 17 Sep's equity low was 9,799.31 against an anchor of 10,220.35, i.e. -$421 (84% of the limit) -- the 80% breaker would have tripped that day too (EA `GRIND_MAE_*` GVs read at close-out). **Question for Gemini before cycle 3: is a rule on carried MTM needed, and what?** Candidates only, not decisions: report carried MTM as a share of the daily limit at each roll (ADR-159 already records it); block NEW entries for the whole FTMO day when the day opens above a set share; or size the fleet so plausible stack MTM stays well inside the limit (cycle 3's single arm roughly halves it). Operator stance applies: a clear rule, no arbitrary barrier | done: ADR-160 R4 gate merged `c22e9ff`; pipshed D5 pending |
| C29 | **NZDCHF's magic is not in the EA's fleet table.** `GRIND_CAP_ALL_MAGICS` (`grind_cap.mqh`) holds the 16 cycle-2 magics and test CM1 asserts size 16. `22260701` (ADR-154, cycle 3) appears only in the preset. `Grind_IsFleetMagic` (`grind_exitq.mqh:105`) gates the guard's resting-ENT count, so on the cycle-3 fleet the guard undercounts NZDCHF's resting entries and eats the 4-slot margin that protects exits from the 200 limit. Fix: add the magic, update CM1, one test. Fold into the ADR-159 EA prompt. Any new magic (C30) must be added the same way | done (ADR-159 EA `1c4a549`; fleet table 18, fixtures updated) |
| C30 | **Eleven instances: the nine plus two duplicates** (operator, 2026-09-23), to raise the dollar weight of cheap pairs at 0.01 lots without partial fills (0.02 is out while C23 is open). Operator's candidates: AUDCAD and NZDCHF. Data: at 0.01 lots, AUDNZD $0.057 per pip, AUDCAD/NZDCAD $0.071, USD pairs $0.100, CHF-quoted pairs about $0.122 (NZDCHF should match), so NZDCHF is heavier per pip than GBPUSD; the cheapest are AUDNZD, AUDCAD, NZDCAD. Needs: magics in the fleet table (C29), simplest as each pair's ALT magic and slot with OPT inputs; a re-run of the slot model (nine peak ~144; rough +2 gives mid-170s vs the 194 guard); s4 counted per pair on the primary magic only; pre-registration amendment A3 | DECIDED 2026-09-23: eleven -- duplicates of AUDNZD and NZDCAD (cheapest per pip; NZDCAD over AUDCAD to avoid adding AUD); pre-registration A3 |
| C31 | **The EA does not notice an externally flattened book.** After FTMO closed all 128 positions and deleted all orders at 13:51Z on 23 Sep, all 16 instances ran on for ~47 minutes reporting their old layers with `recon_ok:true` and `invariant_ok:true`, while the shared API count rose 506 -> 1,906 (13:16Z -> 14:38Z) to `WARN_API_SOFT_LIMIT`. **New evidence (the ADR-159 banner, 24 Sep):** every instance logged `QUARANTINE_ENTER` at 13:51Z (the liquidation minute) and `WARN_API_ENTRY_STOP` at 13:53Z -- so the EAs DID react (quarantine), then exhausted the daily request budget. First: read the Experts log around 13:51Z for what each instance tried, and find which checks run on a live tick versus only at startup. An account-level "book vanished" detection (positions and orders gone, balance moved) should halt, not retry | not started |
| C32 | **The currency cap cannot be enabled with a partial fleet.** `Grind_CapReadStoredPeer` (`grind_cap.mqh`) treats a fleet magic with no stored exposure as a FAILED peer (value maxed), and `Grind_CapAllowsEntry` then blocks. The table lists all 18 known magics; cycle 3 runs nine, so every leg would block the moment a threshold is set. Harmless while thresholds stay 0.0 (it returns before reading peers). Fix before anyone enables the cap: count only peers whose heartbeat or exposure GV is fresh, or list only the running magics | open, cap disabled |
| C33 | **Three pipshed verify scripts fail on `main`:** `verify_fxgrind_panel.py`, `verify_grind_exposure.py` (AssertionError) and `verify_grind_pnl_render.py` (`KeyError: 'arm_summaries'`). Pre-existing: identical at `b81a339`, before ADR-159 | not started |
| C34 | **Archive short of the broker on 16-17 Sep:** 3 and 4 scalps missing in `fill_logs` against the deal dump (exact on every other day from 14 Sep). The FOMC halt days; look for `TELEMETRY_QUEUE_DROPPED` / `TELEMETRY_BATCH_REJECTED` markers then. Historical only | not started |
| C35 | **No preset may set `InpBreakerEnable=false`.** It is ADR-158's per-instance off switch, so one disabled instance never adopts a peer's trip (DeepSeek ADR-159 T-1, by design). Add to the cycle-3 preset byte-check (A4) and the attach read-back | done: `InpBreakerEnable=true` explicit in all eleven presets; in the attach checklist |
| C36 | **Pipshed ADR-160 D5:** migration `003` adds nullable `gated_seconds` and `history_ok` columns to `daily_snapshots`, backfilled from `detail` (both keys are already in every snapshot's JSON); a Gated column (hours, 1 dp) on the Daily card; gated hours per day in `s4_scalps.py`. Pipshed only, safe mid-cycle | not started |
| C37 | **ADR-161: entry session window** (Fleet B Phase 1). New entries only 07:00-16:55 Toronto, Mon-Fri (Gemini G1: 5 min before the rollover); cancel own entries at the close and retry on ticks until none; `InpSessionEnable` default OFF. `docs/architecture/ADR-161-session-window.md` | MERGED `25a5f93` 2026-09-24 (suite 1830/1830, DeepSeek verified); deploy on the Linux box at the Phase 1 switch (ADR-161 s6) |
| C38 | **Fleet B on the dashboard.** Today only the JSON `/status_b` route shows it; the dashboard is built on one global `GRIND_INSTANCES` list (~12 uses). Make it fleet-aware or add a Fleet B page | DONE 2026-09-24: second Railway service with `GRIND_FLEET=B` (pipshed `39df6e0`), `https://pipshed-copy-production.up.railway.app`; custom domain `https://linux.pipshed.com` live 2026-09-24 (Cloudflare CNAME to Railway, proxied) |
| C39 | **Cycle-4 precursor idea (operator, 2026-09-24):** a non-USD five-currency ring (EURCAD, EURGBP, GBPCHF, AUDCHF, AUDCAD -- each currency twice) with three arms per pair in a corner layout in (add, exit) space (OPT; OPT + d add; OPT + d exit), cap 7, and a pooled plane fit across the ring (15 points, 7 unknowns) to point at the next geometry. Needs the new pairs' magics in the fleet table (C29 lesson), a slot model (~210 peak vs 194), and its own ADR | idea |
| C41 | **Test harness skips records after a cancel.** The order-test path of `Grind_CancelOwnEntryOrders` walks `g_grind_order_test_records` forwards while a successful cancel removes the record and shifts the array, so the next record is skipped (the live path walks backwards and is correct). Test-only; SW6 works around it. Fix: walk backwards, then check no existing test relied on the skip | not started |
| C42 | **Unparseable ENT comments are invisible to cancels** (DeepSeek ADR-161 T-3, pre-existing). `GrindCommentBuild` prints FATAL on a comment over 31 chars but still returns it, and the order is placed; a broker that rewrites comments would do the same. Cancel, count and reconstruction all rely on the parse. Fleet B and cycle 3 comments are ~19 chars. Smallest fix: refuse to place an ENT whose comment is too long | not started |
| C43 | **Holiday early closes vs the session window.** Brokers often close early on 24 and 31 Dec; if before 16:55 Toronto, the window's cancel lands in a closed market and entries rest over the holiday (the 5-min `SESSION_CANCEL_STUCK` WARN would show it). Check IC Markets' holiday schedule in December | before December |
| C44 | **Optional ADR-161 hardening** (DeepSeek T-2/T-6, bounded): call `Grind_SessionStep` at the top of `OnTradeTransaction` (closes the <=1 s fill-time gap at 16:55); cancel leftover entries on the open transition (so a stale 16:55 L0 is replaced at 07:00's mid); cap retries per closure | optional |
| C45 | **ADR-162 (draft): roll at cap by the grid's own rule, replacing ADR-157's trigger** (operator, 2026-09-24: passive ejection became "the most complicated layer add"; boring is best). At cap, when price reaches the level where the next add would go, do not add: move the MOST UNDERWATER layer's resting exit (F1 barbell) to where that virtual layer's exit would be (add level +/- exit pips) -- an `OrderModify` via ADR-155's existing offset plumbing (durable, I6-aware). **One rolled layer per side**, re-armed only when it FILLS (a retrace of X), so a one-way trend gets at most one roll; **no march** (retires ADR-157's orphan trailing, W, k and the M1 data). Unfilled and price N add steps beyond -> WARN on pipshed for a MANUAL decision (ADR-155 command gains a "roll here" mode); the operator judges the whole book (other layers, opposite side, swap, FTMO headroom, weekend, correlated pairs). Economics unchanged (realises the oldest layer's loss; full recovery is max regret) but X pips better and one retrace later than ejection; less aggressive in chop. **Evidence first: review cycle 3's live `EJECT_ACCEPTED` / `EJECT_FILLED` at week end** -- per ejection: move afterwards, whether it marched, what the rolled exit would have done; sets N. Not mid-cycle 3 (auto-eject is pre-registered ON): build behind a default-OFF input for a Fleet B phase or cycle 4, with Gemini | draft; evidence at week end |
| C46 | **Per-layer exit: self-describing layers** (operator, 2026-09-24: react to our own trade data by nudging add/exit as scalps accrue, on a LIVE book). Prerequisite for any on-the-fly exit change: today I6 halts an instance whose resting exits differ from `InpExitPips`. Proposal: the ENT comment carries the layer's exit distance (e.g. `GRIND|OPT|L|L03|ENT|X10`, 23 of 31 chars; positions keep their opening comment on both brokers), so a changed exit applies to NEW layers only and I6 checks each layer against its own recorded exit (ADR-155's per-ticket offset is the precedent). Parsing must accept the old format (live books). Bonus: every archived scalp is tagged with the geometry it traded under | design |
| C47 | **Per-side add and exit inputs** (long and short independent; `cycle4-live-geometry-search.md` Part B). Needs C46 for exit. Touches presets, the ADR-153 ratio guard, config dump/archive config fields, heartbeat and pipshed | design |
| C48 | **Plane-fit tooling on the archive:** per pair and side, scalp rate and pips (plus swap, excluding MTM; depth policed separately) against the (add, exit) each scalp traded under (C46 tags). Local plane from simultaneous arms; step incrementally on evidence. Replaces the design note's R5 (flat restart on a fresh magic) with incremental stepping on a live book -- note rev 3 pending. Fleet B is where C46-C48 are built and proven | design |
| C40 | **Observation: I3 transient when F1 moves an exit** (cycle 3, AUDNZD, 24 Sep 03:13Z/03:19Z): cancel-then-place leaves the required layer uncovered for ~200 ms; I3 quarantines and releases on the next check (`I3_SHORT_NAKED ms=203 checks=1`). Benign. Place-before-cancel would remove the gap but needs a spare slot; not mid-cycle. Watch the episode count | observe |

## D. STANDING / HYGIENE

| # | Item | Status |
|---|---|---|
| D1 | **Daily-loss headroom** is checked by eye in MetriX and was last verified 16-Sep. It is the one account limit nobody watches. **It ended cycle 2 on 23 Sep** (C28); ADR-158's breaker and ADR-159's snapshot watch it from cycle 3 | recurring |
| D2 | **`research/geometry-depth-holdtime` is not merged**, though the cycle-2 memo says it is | small |
| D3 | **`.gitattributes` comment says "Docs stored CRLF"**, but `eol=crlf` controls the working copy; the repo stores LF | cosmetic |

Line count: 90
