This message has a line count at the bottom

# HANDOFF 2026-09-17 -- ADR-151 PHASE A DEPLOYED; ALL 14 RUNNING; GUARD BINDS

Continues `HANDOFF_2026-09-16c.md`. New chat. Built, tested, merged and
deployed ADR-151 Phase A (exit queue + commitment guard) and brought every
halted instance back. All times UTC. VPS log clock is UTC; broker is UTC+3.

---

## 0. STATE AT 02:53Z

| | |
|---|---|
| fxmatrix main | `c39fb84` (merge of `feat/adr151-exit-queue` @ `5bc5a88`); `ea/` tree `d704e79` identical to `5bc5a88` |
| VPS | `c39fb84`, `deploy.ps1` byte-identical, `fxgrind.mq5` compiled 0 errors at **02:15:57Z** |
| Desktop suite | **1136/1136** at `5bc5a88` (baseline 1038/1038 on `6cc2899`, confirmed) |
| Rollback build | `rollback/adr151-k99-r2` @ `f7e2123`, compiled 0/0 on desktop, NOT merged |
| ADR-150 binary | `C:\fxmatrix_backup\fxgrind_adr150.ex5` (213,270 bytes). Do NOT restore while any layer is held (s5) |
| Fleet | **14 attached, 0 halted** (status c15, 02:53:05Z). NZDCAD ALT / AUDNZD ALT still detached (09-16), books empty. NZDCHF never attached (ADR-146 D1) |
| Book | AccountLimits 02:52:54: **91 positions + 79 orders = 170** / 200 |
| Guard | 170 + 26 resting entries = **196 > 194**: new entries deferred fleet-wide (s3) |
| api_count | 1447 (00:45Z) -> 1539 (02:53Z), fleet-wide |

## 1. WHAT WAS DONE, IN ORDER

| Step | Evidence |
|---|---|
| Questions to previous chat | `prompts/questions_for_previous_chat.md` `bf1fd82`; answers `prompts/answers_from_previous_chat.md` `8d8bbce` |
| Gemini ruling on AM1-AM8 + rollback + resume order | Request `prompts/gemini_adr151_phaseA_amendments.md`; response in `prompts/gemini_adr151_phaseA_amendments_response.md` (committed with this handoff). AM1-AM8 all APPROVE; K=99 rollback APPROVE; option (ii) APPROVE |
| Spec rev 2 (AM9: K override seam, EQ5, MQ7, rollback branch) | `prompts/cursor_adr151_phaseA.md` `6cc2899`, 315 lines |
| Cursor build | `51d9be7` tests, `568ac58` impl, `7ed2a7a` report; rollback `a8c08a0` (superseded) |
| Suite at 568ac58 | 1113/1134, 21 FAIL. Diagnosed (s4 D4), none in live logic |
| Fix 1 spec | `prompts/cursor_adr151_phaseA_fix1.md` `6f75376`, 168 lines |
| Cursor fix 1 | `4d1c6ee` tests, `1390052` seams, `5bc5a88` report; rollback r2 `f7e2123` |
| Desktop gates | suite 1136/1136; feature EA 0/0; rollback EA 0/0 |
| Merge | `c39fb84`. Two-dot diffstat showed the fix1 spec as "deleted": artefact, see TRAPS |
| Runbook | `prompts/runbook_adr151_deploy.md` (committed with this handoff) |
| Detach 7 parked | c3 01:59Z books clean; c5 02:10Z confirmed; Experts list showed exactly the 7 running |
| Deploy | backup ex5; deploy.ps1; `grind_exitq.mqh` hash checked; compile 02:15:57Z |
| Verify | 16 REMOVE ok=True (retcode 10009) at 02:15:57Z; AccountLimits 87+80=167; c6/c7 all recon_ok; api_count flat 15 min; Experts log clean after 02:14 |
| Reattach, one at a time | AUDNZD OPT 02:33, NZDCAD OPT 02:35, AUDCHF OPT 02:38, AUDCHF ALT 02:41, EURUSD ALT 02:45, EURUSD OPT 02:49, GBPUSD OPT 02:51 |

## 2. LIVE BEHAVIOUR OBSERVED (ADR-151)

**Trim on reload** (02:15:57Z): GBPUSD ALT 9, EURGBP ALT 3, EURGBP OPT 2,
CADCHF ALT 1, AUDCAD ALT 1 (its short L03 filled at 02:05 and made L00 rank 3;
predicted 15, actual 16, the miss was mine). Reattach trims: EURUSD ALT 4,
EURUSD OPT 5, GBPUSD OPT 7. Held layers carry formula `exit_target`, never 0.0.

**Fill -> exit -> trim**, 8 live cases, exit placed 92-207 ms after the fill:
AUDCAD OPT S L03 (02:35:28), NZDCAD OPT S L01 (02:36:26) and L L02 (02:46:12),
AUDCHF OPT S L03 (02:40:42), AUDCHF ALT S L03 (02:42:52), EURUSD ALT L L07
(02:45:39), EURUSD OPT L L08 (02:51:34), GBPUSD OPT L L10 (02:51:37). Where
the fill pushed a required layer to rank 3, its exit was cancelled in the same
event.

**Locked pairs netted on reattach:** AUDCHF ALT +1.34, EURUSD ALT +0.75.

**One entry per tick:** AUDNZD OPT L0 long 02:33:12.274, short 02:33:13.459.

**Guard deferral and release:** AUDCHF ALT long L0 absent at c12 (02:42Z);
placed 02:45:22.613, 149 ms after EURUSD ALT's reattach trims freed slots.
Deferred again later (AUDCHF ALT short add, EURUSD OPT L09 add).

**NOT yet exercised live:** a front exit filling and releasing a held exit
behind it; the hold-cancel / fill race (deal lookup, `exit_position_ticket`
set, CloseBy queued); `QUARANTINE_ENTER/RELEASE` on unwind (expected by design,
memo s4.4); `SLOT_LOCK_STOLEN`. All need price to reverse.

## 3. FINDINGS -- CAPACITY

**The guard now binds in steady state.** 170 on the book, 30 physically free,
but 26 resting entries each reserve their future exit: 170 + 26 = 196 > 194.
Entries wait; exits need only 1 free slot and are unaffected. Many resting
entries are adds far from market (e.g. AUDCAD OPT L02 ENT resting since 01:54Z)
and they spend fleet-wide entry budget while doing nothing.

**Consequence for the detached NZD ALT arms:** they would attach safely (flat
books, guard-protected) but their L0s would defer with everyone else's, and
take entry sends from other instances when slots free. Not reattached tonight.

Levers, none decided: fewer resting adds (e.g. only near-market adds rest);
`GRIND_SLOT_MARGIN` 4 -> lower; K 2 -> 1; majors cap 12 -> 8 (follow-on ADR,
I7 trap); operator closing deep layers. Each needs DeepSeek + Gemini.

**Memo s3 capacity table was wrong for majors.** One-sided mixed fleet at
K=2, H=1 is 4x16 + 10x12 = 184 <= 196 (table applied P=12 to all pairs). The
case for 12 -> 8 is the operator's (scalp income vs depth, risk spread), per
`HANDOFF_2026-09-16.md` s7, not the arithmetic. "Measure first" still undone.

**Request rate:** ~320/hour quoted at session start was storm-contaminated.
Resting fleet rate was 18 requests in 74 min (00:45-01:59Z).

## 4. FINDINGS -- DEFECTS AND PROCESS

D1. **Commit 1 (`51d9be7`) does not compile.** `fxgrind_tests_adr151.mqh:17`
    declares the ranked `Grind_ReconCheckInvariants` overload, body only in
    commit 2 (`function must have a body`). Tests-fail-first proof unavailable
    for this change. Not fixed; history left as is.
D2. EQ/SG/LK tests pass at both commits (`grind_exitq.mqh` was complete in
    commit 1). Spec required this be reported; it was not.
D3. **Both Cursor reports inaccurate.** Rev 1: "3 existing tests modified"
    (~15 call sites), named SB4 (untouched), rollback "(pending)". Fix 1: C6
    reported as `c2ed0ef`, on origin `5bc5a88`. Branch reading caught all.
D4. 21 FAILs at `568ac58`: per-tick flag leaked between tests (A8, SB2, PO3);
    seams did not model slot changes or reads (SG5, SG6, MQ3, MQ6, CV3); two
    wrong tests (EQ2 expected ascending for shorts; RI5 fixture had no held
    layer); Q10 fixture not updated. Fixed in fix 1; production paths
    unchanged when test flags are off.
D5. **16c deploy plan was wrong twice.** A compile clears every halt
    (`OnInit` sets `g_grind_halted=false`, fxgrind.mq5 ~125), so "parked"
    cannot survive a compile. And restoring the ADR-150 ex5 after trims would
    fail reconstruction fleet-wide. Replaced by Gemini option (ii) + K=99.
D6. **Claude repeated "8 halted / 6 running" for hours;** actual 7/7. The
    operator's note said 8 but listed 7 names; nobody counted.
D7. Claude called NZDCAD OPT "still attached" at c4 from P&L that moved
    during the detach and from `api_count` (fleet-wide). Two post-detach reads
    identical to the cent is the test.
D8. `TelemetryAPIKey` appeared in full in a chat screenshot. **Rotate.**
D9. Gemini response pasted in chat, not committed until this handoff.
D10. Pre-existing add clamp: a ladder paused for hours re-adds AT MARKET on
    reattach (EURUSD ALT/OPT, GBPUSD OPT, AUDCHF OPT all filled within
    minutes). Not ADR-151. Worth a decision.
D11. `api_count` increment is read-then-set (grind_api_counter.mqh ~47-51);
    a lost update sets `g_grind_api_counter_broken`, which silently stops that
    instance trading, including held-exit release. Never seen. Whether pipshed
    shows the flag: unverified.

## 5. ROLLBACK -- AS IT STANDS

R1 (transition build, restores every exit where slots exist):

    cd D:\fxmatrix
    git checkout main
    git pull origin main
    git merge --no-ff origin/rollback/adr151-k99-r2 -m "ROLLBACK R1: GRIND_EXITQ_K 99"
    git diff HEAD~1 --stat      # ea/grind_config.mqh | 2 +- and nothing else
    git push origin main

Then VPS deploy.ps1 + compile. R2 (true code rollback) only after a status
read shows ZERO held layers fleet-wide; write the revert against actual main
history at that time. **Never restore the ADR-150 ex5 while any layer is held.**
At 02:53Z, 38 layers are held.

## 6. NEXT SESSION

  1. **Watch the untested live paths** (s2 "NOT yet exercised"): `ea_events`
     for QUARANTINE pairs and any INVARIANT_FAIL; `send_logs` for a release
     PENDING EXT after an OUT_BY CloseBy; hold-cancel race outcome.
  2. **Rotate `TelemetryAPIKey`** (D8) and the DeepSeek key (16c, still open).
  3. **Capacity design** (s3): guard steady-state binding, resting-add
     budget, margin/K, NZD ALT arms. DeepSeek + Gemini before any change.
  4. Majors cap 12 -> 8 follow-on ADR, rationale restated (s3), I7 migration.
  5. Add clamp after long pause (D10): keep, or require grid level.
  6. Pipshed heartbeat fields: slots used/free, fleet resting_ent, held exits
     per side, guard-deferral count, `api_counter_broken`.
  7. Spec template: stub commit must declare every new SIGNATURE with a body
     (D1); require reporting tests passing in both states (D2).
  8. ADR-151 status line -> "Phase A implemented and deployed, merge c39fb84".
     ARCHITECT s1 frame (exactly one resting exit) and s3 courier are stale.
  9. Keep `rollback/adr151-k99-r2` until a week stable; delete
     `rollback/adr151-k99` (superseded, never compiled).
 10. Phase B before carry is ever enabled.
 11. Carried from 16c: FTMO 2,000/day verification; F10; finalise 16b; repo
     privacy / pipshed auth; DeepSeek runner fixes.

## 7. STANDING RULES ADDED TONIGHT

- **Never run `fxgrind_tests.mq5` on the VPS.** LK tests delete the live
  `GRIND_SLOT_LOCK` GV; prune tests delete carry GVs terminal-wide.
- Reattach = attach + preset + **fill `TelemetryAPIKey`** + confirm magic/slot.
- Branch review uses **three-dot** diffstat (`origin/main...branch`).

Line count: 175
