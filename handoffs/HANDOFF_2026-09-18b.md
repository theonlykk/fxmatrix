This message has a line count at the bottom

# HANDOFF 2026-09-18b -- ADR-152 PHASE 2 BUILT AND DEPLOYED IN ONE DAY

Continues `HANDOFF_2026-09-18.md`, which stops at the Phase 1 deploy at 04:59Z
and covers none of the below. All times UTC; broker = UTC+3.

Phase 1 is now on ALL SIXTEEN instances. Phase 2 (entry horizon) is on
GBPUSD OPT + ALT only. Seventeen reinitialisations today, no layer lost,
nothing halted at any point.

---

## 0. STATE AT 16:32Z

| | |
|---|---|
| fxmatrix main | `2fdc899`. VPS pulled and compiled from it at 16:03:55Z |
| Suite | **1226/1226**, operator-run after a clean GUI compile of both files |
| Fleet | 16 attached, 0 halted, all `recon_ok` / `invariant_ok`, none quarantined |
| Phase 1 | all 16, via `InpFillTimePlace=true` + `InpSlotNearReserve=8` in 16 presets |
| Phase 2 | GBPUSD OPT (22260101) + ALT (22260102) only, `InpEntryHorizonPips=20` |
| Guard | **196**. Pinned 195-197 all day: 05:01, 05:11, 13:07, 16:02, 16:32 |
| Baseline | `docs/baselines/baseline_20260918.csv` -- 88 adds, median fill-to-placement **32 min**, mean 62, max 329, 98% over one minute |
| Account | realised before the 16:03 compile +$45.31; counters reset at compile. `api_count` 540 |

---

## 1. WHAT SHIPPED

**`feat/adr152-phase2-horizon`, merged fast-forward to `2fdc899`.** Four
commits plus two fix rounds. D3 entry horizon, D4 transition budget, D6 gap
policy, telemetry, and the config-dump fix (G1 from 18a).

**Fail-closed everywhere.** All three ADR-152 inputs default off in the
compiled EA (`false` / `0` / `0.0`). 16 presets carry the Phase 1 opt-in, only
the 2 GBPUSD carry `InpEntryHorizonPips=20`, both NZDCHF files untouched. A
chart that loads no preset runs baseline.

**Two implementation defects found before deploy, both in
`Grind_ApplyEntryHorizon`, both in the same direction -- a side silently
losing its ability to add.**

D1 (found by review, `3cfdd6c`): `in_place` cleared `add_held` BEFORE the
send. `Grind_SendNextAddEnt` returns false on guard refusal, the normal case
at 195, leaving the side neither held nor resting. The branch gates on
`add_held`, so it never retried. First placement attempt would have disabled
adds on that side for the session.

D2 (found by the suite, `9269ffd`): `in_place` gated on `add_held` at all.
`add_held` is only ever set true inside `in_cancel`, so a side that had never
been held could not place while in the place band. Same production path:
guard refuses in the floor band, price drifts into the place band, side stuck.
Fixed by dropping the gate and charging a transition only when
`was_held` -- an ordinary placement is not a D3 transition and must not burn
the 20/day budget.

Also corrected: T10 was testing a gradual approach through the floor, not a
gap, and asserted a gap count from a side the engine had correctly un-held.
Rewritten to keep the add held across the crossing, plus T10b for a gap that
lands inside the floor.

---

## 2. THE MEASUREMENT THAT RESHAPED THE DAY

GBPUSD ALT, 2026-09-18, from the VPS terminal log:

    10:38:55.714  L14 entry fills; its exit rests 1 ms later
    10:38:55.x    add to L15 is due. Cap allows it. GUARD refuses it
    11:51:59.702  buy limit 1.33436 finally reaches the market
    11:52:40.986  it fills

**73 minutes** from due to placed, then 41 seconds to fill. It nearly expired:
a few more minutes of blocking and that layer would not exist. It is the only
GBPUSD long layer in profit.

Fleet-wide, via `scripts/fill_to_placement.py` on the terminal log: 107 entry
fills, 88 paired, **median 32 minutes**, max 329.

**Phase 1 optimises milliseconds against this.** That is not a criticism of
Phase 1 -- it is why Phase 2 mattered enough to build the same day.

The parser pairs only ENTRY fills (a deal followed within 2 s by an
opposite-direction limit at exactly `exit_pips` -- ADR-151's exit). An earlier
run that paired every deal gave a median of 146 min; three quarters of that
was exit fills paired with unrelated orders. Cross-arm pairing remains: on a
symbol with both arms attached, individual rows can mis-pair. The
distribution is sound; do not quote single rows.

---

## 3. FOUR GEMINI RULINGS TODAY -- ALL BINDING

**R1. Phase 1 gate metrics struck.** `entry_place_latency_ms` and
`near_reserve_blocks` are both unfit and are removed from the gate. The gate
is now manual terminal-log extraction of the fill-to-placement gap, recorded
as the Phase 2 baseline. Reasons: latency is a last-value that any tick-path
add overwrites; `near_reserve_blocks` increments per TICK while a far add is
pending, so it reports blocked time, not refused entries -- GBPUSD read 22,309
and 7,930 over eight hours.

**R2. Standalone Phase 1 fleet rollout CANCELLED.** Phase 1 could not be
exercised while the guard is saturated, and the saturation is what Phase 2
fixes -- a catch-22. Phase 1 therefore deployed to the other 14 concurrently
with Phase 2. Step 7 of the 17g checklist no longer exists.

**R3. Market-closed hours are zeroed from all pilot windows.** Friday 21:00Z
to Sunday 21:00Z pauses the promotion clock.

**R4. Roll-at-cap: fleet simulator RESCINDED, live A/B approved, build and
measurement both deferred behind Phase 2.** See
`docs/architecture/roll-at-cap-notes.md` s6 -- do not re-litigate from here.

---

## 4. OPEN QUESTION FOR GEMINI -- MAY BE URGENT

**The pilot horizon may be set too wide to bite.**

ADR-152's own Context says the orders clogging the guard on 17-Sep were "adds
8 to 16 pips from mid". On GBPUSD, `add_pips` is 10, so the D3 floor rests
everything within 10 pips and the hold threshold is 40. **Nothing in the
observed problem range can be held on the pilot pair.**

Consistent with that, at 16:31Z both pilot arms read `add_held_long` and
`add_held_short` FALSE on all four sides, `entry_transitions_used` 0,
`add_gap_missed` 0, `guard_total` 196.

Those zeros are correct behaviour for the current prices -- ALT's long add
rests 9 pips from mid, OPT's short add 9 pips -- but they are also what an
inert configuration looks like. The two are indistinguishable so far.

**Watch before the close: does `add_held_long` or `add_held_short` ever read
true on either pilot arm.** If it never does, the horizon needs narrowing
toward the 8-16 range the ADR itself measured, not `2 x add_pips`.

---

## 5. MECHANICS LEARNED TODAY

- **A compile does NOT reload presets.** MT5 reuses inputs stored on the
  chart. Confirmed twice. A deploy is a compile PLUS a manual reattach of
  every chart, with the API key copied out and pasted back because the preset
  carries it blank. Sixteen dialogs.
- **D3 is adds only; L0 is never held.** L0 re-quotes toward mid every tick
  outside the deadband and is untouched by the horizon.
- **An add is priced off the DEEPEST LAYER, not off mid**
  (`deepest_entry -/+ add_pips`), which is how a gap between an add target and
  mid opens at all. As a ladder's deep end exits, the anchor climbs and the
  target tracks mid without any re-quote.
- **Heartbeat fields are per SIDE**, not per instance: `add_held_long` /
  `add_held_short` and so on. Better than the spec asked for.
- **The terminal log is UTF-16.** Reading it as UTF-8 silently parses zero
  rows. `scripts/fill_to_placement.py` sniffs the encoding and prints what it
  decoded.
- **Trades lines are in `Terminal\<hash>\logs`**, expert `Print` output in
  `Terminal\<hash>\MQL5\Logs`. They are different files.
- **`logs/` is gitignored.** Derived measurements belong in
  `docs/baselines/`, not there.

---

## 6. NEXT SESSION, IN ORDER

1. **Watch the pilot.** `add_held` true on any pilot side is the first proof
   Phase 2 is live rather than merely loaded. Then `entry_transitions_used`
   rising slowly (not jumping -- that would be hysteresis collapse), and
   `guard_total` falling below 194.
2. **Put s4 to Gemini** if `add_held` never goes true: is
   `InpEntryHorizonPips=20` too wide for GBPUSD given the ADR's own 8-16 pip
   evidence?
3. **Run `fill_to_placement.py` again** after a full session on Phase 2 and
   compare against the 32-minute baseline. That is the promotion gate now (R1).
4. Phase 2 promotion gate as written: resting entries fleet-wide below 14,
   guard total below 185 at daily peak, `add_gap_missed` measured, requests
   below 1,800.
5. **pipshed renders none of the new keys** -- `add_held`, `add_held_target`,
   `entry_transitions_used`, `add_gap_missed`, `guard_total`,
   `entry_place_latency_ms`. Everything above needs a VPS log grep. This is
   now the biggest observability gap.
6. Doc corrections still outstanding: the preset/reinit trap into `02_TRAPS`;
   `README.md` still says fourteen instances; ARCHITECT s1, s8, s9 still say
   twelve, and Gemini has ordered that fixed.
7. `guard_total` peak latch -- it is a last-sample, so a transient breach
   between heartbeats leaves no trace.
8. MetriX daily-loss check, unverified since 16-Sep.
9. Then, and only then, the passive-roll ADR (s3 R4).

Carried: 2015q1 tail CSVs for five pairs; Phase B carry premise correction
with Gemini; `deploy.ps1` preset copy with hash check.

Line count: 193
