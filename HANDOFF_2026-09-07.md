# FXMATRIX / FXGRIND — HANDOFF

**Written:** 2026-09-07 evening, amended 2026-09-08 00:40 ET
**Repo state:** `main` at `973d83d`
**Live:** six fxgrind instances + six v2 arms on FTMO demo 1514432484
**Demo expires:** 2026-09-09

This captures what is DONE, what is RULED but NOT BUILT, and what is OPEN.
Everything below was decided today; none of it is in anyone's head only.

---

## 1. STATE OF THE LIVE SYSTEM

Six fxgrind instances redeployed this evening on a clean book after the hedging
exit defect. All healthy: `recon_ok`, `invariant_ok`, `halted:false`,
`peer_read_failed:false`.

| Instance | Magic | Width/Exit | Add | Stranded | MaxLayers |
|---|---|---|---|---|---|
| GBPUSD OPT | 22260101 | 5 / 5 | 10 | 10 | 12 |
| GBPUSD ALT | 22260102 | 5 / 7 | 10 | 10 | 12 |
| EURUSD OPT | 22260201 | 7 / 5 | 14 | 14 | 12 |
| EURUSD ALT | 22260202 | 7 / 7 | 14 | 14 | 12 |
| EURGBP OPT | 22260301 | 3 / 2 | 6 | 6 | 8 |
| EURGBP ALT | 22260302 | 3 / 5 | 6 | 6 | 8 |

Six v2 arms (signal `2026xxxx`, dumb `2126xxxx`) still running, untouched.
Currency caps ship DISABLED (thresholds 0.0) per the Cap-Enablement Gate — the
CAS machinery publishes and reads so it can be observed before being armed.

**NOT YET PROVEN LIVE:** the CloseBy pairing. As of writing, EURGBP OPT and ALT
each hold a short with a resting exit. No exit has filled since the fix, so no
CloseBy has executed on this account. That is the one thing worth watching
before the demo expires.

---

## 2. TODAY'S DEFECT AND FIX (context for anyone reading cold)

**The account is HEDGING.** Confirmed directly: a buy at 0.85889 and a sell at
0.85925 were open simultaneously under the same magic. On a hedging account an
opposing limit order does NOT close a position — it opens a new one.

fxgrind's `Grind_PlaceLimit` never set `req.position`, so every exit opened an
opposing position and left its layer naked. Invariant `I3_SHORT_NAKED` caught it
on the first fill and both EURGBP instances halted in place. Total cost across
eight unintended positions: **-$0.23** — the failed exits hedged the layers they
failed to close, so the book drifted toward flat rather than accumulating risk.

**Fix (merged, `cda7679` + `8491d96`):**
- Ported v2's `TRADE_ACTION_CLOSE_BY` queue with per-side arrays, backward
  iteration, retry limit and halt-on-exhaustion (`ea/grind_closeby.mqh`)
- Exit fill (`EXT` + `DEAL_ENTRY_IN`) queues the pair; scalp and realised P&L
  now accrue on `DEAL_ENTRY_OUT_BY`, NOT `DEAL_ENTRY_OUT` which never occurs
- Invariants I3/I4 amended: a layer is covered by EITHER a resting `EXT` order
  OR an open `EXT` position awaiting CloseBy
- Reconstruction derives the CloseBy queue on init from paired `ENT`/`EXT`
  positions at the same slot/side/layer; idempotency guard inside
  `Grind_QueueCloseBy` because the dedup array is wiped on restart

**Architectural lesson worth keeping:** the retired SRE's CloseBy deal-pairing
was NOT legacy complexity to escape. It is the necessary second half of this
exit model on a hedging account. fxgrind kept the model and dropped the
machinery. Recorded in ADR-125.

---

## 3. RULED BUT NOT BUILT — next cycle

### 3.1 Add-pacing gate (RATIFIED 2026-09-07)

**Nothing currently limits how fast layers are added.** A single fast tick fills
every resting entry it passes. The layer cap bounds the destination, not the
velocity, and Gate A is equity-based so fast deep fills hit the $500 daily limit
immediately.

V1 had this and it was lost in the V1→V2 transition, not in the move to
fxgrind. Source: `ea/Globals.mqh:30`, `MinLayerIntervalSeconds = 300`, enforced
at `ExecutionEngine.mqh:1149` and `FXMatrix.mq5:274`.

**Ruling:** reinstate at **300 seconds** (one M5 bar).

**The argument that settled it:** pacing does not optimise entry price — it
throttles the *velocity of drawdown*. An unpaced grid absorbs its full layer cap
in one tick on a news break, taking a vertical equity hit before anyone can
react. A 300s pace takes layer 1 and waits, giving five minutes to observe and
halt.

**Statelessness (Gate 1 preserved):** derive from `POSITION_TIME` of the
previous layer, which MT5 records natively. Before placing `L03`, check
`TimeCurrent() - PositionGetInteger(POSITION_TIME)` of `L02`. No persistence,
survives restart.

**Also ratified:** keep deepening pace and re-engagement pace DISTINCT, as V1
did. `MinLayerIntervalSeconds` throttles accumulation of underwater inventory;
`ExitResetDelaySeconds` (V1 default 30s) prevents immediate re-entry after a
scratch at the same micro-level. Different failure modes; do not merge.

**Layer cap unchanged.** Cap and pacing are orthogonal — cap dictates solvency,
pacing dictates velocity. Do NOT raise the cap because accumulation is
rate-limited.

### 3.2 Widening ratchet — REJECTED (RATIFIED 2026-09-07)

V2 had a three-part mechanism (`fxmatrix_v2_engine.mqh:470-484, 670, 704, 722`):
widen spacing x1.304 beyond depth 3; partial reset anchoring the next add on the
**exit price** at the tight floor when a layer scalped out (the `V2_Reload` vs
`V2_Add` distinction); full reset when the side went flat.

Read as a regime heuristic: widen while a trend runs, tighten on the first
unwind.

**Ruling: rejected on all three counts.**

- **Pacing renders widening redundant.** Both attack toxic accumulation in a
  trend. Pacing is superior because it keys on exogenous time — widening just
  stretches the grid and buys the whole move anyway.
- **The reload anchor is permanently dead.** Reconstructing "a scalp recently
  completed" would require parsing `DEAL_ENTRY_OUT_BY` history in `OnInit`,
  destroying Gate 1's comment-only reconstruction.
- **Depth-derived widening rejected for this cycle** even though it IS
  derivable and would preserve Gate 1 — because fixed geometry is what allowed
  simulator/EA parity, and reintroducing widening invalidates the entire sweep
  that selected the current coordinates.

**Geometry stays fixed at `add = 2.0 x width`.**

### 3.3 OPT/ALT slot allocation — OPEN, needs a decision

Gemini's directive was to dedicate next cycle's OPT/ALT slots to **pacing vs
no-pacing** rather than the current exit-elasticity test (5/5 vs 5/7).

**This is worth pushing back on before accepting.** Pacing is a *risk control*.
If it works you want it on both arms; you do not A/B a circuit breaker by
running half the fleet without one. Exit elasticity is an *edge question* the
two-slot design was built for, and it has produced zero data so far.

Both are legitimate uses of two slots. Decide deliberately rather than inherit.

---

## 4. JPY ANALYSIS — COMPLETE, BRANCH PARITY VERIFIED

Merged as `8e48094`. The JPY pip-value branch in `sim_costs.py` had never
executed against real data; it now has assertions.

**Broker facts, observed live:** USDJPY 153.630, AUDJPY 110.883, CHFJPY 189.850
— all three decimals. So a JPY pip is 0.01.

**Verified independently, not just via the test suite:**

| Check | Result |
|---|---|
| USDJPY `pip_size` | 0.01 |
| Pip value in JPY @ 0.01 lots | 10.0 |
| Pip value in USD @ rate 153.63 | **0.0651** (vs 0.10 for a USD-quoted pair) |
| AUDJPY, CHFJPY | identical 0.0651 — base currency does not leak |
| Direction | higher rate gives LOWER USD value (divide, not multiply) |
| 3-pip scalp net | **0.1353** (not 0.24) |

**The JPY branch works. No reroute of the ring needed.**

**Two findings from reading the code:**

1. **`point` in this codebase means "one pip in price units", NOT the MT5 tick
   size.** Confirmed by usage — all P&L math uses `pip_size`, and
   `grid_sim_v4_fixed.py:56` comments "point already IS 1 pip in this
   codebase's convention". So USDJPY's `point=0.01` is internally consistent
   despite three-decimal quotes. Misleading name; correct behaviour. Worth
   renaming someday.

2. **`conversion_pair` is metadata that nothing reads.** Callers pass
   `conversion_rate` explicitly. Adding AUDJPY/CHFJPY specs needed no logic
   change — but it means **something will have to supply the USDJPY series** the
   way `load_aligned_gbpusd_closes` supplies GBPUSD for EURGBP, before any JPY
   pair can be swept. That is a real gap, not yet addressed.

**Spreads recorded as PROVISIONAL:** USDJPY 1.1, AUDJPY 1.6, CHFJPY 2.6 pips —
single-observation Labor Day snapshots, commented as such in source. Must be
re-measured before informing any deployment decision.

---

## 5. THE RING — DESIGNED, DATA ROUTE PROVEN

Proposed eight-pair ring, every currency appearing in exactly TWO pairs, which
keeps the CAS cap readable:

**USD → CAD → NZD → AUD → JPY → CHF → GBP → EUR → USD**

`EURUSD, USDCAD, NZDCAD, AUDNZD, AUDJPY, CHFJPY, GBPCHF, EURGBP`

Plus five conversion series: `GBPUSD, USDJPY, USDCHF, NZDUSD, AUDUSD`.

**GBPUSD is dropped as a traded pair** — the ring routes GBP through CHF and
EUR. It stays as a data series because EURGBP needs it. Note this retires the
pair with the most validated geometry (a clean 5/5 in the confirmation sweep);
that is a real trade, not a free reorganisation.

All eight confirmed available in FTMO Market Watch.

### 5.1 DATA ROUTE — SOLVED (verified 2026-09-08 00:01)

**A live terminal only holds ~16 months of M5.** A chart-side `CopyRates`
script returned ~100,000 bars per symbol reaching back only to **2025.05.02**,
despite "Max bars in chart" set to Unlimited. Not a chart setting, not chunking.

**The Strategy Tester holds twelve years.** Same account, same broker. It
downloads its own history independently of the chart cache. That asymmetry is
why the original windows exist and why nobody could reproduce them today.

**PROVEN, single EURUSD run:**

    EURUSD: history synchronized from 2014.01.01 to 2026.09.04
    CopyRates copied=941172 err=0
    WROTE EURUSD_test.csv bars=867436 first=2015.01.02 09:00 last=2026.09.04 23:50
    Test passed in 0:00:06.693

**867,436 bars in under 9 seconds**, 48 MB, format byte-correct for
`load_mt5_csv`. History actually begins **2014.01.01** — 2015 was not the limit.

### 5.2 CONSEQUENCE: the multi-symbol exporter spec is UNNECESSARY

`CURSOR_TASK_tester_history_exporter.md` was written assuming the pull would be
slow and that thirteen Tester runs was a cost worth engineering away. At eight
seconds per symbol it is under two minutes total. **Do not build it.**

**Use `scripts/DataExporterEA.mq5` as-is.** Change `Symbol` and `InpOutFile` per
run, thirteen times. Cursor can drive the Tester, so this is a loop on its side
rather than an EA change.

`scripts/export_history.mq5` (chart-side) does NOT work for this purpose —
mark it superseded.

**Run settings that worked:** Expert `DataExporterEA`, M5, Custom period
2015.01.01 to today, Modelling "Open prices only", `InpFrom`/`InpTo` matching,
`InpOutFile` per symbol. Output lands in
`%APPDATA%\MetaQuotes\Terminal\Common\Files\` (FILE_COMMON).

**Do not remove the 4401 retry loop** in `DataExporterEA.mq5` — error 4401 is
"history updating", the Tester fetching on demand, and it is the mechanism.

### 5.3 SPREAD CONSTANTS — now answerable

The exported EURUSD 2015 bars show SPREAD values of 11-12 points = **1.1-1.2
pips**. `PAIR_SPREAD_PIPS["EURUSD"]` is **0.18** — a factor of six apart.

Either spreads compressed that much in a decade, or the constant is wrong. Those
constants trace to the inaugural commit `2dd60ed` (2026-07-18) and were never
measured. **The exported data now contains a per-bar SPREAD column, so this is
measurable rather than arguable.** Worth doing before any sweep uses them.

## 6. READ ACCESS — PUBLIC STATUS ENDPOINT (built 2026-09-08)

**URL:** `https://pipshed.com/api/g/k7m9p2x4q/status`

Unauthenticated, GET only, no parameters. Returns fxgrind instance and arm state
as JSON. Built so the engineering assistant can read live fleet state directly
instead of working from pasted screenshots.

**PASTE THE URL ONCE PER CONVERSATION.** The assistant can only fetch URLs that
appear in the current context. Memory carries project facts but not fetch
permissions, so a new session needs the URL pasted once, then it works
throughout.

**What it exposes:** per instance -- layer counts, geometry, health flags
(halted, recon_ok, invariant_ok, cap_blocked, peer_read_failed), scalps,
net_mtm, realised_pnl_today, api_count, cap leg NAMES, microstructure counters.
Plus the two arm aggregates.

**What it deliberately does NOT expose:** account balance, equity, margin,
drawdown, MAE, distance to floor, ANY v2 data, net exposure, best quotes, open
position detail, magic numbers, the API key. Fields are picked by an explicit
whitelist (`_PUBLIC_GRIND_INSTANCE_KEYS`), NOT by filtering the card dict -- so
a field added to the summariser later cannot leak by default.

Cache-Control no-store on both 200 and 500 paths; any exception returns a static
`{"error": "internal error"}` with no traceback.

**Security note:** the path is obscure, which is weak on its own. It is
acceptable only because the payload is structurally safe to leak. At 0.01 lots
on a demo, per-instance P&L is negligible. **If this strategy ever runs at size,
rotate the path** -- it is one string in `app.py`.

### 6.1 THE LIMIT: fxgrind publishes no price levels

The endpoint returns `open_layers_short: 1`. It cannot return "short at 0.85871,
exit resting at 0.85851, next add at 0.85931" -- because **the EA never sends
price levels**. Its payload is counts, geometry, health and P&L. No entry
prices, no exit prices, no resting-order levels.

By contrast v2 publishes `active_pods[symbol].layer_detail[]` with
`entry_price`, `exit_price_fixed`, `direction`, `lot_size`, `layer_index` and
`position_ticket` per layer -- which is why the v2 pod cards can show a position
table and the grind cards cannot.

**This is now a read-access limit, not just a dashboard cosmetic.** It bounds
what can be diagnosed without opening the terminal.

**The fix (post-demo EA work):** give fxgrind a `layer_detail` equivalent --
per layer its entry, exit, direction, lot and index -- plus its resting L0 and
add levels. Same information v2 already publishes, so the dashboard could then
use ONE code path for both rather than the parallel renderers built during this
session.

Note the design tension worth recording: **the flat payload was the right call
for state reconstruction and the wrong call for observability.** Reconstruction
wanted the book self-describing on the broker; the dashboard and any remote
reader want the EA to describe its own state. Those pulled in opposite
directions and only the first was designed for.

Related and unfixed: v2's `working_orders` is `{}` -- an EA-side decay -- so the
Best Working Quotes panel shows dashes for both families. Deferred until after
the demo.

---

## 7. OTHER OPEN ITEMS

| Item | Status |
|---|---|
| **ADR-124** | Still the only documentation gap. The shipped-but-undocumented fill-triggered re-centering write-up Gemini asked for on Saturday. |
| **Telemetry API key** | Unrotated, and visible in a screenshot shared during this session. Low impact (worst case is junk heartbeats) but worth doing. |
| **Holdout evaluation** | Gate 4 ratified `vaccine_rally` + `june_blowup` as out-of-sample. Selection is now locked, so it is runnable. Never run. |
| **Stress-gate re-run** | The 2004-2026 crisis sweep was never re-run under the corrected cost model. Scoped by Gemini as pair-admission work, not a precondition. |
| **Order virtualisation ("purgatory")** | Design note ratified to backlog; deferred until a second ring is greenlit. Key ruling: derive the virtual grid from fixed geometry, never persist it. **See 7.1 for the test strategy — it changes when this is worth building.** |
| **Property-based parity harness** | Ratified to the architectural backlog — fuzz price paths through both the Python core and an MQL5 runner, assert identical geometry. To be built before a second ring. |
| **v2 `working_orders` is empty** | EA-side decay. Best-quotes panel shows dashes. Deferred until after the demo. See 6.1. |
| **fxgrind publishes no price levels** | See section 6.1 -- now a READ-ACCESS limit, not just a dashboard cosmetic. Post-demo EA work. |
| **`tickets[]` unchecked ArrayResize** | Pre-existing, in `grind_recon.mqh`. Same class as the bug that crashed T19. Harmless today. |

### 7.1 PURGATORY: make the order limit an INPUT, and test at 20

Khalid's idea, and it resolves the hardest problem with virtualisation.

**The problem with testing at the real threshold.** FTMO's limit is 200 and the
project gate is 180. Reaching it needs a sustained multi-pair trend across six
instances — possibly weeks of waiting — and when it finally fires you would be
debugging demotion and revival logic under exactly the market conditions you
least want to be debugging anything.

**The fix: make the threshold an EA input, not a constant.** Set it to 20 for
testing. The mechanism then engages on the first busy afternoon, in ordinary
conditions, with an operator watching.

**Why this matters more than convenience.** At a 190 threshold the code path is
COLD — it runs almost never, and cold paths are where bugs live. At 20 it runs
constantly, which is the difference between a mechanism that has been tested and
one that has merely been written. Today's hedging defect is the case in point:
it survived design review, spec review and 231 unit tests, and was caught by the
first live fill.

**Two things it makes testable that otherwise are nearly impossible:**

1. **Demotion and revival under real price movement.** Orders leaving the book
   as they drift out of range and returning as price comes back, repeatedly,
   within a single session.

2. **The Gate 1 derivation.** Restart the terminal with orders in purgatory and
   confirm reconstruction rebuilds the virtual grid from fixed geometry alone —
   the design Gemini ratified. At a threshold of 20 that restart can be forced
   on demand rather than waiting for a coincidence.

**Caveat:** a low threshold means the live grid is thinner than intended, so
harvest during the test period is NOT representative. This is a mechanism test,
not a source of geometry conclusions. Do not read A/B results from a session
run at 20.

**Consequence for sequencing.** Purgatory was deferred until a second ring is
greenlit, on the grounds that the ceiling does not bind today. This changes that
slightly: it can be built and PROVEN early, at low risk, so that when breadth
does need it the mechanism is already exercised rather than freshly written
against a hard limit.

---

## 8. STANDING DISCIPLINES THAT EARNED THEMSELVES TODAY

- **Verify against committed source, never a self-report.** Three of today's
  bugs were found by reading code rather than trusting output.
- **Fail closed.** The invariant caught a fundamental design defect on the first
  live fill and halted rather than compounding it. Total cost: 23 cents.
- **Report, do not fix, when the cause is unclear.** Cursor stopping to ask
  about `net_mtm` composition prevented a wrong-but-plausible P&L field.
- **Never adjust an expected value to match output.** That converts a test into
  a description, and it is how the flat-pip bug survived months.
- **One owner for shared logic, plus a cross-implementation parity test.** The
  cost model diverged across fourteen files precisely because it had neither.

---

## 9. IF YOU DO NOTHING

The fleet runs. Six grind instances on ratified geometry with a working exit
mechanism, six v2 arms, full telemetry on pipshed, fail-closed halts on any
invariant violation. The demo expires on the 9th regardless.

Nothing in sections 3, 5, 6 or 7 blocks anything.
