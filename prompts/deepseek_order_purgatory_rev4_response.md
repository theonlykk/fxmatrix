# DeepSeek R1 Audit Report

## Internal Reasoning

We need answer user's prompt. Need act as quantitative auditor, output required format for each T-1..T-8, givens check, rev3 fixes check, premise verdict, override check last line. Need attack rev4 changes against actual appended source. Must write zero implementation code. We have memo and code. Need be exhaustive, concrete. Need identify exploits etc.

We need parse. This is a red team of rev4 design not yet implemented? Source appended is current baseline 9f67f0f probably before rev4. We must attack rev4 changes against actual appended source. The source does not include rev4 implementation; memo describes changes. We must reason feasibility and conflicts with actual code. Need identify bugs.

Key rev4 changes C1-C8. Threats T-1..T-8.

Need understand system deeply.

fxgrind passive limit MM on MT5 hedging. 14 EA instances share one account. Each instance magic/slot. Long/short side ladders. Each layer = position plus exit limit at entry +/- exit_pips (optionally carry shifted). Exit fill opens opposite position, then CloseBy nets original and opposite. Invariants rebuild book from broker tickets every tick; failure quarantines (3000ms, 3 checks) then halts. OnTick checks invariants before trading. OnTimer does no trading except telemetry/carry pass.

Rev4: keep K nearest exits per side resting; ranks K+1..K+H may rest; beyond K+H held and released as front exits fill and net. Exits allowed at free>=1; entries at free - resting_ENT >= 2+margin.

Threats:

T-1 TARGET PARITY. Can target() at release differ from what I6 later expects (ledger changes between release and next rebuild, sign guard flips, validated-shift deletion per G4)? Construct halt.

Need inspect code. I6 in recon: for each layer with exit coverage, computes shift = Grind_CarryShiftGetForRecon(position_id). This gets validated shift: fetch GV, if 0 returns 0; if outside bound deletes and returns 0. Then Grind_ReconExitMatchesEntry(entry, exit_target, exit_pips, point, is_long, shift, exit_is_filled). It computes expected = formula + shift. Require abs(diff)<=2*point+eps, unless exit_is_filled and favorable, then returns true if diff favorable (long diff>=0 etc). I6 only for layers with exit coverage? In actual recon code, I3 checks every layer has coverage; I6 checks every layer too in CheckInvariants (loop over all long_layers regardless coverage? Actually loop: for each long_count: if no position fail I3; if !coverage fail I3; then compute shift and I6 using exit_target. So currently I6 for every layer with position, requiring exit_target. With rev4 C7, I1 and I6 only for layers with an exit; I3 ranks 1..K etc. But actual code currently I6 for all layers. Rev4 would modify. We need consider design in memo. The source doesn't implement rev4, so we reason.

T-1: target() at release uses position's swap ledger as G3 does. Release places exit at target(layer) clamped, stores applied shift = X - formula via Grind_CarryShiftSet. I6 later computes expected = formula + validated shift. If shift stored, same as X - formula, so expected = X. But if validated-shift deletion per G4 happens (shift fails bound), I6 uses 0 => expected = formula, but exit_target = X (clamped shifted). If X != formula => mismatch. If exit is resting and not filled, I6 requires approx exact; fails. Is it quarantinable? I6_LONG_EXIT / I6_SHORT_EXIT not quarantinable? G8: I3 quarantinable; I5 and I7 not. But Grind_IsQuarantinableReason includes I6_LONG_EXIT_FILL_ADVERSE and I6_SHORT_EXIT_FILL_ADVERSE only, not I6_LONG_EXIT / I6_SHORT_EXIT. Wait in code: reasons are "I6_LONG_EXIT_FILL_ADVERSE" if exit_is_filled else "I6_LONG_EXIT". Grind_IsQuarantinableReason only includes FILL_ADVERSE, not plain I6_LONG_EXIT. Thus plain I6 failure -> GRIND_INV_HALT immediately, no quarantine. So if stored shift deleted due to bound, I6 fails and halts. That is T-2 also. For T-1, need sign guard flips or ledger changes.

Release at target(layer): target computed at release. It uses position's swap ledger and pending swap. It may apply sign guard. Store shift X - formula. Next rebuild I6 gets shift via Grind_CarryShiftGetForRecon, which validates bound and maybe deletes. Also I6 uses current exit_target from broker order price. So if no delete, shift = X - formula exactly, expected = formula + (X - formula)=X. So parity holds even if ledger changes, because I6 validates against stored shift, not recomputing ledger. Unless the stored shift is 0.0. Important: Grind_CarryShiftSet stores applied shift. If applied shift is exactly 0.0, then Get returns 0.0 and GetValidated returns 0.0 (treats as none). If X == formula (no carry shift/clamp), storing 0 is fine because expected = formula. If X != formula but applied shift rounds to 0? Unlikely if prices normalized to 5 digits, shift maybe smaller than half point? But target may be formula if carry off. If carry off, X likely formula; store 0. Fine.

But sign guard flips: target(layer) at release applies sign guard; if blocked, target = formula? Memo 4.1: target = carry-shifted theoretical price, or formula if carry off or sign guard blocks. So if sign guard blocks, X=formula (maybe clamped). Store shift X - formula = 0. I6 sees shift 0, expected formula. OK.

Ledger changes between release and next rebuild: not used by I6; I6 uses stored shift. So no issue unless validated shift deletion. The deletion is because bound check uses open_time and nightly_max_pips. For release shifts, if |shift| > max_nights*nightly_max*2, deleted. Need T-2.

T-2 SHIFT BOUND. With C2 and C5, find reachable release whose stored shift fails Grind_CarryShiftWithinBound. What does I6 then do? Is fatal or fixable by exempting release shifts or relaxing I6.

We need determine reachable release with large shift. Grind_CarryShiftWithinBound: open_time <=0 or nightly_max<=0 returns true. Else nights = (now - open_time)/86400, max_nights = nights+7; max_price = max_nights * nightly_max_pips * pip_size * 2.0. If |shift| <= max_price true. nightly_max_pips = max(abs(swap_long), abs(swap_short)) shifted with multiplier 3 via Grind_CarryShiftPips(swap_points,3,digits). That's per-night max pips? Actually 3x nightly. max_price = (nights+7)*nightly_max*2. That's huge: ~14 nights * 3*nightly? For typical swap maybe few pips. Could be e.g. 0.5 pips/night => nightly_max=1.5 pips, max_price=(0+7)*1.5*2=21 pips. Release clamp can be much larger: gap release clamp against target with G3 clamps. If price gapped far beyond target, clamp to ask + min_dist for long. X - formula could be hundreds of pips if target is far below ask. Example long layer entry 1.1000, exit formula 1.1050 (+50 pips). Market gaps up to 1.2000. target(layer) = 1.1050 (carry maybe 0). Release: price passed target. Clamp long exit: if theoretical <= ask+min_dist, out=ask+min_dist. So X ~ 1.2000 + tiny. Applied shift = X - formula = +950 pips. Bound max ~21 pips -> fails, deleted. Then I6 sees exit_target = 1.2000, expected = 1.1050 + 0 = 1.1050, diff +950 pips, exit resting not filled. I6_LONG_EXIT (not quarantinable) -> halt immediately. That's fatal? Fixable by exempting release shifts or relaxing I6 for resting exits beyond K+H? But memo open point asks. Is it reachable? Yes in gap/fast market. Need concrete tick/fill/cancel/netting sequence. Need also consider release only when rank<=K; held layer might have been held for long. On release after front exits fill/net, price may have moved far. For long, exit is sell limit above entry. If market rises far above target, a sell limit at target would be marketable and could fill immediately if placed? Actually clamp ensures passive: long exit is SELL LIMIT; to be passive it must be above current ask? Wait Grind_CarryClampLongExit: theoretical <= ask+min_dist => out=ask+min_dist. So for long exit (sell limit), it places at ask+min_dist, above ask, so not immediately marketable. But it's far above target? If target 1.1050, ask 1.2000, out=1.2000+min_dist > ask, so it's a sell limit above market. It won't fill until price rises further. I6 expected formula 1.1050, actual 1.2000, diff > tolerance. If shift deleted, halt. If shift kept, I6 passes because expected=formula+shift=1.2000. But shift fails bound because huge. So deletion causes halt. Is bound check done in I6? Yes Grind_CarryShiftGetForRecon calls GetValidated. So fatal if I6 not relaxed. Fixable: exempt release shifts or relax I6 for resting favourable. But note G4 says GetValidated deletes and returns 0 for stored shift that fails bound. If we exempt release shifts, need a different storage or mark. Design-level fix: store release shifts in a separate GV namespace exempt from bound, or have I6 tolerate favourable resting exits (rev3 relaxation). Memo says either. Is it fatal-to-premise? No, fixable. But wait if I6 tolerates favourable RESTING exit: For long, favourable means diff >=0? In Grind_ReconExitMatchesEntry, if exit_is_filled and long diff>=0 returns true. That means exit price is above expected (better for long? Actually long exit is sell limit; higher sell price is better). For resting, a favourable price is not necessarily bad: it's a limit that will fill at better price. But could hide an exit placed too far away, reducing fill probability. But capacity? It might be acceptable. So T-2 verdict EXPLOIT-FOUND, severity fixable-within-design? The threat asks "find a reachable release whose stored shift fails... What does I6 then do? Is it fatal or fixable". We can say EXPLOIT-FOUND (halt path) but fixable by exempting/relaxing. Need concrete sequence.

T-3 RANK BY ENTRY. Can entry order disagree with live exit price order in a way that makes a held exit fillable-but-absent, or a live exit unreachable for long periods, beyond memo s10's stated risk? Memo s10 already states accrued carry larger than add spacing could make held exit nearer to market than live one; price would pass it and release clamps passive. Rare; at or better than target. Need see if entry order can disagree with live exit price order. Ranking by entry price, not target. For longs, entry ascending (lowest entry first/nearest to market? Actually long ladder downward: L0 highest entry, deeper lower entry. Market below all. Nearest to market is lowest entry (deepest). So longs ascending entry = lowest entry first. Shorts descending entry = highest entry first. Formula exit = entry +/- exit_pips. If exit_pips same for all, order by entry equals order by formula. Carry shifts can change target. If carry is per-position, different layers may have different accrued swap (open times, swap points). A held layer with large positive carry shift could have target closer to market than a live layer with small carry. Ranking by entry ignores this. Then a held exit could be marketable (or nearly) while a live exit beyond it rests. But release only when rank<=K. If held layer is rank K+1, its exit is absent. Price could move to its target and pass; since no order, no fill. Later when released, clamp passive, so it won't fill at old target; it places at or better than target? For long, if price passed target upward, clamp places above ask, so it cannot fill at passed target; it's now further away. That's an opportunity cost, not invariant failure. Is it beyond stated risk? Memo s10 says "price would pass it and the release clamps passive. Rare; at or better than target." So already stated. But threat asks if can be worse: a held exit fillable-but-absent, or a live exit unreachable for long periods. Since ranking by entry ignores carry, a live exit with high rank might be far from market while a held exit is near. But if live exits fill first, ranks shift only after netting. Could a live exit be unreachable for long periods? Consider long ladder, K=1. L0 entry 1.1000, L1 entry 1.0900. Market 1.0950. Exits: L0 target 1.1050, L1 target 1.0950. Rank by entry ascending: L1 rank1 (nearest), L0 rank2. If carry shifts L0 target down to 1.0900? Wait long carry shift: formula_exit - direction*accrued_pips*pip. For long direction=1, positive accrued (long pays? Actually swap positive means credit? Grind_CarryShiftedExitPrice = formula - direction*accrued_pips. If accrued positive, long exit shifts down. Could move L0 target below L1 target. If L1 is held (rank2), L0 live at 1.1050? Wait ranking by entry: L1 entry 1.0900 rank1, L0 entry 1.1000 rank2. L1 held, L0 live. L0 target maybe shifted down to 1.0900 due to huge carry. Market 1.0950. L0 live sell limit at 1.0900 is below market (ask 1.0951), so it's marketable? Actually sell limit at 1.0900 when bid 1.0950 is below bid, would be immediate fill (but passive MM never crosses? The carry clamp would prevent: Grind_CarryClampLongExit checks theoretical <= ask+min_dist => out=ask+min_dist, so it wouldn't be below ask. So live exit would be clamped to ask+min_dist, not at 1.0900. So it's not unreachable? It's at market+min. But ranking by entry might put L0 live even though its target shifted near market; L1 held with target maybe further. Then L0 live fills quickly, netting, then L1 releases. Not stuck.

But consider shorts. Short entry descending: highest entry nearest. Carry for short: formula_exit = entry - exit_pips (since direction=-1: entry + -1*exit_pips = entry-exit_pips). Shifted: formula - direction*accrued = formula + accrued. Positive accrued shifts up (nearer? For short, exit is buy limit below market; higher exit is closer to market). If a held short has high carry, its target could be above a live short's target, but ranking by entry puts it later. Then live exit further below market may not fill for long, while held exit is nearer and would fill if present. This is stated risk. The threat asks beyond stated risk. Could it cause rank K hard invariant failure? Rank K is hard: rank K must have exit coverage. But if rank by entry, a layer at rank K might have its exit clamped far away due to carry? Wait carry shift can move target, but rank by entry. Rank K layer has live exit? In rev4, ranks 1..K must have exit. If rank K's exit is clamped far from market, it may not fill, but it's covered. Invariant only checks coverage, not fillability. So no halt. Could a held exit fillable-but-absent cause missed fills, not halt. The threat likely NO-EXPLOIT or DESIGN-UNSAFE? Need assess. The memo admits risk. The question: "Can entry order disagree with live exit price order in a way that makes a held exit fillable-but-absent, or a live exit unreachable for long periods, beyond memo s10's stated risk?" We need answer. We can say EXPLOIT-FOUND? But maybe not fatal. Need concrete.

We need also consider rank K hard: I3 coverage for ranks 1..K. If a rank K layer has no live exit due to release lag, quarantine absorbs. But if carry reorders mentally? Ranking is by entry, stable. So rank K is deterministic. Release when rank <= K. If a layer at rank <=K has no exit, release in same event. The release may fail to place (e.g., free<1? But commitment guard ensures free>=1 for exits; place may fail for other reasons). Then I3 fails, quarantine. Not new.

T-4 RECON FOR HELD LAYERS. Reinit: does populating exit_target from C1 need data not available at reconstruction (position swap unavailable, symbol info not ready, magic lock)? Any path that still yields 0.0?

Reconstruction in Grind_ReconstructState (OnInit) collects broker tickets (positions and orders). For layers without exit, rev4 C1 says populate exit_target from target(layer): formula + carry from position's swap ledger. But at reconstruction, for a position, can we read POSITION_SWAP? Yes if position selected. Grind_CarryExitShiftLayer uses Grind_CarryPositionSwapVolume which selects position by ticket and reads POSITION_SWAP, POSITION_VOLUME, POSITION_TIME. It also uses SymbolInfoDouble for tick value/tick size, swap points, point, digits. At OnInit, symbol info should be available. Magic lock: Grind_ReconstructState called after MagicLockClaim? In fxgrind OnInit: validates, claims magic lock, sets globals, then Grind_ReconstructState. So magic lock is held. Not issue.

However, note Grind_CarryExitShiftLayer uses g_grind_carry_test_pos_count for test; in production uses PositionSelectByTicket. At reconstruction, positions are selected? Grind_ReconCollectBrokerTickets selects positions, then later rebuild. The position selection state may be overwritten. But function selects by ticket itself. Should be fine.

But target(layer) as per G3 includes pending swap (tomorrow's swap) and sign guard. At reinit, server time, swap points available. So can compute. But is there a path yielding 0.0? If position swap unavailable? POSITION_SWAP should be available. But if Grind_CarryLedgerToPips fails (tick_value, tick_size, volume <=0), then Grind_CarryExitShiftLayer returns false; in target() we might fall back? Memo says target = carry-shifted theoretical price, or formula if carry off or sign guard blocks. If ledger conversion fails, maybe formula. So not 0.0. But what about layers with no position? Held layers always have position (by definition layer has position). So reconstruction can compute.

But wait: In actual recon, Grind_ReconEnsureLayer initialises exit_target = 0.0. Then when scanning tickets, for ENT position, it sets entry_price and position_id but does NOT set exit_target. For EXT order/position, it sets exit_target from ticket price. For held layers (no EXT), exit_target remains 0.0. Rev4 C1 says reconstruction must populate exit_target for held layers from target(layer). This is a change to recon. It requires calling carry computation during recon. Is there an issue with g_grind_recon_* not set? In OnInit, g_grind_recon_magic, slot, exit_pips, max_layers set before ReconstructState. So available. But target() needs magic? Grind_CarryExitShiftLayer needs magic for position selection. g_grind_recon_magic is set. So okay.

Potential path: If the position's swap ledger is unavailable because the position is not yet selectable? At OnInit, positions are selectable. Magic lock? Held.

Another path: If sign guard blocks, target = formula. That's not 0.0. So no 0.0. But maybe if entry_price is 0? No.

However, nuance: C1 says "Reconstruction must populate exit_target for held layers from it (never 0.0)." But reconstruction also uses I6? In actual recon, I6 for every layer (until rev4 C7). If exit_target for held layer is set to target(), and I6 uses shift from GV. If no shift GV exists for a held layer (because it was held and never had an exit, so no carry shift stored), Grind_CarryShiftGetForRecon returns 0. Then I6 expects formula, but exit_target = target(layer) which may include carry shift. So I6 would fail for held layers during reinit unless a shift GV is stored for them. Aha! This is important. C1 says populate exit_target from target(layer) (carry-shifted theoretical). But I6 for layers with an exit only (C7) — held layers have no exit, so I6 not checked. Wait C7: "I1 and I6 only for layers with an exit". So held layers have no exit; I6 skipped. So exit_target for held layers is not validated by I6. So no issue for reinit invariant. But then what is exit_target used for? Release uses target(layer) recomputed at release, not stored exit_target? Memo C1 says reconstruction populates exit_target for held layers from it. Release (4.3) places at target(layer). It may use stored exit_target? It says "Release: when a layer reaches rank <= K with no live exit and no filled-exit position, place its exit at target(layer)". So it recomputes target. The stored exit_target is for? Possibly for I6 if later released? But on release it stores shift. The stored exit_target might be used by some other logic? In current code, Grind_TryPlaceExitForLayer uses layer.exit_target. In rev4, release would need to set layer.exit_target to the computed target before placing. If reconstruction populated exit_target, release could use it. But if release recomputes, fine.

But T-4 asks "Any path that still yields 0.0?" Consider layer with no position? Not held. Consider position swap unavailable: if Grind_CarryPositionSwapVolume fails (e.g., PositionSelectByTicket fails due to symbol mismatch? Magic lock? But recon only collects positions for our magic and symbol; should select. However, during OnInit, the terminal might not have symbol info ready? Usually OnInit has symbol info. But if symbol trade mode not full? Grind_GuardsAllowTrading checks trade mode, but recon doesn't. If symbol info not ready, SymbolInfoDouble may return 0, causing Grind_CarryLedgerToPips fail, target falls back to formula (not 0). So no 0.0 unless formula computation fails? formula uses entry_price and exit_pips, point. If point=0, Grind_ExitPrice returns entry. Not 0. If entry_price=0? Could a position have price open 0? No.

But maybe C1 requires calling Grind_CarryExitShiftLayer which writes telemetry and increments counters (g_grind_carry_exit_skipped etc). During recon, those counters might be uninitialized? They are globals. Could cause side effects. Not 0.

Actually, there is a subtle path: Grind_ReconEnsureLayer sets exit_target=0.0. In rebuild, for ENT position, if there is also an EXT order/position, the EXT ticket sets exit_target to the exit price. If no EXT, rev4 must populate. But the rebuild function Grind_RebuildBookFromTicketsInner has access only to tickets, not to positions' swap ledger? It has ticket price and position_id. It could call target() but target() needs magic, symbol, etc. The function signature already has magic, slot, exit_pips, point. It could call. But it doesn't have g_grind_recon_magic? It does. So possible. But there is a catch: during reconstruction, the code collects positions and orders. For a held layer, there is a position ticket. To compute carry, it must select the position by ticket. But the position selection may be invalidated by the loop? No, it can select by ticket. So no 0.0.

But wait: What about a layer that is held because its exit was cancelled, but the position's swap ledger includes accrued swap. At reinit, the exit_target is populated from target(layer). But the shift GV may still exist from a previous release? If the layer was released earlier, then held again (cancel), the GV might still exist. On reinit, if exit_target is set to target(layer) (which recomputes carry from ledger), but the GV has an old shift. If the layer has no exit, I6 skipped. So no issue. But if later released, it stores new shift X - formula. The old GV is overwritten? Grind_CarryShiftSet sets. So okay.

Thus T-4 likely NO-EXPLOIT. But need check "position swap unavailable" – could PositionSelectByTicket fail if the position is not ours? Magic lock? No.

T-5 HOLD CANCEL (C4). Does the confirmed-gone search mis-identify the EXT position (OPT/ALT arms on the same symbol, CloseBy already done, comment truncation)? Double CloseBy?

C4: Hold cancel: DONE -> clear; not-DONE and live -> keep, retry; not-DONE and gone -> find an EXT position for this magic and layer index -> set exit_position_ticket and queue CloseBy; else clear.

We need assess search. In actual code, Grind_CancelPendingOrder returns true only on TRADE_RETCODE_DONE. If false, order may still exist (e.g., retcode not DONE but not removed) or may be gone if it filled. The logic: if not DONE, check if order still exists via Grind_SelectOurOrder? If exists, keep. If gone, search positions for this magic with comment role EXT and layer index. But how to identify the correct EXT position? Multiple instances share account, separated by magic. The search should filter by magic and slot? C4 says "for this magic and layer index". But there are two arms (OPT/ALT) possibly same magic? No, each instance has unique magic. Same magic means same instance. But within an instance, there are long and short sides. A layer index exists on both sides. An EXT position comment includes side letter. So searching "for this magic and layer index" without side could mis-identify: e.g., long layer 2 and short layer 2 both could have EXT positions. But a held exit cancel is for a specific side (long or short). The C4 description says "find an EXT position for this magic and layer index". It omits side. That could mis-identify. Also comment truncation: MT5 comment max length? MT5 comments can be up to 31 chars? The comment build includes slot, side, layer, role. If slot long, truncation could remove role? Need know GrindCommentBuild. Not in appended code. But comment truncation is a risk. If comment truncated, parsing may fail, so search by comment role EXT may not find the correct position. But the memo says comment role EXT and layer index. If parse fails, it may not find any, then clear tracker. That leaves exit_position_ticket 0 while there is an EXT position. Then next tick, recon will see an EXT position for that layer (I2? Actually if there is an EXT position and no layer? Wait the layer has a position (ENT) and an EXT position. If exit_order_ticket cleared and exit_position_ticket not set, recon will see both ENT and EXT positions. In rebuild, for EXT position, it sets exit_position_id. So recon will update. But the engine's in-memory layer might be cleared. Next recon rebuild? The in-memory state is only updated by OnTradeTransaction or recon? Actually recon is called every OnTick via Grind_CheckBookInvariants, which rebuilds into long_tmp and short_tmp but does NOT update g_grind_long/ short. It just checks invariants. So the engine's state is not updated from recon on every tick. Only OnInit reconstructs into g_grind_long/short. So if the engine clears the tracker but the EXT position exists, the engine may think no exit coverage for that layer. Then next OnTick, Grind_RetryMissingExits might place a new exit? Let's trace.

C4 is for hold cancel (cancelling an exit that is beyond K+H). The layer is held (no live exit desired). If the exit order was not-DONE and gone, it might have filled. If it filled, there is an EXT position. The engine should set exit_position_ticket and queue CloseBy. If search fails, it clears tracker. Then layer has no exit_order_ticket and no exit_position_ticket. But there is an EXT position open. The layer's original ENT position still open. This is a naked? Actually it has an EXT position, but engine doesn't know. On next tick, invariants check via recon: it will see ENT position and EXT position for the layer. In recon, for EXT position, it sets has_exit_position = true, exit_target = tickets[i].price. I3 coverage passes. I6? exit_is_filled=true. It will compute shift from GV. If no shift stored (since hold cancel didn't fill? Actually it filled; if it filled, there may be no shift GV unless it was set previously? If the exit was live and had a shift from carry pass, GV may exist). I6 may pass if favourable. But the engine's in-memory g_grind_long still has exit_position_ticket=0. Then on next OnTick, Grind_RetryMissingExits will see exit_order_ticket==0 and exit_position_ticket==0 and try to place a new exit. That would place a new exit order. Then recon will see ENT, EXT position, and new EXT order for same layer -> I2_LONG_EXIT_DUP, quarantinable? I2 is quarantinable. Quarantine 3s. But CloseBy not queued, so the EXT position remains. Could lead to halt if not resolved. Also if CloseBy is already done? The search should not find a position if CloseBy already done. But if CloseBy already done, the EXT position is gone, so search finds none, clear tracker. That's fine.

Double CloseBy: If the search finds an EXT position and queues CloseBy, but the original fill also queued CloseBy via Grind_HandleSideDealFill when the EXT fill was processed? Let's trace. When an exit limit fills, OnTradeTransaction DEAL_ADD triggers Grind_HandleSideDealFill. For EXT entry in, it finds layer by exit order or index, sets exit_order_ticket=0, exit_position_ticket=position_id, queues CloseBy. So normally the deal handler catches the fill and queues CloseBy. C4 is for the race where the cancel (hold) is processed in OnTick before the deal transaction? Actually OnTick calls hold cancel. If the exit order filled just before, the cancel may return not-DONE and gone. Then the deal transaction may not have been processed yet? OnTradeTransaction is asynchronous but should have arrived? The race: OnTick runs, sees layer beyond K+H, calls cancel. The order may have filled already, so cancel fails (not DONE, gone). The deal transaction for the fill might not have been processed yet (queued). C4 searches positions and finds EXT position, sets exit_position_ticket, queues CloseBy. Later OnTradeTransaction processes the fill, also finds layer (by exit order? exit_order_ticket is now 0, so it falls back to Grind_FindLayerByIndex(c_layer). It will find layer. It then sets exit_order_ticket=0, exit_position_ticket=position_id (same), and queues CloseBy again. Grind_QueueCloseBy checks for duplicate ticket1/ticket2 and returns if exists. So no double CloseBy. Good.

But mis-identification: search for EXT position by magic and layer index. If there are multiple EXT positions for same layer? Normally only one. But if the hold cancel is for a long layer 2, and there is also a short layer 2 EXT position (from a short fill), searching without side could pick the short one. Then it would set long layer's exit_position_ticket to the short position ticket, queue CloseBy with long ENT and short EXT. CloseBy on hedging account requires opposite positions? Actually CloseBy can close two positions on same symbol, one buy and one sell. Long ENT is buy, short EXT is buy? Wait: short side: entry is SELL position. Exit for short is BUY position. So short EXT is a BUY position. Long ENT is BUY. CloseBy requires one BUY and one SELL? In MT5 hedging, CloseBy closes one position by another, they must be opposite directions? Actually CloseBy closes a position by an opposite position on the same symbol. So two BUY cannot be closed by each other. The code checks sym != sym2 only, not type. Grind_ProcessCloseByQueue sends TRADE_ACTION_CLOSE_BY with position1 and position_by. If both are BUY, broker returns error. The queue retries up to 10, then if both still selectable, halts with GRIND_CLOSEBY_EXHAUSTED. So mis-identification could cause halt. Is this reachable? Need C4 search without side. The memo text: "find an EXT position for this magic and layer index". It doesn't mention side. But maybe the search is within the side's context? The hold cancel function likely operates on a specific side, so it could filter by side. The memo shorthand may omit it. As auditor, we should point out if the design as written is ambiguous. The threat asks: "Does the confirmed-gone search mis-identify the EXT position (OPT/ALT arms on the same symbol, CloseBy already done, comment truncation)? Double CloseBy?" We can say EXPLOIT-FOUND if the search doesn't filter by side/slot/comment role robustly. But we need base on actual code? The rev4 isn't in code. We can infer from C4 text. The memo says "find an EXT position for this magic and layer index". It does not say side. That's a design flaw. But maybe the function has access to side. The threat explicitly mentions OPT/ALT arms on same symbol. Wait OPT/ALT are different instances with different magic, so "for this magic" filters them. So OPT/ALT not issue. Same magic means same instance; but long and short sides same magic. So side is the issue. Comment truncation: MT5 comment field max length? If slot is long, comment might be truncated and role EXT cut off. But if the search uses position comment parse, it might fail. However, the position's comment is set from the order comment when it opened. If the order comment was truncated, the position comment is also truncated. GrindCommentParse may fail. Then search finds none -> clears tracker -> missing EXT position. That's a bug. But is it reachable? MT5 comments max length? According to MQL5, order comment can be up to 31 characters? Actually MqlTradeRequest.comment max length is 31 characters? Let's recall: MT5 comments are limited to 31 characters. The comment format "OPT-L02-EXT" is short. But slot might be longer. The code has InpSlot string, could be arbitrary. If slot long, truncation. The memo says "comment truncation" as a threat. So likely yes.

Double CloseBy: as above, duplicate queue prevented. But if search mis-identifies and queues CloseBy, and later the correct deal handler also queues CloseBy for the correct EXT position, there could be two different CloseBy tasks: one with wrong EXT, one with correct EXT. The wrong one might halt. So T-5 EXPLOIT-FOUND, fixable by filtering by side/slot and parsing robustly.

T-6 RANK-K HARD (C7). Front exit fills; netting on next OnTick; release in which event exactly? Construct a sequence where rank K stays uncovered past 3000 ms / 3 checks on a quiet symbol (OnTimer does no trading).

Rank K hard invariant: I3 coverage for ranks 1..K, rank K hard. In rev4, I3 for ranks 1..K. Quarantine 3000ms/3 checks. Need sequence where rank K has no exit for >3s/3 checks. Release happens when front exit fills and nets. Netting via CloseBy occurs in Grind_ProcessCloseByQueues at start of OnTick. After netting, layer is removed? CloseBy deal entry out_by triggers Grind_HandleSideDealFill for the original position? Let's trace. Exit fill: opposite position opened (EXT). Original ENT position still open. CloseBy queued. On next OnTick, Grind_ProcessCloseByQueues sends CloseBy. If DONE, broker closes both positions. A DEAL_ADD OUT_BY transaction occurs. OnTradeTransaction handles it: for the side, it looks for layer whose position_ticket == position_id (the original ENT). It marks processed, accumulates PnL, removes layer. So the layer is removed from side. Then ranks shift. The next released layer should be released in the same OnTick? The OnTick sequence: Grind_ProcessCloseByQueues at very beginning. Then invariant check. Then if not halted/quarantined, Grind_OnTickEngine. So if CloseBy completes synchronously? CloseBy send is asynchronous? In MT5, OrderSend for CloseBy returns DONE but the deal transaction may occur before or after. The code processes CloseBy queue at start of OnTick; if send returns DONE, it removes task. The actual positions may be closed by the time of the next tick. The OnTradeTransaction may be called asynchronously. So the layer removal might happen in OnTradeTransaction before next OnTick. But if OnTradeTransaction doesn't fire? It should. But consider: front exit fills; EXT position opened. CloseBy queued. Next OnTick: ProcessCloseByQueues sends CloseBy. If it succeeds, positions closed. OnTradeTransaction DEAL_ADD OUT_BY fires. Grind_HandleSideDealFill removes layer. Then in the same OnTick, after ProcessCloseByQueues, the code does invariant check. At that point, the layer may already be removed? OnTradeTransaction is called by terminal asynchronously, possibly during OrderSend? In MT5, OnTradeTransaction is called after the transaction is processed, often before OrderSend returns? Actually not guaranteed. But typically, OnTradeTransaction is called from the terminal's trade processing, not reentrant from OrderSend. It might be queued and called after OnTick returns. So within the same OnTick, g_grind_long still has the old layer. Ranks not shifted. So release of next layer might be delayed to next OnTick. That's one tick. On a quiet symbol, ticks may be sparse. 3000 ms may pass with fewer than 3 checks. Invariant check happens every OnTick. If no ticks for >3000 ms, quarantine may not progress. But quarantine requires 3 checks and 3000ms. If no ticks, no checks. The invariant failure is detected on a tick, then next tick may be >3000ms later. It needs 3 checks. If ticks are quiet, maybe 3 ticks over >3000ms? Actually GRIND_QUARANTINE_MIN_CHECKS=3. So need at least 3 OnTick calls with failure. On a quiet symbol, if front exit filled, there should be some ticks due to the fill? But after that, market quiet. The rank K layer remains uncovered because release didn't happen? Let's construct.

Scenario: K=1. Layers: L0 rank1, L1 rank2 (rank K? if K=1, rank K is L0). Wait K nearest exits per side. Rank K hard means rank K must have exit. If K=1, only rank1 must have exit. Suppose L0 rank1 has exit. L0 exit fills. It opens EXT position. CloseBy queued. The layer L0 still has ENT position and EXT position (coverage) until CloseBy removes it. So rank1 still L0 with coverage (exit_position_ticket). So I3 passes because coverage includes exit_position. After CloseBy, L0 removed, L1 becomes rank1. But L1 may have no exit (held). Release should happen in OnTickEngine when L1 reaches rank <=K. But if CloseBy completes and layer removed via OnTradeTransaction, and no OnTick occurs after that before next invariant check? Actually invariant check happens every OnTick. If layer removal happens in OnTradeTransaction (asynchronous), the next OnTick will check invariants before OnTickEngine. At that point, L1 is rank1 with no exit. I3 for rank1 fails. Then quarantine starts. Then OnTickEngine may run if not quarantined? Wait OnTick: first ProcessCloseByQueues, then invariant check. If invariant fails, QuarantineStep returns GRIND_INV_QUARANTINE (first time). Then code checks if g_grind_halted; no. Then if g_grind_quarantined: it calls Grind_RetryMissingExits and returns. So during quarantine, OnTickEngine does NOT run. It only retries missing exits. Grind_RetryMissingExits (rev4 restricted to ranks <= K) will try to place exit for L1. So release happens during quarantine via RetryMissingExits. That should fix within one tick. But if the release fails (e.g., free<1? But commitment guard ensures free>=1 for exits; however during quarantine, maybe free<1? The guard is supposed to ensure exits always find slot. But if free=0, Grind_TryPlaceExitForLayer fails. Then next tick retry. Quarantine can last up to 3000ms/3 checks. If ticks are sparse, but each tick retries. If release fails repeatedly for 3 checks over 3000ms, halt. Could release fail? Possibly if free=0. But commitment guard should prevent. However, note: OnTick order: ProcessCloseByQueues, then invariant. If invariant fails, it goes to quarantine branch and calls RetryMissingExits. But RetryMissingExits uses Grind_TryPlaceExitForLayer which places at layer.exit_target. If free<1, it may still attempt? The code doesn't check free in Grind_TryPlaceExitForLayer. The commitment guard described in C8 is not in actual code. Rev4 would add it. If free<1, place fails with 10040. Then quarantine. But guard should prevent free<1 for exits? It says exits allowed at free>=1. It doesn't guarantee free>=1 at all times, just that if free<1, no exit send. So if free=0, release fails. Could free=0? The account limit 200. If all slots used by positions and orders. The guard blocks entries when free - resting_ENT < 2 + margin. But exits are allowed at free>=1. If free=0, no exit can be placed. How could free become 0? If an entry fills, it consumes a pending order slot and adds a position, net change 0? Actually pending order -> position: OrdersTotal decreases by 1, PositionsTotal increases by 1, so free unchanged. Then the exit for that fill needs a new order: free must be >=1. The guard reserves slot via resting_ENT? Let's analyze C8 conservation. free = limit - positions - orders. resting_ENT = pending ENT orders (any magic). Entry send allowed when free - resting_ENT >= 2 + margin. When an entry is placed, orders +1, free -1. resting_ENT +1. So free - resting_ENT decreases by 2. When entry fills: order -1 (free +1), position +1 (free -1) => free unchanged. resting_ENT -1 => free - resting_ENT increases by 1. Then exit placed: order +1, free -1. Net after entry fill + exit place: free -1 (from initial before entry place? Let's do equation. Start free F, resting_ENT R. Place ENT: F' = F-1, R'=R+1, F'-R' = F-R-2. Fill ENT: F'' = F' (order -1, position +1), R''=R'-1 = R. F''-R'' = F-R-2+1 = F-R-1. Place EXT: F''' = F''-1 = F-1, R'''=R. F'''-R''' = F-R-1. So net change from start: free -1, resting_ENT same. So an entry+exit cycle consumes 1 slot permanently (the position remains). That's expected: each layer holds a position slot. The exit order is extra? Wait the exit order is a pending order that will later fill and open an opposite position, then CloseBy closes both. During the time exit is resting, it uses an order slot. So each layer uses 1 position + 1 exit order = 2 slots while waiting. When exit fills, it opens opposite position (+1 position) and exit order disappears (-1 order), net 0. Then CloseBy closes both (-2 positions), freeing 2 slots. So the layer uses 2 slots while open, then releases 2. The guard reserves 2 slots per entry via resting_ENT? Actually resting_ENT counts pending ENT orders. For each pending ENT, it reserves 2 slots (the position and its future exit). The condition free - resting_ENT >= 2 + margin. So when an entry is placed, it consumes 2 slots of "free after reserving". When it fills, the pending ENT is gone, so resting_ENT decreases by 1, freeing 2 reservation? Let's recalc: F - R >= 2 + margin. Place ENT: F-1, R+1 => F-R decreases by 2. Fill ENT: F unchanged, R-1 => F-R increases by 1. Then place EXT: F-1, R unchanged => F-R decreases by 1. Net after fill+exit: F-R decreases by 2? Start F-R. After place: -2. After fill: -1. After exit: -2. So net -2. That means each active layer (position + exit order) consumes 2 slots of F-R. When exit fills: order -1, position +1 => F unchanged, R unchanged. So F-R unchanged. Then CloseBy: positions -2 => F +2, R unchanged => F-R +2. So net back to 0. So conservation holds: the guard ensures that for each entry, there are 2 slots reserved until the layer is fully closed. So free should never go below 1 if the guard is enforced? Let's check worst case. The guard allows entry only if F - R >= 2 + margin. After placing entry, F-R >= margin. Then fill+exit: F-R >= margin - 1? Wait we computed net -2. Let's do precise:

Initial: F, R.
Condition to place ENT: F - R >= 2 + m.
Place ENT: F1 = F-1, R1 = R+1. F1 - R1 = F-R-2 >= m.
Fill ENT: order -1, position +1 => F2 = F1 (since -1+1=0 net? Actually F = limit - positions - orders. Order -1 => orders decrease => F increases by 1. Position +1 => positions increase => F decreases by 1. Net F unchanged. So F2 = F1. R2 = R1 -1 = R. F2 - R2 = F1 - (R+1-1) = F1 - R = (F-1) - R = F-R-1 >= m-1.
Place EXT: F3 = F2 -1 = F-2. R3 = R2 = R. F3 - R3 = F-2 - R = F-R-2 >= m-1? Wait condition for exit: allowed when free >=1, not based on R. So it can place if F2 >=1. If F-R >= 2+m, then F2 = F-1. Could F2 be 0? If F-R = 2+m, then F = R+2+m. F2 = R+1+m. Since R>=0, m>=? margin plausible 6-14. So F2 >= 1+m >0. So free >=1. So exit can place. After exit: F3 = F-2. Could F3 be 0? If m small? F3 = R + m. If R=0, F3 = m >=6. So free >0. So free never hits 0 if margin >0. But what if multiple entries? The guard ensures F-R >= 2+m. So free is at least R + 2 + m. Since R counts pending ENT of any magic. So free >= 2+m + R. So free is always >= 2+m. Wait, is that true? The condition is checked before placing an entry. But what if the account is already full from legacy positions/orders? The guard doesn't force exits. But if all entries are placed under guard, then invariant F - R >= 2+m holds? Let's check initial transition: legacy book may have F=0, R=0. Then guard blocks entries. Exits? Exits allowed at free>=1. If free=0, no exit. But how did free become 0? Legacy book. The transition T-8 deals with that. During normal operation after guard, F-R >= 2+m? Let's verify after each operation:
- Place ENT: F-R decreases by 2.
- Fill ENT: F-R increases by 1.
- Place EXT: F-R decreases by 1.
- Fill EXT: F unchanged, R unchanged => F-R unchanged.
- CloseBy: F increases by 2, R unchanged => F-R increases by 2.
- Cancel ENT: F increases by 1, R decreases by 1 => F-R increases by 2.
- Cancel EXT: F increases by 1, R unchanged => F-R increases by 1.
- Halt cancel ENT: same as cancel ENT.

So F-R can decrease by 2 on ENT place, then by 1 on EXT place, total -3 per layer cycle? Wait after ENT place -2, after fill +1 = -1, after EXT place -1 = -2. So each active layer reduces F-R by 2. When CloseBy, +2. So net 0. So F-R is conserved modulo active layers. The guard requires F-R >= 2+m before placing ENT. So after placing ENT, F-R >= m. After fill, F-R >= m+1. After EXT place, F-R >= m. So F-R never goes below m. Since m >=6, F-R >=6. Then free = F = (F-R) + R >= 6 + R >=6. So free >=6 always. Thus exits always have free>=1. So release cannot fail due to free. Good.

So T-6 quiet symbol: release should happen in quarantine via RetryMissingExits. But wait: RetryMissingExits in rev4 is restricted to ranks <= K. In actual code, Grind_RetryMissingExits loops all layers. Rev4 would restrict. So rank K layer without exit will be retried. So one tick should fix. How could it stay uncovered past 3000ms/3 checks? If the release place fails for another reason: e.g., exit_target <=0? If exit_target is 0.0 (T-4), Grind_TryPlaceExitForLayer places at 0.0, which will be rejected (invalid price) or placed? PlaceLimit with price 0.0 will fail. Then I3 continues to fail, quarantine, halt. So T-4 (0.0) can cause T-6. But if T-4 fixed, no.

Another reason: the layer's exit_target is stale and marketable? Grind_TryPlaceExitForLayer does not clamp. Release in OnTickEngine (rev4 4.3) places at target(layer) (clamped). But during quarantine, RetryMissingExits calls Grind_TryPlaceExitForLayer with layer.exit_target. If layer.exit_target was set during reconstruction to target(layer) but not clamped, and price has moved such that it's marketable, the place might fail with invalid price (e.g., sell limit below market). MT5 rejects non-passive limit orders? Actually a sell limit must be above current ask? If sell limit price is below ask, it's marketable and may be rejected with TRADE_RETCODE_INVALID_PRICE or placed as market? In MT5, limit orders must be at a distance from market; if too close, error. So if price moved, the stored exit_target may be marketable, place fails. Then quarantine continues. But OnTimer does no trading. On next tick, RetryMissingExits tries again with same stale exit_target, fails again. After 3 checks/3000ms -> halt. This is a real exploit! Let's construct: Reinit populates exit_target for held layer using target(layer) at reinit time. Later, front exits fill and net; layer reaches rank <=K. Release via OnTickEngine should clamp against target(layer) recomputed? Memo 4.3 says "place its exit at target(layer) (clamped, 4.5)". So OnTickEngine release should recompute target and clamp. But if the layer is uncovered during quarantine, OnTickEngine does not run; only RetryMissingExits runs. RetryMissingExits uses layer.exit_target, which may be stale and unclamped. If the market moved such that layer.exit_target is no longer passive, place fails. Then quarantine may halt. Is this reachable? Yes. Scenario: K=1, L0 rank1 live, L1 rank2 held with exit_target computed at reinit (or last release attempt) at say 1.1050 for long. Market is 1.1000. L0 exit fills, CloseBy nets. L1 becomes rank1 uncovered. On next OnTick, invariant fails I3. Quarantine starts. OnTick enters quarantine branch, calls RetryMissingExits. It tries to place L1 exit at layer.exit_target = 1.1050. But market has gapped up to 1.1100. For long, exit is SELL LIMIT. Current ask 1.1100. A sell limit at 1.1050 is below ask -> marketable -> MT5 rejects (or fills immediately?). If it rejects, RetryMissingExits fails. On next tick, same. If ticks are sparse or price stays above, after 3 checks/3000ms -> halt. This is a concrete T-6 exploit. But wait: does Grind_TryPlaceExitForLayer clamp? In current code, no. Rev4 C1 says reconstruction populates exit_target from target(layer). C5 says gap release clamp against target(layer) with G3 clamps. C7 says I3 coverage for ranks 1..K. It doesn't explicitly say RetryMissingExits clamps. Memo 4.9 says "Grind_RetryMissingExits before L0 placement in Grind_OnTickEngine, and restricted to ranks <= K." It doesn't mention clamping. In 4.3 release says "place its exit at target(layer) (clamped, 4.5) in the same event". So OnTickEngine release clamps. But RetryMissingExits during quarantine is a different path. If it uses Grind_TryPlaceExitForLayer with stale exit_target, it won't clamp. That could fail. However, maybe the layer.exit_target is updated on each tick? No. So this is a design flaw. Severity: fatal? It can halt. Fixable by making RetryMissingExits recompute/clamp target(layer) before placing, or by having OnTickEngine release before invariant check? But invariant check is before OnTickEngine. Actually OnTick order: ProcessCloseByQueues, then invariant check. If invariant fails, quarantine. Then OnTickEngine skipped. So release only via RetryMissingExits during quarantine. If RetryMissingExits doesn't clamp, halt. Fixable: use same release function in RetryMissingExits. So T-6 EXPLOIT-FOUND, fixable.

But wait: The threat says "Front exit fills; netting on next OnTick; release in which event exactly? Construct a sequence where rank K stays uncovered past 3000 ms / 3 checks on a quiet symbol (OnTimer does no trading)." Our sequence fits: netting happens, layer removed, rank K uncovered. Invariant fails. Quarantine. RetryMissingExits tries to place but fails due to stale marketable price. OnTimer doesn't trade. Quiet symbol: few ticks, but each tick tries and fails. After 3 checks/3000ms -> halt. Need ensure 3 checks occur. If symbol is quiet, ticks may be sparse. But if there are 3 ticks over 3000ms, quarantine halts. If no ticks, no checks, no halt, but also no trading. The invariant failure is detected on a tick. Then next ticks. If symbol is completely quiet, maybe no ticks for hours. Then quarantine doesn't progress, but also no trading. The threat specifies "past 3000 ms / 3 checks on a quiet symbol". So we need enough ticks to do 3 checks. A quiet symbol can still have a few ticks. So yes.

Also, another way: rank K hard invariant might fail because the release happens in OnTickEngine, but invariant check runs before OnTickEngine. So if L1 becomes rank K due to netting in ProcessCloseByQueues, the invariant check in the same OnTick will fail before OnTickEngine can release. Then quarantine branch calls RetryMissingExits. So release happens in quarantine, not OnTickEngine. That's by design? Memo 4.8 says "The release lag is one event; quarantine absorbs it". So it's expected that invariant fails for one check and release happens in quarantine. But if RetryMissingExits doesn't clamp, it may fail. Even if it clamps, it might succeed on first check, so quarantine releases next tick. That's fine.

T-7 COMMITMENT GUARD (C8). Conservation through place / fill / exit / fill / CloseBy / release / hold / halt-cancel. Find a transition that breaks it, or a race larger than a plausible margin (6-14) given one ENT send per instance per tick.

We need analyze C8. free = ACCOUNT_LIMIT_ORDERS - PositionsTotal() - OrdersTotal(). resting_ENT = pending orders whose comment parses with role ENT, any magic. EXIT send allowed when free >= 1. ENTRY send allowed when free - resting_ENT >= 2 + InpSlotMargin. At most one ENT send per instance per tick.

Potential break: resting_ENT counts parsed ENT comments of any magic. But what about pending orders that are not ENT? Exits (EXT) are not counted in resting_ENT. The guard for entries reserves 2 slots per pending ENT. But when an ENT is placed, it is a pending order. It will eventually fill and become a position. The exit for that layer will be placed after fill. The guard ensures that at the moment of entry placement, free - resting_ENT >= 2 + margin. This reserves 2 slots for the future position + exit. However, what if an ENT is cancelled? Then resting_ENT decreases, free increases, so reservation is released. Good.

What about multiple instances? resting_ENT counts any magic. So each instance sees all ENT orders. But "one ENT send per instance per tick". Could there be a race where multiple instances simultaneously check and send ENT, each seeing the same free and resting_ENT, and collectively overcommit? The guard is terminal-local, but each EA instance runs in the same terminal? Wait, 14 EA instances share ONE FTMO demo account. They are separate EA instances on different charts? In MT5, multiple EAs can run on different symbols/charts in same terminal? Or separate terminals? The memo says "Terminal-local, no server request." If they are on different charts in the same terminal, they share the terminal's global variables and can see each other's orders via OrdersTotal. But they run in separate threads? MT5 EAs on different charts run in the same terminal process, but OnTick events are sequential? Actually MT5 processes ticks for different symbols possibly in parallel? MQL5 programs on different charts run in separate threads, but trade operations are serialized by the terminal. However, global variables are shared? GlobalVariable* are terminal-wide. But the guard uses OrdersTotal() and parsing comments. There is no lock. If two instances evaluate the guard simultaneously, they could both see free - resting_ENT >= 2+margin and both send ENT. Then resting_ENT would increase by 2, but their check didn't account for the other's pending send. This is a race. The memo says "at most one ENT send per instance per tick" but not cross-instance atomicity. With 14 instances, they could all send in the same tick if each sees enough free. The margin (6-14) is supposed to absorb races. Is 14 > margin? If margin is 6-14, and 14 instances can send simultaneously, the race could be larger than margin. Let's quantify.

Suppose free - resting_ENT = 2 + margin. If margin = 6, then free - resting_ENT = 8. Each instance condition: >= 2 + margin = 8. So exactly 8. If 14 instances all check simultaneously and see 8, all 14 could send ENT. Each send consumes 2 reservation? Actually placing ENT: free - resting_ENT decreases by 2. If 14 send, total decrease = 28. Starting 8, it would go to -20. But they all send before seeing others' orders. So free could become 0 or negative. But the broker will reject once limit reached. The guard fails to prevent overcommit. The margin must be >= 2*(N-1)? Because each instance can send up to 1 per tick. If N instances, worst-case simultaneous sends = N. Each consumes 2 of free - resting_ENT. So need initial free - resting_ENT >= 2 + margin >= 2*N? Actually condition is >= 2 + margin. After N sends, free - resting_ENT decreases by 2N. To keep free >=1 for exits, we need free - resting_ENT after sends >= 0? Actually exits need free>=1. free = (free - resting_ENT) + resting_ENT. If free - resting_ENT goes negative, free could still be positive if resting_ENT is large. But the guard's purpose is to ensure exits find slots. The critical is free >=1. When an ENT is placed, free decreases by 1. So if multiple ENTs placed, free decreases. If free was 8, and 14 ENTs placed, free would go to -6 (impossible, broker rejects at 0). So some entries will be rejected. The guard is not a hard guarantee across instances. The memo says "except in a race larger than the margin". So they acknowledge race. Threat asks: find a transition that breaks it, or a race larger than a plausible margin (6-14) given one ENT send per instance per tick. With 14 instances, a simultaneous race of 14 is larger than margin 6-14? If margin is 14, need initial free - resting_ENT >= 16. 14 instances each consuming 2 => 28, so still larger. So race of 14 instances is larger than margin if margin < 28? Actually condition is free - resting_ENT >= 2 + margin. If margin=14, initial >=16. 14 sends consume 28, so overcommit by 12. So yes, a fleet-wide simultaneous ENT send can exceed the margin. Is it plausible? They run on different symbols, ticks arrive at different times. But if a major news event moves all pairs, they might all tick simultaneously. The guard is checked per tick per instance. Since MT5 processes ticks sequentially per terminal? Actually if multiple EAs are on different charts in the same terminal, they run in the same thread? I think MT5 runs each EA in its own thread, but OnTick calls are serialized by the terminal? Not entirely sure. MQL5 documentation: Each EA runs in its own thread. So they can run concurrently. Trade operations are synchronized by the terminal, but the guard check and send are not atomic. So race exists. The margin 6-14 is not enough for 14 instances. This is a design flaw. Severity: fixable by increasing margin to at least 2*N + safety, or by using a terminal-wide lock (GlobalVariable mutex) around ENT send. But the memo says "Terminal-local, no server request." A terminal-wide critical section could be done with GlobalVariable or EventSetMillisecondTimer? But no implementation code. So T-7 EXPLOIT-FOUND, fixable.

Also another break: resting_ENT counts parsed ENT comments of any magic. What about entries that are not yet placed but are in flight? MqlTradeRequest send is synchronous? OrderSend returns after server response. So once returned, order exists. The race is only during the window between check and send. So yes.

Another transition: halt-cancel. On halt, cancel own resting ENT. But if an ENT fills between halt and cancel, the position remains. The guard's reservation for that ENT is gone (since it filled), but the position and its future exit need slots. The memo 4.9 acknowledges residual race. The guard conservation: if an ENT fills, resting_ENT decreases by 1, free unchanged. So free - resting_ENT increases by 1. But the position now needs an exit. The guard condition for exits is free>=1. Since free unchanged, it might be 0? If free was 0, exit can't place. But as computed, if guard was enforced, free >= 2+margin. So free was at least 2. When ENT fills, free unchanged. So free >=2. So exit can place. So halt-cancel doesn't break if guard enforced.

What about release? Release places an exit for a held layer. This is an EXIT send, allowed when free>=1. It consumes 1 slot. But the layer already has a position and no exit. The guard's reservation for that layer? When the layer was originally created, its entry was placed under guard, reserving 2 slots. The exit was placed after fill, consuming 1 slot. But if the exit was held (cancelled), that slot was freed. Now release re-places it, consuming 1 slot again. The guard's accounting: F-R. When exit cancelled, F+1, R unchanged => F-R +1. When released, F-1, R unchanged => F-R -1. Net no change. So conservation holds.

What about hold cancel: cancel an exit beyond K+H. F+1. That frees a slot. Good.

What about CloseBy: closes two positions, F+2. Good.

So main race is cross-instance simultaneous ENT sends. Also, resting_ENT counts parsed ENT comments of any magic. But what if an ENT order comment is unparseable? Then it's not counted in resting_ENT. The guard might allow more entries than reserved. How could an ENT order be unparseable? Comment truncation or manual order. If a manual order with empty comment is resting, it won't be counted. Then free - resting_ENT is higher than actual reserved slots. This could break conservation. The memo says "resting_ENT counts parsed ENT comments of any magic". It doesn't count unparseable ENT. Are there unparseable ENT? The EA builds comments with GrindCommentBuild. If slot is long, comment might truncate. Then GrindCommentParse may fail. Those ENT orders would not be counted in resting_ENT. They still occupy slots. So free is reduced, but resting_ENT doesn't include them. The condition free - resting_ENT >= 2+margin uses free (which already subtracts all orders) and resting_ENT (only parsed ENT). So unparsed ENT orders make the guard more conservative? Wait: free = limit - positions - orders. If an unparsed ENT order is resting, it is subtracted in free. But it is not in resting_ENT. So free - resting_ENT is smaller than if it were counted? Actually if it were counted, resting_ENT would be larger, so free - resting_ENT would be smaller. So not counting it makes free - resting_ENT larger, allowing more entries. That's unsafe. Example: 10 unparsed ENT orders resting. free = 100. resting_ENT = 0 (if none parsed). Condition for new ENT: free - resting_ENT >= 2+margin => 100 >= 8, allowed. But the 10 unparsed ENT already reserved 20 slots conceptually. So overcommit. So unparsed ENT orders are a hole. The memo says "resting_ENT counts parsed ENT comments of any magic." It doesn't handle unparsed. Could be fixed by counting all pending orders with magic in our namespace? But other magics? The account is shared with other strategies? The memo says 14 EA instances share one account. They are all fxgrind. Their comments should be parseable unless truncation. So the guard should count all pending orders with magic in the fxgrind namespace, regardless of parse. But the design says parsed. This is a potential break. Severity: fixable by counting all pending orders with magic in fxgrind range, or by ensuring comments never truncate. T-7 includes this.

T-8 TRANSITION. First deploy on today's book (every exit live, 200 used, ~20 ENT resting). Trimming order, guard state, any halt during trim.

Today's book: 92 positions + 88 exit orders + 20 entry orders = 200. free = 0. Rev4 transition: legacy books with every exit resting are legal (extra exits beyond K+H) and trimmed by engine; guard blocks entries until free recovers. On first deploy, Recon will see all layers have exits. I3 for ranks 1..K? Rev4 C7: I3 coverage for ranks 1..K, rank K hard. But legacy book has every exit live. So ranks 1..K have exits. Rank K hard satisfied. I1 and I6 only for layers with an exit. All have exits. I6 checks all? C7 says I6 only for layers with an exit. So all layers checked. Will I6 pass? Legacy exits may not have carry shifts stored for all? They may have GVs from previous carry pass. If no shift, I6 expects formula. But legacy exits might be at carry-shifted prices if carry pass ran. If carry pass ran, it stored shifts. So I6 should pass. But what about layers that had carry shift but the GV was pruned? Grind_CarryPruneShiftGvs is called in OnInit after ReconstructState. Wait in fxgrind OnInit, order: Grind_ReconstructState(); then Grind_CarryPruneShiftGvs(InpMagic); If a legacy exit was carry-shifted but the GV was missing, I6 during ReconstructState would see shift=0 and compare to exit_target (shifted). If mismatch >2 points, I6 fails. But ReconstructState is called before Grind_CarryPruneShiftGvs. So if GV exists, fine. If GV was pruned previously? Prune deletes GVs not in book. So if position is in book, GV kept. So likely fine.

Trimming order: The engine will trim exits beyond K+H. On first OnTick, after invariant check, OnTickEngine runs. It will iterate layers? Rev4 C4 hold cancel: cancel exits beyond K+H. The order of operations: how are ranks computed? By entry price. The engine needs to identify which exits to cancel. If it cancels many at once, free increases. But during trim, guard blocks entries. Is there a halt? Potential issue: The invariant check runs before trimming. If legacy book has extra exits beyond K+H, is that an invariant failure? Rev4 says "legacy books with every exit resting are legal (extra exits beyond K+H) and trimmed by the engine". So no invariant failure for extra exits. I3 only ranks 1..K. So okay. But what about I1? I1 exactly one exit for every layer. Legacy has one exit per layer. So I1 passes. I9 soft for live exits beyond K+H: engine cancels, never halts. So no halt.

But transition could halt due to I6 for held layers? Legacy has no held layers; all exits live. So no.

What about the guard state? free=0. Exits allowed at free>=1. But free=0, so the engine cannot place any new exits. But there are no missing exits initially. As trimming cancels exits, free increases. Then releases can happen. But entries are blocked until free - resting_ENT >= 2+margin. resting_ENT = 20. free initially 0. So free - resting_ENT = -20, entries blocked. As exits are cancelled, free increases. But resting_ENT remains 20 until entries fill or are cancelled. The guard blocks entries. The engine might also cancel stale adds? Grind_EnsureAddNext may cancel stale add if label mismatch. But otherwise entries stay. So trimming reduces exits, freeing slots. Then releases may use slots. Is there a risk of halt during trim? Trimming cancels exits beyond K+H. These exits are for layers that are beyond K+H. The layers still have positions. After cancel, those layers have no exit. But I3 only for ranks 1..K. So if a cancelled layer is beyond K+H, it's not rank 1..K, so I3 doesn't require exit. So no I3 failure. But wait: ranks change as exits are cancelled? Ranking by entry price. Cancelling an exit does not change the layer's rank (which depends on entry price). The set of layers is fixed. So a layer beyond K+H remains beyond K+H. So I3 only checks ranks 1..K. Those are not cancelled. So no I3 failure. I9 soft for live exits beyond K+H: engine cancels them, never halts. So trim is safe.

But what if K+H is smaller than the number of layers, and the engine cancels exits for layers that are within K+H but beyond K? The memo says "ranks K+1..K+H may rest; beyond K+H are held". So it only cancels beyond K+H. So layers K+1..K+H keep their exits. Those layers have exits, so I3 for ranks 1..K still passes. Good.

Potential halt: During trim, if the engine cancels an exit and then later the layer becomes rank <=K due to netting? But cancel only beyond K+H. If a layer beyond K+H becomes rank <=K, that means front layers were removed, ranks shifted. But if it was beyond K+H, there are at least K+H layers before it. For it to become <=K, at least H layers must be removed. That takes time. The engine will release it when it reaches rank <=K. But during the interim, it may have no exit. That's fine because it's beyond K. But what if the engine cancels an exit for a layer that is currently beyond K+H, but the market moves and other layers net quickly, so it becomes rank K while the cancel is still pending? The cancel might complete, leaving it without exit at rank K. Then I3 fails, quarantine. But release will happen. The quarantine absorbs. So no halt if release succeeds. But if release fails due to stale exit_target (T-6), halt. So T-8 may trigger T-6.

Also, transition with 200 used: free=0. The guard says EXIT send allowed when free>=1. So no exit can be placed. But trimming cancels exits, free increases. However, the engine might try to release an exit for a layer that becomes rank <=K due to netting during trim. If free is still 0 (because no cancel has completed yet), release is blocked? The guard says exits allowed at free>=1. If free=0, the release cannot send an exit. Then I3 for rank K fails. Quarantine. On next tick, maybe a cancel completes, free becomes 1, release can place. So quarantine absorbs. But if the cancel is not processed? The cancel is a pending order removal. It should complete. But if the cancel fails? Grind_CancelPendingOrder returns true only on DONE. If it fails, it keeps the exit. So free doesn't increase. Then release cannot place. Quarantine may halt if free stays 0 for 3000ms. Could that happen? The exit cancellation is an EXIT send? Wait, cancelling an exit is a TRADE_ACTION_REMOVE. The guard only restricts EXIT send (placing exit) and ENTRY send. It doesn't restrict cancels. So cancels are always allowed. They should succeed unless the order is gone or frozen. If the order is beyond K+H, it's a resting exit. It should be cancellable. So free will increase. So quarantine should resolve.

But there is a subtle issue: The invariant check runs before the engine's trim. On the very first tick after deploy, the book has 200 used. The invariant check runs. Rev4 C7: I3 for ranks 1..K. Legacy all exits live. So I3 passes. I6 for all layers with exit. Does I6 pass? Need check carry shifts. If some exits are at carry-shifted prices but the GV is missing, I6 fails. Could GV be missing? On first deploy, the previous code (rev3 or older) may not have stored shift GVs for all exits. The carry pass stores shift only when it modifies an exit. If an exit was never modified (carry off or no shift), shift=0. I6 expects formula. If the exit is at formula, passes. If the exit was modified by carry pass, shift GV exists. So should pass. But what if the exit was modified by carry pass, then the GV was deleted due to bound? Then I6 would fail. But that would be a pre-existing issue. On first deploy, Grind_CarryPruneShiftGvs is called after ReconstructState. So if GV exists, kept. So no.

What about I1? Legacy has one exit per layer. Passes. I5, I7? Depth may be > max_layers? If legacy books exceed max_layers, I7 is not quarantinable (G8). That would halt immediately. But memo says "legacy books with every exit resting are legal (extra exits beyond K+H)". It doesn't mention depth > max_layers. If today's book has 12 layers on a side and max_layers=8, I7 would fail. But the memo s3 says P=8 for majors now, but today's book may have 12? The problem statement says "Book at 21:47Z: 92 positions + 88 exit orders + 20 entry orders = 200." It doesn't specify per instance depth. The memo s3 says "P=8, K=3, H=1" for 7 pairs. So max_layers likely 8. If legacy books have more than 8 layers, I7 fails. But that's a pre-existing issue (layer cap change is out of scope). The transition might need to handle I7. But the threat says "First deploy on today's book (every exit live, 200 used, ~20 ENT resting). Trimming order, guard state, any halt during trim." It might assume max_layers is respected. We can mention if legacy depth > max_layers, I7 halts immediately because I7 not quarantinable. But memo says I7 unchanged. However, s3 says "Lowering P is a separate ADR (I7 on reinit)." So if today's book has P=12 and max_layers=8, reinit would halt. But they probably will set max_layers high enough during transition? The memo doesn't say. We can flag it as a risk. But maybe not the focus.

T-8 asks trimming order. The engine must trim beyond K+H. It should cancel from deepest beyond K+H first? The order doesn't matter much. But if it cancels multiple, free increases. The guard blocks entries until free recovers. But there are 20 ENT resting. They are not cancelled automatically unless stale. So entries remain blocked. Is that a halt? No, just no new entries. The existing ENT orders may fill. When an ENT fills, it opens a position. That position needs an exit. But the guard says exit send allowed when free>=1. If free is 0 at the moment of fill, the exit placement will be attempted? In Grind_HandleSideDealFill, after ENT fill, it calls Grind_TryPlaceExitForLayer immediately. This is an EXIT send. Rev4 C8 says exits allowed at free>=1. But the code as written doesn't check free. Rev4 would add a guard? The memo says "Exits are allowed at free >= 1". So if free=0, the exit send should be blocked. But the ENT fill just happened: order -1, position +1, so free unchanged. If free was 0 before fill, it remains 0. So exit send is blocked. Then the layer has no exit. I3 for that layer? If it's rank <=K, I3 fails. Quarantine. If it's beyond K, it's held, so I3 not required. But the layer just filled, so it's a new layer. Its rank by entry? It could be rank <=K if it's near market. If it's an add, it's deeper, so rank maybe beyond K. If it's L0, it might be rank 1. So if free=0 and an L0 fills, I3 fails, quarantine. The guard should have prevented this by not allowing the ENT to be resting? But the ENT was already resting from before the guard was deployed. On transition, there are 20 ENT resting. They were placed before the guard. The guard only blocks new ENT sends. Existing ENT can still fill. If one fills when free=0, the exit cannot be placed. That could cause halt. This is a transition issue. The memo 4.9 says "On either halt path, cancel own resting ENT". But during transition, the guard is not enforced for existing ENT. They can fill and create naked positions. The invariant I3 will catch if rank <=K. Quarantine may not be able to place exit because free=0. But trimming is cancelling exits, which increases free. If free becomes >=1 before the ENT fills, then exit can place. But if the ENT fills before any exit is cancelled, free=0, exit blocked. Then quarantine. While quarantined, OnTickEngine does not run, so trimming doesn't happen? Wait, during quarantine, OnTick calls ProcessCloseByQueues, then invariant check, then if quarantined, it calls Grind_RetryMissingExits and returns. It does NOT call Grind_OnTickEngine. So the trim logic (hold cancel) is in OnTickEngine. If the instance is quarantined due to a missing exit, it will not trim. So free remains 0. RetryMissingExits tries to place the exit, but free=0, so it fails. After 3000ms/3 checks, halt. This is a real transition exploit! Let's construct:

Initial book: free=0, 20 ENT resting. Instance has some layers. An ENT fills (maybe L0 or an add). The fill handler tries to place exit. Free=0, so exit placement fails (or guard blocks). The layer now has no exit. If it's rank <=K (e.g., L0 is rank 1), I3 fails on next invariant check. Quarantine starts. During quarantine, OnTickEngine is skipped, so no trimming. RetryMissingExits tries to place exit, but free=0, fails. 3 checks/3000ms -> halt. The only way to free slots is to cancel some exits beyond K+H, but that's in OnTickEngine. So quashing. This is a fatal-to-premise? It's a transition issue. Fixable by allowing trimming during quarantine, or by prioritising cancels before invariant check, or by not blocking exits when free=0 if there are exits beyond K+H that can be cancelled. But the design says exits allowed at free>=1. So if free=0, no exit. The transition needs to handle. The memo says "legacy books with every exit resting are legal (extra exits beyond K+H) and trimmed by the engine; the guard blocks entries until free recovers." It doesn't mention quarantine blocking trim. So T-8 EXPLOIT-FOUND, fixable by moving trim before invariant check or allowing trim in quarantine.

Let's verify: OnTick order:
```
Grind_ProcessCloseByQueues
if(!halted) {
   check invariants
   quarantine step
   if halt -> halted
}
if(halted) return;
if(quarantined) {
   if(GuardsAllowTrading) Grind_RetryMissingExits
   return;
}
Grind_OnTickEngine(...)
```
So yes, if quarantined, OnTickEngine not called. Trim is in OnTickEngine (hold cancel). So quarantined instance cannot trim. If free=0, RetryMissingExits cannot place exit because guard blocks? Actually the guard is in C8 for EXIT send: allowed when free>=1. But the actual Grind_TryPlaceExitForLayer doesn't check free. Rev4 would add the guard. So if free=0, the exit send is not attempted. So naked layer remains. Quarantine halts. So this is a concrete exploit.

However, could the ENT fill happen while free=0? The ENT was resting before deploy. On transition, the EA starts. It does OnInit -> ReconstructState. Then OnTick. The first OnTick: ProcessCloseByQueues, invariant check. If invariants pass (legacy all exits live), then not quarantined. Then OnTickEngine runs. It will start trimming: cancel exits beyond K+H. This increases free. So trimming happens before any ENT fill? The ENT fill could happen on the same tick? Actually ENT fills are processed in OnTradeTransaction, which can occur at any time, even during OnTick? OnTradeTransaction is called asynchronously. It could happen before the first OnTick? The EA just started. Existing ENT orders might fill immediately if market is at their price. If an ENT fills before the first OnTick, the deal transaction handler runs. At that moment, free=0. It tries to place exit, fails. Then the first OnTick runs invariant check, sees missing exit, quarantines. Then trimming never runs. So yes, this is reachable. Even if the first OnTick runs before any fill, it will trim. But if an ENT fills on the very first tick before OnTick? The order of events: Terminal starts EA, OnInit. Then OnTick events. Existing pending orders can be filled by the broker when market opens or on tick. The fill transaction can arrive before the first OnTick? Possibly. So the race exists.

Fixable: On transition, the engine should immediately cancel excess exits (beyond K+H) in OnInit or before invariant check, or allow trim during quarantine. Or allow exits to be placed when free=0 if there are cancellable exits, by cancelling first. So T-8 EXPLOIT-FOUND, fixable.

Now we need also GIVENS CHECK: any of G1-G8 inaccurate, with file and function. We need verify each.

G1: `Grind_ReconEnsureLayer` initialises `exit_target = 0.0`; the rebuild copies `exit_target` from the exit order or exit position price (~852-919) and into the output (~1015). Check code: Grind_ReconEnsureLayer sets layers[layer_count].exit_target = 0.0; yes. In rebuild, for EXT order, sets long_scratch[idx].exit_target = tickets[i].price; for EXT position, sets exit_target = tickets[i].price. Output copies exit_target. Accurate.

G2: `Grind_AppendLayer` stores `Grind_ExitPrice(entry, exit_pips, _Point, dir)`, no carry. Check code: yes, side.layers[n].exit_target = Grind_ExitPrice(entry_price, exit_pips, _Point, dir); accurate.

G3: `Grind_CarryExitShiftLayer` (~800): reads position's swap ledger, computes accrued pips and theoretical price via `Grind_CarryShiftedExitPrice`, applies `Grind_CarrySignGuardBlocks`, clamps with `Grind_CarryClampLongExit`/`ShortExit`, modifies exit order, then stores `applied = new_exit - formula` with `Grind_CarryShiftSet`. Check code: yes. Accurate.

G4: `Grind_CarryShiftGetValidated` deletes and returns 0 for a stored shift that fails `Grind_CarryShiftWithinBound`. `Grind_CarryShiftGetForRecon` calls it for I6. Check code: yes. Accurate.

G5: `Grind_CarryExitPassBegin` includes only layers with `exit_order_ticket != 0` (~752, ~760). Check code: yes, it checks if layer.position_ticket == 0 || layer.exit_order_ticket == 0 continue. Accurate.

G6: `Grind_ReconExitMatchesEntry` tolerates a favourable price only when the exit is FILLED. Check code: yes, if(exit_is_filled) { if long diff>=0 return true; if short diff<=0 return true; } then return abs(diff)<=2*point+eps. Accurate.

G7: `Grind_CancelPendingOrder` returns true only on TRADE_RETCODE_DONE. `Grind_HandleSideDealFill` queues CloseBy on an EXT fill; halted instances skip it (~1161). Check code: Grind_CancelPendingOrder returns (res.retcode == TRADE_RETCODE_DONE). Grind_HandleSideDealFill: for EXT entry_type IN, it queues CloseBy. Halted instances skip it: in Grind_OnTradeTransactionEngine, if(g_grind_halted) return; before handling. Accurate.

G8: I3 is quarantinable; I5 and I7 are not (grind_quarantine.mqh). Check code: Grind_IsQuarantinableReason includes I3_LONG_NAKED, I3_SHORT_NAKED. Does not include I5 or I7. Accurate.

So all G1-G8 accurate. No counterexample.

REV 3 FIXES CHECK: for rev 3 T-1, T-2/T-8, T-4, T-5, T-6, T-7, T-9 -- does rev 4's fix hold? One line each.

We need know what rev3 T-1 etc were. From memo s7: R3 T-1 Rank-K miss hides indefinitely -> Accepted. Rank K hard (4.8). Rev4 fix: rank K hard. Does it hold? We found T-6 exploit where rank K can stay uncovered past quarantine due to stale exit_target in RetryMissingExits. So rev4 fix for R3 T-1 does NOT fully hold: rank K is hard, but release can fail during quarantine if RetryMissingExits doesn't clamp. So one line: Fails if RetryMissingExits uses stale unclamped exit_target; otherwise design is correct.

R3 T-2, T-8: Held layers have exit_target 0.0; no carry in AppendLayer. Accepted, fixed by 4.1. Does fix hold? C1 populates exit_target for held layers. But as we noted, if release recomputes target, it's fine. However, if RetryMissingExits uses stored exit_target, it may be stale. But the fix for 0.0 is to populate at reconstruction. Does it always populate? T-4 says no path to 0.0. So fix holds? But wait: C1 says reconstruction must populate exit_target for held layers from target(). Does the actual recon code have access to position swap? Yes. So fix holds for 0.0. However, if the layer is held and later released, the stored exit_target might be stale. But that's a different issue. So R3 T-2/T-8 fix holds (no 0.0).

R3 T-4: Hold cancel racing a fill strands tracker. Accepted. Confirmed-gone handling 4.3. Does fix hold? C4 handles confirmed-gone. But as we found in T-5, the search may mis-identify EXT position if not filtered by side. So fix is incomplete. One line: partially holds; mis-identification risk remains if search omits side.

R3 T-5: Clamp on raw target vs shifted I6. Accepted. Clamp against target + stored shift (4.5). Does fix hold? C5 clamps against target and stores shift. But T-2 shows shift bound can delete stored shift causing I6 halt. So fix fails for large gap clamp. One line: fails when stored release shift exceeds bound; I6 then halts.

R3 T-6: Carry reorders ranks; no tie-break. Accepted. Rank by entry, tie by index (4.2). Does fix hold? Ranking by entry is stable. Carry never reorders. Fix holds for rank stability. But T-3 shows fillability mismatch remains, which is a stated risk. So fix holds for its purpose (no churn).

R3 T-7: Carry pass skips held exits. Accepted, verified (grind_carry.mqh ~752). Carry read at release from ledger (4.6). Does fix hold? Yes, carry pass unchanged for live exits; held get carry at release. Fix holds.

R3 T-9: Formula not a worst case. Accepted. Formula demoted to sizing; guard enforces (s3, 4.7). Does fix hold? Guard enforces capacity. But T-7 shows cross-instance race can break guard. So fix fails for fleet-wide simultaneous sends. One line: fails under cross-instance race larger than margin.

R3 override: Not fixable without new state. Rejected as to state: no new persistent state; ledger and shift GV already exist. Does fix hold? Rev4 uses existing state. Yes, holds.

Now we need PREMISE VERDICT and OVERRIDE CHECK.

We need decide if any finding kills premise. The premise is the order purgatory design with exit queue and commitment guard. Our findings: T-2 shift bound halt, T-5 mis-identification, T-6 stale exit_target in quarantine, T-7 cross-instance race, T-8 transition quarantine blocks trim. All are fixable within design (exempt release shifts, filter side, clamp in RetryMissingExits, increase margin/lock, trim before invariant or during quarantine). So premise not dead. But we must be honest: T-8 could be fatal if not handled, but fixable. The instruction: "Do not declare the premise dead for an issue you rate fixable-within-design." So we rate all fixable, so premise verdict: survives, with required fixes.

We need output format exactly. For each threat T-1..T-8: VERDICT, LOAD-BEARING CLAIM, MINIMAL REPRO/MECHANISM, SEVERITY, IF FIXABLE smallest change. Then GIVENS CHECK, REV 3 FIXES CHECK, PREMISE VERDICT, OVERRIDE CHECK last line.

We must write zero implementation code. So no code snippets. We can reference functions. Need be concise but exhaustive.

Let's flesh out each threat.

T-1: VERDICT: NO-EXPLOIT? Or EXPLOIT-FOUND? The threat asks: Can target() at release differ from what I6 later expects (ledger changes between release and next rebuild, sign guard flips, validated-shift deletion per G4)? Construct the halt.

We reasoned: ledger changes don't matter because I6 uses stored shift. Sign guard flips don't matter because if blocked, shift=0 and target=formula. Validated-shift deletion is the only path: T-2. So T-1 itself (parity) is NO-EXPLOIT except the deletion case which is T-2. But the threat includes validated-shift deletion. So we can say EXPLOIT-FOUND via G4 deletion, but that's the same as T-2. To avoid double-count, we can say T-1: NO-EXPLOIT for ledger/sign flips; the only parity break is validated-shift deletion, which is T-2. Or we can rate T-1 EXPLOIT-FOUND and reference T-2. The required format wants for each threat. Let's make T-1: NO-EXPLOIT (parity holds because I6 uses stored shift; ledger and sign guard flips don't affect expected). Then T-2: EXPLOIT-FOUND (deletion breaks parity and halts). That seems clean.

T-2: EXPLOIT-FOUND. Concrete: long layer entry 1.1000, exit_pips 50, formula 1.1050. Market gaps up to 1.2000. Layer held beyond K+H. Front exits fill and net until layer reaches rank <=K. Release computes target=1.1050 (carry 0). Clamp long: theoretical <= ask+min => out=1.2000+min. Store shift = ~950 pips. Grind_CarryShiftWithinBound: open_time recent, nightly_max maybe 1-2 pips, max_price (0+7)*nightly*2 ~14-28 pips. 950 > max. Grind_CarryShiftGetValidated deletes and returns 0. Next rebuild I6: expected = formula + 0 = 1.1050, exit_target = 1.2000+min. diff >2 points. exit_is_filled=false. I6_LONG_EXIT not in quarantinable list -> halt immediately. Severity: fatal-to-premise? The halt is fatal to instance but fixable. Severity: fixable-within-design. Smallest change: exempt release clamps from bound (separate GV or mark) or relax I6 to tolerate favourable resting exits. Memo already open point.

T-3: VERDICT: NO-EXPLOIT? Or DESIGN-UNSAFE? The threat asks if entry order can disagree with live exit price order making held exit fillable-but-absent or live exit unreachable beyond memo s10. We can say DESIGN-UNSAFE? Let's analyze more carefully. Is there a way for a live exit to be unreachable for long periods? Ranking by entry means the live exits are the K nearest by entry. But carry can shift their targets. Suppose a live exit has a large adverse carry shift, moving its target far from market, while a held exit has favourable carry shift moving its target near market. The live exit is rank <=K, so it rests. The held exit is rank >K, so it's absent. Market moves to the held exit's target, but no order there. The live exit is far away and won't fill. The held exit won't be released until the live exit fills and nets. But the live exit may never fill because it's far away. This is a deadlock! Let's construct:

Long side. K=1. L0 entry 1.1000, L1 entry 1.0900. Ranking by entry ascending: L1 rank1 (lower entry), L0 rank2. So L1 is rank1, must have live exit. L0 is rank2, held (if H=0). Carry: L1 has adverse carry (swap negative? For long, formula = entry + exit_pips. Grind_CarryShiftedExitPrice = formula - direction*accrued. If accrued negative (long pays), shift is positive, moving exit up, further from market? Wait long exit is sell limit above entry. Market is below. If exit shifts up, it's further above market, harder to fill. L0 has positive carry, shift down, moving exit closer to market. So L1 live exit is at say 1.1050 (up from 1.0950), L0 held exit target at say 1.0850 (down from 1.1000). Market is 1.0900. L1 live exit at 1.1050 is far above. L0 held exit target at 1.0850 is below market. But wait: if L0 target is below market, then a sell limit at 1.0850 would be marketable (below ask). The clamp would prevent placing it there; it would be placed at ask+min. So when released, it would be placed above market. But the point is: the layer that would fill first (L0) is held, while the live exit (L1) is far away. The live exit won't fill until price rises to 1.1050. If price instead falls to 1.0850, L0's target is passed, but L0 is held, so no fill. The live exit L1 is above, not filled. So no netting, no rank shift, L0 remains held. The market could oscillate below L0's target, and L0 never gets released because L1 never fills. This is a deadlock/unreachable live exit. Is this beyond memo s10? Memo s10 says "Accrued carry larger than add spacing could make a held exit nearer to market than a live one; price would pass it and the release clamps passive. Rare; at or better than target." It acknowledges the held exit nearer to market. But it doesn't mention the consequence: the live exit is further away and may not fill, so the held exit is never released. The memo says "price would pass it and the release clamps passive." That implies price passes the held exit's target, but since it's held, nothing happens. The release only happens when the live exit fills. So if the live exit is far away, the held exit stays held indefinitely. That seems like a design flaw beyond the stated risk. The threat asks: "Can entry order disagree with live exit price order in a way that makes a held exit fillable-but-absent, or a live exit unreachable for long periods, beyond memo s10's stated risk?" Yes, the live exit can be unreachable for long periods because the held exit (which is nearer) cannot fill. This could cause the ladder to be stuck: no exits fill, no new releases, capacity used. It doesn't halt invariants, but it defeats the purpose. Is it fatal-to-premise? It's a performance/opportunity issue, not a halt. But could it cause halt? If the live exit is far away, the layer rank K is covered. I3 passes. So no halt. The premise is still functional, just inefficient. Severity: fixable-within-design? Fix: rank by target instead of entry, or rank by min(entry price, target) with tie-break, or allow release of held exit if its target becomes nearer than a live exit beyond K+H? But ranking by target causes churn (rev3 T-6). The memo chose entry to avoid churn. This is a trade-off. They might accept the risk. The threat asks if beyond memo s10. Memo s10 only says price would pass it and release clamps passive. It doesn't say the live exit is unreachable. I think we should mark T-3 as EXPLOIT-FOUND (design-unsafe for fill ordering) but severity fixable-within-design? Or DESIGN-UNSAFE? The prompt says "Do not declare the premise dead for an issue you rate fixable-within-design." If we rate it fixable, we can say EXPLOIT-FOUND, severity fixable. Fix: rank by target but use hysteresis/deadband to avoid churn, or rank by entry for release but allow a held exit to be released early if its target is more passive than the current rank K live exit? That's a design change. We can say fixable by ranking on target with a stable tie-break and carry-shift deadband, or by permitting a held exit to become live if its target crosses inside the live rank K exit. But that's not the chosen design. Since the memo already notes the risk, maybe it's accepted. The threat says "beyond memo s10's stated risk". We can argue it is beyond because s10 only mentions the held exit being passed, not the live exit being unreachable. So we can mark EXPLOIT-FOUND, severity fixable-within-design (e.g., use target for ranking with deadband). But we must not propose remedies the EA cannot execute. It can execute ranking by target. So okay.

T-4: NO-EXPLOIT. We need provide claim and mechanism why no 0.0. Load-bearing claim: grind_recon.mqh : Grind_ReconstructState / Grind_RebuildBookFromTicketsInner : C1 requires calling target() which uses PositionSelectByTicket and POSITION_SWAP; available at OnInit after magic lock. Minimal repro: none; paths: position swap unavailable -> falls back to formula; sign guard blocks -> formula; no path yields 0.0. Severity: none.

T-5: EXPLOIT-FOUND. Mis-identification. Concrete: hold cancel for long layer 2 fails cancel (order filled). Search positions for magic and layer index without side. Finds short layer 2 EXT position (BUY). Sets long layer exit_position_ticket to short position. Queues CloseBy with long ENT (BUY) and short EXT (BUY). CloseBy send fails (same direction). Retries 10 times, both still selectable -> GRIND_CLOSEBY_EXHAUSTED halt. Or if comment truncated, search fails, clears tracker, leaves EXT position, next tick RetryMissingExits places new exit -> I2 DUP quarantine. Severity: fixable-within-design. Fix: search filter by side letter and slot in comment; if parse fails, use deal history or match position ticket from the failed cancel's order? Actually the order ticket is known; the fill deal has DEAL_ORDER = order ticket. Could search history for deal with that order to get position ID. That's robust. Smallest change: on confirmed-gone, query history for the deal by order ticket to get position ID, instead of searching comments.

T-6: EXPLOIT-FOUND. Rank K uncovered past quarantine due to stale exit_target in RetryMissingExits. Concrete: K=1, L0 live, L1 held. L0 exit fills, CloseBy nets, L0 removed. Next OnTick: invariant check sees L1 rank1 no exit -> I3 fails, quarantine. OnTick enters quarantine branch, calls Grind_RetryMissingExits. L1.exit_target was set at reinit to say 1.1050 (long). Market gapped to 1.1100. Sell limit at 1.1050 is below ask -> invalid. Place fails. Next ticks repeat. After 3 checks/3000ms -> halt. OnTimer does no trading. Severity: fixable-within-design. Fix: make RetryMissingExits recompute target(layer) and clamp (same as release 4.5) before placing. Also ensure exit_target updated on each release attempt.

T-7: EXPLOIT-FOUND. Cross-instance race. Concrete: 14 instances all evaluate guard simultaneously. free - resting_ENT = 2 + margin (e.g., 8). Each sees condition true and sends one ENT. 14 sends consume 28 slots of free - resting_ENT. Broker accepts until 200 limit; some entries fill, exits may be blocked. Or unparsed ENT orders not counted in resting_ENT, allowing overcommit. Severity: fixable-within-design. Fix: increase InpSlotMargin to at least 2*N + safety (e.g., 28+), or implement terminal-wide critical section around ENT send using GlobalVariable lock; and count all pending orders with magic in fxgrind namespace, not only parsed ENT.

T-8: EXPLOIT-FOUND. Transition quarantine blocks trim. Concrete: initial free=0, 20 ENT resting. An existing ENT fills before first OnTickEngine trim. Deal handler tries to place exit; free=0 so guard blocks/fails. Layer naked. First OnTick invariant check sees I3 fail (if rank<=K). Quarantine starts. During quarantine, OnTickEngine (which contains hold cancel trim) is skipped. RetryMissingExits cannot place exit because free=0. 3 checks/3000ms -> halt. Severity: fixable-within-design. Fix: perform trim (cancel exits beyond K+H) in OnInit after ReconstructState, or allow OnTickEngine trim during quarantine, or prioritize cancels before invariant check. Also ensure guard allows exit placement when free=0 if there are cancellable excess exits.

Now GIVENS CHECK: All G1-G8 accurate. We can list one line each or say all accurate. The prompt says "GIVENS CHECK: any of G1-G8 inaccurate, with file and function." So we can say none inaccurate, and optionally brief verification.

REV 3 FIXES CHECK: One line each for rev3 T-1, T-2/T-8, T-4, T-5, T-6, T-7, T-9. We need map to our findings.

Rev3 T-1 (Rank-K miss hides indefinitely): Rev4 makes rank K hard. Does fix hold? We found T-6 exploit where rank K can stay uncovered past quarantine due to stale exit_target in RetryMissingExits. So: Does not fully hold — rank K hard but release path in quarantine can fail on stale unclamped exit_target (T-6).

Rev3 T-2/T-8 (Held layers exit_target 0.0): Fixed by 4.1. Does fix hold? C1 populates exit_target for held layers; no path to 0.0 found (T-4). So: Holds for 0.0, but stored target can become stale (T-6).

Rev3 T-4 (Hold cancel racing fill strands tracker): Fixed by C4. Does fix hold? Partially — C4 handles confirmed-gone, but search can mis-identify EXT position if not side-filtered (T-5).

Rev3 T-5 (Clamp on raw target vs shifted I6): Fixed by C5 (clamp against target + stored shift). Does fix hold? Fails when stored release shift exceeds bound; G4 deletes it, I6 halts (T-2).

Rev3 T-6 (Carry reorders ranks): Fixed by C3 (rank by entry). Does fix hold? Holds for rank stability; but entry-based ranking can make live exit unreachable while held exit is nearer (T-3), which is a fill-ordering risk beyond s10.

Rev3 T-7 (Carry pass skips held exits): Fixed by C6 (carry at release from ledger). Does fix hold? Holds.

Rev3 T-9 (Formula not a worst case): Fixed by C8 commitment guard. Does fix hold? Fails under fleet-wide simultaneous ENT sends or unparsed ENT orders (T-7).

Now PREMISE VERDICT. The premise: order purgatory with K nearest exits and commitment guard. Our findings are all fixable within design? T-2: fixable by exempting release shifts or relaxing I6. T-3: fixable by ranking on target with deadband. T-5: fixable by robust history lookup. T-6: fixable by clamping in RetryMissingExits. T-7: fixable by larger margin/lock and counting all ENT. T-8: fixable by trimming in OnInit/quarantine. None require abandoning the design. So premise verdict: survives, not dead, but requires the listed fixes before implementation. We should say no finding kills the premise.

OVERRIDE CHECK (last line): does any finding kill the premise, or are all fixable within it? Last line should be exactly that. We'll say: "OVERRIDE CHECK: No finding kills the premise; all identified failure modes are fixable within the design by the stated minimal changes." That should be the last line.

We need ensure no implementation code. We can mention function names and design-level changes, but not write code.

Now, let's double-check T-1. Is there any other target parity issue? Consider I6 uses `Grind_CarryShiftGetForRecon` which calls `Grind_CarryShiftGetValidated`. That function deletes shift if fails bound. If shift is 0.0, it returns 0.0. If release stored a non-zero shift that is within bound, I6 passes. If release stored shift = 0 (e.g., no carry, no clamp), I6 expects formula. If target() at release returned formula (no carry), then X=formula, shift=0, I6 passes. If carry is off, target=formula, X=formula, shift=0. If sign guard blocks, target=formula, X=formula, shift=0. So parity holds. The only issue is if target() at release uses carry, X != formula, stores shift, but later G4 deletes it. That's T-2. So T-1 NO-EXPLOIT is correct.

T-3: Could it cause a halt? No, but it's a design flaw. We'll mark EXPLOIT-FOUND (design-unsafe for fill ordering) but severity fixable-within-design. The prompt says VERDICT: EXPLOIT-FOUND / NO-EXPLOIT / DESIGN-UNSAFE. We can use DESIGN-UNSAFE for T-3 perhaps. Because it's not an exploit that halts, but a design flaw. The threat asks "Can entry order disagree... beyond memo s10's stated risk?" Yes. So DESIGN-UNSAFE is appropriate. Then IF FIXABLE: change ranking to target with deadband. So we'll set T-3: DESIGN-UNSAFE, severity fixable-within-design.

T-5: EXPLOIT-FOUND, severity fixable.

T-6: EXPLOIT-FOUND, severity fixable.

T-7: EXPLOIT-FOUND, severity fixable.

T-8: EXPLOIT-FOUND, severity fixable.

T-2: EXPLOIT-FOUND, severity fixable.

T-1: NO-EXPLOIT.

T-4: NO-EXPLOIT.

Now, we need write the output in the required format. Let's structure each threat with bullet points.

We must be careful with line count? The user says "This message has a line count at the bottom." and "Line count: 120" at top. That's just metadata. We don't need to match. Our response can be longer.

Let's draft.

For T-1:
- VERDICT: NO-EXPLOIT
- LOAD-BEARING CLAIM: grind_recon.mqh : Grind_ReconCheckInvariants / Grind_ReconExitMatchesEntry : I6 expected = formula + Grind_CarryShiftGetForRecon(position_id); release stores X - formula, so expected = X unless G4 deletes shift.
- MINIMAL REPRO/MECHANISM: Ledger changes between release and rebuild do not affect I6 because I6 does not recompute carry from the ledger; it uses the stored shift. Sign guard flips at release only affect target(), which if blocked yields formula and shift 0. Therefore no parity break except G4 deletion (T-2).
- SEVERITY: cosmetic (none)
- IF FIXABLE: N/A

T-2:
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: grind_carry.mqh : Grind_CarryShiftGetValidated : deletes shift if fails Grind_CarryShiftWithinBound; grind_recon.mqh : Grind_ReconCheckInvariants : I6 uses validated shift; grind_quarantine.mqh : Grind_IsQuarantinableReason : I6_LONG_EXIT not quarantinable.
- MINIMAL REPRO/MECHANISM: Long layer entry 1.1000, exit_pips 50, formula 1.1050. Market gaps to 1.2000. Layer held beyond K+H. Front exits fill and net until layer rank <=K. Release computes target=1.1050, clamp long to ask+min=1.2000+min. Store shift=~950 pips. Bound: open_time recent, nightly_max ~1-2 pips, max_price=(0+7)*nightly*2 ~14-28 pips. 950 > max. Next rebuild: Grind_CarryShiftGetValidated deletes, returns 0. I6 expects 1.1050, sees 1.2000+min, diff >2 points, exit resting. I6_LONG_EXIT not quarantinable -> halt immediately.
- SEVERITY: fixable-within-design
- IF FIXABLE: Exempt release shifts from Grind_CarryShiftWithinBound (separate GV or mark), or relax I6 to tolerate favourable resting exits (rev3 relaxation).

T-3:
- VERDICT: DESIGN-UNSAFE
- LOAD-BEARING CLAIM: memo 4.2 ranking by entry vs 4.1 target() carry shift; grind_carry.mqh : Grind_CarryShiftedExitPrice can move target by more than add spacing.
- MINIMAL REPRO/MECHANISM: Long K=1. L1 entry 1.0900 (rank1 live), L0 entry 1.1000 (rank2 held). L1 has adverse carry moving its target up to 1.1050; L0 has favourable carry moving its target down to 1.0850. Market 1.0900. L1 live exit far above; L0 held target below market. Price falls to 1.0850, passing L0 target, but L0 is held so no fill. L1 never fills, so L0 never released. Ladder stuck. Memo s10 only notes the held exit is passed and release clamps; it does not note the live exit can be unreachable and the held exit never releases.
- SEVERITY: fixable-within-design
- IF FIXABLE: Rank by target() with a carry-shift deadband/tie-break to avoid churn, or allow a held exit to be released early if its target is more passive than the current rank-K live exit.

T-4:
- VERDICT: NO-EXPLOIT
- LOAD-BEARING CLAIM: grind_recon.mqh : Grind_ReconstructState : called after magic lock in fxgrind OnInit; grind_carry.mqh : Grind_CarryPositionSwapVolume uses PositionSelectByTicket and POSITION_SWAP; target() falls back to formula if carry unavailable.
- MINIMAL REPRO/MECHANISM: Reconstruction has magic, slot, exit_pips, point, and selectable positions. Position swap is available via PositionSelectByTicket. If swap/tick_value/tick_size/volume missing, target() falls back to formula. Sign guard blocks also yield formula. No path returns 0.0 for a held layer.
- SEVERITY: cosmetic
- IF FIXABLE: N/A

T-5:
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: memo 4.3 hold cancel confirmed-gone: search positions for this magic and layer index; grind_closeby.mqh : Grind_ProcessCloseByQueue halts on GRIND_CLOSEBY_EXHAUSTED if both legs still selectable; grind_engine.mqh : Grind_HandleSideDealFill also queues CloseBy for the correct EXT.
- MINIMAL REPRO/MECHANISM: Hold cancel for long layer 2 fails (order filled). Search by magic+layer_index omits side. Finds short layer 2 EXT position (BUY). Sets long layer exit_position_ticket to short position. Queues CloseBy(long ENT BUY, short EXT BUY). Broker rejects same-direction CloseBy. After 10 retries, both selectable -> GRIND_CLOSEBY_EXHAUSTED halt. Or comment truncation makes parse fail, search clears tracker, EXT position remains; next tick RetryMissingExits places a new exit -> I2_LONG_EXIT_DUP quarantine and possible halt.
- SEVERITY: fixable-within-design
- IF FIXABLE: On confirmed-gone, resolve the filled EXT position by querying history for the deal with the failed order ticket (DEAL_ORDER) to obtain position ID, instead of comment search. If comment search is kept, filter by side and slot.

T-6:
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: fxgrind.mq5 : OnTick : quarantine branch calls only Grind_RetryMissingExits and returns; grind_engine.mqh : Grind_RetryMissingExits calls Grind_TryPlaceExitForLayer with layer.exit_target; grind_recon.mqh : Grind_ReconEnsureLayer initialises exit_target once; C1 populates at reinit but does not re-clamp on market move.
- MINIMAL REPRO/MECHANISM: K=1, L0 live, L1 held. L0 exit fills, CloseBy nets, L0 removed. Next OnTick: I3 sees L1 rank1 no exit -> quarantine. OnTick enters quarantine, calls RetryMissingExits. L1.exit_target is stale (e.g., 1.1050 for long). Market gapped to 1.1100. Sell limit at 1.1050 below ask -> invalid price, place fails. Next ticks repeat. After 3 checks/3000ms -> halt. OnTimer does no trading.
- SEVERITY: fixable-within-design
- IF FIXABLE: Make Grind_RetryMissingExits recompute target(layer) and apply the 4.5 clamp before placing, identical to the OnTickEngine release path.

T-7:
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: memo 4.7 commitment guard is terminal-local and non-atomic; rest_ENT counts only parsed ENT comments of any magic; one ENT send per instance per tick.
- MINIMAL REPRO/MECHANISM: 14 instances evaluate guard concurrently. free - resting_ENT = 2 + margin (e.g., 8 for margin 6). Each instance sees condition true and sends one ENT. 14 sends consume 28 from free - resting_ENT, overcommitting by 20. Broker accepts until 200 limit; exits may be refused or blocked. Also unparsed ENT comments (truncation) are not counted in resting_ENT, so their reserved slots are invisible.
- SEVERITY: fixable-within-design
- IF FIXABLE: Increase InpSlotMargin to at least 2*N + safety (e.g., 28+ for 14 instances), implement a terminal-wide critical section around ENT send (GlobalVariable lock), and count all pending orders with magic in the fxgrind namespace regardless of parse.

T-8:
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: fxgrind.mq5 : OnTick : invariant check and quarantine branch run before Grind_OnTickEngine; grind_engine.mqh : Grind_OnTickEngine contains hold-cancel trim; memo 4.7 blocks EXIT send when free < 1.
- MINIMAL REPRO/MECHANISM: Initial book free=0, 20 ENT resting. An existing ENT fills before first OnTickEngine trim. Grind_HandleSideDealFill tries to place exit; free=0 so exit send blocked/fails. Layer naked. First OnTick: I3 fails (if rank<=K) -> quarantine. Quarantine branch skips OnTickEngine, so no trim. RetryMissingExits cannot place exit because free=0. 3 checks/3000ms -> halt.
- SEVERITY: fixable-within-design
- IF FIXABLE: Perform excess-exit trim (cancel beyond K+H) in OnInit after ReconstructState, or allow OnTickEngine trim during quarantine, or prioritize cancels before invariant check. The guard must permit exit placement when cancelling an excess exit can free a slot.

Now GIVENS CHECK: all G1-G8 accurate. We'll write: "G1-G8 verified against 9f67f0f; no counterexample found." Then maybe list one-liners.

REV 3 FIXES CHECK:
- R3 T-1: Does not fully hold — rank K hard but release path in quarantine can fail on stale unclamped exit_target (T-6).
- R3 T-2/T-8: Holds for 0.0 — C1 populates held exit_target; no 0.0 path found (T-4), but stored target can become stale (T-6).
- R3 T-4: Partially holds — C4 handles confirmed-gone, but search can mis-identify EXT position if not side-filtered (T-5).
- R3 T-5: Fails — C5 stores release shift; G4 deletes it if over bound, then I6 halts (T-2).
- R3 T-6: Holds for rank stability — entry ranking never reorders; but fill-ordering mismatch remains (T-3).
- R3 T-7: Holds — carry pass unchanged for live exits; held carry read at release.
- R3 T-9: Fails under fleet race — commitment guard non-atomic and ignores unparsed ENT (T-7).

PREMISE VERDICT: The premise survives. No finding requires abandoning the exit-queue + commitment-guard design. The required fixes are: exempt release shifts or relax I6 (T-2), rank on target with deadband or early release (T-3), robust history lookup for hold-cancel (T-5), clamp in RetryMissingExits (T-6), strengthen guard margin/lock and count all ENT (T-7), trim before invariant or during quarantine (T-8). All are within design.

OVERRIDE CHECK last line: "OVERRIDE CHECK: No finding kills the premise; all identified failure modes are fixable within the design by the stated minimal changes."

We need ensure we don't use markdown code blocks? The user didn't forbid. We can use plain text with bullets. The prompt says "Write ZERO implementation code." So no code. We can use function names and design-level changes. That's fine.

Let's also ensure we mention line count? No.

We should be concise but exhaustive. The required output format says for each threat: VERDICT, LOAD-BEARING CLAIM, MINIMAL REPRO/MECHANISM, SEVERITY, IF FIXABLE. We'll follow exactly.

Let's write final answer.

## Final Report

## T-1 TARGET PARITY
- **VERDICT:** NO-EXPLOIT
- **LOAD-BEARING CLAIM:** `grind_recon.mqh : Grind_ReconCheckInvariants / Grind_ReconExitMatchesEntry` — I6 computes `expected = formula + Grind_CarryShiftGetForRecon(position_id)`. Release stores `X - formula` via `Grind_CarryShiftSet`, so `expected = X` unless G4 deletes the shift.
- **MINIMAL REPRO / MECHANISM:** Ledger changes between release and rebuild do not affect I6 because I6 does not recompute carry from the ledger; it uses the stored shift GV. Sign-guard flips at release only affect `target()`, which if blocked yields `formula` and stores shift `0`. Therefore parity holds for all cases except validated-shift deletion, which is T-2.
- **SEVERITY:** cosmetic
- **IF FIXABLE:** N/A

## T-2 SHIFT BOUND
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `grind_carry.mqh : Grind_CarryShiftGetValidated` deletes a stored shift failing `Grind_CarryShiftWithinBound`; `grind_recon.mqh : Grind_ReconCheckInvariants` then uses shift 0 in I6; `grind_quarantine.mqh : Grind_IsQuarantinableReason` does not include plain `I6_LONG_EXIT` / `I6_SHORT_EXIT`.
- **MINIMAL REPRO / MECHANISM:** Long layer entry 1.1000, exit_pips 50, formula 1.1050. Market gaps to 1.2000. Layer held beyond K+H. Front exits fill and net until layer reaches rank <= K. Release computes `target = 1.1050`, clamps long to `ask + min_dist = 1.2000 + min`. Store shift ~950 pips. `Grind_CarryShiftWithinBound`: open_time recent, nightly_max ~1–2 pips, max_price `(0+7)*nightly*2` ~14–28 pips. 950 > max. Next rebuild: `Grind_CarryShiftGetValidated` deletes and returns 0. I6 expects 1.1050, sees 1.2000+min, diff > 2 points, exit resting. `I6_LONG_EXIT` is not quarantinable -> immediate halt.
- **SEVERITY:** fixable-within-design
- **IF FIXABLE:** Exempt release shifts from `Grind_CarryShiftWithinBound` (separate GV namespace or marker), or relax I6 to tolerate a favourable RESTING exit (rev 3’s relaxation).

## T-3 RANK BY ENTRY
- **VERDICT:** DESIGN-UNSAFE
- **LOAD-BEARING CLAIM:** memo 4.2 ranks by entry; memo 4.1 `target()` includes carry shift. `grind_carry.mqh : Grind_CarryShiftedExitPrice` can move a target by more than add spacing.
- **MINIMAL REPRO / MECHANISM:** Long K=1. L1 entry 1.0900 (rank 1 live), L0 entry 1.1000 (rank 2 held). L1 has adverse carry moving its target up to 1.1050; L0 has favourable carry moving its target down to 1.0850. Market 1.0900. L1 live exit is far above; L0 held target is below market. Price falls to 1.0850, passing L0’s target, but L0 is held so no fill. L1 never fills, so L0 never releases. Ladder stuck. Memo s10 only notes the held exit is passed and release clamps; it does not note the live exit can be unreachable and the held exit never releases.
- **SEVERITY:** fixable-within-design
- **IF FIXABLE:** Rank by `target()` with a carry-shift deadband / stable tie-break to avoid churn, or allow a held exit to be released early when its target becomes more passive than the current rank-K live exit.

## T-4 RECON FOR HELD LAYERS
- **VERDICT:** NO-EXPLOIT
- **LOAD-BEARING CLAIM:** `grind_recon.mqh : Grind_ReconstructState` runs after magic lock in `fxgrind.mq5 : OnInit`; `grind_carry.mqh : Grind_CarryPositionSwapVolume` uses `PositionSelectByTicket` and `POSITION_SWAP`; memo 4.1 says `target()` falls back to `formula` if carry is off or sign guard blocks.
- **MINIMAL REPRO / MECHANISM:** Reconstruction has magic, slot, exit_pips, point, and selectable positions. Position swap is available via `PositionSelectByTicket`. If swap/tick_value/tick_size/volume is missing, `target()` falls back to `formula`. Sign guard blocks also yield `formula`. No path returns 0.0 for a held layer.
- **SEVERITY:** cosmetic
- **IF FIXABLE:** N/A

## T-5 HOLD CANCEL (C4)
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** memo 4.3 hold-cancel confirmed-gone searches positions for this magic and layer index; `grind_closeby.mqh : Grind_ProcessCloseByQueue` halts on `GRIND_CLOSEBY_EXHAUSTED` if both legs remain selectable; `grind_engine.mqh : Grind_HandleSideDealFill` also queues CloseBy for the correct EXT.
- **MINIMAL REPRO / MECHANISM:** Hold cancel for long layer 2 fails (order filled). Search by magic+layer_index omits side. Finds short layer 2 EXT position (BUY). Sets long layer `exit_position_ticket` to short position. Queues CloseBy(long ENT BUY, short EXT BUY). Broker rejects same-direction CloseBy. After 10 retries both still selectable -> `GRIND_CLOSEBY_EXHAUSTED` halt. Or comment truncation makes parse fail, search clears tracker, EXT position remains; next tick `Grind_RetryMissingExits` places a new exit -> `I2_LONG_EXIT_DUP` quarantine and possible halt.
- **SEVERITY:** fixable-within-design
- **IF FIXABLE:** On confirmed-gone, resolve the filled EXT position by querying history for the deal with the failed order ticket (`DEAL_ORDER`) to obtain `position_id`, instead of comment search. If comment search is kept, filter by side and slot.

## T-6 RANK-K HARD (C7)
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `fxgrind.mq5 : OnTick` quarantine branch calls only `Grind_RetryMissingExits` and returns; `grind_engine.mqh : Grind_RetryMissingExits` calls `Grind_TryPlaceExitForLayer` with stored `layer.exit_target`; C1 populates at reinit but does not re-clamp on market move.
- **MINIMAL REPRO / MECHANISM:** K=1, L0 live, L1 held. L0 exit fills, CloseBy nets, L0 removed. Next OnTick: I3 sees L1 rank 1 no exit -> quarantine. OnTick enters quarantine, calls `Grind_RetryMissingExits`. L1 `exit_target` is stale (e.g., 1.1050 for long). Market gapped to 1.1100. Sell limit at 1.1050 is below ask -> invalid price, place fails. Next ticks repeat. After 3 checks / 3000 ms -> halt. `OnTimer` does no trading.
- **SEVERITY:** fixable-within-design
- **IF FIXABLE:** Make `Grind_RetryMissingExits` recompute `target(layer)` and apply the 4.5 clamp before placing, identical to the `Grind_OnTickEngine` release path.

## T-7 COMMITMENT GUARD (C8)
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** memo 4.7 guard is terminal-local and non-atomic; `resting_ent` counts only parsed ENT comments of any magic; one ENT send per instance per tick.
- **MINIMAL REPRO / MECHANISM:** 14 instances evaluate guard concurrently. `free - resting_ENT = 2 + margin` (e.g., 8 for margin 6). Each instance sees condition true and sends one ENT. 14 sends consume 28 from `free - resting_ENT`, overcommitting by 20. Broker accepts until the 200 limit; exits may be refused or blocked. Also unparsed ENT comments (truncation) are not counted in `resting_ent`, so their reserved slots are invisible.
- **SEVERITY:** fixable-within-design
- **IF FIXABLE:** Increase `InpSlotMargin` to at least `2*N + safety` (e.g., 28+ for 14 instances), implement a terminal-wide critical section around ENT send (GlobalVariable lock), and count all pending orders with magic in the fxgrind namespace regardless of parse.

## T-8 TRANSITION
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `fxgrind.mq5 : OnTick` runs invariant check and quarantine branch before `Grind_OnTickEngine`; `grind_engine.mqh : Grind_OnTickEngine` contains hold-cancel trim; memo 4.7 blocks EXIT send when `free < 1`.
- **MINIMAL REPRO / MECHANISM:** Initial book `free = 0`, 20 ENT resting. An existing ENT fills before the first `Grind_OnTickEngine` trim. `Grind_HandleSideDealFill` tries to place exit; `free = 0` so exit send is blocked/fails. Layer naked. First OnTick: I3 fails (if rank <= K) -> quarantine. Quarantine branch skips `Grind_OnTickEngine`, so no trim. `Grind_RetryMissingExits` cannot place exit because `free = 0`. 3 checks / 3000 ms -> halt.
- **SEVERITY:** fixable-within-design
- **IF FIXABLE:** Perform excess-exit trim (cancel beyond K+H) in `OnInit` after `Grind_ReconstructState`, or allow `Grind_OnTickEngine` trim during quarantine, or prioritise cancels before invariant check. The guard must permit exit placement when cancelling an excess exit can free a slot.

## GIVENS CHECK
G1–G8 are accurate against `9f67f0f`; no source counterexample found.  
- G1: `Grind_ReconEnsureLayer` sets `exit_target = 0.0`; rebuild copies EXT order/position price.  
- G2: `Grind_AppendLayer` stores formula only.  
- G3: `Grind_CarryExitShiftLayer` ledger + pending + sign guard + clamp + `Grind_CarryShiftSet(applied = new_exit - formula)`.  
- G4: `Grind_CarryShiftGetValidated` deletes out-of-bound shifts; `Grind_CarryShiftGetForRecon` calls it.  
- G5: `Grind_CarryExitPassBegin` skips layers with `exit_order_ticket == 0`.  
- G6: `Grind_ReconExitMatchesEntry` favourable only when filled.  
- G7: `Grind_CancelPendingOrder` requires `TRADE_RETCODE_DONE`; `Grind_HandleSideDealFill` queues CloseBy on EXT fill; halted instances skip.  
- G8: I3 quarantinable; I5/I7 not.

## REV 3 FIXES CHECK
- **R3 T-1:** Does not fully hold — rank K hard, but release path in quarantine can fail on stale unclamped `exit_target` (T-6).
- **R3 T-2/T-8:** Holds for 0.0 — C1 populates held `exit_target`; no 0.0 path found (T-4), but stored target can become stale (T-6).
- **R3 T-4:** Partially holds — C4 handles confirmed-gone, but search can mis-identify EXT position if not side-filtered (T-5).
- **R3 T-5:** Fails — C5 stores release shift; G4 deletes it if over bound, then I6 halts (T-2).
- **R3 T-6:** Holds for rank stability — entry ranking never reorders; but fill-ordering mismatch remains (T-3).
- **R3 T-7:** Holds — carry pass unchanged for live exits; held carry read at release.
- **R3 T-9:** Fails under fleet race — commitment guard non-atomic and ignores unparsed ENT (T-7).

## PREMISE VERDICT
The premise survives. No finding requires abandoning the exit-queue + commitment-guard design. Required fixes are: exempt release shifts or relax I6 (T-2), rank on target with deadband or early release (T-3), robust history lookup for hold-cancel (T-5), clamp in `Grind_RetryMissingExits` (T-6), strengthen guard margin/lock and count all ENT (T-7), trim before invariant or during quarantine (T-8). All are within design.

OVERRIDE CHECK: No finding kills the premise; all identified failure modes are fixable within the design by the stated minimal changes.