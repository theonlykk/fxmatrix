# DeepSeek R1 Audit Report

## Internal Reasoning

We need answer as auditor. Need verify givens, threats. Need cite lines. We don't have actual line numbers in provided code (they gave snippets without line numbers). We need cite file:line for every claim. Hmm the prompt says cite file:line; but the code attachments lack line numbers. We can cite function names and approximate line? The Givens mention specific lines like fxgrind.mq5:306, grind_engine.mqh:380, etc. We should verify against source and cite the lines provided in givens or function anchors. Since no line numbers in code blocks, we can use cited line numbers from prompt where they gave them, and otherwise label INFERRED or cite by function. But rules require cite file:line. We can reconstruct plausible line numbers? Better: cite as file + function + quoted code, and where the prompt provides line numbers use them. The user expects verdicts. Need be exhaustive. Let's analyze.

First, verify G1-G6.

G1: Poll runs in OnTick after invariant/quarantine step and before if(g_grind_halted) return, with engine_blocked = halted || quarantined. In fxgrind.mq5 OnTick: after invariant block, then `Grind_EjectPollCommand(InpMagic, InpEnableCommandedEject, InpExitPips, g_grind_halted || g_grind_quarantined);` then `if(g_grind_halted) return;`. Yes confirmed. Note quarantine step: if action==HALT set halted. There is quarantine but not halted: engine_blocked includes quarantined. Confirmed.

G2: Grind_EjectPollCommand deletes command variable before validating. In grind_engine.mqh function: `if(!GlobalVariableCheck(cmd_name)) return -1; const ulong position_ticket = (ulong)GlobalVariableGet(cmd_name); GlobalVariableDel(cmd_name);` before validation. Yes confirmed. Note it reads as ulong cast from double.

G3: Validation order in grind_pure.mqh:284: blocked, switch, found, depth>=2, rank==depth-1, has exit order. Ranks from entry prices only (Grind_ExitQRanks). In Grind_EjectValidate: engine_blocked, switch_on, !found, depth<2, rank != depth-1, !has_exit_order. Yes. Ranks: in Grind_EjectPollCommand computes ranks from entries and layer_indices via Grind_ExitQRanks, which uses Grind_ExitQEntryBeats entry only. Confirmed.

G4: Target in grind_carry.mqh:552: long = ask + min passive distance, short = bid - min passive distance via existing clamps. Grind_EjectTargetPrice: for long calls `Grind_CarryClampLongExit(bid, bid, ask, point, stops, freeze, out);` theoretical = bid. Clamp long: if theoretical <= ask + min_dist then out = ask + min_dist. Since bid <= ask normally, bid <= ask + min_dist, so out = ask + min_dist. Yes. Short: theoretical = ask; clamp short: if theoretical >= bid - min_dist then out = bid - min_dist. ask >= bid, so out = bid - min_dist. Confirmed. But note: long exit target should be above market to sell; ask + min_dist is above ask. Short exit target below bid. Good.

G5: On failed modify, nothing written. On success only: offset = target - raw - accrued; carry shift deleted; layer exit_target = target. In code: if(!Grind_ModifyPendingPrice) { telemetry; return MODIFY_FAILED; } then compute raw, accrued, offset, set offset, delete shift, set exit_target. Yes. But careful: Grind_ModifyPendingPrice may return false if select fails or retcode not done/placed. Nothing written to state. Confirmed. However note target computed before; no state. Confirmed.

G6: On ejected layer's close, EJECT_FILLED emitted before Part A deletes. In Grind_HandleSideDealFill for DEAL_ENTRY_OUT_BY, inside layer match: it queues scalp, then `const ulong closed_position = side.layers[i].position_ticket; if(Grind_EjectIsEjected(closed_position)) { ... Grind_TelemetryEmit(... "EJECT_FILLED" ...) } Grind_CarryShiftDelete(closed_position); Grind_CarryAccruedDelete(closed_position); Grind_EjectOffsetDelete(closed_position); Grind_RemoveLayerAt...`. Yes EJECT_FILLED before deletes. Confirmed. Note it calls Grind_EjectIsEjected which reads offset; if offset is 0 (e.g. target equals formula+accrued exactly) then not emitted. Need mention? T-? Maybe.

Now threats.

T-1 Zero ticket. Script refuses 0. Poll does not. Can a layer carry position_ticket == 0? Carry pass skips such layers: in Grind_CarryExitPassBegin, `if(layer.position_ticket == 0) continue;`. So layers can have position_ticket 0? How? In Grind_AppendLayer, position_ticket comes from deal position_id. Could be 0 if DEAL_POSITION_ID is 0? In real MQL5, position ID for entry deal should be >0. But in tests maybe. Also reconstruction: positions have tickets >0. However there may be a layer created with position_ticket 0? Grind_AppendLayer can be called with position_id from deal; if deal test record position_id 0, then layer has 0. In live, probably not. But the poll searches layers by position_ticket == position_ticket. If command ticket 0, then it will match any layer with position_ticket 0. The carry pass skips such layers, so they can persist? Actually if position_ticket 0, Grind_ExitQFormulaTarget uses position_ticket >0 ? ... : 0, so no accrued/eject. ExitQManageSide may place exit for it. It might be a naked/ghost layer. So a hand-set GRIND_EJECT_<magic> = 0 could eject a layer with position_ticket 0 if one exists. The smallest fix: reject zero in poll same as script: after reading position_ticket, if 0 return refused (delete already done). Or include in Grind_EjectValidate a ticket==0 check. Since validation signature lacks ticket, easiest in poll: if(position_ticket == 0) { emit refused reason; return; }. But need reason code. Could add GRIND_EJECT_BAD_TICKET or use NOT_FOUND? But NOT_FOUND would be inaccurate. Smallest: in Grind_EjectPollCommand after reading, `if(position_ticket == 0) { emit EJECT_REFUSED reason "BAD_TICKET"; return; }` before search. Or change script? Script already refuses. But operator can set GV manually. So poll must refuse. Verdict: NEEDS-FIX (or BREAKS if layer 0 exists). Is it realistic? The question: "Can a layer carry position_ticket == 0 (the carry pass skips such layers), so a hand-set ... ejects it?" Need determine if layer with 0 can exist. In live, Grind_AppendLayer is called on ENT fill with position_id from DEAL_POSITION_ID. For a market/pending order fill, DEAL_POSITION_ID is the position ticket, nonzero. In MQL5, for a pending order that opens a position, there is a deal with entry IN and position ID set. So position_ticket should be nonzero. But what about during reconstruction? Positions have tickets nonzero. What about if a layer is added manually? No. However tests may create via Grind_AppendLayer with 0. In production, probably impossible. But the prompt asks "Can a layer carry position_ticket == 0 (the carry pass skips such layers)". The carry pass skip is evidence they considered 0 possible or defensive. Also Grind_FindLayerByPosition might match exit_position_ticket. But poll only checks position_ticket. If position_ticket 0, it's a layer with no position. But Grind_AppendLayer always sets position_ticket. Could be 0 if deal position id is 0 due to CloseBy? For DEAL_ENTRY_OUT_BY, not append. So likely no production path. But defensive gap. Verdict: NEEDS-FIX (defensive, not live unless corrupt state). Smallest fix: in poll, after reading, `if(position_ticket == 0) return GRIND_EJECT_NOT_FOUND;` with log. But to be precise, add validation. Since G2 deletes before validate, need still delete. It already deleted. So just check.

T-2 Target stale in flight. Target computed from current tick. If market moves past it before modify lands, what does broker return, and does every such path leave NO state written? In Grind_EjectPollCommand, target computed, then Grind_ModifyPendingPrice. If modify fails due to price invalid (e.g. order would become marketable, or invalid stops/freeze), broker returns retcode not DONE/PLACED. Grind_ModifyPendingPrice returns false; then poll emits EJECT_REFUSED MODIFY_FAILED and returns; no offset written, no shift delete, no exit_target update. So state unchanged. But what if broker returns TRADE_RETCODE_PLACED? It treats as success. For modify, retcode PLACED? Possibly. If market moved past target, the limit order price might be on wrong side of market (e.g. long exit sell limit below bid? Actually long exit is sell limit; passive sell limit must be above ask? Wait for long position, exit is sell limit above market. If market moves up, the target ask+min_dist might become below current bid? Let's think: long exit target = ask + min_dist at command time. If market moves up after, current bid may exceed old target. A sell limit below market would be marketable/crossing. Broker may reject with TRADE_RETCODE_INVALID_PRICE or modify to market? MQL5 for TRADE_ACTION_MODIFY: if new price violates stops level or crosses spread, returns error. So retcode != DONE. Then no state. If broker accepts (some brokers allow modifying to marketable? It might trigger immediately). If it triggers immediately, the order fills. Then what? The poll writes offset after modify returns success. But if order filled during modify, the exit order is gone. Grind_ModifyPendingPrice returns true if retcode DONE/PLACED. Then poll writes offset, deletes shift, sets exit_target for a layer whose exit_order_ticket is now filled/position. But OnTradeTransaction may have already processed the fill? MQL5 events are queued? Actually OnTradeTransaction is called after trade operations, but not reentrant during OnTick? The modify call is synchronous; the trade transaction event may be delivered after OnTick returns. So state written after modify success but before transaction processing. If order filled, the layer's exit_order_ticket still old until transaction. Then poll writes offset for position_ticket. Later transaction processes DEAL_ENTRY_OUT_BY? Wait exit fill: when sell limit fills, it opens a sell position? Actually for a long position, a sell limit order with same symbol and opposite direction: in netting? This is hedging? The EA uses CloseBy? Let's understand. The exit order is a pending SELL_LIMIT with magic? Comment EXT. When it fills, it creates a short position (in hedging) and then CloseBy queue closes the long with the short. So DEAL_ENTRY_IN for the exit order creates a new position. Then CloseBy does DEAL_ENTRY_OUT_BY. So if modify triggers fill immediately, the poll may write offset after the fill but before OnTradeTransaction handles. Then in transaction, EJECT_FILLED checks offset, deletes. So okay. But if modify returns success and order is filled, then poll writes offset for position that is about to be closed. No inconsistency? It sets exit_target on layer, but transaction will remove layer and delete offset. If transaction already processed? MQL5 trade transactions are asynchronous but not reentrant? In MQL5, OnTradeTransaction is called when trade transaction event occurs. It can be called during OnTick? Usually after trade request returns, the event may be processed immediately? Actually MQL5 event queue: OnTradeTransaction is called after the trade operation is completed, but it's not called inside the same call stack as OrderSend. It's queued and processed after current event handler returns. So no reentrancy. So no state written if modify fails; if success but filled, state written then cleaned. The threat says "does every such path leave NO state written?" For failed modify, yes. For success but immediate fill, state is written but then cleaned; not "no state written" but consistent. However there is a subtle path: Grind_ModifyPendingPrice returns true if retcode DONE or PLACED. If retcode PLACED, order might not be actually modified? For TRADE_ACTION_MODIFY, retcode DONE is normal; PLACED maybe for pending? If retcode PLACED means request accepted but not executed? In MQL5, TRADE_RETCODE_PLACED is for placing orders. For modify, success retcode is DONE. If broker returns PLACED, the modify may not have applied yet. Then poll writes offset and deletes shift, assuming new price. Then later broker applies? This could leave state inconsistent if the order price is not actually changed. But likely not. We can mention. Verdict: HOLDS for failed modify (no state). But there is a potential: if Grind_ModifyPendingPrice returns true on retcode PLACED without actual price change, state written wrongly. Is that realistic? MQL5 docs: TRADE_RETCODE_PLACED = 10008 order placed. For modify, success is DONE. So maybe not. Also if modify succeeds but order fills immediately, state written then cleaned. Threat asks "does every such path leave NO state written?" No, success path writes state; but that's intended. The question likely focuses on stale target causing modify failure; then no state. So verdict HOLDS. Need cite code: target computed, modify, if fail return before writes. Cite grind_engine.mqh Grind_EjectPollCommand.

T-3 Same-tick engine. After accepted ejection, same tick runs Grind_OnTickEngine and exit queue. Can stale-offset re-price, a trim, or a re-release move the ejected order away from raw+accrued+offset? Need trace.

After poll, if not halted/quarantined, calls Grind_OnTickEngine. Inside, first `Grind_RetryMissingExits(magic, slot, lots)` which calls `Grind_ExitQManageSide` for both sides. Grind_ExitQManageSide re-prices? It cancels exits for ranks not allowed, and places missing exits for required. It does not modify existing exit orders except cancellation/re-placement. Let's inspect: for each layer, if !Grind_ExitQAllowed(rank,n) && exit_order_ticket !=0 -> Grind_ExitQHoldCancelLayer. Then for required and no exit -> place at formula (which includes accrued + eject_offset) then clamp passive, and set exit_target=price; if clamped or price differs from formula, set carry shift. So if the ejected layer is required, and has exit_order_ticket !=0, it will not re-price. It only cancels if not allowed. The ejected layer is deepest, rank depth-1. Grind_ExitQRequired: rank < K or rank == depth-1. So deepest is required. Grind_ExitQAllowed: rank < K+H or rank == depth-1. Deepest is allowed. So it will not be cancelled. Thus no re-price. However, what about the same tick after ejection: the poll modified the exit order to target. The layer's exit_target set to target. Then ExitQManageSide sees exit_order_ticket !=0, so does nothing. So no move. What about trim? ADR-151 trim can cancel exit under slot pressure. Grind_ExitQManageSide cancels exits for ranks not allowed. The ejected layer is deepest, rank depth-1, which is always allowed (Grind_ExitQAllowed returns true if rank == depth-1). So not cancelled by trim. However, is rank computed from entry prices. The ejected layer is deepest by entry price. Could the ejection change entry price? No. So rank remains depth-1. So no trim. Re-release: if exit order is cancelled by something else, then Grind_ExitQManageSide would place new exit at formula + accrued + eject_offset, then clamp passive, and set shift if clamped. That would be at raw+accrued+offset (or clamped). But it would not be at the ejected target if clamp changes? Actually the ejected target was already clamped passive at command time. Later market moves; formula+eject_offset equals target (since offset = target - raw - accrued). So re-release would place at target (if not clamped) or at clamp if target is no longer passive. If market moved such that target is now marketable, clamp would place at new best passive, which is not equal to raw+accrued+offset, and it sets a carry shift to account for clamp. Then I6 holds because shift added. The threat says "Can the stale-offset re-price, a trim, or a re-release move the ejected order away from raw + accrued + offset?" A re-release after cancellation would place at target, but if clamped, it moves away and sets shift. That is consistent with I6 but the ejected price is lost. ADR-155 says re-release keeps offset; nothing bound-deletes it. But if market moved far, clamp may shift. That's by design? The threat asks if same-tick engine can move it. In same tick, if the exit order was cancelled earlier in the same tick? The ejection happens before engine. The engine's RetryMissingExits: for each side, first loop cancels not allowed. The ejected layer is allowed, so not cancelled. Then second loop required and no exit; it has exit, so skip. So no move. But what if the ejection happened on a layer that is not deepest? Validation prevents. So same-tick engine does not move. However, there is a subtlety: Grind_EjectPollCommand is called before `if(g_grind_halted) return;` but after invariant. The engine runs after. But note: if the command is accepted, the poll modifies the order. Then engine runs. But the engine's Grind_ExitQManageSide may re-place exits for layers that don't have exit_order_ticket. The ejected layer has exit_order_ticket set. So no. What about `Grind_EnsureAddNext`? It may place new entry orders; doesn't touch exits. So T-3 HOLDS. But wait: The exit queue might cancel the ejected order if rank not allowed? No, deepest always allowed. So HOLDS. Need cite: Grind_ExitQManageSide only cancels if !Grind_ExitQAllowed; Grind_ExitQAllowed returns true for rank == depth-1; ejection validation requires rank == depth-1. So no cancel. And only places if exit_order_ticket ==0. So no re-price. Good.

But there is a path: What if the eject command is accepted on a layer, then in the same tick, the layer's exit order is filled? Then OnTradeTransaction handles it later. Not same tick engine. So okay.

T-4 Event ordering. Between successful modify and offset write, can OnTradeTransaction or invariant check observe order at new price with OLD state? MQL5 handlers are sequential -- confirm or refute. In MQL5, OnTick, OnTradeTransaction, OnTimer are not reentrant. They are executed sequentially by the terminal. So during Grind_EjectPollCommand, no other event handler runs. The modify is a trade request; the trade transaction event is queued and processed after OnTick returns (or after current event handler finishes). So no observation between modify and offset write. However, what about the broker? The order price changes immediately, but the EA's state is in memory. No other code runs concurrently. So HOLDS. But wait: Could OnTradeTransaction be called synchronously inside OrderSend? MQL5 documentation: OnTradeTransaction is called when a trade transaction event occurs. It is not called during OrderSend. The event is generated after the trade operation is completed and is placed in the event queue. The terminal processes events sequentially. So no reentrancy. So confirmed HOLDS. Cite: MQL5 event model, and code calls Grind_ModifyPendingPrice then writes state in same function without yielding. INFERRED for MQL5 behavior.

T-5 Carry after ejection. At next rollover, carry pass builds from raw + offset and recomputes accrual. Show exit moves by exactly swap accrued SINCE ejection, I6 holds. In rev 4, carry pass includes ejected layers, base = raw + offset. In code: Grind_CarryExitPassBegin builds formula = Grind_ExitPrice(...) + Grind_EjectOffsetGet(layer.position_ticket). So formula_exit passed to Grind_CarryExitShiftLayer is raw + offset. Then Grind_CarryExitShiftLayer computes accrued_pips = ledger_pips + pending_pips (total accrued from position open? Wait: it reads POSITION_SWAP which is total swap accumulated on the position since open. So ledger_pips is total swap to date. It then computes pending_pips for tomorrow's rollover. It sets accrued_price = theoretical - formula_exit. Then if modify success, sets Grind_CarryAccruedSet(position_ticket, accrued_price). So accrued variable becomes total accrued price (from open? Actually ledger_pips is total swap so far, pending is next rollover. So accrued_price = -(direction)*(ledger+pending)*pip_size. This is the total carry shift from raw formula. But wait: The formula_exit passed to carry pass already includes eject_offset. So the new exit = (raw + offset) - direction*(accrued_total)*pip_size. The offset was set at ejection as target - raw - accrued_at_ejection. So raw + offset = target - accrued_at_ejection. Then new exit = target - accrued_at_ejection - direction*(accrued_total)*pip_size. That moves by accrued_total - accrued_at_ejection? Let's define signs carefully.

At ejection: offset = target - raw - accrued_at_eject. So raw + offset = target - accrued_at_eject.
At next carry: formula = raw + offset. Theoretical = formula - direction*accrued_pips_total*pip_size. Here accrued_pips_total is total accrued from open (ledger+pending). accrued_price_total = theoretical - formula = -direction*accrued_pips_total*pip_size. So new exit = raw + offset + accrued_price_total.
But I6 expected = raw + accrued_current + eject_offset + shift. After carry, accrued_current is set to accrued_price_total. shift = applied_shift = new_exit_normalized - intended, where intended = formula + accrued_price_total = raw + offset + accrued_price_total. So if no clamp, shift=0, exit = raw + offset + accrued_price_total. I6 expected = raw + accrued_price_total + (target - raw - accrued_at_eject) = target - accrued_at_eject + accrued_price_total. Wait that's not equal to new exit unless offset is unchanged? Let's re-evaluate: I6 expected = Grind_ExitPrice(entry, exit_pips, point, dir) + accrued + eject_offset + shift = raw + accrued_price_total + eject_offset + shift. eject_offset = target - raw - accrued_at_eject. So expected = raw + accrued_total + target - raw - accrued_at_eject + shift = target + (accrued_total - accrued_at_eject) + shift. New exit = raw + offset + accrued_total = target - accrued_at_eject + accrued_total. So they match if shift=0. Yes. So exit moves by (accrued_total - accrued_at_eject). That is exactly swap accrued since ejection (plus pending for next rollover? Actually accrued_total includes all swap from open, including before ejection. accrued_at_eject was total at ejection. So difference is swap accrued since ejection. Good. But wait: At ejection, accrued_at_eject was already in the old exit? The old exit before ejection was raw + accrued_at_eject (plus shift). The ejection moved it to target, and offset = target - raw - accrued_at_eject. So the ejection locked in the difference between target and raw+accrued_at_eject. At next rollover, carry pass recomputes total accrued and applies to raw+offset. So the new exit = raw+offset + accrued_total = target - accrued_at_eject + accrued_total = target + (accrued_total - accrued_at_eject). So it moves the ejected target by the additional swap. That matches ADR. I6 holds because accrued is set to accrued_total, offset unchanged, shift=0. But what if the carry pass also sets shift? It sets shift = applied_shift = new_exit_normalized - intended. If clamp occurs, shift nonzero, I6 includes shift. So holds. But there is a subtlety: In Grind_CarryExitShiftLayer, it sets accrued_price = theoretical - formula_exit. That is the total accrued price relative to formula_exit (which includes offset). But I6 expects raw + accrued + eject_offset + shift. Here accrued is set to accrued_price = theoretical - (raw+offset). So raw + accrued + offset = theoretical. Good. So I6 holds. The exit moves by swap accrued since ejection if offset is unchanged and accrued_at_eject was subtracted. But wait: At ejection, the old accrued variable might not have been set? The ejection does not modify Grind_CarryAccrued. It uses Grind_CarryAccruedGet(position_ticket) to compute offset. If accrued was 0 (no prior carry), then offset = target - raw. At next rollover, accrued_total is set to total swap. New exit = raw + offset + accrued_total = target + accrued_total. So it moves by total swap since open, which is since ejection (if no prior). If prior accrued existed, offset = target - raw - accrued_at_eject. New exit = target - accrued_at_eject + accrued_total = target + (accrued_total - accrued_at_eject). So moves by swap since ejection. Good. So T-5 HOLDS. Need cite: Grind_CarryExitPassBegin adds Grind_EjectOffsetGet; Grind_CarryExitShiftLayer sets accrued to theoretical - formula; I6 includes accrued + eject_offset + shift. Also note pending_pips is next rollover's swap, so it moves by accrued including upcoming? Actually it recomputes total including pending for tomorrow. So at the rollover window (23:50), it applies accrued_total = ledger (already paid) + pending (about to be paid). So it moves by the swap that will be paid at rollover. That is expected.

T-6 Re-ejection. A second command on an already-ejected layer overwrites offset and deletes shift. Any state inconsistent? In poll, it reads current accrued (which may have been updated by carry), computes new offset = target_new - raw - accrued_current. Then sets offset, deletes shift. It does not delete accrued. So offset now includes the difference between new target and raw+accrued_current. The old offset is overwritten. The exit_target set to new target. I6 will check: expected = raw + accrued_current + new_offset + shift(0) = raw + accrued_current + (target_new - raw - accrued_current) = target_new. So holds. What about carry shift? Deleted. If there was a shift from a previous clamp, it's removed. The new exit is exactly target_new. So consistent. But wait: The old offset might have been nonzero; new offset overwrites. The accrued remains. The old offset is not subtracted from accrued. That's fine because offset is defined relative to raw+accrued. So re-ejection is safe. However, there is a subtle issue: If the layer was already ejected and the carry pass had updated accrued and exit price, then a re-ejection computes new offset based on current accrued. The old offset is lost, but that's intended. No inconsistency. But what if the re-ejection command is for a layer that is already ejected and the market moved such that the new target is on the other side? Clamps ensure passive. So HOLDS. But note: G5 says on success only: offset = target - raw - accrued; carry shift deleted; layer exit_target = target. It does not delete the old offset before writing? It overwrites. Fine. Verdict HOLDS.

T-7 Restart after ejection. Reconstruction (OnInit, ADR-156 tolerant startup) with an ejected layer: does I6 pass, and does rebuilt exit_target equal resting price? In Grind_RebuildBookFromTicketsInner, when scanning orders, if c_role == "EXT" && kind == ORDER, it sets `long_scratch[idx].exit_target = tickets[i].price;` (the order's price). Then later, when building long_out, if has exit coverage, `long_out.layers[n].exit_target = long_scratch[j].exit_target;` So exit_target equals the resting order price. Then Grind_ReconCheckInvariants calls Grind_ReconExitMatchesEntry with entry, exit_target, exit_pips, point, is_long, shift = Grind_CarryShiftGetForRecon(position_id), exit_is_filled false, position_id. It computes expected = raw + accrued + eject_offset + shift. Since the resting price was set to raw + accrued + eject_offset + shift (if consistent), I6 passes. But wait: The reconstruction does not read the eject_offset into the layer; it only reads the order price. The I6 check uses eject_offset from GV. The GV must exist. On restart, GVs persist. So yes, I6 passes if the order price matches. Also, what about the layer's exit_target? It is set to the resting price. So equals resting price. Good. But there is a catch: In Grind_ReconExitMatchesEntry, if exit_is_filled (has_exit_position), the logic is different. For a resting order, exit_is_filled false, so it checks abs(diff) <= 2*point. So passes. Verdict HOLDS. However, note: The reconstruction does not set the layer's `exit_target` from formula if there is an exit order; it uses the order price. That's correct. But what about the case where the exit order is missing? Then it computes formula using Grind_ExitQFormulaTarget, which includes eject_offset. So if the offset exists but no exit order, it places at raw+accrued+offset. That's consistent. So HOLDS.

T-8 Halted/quarantined refusal. A command arriving while blocked is deleted with HALTED. Any path where a blocked instance still moves an order? In OnTick, the poll is called with `engine_blocked = g_grind_halted || g_grind_quarantined`. If blocked, Grind_EjectValidate first checks engine_blocked and returns GRIND_EJECT_HALTED. Then poll emits refused and returns before modifying. So no order move. However, note: The poll deletes the command before validating. So if halted, command is consumed and refused. That's intended. But is there a path where blocked instance still moves an order? The poll is the only mover. But what about the invariant check? If halted, OnTick returns before engine. But the poll is before the halt return. So if halted, poll is called, sees engine_blocked true, refuses. So no movement. If quarantined, same. So HOLDS. But wait: Could `g_grind_quarantined` be true while `g_grind_halted` false, and the poll is called with engine_blocked true. Yes, refuses. So no movement. What about if the command was already accepted before the instance became halted? Then the order was moved while not blocked. That's not "while blocked". So HOLDS.

T-9 Precision. Ticket stored as double in GlobalVariable. Safe for all realistic tickets? MQL5 GlobalVariableSet stores double. MQL5 ticket numbers are ulong, typically up to 2^64? Actually trade ticket is ulong, but in practice may be up to 2^32 or 2^53? The double has 53-bit mantissa, so integers up to 2^53 (9e15) can be represented exactly. Realistic tickets are much smaller (e.g., 123456789). So safe. However, there is a potential issue: The script writes `(double)InpTicket`; the poll reads `(ulong)GlobalVariableGet(cmd_name)`. If ticket > 2^53, precision loss. But realistic tickets are < 2^53. Also, MQL5 GlobalVariableGet returns double; casting to ulong truncates fractional part. Since double can exactly represent integers up to 2^53, safe. So HOLDS. But there is a subtle: GlobalVariableSet stores double, and if the ticket is large, the double may round. For example, ticket 9007199254740993 (2^53+1) cannot be represented. But MT5 tickets are usually 64-bit, but in practice they are generated sequentially and unlikely to exceed 2^53. The question says "all realistic tickets". So HOLDS. Maybe mention INFERRED that broker ticket < 2^53. If not, NEEDS-FIX: store as string? But GV only double. Could store as two GVs or use string in comment? Not smallest. But likely safe.

Now need output format exact headings. Also TEST GAPS. Need list tests missing from Z1-Z16. We don't have Z1-Z16 descriptions. The prompt says "tests missing from Z1-Z16, with expected values". We need infer from ADR testing section and code. The ADR has a Testing section with many tests. The user mentions "33 new-test failures were predicted and seen on a stub". They want test gaps. We can propose tests missing: e.g., zero ticket rejection, poll with ticket 0, modify failure leaves no state (especially if market moved past target), same-tick engine doesn't re-price ejected order, event ordering (hard to test), carry after ejection moves by exactly swap since ejection, re-ejection, restart after ejection with offset, halted/quarantined refusal, precision for large tickets, prune only offset prefix not command, I6 detail includes accrual and offset, clean-up script removes both. But they ask "tests missing from Z1-Z16". We don't have Z1-Z16 list. We can say "Assuming Z1-Z16 cover the ADR bullet list, these are missing: ...". Provide expected values. Need be concrete.

Also need PREMISE VERDICT: is Part B safe to merge? One paragraph. Based on analysis, main gap T-1 zero ticket is defensive; T-2 potential retcode PLACED; but overall safe? The question expects auditor to find issues. Maybe there is a BREAKS hiding. Let's dig deeper.

Re-examine T-1: Can a layer carry position_ticket == 0? In Grind_AppendLayer, position_ticket is set from deal position_id. In live, position_id is nonzero. But there is another path: In Grind_HandleSideDealFill, for c_role == "EXT" and entry_type == DEAL_ENTRY_IN, it sets `side.layers[layer_idx].exit_position_ticket = position_id;` but does not set position_ticket. The layer's position_ticket comes from entry fill. So no. What about reconstruction? Grind_ReconCollectBrokerTickets only collects positions and orders with magic. Positions have ticket >0. So layers built from positions have position_ticket >0. So no layer with 0 in production. But the carry pass explicitly skips ticket 0, suggesting defensive. If no such layer, T-1 is HOLDS (not exploitable). But the threat says "the carry pass skips such layers" as if they can exist. In code, Grind_CarryExitPassBegin does `if(layer.position_ticket == 0) continue;`. That's just a guard. It doesn't prove they exist. Could a layer have position_ticket 0 if a deal has DEAL_POSITION_ID 0? For a pending order that opens a position, DEAL_POSITION_ID is the position ticket. For a deal that increases an existing position, DEAL_POSITION_ID is also the position ticket. For a deal that closes, DEAL_ENTRY_OUT. So always >0. In tests, they might create a layer with 0. But live? Probably not. So T-1: NEEDS-FIX for defensive consistency, but not a live BREAKS. The smallest fix is still add zero check in poll. Should we label NEEDS-FIX or HOLDS? The threat asks "Can a layer carry position_ticket == 0 ... so a hand-set ... ejects it?" If no such layer, then it cannot eject it. But the question implies it might. Since the poll searches by position_ticket, and if no layer has 0, the command with 0 will not find any layer, so it will be refused with NOT_FOUND. So actually T-1 is HOLDS if no layer with 0 exists. But the threat says "the carry pass skips such layers", which might indicate they can exist. Let's see if there is any path that creates a layer with position_ticket 0. In Grind_AppendLayer, the argument position_ticket comes from `position_id` in Grind_HandleSideDealFill. That is `(ulong)Grind_DealGetInteger(deal_ticket, DEAL_POSITION_ID)`. In MQL5, for a deal that opens a position, DEAL_POSITION_ID is the position ticket. It is never 0. For a balance operation, no. So I think no layer with 0 in live. The carry pass skip is just a safety check. So T-1: HOLDS (no such layer), but recommend defensive zero check. However, the prompt says "Can a layer carry position_ticket == 0 (the carry pass skips such layers), so a hand-set ... ejects it?" They want verdict. I can say: In production no, because append only from nonzero DEAL_POSITION_ID; so HOLDS. But tests may create such layer, so for defense NEEDS-FIX? The output format wants verdict HOLDS / BREAKS / NEEDS-FIX. I'd say NEEDS-FIX (defensive) because the poll does not mirror the script's zero refusal, and the carry pass explicitly anticipates zero-ticket layers, so if one ever exists (corrupt state, test stub, future change), a hand-set 0 ejects it. Smallest fix: in Grind_EjectPollCommand after reading position_ticket, if 0, emit refused and return. This is a one-line guard. That's a NEEDS-FIX.

Now T-2 deeper: "The target is computed from the current tick; if the market moves past it before the modify lands, what does the broker return, and does every such path leave NO state written?" We need consider the case where modify succeeds but the order becomes marketable and fills immediately. The broker may return TRADE_RETCODE_DONE for the modify, and the order is filled. Then poll writes offset. Is that a problem? The exit order is filled, so the layer's exit_order_ticket is still the old ticket until OnTradeTransaction processes. The poll sets exit_target = target. Then OnTradeTransaction processes the fill: for c_role == "EXT" && entry_type == DEAL_ENTRY_IN, it sets `side.layers[layer_idx].exit_order_ticket = 0; side.layers[layer_idx].exit_position_ticket = position_id;` and queues CloseBy. It does not delete eject_offset at that point. Later DEAL_ENTRY_OUT_BY for the close will emit EJECT_FILLED and delete offset. So the state is consistent. The only issue: if the modify succeeds but the order is filled, the poll's subsequent `Grind_CarryShiftDelete` deletes shift. That's fine. So no inconsistency. What if the modify fails because price is invalid (market moved past)? Then no state written. So T-2 HOLDS. But wait: There is a path where `Grind_ModifyPendingPrice` returns false if `Grind_SelectOurOrder` fails. That could happen if the order was already filled/cancelled. Then no state. Good.

But consider: The target is computed from `Grind_EjectTargetPrice(is_long)` which uses `Grind_MarketBid/Ask` and `Grind_MarketStopsLevel/FreezeLevel`. Then `Grind_ModifyPendingPrice` calls `Grind_Normalize(new_price)` and sends. If the market moves, the broker may reject with `TRADE_RETCODE_INVALID_PRICE` or `TRADE_RETCODE_INVALID_STOPS`. Returns false. No state. So HOLDS.

T-3 deeper: Same-tick engine. Could `Grind_EnsureAddNext` modify the exit? No. Could `Grind_ExitQManageSide` modify an existing exit order? It only cancels or places. It does not modify existing exit orders. So no. But what about `Grind_ExitQHoldCancelLayer`? It cancels if not allowed. The ejected layer is allowed. So no. What about a trim? ADR-151 trim might cancel exits for ranks not allowed. The ejected layer is rank depth-1, always allowed. So no. What about a re-release? If the exit order was cancelled by something else in the same tick before the engine? The ejection happens before engine. There is no other cancellation between. So no. So HOLDS.

T-4: MQL5 handlers sequential. Confirm. But there is a nuance: `OnTradeTransaction` can be called during `OnTick`? According to MQL5, event handlers are not called recursively. The trade transaction event is added to the queue and processed after the current handler returns. So no observation between modify and offset write. However, the broker itself changes the order price. The invariant check runs in OnTick before the poll? Actually OnTick order: first `Grind_ProcessCloseByQueues`, then if !halted: cap, `Grind_CheckBookInvariants()`, quarantine step. Then poll. So invariant check runs before poll in the same tick. So it doesn't observe the new price after poll in the same tick. Next tick, state is written. So HOLDS. But what about OnTimer? It runs on a timer event, which is also sequential. So no interleaving. HOLDS.

T-5: We already did. HOLDS. But let's check I6 after carry. The carry pass sets `Grind_CarryShiftSet(position_ticket, applied_shift)`. The I6 check uses `Grind_CarryShiftGetForRecon(position_id)` which may validate bound and delete shift if out of bound. If the shift is large (e.g., clamp), it might be deleted by bound check, causing I6 failure. The bound check `Grind_CarryShiftWithinBound` uses `nightly_max_pips` from swap. For an ejected layer, the carry shift could be small (clamp residual). But if the market moved far, the clamp could be large? Actually carry pass only shifts by accrued_pips, which is small. The clamp residual is the difference between theoretical and clamped passive price. If market moved such that theoretical is no longer passive, the clamp could be large. For an ejected layer, formula_exit = raw + offset. If the market moved far, theoretical = raw+offset+accrued might be far from market, so clamp could be large. Then shift could be large. The bound check `max_price = (nights+7)*nightly_max_pips*pip_size*2`. If the market moved far, the clamp residual could exceed this bound, and `Grind_CarryShiftGetForRecon` would delete the shift and return 0. Then I6 would fail because expected price without shift differs from actual exit. This is a BREAKS? Let's examine. The carry pass sets shift to `applied_shift = Grind_Normalize(new_exit) - intended`. If clamped, new_exit is the passive market price, which could be far from intended if the market moved far. The shift could be large. Then on the next invariant check (or restart), `Grind_CarryShiftGetForRecon` calls `Grind_CarryShiftGetValidated`, which checks `Grind_CarryShiftWithinBound`. If the shift exceeds the bound, it deletes it and returns 0. Then I6 fails because the actual exit price includes the clamp but the expected doesn't. This is a known issue? The bound is designed to prevent runaway shifts from bad data. But for an ejected layer, the offset is supposed to handle large moves. The carry shift should only be the clamp residual at rollover. If the market moved far, the clamp residual could be large. But wait: The carry pass only runs at 23:50 rollover window. The market could have moved far since ejection. The ejected target is fixed. At rollover, the carry pass recomputes theoretical = raw + offset + accrued. If the market has moved far away from the ejected target, the theoretical might be far from market. The clamp will set new_exit to the best passive price (ask+min_dist for long). That could be a large shift from theoretical. Then shift is large. The bound check might delete it. Then I6 fails. The ADR says "large move then re-release: an ejected exit trimmed and re-released after the market has moved far keeps its offset; nothing bound-deletes it". But that's about re-release, not carry. The carry pass can set a large shift. However, note that the carry pass is only enabled if `InpEnableCarryPass` is true. But in OnTimer, it's called with `InpEnableCarryPass`. And in OnInit, if InpEnableCarryPass is true, it fails: `if(InpEnableCarryPass) { Print("FATAL: ADR-151 phase A requires InpEnableCarryPass=false"); return INIT_FAILED; }`. Wait! In fxgrind.mq5 OnInit, there is a check: `if(InpEnableCarryPass) { Print("FATAL: ADR-151 phase A requires InpEnableCarryPass=false"); return INIT_FAILED; }`. So the carry pass is currently disabled in production! The ADR says "Depends on F1... ships after... behind a disabled switch." The carry pass is enabled by InpEnableCarryPass, but OnInit refuses if it's true. So the carry pass is not active. The ADR-155 says "carry pass INCLUDES ejected layers" for rev 4. But since InpEnableCarryPass is forced false, the carry pass never runs in production. However, the code includes it for future. The threat T-5 asks "At the next rollover the carry pass builds from raw + offset and recomputes accrual. Show the exit moves by exactly the swap accrued SINCE the ejection, and I6 still holds." This assumes carry pass is enabled. But currently it's disabled. Still, we should analyze the code as written. If enabled, does it hold? The large movement issue with bound check is a potential BREAKS. Let's see: The bound check is in `Grind_CarryShiftGetValidated`. It is called by `Grind_CarryShiftGetForRecon` during I6. The shift set by carry pass is `applied_shift = new_exit - intended`. If clamp occurs, new_exit = best passive price. The intended = formula + accrued = raw + offset + accrued. The difference is the distance from the ejected target (adjusted for accrued) to the market. If the market has moved far, this could be large. The bound is based on nightly max pips * (nights+7) * 2. For a large move, it could exceed. Then the shift is deleted, and I6 fails. This is a real BREAKS if carry pass is enabled and market moves far. But is the carry pass expected to handle large moves? The ADR says "The offset has no bound check, deliberately: an ejection is supposed to be large." The shift does have a bound check. The carry pass is supposed to only shift by accrued, not by market move. But if the market moved far, the clamp forces a large shift. That large shift is essentially a re-pricing to market, which is not carry. The ADR's testing says "large move then re-release: an ejected exit trimmed and re-released after the market has moved far keeps its offset; nothing bound-deletes it". That test is about re-release, not carry. But if carry pass runs, it could bound-delete the shift. However, the carry pass only runs at 23:50. If the market moved far, the clamp could be large. Is there a guard? `Grind_CarrySignGuardAppliesAtShift` is skipped for ejected layers. So it will clamp. Then shift is set. Then I6 on next tick uses `Grind_CarryShiftGetForRecon` which validates bound. If out of bound, deletes shift -> I6 fails. This is a BREAKS for T-5? The threat specifically asks to show exit moves by exactly swap accrued since ejection and I6 holds. It does not hold if clamp occurs and shift is bound-deleted. But the threat may assume no clamp or small clamp. However, the question is a red-team: we should find this. Let's verify the bound check for shift. `Grind_CarryShiftWithinBound`: `max_price = (double)max_nights * nightly_max_pips * pip_size * 2.0;` `max_nights = nights + 7` where nights = (now - open_time)/86400. So for a position open 1 day, max_nights=8, nightly_max_pips from swap. If swap is small, max_price small. A large market move would exceed. Then `Grind_CarryShiftGetValidated` deletes shift and returns 0. Then I6 expected lacks shift, actual exit includes clamp, diff > 2 points -> I6 fails -> HALT. This is a critical BREAKS. But wait: The carry pass sets `Grind_CarryShiftSet(position_ticket, applied_shift);` and also `Grind_CarryAccruedSet(position_ticket, accrued_price);`. The I6 check uses `Grind_CarryShiftGetForRecon(position_id)` which returns 0 if bound-deleted. So I6 fails. The ADR says "an ejected exit must drift with the swap it pays like any other; freezing it would leave an exit that no longer covers its funding." But the clamp at rollover could move it to market, not just swap. That's a design issue. However, is the carry pass currently enabled? No, InpEnableCarryPass forced false. So in production, T-5 is not active. But the ADR is for future. The threat says "At the next rollover the carry pass builds..." So we assume it's enabled. The code as written has this flaw. We should mark T-5 as BREAKS or NEEDS-FIX. The smallest fix: In `Grind_CarryShiftGetForRecon`, do not validate/delete shift for ejected layers? Or set the release GV to bypass bound check? Actually `Grind_CarryShiftGetValidated` checks `release_gv` first: `if(GlobalVariableCheck(release_gv)) return Grind_CarryShiftGet(position_ticket);` So if the release GV is set, it bypasses bound check. In `Grind_ExitQManageSide`, when it places an exit with a shift, it sets the release GV: `GlobalVariableSet(Grind_CarryReleaseGvName(...), 1.0);`. But in `Grind_CarryExitShiftLayer`, when it sets the shift, it does NOT set the release GV. So the shift is subject to bound check. The ADR says "large move then re-release: an ejected exit trimmed and re-released after the market has moved far keeps its offset; nothing bound-deletes it". That refers to the re-release path in `Grind_ExitQManageSide`, which sets the release GV. But the carry pass path does not set the release GV. So if the carry pass sets a large shift, it can be bound-deleted. Is that a bug? The carry pass shift is supposed to be only accrued, so it should be small. But if clamp occurs, it can be large. The clamp happens if the theoretical price is not passive. For an ejected layer, theoretical = raw + offset + accrued. The ejected target was passive at ejection. After market moves, it might not be passive. So clamp will move it to market. That is a large shift. Then bound check deletes it. So I6 fails. This is a real BREAKS for T-5. But wait: The carry pass only runs if `InpEnableCarryPass` is true. But OnInit forbids it. So the code path is dead. The threat says "At the next rollover the carry pass builds..." So we should evaluate as if enabled. The verdict could be NEEDS-FIX: ensure carry pass sets the release GV or exempts ejected layers from bound check. Smallest fix: in `Grind_CarryExitShiftLayer`, after setting shift, also set `GlobalVariableSet(Grind_CarryReleaseGvName(position_ticket), 1.0);`? But that release GV is for the exit queue re-release. Actually `Grind_CarryShiftGetValidated` checks `GRIND_CARRY_RELEASE_PREFIX`. If we set it, the bound check is bypassed. But then the shift is considered "released" and not validated. That might be acceptable. Alternatively, in `Grind_CarryShiftGetForRecon`, if `Grind_EjectIsEjected(position_id)`, return shift without bound check. That is more targeted. The ADR says "large move then re-release: an ejected exit trimmed and re-released after the market has moved far keeps its offset; nothing bound-deletes it". It doesn't mention carry pass. But the carry pass can create a large shift. So NEEDS-FIX.

Let's check T-5 wording: "At the next rollover the carry pass builds from raw + offset and recomputes accrual. Show the exit moves by exactly the swap accrued SINCE the ejection, and I6 still holds." If we assume no clamp (market near target), it moves by accrued and I6 holds. If clamp occurs, it doesn't move by exactly swap and I6 may fail due to bound check. The question asks to show it. So we should show the math and then point out the clamp/bound issue. Verdict: BREAKS (or NEEDS-FIX) because the clamp can move it by more than swap and the bound check can delete the shift causing I6 halt. Smallest fix: in `Grind_CarryExitShiftLayer`, if clamped, the shift is the clamp residual; ensure `Grind_CarryShiftSet` also sets the release GV (bypass bound) or exempt ejected layers in `Grind_CarryShiftGetValidated`. Actually the release GV is a separate GV `GRIND_CARRY_RELEASE_<ticket>`. Setting it will make `Grind_CarryShiftGetValidated` return the shift without bound check. So one line: `if(clamped_out) GlobalVariableSet(Grind_CarryReleaseGvNameLocal(position_ticket), 1.0);` But `Grind_CarryReleaseGvNameLocal` is defined earlier. That would bypass bound. However, it also affects other logic? `Grind_CarryShiftGetValidated` is the only user. So it's safe. Alternatively, in `Grind_CarryShiftGetForRecon`, if `Grind_EjectIsEjected(position_id)`, return `Grind_CarryShiftGet(position_id)` directly. That's also one line. I'd suggest the latter: exempt ejected layers from shift bound validation, because the offset is unbounded by design. But the shift is not the offset; it's the clamp residual. Still, for an ejected layer, the exit can be far from formula, so the shift can be large. The ADR says "The offset has no bound check, deliberately: an ejection is supposed to be large." The shift is used to reconcile the actual price with formula+accrued+offset. If the actual price is far, the shift must be large. So it should not be bound-checked for ejected layers. So fix: in `Grind_CarryShiftGetForRecon`, if `Grind_EjectIsEjected(position_id)`, return `Grind_CarryShiftGet(position_id)` without validation. Or in `Grind_CarryShiftGetValidated`, add a parameter `is_ejected` to bypass. Smallest: in `Grind_CarryShiftGetForRecon`, add `if(Grind_EjectIsEjected(position_id)) return Grind_CarryShiftGet(position_id);` before calling validated. That is one line.

But wait: If the carry pass sets a large shift, and I6 checks it without bound validation, I6 will pass because the shift matches the actual price. So that fixes it. So T-5 NEEDS-FIX.

Now T-6: Re-ejection. We said HOLDS. But consider: A second command on an already-ejected layer. The poll reads accrued. But accrued might have been updated by carry pass. The new offset = target_new - raw - accrued_current. The old offset is overwritten. The exit_target set to target_new. The shift is deleted. I6 expected = raw + accrued_current + new_offset = target_new. So holds. However, what if the layer was ejected, then carry pass ran and set a shift because of clamp. Then re-ejection deletes the shift. The accrued is unchanged. The new offset = target_new - raw - accrued_current. The new exit is set to target_new. I6 holds. But what about the old offset? It's overwritten. So no inconsistency. So HOLDS.

T-7: Restart after ejection. We said HOLDS. But check: In `Grind_RebuildBookFromTicketsInner`, when building layers, if `Grind_ReconLayerHasExitCoverage` is true, it sets `exit_target = long_scratch[j].exit_target;` which came from the order price. If the order is missing, it computes formula with `Grind_ExitQFormulaTarget`, which includes eject_offset. So either way, exit_target equals the resting price or the formula+offset. So I6 passes. But what about the carry shift? The reconstruction calls `Grind_CarryShiftGetForRecon` which may bound-delete the shift. If the shift is large, it deletes it, and I6 fails. This is the same issue as T-5. If the ejected layer has a large shift (from a clamp or re-release), restart could delete the shift and halt on I6. The ADR's testing says "restart after an accepted ejection: reconstruction passes I6". It doesn't mention large shift. But if the shift is large due to market move, restart could fail. The threat T-7 asks: "Reconstruction (OnInit, ADR-156 tolerant startup) with an ejected layer: does I6 pass, and does the rebuilt exit_target equal the resting price?" If there is no shift (or small shift), yes. If there is a large shift, the bound check could delete it and I6 fails. So T-7 also NEEDS-FIX for the same reason. The smallest fix: exempt ejected layers from shift bound validation in reconstruction. That fixes both T-5 and T-7.

T-8: Halted/quarantined refusal. HOLDS. But check: The poll is called with `g_grind_halted || g_grind_quarantined`. If the instance is halted, the poll deletes the command and returns HALTED. No order move. What if the instance is not halted but the invariant check just failed and triggered quarantine? The poll is called after quarantine step. If quarantine step returns HALT, g_grind_halted is set true, so engine_blocked true. If quarantine step returns something else (e.g., QUARANTINE), g_grind_quarantined might be true. The poll sees engine_blocked true. So refuses. So no movement. HOLDS.

T-9: Precision. HOLDS with caveat.

Now, are there any other hidden issues? Let's re-examine T-3 same-tick engine more carefully. After ejection, the poll sets `layer.exit_target = target`. Then `Grind_OnTickEngine` runs. Inside, `Grind_RetryMissingExits` calls `Grind_ExitQManageSide`. In that function, for each layer, it computes ranks from entry prices. The ejected layer is deepest, rank depth-1. It checks `if(!Grind_ExitQAllowed(ranks[i], n) && side.layers[i].exit_order_ticket != 0) Grind_ExitQHoldCancelLayer(...)`. `Grind_ExitQAllowed` returns true if rank < K+H or rank == depth-1. So deepest is allowed. So no cancel. Then second loop: `if(!Grind_ExitQRequired(ranks[i], n)) continue;` Deepest is required. Then `if(side.layers[i].exit_order_ticket != 0 || side.layers[i].exit_position_ticket != 0) continue;` The ejected layer has exit_order_ticket != 0 (we just modified it). So it skips. So no re-price. What about `Grind_EnsureAddNext`? It may place a new entry order, but doesn't touch exits. So no. So T-3 HOLDS.

But wait: What if the ejection command is accepted on a layer, but the layer's exit_order_ticket is 0? Validation requires has_exit_order true. So it must have an exit order. So exit_order_ticket != 0. Good.

Now T-2: What if the modify succeeds but the broker returns TRADE_RETCODE_PLACED? `Grind_ModifyPendingPrice` returns true for DONE or PLACED. If PLACED, the order might not be modified yet? In MQL5, for TRADE_ACTION_MODIFY, the retcode should be DONE. PLACED is for placing a new order. If a broker returns PLACED for modify, it likely means the modify request was accepted but not yet executed? Actually MQL5 docs: TRADE_RETCODE_PLACED = 10008, "The order was placed". It is used for pending order placement. For modify, the success code is TRADE_RETCODE_DONE. So if PLACED is returned, it's probably an error or a broker quirk. But the code treats it as success. Then it writes offset, deletes shift, sets exit_target. If the order price was not actually changed, the state is inconsistent: layer.exit_target = target, but the resting order is still at old price. Then I6 will check the resting order price? Wait, I6 checks the layer's exit_target against formula+accrued+offset+shift. It does not check the broker order price directly; it checks the layer's exit_target field. But in the same tick, the invariant check already ran before the poll. Next tick, the invariant check runs `Grind_CheckBookInvariants` which rebuilds from broker tickets. It scans the actual order price. If the order price is still old, the rebuilt exit_target = old price. Then I6 expected = raw + accrued + offset + shift. offset = target - raw - accrued. So expected = target + shift. But actual = old price. If old price != target, I6 fails. So if the modify didn't actually change the price, I6 will fail on next tick. But the code wrote offset, so I6 fails. This is a BREAKS if PLACED can be returned without price change. Is that realistic? Probably not. But we can mention as a NEEDS-FIX: only accept TRADE_RETCODE_DONE for modify, not PLACED. The existing `Grind_ModifyPendingPrice` is used elsewhere (carry pass, exit queue) and also accepts PLACED. Changing it might affect other paths. But for ejection, we could check that the order price actually equals target after modify? Or just tighten `Grind_ModifyPendingPrice` to only DONE. However, the question asks "what does the broker return, and does every such path leave NO state written?" The broker returns an error for market moved past (invalid price). That path leaves no state. The PLACED path is unlikely. I'll mention it as a minor NEEDS-FIX or note.

Now, another issue: In `Grind_EjectPollCommand`, the command variable is deleted before validation. If the poll returns early because `GlobalVariableCheck` is false, returns -1. If it reads and deletes, then validates. If validation fails, it emits refused and returns. That's fine. But what if the command is for a valid ticket, but the instance is blocked? It deletes and refuses. That's per spec.

Now, check the script: `GlobalVariableSet(name, (double)InpTicket);` If InpTicket is large, precision loss as discussed.

Now, check T-1: The poll searches layers by `g_grind_long.layers[i].position_ticket == position_ticket`. If position_ticket is 0, it will match a layer with position_ticket 0. Can such a layer exist? In `Grind_AppendLayer`, it sets `side.layers[n].position_ticket = position_ticket;` where position_ticket comes from `position_id` in `Grind_HandleSideDealFill`. That is `(ulong)Grind_DealGetInteger(deal_ticket, DEAL_POSITION_ID)`. For a deal that opens a position, DEAL_POSITION_ID is the position ticket. In MQL5, it's always >0. For a deal that closes, it's not appended. So no. But what about `Grind_Recon`? It only creates layers from positions, which have ticket >0. So no. So T-1 is HOLDS in production. But defensive fix is still good. I'll label it NEEDS-FIX (defensive) because the script explicitly refuses 0 but the poll does not, and the carry pass skips 0, indicating awareness. The smallest fix is one line.

Now, check T-4: "MQL5 handlers are sequential -- confirm or refute." Confirm. But there is a catch: `OnTradeTransaction` can be called from within `OnTick`? No, MQL5 event model is single-threaded, non-reentrant. So HOLDS.

Now, TEST GAPS. We need list tests missing from Z1-Z16. The prompt says "tests missing from Z1-Z16, with expected values". We don't know what Z1-Z16 are. They are likely the 33 new tests. We can infer from ADR testing section. The ADR lists many tests. The user says "the 33 new-test failures were predicted and seen on a stub". So the test suite has Z1-Z16? Maybe Z1-Z16 are the test cases. We can list gaps relative to the threats. For each threat, propose a test. For example:

- Z? Zero ticket: set GRIND_EJECT_<magic> = 0, seed a layer with position_ticket=0, expect refused and no modify. But if no layer with 0, expect NOT_FOUND.
- Z? Modify failure stale: seed market, set command, then before poll? Hard to simulate. Use order test hook to make modify fail (send_ok=false or retcode != DONE). Expect EJECT_REFUSED MODIFY_FAILED, no offset, no shift delete, exit_target unchanged.
- Z? Same-tick engine: after accepted ejection, run Grind_OnTickEngine with same tick. Expect exit order price unchanged, exit_target unchanged, no new shift.
- Z? Event ordering: not testable in unit tests; integration.
- Z? Carry after ejection: seed ejected layer with offset, enable carry pass (but OnInit forbids; test via Grind_CarryExitPassStep directly). Set swap, run carry, expect new exit = old target + swap since ejection, accrued updated, shift 0 or clamp residual, I6 passes.
- Z? Carry after ejection with large move and clamp: seed market far from target, run carry, expect shift set and I6 passes (currently would fail due to bound delete). This is a test gap.
- Z? Re-ejection: eject twice, expect offset overwritten, exit_target new, shift deleted, I6 passes.
- Z? Restart after ejection: simulate reconstruct with ejected layer and large shift, expect I6 passes (currently would fail if shift bound-deleted).
- Z? Halted refusal: set g_grind_halted true, set command, expect removed, no modify.
- Z? Precision: set ticket > 2^53, expect poll reads correct? But if >2^53, double precision loss. Maybe test with ticket 9007199254740992 (2^53) and 9007199254740993. Expect the latter fails. But realistic tickets are smaller. Test that 2^53 is exact.
- Z? Prune: orphan GRIND_EJECT_OFFSET_ is pruned; pending GRIND_EJECT_<magic> is NOT. The code prunes only GRIND_EJECT_OFFSET_ prefix. Test: set GRIND_EJECT_OFFSET_999 and GRIND_EJECT_2226. Call Grind_CarryPruneShiftGvs. Expect OFFSET deleted if no position, command kept.
- Z? I6 detail includes accrual and eject_offset. Test: set accrual and offset, trigger I6 failure, check JSON payload contains both.
- Z? Clean-up script removes both prefixes. Test: run clean script, check GVs gone.

We need output format exact headings. The user gave example:

```
GIVENS CHECK     -- G1..G6: CONFIRMED / WRONG (with line)
T-1 .. T-9       -- verdict, evidence, smallest fix
PREMISE VERDICT  -- is Part B safe to merge? one paragraph
TEST GAPS        -- tests missing from Z1-Z16, with expected values
Line count: 96
```

Wait, the line count at the bottom of the user message says "Line count: 96". That is the line count of the prompt? The user said "This message has a line count at the bottom". Actually the prompt ends with "Line count: 96". That's part of the prompt? It might be a test to see if we notice. We should not include that in our answer? The output format doesn't include line count. Probably ignore.

We need produce final answer with exact headings. We should be thorough but not too long. Desired oververbosity 5. But the task is complex. We'll write a structured audit.

Need cite file:line. Since we don't have line numbers, we can use the ones provided in the prompt for G1-G6 and for other claims cite function names and approximate lines from the prompt's own references? The prompt gives some line numbers: fxgrind.mq5:306, grind_engine.mqh:380, grind_pure.mqh:284, grind_carry.mqh:552, grind_engine.mqh:1530. We can use those. For other code, we can cite as `grind_engine.mqh:Grind_EjectPollCommand` or `grind_carry.mqh:Grind_CarryExitPassBegin`. The rule says "Cite file:line for every claim about the code; otherwise label it INFERRED." Since we don't have exact line numbers for many, we can say `grind_engine.mqh:Grind_EjectPollCommand` and if needed, "line inferred from function start". That might be acceptable. But to be safe, we can use the line numbers from the prompt where given, and for others, say "file:function" and label INFERRED if line unknown. Alternatively, we can use the line numbers from the code as if they are known? The code blocks don't have line numbers. I'll cite function names and the line numbers provided in the prompt. For G1-G6, the prompt already gives lines. I'll confirm with those.

Let's map:
- G1: fxgrind.mq5:306 (poll call). Confirmed.
- G2: grind_engine.mqh:380 (Grind_EjectPollCommand). Confirmed.
- G3: grind_pure.mqh:284 (Grind_EjectValidate). Confirmed.
- G4: grind_carry.mqh:552 (Grind_EjectTargetPrice). Confirmed.
- G5: grind_engine.mqh:Grind_EjectPollCommand (after modify). Confirmed.
- G6: grind_engine.mqh:1530 (EJECT_FILLED before deletes). Confirmed.

For threats, cite:
- T-1: grind_engine.mqh:Grind_EjectPollCommand (search by position_ticket), grind_carry.mqh:Grind_CarryExitPassBegin (skip 0), scripts/grind_eject.mq5:OnStart (refuses 0).
- T-2: grind_engine.mqh:Grind_EjectPollCommand (target, modify, fail return), grind_engine.mqh:Grind_ModifyPendingPrice (retcode check).
- T-3: grind_engine.mqh:Grind_ExitQManageSide (cancel/place logic), grind_exitq.mqh:Grind_ExitQAllowed/Required.
- T-4: MQL5 event model (INFERRED).
- T-5: grind_carry.mqh:Grind_CarryExitPassBegin (formula + offset), grind_carry.mqh:Grind_CarryExitShiftLayer (sets accrued and shift), grind_recon.mqh:Grind_ReconExitMatchesEntry (I6), grind_carry.mqh:Grind_CarryShiftGetValidated (bound delete).
- T-6: grind_engine.mqh:Grind_EjectPollCommand (overwrites offset, deletes shift).
- T-7: grind_recon.mqh:Grind_RebuildBookFromTicketsInner (exit_target from order price), grind_recon.mqh:Grind_ReconExitMatchesEntry.
- T-8: fxgrind.mq5:OnTick (poll with blocked), grind_pure.mqh:Grind_EjectValidate (first check blocked).
- T-9: scripts/grind_eject.mq5:GlobalVariableSet double, grind_engine.mqh:GlobalVariableGet cast ulong.

Now, for T-5, we need show exit moves by exactly swap accrued since ejection and I6 holds. Let's do the math with symbols.

Let:
- raw = Grind_ExitPrice(entry, exit_pips, point, dir) = entry + dir * exit_pips * point * 10.
- acc_e = accrued at ejection (total accrued so far).
- target = ejected price.
- offset = target - raw - acc_e.
- At next carry: formula = raw + offset = target - acc_e.
- accrued_total = total accrued from open to now (including pending for next rollover). In code, `accrued_price = theoretical - formula_exit` where theoretical = formula - dir * accrued_pips * pip_size. So accrued_price = -dir * accrued_total.
- new_exit = theoretical = raw + offset + accrued_price = target - acc_e + accrued_price.
- I6 expected = raw + accrued_current + offset + shift = raw + accrued_price + (target - raw - acc_e) + shift = target + (accrued_price - acc_e) + shift. Since new_exit = target - acc_e + accrued_price, expected = new_exit + shift. If no clamp, shift=0, I6 holds. Exit moves by (accrued_price - acc_e) = swap accrued since ejection. If clamp, shift = new_exit_clamped - theoretical, so I6 holds as long as shift is valid. But bound check may delete shift -> I6 fails. So T-5 NEEDS-FIX.

Wait: The code sets `Grind_CarryAccruedSet(position_ticket, accrued_price);` where `accrued_price = theoretical - formula_exit`. That is the total accrued since open? Actually `theoretical = formula - direction * accrued_pips * pip_size`. `accrued_pips = ledger_pips + pending_pips`. `ledger_pips` is derived from `POSITION_SWAP` which is total swap accumulated on the position since open. So yes, total accrued. So `accrued_price` is total accrued from open. At ejection, `acc_e` was the accrued from open up to ejection. So `accrued_price - acc_e` is swap since ejection. So the exit moves by exactly that if no clamp. With clamp, it moves by that plus clamp adjustment. The threat says "Show the exit moves by exactly the swap accrued SINCE the ejection, and I6 still holds." In the no-clamp case, yes. In the clamp case, it moves by more (to market) and I6 holds only if shift is not bound-deleted. Since the bound check can delete the shift, I6 may fail. So the statement "I6 still holds" is not guaranteed. So T-5 is BREAKS (or NEEDS-FIX) for the clamp scenario. The smallest fix: exempt ejected layers from shift bound validation.

Now, check T-7: "Restart after ejection: Reconstruction with an ejected layer: does I6 pass, and does the rebuilt exit_target equal the resting price?" In `Grind_RebuildBookFromTicketsInner`, when it finds an EXT order, it sets `exit_target = tickets[i].price`. So yes, rebuilt exit_target equals the resting price. Then `Grind_ReconCheckInvariants` checks I6 using `Grind_CarryShiftGetForRecon`. If the shift is large and bound-deleted, I6 fails. So again, same issue. So T-7 NEEDS-FIX for large shift. If shift is small/absent, HOLDS.

Now, test gaps: We should list missing tests. Since we don't have Z1-Z16, we can say "Z1-Z16 as described in ADR-155 Testing do not cover:" and then list. Expected values.

Let's draft final answer.

We need exact headings:
```
GIVENS CHECK     -- G1..G6: CONFIRMED / WRONG (with line)
T-1 .. T-9       -- verdict, evidence, smallest fix
PREMISE VERDICT  -- is Part B safe to merge? one paragraph
TEST GAPS        -- tests missing from Z1-Z16, with expected values
```

We'll fill.

For GIVENS CHECK, list G1..G6.

For T-1..T-9, each with verdict, evidence, smallest fix.

For PREMISE VERDICT, one paragraph. Should say: Part B is close but not safe to merge as-is due to T-5/T-7 bound-deletion of carry shift for ejected layers under large moves, and T-1 defensive zero-ticket gap. The core mechanism is sound. Fixes are small. If carry pass is currently disabled, the immediate risk is low, but the code is written to enable later. So merge only after fixes.

For TEST GAPS, list missing tests with expected values.

Let's write.

Need line count? The prompt says "Line count: 96" at bottom. That's likely the line count of the prompt, not our answer. We can ignore.

Let's produce final.

## Final Report

GIVENS CHECK     -- G1..G6: CONFIRMED / WRONG (with line)

G1 CONFIRMED. `fxgrind.mq5:306` calls `Grind_EjectPollCommand(... g_grind_halted || g_grind_quarantined)` after the invariant/quarantine block and before `if(g_grind_halted) return;`. The argument is exactly `g_grind_halted || g_grind_quarantined`.

G2 CONFIRMED. `grind_engine.mqh:380` (`Grind_EjectPollCommand`) reads `GlobalVariableGet(cmd_name)` then immediately `GlobalVariableDel(cmd_name)` before any validation.

G3 CONFIRMED. `grind_pure.mqh:284` (`Grind_EjectValidate`) checks in order: `engine_blocked`, `switch_on`, `!found`, `depth < 2`, `rank != depth - 1`, `!has_exit_order`. Ranks in the poll come from `Grind_ExitQRanks`, which uses `Grind_ExitQEntryBeats` and entry price only (`grind_exitq.mqh:Grind_ExitQEntryBeats`).

G4 CONFIRMED. `grind_carry.mqh:552` (`Grind_EjectTargetPrice`): long calls `Grind_CarryClampLongExit(bid, bid, ask, ...)`; since `bid <= ask`, clamp returns `ask + min_dist`. Short calls `Grind_CarryClampShortExit(ask, bid, ask, ...)`; returns `bid - min_dist`.

G5 CONFIRMED. In `grind_engine.mqh:Grind_EjectPollCommand`, if `Grind_ModifyPendingPrice` fails, it emits `EJECT_REFUSED MODIFY_FAILED` and returns before any state write. On success only, it computes `offset = target - raw - accrued`, writes `Grind_EjectOffsetSet`, deletes carry shift, and sets `layer.exit_target = target`.

G6 CONFIRMED. In `grind_engine.mqh:1530` (`Grind_HandleSideDealFill`, `DEAL_ENTRY_OUT_BY`), `EJECT_FILLED` is emitted inside the `if(Grind_EjectIsEjected(closed_position))` block, before `Grind_CarryShiftDelete`, `Grind_CarryAccruedDelete`, `Grind_EjectOffsetDelete`, and `Grind_RemoveLayerAt`.

T-1 .. T-9       -- verdict, evidence, smallest fix

T-1 Zero ticket — NEEDS-FIX (defensive).  
Evidence: The script refuses `InpTicket == 0` (`scripts/grind_eject.mq5:OnStart`). The poll does not: it casts `(ulong)GlobalVariableGet(cmd_name)` and searches layers for `position_ticket == position_ticket` (`grind_engine.mqh:Grind_EjectPollCommand`). The carry pass explicitly skips layers with `position_ticket == 0` (`grind_carry.mqh:Grind_CarryExitPassBegin`), so such a layer can exist in corrupt/test state. If it does, a hand-set `GRIND_EJECT_<magic> = 0` matches it and can eject it. In normal live state, `Grind_AppendLayer` only receives nonzero `DEAL_POSITION_ID`, so a zero-ticket layer should not exist.  
Smallest fix: In `Grind_EjectPollCommand`, immediately after `position_ticket = (ulong)GlobalVariableGet(cmd_name); GlobalVariableDel(cmd_name);`, add `if(position_ticket == 0) { emit EJECT_REFUSED reason "BAD_TICKET"; return GRIND_EJECT_NOT_FOUND; }`.

T-2 Target stale in flight — HOLDS (with minor retcode caveat).  
Evidence: Target is computed, then `Grind_ModifyPendingPrice` is called. If the market has moved past the target, the broker returns a non-DONE retcode (e.g. `TRADE_RETCODE_INVALID_PRICE`/`INVALID_STOPS`), `Grind_ModifyPendingPrice` returns false, and the poll returns `GRIND_EJECT_MODIFY_FAILED` before any offset/shift/exit_target write (`grind_engine.mqh:Grind_EjectPollCommand`). If the modify succeeds but fills immediately, the state write happens, but the subsequent `OnTradeTransaction` path cleans it via `EJECT_FILLED` and deletes (`grind_engine.mqh:1530`). Minor caveat: `Grind_ModifyPendingPrice` accepts `TRADE_RETCODE_PLACED` as success (`grind_engine.mqh:Grind_ModifyPendingPrice`); for a modify, only `DONE` is normally valid. If a broker ever returns `PLACED` without changing the price, state would be written against an unchanged order and I6 would fail on the next tick.  
Smallest fix: For this path, require `res.retcode == TRADE_RETCODE_DONE` only; or after a successful modify, verify `Grind_OrderGetPriceOpen(exit_order_ticket)` equals `req.price` before writing state.

T-3 Same-tick engine — HOLDS.  
Evidence: After the poll, `Grind_OnTickEngine` calls `Grind_RetryMissingExits` → `Grind_ExitQManageSide`. That function only cancels exits where `!Grind_ExitQAllowed(rank, n)` and only places when `exit_order_ticket == 0` and `Grind_ExitQRequired(rank, n)` (`grind_engine.mqh:Grind_ExitQManageSide`). The ejected layer is validated to be `rank == depth - 1` (`grind_pure.mqh:284`). `Grind_ExitQAllowed` and `Grind_ExitQRequired` both return true for `rank == depth - 1` (`grind_exitq.mqh:Grind_ExitQAllowed`, `Grind_ExitQRequired`). The ejected layer already has `exit_order_ticket != 0` after the successful modify. Therefore the same-tick engine does not cancel, re-price, or re-release it. No trim path touches `rank == depth - 1` either.

T-4 Event ordering — HOLDS.  
Evidence: MQL5 event handlers (`OnTick`, `OnTradeTransaction`, `OnTimer`) are executed sequentially and are not reentrant. The modify in `Grind_EjectPollCommand` is a trade request; the resulting `OnTradeTransaction` event is queued and processed after the current handler exits. The invariant check runs earlier in the same `OnTick` (`fxgrind.mq5:OnTick`), before the poll. Therefore no other handler or invariant check can observe the new order price with old in-memory state between the successful modify and the offset write. INFERRED for the MQL5 event model; source shows no yield between modify and state write.

T-5 Carry after ejection — NEEDS-FIX.  
Evidence: `Grind_CarryExitPassBegin` builds `formula = Grind_ExitPrice(...) + Grind_EjectOffsetGet(layer.position_ticket)` (`grind_carry.mqh:Grind_CarryExitPassBegin`). `Grind_CarryExitShiftLayer` then computes `theoretical = formula - dir * accrued_pips * pip_size`, sets `accrued_price = theoretical - formula`, and on successful modify writes `Grind_CarryAccruedSet(position_ticket, accrued_price)` and `Grind_CarryShiftSet(position_ticket, applied_shift)` (`grind_carry.mqh:Grind_CarryExitShiftLayer`).  
Let `raw = Grind_ExitPrice(entry, exit_pips, point, dir)`, `acc_e = accrued at ejection`, `target = ejected price`, `offset = target - raw - acc_e`. Then `formula = raw + offset = target - acc_e`. Let `acc_t = total accrued from open to now` (ledger + pending). Then `theoretical = target - acc_e - dir * acc_t * pip_size`. Since `accrued_price = theoretical - formula = -dir * acc_t * pip_size`, the new exit is `target - acc_e + accrued_price`. The I6 expected is `raw + accrued_price + offset + shift = target - acc_e + accrued_price + shift`. If no clamp, `shift = 0` and I6 holds exactly. The exit moves by `accrued_price - acc_e`, i.e. swap accrued since ejection.  
Break: If the market has moved far, `Grind_CarryClampLongExit`/`ShortExit` clamps `theoretical` to the best passive price, producing a large `applied_shift`. The shift is then stored. On the next I6 check or restart, `Grind_CarryShiftGetForRecon` calls `Grind_CarryShiftGetValidated`, which deletes the shift if it exceeds `Grind_CarryShiftWithinBound` (`grind_carry.mqh:Grind_CarryShiftGetValidated`). I6 then sees a large actual exit with no shift and fails, halting the instance.  
Smallest fix: Exempt ejected layers from shift bound validation. In `Grind_CarryShiftGetForRecon`, before calling `Grind_CarryShiftGetValidated`, add `if(Grind_EjectIsEjected(position_id)) return Grind_CarryShiftGet(position_id);`. Alternatively, when `Grind_CarryExitShiftLayer` sets a clamped shift, also set the release GV to bypass the bound check.

T-6 Re-ejection — HOLDS.  
Evidence: A second command overwrites `GRIND_EJECT_OFFSET_<pos>` with `new_offset = new_target - raw - accrued_current`, deletes carry shift, and sets `layer.exit_target = new_target` (`grind_engine.mqh:Grind_EjectPollCommand`). The old offset is simply replaced; accrued is not deleted. I6 expected becomes `raw + accrued_current + new_offset = new_target`, so I6 holds. No inconsistent state remains.

T-7 Restart after ejection — NEEDS-FIX (same root cause as T-5).  
Evidence: `Grind_RebuildBookFromTicketsInner` sets `exit_target` from the actual resting order price when an `EXT` order is found (`grind_recon.mqh:Grind_RebuildBookFromTicketsInner`). So the rebuilt `exit_target` equals the resting price. `Grind_ReconCheckInvariants` then calls `Grind_ReconExitMatchesEntry` with `shift = Grind_CarryShiftGetForRecon(position_id)` (`grind_recon.mqh:Grind_ReconCheckInvariants`). If the ejected layer has a large carry shift (from a prior clamp), `Grind_CarryShiftGetForRecon` can bound-delete it, and I6 fails on restart. If the shift is small or absent, I6 passes.  
Smallest fix: Same as T-5: do not bound-validate the carry shift for ejected layers during reconstruction.

T-8 Halted/quarantined refusal — HOLDS.  
Evidence: `fxgrind.mq5:OnTick` passes `g_grind_halted || g_grind_quarantined` as `engine_blocked`. `Grind_EjectValidate` checks `engine_blocked` first and returns `GRIND_EJECT_HALTED` (`grind_pure.mqh:284`). The poll then emits refusal and returns before `Grind_ModifyPendingPrice`. The command is already deleted (G2), so it is acted on once and refused. No order moves while blocked.

T-9 Precision — HOLDS for realistic tickets.  
Evidence: `scripts/grind_eject.mq5:OnStart` writes `(double)InpTicket`; `grind_engine.mqh:Grind_EjectPollCommand` reads `(ulong)GlobalVariableGet(cmd_name)`. MQL5 `double` has a 53-bit mantissa, so integers up to 2^53 (9,007,199,254,740,992) are exact. Realistic MT5 position tickets are well below this. INFERRED for broker ticket ranges. If a broker ever issues tickets above 2^53, precision loss could map a command to a wrong ticket.  
Smallest fix (if paranoid): Do not store the ticket in a double GV; store it as a string in a separate GV or use two GVs (high/low 32-bit halves). But for realistic tickets, no fix is required.

PREMISE VERDICT  -- is Part B safe to merge? one paragraph

Part B is close but not safe to merge as-is. The core passive-ejection mechanism is sound: the poll deletes before validating, refuses correctly when blocked, writes state only after a successful modify, and the same-tick engine does not disturb the ejected order because the deepest layer is always exit-allowed and required. The restart path rebuilds `exit_target` from the resting order price, so I6 normally passes. However, there are two real gaps: (1) the carry pass can clamp an ejected exit to a far market and store a large carry shift, which `Grind_CarryShiftGetForRecon` may then bound-delete, causing an I6 halt on the next check or restart; (2) the poll does not mirror the script’s zero-ticket refusal, so a corrupt/test layer with `position_ticket == 0` could be hand-ejected. The first is a latent halt risk when the carry pass is enabled; the second is defensive but cheap. Both fixes are one-liners. Merge only after fixing T-5/T-7 and adding the T-1 zero-ticket guard.

TEST GAPS        -- tests missing from Z1-Z16, with expected values

Assuming Z1-Z16 cover the ADR-155 bullet list, the following tests are missing:

1. Zero-ticket poll refusal. Seed a layer with `position_ticket == 0`; set `GRIND_EJECT_<magic> = 0`; call `Grind_EjectPollCommand`. Expected: `EJECT_REFUSED` with `BAD_TICKET`/`NOT_FOUND`, no `Grind_ModifyPendingPrice`, no offset written.

2. Modify failure leaves no state. Seed a valid deepest layer and a command. Force `Grind_OrderEngineSend` to return false or a non-DONE retcode. Expected: `GRIND_EJECT_MODIFY_FAILED`, `GRIND_EJECT_OFFSET_<pos>` absent, `GRIND_CARRY_SHIFT_<pos>` unchanged, `layer.exit_target` unchanged.

3. Same-tick engine after ejection. Accept an ejection, then call `Grind_OnTickEngine` with the same market. Expected: the exit order price is unchanged, `layer.exit_target` unchanged, no new carry shift, and `Grind_ExitQManageSide` performs no cancel/re-place for that layer.

4. Carry after ejection, no clamp. Seed an ejected layer with `offset = target - raw - acc_e`. Set swap such that total accrued becomes `acc_t`. Run `Grind_CarryExitPassStep`. Expected: new exit = `target - acc_e - dir * acc_t * pip_size` (i.e. moves by swap since ejection), `GRIND_CARRY_ACCRUED_<pos>` = `-dir * acc_t * pip_size`, `GRIND_CARRY_SHIFT_<pos>` = 0, and I6 passes.

5. Carry after ejection, large move + clamp. Same as above but seed market far from `target` so `Grind_CarryClampLongExit`/`ShortExit` clamps. Expected: new exit = best passive price, `GRIND_CARRY_SHIFT_<pos>` = clamped residual, and I6 passes on the next check/restart. This test currently fails if the residual exceeds `Grind_CarryShiftWithinBound` because the shift is deleted.

6. Restart after ejection with large shift. Simulate reconstruction with an ejected layer whose resting order is far from `raw + accrued + offset`. Expected: I6 passes; rebuilt `exit_target` equals the resting order price. Currently fails if `Grind_CarryShiftGetForRecon` bound-deletes the shift.

7. Re-ejection. Eject a layer, then eject the same layer again with a new target. Expected: `GRIND_EJECT_OFFSET_<pos>` overwritten to `new_target - raw - accrued_current`, carry shift deleted, `layer.exit_target = new_target`, I6 passes.

8. Halted/quarantined refusal. Set `g_grind_halted = true` (or `g_grind_quarantined = true`); set a valid command. Call `Grind_EjectPollCommand` with `engine_blocked = true`. Expected: `GRIND_EJECT_HALTED`, command deleted, no modify.

9. Precision boundary. Set `GRIND_EJECT_<magic>` to 2^53 (9007199254740992) and 2^53+1 (9007199254740993). Call the poll with a matching layer. Expected: 2^53 is read exactly; 2^53+1 may round and should be documented as unsupported. (Realistic tickets pass.)

10. Prune prefix safety. Set `GRIND_EJECT_OFFSET_999` (no position 999) and `GRIND_EJECT_2226` (pending command). Call `Grind_CarryPruneShiftGvs(2226)`. Expected: `GRIND_EJECT_OFFSET_999` deleted; `GRIND_EJECT_2226` retained.

11. I6 detail payload. Force an I6 failure on an ejected layer. Expected: logged `expected` includes `accrued + eject_offset + carry_shift`, and the JSON contains `accrued`, `eject_offset`, and `carry_shift` fields with correct values.

12. Clean-up script / test reset. Run `scripts/grind_gv_clean.mq5` and `Grind_TestClearCarryState`. Expected: both `GRIND_EJECT_<magic>` and `GRIND_EJECT_OFFSET_<pos>` are removed.

<!-- finish_reason=stop -->
