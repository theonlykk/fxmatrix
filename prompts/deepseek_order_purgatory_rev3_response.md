# DeepSeek R1 Audit Report

## Internal Reasoning

We need answer to user. We need produce required output format for each T-1..T-9, GIVENS CHECK, PREMISE VERDICT, OVERRIDE CHECK last line. We are Phase 1 Red Team. Need find exploits based actual source. Need be exhaustive. No implementation code. Need JSON? No, text.

We need analyze deeply. We have code and memo. Need attack rev3 design against source. Need concrete sequences. We have source: grind_engine.mqh, fxgrind.mq5, grind_recon.mqh, grind_quarantine.mqh, grind_carry.mqh, grind_closeby.mqh. Also memo.

Need understand code actual. Let's parse.

Important facts:

- In OnTick: `Grind_ProcessCloseByQueues` first. Then if not halted check invariants `Grind_CheckBookInvariants` from broker tickets. Quarantine step. If halted return. If quarantined: if guards allow, `Grind_RetryMissingExits` and return. Else `Grind_OnTickEngine`.

- `Grind_OnTickEngine`: guards, reconcile stray L0, compute straddle targets, place L0 if flat, recenter opposite L0, cap transition, `Grind_RetryMissingExits`, then `EnsureAddNext` for sides with depth>0 or add pending.

- `Grind_RetryMissingExits`: for all layers in long/short in-memory, if exit_order_ticket==0 and exit_position_ticket==0 then place exit for layer. It does NOT rank or queue. In rev3, OnTickEngine calls this unconditionally. That means even held exits beyond K+H will be placed every tick, unless the design modifies this. D2 says ranks beyond K+H MUST NOT have live exit. D6 I3 only for ranks 1..J. But existing `Grind_RetryMissingExits` is called every tick and will place exits for all layers missing. In rev3, held exits have exit_order_ticket==0? They are held in purgatory, so tracker must be 0 but no order. Then `Grind_RetryMissingExits` will place them all. This is fatal to design. The memo says carried forward move RetryMissingExits before L0 placement in `Grind_OnTickEngine` (T-6). Actually code already has it after L0 placement? Let's check: In `Grind_OnTickEngine`, order:
- ReconcileStrayL0
- compute buy/sell targets
- if long flat place L0
- if short flat place L0
- recenter opposite L0
- OnSideCapTransition
- `Grind_RetryMissingExits(magic, slot, lots);`
- then EnsureAddNext...
Memo s4.7 says "Move Grind_RetryMissingExits before L0 placement in Grind_OnTickEngine (T-6: no adverse interaction)." But code appended has it after L0 placement, before EnsureAddNext. Wait baseline e83b52f maybe before rev3. But rev3 design says carried forward: move before L0 placement. Source currently has it after L0. The memo says move before L0 placement. But even if moved, it still runs every tick and places all missing exits. Unless rev3 also changes `Grind_RetryMissingExits` to only place ranks <= K. But design doesn't explicitly say modify RetryMissingExits except move. D3 says release when layer reaches rank <=K without live exit place its exit in that event. But RetryMissingExits is a catch-all. If not restricted, it defeats queue. This is a major exploit. Need verify.

The prompt says "GIVENS source-verified; do NOT re-litigate". G4 `Grind_TryPlaceExitForLayer` places at layer.exit_target no clamp. G3 `Grind_HandleSideDealFill` places exit on ENT fill immediately. G7 halted instances ignore fills. G8 quarantine.

Rev3 design proposes D1-D9. Need attack each threat. We need find exploits. The most obvious: existing `Grind_RetryMissingExits` will place exits for held layers. The design D2 says MUST NOT. The memo says Reinit needs no migration, engine trims. But `Grind_RetryMissingExits` called every tick in OnTick (quarantined) and in OnTickEngine. It will repopulate all exits. That means capacity not freed. The queue cannot hold. Also `Grind_HandleSideDealFill` on ENT fill immediately places exit for the layer regardless of rank. So new deepest layer will get exit placed immediately (G3). D2 says beyond K+H MUST NOT. D3 says release when reaches rank <=K. But G3 places exit on ENT fill immediately, even if beyond K+H. The design says "On a layer becoming nearest-K without a live exit ... place its exit in the same event handler that caused it." But existing handler places exit for every new ENT fill. So a new deepest layer's exit is placed immediately, violating hold rule. That increases live exits beyond K+H. Then engine must cancel it. But cancel is extra cost; also during that tick the exit is live. More importantly, if cancel fails, it remains. This is a gap. The memo says D4 hold cancel when live exit falls beyond K+H. But initial placement on ENT fill still occurs. So design must change `Grind_HandleSideDealFill` to only place exit if layer rank <=K. Not stated. It says "Exit placement on an ENT fill for a layer ranked within K" in s6 WHAT DOES NOT CHANGE? Wait s6: "Geometry, targets, exit distance rule, ADR-123/124, the ADR-149 exposure cap, exit placement on an ENT fill for a layer ranked within K, reconstruction's broker-ticket basis." So they intend to modify exit placement on ENT fill to only for ranked within K. The appended code still has unconditional. But the design says it will change? The memo says "WHAT DOES NOT CHANGE: ... exit placement on an ENT fill for a layer ranked within K" That implies it changes to ranked within K? Actually phrase ambiguous: "exit placement on an ENT fill for a layer ranked within K" does not change, meaning for those layers it still places. It doesn't explicitly say what happens for beyond K. D3 says release when layer reaches rank <=K without a live exit. So new layer beyond K+H should not get exit. So code must be modified. But we attack design, not implementation? The source is baseline. The design proposes changes. We need find if design can be executed. If they forget to modify `Grind_HandleSideDealFill`, then exploit. But as red team, we can point out design doesn't specify modifying `Grind_RetryMissingExits` and `Grind_HandleSideDealFill` to respect ranks. The memo s4.7 only says move RetryMissingExits before L0 placement. It doesn't say restrict it. So D2 is violated by existing code. This is a concrete source counterexample. We should report.

Also D6 invariant relaxation: I3 only for ranks 1..J, J=max(1, K-1). But `Grind_CheckBookInvariants` rebuilds from broker tickets. It does not know ranks? It can compute exit targets and ranks. But the current `Grind_ReconCheckInvariants` requires exit coverage for EVERY layer. To implement D6, they must modify. But threat: can a genuinely lost exit hide? Suppose a near layer's exit is lost but its rank is miscomputed due to carry shift? Or because a deeper layer has a more favourable exit target? Need analyze ranking.

D1: rank layers by exit target nearest to market, carry shift included. Longs ascending exit target, shorts descending. But exit target is computed as entry +/- exit_pips + carry shift. The carry shift is per-position. However, `Grind_ReconLayerScratch` only stores `exit_target` from actual order/position price. The rank must be computed from broker tickets. For layers with no live exit, their exit target is not on broker book. But D1 says recomputed from broker book every tick. How do you rank a layer without live exit? You need to compute its target from entry price + exit_pips + carry shift. The entry price is from position. Carry shift from `Grind_CarryShiftGetForRecon` uses persisted state keyed by position id. That works if position exists. So ranks can be computed.

But subtle: For a layer whose exit order is live, the exit target on broker book may be clamped (D5) or carry-shifted. For a layer with no exit, the target is formula + carry. The ranking must use the same target. If a live exit was clamped (favourable), its actual price is better than target. That could change its rank relative to formula. D1 says "rank layers by exit target ... carry shift included." Does target mean actual exit order price or theoretical? If actual, a clamped exit at max(target, bid+stops) for long could be higher than theoretical, potentially ranking further away, causing it to be beyond K+H and cancelled even though it was a near target? Wait long exit: theoretical target above entry. Clamp for long: max(target, bid+stops). If target is below bid+stops (gap through), clamp makes it higher (further above market). That means it becomes further from market, so rank worsens (larger ascending). It might fall beyond K+H, get cancelled, then released? But it was already through market? Actually if target was below market, price gapped up through it. A sell limit at target would be marketable/invalid. Clamping to bid+stops places it above current bid, so it becomes a valid sell limit above market. That is further from market than theoretical (which was below market). The layer's true exit should have filled at gap. By clamping, it doesn't fill immediately; it waits for price to go higher. That may be acceptable? But D5 says "It never crosses the spread and exits at or better than target." For a long, sell limit above market; if price gapped up through target, a sell limit at target would be below market, so it would fill immediately at market (better). But they clamp to bid+stops, so it does not fill immediately; it waits for price to rise further. That is worse than target? Actually target was entry+exit_pips. If price gapped up through target, selling at target would be worse than selling at current higher price. Clamping to bid+stops sells at higher price, better. But it delays fill. This is a design choice. But invariant I6: `Grind_ReconExitMatchesEntry` tolerates a favourable price only for FILLED exit. For resting exit, it requires exact match within 2 points. A clamped resting exit has a favourable price (for long, higher than expected), so diff = actual - expected > 0. For long, `exit_is_filled=false`, so it checks `MathAbs(diff) <= 2*point`. If clamped price is more than 2 points away, I6 fails. The memo s4.4 says I6 must tolerate a favourable RESTING exit. But D6 says I6 only for layers with an exit; favourable resting price tolerated. So they plan to modify. But there is a threat: if they tolerate favourable resting exit, then a genuinely lost exit could be masked? Actually if an exit is at a favourable price, it's still coverage. But if it's too far, it might not be a valid exit? For long, a sell limit at higher price is still an exit. But if it's arbitrarily high due to repeated clamping, it may never fill. But it's still coverage. The invariant relaxation could allow an exit that is not at the correct distance. But D6 says I6 only for layers with an exit; favourable resting tolerated. That means any resting exit with price >= expected is tolerated. Could a bug place an exit at, say, entry + 1000 pips, and I6 passes because it's favourable? For long, expected = entry + exit_pips + shift. If actual is higher, diff > 0, favourable, tolerated. That could hide a mispriced exit that never fills. But is that a problem? It holds a slot. But the design might place exits only at target or clamped. If a bug places at wrong price, I6 won't catch. But that's a general invariant relaxation risk. We need concrete.

More critical: D6 says I3 only for ranks 1..J, J=max(1,K-1). Why J=K-1? They want rank K to be allowed to be missing to avoid quarantine on front fill. But consider K=3, H=1. Ranks 1..J=2 must have exit coverage. Rank 3 may be missing. Ranks 4..5 may have. Rank 6+ must not. If rank 1 exit is lost (e.g., manually cancelled, or failed to place), but rank 2 has exit, then I3 checks ranks 1..2. Rank 1 missing -> I3 fails. So near loss caught. But what if rank 1's exit is lost and rank 2's exit is also lost, but rank 3 has exit? Rank 1 missing -> I3 fails. So fine. What if carry shift reorders ranks such that a lost exit is considered rank K? Suppose K=3. A layer's exit is lost. Due to carry shift change overnight, its target moves and it becomes rank 3 (outside J=2). Then I3 doesn't require it. It is allowed to be missing. But it's actually a near layer? The rank is computed from current targets. If it's rank 3, it's the release target, allowed one event. But if it never gets released? D3 says when a layer reaches rank <=K without live exit, place its exit. So rank 3 should be released. But if it's missing and rank 3, it's within K, so D3 should place it. Wait D6 I3 only for ranks 1..J, J=max(1,K-1). So rank K is not required by I3. But D3 says release when reaches rank <=K. So if rank 3 missing, engine should place it. If engine fails to place (e.g., slot guard), then I3 won't catch it, and it can stay naked. But that's by design: they allow one event for rank K to avoid quarantine on every front fill. However, if the release fails and is not retried, rank K can be naked indefinitely. `Grind_RetryMissingExits` would place it? But if they restrict RetryMissingExits to only ranks <=K? The design doesn't specify. If they leave it unconditional, it places all. If they restrict it to ranks <=K, then rank K missing will be retried every tick. So it would eventually place. But if slot guard blocks exits? D7 says exits allowed when free>=1. So it should place. So rank K missing is transient. But what if free==0? Then exit cannot be placed. I3 for rank K passes, so no quarantine. The layer stays naked. The instance continues? OnTick: invariant ok, not quarantined, OnTickEngine runs. It will try to place exit, fail. Then EnsureAddNext may place entries? D7 says entries allowed when free>=2+margin. If free==0, entry blocked. So no new positions. But the naked layer is not covered by an exit. If price moves against, no exit. But that's the original problem? Actually the layer is naked because no exit. The design allows rank K naked to avoid quarantine. But the instance doesn't halt. It will keep trying to place exit when free>=1. If free is 0 because account is full, it can't. But how can free be 0? Account limit 200. If positions + orders = 200. The layer has a position, so it counts. If it has no exit, the number of orders is less. To be at 200, other instances have orders. So free=0. The instance cannot place its exit. It remains naked. The account is full, so no new orders. But the naked position is unhedged. If price moves, it loses. That's bad. But is it fatal to premise? The premise was to prevent naked layers. By allowing rank K to be naked, they reintroduce the exact failure mode for one layer per side. The memo says "allowing one event for it avoids quarantine on every front fill". But if the account is full, the release cannot happen. The layer stays naked. The original problem was at 200, exit refused, layer naked, instance halts. Rev3 says don't halt, just keep naked? That seems worse. But maybe the capacity formula ensures free >=1 because K+H exits are held, so there is always room for one release. Let's check capacity formula. If all instances at worst case 2N*(P+K+H+1) <= 200 - margin, then free >= margin. So exits always allowed. But if margin is small or formula violated, free could be 0. The design claims capacity formula bounds. Threat T-9 attacks capacity. If capacity formula is wrong, free could be 0, and rank K naked never gets exit. So T-9 and T-1 related.

Now, let's go threat by threat. Need produce for each T-1..T-9. We need be precise with source references. We have line numbers? The prompt gives approximate line numbers in GIVENS. We can reference functions. We need cite file : function : invariant.

Let's identify actual line numbers from code? Not given, but we can reference function names.

We need find concrete exploits. Let's analyze each threat.

T-1 INVARIANT RELAXATION. Can a genuinely lost exit hide under D6 (a near layer mis-ranked, carry shift reordering ranks, equal exit prices)?

We need consider `Grind_ReconCheckInvariants` current logic. It requires exit coverage for every layer. D6 changes to only ranks 1..J. How is rank computed? It must be computed from broker tickets. But `Grind_RebuildBookFromTicketsInner` builds `GrindReconLayerScratch` array. It does not sort by exit target. It stores layers in order of ticket scan. To implement D6, they need to sort by exit target. But the design says D1 recomputed from broker book every tick. The invariant check `Grind_CheckBookInvariants` uses `Grind_RebuildBookFromTicketsInner`. If they modify I3 to only check ranks 1..J, they must compute ranks inside the invariant check. However, the current `Grind_RebuildBookFromTicketsInner` checks I3 before it even has all layers? Let's see: In `Grind_RebuildBookFromTicketsInner`, after scanning tickets, it loops over long_scratch and checks `if(long_scratch[i].has_position && !Grind_ReconLayerHasExitCoverage(long_scratch[i]))` -> I3 fail. This happens before `Grind_ReconCheckInvariants`. To relax I3, they must modify this loop. But this loop does not know rank. It just iterates over layers in arbitrary order. If they skip I3 for some layers, they need to compute rank. But the scratch does not store market price or side direction? It has layer_index, entry_price, exit_target if exists. For layers without exit, exit_target is 0.0 (from `Grind_ReconEnsureLayer`). So to rank, they need to compute theoretical exit target for missing exits. That requires entry_price, exit_pips, carry_shift. The carry_shift is obtained via `Grind_CarryShiftGetForRecon(position_id)`. That function returns 0.0 if position not found or shift invalid. Note: `Grind_CarryShiftGetForRecon` calls `Grind_CarryNightlyMaxPips(_Symbol)` and validates bound. If the shift is outside bound, it deletes and returns 0.0. So rank can change if carry shift is pruned. That could reorder ranks. If a near layer's carry shift is deleted, its exit target moves back to formula, possibly making it rank further away, so it could be mis-ranked as beyond J and its missing exit tolerated. But the layer is actually near? Let's construct.

Suppose long side. K=3, H=1, J=2. Layers: L0 entry 1.1000, exit_pips 50 pips -> target 1.1050. L1 entry 1.1010, target 1.1060. L2 entry 1.1020, target 1.1070. L3 entry 1.1030, target 1.1080. Market bid 1.1040. Ranks: targets ascending: L0 1.1050, L1 1.1060, L2 1.1070, L3 1.1080. Rank 1=L0, rank2=L1, rank3=L2, rank4=L3. J=2. I3 requires exits for L0 and L1. L2 (rank3) may be missing. L3 (rank4) may have. Now suppose carry shift for L0 is +20 pips (long carry shift? For long, carry shift is subtracted? Need check sign. `Grind_CarryShiftedExitPrice(formula_exit, direction, accrued_pips, pip_size)` returns `formula_exit - direction * accrued_pips * pip_size`. For long direction=1, so new_exit = formula - accrued_pips. So positive accrued_pips lowers exit. That's favourable? Actually for long, lower sell limit is closer to market (since market below). So a positive carry shift makes exit target smaller, i.e., closer to market. In D1 ranking, longs ascending exit target nearest to market: lower target = nearer. So a positive carry shift makes a long exit nearer. If carry shift is deleted, target increases, making it further. So L0 without carry shift might become rank 3? Let's test. L0 entry 1.1000, formula 1.1050. With carry shift +20 pips (0.0020), target = 1.1030. L1 target 1.1060. L2 target 1.1070. L3 target 1.1080. Market 1.1040. Ranks: L0 1.1030 (rank1), L1 1.1060 (rank2), L2 1.1070 (rank3), L3 1.1080 (rank4). If carry shift for L0 is deleted (e.g., `Grind_CarryPruneShiftGvs` runs on init or carry pass? Actually `Grind_CarryPruneShiftGvs` is called in `Grind_CarryExitPassBegin` and OnInit. It deletes shift if position not in book or not selectable. If position is in book, it remains. But `Grind_CarryShiftGetForRecon` may delete if out of bound. Suppose the shift is invalidated due to `Grind_CarryShiftWithinBound` failing. Then shift becomes 0. L0 target becomes 1.1050. Ranks: L0 1.1050, L1 1.1060, L2 1.1070. L0 still rank1. Not moved beyond J. To move beyond J, need multiple layers' shifts. Carry shifts can reorder. The design says carry shift included in ranking. So if carry shift changes, ranking changes. Is that well-defined? The carry pass modifies exit prices. During the pass, some exits are modified, some not. The ranking is recomputed every tick from broker book. Between modifications, some exits have new shift, some old. That could cause a layer to temporarily be mis-ranked. But does it hide a lost exit? Suppose a near layer's exit is lost (no order). Its theoretical target with current carry shift is used for ranking. If carry shift is temporarily stale (e.g., not yet applied to its exit because it's held? But held exits have no order, so their target is computed from persisted shift. The carry pass only modifies live exits. Held exits' shifts are updated? The carry pass `Grind_CarryExitPassBegin` only iterates layers with `exit_order_ticket != 0`. Held exits have exit_order_ticket=0, so they are not in the carry pass. Their persisted carry shift is not updated for the new day? Wait `Grind_CarryShiftGetForRecon` reads persisted shift from GlobalVariable. The carry pass updates the shift by modifying the live exit order and calling `Grind_CarryShiftSet(position_ticket, applied_shift)`. For held exits, no order, so no modification, no shift update. But the carry pass is supposed to shift exit prices nightly. If an exit is held, it is not shifted. Later when released, it will be placed at `layer.exit_target` which is computed at ENTRY fill time (in `Grind_AppendLayer`) using `Grind_ExitPrice(entry_price, exit_pips, _Point, dir)` plus? Wait `Grind_AppendLayer` sets `layer.exit_target = Grind_ExitPrice(entry_price, exit_pips, _Point, dir);` It does NOT include carry shift. The carry shift is applied later by modifying the exit order. For held exits, there is no order, so no carry shift applied. D5 says "Held exits return at their original price (entry +/- exit_pips + carry shift)." But if carry shift was never applied because held, then when released, `Grind_TryPlaceExitForLayer` uses `layer.exit_target` which is just entry +/- exit_pips, no carry shift. That is a bug: held exits are placed without carry shift. The design D1 says ranking includes carry shift. D4 says held exits return at original price (entry +/- exit_pips + carry shift). But the code's `Grind_AppendLayer` does not include carry shift in `exit_target`. The carry shift is only applied by modifying an existing exit order. So if a layer's exit is held (never placed), its `exit_target` lacks carry shift. When released, it will be placed at the wrong price (missing carry shift). Then I6 will compare to expected = entry +/- exit_pips + carry_shift. `Grind_ReconExitMatchesEntry` uses `shift = Grind_CarryShiftGetForRecon(position_id)`. If carry shift exists in persisted state (from a previous night when it was live? But if it was never live, no shift persisted). So expected includes shift, actual lacks shift -> I6 fails. This is a concrete schema mismatch: `Grind_TryPlaceExitForLayer` uses `layer.exit_target` which is set without carry shift. The design assumes held exits return at original price with carry shift, but the code doesn't support that. This is a strong finding. Let's verify: `Grind_AppendLayer` is called on ENT fill. It sets `exit_target = Grind_ExitPrice(entry_price, exit_pips, _Point, dir);` No carry. Then `Grind_TryPlaceExitForLayer` places at `layer.exit_target`. The carry pass later modifies the order and updates `Grind_CarryShiftSet`. For a held exit (never placed), no carry pass. So when released, `layer.exit_target` is without carry. The design D5 says held exits return at original price (entry +/- exit_pips + carry shift). But the code doesn't compute that. It would need to compute `Grind_ExitPrice(...) + shift` at release time. The memo says "Held exits return at their original price (entry +/- exit_pips + carry shift)." That implies they must modify `Grind_TryPlaceExitForLayer` or `Grind_AppendLayer` to include carry shift. But current code doesn't. This is a schema mismatch. Could be fixable within design. But it's an exploit for T-7 carry pass. Let's hold.

Back to T-1: Can a genuinely lost exit hide under D6? Suppose a layer's exit is lost (e.g., manually cancelled, or cancel failed but tracker cleared). The invariant check rebuilds from broker tickets. It sees no exit for that layer. It computes rank. If rank <= J, I3 fails. If rank > J, I3 passes. So if the lost exit is a near layer (rank <= J), it's caught. If it's rank K, it's allowed. But D3 says release when reaches rank <=K. So rank K missing should be placed. If the engine fails to place it (e.g., slot guard free=0), it stays missing. I3 doesn't catch it. So a genuinely lost exit at rank K can hide indefinitely if free=0. But is that a "lost exit"? It's a missing exit. The design intentionally allows it. But if free=0, the account is full. The capacity formula claims free >= margin. If formula holds, free>=1, so exit can be placed. So T-1 exploit depends on capacity violation (T-9). If capacity holds, rank K missing is transient. But there is another way: carry shift reordering can make a layer that should be rank <=J appear as rank K due to a shift that is not applied to its exit? Let's construct.

Suppose K=3, J=2. Layer L1 has a live exit. Its exit target includes carry shift. Layer L2 has no exit (lost). Its theoretical target for ranking is computed from entry + exit_pips + carry_shift. If the carry shift for L2 is stale (e.g., 0 because never applied), its theoretical target might be higher (for long) than if shift were applied, making it rank further away. So it could be ranked beyond J, even though with correct shift it would be rank 2. But the invariant check uses `Grind_CarryShiftGetForRecon` which reads persisted shift. If L2 never had a live exit, there is no persisted shift. So shift=0. Then its rank might be wrong. But if it never had a live exit, it's a held exit. If it's held, it's supposed to be beyond K+H? Actually D2: ranks 1..K MUST have live exit; K+1..K+H MAY; beyond MUST NOT. So a held exit is intentionally without live exit. If it's rank K+1..K+H, it MAY be without. So I3 doesn't require it if J=K-1. If it's rank <=J, it MUST have. So if a held exit is actually rank 1..J, I3 catches it. But if its theoretical target is miscomputed due to missing carry shift, it might be ranked K+1..K+H when it should be rank <=J. Then I3 won't catch it, and it won't be released because D3 only releases when rank <=K. So it stays naked. This is a concrete exploit: a held exit with no persisted carry shift is ranked using shift=0, which for a long could place it further than reality (if true shift positive), so it hides beyond J. But wait: if it has no persisted carry shift, what is its true target? The design says "carry shift included" in ranking. If the shift is not persisted, then shift=0 is the only available. So the ranking is consistent with available data. But the layer might have accrued carry from previous nights when it was live? If it was live, it would have been modified and shift set. If it's held, it was never live, so no carry. So shift=0 is correct. So no issue. But if it was live, then held (cancelled), the persisted shift remains. When cancelled, the tracker is cleared, but the shift GlobalVariable is not deleted. So it remains. So ranking uses persisted shift. That's correct. So T-1 maybe no exploit unless capacity fails.

Equal exit prices: D1 ranking by exit target. If two layers have equal exit targets, tie-break? The design doesn't specify. If tie, rank order arbitrary. Could cause churn? Suppose two layers have same exit target. One has live exit, one doesn't. If the one without is ranked within J, I3 fails. If the one with is ranked within J, passes. But if they swap rank each tick due to unstable sort, the invariant might pass one tick and fail next? Actually if they have same target, both are either within J or not? If K=1, J=max(1,K-1)=1. Rank 1 must have exit. If two layers have same target, one is rank1, one rank2. If the one without exit is rank1, I3 fails. If the one with is rank1, I3 passes. If sort is unstable and swaps, it could oscillate between pass and fail, causing quarantine? But the invariant is checked every tick. If it fails, quarantine. If next tick passes, quarantine releases. That's churn. But is it an exploit? Could cause repeated quarantine. But not fatal. However, equal exit prices can occur if two layers have same entry price? But layers have different entry prices by construction (adds are spaced). Carry shift could make targets equal. If a layer's carry shift exactly offsets the entry difference. Then tie. The design doesn't specify tie-break. This is a design gap. Could hide a lost exit if the one without exit is ranked beyond J due to tie-break luck. But it's unstable. We can report as DESIGN-UNSAFE or NO-EXPLOIT? Probably fixable within design (specify tie-break by layer index). But the prompt wants exploits. We can say EXPLOIT-FOUND if we can construct a sequence where a genuinely lost near exit hides. Let's think of a concrete one.

Suppose long side, K=3, H=1, J=2. Layers: L0 entry 1.1000, L1 entry 1.1005, L2 entry 1.1010. Exit_pips 50 pips -> 0.0050. Market bid 1.1040. Targets: L0 1.1050, L1 1.1055, L2 1.1060. Ranks: L0 rank1, L1 rank2, L2 rank3. J=2. I3 requires L0 and L1 exits. Now suppose L1's exit is lost (no order). Its theoretical target for ranking is 1.1055. L0 has exit at 1.1050. L2 has no exit (held). L2 target 1.1060. Ranks: L0 rank1, L1 rank2, L2 rank3. I3 checks rank1..2. L1 missing -> I3 fails. So caught. Now apply carry shift. Suppose L1 has a positive carry shift of +10 pips (0.0010), making its target 1.1045. L0 has no shift, target 1.1050. L2 no shift, target 1.1060. Ranks: L1 1.1045 (rank1), L0 1.1050 (rank2), L2 1.1060 (rank3). J=2. I3 requires rank1..2. L1 missing at rank1 -> fails. So still caught. What if L0 has a negative carry shift? For long, negative shift increases target. Suppose L0 shift -20 pips -> target 1.1070. L1 shift +10 -> target 1.1045. L2 shift 0 -> 1.1060. Ranks: L1 1.1045 (rank1), L2 1.1060 (rank2), L0 1.1070 (rank3). J=2. I3 requires L1 and L2. L1 missing -> fails. So any missing layer that becomes rank <=J fails. The only way a missing layer hides is if it's rank >J. But D3 says release when rank <=K. So if it's rank K (3), it's not required by I3 (J=2), but it should be released. If release fails, it hides. So the hiding requires release failure. Release failure can happen if slot guard blocks exit (free=0). Or if `Grind_TryPlaceExitForLayer` returns false (e.g., 10040). The design D7 says exits allowed when free>=1. If free=0, exit send blocked. So if the account is full, rank K missing exit cannot be placed. I3 doesn't check rank K. So it hides. This is exactly the original problem: at 200, exit refused. Rev3 allows rank K to be naked without halting. But the design's capacity formula claims free>=margin always. If formula is wrong, this happens. So T-1 is tied to T-9. We can report T-1 as EXPLOIT-FOUND if capacity formula can be violated, leading to rank K naked indefinitely. But the threat T-1 specifically asks "Can a genuinely lost exit hide under D6". Yes, if it's rank K and free=0, it hides because I3 doesn't check rank K. But is it "genuinely lost"? It's missing. The design intends rank K to be allowed missing for one event. If free=0, it's not one event, it's permanent. So exploit.

Also, note `Grind_CheckBookInvariants` uses `Grind_RebuildBookFromTicketsInner` which checks I3 before `Grind_ReconCheckInvariants`. In the inner function, it checks for every layer with position and no exit coverage, regardless of rank. So to implement D6, they must modify that early check. If they only modify the later `Grind_ReconCheckInvariants`, the early check will still fail for any missing exit. So they must change both. This is a source location they might miss. But that's implementation, not design. We can mention as a risk.

T-2 RANKING. Ties, carry shifts that change order overnight, a layer whose exit filled but is not yet netted: is D1 well-defined every tick?

We need analyze. D1: rank by exit target nearest to market, carry shift included. Recomputed from broker book every tick. No held-exit state only in memory.

For a layer whose exit filled but not yet netted: The exit order is gone, and an exit position exists (`exit_position_ticket != 0`). In `Grind_ReconLayerHasExitCoverage`, it counts as coverage. But for ranking, what is its exit target? The exit position's price is the fill price. That may be different from the original target. If the exit filled at a favourable price (e.g., gap), its exit target from broker is the fill price. But the layer's theoretical exit target is entry + exit_pips + carry. For ranking, should we use the filled position's price or the original target? The design D1 says "rank layers by exit target ... carry shift included." The exit target is a property of the layer, not the filled exit. But after fill, the layer's `exit_target` in memory is still the original? In `Grind_HandleSideDealFill`, when EXT fills, it sets `exit_order_ticket=0` and `exit_position_ticket=position_id`. It does not change `layer.exit_target`. In recon, `Grind_RebuildBookFromTicketsInner` for an EXT position sets `exit_target = tickets[i].price` (the fill price). So after reinit, the layer's exit_target becomes the fill price, not the original target. This is a schema mismatch: the exit target for a filled exit position is the fill price, which may be clamped or gapped. For ranking, using the fill price could mis-rank the layer. If the fill price is better (further from market?), it might rank differently. But the layer's exit is already filled; it's waiting for CloseBy. Should it still be ranked? The design says "a layer whose exit filled but is not yet netted" is a threat. In D2, ranks 1..K MUST have a live exit. A filled exit position is not a live exit order; it's a position. Does it count as a live exit? The design says "live exit" probably means exit order OR filled exit position? In G1, exit coverage is order OR filled exit position. D2 says "MUST have a live exit". It's ambiguous. If a filled exit position counts as a live exit, then it can satisfy D2. But it's not resting; it's a position waiting to be netted. It holds a slot. For capacity, it counts as a position, not an order. The design's capacity formula counts positions + live exits (K+H). A filled exit position is a position, so it's already in P? Actually P is positions of the original entries? The formula: positions P + live exits (K+H) + one L0. But if an exit fills, it opens an opposite position. That increases position count by 1 (the exit position), while the original position remains until CloseBy. So between fill and netting, there are P+1 positions. The formula doesn't account for this transient. This is T-3 and T-9.

For T-2, if a filled exit position is considered for ranking, its "exit target" is the fill price. That could be different from the theoretical target. Suppose a long layer exit fills at 1.1050. The fill price is 1.1050. Its theoretical target was 1.1050. Same. But if it filled at a clamped price, say 1.1052, then its exit_target becomes 1.1052. For ranking, longs ascending: higher target means further from market. So it might be ranked further away. But it's already filled, so it doesn't need a live exit. If it's ranked within K, D2 would say it MUST have a live exit, but it has a filled exit position. Does that satisfy? If not, the engine would try to place another exit for the same layer, causing I2 duplicate? But `Grind_TryPlaceExitForLayer` checks `if(layer.exit_order_ticket != 0 || layer.exit_position_ticket != 0) return false;`. So it won't place. So it's safe. But the invariant I1 checks exactly one exit: if it has an exit position, it counts as one. So no duplicate. However, if the layer's exit position is not yet netted, and a new deeper layer is added, the ranking might push the filled layer beyond K+H, and D4 says cancel a live exit. But it's not a live exit order; it's a filled position. The engine cannot cancel a position. So D4 might try to cancel a non-existent order? The design says "when a live exit falls beyond K+H, cancel it". A filled exit position is not a live exit order. So it shouldn't be cancelled. But if the ranking includes filled exit positions as "live exits", then D4 might attempt to cancel a position? The code `Grind_CancelPendingOrder` only works on orders. So it would fail. But the design might treat filled exit positions as not live exits for the purpose of D2/D4. The memo D6 says I3 exit coverage only for ranks 1..J. I1 only for layers that have an exit. I6 only for layers with an exit. So a filled exit position counts as "has an exit". But for D2 "MUST have a live exit", is a filled exit position a live exit? It's not an order. The term "live exit" in the memo is used for resting orders. D3 says "place its exit". D4 says "cancel it". So live exit likely means resting order. A filled exit position is not a live exit. So a layer with a filled exit position but no order would be considered as not having a live exit. If its rank <=K, D3 would try to place an exit. But `Grind_TryPlaceExitForLayer` refuses because `exit_position_ticket != 0`. So the layer would never get a live exit until netted. During that time, it's not covered by a resting exit. But it has a filled exit position, which is coverage. So I3 passes if rank <=J. But D2 says MUST have live exit. It doesn't. So the design's state machine is inconsistent: a layer with a filled exit position is in limbo. It cannot place a new exit until the position is netted. But the netting may be delayed (CloseBy queue). During that delay, if the market moves, the original position is still open, and the exit position is open. They are offsetting? Actually on a hedging account, the exit position is opposite direction. The original position is still open. The net exposure is zero (one long, one short). So it's effectively closed. But they remain as two positions until CloseBy. So it's not naked. The risk is capacity: two positions instead of one. But for ranking, if we treat it as having coverage, it's fine. The threat is whether D1 is well-defined. If the ranking uses the filled exit position's price, it might mis-rank. But since it doesn't need a new exit, it doesn't matter for release. However, it could affect the rank of other layers. For example, if a filled exit position has a very favourable price (e.g., 1.1100), for a long, that's further from market, so it might be ranked last. But it's already filled. If it's ranked last, it might push other layers' ranks. But the number of layers is the same. The ranking of other layers is unaffected by the filled layer's target if we only consider layers that need exits? Actually the rank is over all layers. If a filled layer is ranked high (near), it occupies a rank slot. But it doesn't need a live exit. D2 says ranks 1..K MUST have a live exit. If a filled layer is rank 1, it doesn't have a live exit. Does that violate D2? If so, the engine would try to place an exit, fail, and maybe halt? The design says "live exit". A filled exit position is not a live exit. So D2 would be violated. The design would need to exclude layers with filled exit positions from the rank requirements, or count them as saturated. The memo doesn't specify. This is a design gap. Could cause I3 to fail? I3 requires exit coverage for ranks 1..J. If a filled layer is rank 1, it has exit coverage (filled position), so I3 passes. D2 is not an invariant, it's a rule for the engine. So the engine might try to place a live exit for it, but `Grind_TryPlaceExitForLayer` refuses. So no action. So it's okay. But if the engine's logic is "if rank <=K and no live exit, place exit", it will call `Grind_TryPlaceExitForLayer`, which returns false. It might then think it failed and retry every tick. That's a request storm? `Grind_TryPlaceExitForLayer` just returns false without sending. So no storm. But it's a logic inefficiency. Not fatal.

Ties: D1 doesn't specify tie-break. Could cause churn if two layers swap ranks. But since rank only determines which exits are live, and both have same target, swapping doesn't change which have live exits if both have or both don't. If one has and one doesn't, swapping would change which one is required to have an exit. If the one without is rank 1, I3 fails. If it's rank 2, I3 passes. If tie and sort is unstable, the invariant could oscillate. But if the one without is rank 1, it means it's within J. The engine should release it. If it doesn't, I3 fails. So it's caught. If the one with is rank 1, the one without is rank 2. If J=1, I3 only requires rank 1. So the missing exit at rank 2 is allowed. But D3 says release when rank <=K. If K=2, rank 2 is within K, so it should be released. If release fails, it hides. Again capacity. So ties exacerbate but don't create new fundamental issue.

Carry shifts changing overnight: The carry pass runs in OnTimer at 23:50 server. It modifies live exits. During the pass, some exits are modified, some not. The ranking is recomputed every tick from broker book. If a layer's exit is modified to a new target, its rank may change. This could cause a layer that was rank K+H to become rank K+H+1, triggering cancel, while another becomes rank K, triggering release. The pass is chunked (2 per timer step). So over several seconds, the book is inconsistent. During this, invariants are checked every tick. I6 checks exit price against expected using carry shift. For modified exits, the shift is updated. For unmodified exits, the old shift? Wait `Grind_CarryShiftGetForRecon` reads persisted shift. If an exit hasn't been modified yet, its persisted shift is still old. The invariant I6 computes expected = entry +/- exit_pips + shift. The actual exit price is still old (based on old shift). So if shift is old, expected matches actual. So I6 passes. After modification, shift is updated, actual matches new expected. So I6 passes. But during the pass, the ranking uses actual exit prices. Some are shifted, some not. This could temporarily reorder ranks. Does that cause a missing exit to hide? Suppose a layer's exit is held (no order). Its theoretical target uses persisted shift. If the shift is updated for live exits but not for held exits (because held exits don't have orders), then the held exit's target is based on old shift. Meanwhile, live exits have new shift. This could mis-rank the held exit. For example, if carry shift increases for longs (making exits closer), live exits move closer, held exit's theoretical target (using old shift) is further. It might become rank K+H+1, so it's supposed to be beyond hold. But it's already held. If it should be within K, it won't be released. This is a concrete bug: the carry pass does not update the carry shift for held exits. So their ranking is based on stale shift. As a result, a held exit that should be released may not be, because its theoretical target is stale and ranks it too far. Or a held exit that should be held may be ranked within K and released at the wrong price (without updated shift). Let's construct.

Suppose long side, K=1, H=0. Layers: L0 entry 1.1000, L1 entry 1.1010. Exit_pips 50 pips -> 0.0050. Market bid 1.1030. L0 exit is live, L1 exit is held (no order). L0 target 1.1050. L1 theoretical target 1.1060. Ranks: L0 1.1050 (rank1), L1 1.1060 (rank2). K=1, so L0 must have live exit (it does). L1 is beyond K+H (1+0=1), so MUST NOT. It's held, correct. Now carry pass runs. Nightly carry for long is positive, say +20 pips (0.0020). The carry pass modifies L0's exit: new target = 1.1050 - 0.0020 = 1.1030. It updates shift for L0 to -0.0020? Wait `applied_shift = new_exit - formula_exit = 1.1030 - 1.1050 = -0.0020`. So shift negative. `Grind_CarryShiftSet(L0, -0.0020)`. Now L0 target is 1.1030. L1 is held, no order, so its shift remains 0. Its theoretical target is still 1.1060. Now market bid is 1.1030. Ranks: L0 1.1030 (rank1), L1 1.1060 (rank2). K=1. L0 has live exit. L1 held. Correct. But what if the carry shift was negative? Suppose long carry is negative? Actually swap long can be negative. `Grind_CarryShiftedExitPrice` for long: new_exit = formula - accrued_pips. If accrued_pips positive, new_exit lower. If accrued_pips negative, new_exit higher. So shift can be positive or negative. Suppose accrued_pips = -20 pips -> new_exit = formula + 0.0020 = 1.1070. L0 target becomes 1.1070. L1 theoretical target 1.1060. Now ranks: L1 1.1060 (rank1), L0 1.1070 (rank2). K=1. Rank1 is L1, which has no live exit. So D2 says rank1 MUST have live exit. The engine should release L1. But L1's theoretical target is 1.1060 (shift 0). However, if L1 had a carry shift, it should also be shifted by -20 pips? Wait the carry pass applies to all positions, including held ones. But held exits are not modified because they have no order. The design says "Held exits return at their original price (entry +/- exit_pips + carry shift)." If the carry pass doesn't update the held exit's shift, then when released, it will be placed at the wrong price. In this scenario, L1 becomes rank1, so the engine will try to release it. It will call `Grind_TryPlaceExitForLayer` using `layer.exit_target`. But `layer.exit_target` in memory was set at `Grind_AppendLayer` to 1.1060 (no shift). It has not been updated by the carry pass. So it will place an exit at 1.1060. But the correct expected price after carry shift is 1.1060 + (-0.0020)? Wait for long, carry shift is subtracted? Let's recalc: `Grind_CarryShiftedExitPrice(formula_exit, direction, accrued_pips, pip_size) = formula_exit - direction * accrued_pips * pip_size`. direction=1. So new_exit = formula_exit - accrued_pips. If accrued_pips = -20 pips (i.e., -0.0020), then new_exit = formula_exit - (-0.0020) = formula_exit + 0.0020 = 1.1060 + 0.0020 = 1.1080. Wait I had L0 formula 1.1050, accrued_pips -20 pips -> new_exit = 1.1050 + 0.0020 = 1.1070. Yes. So for L1, formula = 1.1060. Correct shifted exit = 1.1060 + 0.0020 = 1.1080. But `layer.exit_target` is 1.1060. So it will place at 1.1060, which is 20 pips worse than expected. I6 will check: expected = formula + shift = 1.1060 + 0.0020 = 1.1080. Actual = 1.1060. diff = -0.0020. For long, exit_is_filled=false. `Grind_ReconExitMatchesEntry` checks `MathAbs(diff) <= 2*point`. 0.0020 is much larger. So I6 fails. The instance quarantines then halts. This is a concrete exploit. The design D5 says held exits return at original price with carry shift. But the code does not update `layer.exit_target` for held exits during the carry pass. The carry pass only modifies live exit orders and sets the shift GlobalVariable. When a held exit is released later, `Grind_TryPlaceExitForLayer` uses the stale `layer.exit_target` from memory (or from recon, which sets exit_target from the exit order price? Wait if the exit is held, there is no exit order. So on reinit, `Grind_RebuildBookFromTicketsInner` creates a layer with `has_position=true` and `has_exit_order=false`. It sets `exit_target` to 0.0? Let's check: In `Grind_ReconEnsureLayer`, `exit_target = 0.0`. Then when scanning ENT position, it sets `has_position=true`, `entry_price`, `position_id`. It does not set `exit_target`. So for a layer with no exit, `exit_target` remains 0.0. Then in `Grind_ReconCheckInvariants`, it checks I3: `if(!Grind_ReconLayerHasExitCoverage) fail`. So a layer with no exit fails I3. But under D6, I3 is relaxed for ranks >J. So it would pass. Then in `Grind_RebuildBookFromTicketsInner`, after invariants, it copies scratch to `long_out.layers`: `long_out.layers[n].exit_target = long_scratch[j].exit_target;` which is 0.0 for a held layer. So after reinit, a held layer's `exit_target` is 0.0. Then when released, `Grind_TryPlaceExitForLayer` will place at 0.0! That's catastrophic. But wait, the design says "No held-exit state lives only in memory." and "Carry shift ... a held exit's target is recoverable after reinit." This implies they must modify recon to compute the theoretical exit target for layers without an exit. The current code does not. So on reinit, held layers have `exit_target = 0.0`. If the engine then tries to release one (e.g., it becomes rank <=K), it will place an exit at 0.0, which is invalid. This is a schema mismatch. The design must add code to compute `exit_target` for held layers from entry + exit_pips + carry shift during recon. The memo acknowledges this: "Carry shift: Grind_CarryShiftGetForRecon ... already reads a validated per-position shift from persisted state, so a held exit's target is recoverable after reinit." But the code does not use it to populate `exit_target` for missing exits. So it's a gap. This is a strong exploit for T-8 (reconstruction) and T-7 (carry pass). We should report.

Let's continue T-2. The threat asks: "Ties, carry shifts that change order overnight, a layer whose exit filled but is not yet netted: is D1 well-defined every tick?" Our finding: D1 is not well-defined for a layer with no exit on reinit because `exit_target` is 0.0. For a layer whose exit filled but not netted, recon sets `exit_target` to the fill price, which may differ from theoretical, causing mis-ranking. For carry shifts changing overnight, held exits' targets are stale. So verdict EXPLOIT-FOUND or DESIGN-UNSAFE. We can mark DESIGN-UNSAFE with concrete mechanisms.

T-3 RELEASE TIMING. Front exit fills; CloseBy nets on a later tick. Between fill, netting and release, does any invariant fail beyond quarantine?

Sequence:
- Layer L2 (rank 1) has live exit. Price hits, exit order fills. Deal comes in. `Grind_HandleSideDealFill` for EXT IN: finds layer by exit order, sets `exit_order_ticket=0`, `exit_position_ticket=position_id`. Queues CloseBy. The original position (ENT) remains. The exit position (EXT) is open. So the layer now has `has_position=true` (original) and `has_exit_position=true` (filled exit). In recon, this layer has exit coverage (filled position). I3 passes. I1 counts exit position as one. I6 checks exit price. `exit_target` in memory is still the original target? In `Grind_HandleSideDealFill`, it does not update `exit_target`. So `layer.exit_target` remains the original. The exit position's price is the fill price. I6 in recon uses `Grind_RebuildBookFromTicketsInner`: for EXT position, it sets `exit_target = tickets[i].price` (the fill price). So on next invariant check (which rebuilds from broker tickets), the layer's exit_target becomes the fill price. If the fill price is at the original target, I6 passes. If it filled at a clamped or gapped price, I6 may fail? `Grind_ReconExitMatchesEntry` with `exit_is_filled=true` tolerates favourable price. For long, if fill price >= expected, it passes. So if it filled at a better price, passes. If it filled at a worse price (adverse), I6 fails -> quarantine. But a limit order should fill at its price or better. So it should be favourable. So I6 passes.

Now, the layer has a filled exit position. It does not have a live exit order. D2 says ranks 1..K MUST have a live exit. This layer is rank 1 (or whatever). It does not have a live exit. So D2 is violated. The engine should release the next held exit? D3 says "when a layer reaches rank <=K without a live exit ... place its exit in that event." Here the layer itself is rank 1 without a live exit. But it has a filled exit position. Should the engine place a new exit for it? `Grind_TryPlaceExitForLayer` refuses because `exit_position_ticket != 0`. So it cannot. So the layer remains without a live exit until CloseBy nets it. During this time, is there any invariant failure? I3 passes because it has exit coverage. I1 passes. So no invariant failure. The design says "Release: when a layer reaches rank <=K without a live exit (front exit filled and netted, or a new deeper layer), place its exit in that event." The front exit filled but not netted: the layer is still rank <=K, without a live exit. The release should happen when netted? The memo says "front exit filled and netted". So release waits for netting. But what about the other layers? When the front exit fills, the layer is still in the book until netted. Its rank is still 1. The next layer is rank 2. If K=3, ranks 1..3 must have live exits. Rank 2 and 3 have live exits. Rank 1 has a filled exit position, no live exit. So D2 is violated for rank 1. But no invariant fails. The engine might try to release rank 4? No, because rank 1 is still occupying a slot. The number of layers hasn't decreased. So no new layer becomes rank <=K. So no release. The system just waits for CloseBy. CloseBy is processed at the start of next OnTick. It may take a few ticks. During this time, the account has an extra position (the exit position). This increases position count. Capacity formula doesn't account for this transient. If the account is at capacity, this extra position could cause free to drop to 0, blocking exit sends? But exits are already placed. It could block new entries. D7 says entries allowed when free>=2+margin. If free drops, entries deferred. That's okay. But what if CloseBy fails or is delayed? The exit position remains. The layer has no live exit. If the market moves, the original position is still open, but it's offset by the exit position. Net exposure zero. So it's not naked. So no invariant fail. The threat asks "does any invariant fail beyond quarantine?" I3 passes. I6 passes. So NO-EXPLOIT? But there is a subtle: I1 checks exactly one exit. The layer has one exit position. That's fine. But what if `Grind_HandleSideDealFill` is called for the EXT IN deal, and it queues CloseBy. Before CloseBy is processed, another tick comes. `Grind_CheckBookInvariants` rebuilds from broker. It sees the original position and the exit position. It correctly pairs them by comment? The exit position has comment from the exit order? The exit order was placed with comment `GrindCommentBuild(slot, side, layer_index, "EXT")`. When it fills, the resulting position gets that comment? In MT5, the position comment is typically the order comment. So the recon can parse it. It will see ENT position and EXT position for the same layer. It sets has_position and has_exit_position. I3 passes. I1 counts exit position. So no fail.

However, there is a potential I2 duplicate: If the engine, during the time between fill and netting, tries to release the next held exit for the same layer? No, it's the same layer. `Grind_TryPlaceExitForLayer` refuses. So no duplicate.

What about `Grind_RetryMissingExits`? It checks `if(exit_order_ticket == 0 && exit_position_ticket == 0)`. For this layer, exit_position_ticket != 0, so it skips. Good.

So T-3 seems NO-EXPLOIT for invariants. But the capacity formula is violated (extra position). That's T-9. We can mention T-3 as NO-EXPLOIT but note capacity transient.

T-4 HOLD CANCEL. Cancel racing a fill of that exit; cancel non-DONE; release of a still-live exit producing two EXT orders for one layer (I2 halt?).

Scenario: D4 says when a live exit falls beyond K+H, cancel it. It calls `Grind_CancelPendingOrder`. This returns true only on TRADE_RETCODE_DONE. If it returns false (e.g., non-DONE, like order already filled or not found), the tracker is not cleared. But what if the exit filled between the decision to cancel and the cancel send? The cancel will fail because order is gone. `Grind_CancelPendingOrder` first checks `Grind_SelectOurOrder(ticket, magic)`. If the order is already filled, `OrderSelect` will fail (it's no longer a pending order). So `Grind_CancelPendingOrder` returns false. The tracker is not cleared. The layer still has `exit_order_ticket = ticket`. But the exit has filled, and a deal will arrive. `Grind_HandleSideDealFill` will process the EXT IN deal. It finds the layer by exit order ticket. It sets `exit_order_ticket = 0`, `exit_position_ticket = position_id`. So the tracker is cleared then. So no duplicate. But what if the cancel send is in flight and the order fills? The cancel may return non-DONE (e.g., TRADE_RETCODE_INVALID_ORDER). The tracker remains. The deal arrives. The handler finds layer by exit order ticket. It sets exit_order_ticket=0. So okay. But what if the cancel succeeds (DONE) just before the fill? Then the order is removed. The tracker is cleared? D4 says "clear the layer's exit tracker only on confirmed cancel or confirmed gone." If cancel returns DONE, the engine clears `exit_order_ticket = 0`. Then later, if the order had already filled but cancel returned DONE? That can't happen; if it filled, cancel cannot succeed. So no race.

What about release of a still-live exit producing two EXT orders? D3 says release when a layer reaches rank <=K without a live exit. If a layer actually has a live exit but the engine thinks it doesn't (tracker cleared erroneously), it will place a second exit. The design says clear tracker only on confirmed cancel or confirmed gone. But what if the tracker is cleared by `Grind_CancelPendingOrder` returning true, but the order is not actually gone? `Grind_CancelPendingOrder` returns true only on TRADE_RETCODE_DONE. In MT5, DONE means the order was successfully deleted. So it's gone. So safe.

But there is a bug: In `Grind_CancelPendingOrder`, it returns true only on `res.retcode == TRADE_RETCODE_DONE`. If the order is already gone, `Grind_SelectOurOrder` returns false, so it returns false. The design says "clear the tracker only on confirmed cancel or confirmed gone". The code does not have a "confirmed gone" path for exits. `Grind_CancelPendingOrder` just returns false if not selectable. The design says "the pattern Grind_EnsureAddNext already uses". Let's look at `Grind_EnsureAddNext`: if `!Grind_SelectOurOrder(add_pending_ticket, magic)`, it checks if it's a position; if so return; else clears `add_pending_ticket = 0`. So it clears if gone. For exits, D4 says use the same pattern. But the existing `Grind_TryPlaceExitForLayer` and `Grind_RetryMissingExits` don't have that pattern. If they implement D4, they need to add a "confirmed gone" check. If they don't, a cancel that returns false because order is already gone will leave the tracker set, and the layer will never be released because `exit_order_ticket != 0`. This could cause a missing exit to hide? Actually the exit is gone, but tracker says live. So the layer appears to have an exit. I3 passes. But it has no actual exit. The engine won't try to place a new one because tracker is non-zero. This is a critical schema mismatch: if the exit order is manually deleted or fills and the deal is missed (e.g., due to halted instance? G7 says halted instances ignore fills). If halted, fills are ignored. `Grind_OnTradeTransactionEngine` returns early if `g_grind_halted`. So if an exit fills while halted, the deal is ignored. The exit order is gone, but the in-memory tracker still has `exit_order_ticket`. On reinit, recon rebuilds from broker tickets, so it will see no exit order and either fail I3 or (under D6) allow it if rank >J. So in-memory tracker is not persisted. But during the halt, the engine doesn't run. On reinit, it's fixed. So not a live exploit.

But consider a non-halted instance: exit order is cancelled by D4. `Grind_CancelPendingOrder` returns false because order already gone (e.g., filled). The deal for the fill will arrive. `Grind_HandleSideDealFill` will process it, find the layer by exit order ticket, and set `exit_order_ticket = 0`. So tracker cleared. So no issue. What if the order was cancelled externally (not by EA) and no deal arrives? Then tracker remains. The engine thinks exit is live. I3 passes. But actual exit is missing. This is a lost exit hiding. The design's D4 says clear tracker on confirmed gone. If they implement that, it would catch it. But the current code doesn't. The design says they will implement it. So it's a design requirement. We can mention as a risk if not implemented. But the threat asks if D4 can produce two EXT orders. If tracker is not cleared on confirmed gone, then when the layer reaches rank <=K, the engine sees `exit_order_ticket != 0` and does not place a new exit. So no duplicate. It just stays naked. So no I2 halt. But it's a hidden lost exit. That's T-1.

What about cancel racing a fill: Suppose the engine decides to cancel because rank > K+H. It calls `Grind_CancelPendingOrder`. The order fills at the same moment. The cancel returns false (e.g., TRADE_RETCODE_INVALID_ORDER). The tracker is not cleared. The deal arrives. `Grind_HandleSideDealFill` finds layer by exit order ticket. It sets `exit_order_ticket = 0`, `exit_position_ticket = position_id`. So tracker updated. No duplicate. But what if the deal arrives before the cancel? Then the exit order is already gone. `Grind_CancelPendingOrder` will fail `Grind_SelectOurOrder` and return false. The tracker is already cleared by the deal handler. So `Grind_CancelPendingOrder` returns false, but the tracker is already 0. The caller might then clear it again? Harmless. So no issue.

What about release of a still-live exit: D3 says place exit when layer reaches rank <=K without live exit. If the layer has a live exit but the engine mistakenly thinks it doesn't (e.g., tracker cleared by a bug), it places a second exit. The broker will accept it if the first is still live. Then the layer has two exit orders. Recon will detect I2 duplicate (exit order duplicate) and quarantine/halt. This is possible if the tracker is cleared incorrectly. The design says clear only on confirmed cancel or gone. So if implemented correctly, no. But there is a specific race: `Grind_CancelPendingOrder` returns true on DONE. The engine clears tracker. But what if the cancel is for an exit that was already filled, but the broker returns DONE because it was already gone? In some brokers, deleting a non-existent order returns DONE? The code checks `Grind_SelectOurOrder` first. If the order is gone, `OrderSelect` fails, so it returns false. So DONE only if it was live and successfully deleted. So safe.

Thus T-4 seems NO-EXPLOIT if design implemented as stated. But we can point out a gap: the design says "clear the layer's exit tracker only on a confirmed cancel or confirmed gone -- the pattern Grind_EnsureAddNext already uses." But `Grind_EnsureAddNext` clears on `!Grind_SelectOurOrder` only if not a position. For exits, "confirmed gone" means the order is no longer in OrdersTotal. The design must add that check. If they just call `Grind_CancelPendingOrder` and clear on true, a false due to already gone will leave the tracker set. That's a bug. So we can mark as DESIGN-UNSAFE or EXPLOIT-FOUND with concrete sequence: Exit order for layer L4 is beyond K+H. Engine calls cancel. But the order was already filled by a previous tick's deal, and the deal was missed because the instance was quarantined? Wait if quarantined, OnTick does `if(g_grind_quarantined) { if(GuardsAllowTrading) Grind_RetryMissingExits(...); return; }`. During quarantine, OnTickEngine does not run, so D4 cancel does not run. So no. If not quarantined, deal handler processes fills immediately in OnTradeTransaction. So tracker is cleared. So the only way tracker is stale is if the deal is missed. Deals can be missed if `Grind_DealWasProcessed` already marked? No, if it was processed, tracker would be updated. If the deal is not selected because `HistoryDealSelect` fails? Then tracker not updated. But `Grind_HandleSideDealFill` returns early if `!Grind_DealSelect`. So the deal is not processed. The tracker remains. This can happen if the deal is not yet in history when the transaction arrives? In MT5, `OnTradeTransaction` with DEAL_ADD should have the deal available in history. But sometimes it might not be immediately selectable? The code uses `HistoryDealSelect(deal_ticket)`. If it fails, it returns and does not mark processed. The same transaction might not be retried. So the exit fill could be missed. Then the exit order is gone, but tracker says live. This is a known issue? The design G7 says halted instances ignore fills. But non-halted? The code has `if(Grind_DealWasProcessed(deal_ticket)) return; if(!Grind_DealSelect(deal_ticket)) return;` So if history select fails, it silently returns. That deal will never be processed. So the exit fill is lost. The layer's `exit_order_ticket` remains set. The actual exit position exists. On next tick, `Grind_CheckBookInvariants` rebuilds from broker. It will see the original position and the exit position (because the exit order filled, creating a position). So the invariant check will see `has_exit_position=true`. It will pass I3. But the in-memory tracker still thinks there's an exit order. This is a mismatch between memory and broker book. The design D1 says "No held-exit state lives only in memory." But the tracker is in memory. The engine uses the in-memory tracker for `Grind_TryPlaceExitForLayer` and `Grind_RetryMissingExits`. If the tracker says exit_order_ticket != 0, it won't place a new exit. But the actual exit is filled and waiting for CloseBy. The CloseBy queue is only populated in `Grind_HandleSideDealFill`. Since the deal was missed, the CloseBy is not queued. So the original and exit positions will never be netted by the EA. They will remain as two positions. The layer is effectively closed (hedged), but the EA doesn't know. On reinit, recon will derive CloseBy from book (since it sees both positions with same layer). So it will fix. But until reinit, the layer is stuck. This is a pre-existing bug, not introduced by rev3. But rev3 relies on the tracker for hold/release. So if a deal is missed, the queue logic can be corrupted. For example, the layer's exit filled, but tracker says live. The layer's rank may fall beyond K+H. The engine will try to cancel the exit order. `Grind_CancelPendingOrder` will fail because order is gone. The tracker is not cleared (design says clear on confirmed gone, but if they only clear on true, it stays). So the tracker remains. The layer is considered to have a live exit. It will never be released. The next layer may be rank K+1 and held. So the queue is stalled. This is a concrete exploit sequence: missed deal -> stale tracker -> hold cancel fails -> no release. But is this new to rev3? Rev1/rev2 also had trackers. But rev3's D4 specifically relies on clearing tracker on confirmed gone. If implemented correctly, it would clear. But the design says "the pattern Grind_EnsureAddNext already uses". In `Grind_EnsureAddNext`, if `!Grind_SelectOurOrder`, it checks if it's a position; if not, clears tracker. For an exit order that filled, `Grind_SelectOurOrder` will fail (it's not an order). It will then check `Grind_SelectOurPosition`. The exit position exists. So `Grind_SelectOurPosition` returns true. In `Grind_EnsureAddNext`, if it's a position, it returns without clearing. That's for add pending ticket: if the pending order filled, it becomes a position, so the tracker should not be cleared? Actually for an add, if the pending order fills, it becomes a position. The add_pending_ticket is cleared in `Grind_HandleSideDealFill` when the ENT deal arrives. So the pattern `Grind_EnsureAddNext` uses: if order not selectable but position selectable, it returns (does not clear). It relies on the deal handler to clear. So for exits, if the exit order filled, the exit position is selectable. The design says "clear the layer's exit tracker only on a confirmed cancel or confirmed gone". "Confirmed gone" might mean the order is gone and no position? But for an exit, if it filled, there is a position. So is it "gone"? The exit order is gone, but the exit position exists. The layer still has coverage. The tracker should be updated to point to the exit position, not cleared. But `Grind_CancelPendingOrder` doesn't handle that. The deal handler is supposed to update `exit_order_ticket=0` and `exit_position_ticket=position_id`. If the deal is missed, the tracker remains as order. So the design's "confirmed gone" is not sufficient because the order can be gone but replaced by a position. The engine needs to detect that. So T-4 has a real exploit: missed deal -> stale exit order tracker -> hold cancel fails -> queue stalls. But is a missed deal plausible? `HistoryDealSelect` can fail if the deal is not yet in history. The code does not retry. This is a known MT5 issue. The design should account for it. But the prompt says G7: halted instances ignore fills. For non-halted, the deal should be processed. However, there is a window: `OnTradeTransaction` is called for each deal. If the instance is quarantined, `Grind_OnTradeTransactionEngine` is still called? Let's check `OnTradeTransaction` in fxgrind.mq5: it calls `Grind_OnTradeTransactionEngine` unconditionally. Inside, it checks `if(g_grind_halted) return;`. It does NOT check quarantine. So even if quarantined, deal fills are processed. Good. So only halted instances ignore fills. If not halted, deals are processed. So missed deal due to history select failure is the only risk. But that's a pre-existing risk. We can mention as a design gap.

T-5 GAP CLAMP. Does D5 ever cross, get rejected, or leave I6 failing? Several exits released at one clamped price?

D5: if target is already through market, clamp passive: long at max(target, bid+stops), short at min(target, ask-stops). Using ADR-013 clamp pattern. It never crosses the spread and exits at or better than target. Consequence: I6 must tolerate favourable resting exit.

Let's analyze clamp. For long, exit is a SELL LIMIT. Target is entry + exit_pips. If market bid > target, then target is below market. A sell limit below market would be marketable and would fill immediately at market (or be rejected). The clamp sets price = max(target, bid + stops_level). Since bid > target, bid+stops > target, so clamped price = bid+stops. This is above the bid. For a sell limit, you want to sell above market. So it's passive. It does not cross. Good. But wait: `Grind_Adr013ClampBuy` is for BUY limits. The memo says use existing ADR-013 clamp pattern. There is `Grind_Adr013ClampBuy` and `Grind_Adr013ClampSell`. For a long exit (SELL LIMIT), they should use the sell clamp? Actually `Grind_Adr013ClampSell` is for SELL limits? Let's check code: In `Grind_TryRecenterOppositeL0`, if `opposite_is_long` (buy limit), it uses `Grind_Adr013ClampBuy`. So `ClampBuy` is for buy limits. For a long exit, it's a sell limit, so they would use `Grind_Adr013ClampSell`? Wait, a long entry is a BUY LIMIT. Its exit is a SELL LIMIT. So for long exit, use sell clamp. But the memo D5 says "long at max(target, bid+stops)". That is a manual clamp, not necessarily `Grind_Adr013ClampSell`. `Grind_Adr013ClampSell` probably clamps a sell limit to be above bid + stops? Let's infer from `Grind_EnsureAddNext`: for short side (is_long=false), it uses `Grind_Adr013ClampSell(add_target, bid, ask, _Point, stops, clamped)`. A short add is a SELL LIMIT. So `ClampSell` is for sell limits. It likely ensures price >= bid + stops. So D5's long exit clamp is exactly `ClampSell`. Good.

Now, I6. For a resting exit, `Grind_ReconExitMatchesEntry` with `exit_is_filled=false` requires `MathAbs(diff) <= 2*point`. If clamped, actual price is bid+stops. Expected is entry+exit_pips+shift. The diff is actual - expected. For long, if target was below market (bid > expected), then actual = bid+stops > expected. diff > 0. If the gap is large, diff > 2 points. So I6 fails. The memo says I6 must tolerate a favourable resting exit. If they modify I6 to tolerate favourable resting exit for long (diff >= 0), then it passes. But what about short? For short, exit is a BUY LIMIT. Target = entry - exit_pips. If market ask < target, then target is above market. A buy limit above market would be marketable. Clamp: short at min(target, ask - stops). Since ask < target, ask - stops < target, so clamped = ask - stops. This is below ask. For a buy limit, you want to buy below market. So passive. Expected = entry - exit_pips + shift. Actual = ask - stops. Since ask < expected, actual < expected. diff < 0. For short, favourable is lower price. So diff <= 0 is favourable. So I6 must tolerate favourable resting exit for both directions: long diff >= 0, short diff <= 0. The current `Grind_ReconExitMatchesEntry` only tolerates favourable when `exit_is_filled=true`. So they must change the condition to also allow when resting? The memo says "favourable resting price tolerated". That means if diff is favourable, return true regardless of filled. So they will change. That's fine.

But there is a subtle: The clamp uses `stops` (stops_level). But `stops_level` can be 0. Then min_dist = point? In `Grind_CarryMinPassiveDistance`, it uses `MathMax(point, MathMax(stops_level * point, freeze_level * point))`. In D5, they say `bid + stops`. If stops is 0, then bid + 0 = bid. A sell limit at bid. Is that passive? The bid is the sell price? Actually for a sell limit, you can place at bid? The current ask is higher. A sell limit at bid is below ask, so it would be marketable? In MT5, a sell limit must be placed above the current bid by stops_level. If stops_level=0, it can be placed at bid? Actually a sell limit is an order to sell at a price higher than the current market. The current market for selling is the bid. If you place a sell limit at bid, it's at the market, not above. It might be rejected or fill immediately. The ADR-013 clamp likely ensures it's at least stops_level away. If stops_level=0, it might allow at bid. But then it could be marketable? The code has `Grind_SellLimitMarketable(clamped, bid)`. In `Grind_EnsureAddNext`, after clamping, it checks `if(!is_long && !Grind_SellLimitMarketable(clamped, bid)) return;`. So if clamped is marketable, it returns without placing. So D5 must also check marketability. If it clamps to bid+0 = bid, and `SellLimitMarketable` says it's marketable, it won't place. Then the exit is not placed. That could leave a naked layer. But the design says "clamp to the passive side". So they must ensure it's not marketable. If stops=0, the clamp might fail. They should use `MathMax(point, stops)`. The ADR-013 clamps likely handle this. We can check `Grind_Adr013ClampSell` definition? Not in given code. It's in grind_pure.mqh maybe. Not provided. But we can infer from `Grind_CarryMinPassiveDistance` which uses `MathMax(point, ...)`. So likely ADR-013 uses at least point. So probably okay.

Several exits released at one clamped price: Suppose a gap through many levels. Multiple held exits are released at the same clamped price. For long, all get clamped to bid+stops. So multiple sell limits at same price. Is that allowed? Yes, multiple orders at same price. But they are for different layers. They will all fill at that price if market reaches it. But they are all above market. If market gaps up, they might all be filled? Actually if market is at bid, and we place sell limits at bid+stops, they are above market. They won't fill until market rises to that price. But if the gap was up, the market is already high. The target was below market, meaning the exit should have filled during the gap. By clamping, we place above market, so they wait for further rise. That's a loss of profit (or missed exit). But the memo says acceptable. However, if multiple exits are placed at the same price, they are all resting. On the next tick, if market is still at the same level, they remain. I6 will pass because favourable. But what if the market then drops? They won't fill. The original positions remain open. But they are hedged? No, the exits are not filled, so the original positions are still open and naked? Wait, if the exit was supposed to fill during the gap, the original position should have been closed. By clamping, we prevent the fill. So the original position remains open. It is naked (no exit filled). But it has a resting exit above market. So it is covered by an order. I3 passes. But economically, the position missed its exit. This is a design choice. Not an invariant failure.

What about I6 with multiple clamped exits? All have the same actual price. Expected prices differ by layer entry. So diffs differ. For a long, actual = bid+stops. Expected = entry_i + exit_pips + shift_i. If some entries are higher, expected is higher. Some may be close to actual. If actual is only slightly above expected for one layer, diff might be within 2 points. For others, diff large. I6 with favourable tolerance passes for all if actual >= expected. But what if for some layer, actual < expected? That would mean the clamped price is not favourable. Can that happen? For long, we clamp to max(target, bid+stops). Since target is below market (bid > target), and bid+stops > bid > target. So actual = bid+stops > target. But expected = entry + exit_pips + shift. The target is exactly entry + exit_pips? Wait `layer.exit_target` is entry + exit_pips (without shift). The expected in I6 is entry + exit_pips + shift. The clamp uses `target` which is `layer.exit_target`. So actual = max(exit_target, bid+stops). Since bid > exit_target, actual = bid+stops. Now expected = exit_target + shift. So actual - expected = (bid+stops) - (exit_target + shift). Since bid > exit_target, and stops >=0, actual > exit_target. So actual - expected > -shift. If shift is positive (for long, shift is subtracted? Wait for long, expected = exit_target + carry_shift. Carry shift can be positive or negative. If carry_shift is positive, expected > exit_target. Then actual - expected = bid+stops - exit_target - shift. Since bid+stops > exit_target, the first part is positive. If shift is large, actual - expected could be negative. For example, exit_target = 1.1050, bid = 1.1060, stops = 0.0005, actual = 1.1065. shift = +0.0050 (50 pips). expected = 1.1100. actual - expected = -0.0035. So actual < expected. For long, favourable is actual >= expected. Here actual < expected, so it's adverse. Then I6 would fail even with favourable tolerance. This is a concrete exploit: a gap release with a large positive carry shift can produce a clamped price that is below the expected shift-adjusted target, causing I6 adverse failure and halt. Let's verify signs.

For long, `Grind_CarryShiftedExitPrice(formula_exit, direction, accrued_pips, pip_size)` returns `formula_exit - direction * accrued_pips * pip_size`. So new_exit = formula_exit - accrued_pips. If accrued_pips is positive, new_exit is lower than formula. That means the correct exit target after carry is lower (closer to market). In I6, expected = `Grind_ExitPrice(entry, exit_pips, point, dir) + carry_shift`. Here `carry_shift` is the persisted shift, which is `applied_shift = new_exit - formula_exit = -accrued_pips`. So if accrued_pips positive, carry_shift negative. So expected = formula_exit + carry_shift = formula_exit - accrued_pips. That matches new_exit. So for long, a positive accrued_pips gives negative carry_shift, lowering expected. So expected is lower than formula. If carry_shift is negative, expected < exit_target. Then actual = bid+stops > exit_target > expected. So actual > expected, favourable. So I6 passes. What if accrued_pips is negative? Then carry_shift positive, expected > exit_target. Then actual could be less than expected if the gap is small and stops small. Let's construct: Long. entry = 1.1000. exit_pips = 50 pips -> 0.0050. formula = 1.1050. accrued_pips = -20 pips -> carry_shift = +0.0020. expected = 1.1070. Market gaps up. Suppose bid = 1.1060. target = 1.1050. Since bid > target, gap through. Clamp: actual = max(1.1050, 1.1060 + stops). Suppose stops = 0.0005. actual = 1.1065. Now actual (1.1065) < expected (1.1070). diff = -0.0005. For long, favourable is diff >= 0. Here diff < 0, adverse. I6 fails. So the instance quarantines and halts. This is a concrete exploit for T-5. The design D5 says "I6 must tolerate a favourable RESTING exit." But in this case, the clamped resting exit is not favourable relative to the carry-shifted expected price. It is adverse. So I6 would fail even with the proposed relaxation. The design's D5 only considers target, not carry-shifted expected. The clamp uses `layer.exit_target`, which may not include carry shift (as we saw, `layer.exit_target` is set without carry shift). The expected in I6 includes carry shift. So there is a mismatch. The clamp should use the carry-shifted target, not the raw exit_target. If it clamps to `max(expected, bid+stops)`, then actual >= expected, favourable. But D5 says clamp to `max(target, bid+stops)`. If target is raw, and carry shift makes expected higher, the clamp may be below expected. This is a critical bug. The memo D5 says "long at max(target, bid+stops)". It doesn't mention carry shift. But D1 says ranking includes carry shift. The exit target for a layer should include carry shift? Actually the layer's `exit_target` in memory is the raw formula. The carry shift is applied by modifying the order. For a held exit, there is no modified order. When released, it should be placed at the carry-shifted target. If D5 clamps using the raw target, it's wrong. So T-5 EXPLOIT-FOUND.

T-6 HYSTERESIS. Construct a price path that churns cancel/place despite H.

Hysteresis: H=1. K=3, H=1. Ranks 1..3 MUST have live exit. Ranks 4..4 MAY. Ranks >4 MUST NOT. So an exit is cancelled only when it falls to rank 5 (K+H+1 = 3+1+1=5?). Wait D2: ranks 1..K MUST; K+1..K+H MAY; beyond K+H MUST NOT. So with K=3, H=1: ranks 1-3 MUST, rank 4 MAY, ranks >=5 MUST NOT. D4: hold when a live exit falls beyond K+H, i.e., rank >=5, cancel it. D3: release when a layer reaches rank <=K without a live exit. So an exit can be live at rank 4 (MAY). If it moves to rank 5, cancel. If it moves back to rank 4, it MAY be live, but it's not released unless it reaches rank <=3? Actually D3 says release when reaches rank <=K. So if it was cancelled at rank 5, and then moves to rank 4, it is not released (since rank 4 > K). It will be released only when it reaches rank 3. So the hysteresis band is ranks 4..? Actually the MAY zone is K+1..K+H. For H=1, that's rank 4. So an exit can be live at rank 4. If it moves to rank 5, cancel. If it moves back to rank 4, it is not automatically re-placed because release is only at rank <=K. So it stays held. Then if it moves to rank 3, it is released. So the cancel/place cycle requires the rank to oscillate between <=3 and >=5. That's a width of at least 2 ranks. Could a price path cause that? Yes, if an add fills and then exits fill, ranks shift. Consider a single side. New add fills, creating a new deepest layer. This new layer has the furthest exit target? For a long ladder, deeper layers have higher entry price? Actually adds are priced from the deepest layer. Let's understand geometry. Long ladder: L0 entry at some price. Adds are placed below? The memo says "adds are priced from the deepest layer". For a long, add target = deepest entry - add_pips? Or +? In `Grind_ComputeAddTarget`: `anchor = deepest entry_price; return Grind_AddTargetPrice(anchor, add_pips, _Point, is_long ? 1 : -1);` For long, direction 1, so add target = anchor + add_pips. So deeper layers have higher entry price. Their exits are entry + exit_pips. So deeper layers have higher exit targets. Market moves up. The nearest exit to market is the deepest layer's exit (highest target? Wait for longs, ascending exit target nearest to market. Market is below exits. The lowest exit target is nearest. Deeper layers have higher entry, so higher exit target, so they are further from market. Actually if market is below all exits, the lowest exit is nearest. L0 has lowest entry, so lowest exit, so nearest. Deeper layers have higher exits, further. So rank 1 = L0, rank 2 = L1, etc. When price rises, the nearest exit fills first. So L0 exit fills. Then L0 is removed? No, exit fills, opens opposite position, then CloseBy nets, removing L0. Then L1 becomes rank 1. So ranks shift down. A new add is placed at the deepest layer. The new layer has the highest exit target, so it becomes the last rank. This pushes all existing layers' ranks up by 1? Actually adding a new deepest layer increases the number of layers. The new layer has the highest exit target, so it is placed at the end of the sorted list. The ranks of existing layers remain the same relative order, but their rank numbers? If we sort ascending, the new layer is at the end. So existing layers' ranks are unchanged (since they are all lower). Wait, rank is 1..N. If we add a layer with highest exit target, it becomes rank N. Existing layers keep their ranks. So adding a new deepest layer does NOT push existing exits beyond K+H. It just adds a new layer at the end. The memo says "On a live exit falling beyond rank K+H (a new deeper layer pushed it back): cancel it". But how can a new deeper layer push an existing exit back? If the new layer has a higher exit target, it goes to the end. Existing layers' ranks are unchanged. So no exit is pushed back. Unless the new layer's exit target is not the highest? For longs, deeper layers have higher entry, so higher exit target. So new deepest layer has highest exit target. So it goes to the end. So existing exits are not pushed back. They might be pushed back if the new layer's exit target is lower? That would happen if adds are placed above? Let's check geometry. `Grind_ComputeAddTarget` for long: anchor = deepest entry. `Grind_AddTargetPrice(anchor, add_pips, _Point, 1)` -> probably anchor + add_pips. So add target is higher than deepest entry. So new layer entry is higher. So its exit target is higher. So it goes to the end. So existing exits are not pushed back. So D4 "a new deeper layer pushed it back" seems incorrect for the normal geometry. Wait, maybe the add is placed below the deepest layer? For a long, you buy lower? Actually a long ladder might have adds below the lowest entry? Let's think: L0 is placed at straddle buy price below market. Adds are placed from the deepest layer. The deepest layer is the lowest? In `Grind_FindDeepestLayerArrayIndex`, it finds the layer with maximum `layer_index`. Layer indices increase as adds fill. So L0 is index 0. L1 is index 1. The deepest is the highest index. For a long, `Grind_ComputeAddTarget` uses `anchor = deepest.entry_price`. `Grind_AddTargetPrice(anchor, add_pips, _Point, 1)`. If add_pips positive, it adds. So add target = deepest entry + add_pips. So the new add is placed higher than the deepest entry. So entry prices increase with index. So L0 lowest, L1 higher, L2 higher. So exits: L0 exit = L0 entry + exit_pips. Since exit_pips > 0, L0 exit < L1 exit < L2 exit. So L0 is nearest to market (lowest). L2 is furthest (highest). So rank 1 = L0, rank 2 = L1, rank 3 = L2. When price rises, L0 exit fills first. Then L0 is removed. L1 becomes rank 1. Then L1 exit fills. Then L2 becomes rank 1. Meanwhile, new adds are placed at the deepest layer. Suppose L0 fills and is removed. The deepest layer is now L2. `Grind_EnsureAddNext` will place a new add at L2.entry + add_pips = L3 entry. So L3 exit = L3 entry + exit_pips > L2 exit. So L3 is the new furthest. So the order of exits is L1 (lowest), L2, L3 (highest). So the new layer is at the end. Existing L1, L2 keep their relative order. Their ranks are now 1 (L1), 2 (L2), 3 (L3). So L2 was rank 2, now rank 2? Actually before L0 filled, ranks were L0=1, L1=2, L2=3. After L0 removed and L3 added, ranks: L1=1, L2=2, L3=3. So L2 stays rank 2. No push back. So D4's scenario of a new deeper layer pushing an existing exit beyond K+H doesn't occur in this geometry. Wait, what if K=1? Then only rank 1 must have live exit. L0 exit is live. L1, L2 are held. When L0 fills, L1 becomes rank 1, so it is released. L2 remains rank 2, held. When L1 fills, L2 becomes rank 1, released. So no cancel needed. The only time an exit is cancelled is if it was live and its rank increases. But rank increases only if a new layer is inserted before it. That would require a new layer with a lower exit target than an existing live exit. But new layers have higher exit targets. So they are inserted after. So ranks of existing layers never increase. They only decrease when front layers are removed. So D4 (hold cancel) would never trigger in normal operation! Let's check: D2 says ranks 1..K MUST have live exit. Initially, all K nearest have live exits. The rest are held. As front exits fill, layers are removed, ranks decrease. Held exits become nearer, so they are released. No exit's rank increases. New layers are added at the end, so they don't affect ranks of existing layers. Therefore, a live exit never falls beyond K+H. So D4 is never needed. The only way a live exit falls beyond K+H is if the market moves such that the ranking changes? No, ranking is by exit target, which is fixed per layer (plus carry shift). Market price doesn't affect the order of exit targets; it only affects which are "nearest to market"? Wait D1 says "rank layers by exit target nearest to market". The ranking is by exit target, not by distance to market. The distance to market is market-dependent. But the order of exit targets is fixed if we sort by target. For longs, ascending target. The nearest to market is the lowest target, assuming market is below all targets. But what if market moves above some targets? Then some targets are below market. The nearest to market might not be the lowest target. Actually if market is above a target, that target is below market. The distance to market is absolute difference. The nearest could be the one closest to market, which might be a target just below market or just above market. D1 says "rank layers by exit target nearest to market (longs ascending, shorts descending)". This implies they assume all long exits are above market, so ascending target = nearest first. But if market moves above some exits, those exits become below market. Then the nearest to market could be the highest below market? Let's read D1 carefully: "rank layers by exit target nearest to market (longs ascending, shorts descending), carry shift included." This is a sorting rule: for longs, ascending exit target. It does not use current market price. It just sorts by exit target. The phrase "nearest to market" is an interpretation assuming market is below all longs. But if market gaps up, some long exits are below market. Then the lowest exit target is furthest below market, not nearest. The actual nearest to market would be the exit target just above market? Or the one just below? The design says "longs ascending exit target (every resting long exit is above bid, so lowest = nearest)". This parenthetical in memo s4.1 assumes every resting long exit is above bid. But if market gaps up, some long exits are below bid. Then they are not above bid. They would be marketable. D5 handles gap release by clamping. So after clamping, they are placed above market. But what about existing live exits that were placed before the gap? If market gaps up through them, they would have filled immediately (limit orders fill when price crosses). So they wouldn't remain resting below market. So the only exits that are below market are those that haven't been placed yet (held). When released, D5 clamps them above market. So after clamping, all live exits are above market for longs. So the assumption holds for live exits. For held exits, their theoretical targets might be below market. But they are not live. For ranking, we rank all layers, including held ones. If a held exit's theoretical target is below market, it might be ranked as nearest (lowest target). But it's below market. The actual nearest to market would be the lowest target above market? Let's think: Suppose market gaps up. Long exits: L0 target 1.1050, L1 target 1.1060, L2 target 1.1070. Market bid = 1.1065. L0 target is below market. L1 target is below market? 1.1060 < 1.1065, yes. L2 target 1.1070 > market. The nearest to market is L2 (1.1070) or L1 (1.1060)? Distance: L1 distance = 0.0005, L2 distance = 0.0005. Tie. L0 distance = 0.0015. So the nearest is L1 or L2. But D1 says ascending exit target: L0 (1.1050) rank1, L1 (1.1060) rank2, L2 (1.1070) rank3. So L0 is rank1 even though it's furthest below market. If L0 is held (no live exit), D3 would release it because it's rank <=K. But its target is below market. D5 would clamp it to bid+stops, so it would be placed above market. That's okay. But what if L0 already has a live exit? It would have filled during the gap. So it wouldn't be live. So the only layers with targets below market are those without live exits. They get released and clamped. So the ranking by ascending target still works because the ones below market are exactly the ones that need release. But wait: D2 says ranks 1..K MUST have live exit. If L0 is rank1 and has no live exit, D3 releases it. After release, it gets a clamped exit above market. Now its actual exit price is above market. But its theoretical target is still below market. The ranking uses "exit target". Does it use the theoretical target or the actual exit price? D1 says "rank layers by exit target ... carry shift included". The exit target is a property of the layer, not the actual order price. If it uses the theoretical target, then after clamping, the layer's theoretical target is still below market, so it remains rank1. But now it has a live exit at a clamped price. Its rank is still 1. That's fine. But then the next layer L1 might also have a theoretical target below market. It is rank2. If K=1, only rank1 must have live exit. L0 has it. L1 is rank2, held. That's correct. So the ranking by theoretical target works.

Now, could a live exit's rank increase? Suppose a new layer is added with a theoretical target lower than an existing live exit. But new layers have higher entry, so higher target. So no. What if carry shift makes a new layer's target lower? Carry shift is per position. A new position has no carry shift initially (0). So its target is entry + exit_pips. Existing positions may have carry shifts that lower their targets. So a new layer could have a higher entry but a lower target if existing layers have large negative carry shifts? Let's see: For long, carry shift is subtracted? Actually positive accrued_pips lowers target. So existing layers could have their targets lowered by carry. A new layer has no carry yet, so its target is higher. So new layer target is still higher than existing? Not necessarily. Suppose existing L0 entry 1.1000, exit_pips 50 -> formula 1.1050. Carry shift +100 pips -> target 1.0950. New L1 entry 1.1010 -> formula 1.1060. Carry shift 0 -> target 1.1060. So L0 target 1.0950, L1 target 1.1060. L0 is lower, so rank1. L1 is higher, rank2. So new layer is still after. What if existing layers have negative carry shift (target higher)? Then they are further. New layer might be lower? Suppose L0 entry 1.1000, formula 1.1050. Carry shift -50 pips -> target 1.1100. New L1 entry 1.1010, formula 1.1060. L1 target 1.1060 < L0 target 1.1100. So L1 becomes rank1, L0 becomes rank2. This is a new layer inserted before an existing layer! This can happen if carry shifts are large and negative. Then the new layer's exit target is lower than the existing layer's target. So the new layer is inserted at the front. This would push existing layers' ranks up by 1. An existing live exit at rank K could be pushed to rank K+1. If H=0, K+1 > K+H, so it must be cancelled. This is exactly the scenario D4 addresses: a new deeper layer pushed it back. But note: the new layer is "deeper" in layer index (L1), but its exit target is lower. So it's inserted before. This can happen due to carry shifts. So D4 is needed when carry shifts reorder the ladder. This is a source of churn. If carry shifts change overnight, they can reorder ranks. The design's hysteresis H is supposed to damp churn. But if the new layer is inserted before, it may cause a live exit to be pushed beyond K+H, triggering cancel. Then if the carry shift changes back, it might be re-released. This is a potential churn path. We can construct a price path? Actually carry shifts change only at 23:50. But adds can happen anytime. If an add fills, it creates a new layer with no carry shift. If existing layers have large negative carry shifts (targets higher), the new layer may be inserted before them. This could push a live exit beyond K+H. The engine would cancel it. Then later, if the existing layer's carry shift is updated (e.g., next carry pass), its target might drop, moving it back. Then the cancelled exit is re-released. This is a cancel/place cycle not driven by price but by carry pass. But T-6 asks for a price path that churns despite H. H dampens oscillation around one level. But if the reordering is by carry shift, H may not help. However, we can also construct a price path: Suppose market oscillates such that an add fills and then price retreats? Adds are limit orders. If market oscillates around the add price, the add may fill and then the new layer's exit might be close to market. But the add fill creates a new layer. If the new layer's exit is near market, it might be released. Then price moves, the new layer's exit fills. Then another add fills. This is normal. H=1 adds one extra slot to damp. But can we force cancel/place? Consider K=1, H=1. Ranks 1 MUST, rank 2 MAY, rank 3+ MUST NOT. Suppose we have layers L0, L1, L2. L0 exit live (rank1). L1 exit live (rank2, MAY). L2 held. Now a new add L3 fills. Its exit target is higher, so it goes to rank4. L1 remains rank2. No cancel. Now L0 exit fills. L0 removed. Ranks: L1 becomes rank1 (must have live, it does), L2 becomes rank2 (MAY, it's held), L3 becomes rank3 (MUST NOT). L2 is now rank2. It is allowed to be live, but it's held. Is it released? D3 says release when reaches rank <=K. K=1, so only rank1 is released. L2 is rank2, so not released. L1 is rank1, already live. So no release. Then L1 exit fills. L1 removed. Ranks: L2 becomes rank1 (must have live, it's held). So L2 is released. It gets a live exit. L3 becomes rank2 (MAY, it's held). So L2 release happens. No cancel. Now suppose price oscillates. L2 exit fills, L2 removed. L3 becomes rank1, released. So no cancel. To get a cancel, we need a live exit to move beyond K+H. With K=1, H=1, K+H=2. So an exit at rank 3 must be cancelled. How can an exit be at rank 3? If we have L0, L1, L2, L3. Suppose L0 is live (rank1), L1 is live (rank2), L2 is live? Wait rank2 is MAY, so it can be live. L3 is held. Now a new add L4 fills. Its exit target is higher, so it goes to rank5. No change. What if carry shift makes L4's target lower than L1's? Then L4 could be inserted at rank2, pushing L1 to rank3, L2 to rank4, L3 to rank5. Then L1 (was rank2, live) is now rank3. K+H=2, so rank3 > 2, so L1 must be cancelled. This is a cancel triggered by a new add with a lower target due to carry shift. So yes, if carry shifts reorder, a new add can push a live exit beyond K+H. Then later, if the carry shift changes, the order might revert, and the exit is re-released. This is a churn path, but it requires carry shifts to change (nightly) or new adds with different carry. Not a pure price path. The threat says "Construct a price path that churns cancel/place despite H." Maybe we can construct a price path where an add fills and then price moves such that the new layer's exit becomes closer than an existing live exit? But exit targets are fixed. Price movement doesn't change which exit target is lower. So the only way a new layer is inserted before an existing live exit is if its exit target is lower. For a long, a new layer has higher entry, so higher target, unless carry shifts make existing targets higher. Carry shifts are based on swap, which changes daily. So not a pure price path. However, there is another mechanism: the exit target for a layer is recomputed from broker book. If a live exit is clamped (D5), its actual price on the broker book is higher than its theoretical target. If ranking uses the actual exit price, then a clamped exit could be ranked higher (further) than its theoretical target. This could cause it to be pushed beyond K+H and cancelled. Then when it is re-released, it might be clamped again or not, causing churn. For example, a gap causes L0's exit to be clamped to a high price. Its actual exit price is now high, so it ranks further away. If there are other layers, L0 might be pushed beyond K+H. The engine cancels it. Then the gap closes, and L0's theoretical target is now above market again. It might be re-released at the theoretical target (lower), so it becomes rank1 again. This is a cancel/place cycle driven by a price gap. Let's construct: Long side. K=1, H=0. Layers: L0 entry 1.1000, exit target 1.1050. L1 entry 1.1010, exit target 1.1060. Market bid 1.1040. L0 exit is live at 1.1050 (rank1). L1 exit is held. Now a gap up: market bid jumps to 1.1060. The L0 exit at 1.1050 is now below market. It should fill, but suppose it's a limit sell at 1.1050. In MT5, if market gaps through, the limit order will fill at the opening price, which is above 1.1050. So it fills. It doesn't remain resting. So D5 is only for held exits that are released after a gap. A live exit that is already placed will fill during the gap. So it won't be clamped. So the clamped exit scenario only applies to held exits being released. Once released, it's placed at clamped price. Then if market moves back, that clamped exit is now above market. It remains resting. Its actual price is high. Its theoretical target is lower. If ranking uses actual price, it might be ranked further away. But it's already live. If a new layer is added, could it push this clamped exit beyond K+H? Suppose K=1, H=0. We have L0 (live exit at clamped 1.1065), L1 (held). Market moves down to 1.1040. L0's actual exit is 1.1065. L1's theoretical target is 1.1060. Ranking by actual exit price for L0 and theoretical for L1? D1 says "rank layers by exit target". If L0's target is its actual exit price (1.1065) and L1's target is 1.1060, then L1 is rank1, L0 is rank2. K=1, so L0 is rank2 > K+H=1, so L0 must be cancelled! Then L1 is released. L1's target is 1.1060, which is above market, so no clamp. It becomes rank1. Now L0 is held. Later, market moves up again? L1 might fill. This is a churn. But wait: D1 says "rank layers by exit target ... carry shift included". What is a layer's exit target? For a layer with a live exit, is it the live order price or the theoretical exit target? The memo D1 says "The ordering depends only on broker positions and the exit price rule (entry +/- exit_pips + carry shift), so it is recomputed from the broker book every tick and after reinit." This implies the exit target is computed from the entry price and carry shift, not from the live order price. So even if a live exit is clamped, its ranking uses the theoretical target, not the clamped price. Let's re-read: "The ordering depends only on broker positions and the exit price rule (entry +/- exit_pips + carry shift), so it is recomputed from the broker book every tick and after reinit." So the exit target is the theoretical target. The live order price may differ due to clamping. But the ranking uses the theoretical target. So a clamped live exit is ranked by its theoretical target, which is lower (for long) than the clamped price. So it remains rank1. So no churn from clamping. Good. So D1 is well-defined if they compute theoretical targets for all layers. But the code's `layer.exit_target` is set to the raw formula without carry shift. And on recon, for a layer with a live exit order, `exit_target` is set to the order price. So the code does NOT compute theoretical targets. It uses the actual order price for layers with exits, and 0.0 for layers without. This is a major schema mismatch. The design D1 requires theoretical targets (entry + exit_pips + carry shift) for ALL layers, regardless of whether they have a live exit. The current code does not store that. `Grind_AppendLayer` sets `exit_target` without carry shift. `Grind_RebuildBookFromTicketsInner` sets `exit_target` from the order/position price for those with exits, and 0.0 for those without. So D1 cannot be implemented without significant changes to recon and layer struct. This is a critical finding. We should report in T-2 and T-8.

T-7 CARRY PASS. The nightly carry pass shifts exit prices. What happens to held exits, and to ranks, across a carry pass?

As analyzed, the carry pass only iterates layers with `exit_order_ticket != 0`. Held exits have `exit_order_ticket == 0`, so they are skipped. Their persisted carry shift is not updated. Their `layer.exit_target` is not updated. When they are later released, `Grind_TryPlaceExitForLayer` places at `layer.exit_target`, which lacks the new carry shift. This causes I6 failure or wrong price. Also, ranking uses `Grind_CarryShiftGetForRecon`, which reads persisted shift. For held exits, if they had a shift from a previous time they were live, that shift remains. But the carry pass does not update it for the new day. So the shift is stale. The correct shift should include the new night's swap. So held exits are systematically mispriced across carry passes. This is a concrete exploit. Sequence: Layer L1 is held (no exit). Nightly carry pass runs. It skips L1. Next day, L0 exit fills and nets. L1 becomes rank1, released. It is placed at old target without the new carry shift. I6 fails (adverse or not matching) -> quarantine -> halt. Or if I6 is relaxed to tolerate favourable, it might pass if the stale shift happens to be favourable, but it's still economically wrong. So T-7 EXPLOIT-FOUND.

T-8 RECONSTRUCTION. Reinit on a legacy full-exit book, on a held book, and on a book where held exits' positions have closed manually.

- Legacy full-exit book: Every layer has an exit order. On reinit, `Grind_ReconCheckInvariants` requires exit coverage for every layer. It passes. But D6 wants I3 only for ranks 1..J. The current code checks all. If they modify to D6, they must compute ranks. But for a full-exit book, all have exits, so I3 passes anyway. The engine then trims to K+H. That's fine. But note: the current `Grind_RebuildBookFromTicketsInner` checks I3 for every layer. If they change to D6, they need to ensure that a legacy full-exit book still passes. It will, because all have exits. So no issue.

- Held book: Some layers have no exit. On reinit, the current code will fail I3 because it requires exit coverage for every layer. Under D6, it should only require for ranks 1..J. But as noted, the current code sets `exit_target = 0.0` for held layers. So the invariant check and ranking cannot work. If they relax I3, the held layers pass, but their `exit_target` is 0.0. When released, they will be placed at 0.0. This is a fatal schema mismatch. The design says "Carry shift: ... a held exit's target is recoverable after reinit." But the code does not populate it. They must modify `Grind_RebuildBookFromTicketsInner` to compute `exit_target` for layers without an exit using `entry_price`, `exit_pips`, and `Grind_CarryShiftGetForRecon(position_id)`. This is not in the proposed design. So T-8 EXPLOIT-FOUND.

- Held exits' positions have closed manually: Suppose a layer has no exit order, but its original position is closed manually. Then on reinit, the layer has no position and no exit. The recon will not create a layer for it (since no ENT position and no EXT). So it's gone. That's fine. But what if the layer had an exit position (filled exit) but the original position was closed manually? Then recon sees the exit position but no ENT position. It will create a layer with `has_position=false` and `has_exit_position=true`. This triggers I4 orphan exit. That's an invariant failure. Not new.

T-9 CAPACITY FORMULA. Is P + K + H + 1 per instance a true worst case? Find a reachable state exceeding it (two-sided, halted instances, CloseBy lag, deferred releases).

The formula: positions P + live exits (K+H) + one L0 = P + K + H + 1. They assume one side laddered to cap P, other side flat. Worst case one-sided.

Let's analyze reachable states.

- Two-sided ladders: The memo says two-sided ladders are 2P + 2(K+H). The fleet formula 2N x (P+K+H+1) assumes all instances one-sided. But an instance can have both sides laddered. The capacity formula per instance for one-sided is P+K+H+1. For two-sided, it's 2P+2(K+H) plus maybe L0? But if both sides have layers, there is no L0 on either side (L0 is only when flat). So two-sided max is 2P + 2(K+H). Compare to one-sided: P+K+H+1. For P=8, K=3, H=1: one-sided = 8+3+1+1=13. Two-sided = 2*8 + 2*4 = 16+8=24. So two-sided is much larger. The fleet formula uses 2N*(P+K+H+1). If some instances are two-sided, the total can exceed 2N*(P+K+H+1). The memo acknowledges: "Two-sided ladders (EURGBP style) are 2P + 2(K + H)." And in s3 conclusions: "Two-sided ladders: separate cap for EURGBP-type pairs, or accept the rarer worst case?" So they know. The formula 2N*(P+K+H+1) is not a true worst case if any instance is two-sided. The worst case is N*(2P+2(K+H)) for two-sided plus others. For 7 pairs, 14 instances, if all two-sided at cap: 14 * (2*8 + 2*4) = 14 * 24 = 336. Way over 200. But is two-sided at cap reachable? Yes, if price oscillates around the straddle, both sides can build ladders. The memo says "Two-sided ladders (EURGBP style) are 2P + 2(K + H)." So the capacity claim in the prompt says "Capacity claim (memo s3): 2N x (P + K + H + 1) <= 200 - margin bounds the fleet in the worst one-sided case." The prompt itself says "worst one-sided case". So T-9 asks if P+K+H+1 per instance is true worst case. It is not, because of two-sided. But the memo already acknowledges two-sided. The threat asks "Find a reachable state exceeding it (two-sided, halted instances, CloseBy lag, deferred releases)." So we can say EXPLOIT-FOUND: two-sided ladders exceed the formula. Also CloseBy lag: between exit fill and netting, there is an extra position. The formula counts positions P. But during CloseBy lag, an exit fill creates an opposite position, so positions become P+1. The exit order is gone. So the count is P+1 positions + (K+H-1) live exits? Actually when an exit fills, the exit order is removed, and an exit position is created. So live exits decrease by 1, positions increase by 1. Total count: positions (P+1) + live exits (K+H-1) + L0 = P+K+H+1. Same total! Because the exit order becomes a position. So the formula still holds. But wait, the exit position is an extra position, but the exit order is gone. So total count unchanged. So CloseBy lag doesn't increase total count. However, if the exit fills and then a new add fills before CloseBy nets, the new add creates a new position and places a new exit (if rank <=K). That could increase count. Let's sequence: P=8, K=3, H=1. One-sided. Total = 8+4+1=13. Exit L2 fills. Now positions = 9 (8 original + 1 exit position). Live exits = 3 (K+H-1). Total = 12? Wait 9+3+1=13. Same. Then CloseBy nets, positions back to 8, live exits 3? Actually after netting, L2 is removed. The layer count decreases. Then a held exit is released, so live exits becomes 4? Let's track: Initially 8 positions, 4 live exits (K+H=4), 1 L0 = 13. Exit L2 fills: positions 9, live exits 3, L0 1 = 13. CloseBy nets: L2 layer removed. Positions 8 (7 original + 1 exit? No, netting closes both the original L2 and the exit position. So positions decrease by 2? Actually original L2 and exit position are closed by CloseBy. So positions = 8 - 1 (L2 original) - 1 (exit position) + 1? Wait before CloseBy, we had 8 original positions + 1 exit position = 9. CloseBy closes the original L2 and the exit position. So positions = 7. The exit order for L2 was already gone. So now we have 7 positions, live exits 3. Then the next held exit is released (because a layer reached rank <=K). That adds 1 live exit. So live exits = 4. Total = 7 + 4 + 1 L0 = 12. But wait, the L0 is only on the flat side. Here we are on the laddered side, so no L0. The formula P+K+H+1 includes one L0 on the flat side. So total across both sides: laddered side has 7 positions + 4 live exits = 11. Flat side has 1 L0 = 1. Total = 12. So it decreased. So CloseBy lag doesn't exceed the formula. However, what if the exit fills and then a new add fills before CloseBy? The new add requires an entry order. Entry orders are allowed when free >= 2+margin. The add fill creates a new position and places an exit if rank <=K. This could increase total. Let's sequence: After exit L2 fills, positions=9, live exits=3. Total=12 (plus L0=13). Now an add fills (it was already resting). This creates a new layer at the bottom. Positions=10. It places an exit if its rank <=K. K=3. The new layer has the highest exit target, so it's rank 8 (beyond K). So it does NOT place an exit. So live exits remain 3. Total = 10+3+1=14. That exceeds 13! Wait, the formula was P+K+H+1 = 8+3+1+1=13. Here we have 10 positions + 3 live exits + 1 L0 = 14. How did we get 10 positions? Initial 8 positions. Exit L2 fills -> 9 positions. An add fills -> 10 positions. But wait, the add was already resting as an entry order. The formula counts live exits and L0, but does it count entry orders? The capacity formula in s3: "positions P + live exits (K + H) + one L0 on the flat side = P + K + H + 1". It does not count the entry add order! The account limit counts positions + pending orders. The formula omits the resting add entry order. In the original problem, the book had 92 positions + 88 exit orders + 20 entry orders = 200. The formula only counts positions and live exits and one L0. It ignores the add entry orders. In `Grind_EnsureAddNext`, the EA maintains one add pending order per side. So on the laddered side, there is always one add entry order resting (unless cap reached). The formula should include that add entry order. The memo s3 says: "(One layer below cap gives the same total: P-1 positions + K+H exits + 1 add + 1 L0.)" So they do account for the add order! Let's read: "One layer below cap gives the same total: P-1 positions + K+H exits + 1 add + 1 L0." So the formula P+K+H+1 is for at cap? At cap, there is no add order because max layers reached. So P positions + K+H exits + 1 L0 = P+K+H+1. One layer below cap: P-1 positions + K+H exits + 1 add + 1 L0 = P+K+H+1. So they do include the add order. Good. So the formula accounts for the add. In my sequence, after exit L2 fills, we have 9 positions, 3 live exits, and there is no add because we are at cap? Actually initially at cap P=8. So no add order. After exit L2 fills, the layer count is still 8? No, the exit fill does not remove the layer until CloseBy. So the number of layers is still 8. The exit order is gone, replaced by an exit position. So positions=9 (8 original + 1 exit position). The layer count is 8. Since we are at cap, no add order. Then CloseBy nets, L2 layer removed. Layer count becomes 7. Now we are below cap, so an add order is placed. The add order is a pending order. So now we have 7 positions (original) + 4 live exits (K+H) + 1 add + 1 L0 = 7+4+1+1=13. That matches. But in my sequence, I had an add filling before CloseBy. Can an add fill before CloseBy? The add order is placed only after CloseBy? Let's check `Grind_EnsureAddNext`: it is called in `OnTickEngine` every tick. It places an add if `n <= 0 || !Grind_CanPlaceEntryLayer(n, max_layers)`? It places if depth < max_layers. So if we are at cap, it does not place an add. So before CloseBy, the layer count is still 8 (since the exit fill does not remove the layer until CloseBy). So we are at cap, no add. So no add can fill before CloseBy. So my sequence is invalid because the add would not be placed until after CloseBy. After CloseBy, layer count becomes 7, then an add is placed. So the total is 13. So the formula holds. Good.

What about deferred releases? D7 says entries deferred when free < 2+margin. Exits allowed when free >=1. If free is 0, exits blocked. But the formula claims free >= margin. If margin is 10, free >=10. So exits always allowed. So no.

What about halted instances? A halted instance does not manage its queue. Its live exits keep working, held exits stay held. So its count is whatever it was. The fleet formula assumes all instances at worst case one-sided. If some are halted, they might have more or fewer orders. But halted instances don't trade, so they don't increase count. They might have a full book. The formula bounds the fleet if all are at worst case. So no.

What about carry pass? The carry pass modifies exits. It doesn't change count.

What about multiple L0? The design says one L0 on the flat side. But what if both sides are flat? Then two L0s? The formula assumes one side laddered, other flat. If both sides flat, there are two L0s. But that's a transient. The worst case one-sided is when one side is at cap and the other flat. So one L0. So formula holds for that case.

What about two-sided ladders? As noted, the formula per instance for two-sided is 2P + 2(K+H) +? If both sides are laddered, there is no L0. So total = 2P + 2(K+H) + 0? But also each side may have an add order if not at cap. At cap, no add. So two-sided at cap: 2P positions + 2(K+H) exits = 2P + 2(K+H). For P=8, K=3, H=1: 16 + 8 = 24. The one-sided formula gives 13. So two-sided exceeds the per-instance formula. The fleet formula 2N*(P+K+H+1) assumes all instances one-sided. If some are two-sided, the fleet total can exceed 2N*(P+K+H+1). For example, if all 14 instances are two-sided at cap, total = 14 * 24 = 336 > 200. So the capacity claim is false. The memo acknowledges this in s3: "Two-sided ladders (EURGBP style) are 2P + 2(K + H)." And in the table, they only compute one-sided. The prompt says "Capacity claim (memo s3): 2N x (P + K + H + 1) <= 200 - margin bounds the fleet in the worst one-sided case." The threat T-9 asks "Is P + K + H + 1 per instance a true worst case? Find a reachable state exceeding it (two-sided, halted instances, CloseBy lag, deferred releases)." So we can say EXPLOIT-FOUND: two-sided ladders exceed it. The memo already notes this as a risk (s10: "Worst-case capacity depends on P"). But the formula in the prompt is claimed as the worst case. So it's not. This is a fatal-to-premise? The premise is that a K-nearest exit queue with the capacity formula solves the observed failures. If the capacity formula is wrong for two-sided, the account can still hit 200, causing naked layers. The memo says "Two-sided ladders: separate cap for EURGBP-type pairs, or accept the rarer worst case?" So they haven't solved it. So the premise is not fully solved. But is it fatal? The observed failures were one-sided? The memo says GBPUSD ALT had 12 resting exits. That's one-sided. EURGBP style is two-sided. If the fleet has EURGBP, it can still hit the limit. So the design doesn't solve for all pairs. The memo says "accept the rarer worst case?" So it's a known limitation. We can mark as EXPLOIT-FOUND or DESIGN-UNSAFE. Since the prompt asks for threats, we should report it.

Also, another capacity issue: The formula counts live exits as K+H. But D2 says ranks K+1..K+H MAY have a live exit. So the maximum live exits is K+H. That's correct. But what about the transition period? When the queue is first applied to a legacy full-exit book, all exits are live. So initially, an instance can have P + P exits + L0 = 2P + 1. For P=8, that's 17. The fleet at transition could exceed the formula. The memo says "Transition: legacy books with every exit resting are legal (extra exits are ranks beyond K+H) and trimmed by the engine." During the trimming, the account can be over the formula. If the account is already near 200, the trimming itself might be blocked because cancels are allowed? Cancels don't need free slots. But new orders (releases) might be blocked. The trimming reduces orders, so it should help. But during the transition, the count can be higher than the steady-state formula. The formula is for steady-state. The initial state might already be at 200. The memo says the observed book at 21:47Z was 92 positions + 88 exits + 20 entries = 200. So the transition starts at 200. The design says "Reinit needs no migration." But if the account is at 200, the engine needs to cancel exits to make room. Cancels are allowed. But if it tries to release a new exit before cancelling enough, it might get 10040. The design says exits allowed when free >=1. If free is 0, it won't send. So it will cancel first? D4 says cancel when a live exit falls beyond K+H. On reinit, all exits are live. The engine trims by cancelling those beyond K+H. It can do that because cancels don't require free slots. So it can reduce the count. So it should recover. But there is a window where the account is full and a front exit fills. The exit fill opens an opposite position. Then CloseBy nets. During this, no new exit is placed because free=0. So a layer might be naked. But the trimming is happening. So it's a risk. We can mention.

Now, we need to write the answer in required format. For each T-1..T-9: VERDICT, LOAD-BEARING CLAIM, MINIMAL REPRO/MECHANISM, SEVERITY. Then GIVENS CHECK, PREMISE VERDICT, OVERRIDE CHECK.

We need be concise but exhaustive. Desired oververbosity 5. We have many findings. Let's organize.

We should prioritize the strongest exploits:

1. D1/D6 schema mismatch: `layer.exit_target` does not include carry shift and is 0.0 for held layers on recon. This breaks ranking, release, I6. (T-2, T-7, T-8)
2. Existing `Grind_RetryMissingExits` and `Grind_HandleSideDealFill` place exits unconditionally, violating D2 hold. (T-1, T-2)
3. D5 clamp uses raw target, not carry-shifted expected, causing I6 adverse fail on gap release with carry shift. (T-5)
4. Carry pass skips held exits, leaving stale shift. (T-7)
5. Capacity formula ignores two-sided ladders and transition. (T-9)
6. D6 I3 relaxation allows rank K naked if free=0. (T-1)

Let's go threat by threat.

T-1 INVARIANT RELAXATION.
Verdict: EXPLOIT-FOUND.
Load-bearing: grind_recon.mqh : Grind_RebuildBookFromTicketsInner : I3_LONG_NAKED / I3_SHORT_NAKED early check at lines ~ after ticket scan; and grind_engine.mqh : Grind_RetryMissingExits (unconditional). Also D6 I3 only ranks 1..J.
Mechanism: Account at 200. Instance long side, K=3, H=1, J=2. Rank 3 layer has no exit (held or failed placement). Free=0. D7 blocks exit send. I3 checks only ranks 1..J, so rank 3 missing passes. The layer is naked. `Grind_RetryMissingExits` is called every tick, but if free=0, `Grind_TryPlaceExitForLayer` sends and gets 10040, returns 0. The layer remains naked indefinitely. No halt. Concrete: exit at rank 1 fills; CloseBy pending; account full; rank 3 becomes rank 1? Wait if rank 1 fills, the layer is removed after netting. Then rank 3 becomes rank 1. It is now within J? J=2. If it becomes rank 1, I3 requires it. So it would fail I3 if naked. But if free=0, it can't place. I3 fails -> quarantine -> halt. So the hiding only works for rank K (3) when J=2. If rank 1 fills, rank 2 becomes rank1 (covered), rank3 becomes rank2 (covered by J=2). So rank3 is now rank2, which is within J. So I3 would catch it. So the naked layer at rank K is only allowed while it remains rank K. Once it moves to <=J, it's caught. But if it stays rank K, it's allowed. So it can hide indefinitely as long as it's rank K. That's the release target. The design intends it to be released. If release fails due to free=0, it hides. So yes.

Severity: fatal-to-premise (reintroduces naked position without halt).

T-2 RANKING.
Verdict: EXPLOIT-FOUND / DESIGN-UNSAFE.
Load-bearing: grind_engine.mqh : Grind_AppendLayer sets `exit_target = Grind_ExitPrice(entry, exit_pips, _Point, dir)` without carry shift. grind_recon.mqh : Grind_RebuildBookFromTicketsInner sets `exit_target` from order/position price for layers with exits, and leaves 0.0 for layers without. D1 requires ranking by theoretical target including carry shift for all layers.
Mechanism: Layer L1 is held (no exit). On reinit, recon creates layer with `has_position=true`, `has_exit_order=false`, `exit_target=0.0`. D1 ranking for longs ascending would place L1 at rank 1 (target 0.0 < any real target). If K=3, it's rank1, so D3 releases it. `Grind_TryPlaceExitForLayer` places at `layer.exit_target = 0.0` -> invalid price. If not reinit, in-memory `exit_target` lacks carry shift. When released after a carry pass, it places at raw target, not carry-shifted expected. I6 fails. Also a filled exit not yet netted: recon sets `exit_target` to fill price, which may differ from theoretical, causing mis-ranking.
Severity: fatal-to-premise.

T-3 RELEASE TIMING.
Verdict: NO-EXPLOIT (invariants pass) but capacity transient.
Load-bearing: grind_engine.mqh : Grind_HandleSideDealFill EXT IN sets exit_position_ticket, queues CloseBy. grind_recon.mqh : Grind_ReconLayerHasExitCoverage counts filled exit position as coverage.
Mechanism: Front exit fills. Layer has `exit_position_ticket` set. I3 passes because coverage counts. I1 passes (one exit position). I6 passes if fill favourable. CloseBy processed next tick. No invariant failure. Quarantine not triggered. The only issue is extra position during lag, but total count unchanged (exit order becomes position). So NO-EXPLOIT for invariants.
Severity: cosmetic (capacity transient handled by formula? Actually total count same, so fine).

T-4 HOLD CANCEL.
Verdict: DESIGN-UNSAFE (missed deal -> stale tracker -> queue stalls). But if deal processed, no duplicate.
Load-bearing: grind_engine.mqh : Grind_CancelPendingOrder returns true only on DONE; does not clear on confirmed gone. grind_engine.mqh : Grind_HandleSideDealFill updates tracker only on deal. fxgrind.mq5 : OnTradeTransaction calls engine even in quarantine, but if HistoryDealSelect fails, deal is lost (no retry).
Mechanism: Exit order for L4 is beyond K+H. Engine calls `Grind_CancelPendingOrder`. Meanwhile, the exit order fills. The deal is missed because `Grind_DealSelect` fails (e.g., history not ready). `Grind_HandleSideDealFill` returns early, tracker still has `exit_order_ticket`. Cancel fails because order gone. Tracker remains set. L4 appears to have a live exit. When it reaches rank <=K, `Grind_TryPlaceExitForLayer` refuses because `exit_order_ticket != 0`. The layer never gets a live exit. The actual exit position exists but CloseBy not queued. The queue stalls. Later reinit fixes, but until then naked.
Severity: fixable-within-design (add confirmed-gone check and deal retry).

T-5 GAP CLAMP.
Verdict: EXPLOIT-FOUND.
Load-bearing: grind_engine.mqh : Grind_TryPlaceExitForLayer places at `layer.exit_target` (raw). grind_recon.mqh : Grind_ReconExitMatchesEntry expects `entry +/- exit_pips + carry_shift`. D5 clamps to `max(target, bid+stops)` using raw target.
Mechanism: Long layer entry 1.1000, exit_pips 50 pips, raw target 1.1050. Carry shift +20 pips (expected 1.1070). Market gaps up to bid 1.1060. Held exit released. D5 clamps to max(1.1050, 1.1060+0.0005) = 1.1065. But expected is 1.1070. actual 1.1065 < expected 1.1070, so diff adverse for long. I6 fails even with favourable-resting relaxation (which only tolerates diff >= 0). Quarantine -> halt.
Severity: fatal-to-premise.

T-6 HYSTERESIS.
Verdict: NO-EXPLOIT for pure price path; DESIGN-UNSAFE for carry-induced reordering.
Load-bearing: D1 ranking by theoretical target; new layers have no carry shift. Carry shifts can reorder ranks if existing layers have large negative shifts.
Mechanism: Long side, existing L0 has carry shift -50 pips (target 1.1100). New add L1 fills with no shift, target 1.1060. L1 is inserted before L0, pushing L0 from rank K to K+1. If H=0, L0 exit (live) is beyond K+H, cancelled. Later carry pass updates L0's shift, moving it back, re-releasing. This churns cancel/place. H=1 absorbs one rank, but larger shifts can still exceed. Also ties in exit targets with no tie-break can cause rank oscillation if one has live exit and one doesn't.
Severity: fixable-within-design (specify tie-break, update held shifts).

T-7 CARRY PASS.
Verdict: EXPLOIT-FOUND.
Load-bearing: grind_carry.mqh : Grind_CarryExitPassBegin iterates only layers with `exit_order_ticket != 0`. Held exits have 0, so skipped. Their `layer.exit_target` and persisted shift are not updated.
Mechanism: L1 held. Nightly carry pass runs, skips L1. L0 fills, L1 becomes rank1, released. `Grind_TryPlaceExitForLayer` places at old `layer.exit_target` (no new carry shift). I6 expects new shift. Mismatch -> quarantine -> halt. Or if I6 relaxed, exit is mispriced.
Severity: fatal-to-premise.

T-8 RECONSTRUCTION.
Verdict: EXPLOIT-FOUND.
Load-bearing: grind_recon.mqh : Grind_RebuildBookFromTicketsInner sets `exit_target = 0.0` for layers without an exit (from Grind_ReconEnsureLayer). It does not compute theoretical target from entry + exit_pips + carry shift.
Mechanism: Reinit on a held book. Layer L1 has position, no exit. Recon creates scratch with `exit_target=0.0`. After invariants (relaxed I3 passes), it copies to `g_grind_long.layers` with `exit_target=0.0`. Engine runs. L1 is rank1 (target 0.0). D3 releases it. `Grind_TryPlaceExitForLayer` places sell limit at 0.0. Invalid price -> 10040 or worse. Instance halts or naked. This is a direct schema mismatch: design says held exit target is recoverable via `Grind_CarryShiftGetForRecon`, but code never uses it to populate `exit_target`.
Severity: fatal-to-premise.

T-9 CAPACITY FORMULA.
Verdict: EXPLOIT-FOUND.
Load-bearing: MEMO s3 formula `2N x (P + K + H + 1)`. Code counts positions + orders. Two-sided ladders: memo s3 admits `2P + 2(K+H)`. For P=8, K=3, H=1: one-sided 13, two-sided 24. 14 instances two-sided = 336 > 200.
Mechanism: All 14 instances on EURGBP-style pairs build both sides to cap. Each instance: 16 positions + 8 live exits = 24. Total 336. Account hits 200. Exits refused. Layers naked. The formula only bounds one-sided. The memo acknowledges this as a risk (s10) but the premise claims the formula solves the observed failures. It does not for two-sided.
Severity: fatal-to-premise.

GIVENS CHECK:
G1: Accurate? `Grind_ReconCheckInvariants` requires exit coverage for every layer. Yes, in code. But note: D6 changes this. No counterexample. G2: `Grind_CheckBookInvariants` rebuilds from broker tickets, never compares in-memory. Yes. G3: `Grind_HandleSideDealFill` places exit immediately on ENT fill; on EXT fill queues CloseBy; OnTick processes CloseBy first. Yes. G4: `Grind_TryPlaceExitForLayer` places at `layer.exit_target` no clamp. Yes. G5: `Grind_CancelPendingOrder` returns true only on DONE. `Grind_EnsureAddNext` clears tracker on true or confirmed-gone. Yes. G6: `Grind_CarryShiftGetForRecon` returns validated per-position shift from persisted state. Yes. G7: Halted instances ignore fills. `OnTimer` runs no trading logic. Yes. G8: Quarantine constants. Yes. So all GIVENS accurate. No counterexample.

PREMIISE VERDICT: Does a K-nearest exit queue with the capacity formula solve the observed failures? No. The queue mechanism is not implementable with current data model because exit targets are not stored/derived for held layers; existing retry and ENT fill handlers place exits unconditionally; carry pass skips held exits; capacity formula ignores two-sided ladders. The observed failures (naked layers at 200) can still occur in two-sided and transition states, and the invariant relaxation allows rank K naked indefinitely if free=0. So premise fails.

OVERRIDE CHECK: Does any finding kill the premise, or are all fixable within it? Several findings are fatal to premise as stated: D1/D6 schema mismatch (held exit target not computed), D5 clamp vs carry-shift I6, carry pass skipping held exits, reconstruction 0.0 target, capacity formula for two-sided. These are not mere implementation bugs; they require new persistent state or changes to recon/carry/invariant logic that the design does not specify. The core insight (queue) may be viable, but the proposed rev3 design is not. So the premise as written is killed.

We need write in the required format. Let's draft.

We should be careful to not propose remedies. We can mention what is missing.

Let's structure:

For each T, write:
- VERDICT: ...
- LOAD-BEARING CLAIM: file : function : invariant
- MINIMAL REPRO / MECHANISM: ...
- SEVERITY: ...

Then GIVENS CHECK: ...
Then PREMISE VERDICT: ...
Then OVERRIDE CHECK (last line): ...

Make sure last line is OVERRIDE CHECK. The prompt says "OVERRIDE CHECK (last line): does any finding kill the premise, or are all fixable within it?" So the last line should be that.

We need include line numbers? We can reference function names and approximate lines from GIVENS. We don't have exact line numbers for all. We can use ~ for approximations.

Let's write.

## Final Report

T-1 INVARIANT RELAXATION
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: `grind_recon.mqh` : `Grind_RebuildBookFromTicketsInner` : early I3 checks `I3_LONG_NAKED` / `I3_SHORT_NAKED` for every layer with position and no coverage; D6 would relax this to ranks 1..J only. `grind_engine.mqh` : `Grind_RetryMissingExits` places exits unconditionally for any layer with `exit_order_ticket == 0 && exit_position_ticket == 0`.
- MINIMAL REPRO: Account at 200 (free = 0). Long side, K = 3, H = 1, J = max(1, K-1) = 2. Layer at rank K = 3 has no live exit. D7 blocks exit sends when free < 1. I3 checks only ranks 1..2, so rank 3 missing passes. `Grind_RetryMissingExits` runs every tick, calls `Grind_TryPlaceExitForLayer`, gets 10040, returns 0. The layer remains naked indefinitely. No halt occurs because I3 never fails for rank 3. If the layer later moves to rank <= J, I3 fails and quarantine/halt follows, but until then the naked position is hidden.
- SEVERITY: fatal-to-premise (reintroduces the exact naked-layer failure mode the design claims to eliminate).

T-2 RANKING
- VERDICT: EXPLOIT-FOUND / DESIGN-UNSAFE
- LOAD-BEARING CLAIM: `grind_engine.mqh` : `Grind_AppendLayer` sets `exit_target = Grind_ExitPrice(entry_price, exit_pips, _Point, dir)` with no carry shift. `grind_recon.mqh` : `Grind_RebuildBookFromTicketsInner` sets `exit_target` from the live order/position price for layers that have an exit, and leaves `exit_target = 0.0` for layers with no exit (from `Grind_ReconEnsureLayer`). D1 requires ranking by theoretical target `entry +/- exit_pips + carry_shift` for every layer.
- MINIMAL REPRO: Long side, layer L1 is held (no exit). On reinit, recon creates L1 with `has_position = true`, `has_exit_order = false`, `exit_target = 0.0`. D1 ascending long ranking places L1 at rank 1 (0.0 < any real target). D3 releases it. `Grind_TryPlaceExitForLayer` places a sell limit at `layer.exit_target = 0.0`. Invalid price / 10040. If no reinit, in-memory `exit_target` lacks carry shift; after a carry pass it is stale, so release places at raw target while I6 expects carry-shifted target. A filled exit not yet netted is worse: recon sets `exit_target` to the fill price, which may differ from the theoretical target, mis-ranking the layer.
- SEVERITY: fatal-to-premise (D1 is not well-defined with the current layer state model).

T-3 RELEASE TIMING
- VERDICT: NO-EXPLOIT (invariants pass; capacity transient only)
- LOAD-BEARING CLAIM: `grind_engine.mqh` : `Grind_HandleSideDealFill` EXT IN sets `exit_order_ticket = 0`, `exit_position_ticket = position_id`, queues CloseBy. `grind_recon.mqh` : `Grind_ReconLayerHasExitCoverage` counts a filled exit position as coverage.
- MINIMAL REPRO: Front exit fills. `OnTradeTransaction` processes the EXT IN deal. Layer now has `exit_position_ticket != 0`. Next tick `Grind_CheckBookInvariants` rebuilds from broker tickets; it sees original position + exit position for the same layer, so I3 passes, I1 counts one exit, I6 passes if fill is favourable. CloseBy runs at start of next `OnTick`. No invariant fails beyond quarantine. The only effect is an extra open position during the lag, but total count is unchanged because the exit order became a position.
- SEVERITY: cosmetic (capacity formula still holds in steady state; lag does not increase total count).

T-4 HOLD CANCEL
- VERDICT: DESIGN-UNSAFE (missed deal → stale tracker → queue stalls; duplicate exit only if tracker cleared incorrectly)
- LOAD-BEARING CLAIM: `grind_engine.mqh` : `Grind_CancelPendingOrder` returns true only on `TRADE_RETCODE_DONE`; it does not clear the tracker on confirmed-gone. `grind_engine.mqh` : `Grind_HandleSideDealFill` updates the tracker only when the deal is selected. `fxgrind.mq5` : `OnTradeTransaction` calls the engine even in quarantine, but if `HistoryDealSelect` fails, the deal is silently dropped (no retry).
- MINIMAL REPRO: Exit order for L4 is beyond K+H. Engine calls `Grind_CancelPendingOrder`. Meanwhile the exit order fills. `OnTradeTransaction` fires, but `Grind_DealSelect` fails (history not ready). `Grind_HandleSideDealFill` returns early. Tracker still has `exit_order_ticket`. Cancel fails because order is gone. Tracker remains set. L4 appears to have a live exit. When L4 later reaches rank <= K, `Grind_TryPlaceExitForLayer` refuses because `exit_order_ticket != 0`. The actual exit position exists but CloseBy is not queued. The queue stalls until reinit. No I2 duplicate (no second EXT is placed), but the layer is effectively naked from the queue's perspective.
- SEVERITY: fixable-within-design (add confirmed-gone handling and deal retry), but currently not covered.

T-5 GAP CLAMP
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: `grind_engine.mqh` : `Grind_TryPlaceExitForLayer` places at `layer.exit_target` (raw formula). `grind_recon.mqh` : `Grind_ReconExitMatchesEntry` expects `entry +/- exit_pips + carry_shift`. D5 clamps using `max(target, bid+stops)` or `min(target, ask-stops)` on the raw target, not the carry-shifted expected target.
- MINIMAL REPRO: Long layer, entry 1.1000, exit_pips 50 pips, raw target 1.1050. Carry shift +20 pips (expected = 1.1070). Market gaps up; bid = 1.1060. Held exit released. D5 clamps to `max(1.1050, 1.1060 + 0.0005) = 1.1065`. Expected is 1.1070. Actual 1.1065 < expected 1.1070, so diff is adverse for a long. I6 fails even with the proposed “favourable resting exit” relaxation, which only tolerates `diff >= 0` for longs. Quarantine → halt.
- SEVERITY: fatal-to-premise (the clamp rule and I6 relaxation are inconsistent with carry-shifted targets).

T-6 HYSTERESIS
- VERDICT: NO-EXPLOIT for pure price path; DESIGN-UNSAFE for carry-induced reordering and ties
- LOAD-BEARING CLAIM: D1 ranks by theoretical target including carry shift. New layers have no carry shift. Existing layers with large negative carry shifts can have higher targets. `grind_carry.mqh` : `Grind_CarryExitPassStep` modifies only live exits.
- MINIMAL REPRO: Long side, existing L0 has carry shift −50 pips (target 1.1100). New add L1 fills with no shift, target 1.1060. L1 is inserted before L0, pushing L0 from rank K to rank K+1. With H = 0, L0’s live exit is beyond K+H, so it is cancelled. Later a carry pass updates L0’s shift, moving its target back, and it is re-released. This is a cancel/place cycle not driven by price. H = 1 absorbs one rank, but larger shifts can still exceed the band. Equal exit targets also have no specified tie-break; if one layer has a live exit and the other does not, an unstable sort can oscillate between I3 pass and fail.
- SEVERITY: fixable-within-design (tie-break rule, held-exit shift update), but not currently specified.

T-7 CARRY PASS
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: `grind_carry.mqh` : `Grind_CarryExitPassBegin` iterates only layers with `layer.exit_order_ticket != 0`. Held exits have `exit_order_ticket == 0`, so they are skipped. Their persisted shift and `layer.exit_target` are never updated.
- MINIMAL REPRO: L1 is held (no exit). Nightly carry pass runs; L1 is skipped. Next day L0 exit fills and nets; L1 becomes rank 1 and is released. `Grind_TryPlaceExitForLayer` places at L1’s old `layer.exit_target` (no new carry shift). I6 expects the new carry-shifted target. Mismatch → quarantine → halt. If I6 is relaxed to tolerate favourable resting exits, the exit is still mispriced relative to the correct carry-adjusted level.
- SEVERITY: fatal-to-premise (held exits systematically miss carry adjustments).

T-8 RECONSTRUCTION
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: `grind_recon.mqh` : `Grind_ReconEnsureLayer` initialises `exit_target = 0.0`. `Grind_RebuildBookFromTicketsInner` never computes a theoretical target for layers without an exit; it only copies `long_scratch[j].exit_target` into `long_out.layers`. D1 requires held-exit targets to be recoverable from `entry_price + exit_pips + carry_shift`.
- MINIMAL REPRO: Reinit on a held book. Layer L1 has a position and no exit. Recon creates L1 with `exit_target = 0.0`. Under D6, I3 is relaxed and passes for L1 if it is beyond J. The engine runs. L1 is ranked 1 (target 0.0). D3 releases it. `Grind_TryPlaceExitForLayer` places a sell limit at 0.0. Invalid price / 10040. The instance halts or leaves the layer naked. The memo claims the hold target is recoverable via `Grind_CarryShiftGetForRecon`, but the code never calls it to populate `exit_target` for missing exits.
- SEVERITY: fatal-to-premise (reinit on a held book is broken).

T-9 CAPACITY FORMULA
- VERDICT: EXPLOIT-FOUND
- LOAD-BEARING CLAIM: MEMO s3 formula `2N x (P + K + H + 1)`. Code counts positions + pending orders (`PositionsTotal() + OrdersTotal()`). Memo s3 itself admits two-sided ladders are `2P + 2(K + H)`.
- MINIMAL REPRO: All 14 instances on EURGBP-style pairs build both sides to cap. P = 8, K = 3, H = 1. One-sided worst case per instance = 13. Two-sided at cap per instance = 2*8 + 2*4 = 24. Total = 14 * 24 = 336 > 200. Account hits 200, exits refused, layers naked. The formula only bounds the one-sided case. The memo acknowledges this as a risk (s10) but the premise claims the formula solves the observed failures. It does not for two-sided fleets. Transition from a legacy full-exit book (all exits live) can also temporarily exceed the steady-state formula.
- SEVERITY: fatal-to-premise (capacity claim is not a true worst case).

GIVENS CHECK
- G1: Accurate. `Grind_ReconCheckInvariants` requires exit coverage for every layer; `Grind_ReconLayerHasExitCoverage` is order OR filled exit position; I6 uses `Grind_ReconExitMatchesEntry` with favourable tolerance only when `exit_is_filled`.
- G2: Accurate. `Grind_CheckBookInvariants` rebuilds from `Grind_ReconCollectBrokerTickets` and never compares against `g_grind_long` / `g_grind_short`.
- G3: Accurate. `Grind_HandleSideDealFill` places exit on ENT fill; on EXT fill queues CloseBy; `OnTick` calls `Grind_ProcessCloseByQueues` first.
- G4: Accurate. `Grind_TryPlaceExitForLayer` places at `layer.exit_target` with no marketability clamp.
- G5: Accurate. `Grind_CancelPendingOrder` returns true only on `TRADE_RETCODE_DONE`; `Grind_EnsureAddNext` clears tracker on true or confirmed-gone.
- G6: Accurate. `Grind_CarryShiftGetForRecon` returns a validated per-position shift from persisted state.
- G7: Accurate. `Grind_OnTradeTransactionEngine` returns early if `g_grind_halted`; `OnTimer` runs no trading logic.
- G8: Accurate. Constants match `GRIND_QUARANTINE_MIN_MS 3000`, `GRIND_QUARANTINE_MIN_CHECKS 3`; I3 is quarantinable, I7 and I5 are not.

PREMISE VERDICT
The K-nearest exit queue with the proposed capacity formula does not solve the observed failures. The design is not implementable against the current source without additional persistent state and logic that the memo does not specify: held-exit targets are not computed or carried across reinit (`exit_target = 0.0`), `Grind_RetryMissingExits` and `Grind_HandleSideDealFill` place exits unconditionally, the carry pass skips held exits, the gap clamp uses a raw target that can be adverse to the carry-shifted I6 expectation, and the capacity formula ignores two-sided ladders. The observed failure mode (naked layers when the account hits 200) remains reachable in two-sided and transition states, and the I3 relaxation can hide a rank-K naked layer indefinitely when `free == 0`.

OVERRIDE CHECK (last line): Findings T-1, T-2, T-5, T-7, T-8, and T-9 kill the premise as written; they are not fixable within the proposed design without new persistent state, modified recon/carry/invariant logic, and a two-sided capacity bound.