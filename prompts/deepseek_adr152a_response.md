# DeepSeek R1 Audit Report

## Internal Reasoning

We need answer with required format. Need audit code. Need check givens against source. Need exhaustive. Need no implementation code. We need parse. We have only code shown? The prompt says READ access to ea/ docs, but in message only code to audit includes many files but not grind_config.mqh, grind_api_counter.mqh, grind_comment.mqh, grind_cap.mqh, grind_recon_failure etc. Need mark unavailable for claims needing those. Need use file:line from actual source opened. We don't have line numbers in pasted code. We can cite approximate? The instruction says file:line from actual source you opened (or NOT VERIFIED -- file unavailable). Since pasted doesn't have line numbers, we can cite function and line ranges maybe? We need be honest: "grind_engine.mqh: Grind_TryPlaceL0 ... (line numbers not present in pasted source)". But required says print CONFIRMED/REFUTED with file:line. We can use function names and code snippets, but not actual line numbers. Maybe use "grind_engine.mqh :: Grind_HandleSideDealFill (ENT branch, after ExitQManageSide)" as file:function. But GIVENS CHECK wants file:line. We can say "CONFIRMED (function-level; pasted artifact lacks line numbers)". However maybe we can infer line numbers? The prompt gives G1 ~921, etc. But source pasted no line numbers. We could state exact line numbers from prompt? Need not fabricate. Better: Use given line references and verify code existence. For each G, say CONFIRMED/REFUTED at file:function/source marker. But format asks file:line. Hmm.

Need analyze threats deeply. Let's inspect code.

We need understand system.

Key files:

grind_engine.mqh:
- Grind_HandleSideDealFill(side, is_long, deal_ticket, magic, slot, exit_pips, deadband_pips, max_layers, lots)
  - checks processed, selects deal, symbol, magic.
  - entry_type.
  - if OUT_BY: for side layers where position_ticket == position_id and exit_position_ticket != 0, mark processed, accumulate, queue event, CarryShiftDelete, RemoveLayerAt, ExitQManageSide, return. Note: no flag.
  - parse comment; slot, side, role.
  - MarkDealProcessed.
  - if role ENT:
    - clear matching pending ticket (l0 or add)
    - AppendLayer(deal_price, position_id, c_layer, exit_pips, is_long)
    - ExitQManageSide
    - if c_layer == 0 && l0_pending_ticket != 0 && != order_ticket: stray L0 cancel
    - return
  - if role EXT && entry_type == IN: exit fill, find layer, set exit_order_ticket=0, exit_position_ticket=position_id; QueueCloseBy; QueueExitMicrostructureMeasure; return.

No EnsureAddNext. So G1 confirmed.

Grind_OnTickEngine:
- GuardsAllowTrading
- g_grind_ent_sent_this_tick = false
- Grind_RetryMissingExits
- ReconcileStrayL0 for long/short
- compute bid ask mid stops
- buy_target/sell_target clamp
- if long depth 0 and marketable place L0
- if short depth 0 and marketable place L0
- if long >0 and short==0 TryRecenterOppositeL0(short,...)
- if short>0 and long==0 TryRecenterOppositeL0(long,...)
- OnSideCapTransition long/short
- if long depth>0 || long.add_pending_ticket !=0 EnsureAddNext(long)
- if short depth>0 || short.add_pending_ticket !=0 EnsureAddNext(short)

So G2 confirmed: EnsureAddNext only at end (plus no other calls; but search: yes only calls in OnTickEngine; tests may call? no).

G3: g_grind_ent_sent_this_tick defined grind_exitq.mqh line? in pasted: bool g_grind_ent_sent_this_tick = false; after forward declarations. cleared in OnTickEngine before RetryMissingExits. Set after successful entry send in both TryPlaceL0 and EnsureAddNext. One entry send per instance per tick? Yes, same global; L0 and add share. But note: TryPlaceL0 sets if l0_pending_ticket>0. EnsureAddNext sets if add_pending_ticket>0. It is not checked in Exit placement. Good.

G4: EnsureAddNext: if side.add_pending_ticket !=0, select order, parse label; if !label_ok cancel and return. Then later if n<=0 or !CanPlace return; cap check; compute target; clamp; marketable; if side.add_pending_ticket !=0 { resting price; if within deadband return; ModifyPendingPrice; return; }. So not place-once; re-prices if outside deadband. Confirmed.

G5: Adr013ClampBuy: min(theoretical, bid - min_dist) if theoretical >= bid else theoretical. BuyLimitMarketable returns buy_limit < ask. It is misnamed returns TRUE when buy limit passive? Wait buy limit < ask means passive (below ask). Yes. Clamped add rests below bid? If theoretical >= bid, out = min(theoretical, bid - min_dist) <= bid - min_dist < bid. If theoretical < bid, out=theoretical < bid. So below or equal? min_dist >= point, so below bid if theoretical >= bid; if theoretical<bid, below bid. So rests below bid. Never reaches market? Buy limit below bid is passive, yes. Confirmed.

G6: Grind_SlotEntryAllowed formula. Evaluated inside Grind_SlotLockAcquire? Actually in TryPlaceL0 and EnsureAddNext: acquire lock, then compute limit/used/resting_ent, then Grind_SlotEntryAllowed, release. It is inside lock. Grind_SlotLockAcquire retries Sleep(1) up to GRIND_SLOT_LOCK_MAX_RETRIES, steals stale lock. Confirmed from exitq.mqh.

G7: Grind_RetryMissingExits runs at top of OnTickEngine, before entry work. Confirmed. Exits priority.

G8: Reconstruction, invariants and ValidateAddLabelIndex as ADR-151; no invariant asserts layer has resting entry order. Need check recon code. Grind_ReconCheckPendingAddCorrupt only checks if add_pending_ticket !=0 it is an order ticket; doesn't assert existence of add for layer. Invariants I1-I8: no check for ENT order. Confirmed.

Now threats.

T-1: Is flag-plus-next-tick equivalent to same-tick? Need construct price path where flag plus next tick misses fill. The ADR premise: after ENT fill in OnTradeTransaction, set flag. It is serviced at next OnTickEngine, which occurs at next tick. MT5 order of events: OnTradeTransaction may be called asynchronously? It is called by terminal when trade transaction occurs, often after execution; OnTick is called on ticks. There may be multiple deals in a burst before next tick. The flag is per side, no layer index. If price moves between fill and next tick, the add target computed at service time uses current deepest layer entry price? Wait Grind_ComputeAddTarget uses deepest layer entry_price, which is the filled layer's entry price appended in handler. So target is based on fill price, not current mid. The clamp uses current bid/ask. If price drops before service, theoretical target (e.g., entry 1.1000, add 10 pips -> 1.0990 for long) may be above current bid. Clamp sets to bid - min_dist ~1.0984, so it rests lower, misses the fill at 1.0990 that occurred between. That is the ADR's own repro. But does flag-plus-next-tick fix it? The ADR says it places earlier in same tick? Wait D2: service due adds FIRST in Grind_OnTickEngine. But the flag is set in OnTradeTransaction, which is not inside OnTickEngine. If OnTradeTransaction fires after OnTick for that tick, the flag is serviced on the next OnTick. If price ticks down between fill and next OnTick, still misses. If OnTradeTransaction fires before OnTick? MT5 event sequence: OnTick is called on new tick; OnTradeTransaction may be called when trade transaction occurs, often between ticks or during. It is not guaranteed same tick. The ADR claims "same tick" but actually flag-plus-next-tick is at best next OnTick. The only way to be same tick as fill is to place inside transaction handler (D4 forbids). Or if MT5 delivers OnTradeTransaction and then OnTick before price moves? Not guaranteed. The ADR's repro itself: add fills at 1.1000, price drops to 1.0985 before EnsureAddNext runs. With flag, EnsureAddNext runs at start of next OnTick. If the drop happens before next OnTick, it still misses. So T-1 exploit: flag does not eliminate latency; it reduces one inter-path delay (tick allowance) but not the tick boundary. Need check if OnTradeTransaction can be invoked synchronously during OnTick? In MT5, OnTradeTransaction is called when a trade transaction is executed, independent of OnTick. It may be called during OnTick? Actually terminal calls OnTradeTransaction in the same thread, possibly after OrderSend returns? If OrderSend is called from OnTick, the transaction event may be queued and processed after OnTick returns. If fill occurs from a pending order triggered by price, OnTradeTransaction is called when the terminal receives trade transaction, which may be before or after OnTick for the tick that triggered it. MQL5 docs: OnTradeTransaction is called when a trade transaction is executed. It can be called during OnTick? The event queue: OnTick, OnTradeTransaction, OnTimer. There's no guarantee. So flag-plus-next-tick can miss. Severity fatal-to-premise? It means ADR does not solve the stated latency fully. But maybe it still buys some: it bypasses g_grind_ent_sent_this_tick, so if OnTradeTransaction fires before OnTick, the due add is placed at start of that OnTick before other entries. But if the price moved in the same tick? The fill itself happened at some price; the next tick may be same or later. If the fill was a limit order resting, the market touched the price. After fill, price may continue moving. The next tick could be lower (for long buy add). So still misses. The ADR's own repro: "Before Grind_EnsureAddNext runs, price drops to 1.0985." If flag service runs at start of next OnTick, and price at that OnTick is 1.0985, same miss. So T-1 EXPLOIT-FOUND. It does not achieve same-tick; it only reorders within next tick. To actually place same tick, must send from OnTradeTransaction or have OnTradeTransaction call a function that places. D4 prevents. So premise not fully fixed. But maybe D2 says before L0 and before g_grind_ent_sent_this_tick can be consumed. If OnTradeTransaction fires after OnTick, the flag waits. If it fires before OnTick, it runs at start. Still not same tick as fill if fill was from previous tick. Need define "same tick" relative to fill. The fill happens in OnTradeTransaction, not OnTick. The next OnTick may be milliseconds later. The ADR's claim "first tick after the fill" is correct, not same tick. The threat asks "Is the flag genuinely equivalent to same-tick?" Answer no. Exploit: price path: OnTick N: price ticks down to 1.0985, triggers pending buy limit at 1.0990? Wait buy limit at 1.0990 fills when ask <=? For buy limit, fill when ask <= limit. If price drops, it fills. Then OnTradeTransaction for fill occurs. Before next OnTick, price continues to 1.0980. Next OnTick: flag service computes target 1.0990 (entry 1.1000 - 10 pips). Current bid maybe 1.0979, clamp to bid - min_dist = 1.0978. Rests lower. The market already passed 1.0990, so no fill. A same-event placement at fill time would have placed at 1.0990, which might have filled on the subsequent drop. So yes.

T-2: Bypassing g_grind_ent_sent_this_tick. What did budget protect? It limits one entry send per instance per tick. Shared across sides and paths. Why? Maybe to avoid multiple OrderSend in one tick consuming API requests, and to prevent L0 and add both placing in same tick causing over-commitment? Also lock contention? The guard uses global account limit, but per-instance budget prevents burst. With D3, due add may bypass once per side. So in a tick, you could have due long add + due short add + L0 long + L0 short + ordinary EnsureAddNext long/short. But D3 says may place one entry per side even if g_grind_ent_sent_this_tick already set. And D2 services due adds first, before L0. So if both sides have due flags, ServiceDueAdds could place two entries (one per side) in the same tick. Then g_grind_ent_sent_this_tick is set? It says bypasses once per side. Does ServiceDueAdds set g_grind_ent_sent_this_tick? D3 says "may place one entry per side even if g_grind_ent_sent_this_tick is already set, and clears the flag it consumed." It doesn't say it sets the global. If it does not set, then ordinary paths could also place in same tick, because g_grind_ent_sent_this_tick remains false. Wait D2: "before g_grind_ent_sent_this_tick can be consumed by another path: g_grind_ent_sent_this_tick = false; Grind_ServiceDueAdds(...); ... existing L0 / recenter / cap / EnsureAddNext work ..." If ServiceDueAdds places a due add and does not set g_grind_ent_sent_this_tick, then later L0 placement could also send, violating original one-entry-per-tick budget. D3 says "A due add may bypass g_grind_ent_sent_this_tick once per side." That implies it can place despite the flag. But if it doesn't set the flag, then the flag is still false, so the budget is not enforced for subsequent sends. The original budget was one entry send per instance per tick. With D3, you could get up to? Due long + due short = 2. Then ordinary L0 long + L0 short = 2 more (if both flat and marketable), plus ordinary EnsureAddNext for sides with depth>0 or pending. But EnsureAddNext for sides that just had due add placed: add_pending_ticket set, so it will go into modify path, not send. For the other side, if due add placed, add_pending_ticket set. So ordinary EnsureAddNext won't place new. But L0 placement for opposite side? If side has depth 0, due add cannot exist for that side because no layers. So due flags only for sides with fills (depth>0). The opposite side may be flat. D2 services due adds before L0. Suppose long has due add. ServiceDueAdds places long add, does not set g_grind_ent_sent_this_tick. Then L0 placement check: if short depth==0 and sell limit marketable, Grind_TryPlaceL0(short) can place a short L0. That's a second entry send in same tick. Then later EnsureAddNext long sees add_pending_ticket !=0, may modify; short depth now 1? Wait L0 is pending order, not filled, so short depth still 0. EnsureAddNext short condition: if depth>0 || add_pending_ticket !=0. short depth 0, add_pending_ticket 0, so skip. So total 2 sends. If both sides had due flags, ServiceDueAdds places 2. Then L0 placements for flat sides? If both sides have depth>0, L0 not placed because depth>0. If one side flat, could place L0. So up to 3 sends per instance per tick? Let's construct: long has due flag (depth>0). short is flat. ServiceDueAdds places long add. Then L0 short places. Then maybe if long add placed, EnsureAddNext long modifies (another send). So 2 new entries + 1 modify. If both sides have due flags, 2 places. Then if after due adds, one side depth? Both depth>0, no L0. So max 2 places (one per side) plus modifies. But original budget allowed 1 entry send per instance per tick. Now up to 2 places per tick. Is that a problem? The guard uses account limit; but burst API requests? At 16 instances, if all have fills, could be 32 places in one tick instead of 16. That's a request rate spike. The ADR claims zero additional sends in steady state, but in a burst of fills across both sides/instances, it doubles the per-tick entry sends. Also lock contention: each service acquires lock. Could cause Sleep(1) retries. The original budget likely protected against API rate limits and lock contention. Also ordering: due adds before L0 could place far adds before near L0, consuming guard units. However D3 says guard not bypassed. But priority: due adds are far adds (they are next add after fill, potentially far from mid). Servicing them before L0 means a far add can consume the last guard slots, starving L0 (near mid) of a flat opposite side. That's T-4. For T-2, the bypass allows two sends per instance per tick, violating the one-entry budget. Need quantify: 16 instances * 2 sides = 32 places per tick worst case, plus modifies. FTMO limit 2000/day? Not directly. But API counter soft warn 1800. 32 per tick is not huge if ticks are milliseconds, but a burst of fills in one tick could produce 32. The original budget was one per instance per tick, so 16. Doubling. Also lock: each place acquires global lock. ServiceDueAdds for both sides on each instance will acquire lock sequentially? Actually per instance, long then short. Across 16 instances, if all have due flags, they will contend for one global lock. Each lock hold includes AccountInfoInteger, PositionsTotal, OrdersTotal, OrdersTotal scan. 32 acquisitions in a burst. Sleep(1) retries could delay. The lock is global, but each instance has its own process? MT5 EAs run in separate charts, separate threads? They share global variables but lock is via GlobalVariable. So contention real. Original budget reduced lock acquisitions to one per instance per tick. D3 doubles. Could be acceptable? The threat says show a burst where one bypass per side per tick produces a request rate or ordering the original budget was there to prevent. We can show burst of fills on both sides across all 16 instances: 32 due adds, ServiceDueAdds places 2 per instance = 32 OrderSend before any L0. The original budget would have allowed 16 entry sends per tick (one per instance). Now 32. If all also have L0 flat opposite? But if due add on a side, that side has depth>0, so opposite may be flat. If both sides have due flags, both depth>0, so no L0. So max places 2 per instance. Original max 1 per instance. So request rate doubles. Also if due add refused by guard, it stays due and retries next tick. If many instances have due flags and guard saturated, they will each acquire lock every tick to check guard, then sleep? Actually if guard refuses, they release and retry next tick. So 32 lock acquisitions per tick on saturated account. That's T-6.

T-3: Flag lifecycle. Enumerate paths. The ADR defines globals g_grind_add_due_long/short. They are not in source. Need analyze where they would need clearing. Setting: in ENT branch after ExitQManageSide. Clearing: D5 when serviced placed, re-priced, or refused by cap/label validation. Also R2 says clear in OnInit. But what about halt? If g_grind_halted set, OnTick returns before OnTickEngine. Flag remains. On reinit, OnInit clears (if implemented). If halt occurs, flag survives in memory. If halt is later cleared? g_grind_halted is set false in OnInit only. So no trading while halted. But if halt is manual? Not. Quarantine: OnTick if quarantined, returns after RetryMissingExits; does not clear due flags. If quarantine releases, flag may still be set. But during quarantine, no new entries. When quarantine releases, OnTickEngine runs, ServiceDueAdds first. The flag could fire an add against a ladder that changed during quarantine. Could a fill occur during quarantine? Quarantine only stops new entries, but existing exits still managed. Existing pending entry orders? If quarantined, are entry orders cancelled? No, quarantine doesn't cancel entries. So an existing add could fill during quarantine. The fill handler runs in OnTradeTransaction regardless of quarantine? Grind_OnTradeTransactionEngine checks g_grind_halted, but not g_grind_quarantined. So if quarantined but not halted, ENT fill will set due flag. Then OnTick sees quarantined, calls RetryMissingExits, returns. Flag remains. During quarantine, more fills could occur. When quarantine releases (invariant ok), next OnTickEngine services due adds. At that point, the ladder may have changed (layers filled, removed). The flag is just boolean, not tied to layer index. It will compute next add from current deepest layer, which may be a different layer than the one that filled. That could place an add that is not implied by the fill. Also manual close: if user manually closes a layer, OnTradeTransaction OUT? If manual close, deal entry OUT? Grind_HandleSideDealFill only handles OUT_BY or EXT IN. For a manual close of an entry position, entry_type would be DEAL_ENTRY_OUT, role? The deal comment may be empty or manual. It won't match role ENT or EXT IN? It may parse comment? If manual close, comment may not have slot/role, so returns. So layer remains in tracker? Actually manual close would cause invariant fail? Invariants every tick would detect missing position? The recon from broker tickets would rebuild without that position, but g_grind_long layers still has it? Wait invariants run Grind_CheckBookInvariants which rebuilds from broker tickets and compares to internal? Actually Grind_CheckBookInvariants creates long_tmp and short_tmp from broker tickets, runs invariants on that book, but does not compare to g_grind_long/g_grind_short. It just checks the broker book is internally consistent. So a manual close could leave internal tracker stale until reinit? But that's outside. For flag, if a manual close happens after a fill set flag, the flag remains. Then next tick service could place an add based on stale layer. But the ADR's scope is fills. More direct: recon failure. If Grind_ReconstructState fails during OnInit or test, it sets g_grind_halted and cancels entries. The due flags are globals; if not cleared in OnInit before reconstruct, a stale flag could fire after reconstruct? But if recon fails, halted, OnTick returns. If recon succeeds, OnInit clears? R2 says must clear in OnInit. The ADR includes R2 as risk, not decision. D5 doesn't mention reinit clearing. The threat asks find one where flag survives and later fires add against ladder no longer implies it. Quarantine is one: fill during quarantine sets flag; quarantine releases; ladder may have changed due to exits causing layer removal (scalps). Actually during quarantine, exits still run via RetryMissingExits. If an exit fills, Grind_HandleSideDealFill OUT_BY removes layer. That could remove the layer that caused the due flag (if the entry layer's exit filled). Then due flag still set. When quarantine releases, ServiceDueAdds computes next add from remaining deepest layer. If the side becomes empty, Grind_ComputeAddTarget returns 0 (depth_idx <0), so no add. But if there are other layers, it may place an add based on a different layer. That add is not the next add after the fill that set the flag. This is a flag leak. Also cap reached: if flag set, then before service, cap becomes reached? But cap is based on depth. If depth increases due to another fill, the flag may be for an older fill. D5 clears at cap. But if cap is reached, EnsureAddNext returns early without clearing? D5 says clears once refused by cap/label validation. But implementation must explicitly clear on cap. The ADR says "Grind_EnsureAddNext already returns early at cap; the flag is cleared by D5 without a send." That implies ServiceDueAdds must clear flag even if EnsureAddNext returns early. But EnsureAddNext doesn't know about due flag. So new code must handle. If not, flag stuck. But the threat is about ADR as written: D5 says clears once serviced, placed, re-priced or refused by cap/label validation. It doesn't specify halt/quarantine/reinit. R2 mentions reinit. There is no clearing on halt/quarantine. So T-3 EXPLOIT-FOUND: flag survives quarantine/halt (until reinit) and can fire against changed ladder. Severity fixable-within-design? Add clearing in OnTick when halted/quarantined, and in OnInit. But as written, missing. The threat asks find one. We can use quarantine: fill during quarantine sets flag, quarantine release after exit removes layer, flag fires add based on different anchor. Or even simpler: two fills on same side in one burst. D5 says one add per side. But if first fill sets flag, second fill sets same flag. ServiceDueAdds later places one add. That's intended. But if between fills, the first fill's exit fills and layer removed? In a burst, unlikely. Another: manual close not clearing flag. But manual close isn't handled by engine. The flag could later fire an add for a ladder that no longer has that layer. However if manual close leaves internal tracker, that's bigger issue. Quarantine is better.

T-4: Ordering. Servicing due adds before L0 and before recentring. Find state where this starves a flat opposite side, or add placed before recentring produces worse book. Suppose long has depth>0 and due add flag. Short is flat with a resting L0 that is stranded? Actually recentring applies when one side has depth>0 and opposite side flat with l0_pending_ticket !=0. In OnTickEngine, L0 placement for flat side happens before recentring. But due adds are serviced FIRST, before L0 placement. So if long has due add, ServiceDueAdds places long add, consuming guard units. Then L0 placement for short flat side may be refused because guard near limit. So the due add (far from mid) starves the opposite side's initial L0 (near mid). The ADR claims D6 scope adds only, L0 unchanged, but ordering changes priority. The original order: L0 first, then recenter, then EnsureAddNext. New order: due adds first, then L0. This inverts priority between far adds and L0. In a near-limit account, a due add can consume the last 2 units, preventing L0 on the opposite side. That's exactly the purgatory defect (far adds starving footholds) but introduced by ADR-152a. The guard has no reserved band (that's ADR-152 A2). So T-4 EXPLOIT-FOUND: due add before L0 can starve flat opposite side. Example: limit 200, used=190, resting_ent=0. Guard allows if used+resting_ent <= 198? Wait formula: used + resting_ent <= limit - (2 + GRIND_SLOT_MARGIN). Need know GRIND_SLOT_MARGIN from grind_config.mqh. Not provided. In memo, limit 200, ceiling 194. So needed = 6? 2 + GRIND_SLOT_MARGIN = 6 -> margin=4. So allowed if used+resting_ent <= 194. Suppose used=192, resting_ent=0. Due long add places: costs? When placing an entry order, used = positions + orders. Before place, used=192, resting_ent=0. Check: 192 <= 194 yes. Place long add: orders +1, used=193, resting_ent=1? Resting_ent counts every fleet pending order whose role not EXT. So after place, used=193, resting_ent=1. Now L0 short: check used+resting_ent = 194 <= 194 yes, so it can still place. If used=193 initially: due long add check 193 <= 194 yes, place -> used=194, resting_ent=1. L0 short check 194+1=195 > 194, refused. So L0 starved. If due add placed first, it uses the last slot. If L0 had been placed first, it would use it, and due add would be refused. Which is better? L0 is the initial entry for a flat side; due add is for a side already having exposure. The ADR prioritizes the add. That is priority inversion against L0, not just exits. The ADR R1 only mentions exits. So T-4 exploit. Also recentring: due add before recentring could modify? Due add places new order; recentring would modify opposite L0. If due add consumes guard, recentring is a modify (not new entry) so guard not checked? Recentring uses Grind_ModifyPendingPrice, no slot guard. So not affected. But L0 placement is affected. So severity fatal-to-premise? It reintroduces starvation the memo was trying to fix. At least fixable-within-design by moving due adds after L0? But D2 explicitly says before L0. So as written, unsafe.

T-5: Interaction with G4 re-pricing: a due flag when an add is already pending and outside deadband. Does D5 collapse modify path, double-send, or leave flag stuck? Scenario: side has an add_pending_ticket resting. An ENT deal fills? Wait if add_pending_ticket exists and it fills, the ENT branch clears add_pending_ticket. So after fill, add_pending_ticket=0. But what if a fill occurs on L0 while an add is already pending? L0 fill sets due flag. add_pending_ticket remains? In ENT branch, if order_ticket == side.l0_pending_ticket, clears l0_pending_ticket. It does not clear add_pending_ticket. So an add may already be pending. After L0 fill, due flag set. Next tick ServiceDueAdds calls something like EnsureAddNext. Inside EnsureAddNext, side.add_pending_ticket !=0, so it checks label. The required_index = next index after deepest layer. The pending add may have been placed for the old next index. After L0 fill, the deepest layer may still be the same? Actually L0 fill appends layer 0. Before fill, there was no layer 0? Wait L0 is the first layer. If L0 fills, side depth goes from 0 to 1. But add_pending_ticket could exist only if depth>0 before? EnsureAddNext is called if depth>0 or add_pending_ticket !=0. If depth was 0, add_pending_ticket should be 0. So L0 fill happens when depth was 0, so no add_pending_ticket. So not that.

What about an add fill while another add is pending? The ENT branch clears the matching pending ticket if order_ticket == side.add_pending_ticket. So if the filled order is the pending add, it clears it. Then due flag set. ServiceDueAdds sees add_pending_ticket=0, places new add. So no double.

But what if the fill is for some other order? The side can only have one add_pending_ticket. So if an add fills, it is that ticket. So cleared.

Could due flag be set when add_pending_ticket is already nonzero from a different order? Suppose a fill on L0? As above, if L0 fills, side depth goes from 0 to 1. add_pending_ticket must be 0. Suppose a fill on an exit? ENT branch only for role ENT. So no.

What about multiple fills in a burst: first fill clears add_pending_ticket, sets flag. Before service, second fill? But service happens on next OnTick, after all OnTradeTransaction events in burst. The second fill could be another ENT for the same side. If the second fill is for an order that was not tracked? Actually after first fill, the engine state has cleared add_pending_ticket and appended layer. The second deal in same burst might be for another pending order that was already in the book. But if the first fill was the add, there is no other pending add. Could be L0? If side had L0 and add pending, and both fill in same burst? Then first fill might clear L0, append layer, set flag. Second fill (add) then clears add_pending_ticket, appends another layer, sets flag again. ServiceDueAdds later sees depth increased by 2, places one add. That's intended by D5 (one add per side). No double.

But T-5 specifically: "a due flag when an add is already pending and outside the deadband." How can an add be already pending when due flag set? Consider: an ENT fill for the opposite side? No, due flag is per side. It is set only when that side has an ENT fill. If an add is already pending on that side, the only way is if the fill is for L0 while an add is pending. But as argued, add pending requires depth>0, so L0 cannot be pending. Wait, could side have depth>0 and l0_pending_ticket !=0? L0 pending is placed only when depth==0. Once depth>0, l0_pending_ticket should be 0. But there is a stray L0 cancel on fill. So normally no. Could the fill be for a position that is not the add_pending_ticket? For example, an add order was placed, then before it fills, another add is placed? There is only one add_pending_ticket per side. So no.

What if the due flag is set from a fill on the opposite side? No, per-side flag set for filled side only.

So T-5 might be NO-EXPLOIT for double-send. But there is a subtlety: D5 says flag cleared once placed, re-priced, or refused by cap/label validation. If ServiceDueAdds calls EnsureAddNext, and EnsureAddNext finds an existing add_pending_ticket outside deadband, it will call ModifyPendingPrice and return. That is a re-price. D5 says flag cleared once re-priced. So flag cleared. No double-send. But if the modify fails (e.g., Grind_ModifyPendingPrice returns false), EnsureAddNext ignores return value. The flag is cleared anyway? D5 says cleared once re-priced, but if modify fails, it wasn't re-priced. Then flag would be stuck or cleared? The ADR doesn't specify handling modify failure. If cleared, the modify is lost until next tick's ordinary EnsureAddNext (which will retry because add_pending_ticket still exists and outside deadband). So no harm. If not cleared, flag stuck, but ensureAddNext would retry anyway. So T-5 likely NO-EXPLOIT for double-send, but there is a design ambiguity: D5 says clears once re-priced, but does not say clears if modify fails. Could leave flag stuck. But since ordinary EnsureAddNext would handle it, not fatal. However, if ServiceDueAdds bypasses g_grind_ent_sent_this_tick and calls EnsureAddNext, and EnsureAddNext sees add_pending_ticket !=0 and outside deadband, it calls ModifyPendingPrice. That modify does not check g_grind_ent_sent_this_tick. So it can send even if global flag set. That's fine. But does it consume the due flag? If it clears, then the due add is considered serviced. The add is re-priced, which is the correct action. So no double. I'd say NO-EXPLOIT but note ambiguity. However threat asks "Does D5 collapse the modify path, double-send, or leave the flag stuck?" We can answer: It doesn't double-send because EnsureAddNext's existing add_pending_ticket branch returns after modify. It could leave flag stuck if modify fails and D5 clears only on success. But that's fixable. Severity cosmetic/fixable.

T-6: Guard refusal loop. Due add refused every tick while guard saturated. Bound retry cost in OrderSend attempts and lock acquisitions per minute. If guard refuses, D3 says stays due and retries next tick. On each OnTick, ServiceDueAdds runs, calls EnsureAddNext, which acquires lock, checks guard, releases, returns. No OrderSend. So OrderSend attempts = 0. Lock acquisitions = 1 per side per tick. At 16 instances, if all have due flags on both sides, 32 lock acquisitions per tick. Ticks can be many per second. But guard saturation means account near 194. The lock is a global variable. Each acquisition does GlobalVariableSetOnCondition, then if fails, reads GlobalVariableGet, checks stale, Sleep(1). If many instances contend, each may Sleep(1) up to GRIND_SLOT_LOCK_MAX_RETRIES. Need know max retries. Not in pasted. But typical maybe 50? Not available. The lock hold time is short: AccountInfoInteger, PositionsTotal, OrdersTotal, scan OrdersTotal for resting_ent. That's relatively expensive (OrdersTotal up to 200). 32 instances * scan 200 orders = 6400 order scans per tick per instance? Actually each lock acquisition scans all orders. If 32 acquisitions per tick, that's 32 * 200 = 6400 OrderSelect calls per tick. At 10 ticks/sec, 64k calls/sec. This could be heavy. But the original code already did this for L0 and EnsureAddNext. The new due adds add up to 2 extra lock acquisitions per instance per tick when guard refuses. Actually ordinary EnsureAddNext also runs at end of OnTickEngine, but only if depth>0 or add_pending_ticket !=0. If due add is refused, it stays due. At end, ordinary EnsureAddNext also runs for that side (since depth>0). It will also try to place? Wait if due add refused, EnsureAddNext at end will also run and likely also refuse. So the side gets two guard checks per tick: one in ServiceDueAdds, one in ordinary EnsureAddNext. That doubles lock acquisitions for that side. If both sides due, up to 4 per instance per tick. That's a contention risk. Also, D3 says due add bypasses g_grind_ent_sent_this_tick once per side. But if refused, does it set g_grind_ent_sent_this_tick? No, because no send. Then ordinary EnsureAddNext at end will also attempt, using the same global flag? It will check g_grind_ent_sent_this_tick. If false, it may acquire lock and check guard again. So indeed two attempts per side per tick. Over 16 instances, 128 lock acquisitions per tick worst case. This is a contention risk. Bound: If ticks arrive every 100ms, 10 ticks/sec -> 1280 lock acquisitions/sec. Each may Sleep(1). Could cause delays. The original budget reduced this. So T-6 EXPLOIT-FOUND or DESIGN-UNSAFE. But is it fatal? The lock is designed to handle contention with stale steal. But at 16 instances, it's a global bottleneck. The ADR doesn't bound retries other than existing max. The retry cost in OrderSend attempts is zero, but lock contention is real. Severity fixable-within-design (e.g., don't retry due add if guard already refused in same tick, or clear due flag on guard refusal? But D3 says stays due. Could mark as attempted this tick to avoid duplicate). However as written, it doubles lock pressure. I'd say EXPLOIT-FOUND for contention.

T-7: Transaction-path safety. Setting flag inside Grind_HandleSideDealFill interact with Grind_DealWasProcessed / MarkDealProcessed, CloseBy queue, OUT_BY branch. Need examine.

Grind_HandleSideDealFill is called for both sides for each DEAL_ADD transaction. It checks Grind_DealWasProcessed at top. If processed, returns. Then selects deal, checks symbol, magic, comment, etc. For ENT branch, it calls Grind_MarkDealProcessed(deal_ticket) BEFORE processing. Then appends layer, ExitQManageSide, returns. If we set flag after ExitQManageSide, it happens after MarkDealProcessed. So the flag is set exactly once per deal because the second side call will see DealWasProcessed and return. Good. However, consider OUT_BY branch: It only marks processed if it finds the matching layer with exit_position_ticket !=0. If not, it returns without marking processed. Then the other side call will also process. That's existing. But if we set flag only in ENT branch, OUT_BY doesn't set flag. Fine.

Multi-deal burst: OnTradeTransactionEngine calls Grind_HandleSideDealFill for long then short for each transaction. For a single ENT deal, the first side (say long) processes, marks processed, sets flag. The second side (short) sees processed and returns. So one flag. For two ENT deals on same side in same burst (e.g., two add orders fill), each transaction will be processed separately. First deal: long processes, marks, sets flag. Second deal: long processes, marks, sets flag again (same boolean). Net one flag. That's idempotent. But what if the two deals are for different sides? First deal long sets long flag. Second deal short sets short flag. Both flags set. ServiceDueAdds later places one add per side. That's intended (one per side). But could a single deal be processed twice? The processed check prevents. Could a deal be marked processed before flag set? Yes, MarkDealProcessed is before AppendLayer and before flag set. If an exception? MQL no exceptions. If AppendLayer fails? ArrayResize could fail? Not checked. If flag set after ExitQManageSide, and ExitQManageSide triggers something? It doesn't send orders? It can place exits and cancel exits. It does call Grind_PlaceLimit for exits. That's an OrderSend inside the fill handler! Wait, G1 says "calls Grind_ExitQManageSide (exit placed in the same event)". So there IS an OrderSend from OnTradeTransaction already, for exits. D4 says "No OrderSend from OnTradeTransaction" but the existing code already places exits in the fill handler. D4 refers to the new add order. It says "No OrderSend from OnTradeTransaction (lock sleeps; re-entrancy)." But exits are placed there. So D4 is only about the new add. However, this means the fill handler already does I/O. The ADR's claim "The fill path gains one boolean assignment and no I/O" is false if you consider the existing exit placement? It means the new change adds no I/O. But the fill path already has I/O. That's a premise issue: the fill path is already doing OrderSend for exits, so the re-entrancy and lock concerns already exist? Wait exits use Grind_PlaceLimit which uses Grind_OrderSendCounted, not Grind_SlotLockAcquire? Grind_ExitQManageSide does not acquire the slot lock; it checks Grind_SlotExitAllowed without lock. So it sends orders without the lock. So the concern about lock sleeps is specific to entry placement. But re-entrancy: placing an exit in the fill handler generates further transactions. That already happens. So D4's re-entrancy concern is not unique to adds. However, the ADR wants to avoid adding entry sends in the fill handler. That's a design choice. For T-7, setting a flag is safe with respect to DealWasProcessed because it's after the mark. But there is a subtle issue: the ENT branch returns after setting flag. If the same deal is processed for the other side, it returns due to DealWasProcessed. So flag is not duplicated. But what about the CloseBy queue? The OUT_BY branch does not set flag. If an exit fills via OUT_BY (CloseBy), the layer is removed. No add flag is set. That's correct because a scalp close doesn't schedule a new add? Wait, if a layer's exit fills, the layer is removed. The side depth decreases. Should the next add be re-evaluated? The next add target is based on deepest layer. If the deepest layer is removed, the next add target changes. Today, Grind_EnsureAddNext runs every tick and will recompute. But if an exit fills and removes a layer, should we place an add? The next add may no longer be needed (if depth decreased, the ladder should be shorter). Actually the ladder is a sequence of layers. If a layer is removed, the next add target is based on the new deepest layer. The existing add_pending_ticket may now be stale. Grind_EnsureAddNext will detect label mismatch and cancel. But no due flag is set. So no immediate action. That's existing behavior. The ADR only schedules adds after ENT fills, not after exits. That's correct scope (adds only). But could an exit fill and an ENT fill happen in the same burst? The flag from ENT remains. Then service places add based on new depth. That's fine.

Potential T-7 issue: The flag is a global boolean. If the fill handler is called for a deal that is later determined to be a duplicate (e.g., DealWasProcessed returns false initially, then MarkDealProcessed, then flag set). If the deal is somehow processed again in the same transaction? No.

Another: The OUT_BY branch can remove a layer and then call ExitQManageSide. If a due flag was already set from a previous ENT fill on the same side, and then an OUT_BY (scalp close) happens before service, the flag remains. When service runs, it may place an add based on the reduced ladder. That's T-3.

Could setting the flag inside the handler lose it if Grind_HandleSideDealFill returns early after MarkDealProcessed? In ENT branch, it marks processed, then does work, then sets flag, then returns. If ExitQManageSide somehow causes an early return? No, it's void. If ValidateAddLabelIndex? Not called here. So no.

What about the CloseBy queue: In EXT ENT? The EXT branch for entry_type == DEAL_ENTRY_IN (exit order filled) does not set flag. It queues CloseBy. That's correct: exit fill should not schedule an add. But wait, when an exit order fills, it becomes a position. The layer's exit_position_ticket is set. Then later CloseBy is processed to close the original position. If CloseBy succeeds, an OUT_BY deal occurs, which removes the layer. No add flag. Good.

So T-7 likely NO-EXPLOIT for flag duplication/loss. But there is a design issue: setting flag after ExitQManageSide means if ExitQManageSide places an exit order, that OrderSend generates a new transaction. Could that transaction be processed before the flag is set? The flag is set in the same handler after ExitQManageSide returns. The new transaction is queued and processed later, not re-entrantly. So flag will be set before the next transaction is processed. No race.

However, D4 says no OrderSend from OnTradeTransaction. But ExitQManageSide already does OrderSend. If the motivation for D4 is to avoid lock sleeps, then exit placement doesn't use the lock, so it's okay. If the motivation is re-entrancy, exit placement already does it. So the ADR's D4 is not a new constraint but an existing reality. This might weaken the premise: the fill path is already unsafe by that definition, so adding a flag doesn't change it. But the ADR wants to avoid adding entry sends because entry sends require the lock. So D4 is specifically about not calling Grind_EnsureAddNext in the handler. That's fine.

T-8: Premise. Is latency real enough to justify touching fill path? Attack with source: ticks arrive in milliseconds, adds already rest. Quantify what D1-D3 actually buys before ADR-152 ships. Today, after an ENT fill, the next add is placed by EnsureAddNext at end of next OnTick. But note: in today's code, an add is usually already resting. When an add fills, the next add is not placed. So after a fill, there is a gap in the ladder until next tick. The latency is at most one tick, plus if g_grind_ent_sent_this_tick was consumed by another path. In a liquid symbol, ticks are milliseconds. The ADR's own repro requires price to drop 5 pips (from 1.1000 to 1.0985) between fill and EnsureAddNext. For a 5-pip move to happen in one tick, it must be a fast market/gap. In such a market, even if placed same tick, the clamp might still miss? Actually if placed same event, the order would rest at 1.0990. If price gaps down through 1.0990, the limit order would fill at 1.0990 (or better) when price crosses. If placed late, it rests lower. So the ADR does help in fast moves. But how often? Before ADR-152, adds are not held; they are resting. So the next add is already in the book. Wait, the ADR context says: "Today this is usually invisible: a resting add is already in the book, so the delay only affects the NEXT add after a fill." So when an add fills, it is removed from the book. The next add is not yet placed. So there is a moment where the next add is missing. The delay is one tick. If the price moves in that tick, the next add misses. But the next add is at add_pips away (e.g., 10 pips). For it to fill in that same tick, price would have to move 10 pips in one tick. That's a gap. So the latency matters only for gap events. The memo's T-4 already says gaps forfeit fills by construction under purgatory. The ADR-152a is a prerequisite for purgatory, where adds are held. Under purgatory, after a fill, the next add is not resting (it's held). So the delay is the entire time until the next tick. That's the same one tick. But if price moves during that tick, the held add misses. The ADR-152a only ensures the place attempt happens first in the next tick, not that it happens at fill time. So it doesn't solve the gap issue. It only reduces the chance that another entry consumes the tick allowance. How often does that happen? g_grind_ent_sent_this_tick is one per instance per tick. There are 16 instances, so per instance, the chance that another path consumes the tick is low unless the instance has both sides active. But if both sides are active, L0 placement only happens when flat. So competing paths are L0 on flat opposite side, recentring (modify, not entry), and EnsureAddNext on the other side. So the tick allowance is rarely consumed by another entry except when the opposite side is flat and placing L0. In a ladder, both sides are often active. So the bypass matters only when the opposite side is flat and needs L0. In that case, the due add would be serviced first, potentially starving L0 (T-4). So the ADR's benefit is marginal and introduces priority inversion. Before ADR-152, since adds are already resting, the next add after a fill is the only one missing for a tick. The ADR-152a doesn't make it same-tick; it just reorders within the next tick. So the latency reduction is at most one tick path ordering, not elimination. The premise of "same-tick" is false. The ADR should be folded into ADR-152 because ADR-152 is the one that makes the latency matter (by holding adds). Before ADR-152, the latency is invisible. The ADR-152a alone, before ADR-152, changes little and adds risk. So premise verdict: not worth as its own ADR; fold into ADR-152. D1-D6 are not mechanically safe as written due to T-1, T-3, T-4, T-6. But fixable within design? T-1 fix requires violating D4 (place in transaction handler) or accepting next-tick latency. If the premise is to eliminate the latency, it fails. So fatal-to-premise? The threat asks: does any finding kill the ADR-152a premise, or are all findings fixable within it? T-1 kills the stated premise of same-tick equivalence. The ADR claims it fixes the latency, but it only defers to next tick. Unless the definition of "same-tick" means "next tick before other entries", which it does say in D2: "services due adds FIRST". The ADR's own decision D1-D3 says "The next add is placed on the first tick after the fill, ahead of every other entry". So the ADR does not claim same-tick as in the same event; it claims first tick after fill. The threat T-1 asks "Is the flag genuinely equivalent to same-tick?" The ADR's context says "same-tick placement after a fill" in A3, but D1-D3 says first tick after fill. So there is a mismatch between the ADR title/premise and the decision. The ADR's decision is honest: first tick after fill. So T-1 might be a premise attack: if the original problem requires same-tick, then flag-plus-next-tick is insufficient. The ADR's own repro: "Before Grind_EnsureAddNext runs, price drops to 1.0985." With flag, EnsureAddNext runs at start of next tick, but price could still be 1.0985. So the repro still stands. Thus the ADR does not fix its own verified repro. That is fatal-to-premise. The ADR's repro assumes EnsureAddNext runs late in the same tick? Actually the repro says "Before Grind_EnsureAddNext runs, price drops to 1.0985." In today's code, EnsureAddNext runs at end of OnTickEngine. If the fill happens during OnTick? Wait, how does a pending order fill? It fills when price touches it. The fill event is processed in OnTradeTransaction, which may occur during OnTick? Actually MT5 processes price ticks: OnTick is called. During OnTick, the EA may call OrderSend. Pending orders are triggered by the broker, not by OnTick. The broker sends a trade transaction. OnTradeTransaction is called. This can happen at any time, independent of OnTick. So the fill handler runs when the transaction arrives. The next OnTick may be the next price tick. If price drops from 1.1000 to 1.0985, that is a price movement that would generate OnTick events. The fill at 1.1000 would have occurred when price hit 1.1000. Then price continues to 1.0985. There may be multiple ticks in between. OnTradeTransaction for the fill could arrive after some ticks. But the EA's OnTick may run on each tick. So by the time the next OnTickEngine runs after the fill, the price could be 1.0985. With the flag, ServiceDueAdds runs at the top of that OnTickEngine. It still sees price 1.0985. So it misses. To catch the fill at 1.1000, the add would have to be placed immediately when the fill occurs, before price moves. That requires OrderSend in OnTradeTransaction. D4 forbids. So the ADR does not fix the repro. That is fatal. The only benefit is if the next OnTickEngine would have been delayed by other work or if g_grind_ent_sent_this_tick was consumed. But the repro's price drop is the main issue. So T-1 EXPLOIT-FOUND, fatal-to-premise.

Now T-8: quantify. Before ADR-152, adds rest. The delay only affects the next add after a fill. In steady state, each fill removes an add and schedules the next. The next add is placed on the next tick. In a fast market, the next add may miss a gap. But the ADR-152a doesn't solve that; it just places it earlier in the next tick. So the benefit is only when the next tick's allowance would have been consumed by another entry. How often? g_grind_ent_sent_this_tick is set by L0 or add. In a ladder with both sides having depth, L0 is not placed. Recentring is modify. So the only competitor is the other side's EnsureAddNext. But both sides' EnsureAddNext run at the end of OnTickEngine. The order is long then short. If both sides need to place an add, the first one (long) places and sets g_grind_ent_sent_this_tick, causing the second (short) to skip. That's a real case: both sides have a fill in the same tick, or one side has a fill and the other side also needs to place an add (e.g., after a recentring or cap transition). The ADR-152a services due adds first, so both sides' due adds can place (bypass once per side). That fixes the starvation of one side by the other. But is that the purgatory case? Under purgatory, far adds are held, so after a fill, the next add must be placed. If both sides fill, both need to place. Today, one would be delayed to next tick. The ADR-152a allows both in one tick. That is a real benefit. But it also allows two entry sends per instance per tick, increasing API load. The ADR claims zero additional sends in steady state, but in a tick where both sides have due flags, it sends two instead of one (the second would have been sent next tick). Over time, it's the same total sends, just moved earlier. So request rate per tick can double, but daily total same. The latency benefit is real for the second side. But is it worth the risk? The ADR's own R1 says exits keep priority. But T-4 shows L0 starvation. The ADR could be folded into ADR-152 because the holding logic is what creates the need for immediate placement. Before ADR-152, the next add is already resting? Wait, no: after an add fills, the next add is not resting; it's placed on next tick. So even today, there is a missing add for one tick. But because adds are not held, the missing add is just the next layer. The price would have to move to that next layer's price in one tick. That's a gap. The ADR-152a doesn't eliminate the gap; it just places earlier in the next tick. So its value is marginal. Therefore, fold into ADR-152.

Now we need produce output.

Let's structure:

GIVENS CHECK:
G1 CONFIRMED. File: grind_engine.mqh, Grind_HandleSideDealFill, ENT branch: after AppendLayer and ExitQManageSide, no EnsureAddNext. (Line ~1001-1020 per prompt; pasted source lacks line numbers but code matches.)
G2 CONFIRMED. grind_engine.mqh, Grind_OnTickEngine: EnsureAddNext only at end after L0/recenter/cap.
G3 CONFIRMED. grind_exitq.mqh: g_grind_ent_sent_this_tick defined; grind_engine.mqh OnTickEngine sets false before RetryMissingExits; TryPlaceL0 and EnsureAddNext set true on successful send.
G4 CONFIRMED. grind_engine.mqh, Grind_EnsureAddNext: if add_pending_ticket exists and label ok, computes clamped target; if outside deadband calls ModifyPendingPrice and returns.
G5 CONFIRMED. grind_pure.mqh: Grind_Adr013ClampBuy returns min(theoretical, bid - min_dist); Grind_BuyLimitMarketable returns buy_limit < ask (passive).
G6 CONFIRMED. grind_exitq.mqh: Grind_SlotEntryAllowed formula; called inside lock in TryPlaceL0 and EnsureAddNext; Grind_SlotLockAcquire retries Sleep(1) and steals stale.
G7 CONFIRMED. grind_engine.mqh, OnTickEngine: Grind_RetryMissingExits before L0/recenter/EnsureAddNext.
G8 CONFIRMED. grind_recon.mqh: no invariant asserts resting entry order. Grind_ReconCheckPendingAddCorrupt only checks add_pending_ticket is an order ticket; I1-I8 don't check ENT order presence. Grind_ValidateAddLabelIndex exists in grind_engine.mqh.

Then T-1..T-8.

For each: VERDICT, LOAD-BEARING CLAIM, MINIMAL REPRO/MECHANISM, SEVERITY.

Need be precise with numbers.

T-1: EXPLOIT-FOUND. Load-bearing: grind_engine.mqh :: Grind_HandleSideDealFill (ENT branch) only sets flag; grind_engine.mqh :: Grind_OnTickEngine services at next tick; D4 forbids send in handler. Repro: Long add_pips=10. Pending buy limit at 1.0990 fills when ask touches 1.0990 (entry price 1.1000? Wait the add fills at 1.1000? The ADR repro says "An add fills at 1.1000, mid ~1.1000, next target 1.0990." Let's use that. So deal_price=1.1000. This is an add fill. The next add target = 1.1000 - 10 pips = 1.0990. OnTradeTransaction sets due long flag. Before next OnTick, price drops to 1.0985. Next OnTick: ServiceDueAdds computes target 1.0990, current bid ~1.0984? Actually if mid 1.0985, bid maybe 1.0984, ask 1.0986. Clamp buy: theoretical 1.0990 >= bid 1.0984, min_dist=stops_level*point (assume 1 point). out = min(1.0990, 1.0984 - 0.00001) = 1.09839. So rests at 1.09839, below bid. The market already traded through 1.0990 on the way down, but no order was there. Same-event placement at fill time would have placed 1.0990, which would have filled on the drop to 1.0985. So miss. Severity: fatal-to-premise (the ADR's own repro is not fixed).

T-2: EXPLOIT-FOUND. Load-bearing: grind_exitq.mqh: g_grind_ent_sent_this_tick; grind_engine.mqh: TryPlaceL0/EnsureAddNext set it; D3 bypass. Mechanism: At start of OnTickEngine, g_grind_ent_sent_this_tick=false. ServiceDueAdds places due long add (sets? D3 doesn't say it sets the global; if it does not, then later L0 can also place). Suppose both sides have due flags. ServiceDueAdds places long add and short add. It does not set g_grind_ent_sent_this_tick (per D3, it bypasses, not necessarily sets). Then L0 placement for a flat side? But if both sides have due flags, both sides have depth>0, so L0 not placed. So max 2 places. If only long has due flag, short is flat. ServiceDueAdds places long add. g_grind_ent_sent_this_tick remains false. Then L0 short places. That's 2 entry sends in one tick for one instance. Original budget allowed 1. Across 16 instances, if all have one due flag and one flat opposite, 32 sends in one tick vs 16. Also lock acquisitions: each send acquires lock. So request rate doubles. Severity: fixable-within-design (e.g., ServiceDueAdds sets g_grind_ent_sent_this_tick after placing, or due adds are counted against the budget per instance). But as written, D3 doesn't specify setting the global, so exploit.

T-3: EXPLOIT-FOUND. Load-bearing: D1 sets flag; D5 clears only on service/place/reprice/refusal; no clearing on halt/quarantine/reinit except R2 risk. Source: grind_engine.mqh OnTick returns early if g_grind_halted or g_grind_quarantined; Grind_HandleSideDealFill runs in OnTradeTransaction even if quarantined (only checks g_grind_halted). Mechanism: Instance quarantined due to I3_NAKED. A pending add order fills. OnTradeTransaction: g_grind_halted false, so Grind_HandleSideDealFill runs, appends layer, ExitQManageSide, sets due long flag. OnTick: g_grind_quarantined true, so returns after RetryMissingExits. Flag remains. During quarantine, the exit for that layer fills (OUT_BY), removing the layer. Quarantine releases. Next OnTickEngine: ServiceDueAdds sees due long flag, computes next add from remaining deepest layer (which may be an older layer, not the one that filled). It places an add that is not the next add after the fill. Or if no layers left, ComputeAddTarget returns 0 and flag must be cleared; if not, stuck. Severity: fixable-within-design (clear flags on quarantine/halt and in OnInit). But as written, unsafe.

T-4: EXPLOIT-FOUND. Load-bearing: D2 services due adds before L0; grind_exitq.mqh Grind_SlotEntryAllowed uses used+resting_ent <= limit - (2+margin); grind_engine.mqh TryPlaceL0 and EnsureAddNext both check. Mechanism: limit=200, margin=4 (so ceiling 194). used=193, resting_ent=0. Long side depth>0 and has due add flag. Short side flat, mid near, L0 sell limit would be passive. OnTick: due long add serviced first. Guard check: 193+0=193 <=194, allowed. Place long add. Now used=194, resting_ent=1. Then L0 short: guard check 194+1=195 >194, refused. Short side starves. If L0 short had been placed first, it would use the slot, and due long add would be refused (or delayed). The ADR prioritizes a far add over a near L0, reintroducing the purgatory defect. Severity: fatal-to-premise (it creates the same starvation ADR-152 is meant to fix).

T-5: NO-EXPLOIT (or DESIGN-UNSAFE? Let's decide). Load-bearing: grind_engine.mqh Grind_EnsureAddNext existing add_pending_ticket branch handles modify and returns; D5 clears flag on re-price. Mechanism: If due flag set and add_pending_ticket already exists (e.g., from a previous tick) and outside deadband, ServiceDueAdds calls EnsureAddNext. It enters the add_pending_ticket branch, checks label, computes clamped target, sees outside deadband, calls ModifyPendingPrice, and returns. No new placement. D5 says flag clears on re-price. So no double-send. If modify fails, flag may be cleared or not; ordinary EnsureAddNext later will retry modify. No double-send. The only issue is if D5 clears the flag even when modify fails, but that's not harmful because ordinary path retries. So NO-EXPLOIT. Severity: cosmetic (clarify modify failure handling).

Wait: threat asks "a due flag when an add is already pending and outside the deadband." How can add already be pending when due flag set? As discussed, if the fill is for the add itself, add_pending_ticket is cleared. So the only way is if the due flag is from a previous fill and not yet serviced, and an add was placed by ordinary EnsureAddNext in a previous tick? But if due flag is set, ServiceDueAdds runs first every tick, so it would have been serviced. Unless guard refused, but then no add_pending_ticket. So this scenario is unlikely. But if it occurs (e.g., due flag set on L0 fill? But L0 fill means depth was 0, so no add pending). Actually, consider a fill on an add while another add is pending? Not possible because only one add pending. So T-5 might be impossible in practice. But the threat asks to analyze. I'd say NO-EXPLOIT, but note the precondition is nearly unreachable. Severity: cosmetic.

T-6: EXPLOIT-FOUND. Load-bearing: D3 stays due on guard refusal; grind_exitq.mqh Grind_SlotLockAcquire with Sleep(1) and GRIND_SLOT_LOCK_MAX_RETRIES; grind_engine.mqh OnTickEngine also calls EnsureAddNext at end if depth>0 or add_pending_ticket !=0. Mechanism: Guard saturated at 194. 16 instances, both sides have depth>0 and due flags. Each OnTickEngine: ServiceDueAdds tries long and short: 2 lock acquisitions, both fail guard, release, no OrderSend. Then at end, ordinary EnsureAddNext for long and short: 2 more lock acquisitions, both fail guard. Total 4 lock acquisitions per instance per tick. 16 instances = 64 lock acquisitions per tick. If ticks every 100ms, 640/sec. Each failed acquisition may Sleep(1) and scan OrdersTotal up to 200. Contention: global lock, 64 contenders. Sleep up to max retries. OrderSend attempts = 0, but lock contention and CPU cost. Original code would have 2 lock acquisitions per instance per tick (EnsureAddNext only). So ADR doubles lock pressure. Severity: fixable-within-design (e.g., if due add was refused by guard, clear a per-tick attempted flag to avoid duplicate EnsureAddNext, or service due adds only if guard might allow). But as written, contention risk.

T-7: NO-EXPLOIT. Load-bearing: grind_engine.mqh Grind_HandleSideDealFill: Grind_MarkDealProcessed called before ENT branch work; D1 sets flag after ExitQManageSide. Mechanism: Multi-deal burst: two ENT deals on same side. First deal: long handler processes, MarkDealProcessed, AppendLayer, ExitQManageSide, sets due long flag. Second deal: long handler sees DealWasProcessed true, returns. Short handler for both deals also sees processed. So flag set once. If two deals on opposite sides, each side's flag set once. No loss or duplication. CloseBy/OUT_BY branch does not set flag, so no interference. Severity: cosmetic (flag set after MarkDealProcessed is safe; note D4's no-I/O claim ignores existing exit OrderSend in ExitQManageSide).

T-8: EXPLOIT-FOUND (premise weak). Load-bearing: grind_engine.mqh Grind_HandleSideDealFill, Grind_OnTickEngine; ADR context. Mechanism: Before ADR-152, adds are not held. After an ENT fill, the next add is placed by EnsureAddNext at end of next OnTickEngine. The latency is at most one tick plus possible tick-allowance consumption. Ticks are milliseconds. The ADR's own repro requires a 5-pip move between fill and next OnTickEngine. At 10 pips add distance, a 5-pip move is half an add step. In normal markets, this is a fast move. The ADR-152a does not place the add at fill time; it places it at the start of the next tick. So it only helps if the next tick's allowance would have been consumed by another entry. That is a narrow case: both sides need to place an add in the same tick, or the opposite side is flat and needs L0. In those cases, the ADR-152a can starve L0 (T-4) or double the per-tick sends (T-2). Quantify: 16 instances * 2 sides = 32 due adds max per tick. Original budget: 16 entry sends per tick (one per instance). So request rate can double in bursts. Daily total unchanged. Benefit: avoids one-tick delay for the second side in a same-tick double-fill. That is marginal before ADR-152 because the add would have been placed next tick anyway, and the next add is the only missing one. Under ADR-152, the held-add case makes the delay land on the next add, but the delay is still one tick. The ADR-152a still doesn't solve the gap miss. Therefore, fold into ADR-152. Severity: fatal-to-premise (the ADR should not ship separately; it adds fill-path risk for marginal benefit and does not solve its own repro).

PREMISE VERDICT: Not worth as its own ADR. D1-D6 not mechanically safe as written. T-1 kills the same-tick claim; T-4 reintroduces starvation; T-3 flag leak; T-6 lock contention. Fixable within design? T-1 is fundamental: to fix, must send in OnTradeTransaction (violates D4) or accept next-tick latency. If accept next-tick, the ADR's premise is false. So the premise as "fill-driven same-tick placement" is killed. The ADR should be folded into ADR-152 where the holding logic can be designed together with priority and guard reservation. OVERRIDE CHECK: T-1 and T-4 kill the ADR-152a premise as written; findings are not all fixable without changing D2/D3/D4. So the ADR should not ship as its own ADR in current form.

Need be careful with line numbers. Since pasted code lacks line numbers, I'll cite file:function and approximate line from prompt. The prompt gave some line numbers: G1 ~921, ~1001, G2 ~1150-1154, G3 grind_exitq.mqh:19, grind_engine.mqh:1119, ~543, ~804, G4 ~776, G5 grind_pure.mqh:149-161, :137, G6 grind_exitq.mqh ~161, G7 ~1120. I can use those as "per prompt/source". But the instruction says "from the actual source you opened". I opened the pasted code. It doesn't have line numbers. I can say "CONFIRMED (function-level; line numbers from prompt GIVENS match source structure)". That should be acceptable.

Need ensure no implementation code. We are writing analysis, no code.

Let's draft.

We need GIVENS CHECK first. Print CONFIRMED or REFUTED with file:line. I'll use "grind_engine.mqh: Grind_HandleSideDealFill (ENT branch, ~1001-1020 per source baseline)" etc.

Then threats.

Let's write in required format.

For T-1, include concrete numbers.

For T-2, include numbers.

For T-3, include quarantine scenario.

For T-4, include guard arithmetic. Need GRIND_SLOT_MARGIN. From memo, limit 200, ceiling 194, needed = 6, so margin=4. But grind_config.mqh not available. I'll state "assuming GRIND_SLOT_MARGIN=4 as in memo (limit 200 ceiling 194); if different, arithmetic shifts." That's fair.

For T-5, NO-EXPLOIT.

For T-6, include lock acquisitions.

For T-7, NO-EXPLOIT.

For T-8, premise.

Let's ensure we don't accidentally claim things not in source. We have enough.

Now write final. Desired oververbosity 5: concise but complete. The required output format is detailed. I'll follow.

Let's produce.

## Final Report

## GIVENS CHECK

**G1. CONFIRMED.** `grind_engine.mqh : Grind_HandleSideDealFill` — ENT branch clears matching pending ticket, calls `Grind_AppendLayer`, then `Grind_ExitQManageSide`, then returns. No `Grind_EnsureAddNext` call in the handler. (Source matches prompt line ~1001-1020; pasted artifact lacks absolute line numbers but function body is unambiguous.)

**G2. CONFIRMED.** `grind_engine.mqh : Grind_OnTickEngine` — `Grind_EnsureAddNext` appears only at the end, after L0 placement, opposite-L0 recentring, and cap transitions. No other call sites in the pasted engine.

**G3. CONFIRMED.** `grind_exitq.mqh : bool g_grind_ent_sent_this_tick = false;` — cleared at top of `Grind_OnTickEngine` (`grind_engine.mqh`, before `Grind_RetryMissingExits`). Set true after successful send in both `Grind_TryPlaceL0` and `Grind_EnsureAddNext`. Shared global across both sides and both entry paths.

**G4. CONFIRMED.** `grind_engine.mqh : Grind_EnsureAddNext` — when `side.add_pending_ticket != 0` and label is OK, it computes the clamped target; if `Grind_PriceWithinDeadband` is false, it calls `Grind_ModifyPendingPrice` and returns. Not place-once.

**G5. CONFIRMED.** `grind_pure.mqh : Grind_Adr013ClampBuy` returns `min(theoretical, bid - min_dist)` when `theoretical >= bid`; `Grind_BuyLimitMarketable` returns `buy_limit < ask`, i.e. TRUE when the buy limit is passive. A clamped add rests below the bid, never crossing the spread.

**G6. CONFIRMED.** `grind_exitq.mqh : Grind_SlotEntryAllowed` uses `used + resting_ent <= limit - (2 + GRIND_SLOT_MARGIN)`. It is called inside `Grind_SlotLockAcquire` / release in both `Grind_TryPlaceL0` and `Grind_EnsureAddNext`. `Grind_SlotLockAcquire` retries with `Sleep(1)` and can steal a stale lock.

**G7. CONFIRMED.** `grind_engine.mqh : Grind_OnTickEngine` — `Grind_RetryMissingExits` runs first, before any entry placement. Exits have absolute priority today.

**G8. CONFIRMED.** `grind_recon.mqh` — invariants I1-I8 and `Grind_ReconCheckPendingAddCorrupt` do not assert that a layer has a resting entry order. `Grind_ValidateAddLabelIndex` exists in `grind_engine.mqh` and is called at add placement time, not at recon.

---

## T-1. Same-tick equivalence

**VERDICT: EXPLOIT-FOUND**

**LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_HandleSideDealFill` only sets a flag after `Grind_ExitQManageSide`; D4 forbids `OrderSend` from `OnTradeTransaction`; `Grind_OnTickEngine` services due adds at the start of the *next* tick. The ADR’s own verified repro requires placement before the next tick’s price move, not merely before other entry paths.

**MINIMAL REPRO / MECHANISM:**
Long side, `add_pips = 10`. An add fills at `1.1000`; next target is `1.0990`.
1. `OnTradeTransaction` fires for the ENT deal. Handler appends layer, places exit, sets `g_grind_add_due_long = true`. It sends no add (D4).
2. Before the next `OnTick`/`OnTickEngine`, bid/ask falls through `1.0990` to mid `1.0985`.
3. Next `OnTickEngine`: `Grind_ServiceDueAdds` runs first. It computes target `1.0990` from the filled layer’s entry `1.1000`. Current bid is ~`1.0984`.
4. `Grind_Adr013ClampBuy` clamps to `bid - min_dist` ≈ `1.09839`. The add rests below the bid. The market already traded through `1.0990` on the way down, but no order was there.
5. A same-event placement at fill time would have rested at `1.0990` and filled on the drop to `1.0985`.

The flag-plus-next-tick is not equivalent to same-tick. It only reorders work inside the next `OnTickEngine`; it does not eliminate the tick boundary. The ADR’s own repro still occurs if price moves between the fill transaction and the next tick.

**SEVERITY: fatal-to-premise.** The ADR claims to fix the fill→add latency; it does not fix the latency that causes the verified miss.

---

## T-2. Bypassing `g_grind_ent_sent_this_tick`

**VERDICT: EXPLOIT-FOUND**

**LOAD-BEARING CLAIM:** `grind_exitq.mqh : g_grind_ent_sent_this_tick` is the per-instance, per-tick entry-send budget. `Grind_TryPlaceL0` and `Grind_EnsureAddNext` both check it and set it after a successful send. D3 allows a due add to bypass it once per side, but does not clearly state that `ServiceDueAdds` must set the global after placing.

**MINIMAL REPRO / MECHANISM:**
1. At top of `OnTickEngine`, `g_grind_ent_sent_this_tick = false`.
2. Long side has depth > 0 and a due add flag. Short side is flat.
3. `Grind_ServiceDueAdds` places the due long add. If it does not set `g_grind_ent_sent_this_tick`, the global remains false.
4. Later in the same `OnTickEngine`, short side is flat and its straddle L0 is marketable. `Grind_TryPlaceL0` sees `g_grind_ent_sent_this_tick == false` and places the short L0.
5. Result: two entry sends in one tick for one instance. With both sides due, `ServiceDueAdds` can place two. Across 16 instances, worst-case bursts go from 16 entry sends/tick (original budget) to 32 places/tick, plus modifies.

The original budget protected against per-tick request-rate spikes and global slot-lock contention. D3 removes that cap for due adds without specifying a replacement accounting rule.

**SEVERITY: fixable-within-design.** Require `ServiceDueAdds` to set `g_grind_ent_sent_this_tick` after any successful due-add send, or count due adds against a per-instance per-tick budget. As written, D3 is under-specified.

---

## T-3. Flag lifecycle

**VERDICT: EXPLOIT-FOUND**

**LOAD-BEARING CLAIM:** D1 sets the flag in the fill handler. D5 clears it only when serviced, placed, re-priced, or refused by cap/label validation. R2 mentions reinit clearing as a risk, but there is no clearing on halt or quarantine. `Grind_OnTickEngine` returns early when `g_grind_halted` or `g_grind_quarantined`; `Grind_HandleSideDealFill` still runs in `OnTradeTransaction` when quarantined because `Grind_OnTradeTransactionEngine` only checks `g_grind_halted`.

**MINIMAL REPRO / MECHANISM:**
1. Instance enters quarantine on `I3_*` or `I6_*` (not halt). `g_grind_quarantined = true`.
2. A pre-existing resting add order fills. `OnTradeTransaction` calls `Grind_HandleSideDealFill`. `g_grind_halted` is false, so the ENT branch runs: appends layer, places exit, sets `g_grind_add_due_long = true`.
3. Next `OnTick`: `g_grind_quarantined` is true, so `Grind_OnTickEngine` is skipped. Only `Grind_RetryMissingExits` runs. The due flag remains.
4. During quarantine, the exit for that newly filled layer fills via `OUT_BY`; `Grind_RemoveLayerAt` removes that layer. The ladder now has a different deepest layer than the one that set the flag.
5. Quarantine releases. Next `OnTickEngine` services due adds first. `Grind_ComputeAddTarget` now uses the new deepest layer, not the fill that motivated the flag. It can place an add that no prior fill implied.
6. If the side became empty, `Grind_ComputeAddTarget` returns `0.0`; the flag must be explicitly cleared or it stays stuck.

**SEVERITY: fixable-within-design.** Clear due flags on quarantine entry/exit, halt, and in `OnInit` before reconstruction. Not specified in D5.

---

## T-4. Ordering vs. L0 and recentring

**VERDICT: EXPLOIT-FOUND**

**LOAD-BEARING CLAIM:** D2 services due adds before L0 placement. `grind_exitq.mqh : Grind_SlotEntryAllowed` is `used + resting_ent <= limit - (2 + GRIND_SLOT_MARGIN)`. From the memo, limit 200 and ceiling 194, implying `GRIND_SLOT_MARGIN = 4`. Both `Grind_TryPlaceL0` and `Grind_EnsureAddNext` use the same guard inside the same lock.

**MINIMAL REPRO / MECHANISM:**
Assume account limit 200, ceiling 194.
1. `used = 193`, `resting_ent = 0`.
2. Long side has depth > 0 and a due add flag. Short side is flat; mid is near, so a short L0 sell limit would be passive and valid.
3. `Grind_ServiceDueAdds` runs first. Due long add guard check: `193 + 0 = 193 <= 194`, allowed. Place long add. Now `used = 194`, `resting_ent = 1`.
4. Short L0 placement later in the same tick: guard check `194 + 1 = 195 > 194`, refused.
5. Short side starves. If short L0 had been placed first, it would have consumed the last slot, and the far due add would have been refused or delayed.

This reintroduces exactly the purgatory defect the memo is trying to fix: a far add consumes the last guard units and starves a near-market L0 on the opposite side. R1 only guards exits; L0 is not protected.

**SEVERITY: fatal-to-premise.** The ADR’s ordering creates a new priority inversion against L0, contrary to D6’s claim that L0 is unchanged.

---

## T-5. Interaction with G4 re-pricing

**VERDICT: NO-EXPLOIT**

**LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_EnsureAddNext` — if `side.add_pending_ticket != 0` and the label is OK, it clamps the target; if outside the deadband it calls `Grind_ModifyPendingPrice` and returns. D5 clears the flag once re-priced.

**MINIMAL REPRO / MECHANISM:**
The precondition — a due flag with an add already pending on that same side — is nearly unreachable:
- If the fill is the pending add, the ENT branch clears `add_pending_ticket`.
- If the fill is an L0 fill, the side had depth 0, so no add could have been pending.
- If both sides fill in a burst, each side’s pending add is cleared by its own fill.

If the state is artificially constructed, `ServiceDueAdds` → `EnsureAddNext` enters the existing `add_pending_ticket` branch, computes the clamp, sees it outside the deadband, calls `ModifyPendingPrice`, and returns. No new placement occurs. D5 clears the flag on re-price. No double-send. If the modify fails, the ordinary end-of-tick `EnsureAddNext` will retry the modify on the next tick.

**SEVERITY: cosmetic.** Clarify that a failed modify does not leave a stuck flag, but mechanically it does not double-send.

---

## T-6. Guard refusal loop

**VERDICT: EXPLOIT-FOUND**

**LOAD-BEARING CLAIM:** D3 says a due add refused by the commitment guard stays due and retries next tick. `grind_exitq.mqh : Grind_SlotLockAcquire` uses a global variable lock with `Sleep(1)` retries and stale steal. `grind_engine.mqh : Grind_OnTickEngine` also calls ordinary `Grind_EnsureAddNext` at the end for any side with depth > 0 or a pending add.

**MINIMAL REPRO / MECHANISM:**
Guard saturated at ceiling 194. 16 instances, both sides have depth > 0 and due flags.
Per `OnTickEngine` per instance:
1. `ServiceDueAdds` long: acquire lock, check guard, fail, release. No `OrderSend`.
2. `ServiceDueAdds` short: acquire lock, check guard, fail, release. No `OrderSend`.
3. End-of-tick ordinary `EnsureAddNext` long: acquire lock, check guard, fail, release.
4. End-of-tick ordinary `EnsureAddNext` short: acquire lock, check guard, fail, release.

That is 4 global-lock acquisitions per instance per tick, or 64 per tick across the fleet. Each acquisition scans `OrdersTotal()` (up to ~200 orders) to compute `resting_ent`. At 10 ticks/sec, that is 640 lock acquisitions/sec and ~128k order scans/sec. The global lock becomes a fleet-wide contention point. `Sleep(1)` retries can add latency. Original code would have at most 2 acquisitions per instance per tick (the two end-of-tick `EnsureAddNext` calls).

**SEVERITY: fixable-within-design.** Do not retry a due add twice in the same tick after a guard refusal; mark the side as attempted-this-tick, or service due adds only when the guard might allow. As written, the ADR doubles lock pressure under saturation.

---

## T-7. Transaction-path safety

**VERDICT: NO-EXPLOIT**

**LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_HandleSideDealFill` calls `Grind_MarkDealProcessed(deal_ticket)` before the ENT branch work. D1 sets the flag after `Grind_ExitQManageSide`. The `OUT_BY` branch and the `EXT` branch do not set the add flag. `Grind_DealWasProcessed` short-circuits the second side’s call for the same deal.

**MINIMAL REPRO / MECHANISM:**
Multi-deal burst:
1. Two ENT deals on long side. First deal: long handler selects, validates, marks processed, appends layer, places exit, sets `g_grind_add_due_long = true`. Short handler for the same deal sees `Grind_DealWasProcessed` true and returns.
2. Second deal: long handler sees processed true and returns. Short handler also returns. Flag remains set once.
3. Two deals on opposite sides: long deal sets long flag; short deal sets short flag. Each is set once.
4. `OUT_BY` scalp close: removes layer, calls `ExitQManageSide`, does not set an add flag. No interference.
5. `EXT` entry: queues CloseBy and microstructure measure, does not set an add flag.

Setting the flag after `MarkDealProcessed` is safe. D4’s “no I/O” claim is narrower than it sounds — `ExitQManageSide` already places exit orders inside the handler — but that is existing behavior, not a new flag-path risk.

**SEVERITY: cosmetic.** Note that the fill path already performs exit `OrderSend`; D4 should be read as “no new entry `OrderSend`,” not “no I/O.”

---

## T-8. Premise: is the latency real enough?

**VERDICT: EXPLOIT-FOUND**

**LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_HandleSideDealFill` and `Grind_OnTickEngine`. Before ADR-152, adds are not held: after an add fills, the next add is placed by `Grind_EnsureAddNext` at the end of the next `OnTickEngine`. Ticks arrive in milliseconds. The ADR’s own repro requires a ~5-pip move between fill and service.

**MINIMAL REPRO / MECHANISM:**
Quantify what D1-D3 buy before ADR-152 ships:
- The delay is at most one tick, plus the chance that `g_grind_ent_sent_this_tick` was consumed by another path.
- In a normal two-sided ladder, L0 placement only competes when the opposite side is flat. `Grind_TryRecenterOppositeL0` is a modify, not an entry send. So the main competitor is the other side’s `EnsureAddNext`.
- The only case where ADR-152a gives a real benefit is when both sides need to place an add in the same tick, or when a due add and a flat-side L0 compete. In those cases, it can place two entry sends instead of one (T-2) and can starve the flat side’s L0 (T-4).
- The ADR-152a still does not place the add at fill time. If price moves in the gap between the fill transaction and the next `OnTick`, the clamp moves the add lower and the fill is missed. The ADR’s own repro is not fixed.
- Under ADR-152, the held-add case makes the next add absent after a fill, so the one-tick delay lands on the next add. But the ADR-152a still only reorders within the next tick. It does not eliminate the delay. The gap-miss problem (memo T-4) remains.
- Request cost: daily total is roughly unchanged, but per-tick bursts can double. Before ADR-152, the latency is mostly invisible because adds are resting. The ADR-152a alone changes little and adds fill-path risk.

**SEVERITY: fatal-to-premise.** The ADR should be folded into ADR-152. The holding logic is what creates the urgency; designing the entry-placement priority, guard reservation, and flag lifecycle together is safer than shipping this as a prerequisite.

---

## PREMISE VERDICT

Fill-driven add placement is **not worth the fill-path risk as its own ADR** in the current form.

- D1-D6 are **not mechanically safe as written**.
- T-1 kills the same-tick equivalence: the flag-plus-next-tick still misses the ADR’s own verified repro unless entry `OrderSend` is allowed from `OnTradeTransaction`, which D4 forbids.
- T-4 reintroduces far-add starvation of near-market L0, the exact defect ADR-152 is meant to address.
- T-3 leaves the flag alive across quarantine/halt, allowing an add against a changed ladder.
- T-6 doubles global slot-lock pressure under guard saturation.
- T-2 is under-specified: D3 does not state that `ServiceDueAdds` must set `g_grind_ent_sent_this_tick`, so two entry sends per instance per tick become possible.

The ADR should be merged into ADR-152, where the held-add state, guard reservation, L0 priority, and flag lifecycle can be designed together. If it must remain separate, it needs a new decision on placement inside the transaction handler or an explicit acceptance that the next-tick delay remains.

## OVERRIDE CHECK

**T-1 and T-4 kill the ADR-152a premise as written.** T-1 means the ADR does not fix the latency it was extracted to fix; T-4 means its ordering creates a new starvation path against L0. These are not cosmetic and cannot be fixed without changing D2/D3/D4 or folding the work into ADR-152.