# DeepSeek R1 Audit Report

## Internal Reasoning

We need answer per required format. Need audit. We have code. Need examine threats T-1..T-9, givens check, premise verdict, override. We are red team, zero implementation. Need be exhaustive, concrete source references. Let's analyze carefully.

We need know actual source line numbers approximate. We have code snippets. Need find exploits.

Key context: fxgrind, MT5. 14 EA instances share one FTMO demo account hedging. ACCOUNT_LIMIT_ORDERS=200 counts positions + pending orders combined per memo observation (but MT5 docs maybe ACCOUNT_LIMIT_ORDERS is pending orders only? Need discuss T-2). Actually MT5 ACCOUNT_LIMIT_ORDERS: "Maximal number of pending orders on the account" per docs? Let me recall. MQL5 ENUM_ACCOUNT_INFO_INTEGER: ACCOUNT_LIMIT_ORDERS — Maximal number of pending orders allowed on the account. Hmm. The background says observed counts positions+orders at refusals. But MT5 docs: ACCOUNT_LIMIT_ORDERS is maximum number of active pending orders? Let's verify from memory: MT5 account properties: ACCOUNT_LIMIT_ORDERS — Maximum number of pending orders, ACCOUNT_LIMIT_VOLUME? There is ACCOUNT_MARGIN_SO_MODE etc. I recall ACCOUNT_LIMIT_ORDERS "Maximal number of pending orders on the account" maybe. But FTMO might enforce combined? The memo says read directly positions=81 orders=113 at first refusals, total 194? Wait 81+113=194 not 200. Hmm "positions=81 orders=113 at the first refusals" then later at 200. Maybe in-flight. Need attack D1.

Let's parse code.

Important: OnTick order in fxgrind.mq5:

void OnTick()
{
   Grind_ProcessCloseByQueues(InpMagic, InpVerboseLog);

   if(!g_grind_halted) {
      Grind_CapPublishOwnExposure(...);
      const bool ok = Grind_CheckBookInvariants();
      const int action = Grind_QuarantineStep(ok, g_grind_invariant_reason, GetTickCount64());
      if(action == GRIND_INV_HALT) {
         g_grind_halted = true;
         g_grind_halt_reason = g_grind_invariant_reason;
         Grind_TelemetryCritical(...);
         Grind_InvariantEmitArchive(...);
      }
   }

   if(g_grind_halted)
      return;

   if(g_grind_quarantined) {
      if(Grind_GuardsAllowTrading(InpMagic, InpLots))
         Grind_RetryMissingExits(InpMagic, InpSlot, InpLots);
      return;
   }

   Grind_OnTickEngine(...);
}

G4 says OnTick ProcessCloseByQueues; if not halted, invariants + quarantine; on HALT sets g_grind_halted directly. If quarantined, runs only RetryMissingExits and returns. Correct. Also note Grind_ProcessCloseByQueues runs before halted check—so halted instance still sends CloseBy (allowed). Good.

G5 Grind_HaltCritical second halt entry. It sets g_grind_halted and telemetry. Neither halt path cancels orders. Correct. Proposed D6 cancels own ENT on halt both entry points.

Grind_OnTickEngine order:
- guards
- ReconcileStrayL0 long/short
- compute targets
- place L0 for flat sides if marketable (calls TryPlaceL0)
- recenter opposite L0
- cap transitions
- RetryMissingExits
- EnsureAddNext long/short

G3 said order: ReconcileStrayL0, L0 placement, ADR-124 recenter, cap transition, RetryMissingExits, then EnsureAddNext. Yes. D7 move RetryMissingExits before L0 placement. Need assess.

Let's inspect functions.

Grind_TryPlaceExitForLayer: if layer.exit_order_ticket !=0 or exit_position_ticket !=0 return false. Then PlaceLimit, sets exit_order_ticket to returned ticket or 0. No capacity check. If refused, exit_order_ticket remains 0, returns false. Retry next tick.

Grind_HandleSideDealFill on ENT fill: appends layer then TryPlaceExitForLayer immediately. If exit refused 10040, layer naked until retry. Quarantine invariants check I3 detects naked soon. Quarantine min 3000ms and 3 checks. If capacity does not free, retries fail, halt.

Proposed D4: on exit refused 10040, cancel own resting ENT furthest from market (add before L0), clear tracker ticket, re-send exit. If no resting ENT, set GV flag. Peers withdraw own furthest ENT and defer.

Need attack T-1 race. Fourteen instances read same free same tick. Reserve absorbed. Worst burst: free can be > reserve, many instances simultaneously decide ENT allowed and send. Since terminal-local reads, all see same free. Suppose free = reserve+1, 14 instances each send one entry. First send consumes slot, subsequent sends see free? They read at decision time before sends, all see same. They all send. Terminal enforces limit; once reaches 200, later sends refused. But D2 says ENT allowed only when free > reserve. If free = reserve+1, all 14 send. That consumes up to 14 slots. free after = reserve+1-14 = reserve-13. Reserve exhausted. Exits allowed when free>=1. If free goes negative? Actually once at limit, free=0. Then exits refused. D4 tries evict own ENT. If each instance has no resting ENT? But they just placed ENT? They may have resting ENT. But if the burst was entries placed by all, they have resting ENT. Each exit that needs slot can cancel own furthest ENT. However, simultaneous: if all 14 fills occur and all 14 exits try to place at free=0, each cancels own ENT freeing slots. But cancels and sends race. Need construct worst burst where reserve exhausted and exit still refused. Suppose all entries placed in same tick, then price moves and all fill simultaneously (or within same tick). Terminal processes fills, turning orders into positions (count unchanged), but the exit placement after each fill is an OrderSend. If 14 fills happen, 14 exits are needed. Slots freed by fills? A fill converts pending order to position: OrdersTotal decreases by 1, PositionsTotal increases by 1, total unchanged. So no net slot freed. So free remains 0. All 14 exits send at free=0 -> refused. Then D4 local eviction: each cancels its own furthest ENT. But if the fill was of the only ENT for each instance? Need understand layers. Each instance may have multiple layers. The resting ENT furthest from market is likely an add or L0. But if the instance just filled its L0 and no other ENT, no resting ENT to evict. Then set GV flag. Peers withdraw own furthest ENT. But if peers also just filled and have no resting ENT, flag does nothing. So exits remain refused. Quarantine may halt. This is a fatal race: the reserve is not a guarantee because entries allowed at free > reserve can consume more than reserve in one burst (14 instances can consume 14 slots, reserve 10-16). If reserve 10 and 14 instances, free=11 allows all 14 to send, consuming 14, dropping free below 0? Actually free=11, sends 14: first 11 succeed? Wait terminal limit 200. free = 200 - positions - orders. If free=11, only 11 slots available. The 12th+ sends refused 10040. But those refused are ENT sends. D9 says any 10040 on ENT marks deferred. So 11 entries placed, 3 refused deferred. free becomes 0. Then exits? If fills happen for the 11 placed entries, 11 exits needed at free=0. Each instance has at least the just-placed ENT? Wait the just-placed ENT becomes position on fill. No resting ENT remains for that layer. But instance may have other resting adds? If it was flat, L0 placed, then fill, no other ENT. So no resting ENT to evict. 11 exits refused. GV flag set. Peers withdraw own furthest ENT. But peers that also filled have no ENT. Peers that didn't fill may have resting ENT. But total slots needed 11, peers maybe few. Not enough. So D4 fails. Thus T-1 EXPLOIT-FOUND fatal-to-premise? The premise "exits never refused" fails. The design says "exits are never refused for capacity except in an unbounded simultaneous burst". But here burst is bounded (14 instances) and reserve not enough. Since reserve 10-16 but 14 instances can all act. The design's claim is false. D4 local eviction requires a resting ENT; after an entry fills, there is no resting ENT for that layer. If all instances were flat and placed L0 simultaneously, then all fill, none has ENT. Fleet flag cannot create slots from nothing. So exits refused, quarantine, halt. This kills premise. Need be precise: With reserve R, free can be R+1. 14 instances each allowed to send one ENT. Terminal capacity allows only R+1 sends. So after burst, free=0. If those entries fill before any exit placement? In MT5, fill and exit placement happens in OnTradeTransaction, which is triggered by deal add. The EA's OnTradeTransaction runs asynchronously. The exit send occurs in Grind_HandleSideDealFill, which is called from OnTradeTransaction. Multiple fills can be processed in the same tick or across ticks. The exit sends will see free=0. No resting ENT for those filled layers. So D4 step 1 fails (no resting ENT), step 2 sets flag. Peers may have resting ENT, but if all 14 instances were in the burst, they may all have no ENT or some. Worst-case: all 14 instances had a flat side and placed L0; all L0 fill. Then no instance has resting ENT. Free=0. All 14 exits refused. Flag set by first; other instances see fresh flag but have no ENT to withdraw. So exits stay refused. Quarantine 3000ms/3 checks -> halt. This is exactly the observed failure. So D4 is insufficient. Severity fatal-to-premise.

Need also consider T-2 count correctness. D1 uses PositionsTotal()+OrdersTotal(). Can disagree with terminal enforcement. In-flight sends: OrderSend is synchronous? In MT5, OrderSend is synchronous to trade server? Actually OrderSend returns after server response. But terminal may have local pending order list. During OrderSend, before response, OrdersTotal may not include the new order. Concurrent EAs in same terminal are single-threaded? MT5 EAs run in separate threads? Each EA runs in its own thread but terminal processes trade requests sequentially? The terminal's trade server connection is shared. PositionsTotal/OrdersTotal reflect current terminal state. In-flight sends: if multiple EAs send simultaneously, each reads totals before the other's order appears. This is the race. So D1 count is not atomic. Also manual orders: included but not by our magic; eviction only own ENT. If manual orders consume slots, budget still sees them (free lower). Fine. CloseBy transient: CloseBy closes two positions and creates no pending? TRADE_ACTION_CLOSE_BY closes one by opposite, reducing positions by 2? Actually CloseBy closes both positions, so PositionsTotal decreases by 2. But during the operation, maybe transient. D4 says CloseBy always allowed. But while CloseBy is in flight, counts may be stale. Also ACCOUNT_LIMIT_ORDERS semantics: MT5 docs say "Maximal number of pending orders allowed on the account" not positions+pending. If true, then free = limit - positions - orders is wrong; should be limit - orders. But observed background says limit counts combined? The memo says observed positions=81 orders=113 at first refusals, total 194, not 200. Hmm. Actually if ACCOUNT_LIMIT_ORDERS=200 pending only, then 113 pending << 200, so refusals at 194 total? That contradicts. Wait maybe at refusals positions=81 orders=113 total=194, and next sends refused? If limit is pending only, 113 < 200, so not refused. So observed suggests combined. But MT5 docs? Let me recall exactly: ACCOUNT_LIMIT_ORDERS — Maximal number of pending orders on the account. In MT5, there is also ACCOUNT_LIMIT_VOLUME? I think FTMO may set "Maximum number of positions + orders" as a server-side limit, but ACCOUNT_LIMIT_ORDERS might reflect that? Actually MT5 account has "Maximum number of orders" and "Maximum number of positions"? Let's check memory: ENUM_ACCOUNT_INFO_INTEGER includes ACCOUNT_LIMIT_ORDERS (Maximal number of pending orders) and ACCOUNT_LIMIT_VOLUME (Maximal volume of positions and pending orders). There is no combined count property. But the broker can enforce a total. The property might be "Maximal number of active pending orders" not combined. If the design uses ACCOUNT_LIMIT_ORDERS as combined, it may be wrong if the terminal property is pending-only. But the observed data: positions=81 orders=113 at first refusals. If limit=200 pending only, 113+1=114 <200, no refusal. So either observed is from combined server limit but property returns 200 anyway? Or the property is combined. Need not rely on docs; we can say T-2 EXPLOIT-FOUND: D1 assumes ACCOUNT_LIMIT_ORDERS semantics as combined, but MT5 documents it as pending orders only. If property is pending-only, then budget will be overly conservative (free = 200 - 81 - 113 = 6, but actual pending slots = 200-113=87). That would block entries unnecessarily, but not cause exit refusal. The critical failure: if property is pending-only and server enforces combined 200, then free computed = 200 - positions - orders, but actual combined free = 200 - positions - orders, so same? Wait if property returns 200 as pending limit, but server enforces combined 200, then property value 200 is not the combined limit. The server's combined limit might be different, e.g., 200? If server enforces combined 200, and property returns 200 pending-only, then using property as combined limit gives correct combined limit? No, if property is pending-only limit=200, but server combined limit=200, then property value coincidentally equals combined limit. But if server combined limit is different? The observed refusals at total 194? Not 200. So maybe server combined limit is 200? The memo says at 200. Hmm. Need not overclaim. But we can note D1's free formula subtracts positions from a limit that may be pending-only. If ACCOUNT_LIMIT_ORDERS is pending-only, free is understated by PositionsTotal, causing entries to be blocked far too early, but exits still allowed (free >=1). Actually if free understated, exits might be blocked when free=0? Suppose positions=100, orders=99, total=199, pending limit=200. Actual combined free = 1. D1 free = 200-100-99=1. Same. If positions=100, orders=100, total=200. D1 free = 0. Actual pending free = 100. If server enforces combined 200, total=200 so no slots anyway. If server enforces pending 200, total=200 but pending=100, so slots exist for pending. D1 would block ENT (free<=reserve) and exit (free=0) unnecessarily. But observed refusals at total=200? The background says "At 200 the terminal refuses new orders locally". So combined limit is 200. If ACCOUNT_LIMIT_ORDERS returns 200, then it matches combined. But docs say pending only. So maybe FTMO sets ACCOUNT_LIMIT_ORDERS to 200 as combined? Unclear. We can raise as design-unsafe: D1 depends on ACCOUNT_LIMIT_ORDERS being the combined cap; the docs describe it as pending orders only. If the server-side combined cap is not exposed or differs, budget is miscalibrated. But since we must not re-litigate givens without source counterexample, and G1-G8 don't include this. T-2 asks exactly this. We can say EXPLOIT-FOUND: the code has no check; if ACCOUNT_LIMIT_ORDERS returns 0 for unlimited, D1 disables budget; but if it returns pending-only limit, free understates and can block exits? Wait if free understated, exits allowed when free>=1. If positions=200, orders=0, free=0 if limit=200 pending-only? Then exits blocked even though pending limit is 200. But positions=200 > combined limit? If server combined limit is 200, no exits needed? Actually if positions=200, no orders, new exit would be an order, total would be 201 > combined 200, so refused. So blocking exit is correct. If server combined limit is higher, then wrong. Not enough.

Maybe better T-2: PositionsTotal()+OrdersTotal() is not atomic with terminal enforcement. In-flight sends from 14 instances mean the sum read by instance A does not include orders sent by instance B in the same tick. So free is stale. This is the same race as T-1. Also OrdersTotal() includes only orders on the current symbol? No, OrdersTotal() returns total number of orders on all symbols. PositionsTotal() returns total positions on all symbols. ACCOUNT_LIMIT_ORDERS is account-wide. So correct. But OrdersTotal() in MT5 returns number of orders in the terminal, which includes pending orders. PositionsTotal() returns positions. The terminal may not include orders that are being placed but not yet confirmed. So in-flight sends can exceed. Also a fill converts order to position; during the fill transaction, the order may still be in OrdersTotal while position already in PositionsTotal, causing double count, making free appear smaller, possibly blocking exits. But that's conservative. Conversely, after a cancel, the order may still be counted until terminal updates, so free appears smaller. The dangerous direction is free overestimated due to in-flight sends. D1 cannot be atomic. So T-2 EXPLOIT-FOUND: count can disagree; D1 depends on combined semantics that docs describe as pending only.

T-3 Eviction state machine. D4 cancels own resting ENT furthest from market. Cancelling an add or L0 mid-life. Need find sequence that halts, double-places, untracked order.

Consider L0 eviction. If instance has no add, it cancels L0. But L0 is tracked in side.l0_pending_ticket. D4 says cancel furthest ENT (add before L0). If it cancels L0, must clear tracker. But D4 only says "clear its tracker ticket" for the evicted order. Does it clear l0_pending_ticket? If the evicted order is L0, the tracker is side.l0_pending_ticket. If not cleared, I8? I8 only checks add_pending_ticket, not L0. But stray-L0 reconcile: Grind_ReconcileStrayL0 runs at start of next tick. If side.l0_pending_ticket is still set but order is gone, Grind_ReconcileStrayL0 will see it's not an order, not a position, and clear it. So no halt. However, if it was cancelled but filled just before cancel? Race: cancel racing a fill. If the ENT fills before cancel processed, the cancel returns error (order not found? or invalid). The code Grind_CancelPendingOrder checks Grind_SelectOurOrder(ticket, magic). If order already filled, OrderSelect fails, returns false. The tracker remains set. Next tick ReconcileStrayL0 sees position? If filled, l0_pending_ticket equals position ticket? Actually l0_pending_ticket was the order ticket. After fill, the position has a different position ticket? In MT5, a pending order that fills creates a position with the same ticket? I think the position ticket is the order ticket? In MT5, the position identifier is different from order ticket? Actually in hedging accounts, when a pending order triggers, a new position is created with a new position ticket, while the order ticket is the order that triggered. The position ticket is the same as the order ticket? Let me recall: In MT5, a position has POSITION_IDENTIFIER, and the order that opened it has ORDER_TICKET. Often the position ticket equals the order ticket for the initial order. But I'm not 100% sure. In code, Grind_AppendLayer uses position_id from DEAL_POSITION_ID. It does not compare to order_ticket. In Grind_HandleSideDealFill, they set side.l0_pending_ticket = 0 if order_ticket == side.l0_pending_ticket. So they assume order_ticket matches tracker. Then they append layer with position_id. So position_id may differ. If cancel races fill: the fill transaction arrives, HandleSideDealFill sets l0_pending_ticket=0 and appends layer. If cancel was sent before fill but processed after, the cancel might fail because order gone. The code: Grind_CancelPendingOrder returns false, does not clear tracker. But HandleSideDealFill already cleared it. So fine. If cancel processed first, order gone, then fill cannot happen. But if fill already happened, HandleSideDealFill clears tracker. So no double-place. But what if D4 evicts an add: side.add_pending_ticket. If cancel races fill, similar. If cancel succeeds, tracker cleared. If fill happens, HandleSideDealFill clears add_pending_ticket and appends layer, places exit. So no I8. However, if cancel fails because order not found but not filled (e.g., already cancelled by exchange), tracker remains set. Next tick EnsureAddNext sees add_pending_ticket !=0, Grind_SelectOurOrder fails, not position, sets add_pending_ticket=0. So self-heals. But I8 in recon: Grind_ReconCheckPendingAddCorrupt checks if add_pending_ticket is non-zero and order not found as order. This runs during Grind_CheckBookInvariants every tick (OnTick). Wait Grind_CheckBookInvariants calls Grind_RebuildBookFromTickets, which calls Grind_ReconCheckPendingAddCorrupt on long_out.add_pending_ticket. But long_out is a temporary rebuild from broker tickets, not g_grind_long. So it checks the reconstructed state's add_pending_ticket, which is set from broker tickets. If the order is gone and add_pending_ticket was not cleared in the live state, the invariant check rebuilds from broker and won't see the add_pending_ticket because it's not in tickets. So it won't detect the stale live tracker. I8 only triggers if the reconstructed state has add_pending_ticket set but the ticket is not an order. But reconstruction sets add_pending_ticket only from orders present in tickets. So I8 can only trigger if there's an order with comment ENT and layer>0, but it's not found as order? That's impossible because it was found as order. Wait Grind_ReconCheckPendingAddCorrupt is called after building long_out from tickets. It takes tickets array and add_pending_ticket from long_out. long_out.add_pending_ticket is set only when scanning tickets: if c_role=="ENT" && kind==ORDER && c_layer>0, it sets long_out.add_pending_ticket = tickets[i].ticket. Then later Grind_ReconCheckPendingAddCorrupt(tickets, ..., long_out.add_pending_ticket) checks if that ticket is in tickets as ORDER. It is, because it just came from there. So I8 can never fail? Unless there are duplicate? Actually if the ticket was added as ENT order, it's kind ORDER. So I8 always passes. So G7's claim "I8 halt if add_pending_ticket is non-zero and the order is not found as an order" is about reconstruction? The function exists but it seems redundant. Maybe I8 is for live tracker? No, Grind_CheckBookInvariants uses temporary long_out. So I8 is not checking live g_grind_long.add_pending_ticket. This is a major finding: G7 says I8 halts if add_pending_ticket is set but order not found. But the code path in Grind_ReconCheckPendingAddCorrupt is only called with reconstructed state, which is built from tickets. So it cannot detect a stale live tracker. Thus D4's concern about I8 is misplaced? Or maybe there's another call. Let's search: Grind_ReconCheckPendingAddCorrupt is called in Grind_RebuildBookFromTicketsInner after building long_out and short_out. It checks long_out.add_pending_ticket. long_out is the output state. If the add_pending_ticket was set from tickets, it will be found. So I8 is effectively dead code for live corruption. However, G7 says I8 halt if add_pending_ticket is non-zero and the order is not found as an order. Maybe there is a live invariant check elsewhere? Not in provided code. So G7 might be inaccurate. But we must be careful: The function is used in Grind_RebuildBookFromTickets, which is called by Grind_CheckBookInvariants each tick with tickets from broker. So it only sees broker state. If an add_pending_ticket is in live state but order is gone, the broker tickets won't have it, so long_out.add_pending_ticket will be 0 (unless there's another add order). So I8 won't fire. The live state's add_pending_ticket is not checked against broker. So the invariant is not catching that. This could be a GIVENS inaccuracy. But the task says do not re-litigate G1-G8 without source counterexample. We have a source counterexample: I8 as implemented does not check live add_pending_ticket; it checks reconstructed state. So we can list G7 inaccurate. But wait, maybe Grind_CheckBookInvariants is not the only place. In OnTick, Grind_CheckBookInvariants is called. It rebuilds from broker. So yes. So I8 is not a live invariant. This is important for T-3: eviction may leave stale add_pending_ticket, but I8 won't halt because it's not checked live. However, other logic may halt? EnsureAddNext will clear it. So no halt. But if the evicted add is cancelled and tracker not cleared, EnsureAddNext will try to place a new add? Let's trace: Suppose add_pending_ticket = 123. D4 cancels it, clears tracker? D4 says "clear its tracker ticket". If implemented correctly, add_pending_ticket=0. Then next tick EnsureAddNext sees n>0, computes add_target, places new add. So it re-places. That's intended? But the eviction was to free a slot for exit. If it immediately re-places in the same tick? D4 says exit re-send in same pass. The tick order: D4 is called during exit refusal handling, which happens in Grind_HandleSideDealFill (OnTradeTransaction) or Grind_RetryMissingExits (OnTick). If it cancels add and re-sends exit, then later in the same OnTick, Grind_EnsureAddNext will run and may re-place the add, consuming the slot again. That defeats the purpose. Because D7 moves RetryMissingExits before L0, but EnsureAddNext still runs after. So if an exit was refused and D4 evicted an add, then EnsureAddNext will try to place a new add (since depth >0, add_pending_ticket=0). The budget check D2: ENT allowed only when free > reserve. After evicting add, free increased by 1 (if cancel succeeded). Then exit send consumes 1 slot, free back to previous. Then EnsureAddNext checks free. If free <= reserve, it will block (defer). So it won't re-place immediately. Good. But if free > reserve, it may re-place. That could cause another exit refusal later. But the budget should prevent if free <= reserve. So the eviction is temporary. However, there is a race: cancel and exit send are separate OrderSend calls. Between cancel and exit send, another instance might grab the freed slot. D4 says "re-send the exit in the same pass." But OrderSend is not atomic; after cancel, before exit send, another instance could send an ENT and take the slot. Then exit send is refused again. D4 then? It might loop? The design doesn't specify loop. It could set flag. So eviction can fail. This is another race. But T-3 asks state machine halts/double-places/untracked.

Double-place: If D4 cancels L0 but does not clear l0_pending_ticket, then Grind_TryPlaceL0 might see l0_pending_ticket !=0, Grind_SelectOurOrder fails, not position, sets to 0, then proceeds to place new L0? Wait TryPlaceL0: if l0_pending_ticket !=0: if !SelectOurOrder: if SelectOurPosition: return false; side.l0_pending_ticket=0; else return false. So if stale, it clears and then continues to place new L0. That could double-place if the old L0 was actually still resting? But we cancelled it, so no. If cancel failed (e.g., order not found but actually filled), then SelectOurPosition might find the position? If filled, l0_pending_ticket (order ticket) might equal position ticket? If yes, SelectOurPosition returns true, TryPlaceL0 returns false. Good. If not equal, it clears and places new L0, while there is an untracked position from the fill. But HandleSideDealFill should have appended the layer and cleared l0_pending_ticket on fill. So if fill processed, tracker cleared. If cancel raced fill and fill not yet processed, then cancel fails? Actually if fill happened, order is gone, but the deal transaction may not have been processed yet. The live state still has l0_pending_ticket set. D4 tries to cancel, fails (order gone). It might clear tracker? If it clears, then later HandleSideDealFill runs, sees order_ticket == side.l0_pending_ticket? But tracker is 0, so condition fails, doesn't clear (already 0). It appends layer with position_id. That's okay, but the exit might be placed. If D4 cleared tracker but didn't know it filled, it might then re-send exit? Actually D4 is triggered by exit refusal for a different layer. So state is messy. Could lead to double exit? Not clearly.

Untracked order: If D4 cancels an add and clears add_pending_ticket, but the cancel fails because the order is still there (e.g., timeout), then the order remains resting but tracker is 0. Then EnsureAddNext sees add_pending_ticket=0 and places a new add. Now there are two add orders resting for the same side. This violates I2? I2 checks duplicate exits, not entries. I7 checks depth > max_layers. The broker has two ENT orders with same layer index? The new add will have layer index required_index = next index. The old one had layer index required_index-1? Actually add_pending_ticket was for next add. If it was cancelled but cancel failed, the old order still has comment ENT with layer index = required_index. The new add will also be for required_index? Grind_SideNextIndex returns max layer index +1. If the old add is still resting and hasn't filled, it's not a layer in side.layers (only filled positions become layers). So side.layers depth n doesn't include it. Grind_SideNextIndex uses side.layers, so required_index = n. The old add was for n. The new add will also be for n. So two orders with same comment ENT L n. If both fill, Grind_AppendLayer will append two layers with same layer_index? Actually HandleSideDealFill on each fill appends layer with c_layer from comment. Both have same c_layer. Then side.layers will have two layers with same layer_index. Then Grind_CheckBookInvariants will detect I5_LONG_CORRUPT_LAYER_INDICES? Let's see: Grind_ReconLayerIndicesValid checks duplicate layer indices. If two layers have same index, it fails I5. I5 is not quarantinable (Grind_IsQuarantinableReason returns false for I5). So halt. That's a halt from double-place due to eviction cancel failure. But cancel failure is not typical; OrderSend returns retcode. Grind_CancelPendingOrder returns true only if retcode == DONE. If it returns false, D4 should not clear tracker? The design says "clear its tracker ticket" after cancel. If cancel fails, clearing is wrong. The design doesn't specify handling cancel failure. So exploit: D4 cancel fails (e.g., retcode 10008 order not found? or timeout). If code clears tracker regardless, then double-place later. But would cancel fail while order still exists? Possible if freeze level? Or if order is being modified? Or if retcode is not DONE but order still exists. The code's Grind_CancelPendingOrder returns false for any retcode != DONE. It does not distinguish "order already gone" from "cancel rejected". If it returns false because order already gone, clearing tracker is safe. If it returns false because cancel rejected (e.g., TRADE_RETCODE_INVALID_PRICE, TRADE_RETCODE_PLACED? Actually for remove, DONE is success. Could get TRADE_RETCODE_REJECT, TRADE_RETCODE_TIMEOUT, etc.). If timeout, order may still be live. Clearing tracker would orphan it. So T-3 EXPLOIT-FOUND: D4 as specified clears tracker unconditionally; cancel can fail while order remains, leading to double-place and later I5 halt. Severity fixable-within-design (add check on retcode, only clear on success or confirmed gone). But the design memo says "Any eviction must clear the tracker in the same pass as the cancel." It doesn't say only on success. So we can flag.

Also evicting L0: if L0 is cancelled, the side becomes flat with no L0. But there may be no layers. The tick order: ReconcileStrayL0 will see l0_pending_ticket=0, nothing. Then L0 placement may place a new L0 if marketable. That's fine. But if the L0 was cancelled to free a slot for an exit on the other side? Wait L0 is for a flat side. If one side is flat, the other side has layers. The exit is for the non-flat side. Evicting the flat side's L0 frees a slot. But then L0 placement later in the same tick will try to place a new L0, consuming the slot again. D7 moves RetryMissingExits before L0 placement, so the exit gets the slot first. Then L0 placement runs, sees free <= reserve? Actually free after exit send: if it was free=1, exit consumes, free=0. L0 placement check: D2 ENT allowed when free > reserve. free=0 <= reserve, so L0 blocked/deferred. So it won't re-place. Good. But if free was > reserve+1, it might re-place. That's okay if capacity.

But if the eviction cancels an add, the add is for the non-flat side. After exit re-send, EnsureAddNext runs. It checks budget. If free > reserve, it may re-place the add. That could be in the same tick. So the eviction only helps if free <= reserve. If free was high enough to allow adds, then why was exit refused? Exit refused means free was 0 at that moment. After evicting an add, free becomes 1. Exit send consumes it, free=0. So EnsureAddNext sees free=0 <= reserve, so it won't re-place. So no immediate re-place. Good.

But what if there are multiple refused exits? D4 only handles one. If 14 exits refused, each instance tries to evict one add. If an instance has multiple adds, it might evict one for its own exit. But other instances' exits also need slots. They evict their own adds. Total slots freed equals number of evictions. But if total exits needed > total resting adds across all instances, some exits still refused. The fleet flag asks peers to withdraw own furthest ENT. But if all instances are in the same situation, they all withdraw. The number of slots freed is limited by total resting adds. If there were more fills than resting adds, impossible. In the worst burst, all instances were flat and placed L0; fills consumed all L0s, leaving zero adds. So no evictions possible. So T-1 fatal.

T-4 Fleet flag. GRIND_SLOT_STARVED. Staleness, crashed writer, oscillation, request cost. Design says timestamp, expiry. But GV is terminal-local, shared across 14 instances. If one instance sets it and then crashes, the flag remains until expiry. Other instances see fresh flag, withdraw own furthest ENT and defer. They clear when free > reserve. But if the starved condition persists (free=0), flag stays. They withdraw all ENT? "Every instance seeing a fresh flag withdraws its own furthest ENT and defers it." If they withdraw one per tick, over many ticks they could withdraw all ENT, reducing entries. That may be intended. But oscillation: free rises above reserve because all withdrew. Then flag clears. Then instances re-place entries because free > reserve. Then free falls below reserve, exits refused, flag set again. This cycles. Request cost: each cycle involves cancel + re-place per instance. Could be a storm. Also the flag is set when an exit is refused and the instance has no resting ENT. But if free=0, every instance that has an exit refused will set the flag. They all set it at the same time. The flag is a single GV; writes are cheap but still GV operations. The design says zero sends while deferred, but evictions are sends. So request cost not zero. Also if a crashed writer set it, expiry needed. But the memo says "must obey ARCHITECT s13 (TimeTradeServer for staleness)". We don't have that code. So T-4 DESIGN-UNSAFE: flag lacks proper clear condition? The design says clears when free > InpSlotReserve. But if free is stuck at 0 because positions are all filled and no adds to cancel, the flag never clears. Instances will keep withdrawing? They have no ENT to withdraw. They just defer entries. That's fine. But if there are some adds, they withdraw them, free rises, flag clears, they re-place, free falls, flag sets. Oscillation with request cost. Also multiple instances can set the flag, but no ownership. A crashed instance's flag persists until expiry. If expiry is too long, entries are suppressed. If too short, oscillation. Not fatal to premise? It's a mechanism that adds complexity but maybe fixable. However, the core failure is T-1.

T-5 Halt cancels ENT entries. D6: on halt, cancel own resting ENT orders, never EXT. Halt entry points: OnTick invariant halt, and Grind_HaltCritical. But Grind_HaltCritical is called from various places, including inside Grind_ValidateAddLabelIndex, which may be called from EnsureAddNext. If halt occurs, D6 would cancel entries. But the cancel is a trading action. Problems: cancel racing a fill on a halted instance. If halt occurs and then cancel is sent, but an ENT fills before cancel, the fill handler Grind_HandleSideDealFill is called from OnTradeTransactionEngine. But OnTradeTransactionEngine has: if(g_grind_halted) return; (after archive record). So if halted, it does NOT process the fill! Let's check: 

void Grind_OnTradeTransactionEngine(...)
{
   if(trans.type == TRADE_TRANSACTION_DEAL_ADD)
      Grind_ArchiveRecordFill(trans.deal, magic);
   if(g_grind_halted)
      return;
   if(trans.type != TRADE_TRANSACTION_DEAL_ADD)
      return;

   Grind_HandleSideDealFill(...);
   ...
}

So if halted, fills are ignored. So if D6 cancels ENT orders on halt, and an ENT fills before the cancel is processed, the fill is not processed by the halted instance. The position becomes untracked. That's exactly the problem D6 tries to solve, but the race remains: a cancel sent during halt can race a fill, and the halted instance will not process the fill. The resting entry is gone (filled), position is untracked, no exit. On reinit, reconstruction will see the position as an ENT layer with no exit -> I3 naked -> halt again. So D6 does not fully solve the untracked-fill failure; it only reduces it. The race window is between halt and cancel. If halt occurs due to invariant failure, there may be pending fills in flight. The cancel may be too late. Also, if the halt reason is I5 or I8 (tracker untrustworthy), cancelling based on tracker may cancel wrong orders or fail to cancel the actual resting orders. For I5, layer indices corrupt, so identifying "own resting ENT" may be unreliable. For I8, add_pending_ticket corrupt, so you might not know which order to cancel. D6 says cancel own resting ENT orders. But how do you identify them? You could scan OrdersTotal for magic and comment ENT. That's probably intended. But if the tracker is corrupt, scanning by magic+comment might still work. However, if the halt is due to I8, the add_pending_ticket is corrupt, but there may be a resting ENT order with that ticket. Scanning by magic would find it. So possible. But the race with fill is real. Severity fixable-within-design? It reduces risk but doesn't eliminate. The premise doesn't depend on D6? The premise is exits never refused. D6 is secondary. T-5 EXPLOIT-FOUND: cancel racing fill on halted instance leaves untracked position because OnTradeTransactionEngine ignores fills when halted. This can cause re-halt on reinit. Severity fixable-within-design? Maybe by processing fills even when halted, or by not cancelling if fill in flight. But the design as stated doesn't address it. Could be fatal to the claim "halted instances keep filling" is solved. The memo says D6 "removes the untracked-fill failure". It does not. So EXPLOIT-FOUND.

T-6 Tick reorder (D7). Move RetryMissingExits before L0 placement. Current order: ReconcileStrayL0, L0 placement, recenter, cap transition, RetryMissingExits, adds. D7 moves RetryMissingExits before L0 placement. What breaks? Invariants run before engine in OnTick. So no invariant interaction. Stray-L0 reconcile runs before. If we retry exits first, we might place an exit that consumes a slot, then L0 placement is blocked by budget. That's intended. But consider: if a side is flat, L0 is placed. If the other side has a naked layer, retrying exit first uses the slot. Good. But what about ADR-124 recenter? Recenter modifies L0 of flat side. If we retry exits first, and that consumes a slot, then recenter still runs? Recenter is a modify, always allowed. So no issue. However, there is an interaction with Grind_ReconcileStrayL0. Suppose a layer is naked (missing exit) and the same side has a stray L0? ReconcileStrayL0 cancels stray L0 if depth >0. That happens before retry. If we move retry before L0 placement, but after ReconcileStrayL0, same as now? The current order has ReconcileStrayL0 first, then L0 placement, then recenter, then cap, then retry. D7 moves retry to before L0 placement. So new order: ReconcileStrayL0, RetryMissingExits, L0 placement, recenter, cap, adds. Is there any invariant that expects L0 placement before exit retry? The invariants check broker state. They don't care about order. But consider a scenario: long side has depth >0, short side flat. Short has no L0. Long has a naked layer. Current: L0 placement tries to place short L0 (if marketable). It may consume a slot. Then retry long exit fails due to no slot. New: retry long exit first, consumes slot, then short L0 blocked by budget. That's better. But what if the naked layer is on the same side as the L0? L0 is only placed when side depth == 0. So if side depth >0, no L0 for that side. So no conflict. What about recenter? Recenter only if opposite side depth >0 and side depth ==0. So if we retry exits first, we might place an exit for the non-flat side, then recenter the flat side's L0. No conflict. What about cap transition? It just emits telemetry. No interaction. So D7 seems safe. But wait: Grind_RetryMissingExits calls Grind_TryPlaceExitForLayer, which uses layer.exit_target. That doesn't depend on L0 placement. However, if the exit is placed, it consumes a slot. Then L0 placement may be blocked. But the budget check for L0 is new. Without budget, L0 placement could consume the last slot and leave exit naked. So D7 alone (without budget) fixes the ordering. With budget, it's fine. Is there any case where retrying exits before L0 placement causes a double exit? No. So T-6 NO-EXPLOIT or DESIGN-UNSAFE? Probably NO-EXPLOIT for D7. But we can note that D7 does not address the root cause; it only helps when the same instance has both a missing exit and a new L0 to place. In the observed failure, the missing exit is on a filled layer, and the same instance may have a flat opposite side? Actually if one side fills, the other side might be flat and have an L0 resting. Current order: L0 placement might re-place or recenter? Actually L0 placement only if side depth ==0. If the opposite side is flat, it may have an L0 resting. The tick order: L0 placement happens before retry. But TryPlaceL0 checks if l0_pending_ticket !=0 and order resting, returns false (place-once). So it won't place a new L0 if one is already resting. It only places if l0_pending_ticket ==0. So if the flat side already has an L0, L0 placement does nothing. So D7 doesn't change that. The missing exit retry happens after L0 placement, but L0 placement didn't consume a slot because it was already resting. So D7's benefit is only when the flat side has no L0 and tries to place one. In the observed failure, the account was full, so L0 placement would have been refused anyway. But with budget, L0 placement is blocked before send. So D7 is defensive. No exploit found.

T-7 Quarantine timing. D5 unchanged. D4 eviction cancel then re-send across ticks. Can it land inside 3000 ms / 3 checks? Quarantine requires elapsed >= 3000 ms AND checks >= 3. If the exit is refused, the layer is naked. Invariants run every tick. On first detection, quarantine enters. Then on subsequent ticks, if invariant still fails, checks increment. After 3 checks and 3000 ms, halt. If D4 evicts and re-sends in the same pass, it might succeed immediately. The eviction and re-send are within the same OnTick or OnTradeTransaction. So it can happen within milliseconds. If it succeeds, the next invariant check will see exit coverage and release quarantine. So it can land inside 3000 ms. But if eviction requires fleet flag (no local ENT), then it waits for peers to withdraw. That is cross-instance and may take multiple ticks. Could it exceed 3000 ms? Yes. On a slow symbol (low tick rate), ticks may be sparse. If ticks are >1 second apart, 3 checks could take >3000 ms anyway. Quarantine requires both elapsed >= 3000 and checks >= 3. If ticks are 1 second apart, 3 checks = 3 seconds, so halt at exactly 3000 ms. If the fleet flag withdrawal takes 2 ticks, plus exit re-send, it might still be within 3 seconds if ticks are fast. But if ticks are slow (e.g., 1 tick per 5 seconds), 3 checks = 15 seconds, so plenty of time. Wait the condition is elapsed >= 3000 AND checks >= 3. If ticks are slow, checks take longer, so elapsed will exceed 3000 by the time 3 checks occur. So the window is actually the time until 3 checks, which is max(3000 ms, time for 3 ticks). On a slow symbol, 3 ticks could be several seconds, giving eviction more time. But the halt happens when both conditions met. So if ticks are slow, the halt is delayed, giving eviction more time. That's good. But if ticks are very fast, 3 checks can happen within 3000 ms, but elapsed < 3000, so it waits until 3000 ms. So the minimum time is 3000 ms. So eviction has at least 3000 ms. Is that enough? The fleet flag mechanism: instance A sets flag. Instance B sees flag on its next tick, cancels its ENT. Instance A's next tick re-sends exit. If ticks are fast, this is <3000 ms. If instance B is slow or not ticking, it may not see the flag in time. But if B is not ticking, it might be halted or not running. The design assumes all instances running. So likely within 3000 ms if ticks are frequent. But what if the symbol has no ticks? Then no fills either. The naked layer was caused by a fill, so there were ticks. After fill, the market may go quiet. If no ticks, OnTick doesn't run, so no invariant checks, no quarantine step. So no halt. When a tick finally comes, it checks. So the timing is tied to ticks. The quarantine is based on GetTickCount64() (wall clock) and check count. If ticks are sparse, the first check after fill might already be >3000 ms after quarantine start? Wait quarantine start is set on the first check that detects the naked layer. That check happens on some tick. The elapsed is measured from that tick's wall clock. So if the next tick is 10 seconds later, on that tick elapsed >= 3000 and checks >= 2? Actually checks increments to 2. The halt requires checks >= 3. So it needs a third tick. If the third tick is another 10 seconds later, elapsed is 20 seconds. So the window is at least the time for 3 ticks after detection. If ticks are 1 ms apart, 3 checks happen in <3 ms, but elapsed <3000, so it waits until 3000 ms from start. So the window is exactly 3000 ms if ticks are fast, or longer if ticks are slow. So D4 has at least 3000 ms to evict and re-send. Is 3000 ms enough for fleet flag? It requires: instance A sets flag (GV write). Instance B reads flag on next tick, cancels its ENT (OrderSend). Instance A on next tick sees free increased, re-sends exit. If ticks are fast, this is a few ticks, <3000 ms. If the terminal is busy or OrderSend takes time, could exceed. But generally, 3000 ms is tight but plausible. However, if the eviction requires multiple instances to withdraw (because one withdrawal doesn't free enough? Actually one exit needs one slot. One withdrawal frees one slot. So only one peer needs to withdraw. But the flag is broadcast to all; all may withdraw, freeing many slots. That's faster. So likely within 3000 ms. But there is a catch: the quarantine step runs in OnTick before the engine. If the naked layer is detected, quarantine enters. On subsequent ticks, OnTick runs quarantine step, then if quarantined, it runs only Grind_RetryMissingExits and returns. So during quarantine, the engine does not run! That means L0 placement, adds, and crucially, the D4 eviction logic? Where is D4 implemented? D4 is triggered by an exit refusal. During quarantine, OnTick runs Grind_RetryMissingExits. That function calls Grind_TryPlaceExitForLayer. If that send is refused with 10040, D4 would need to handle it. But D4 is part of the new design. If D4 is implemented inside Grind_TryPlaceExitForLayer or its caller, it could run during quarantine. But the memo says "If an exit send returns 10040: ...". It doesn't specify where. If it's in Grind_TryPlaceExitForLayer, it could run. But if it's in the engine's retry logic, it might not run during quarantine because engine is skipped. However, G4 says while QUARANTINED, OnTick runs only Grind_RetryMissingExits and skips the engine. So if D4 eviction is in the engine, it won't run during quarantine. But the design likely puts it in the exit send path. We can flag: if D4 is not integrated into the quarantine-only path, eviction won't happen during quarantine. But the memo says D5 quarantine unchanged, and D4 is about exit refusal. The quarantine path already calls Grind_RetryMissingExits, so D4 must be inside Grind_TryPlaceExitForLayer to work. That's a design detail. We can mention T-7: the quarantine window is at least 3000 ms, but during quarantine the engine is skipped, so any D4 eviction must be triggered by the retry path. If it is, it can work. If not, it fails. But the threat asks "can D4 eviction (cancel then re-send across ticks) land the exit inside 3000 ms / 3 checks on a slow symbol? If not, quantify." The bottleneck is the fleet flag: cross-instance communication requires at least one tick on the peer instance. If the peer instance is not receiving ticks (e.g., symbol quiet), it won't see the flag. But the account has 14 instances on different symbols? Wait, they share ONE FTMO demo account, separated by magic. Are they on the same symbol? The frame says "Fourteen EA instances share ONE FTMO demo account (hedging), separated by magic number." It doesn't specify symbols. The cap code has magics for different currency pairs: GBPUSD, EURUSD, EURGBP, AUDCAD, AUDCHF, CADCHF, NZDCAD, AUDNZD. So they are on different symbols. The account limit is account-wide across all symbols. So instance A on EURUSD may need a slot, but the resting ENTs that can be evicted are on other symbols (e.g., GBPUSD). The D4 local eviction only cancels this instance's own resting ENT. If this instance has no resting ENT (e.g., it just filled its only L0), it sets the fleet flag. Peers on other symbols must see the flag and cancel their own ENTs. But peers only tick when their symbol ticks. If a peer's symbol is quiet, it won't see the flag promptly. So the eviction could be delayed indefinitely if the peer isn't ticking. This is a critical flaw: the fleet flag mechanism relies on all instances ticking frequently. On a slow symbol, the peer may not tick for seconds or minutes. The quarantine window on instance A is based on its own ticks. If instance A's symbol is active, it will tick frequently and reach the 3 checks / 3000 ms halt before a quiet peer sees the flag and cancels. So D4 fails on slow/quiet symbols. This is a strong T-7 EXPLOIT-FOUND: the fleet flag requires remote instances to tick, which is not guaranteed. Quantify: If instance A's symbol ticks at 10 Hz, quarantine halts after 3000 ms. If instance B's symbol ticks once per minute, instance B may not see the flag within 3000 ms. So exit remains refused, instance A halts. Severity fatal-to-premise? It means exits can still be refused for capacity due to cross-instance timing. But the premise is about exits never refused. In this scenario, exit is refused because no local ENT and peers don't respond in time. So it can happen. So fatal-to-premise? Or fixable-within-design? The design could be fixed by having the terminal-wide GV checked not only on tick but also in a timer? But OnTimer runs every 1 second. The code has EventSetTimer(1) and OnTimer. OnTimer does not check the flag. So even if a peer is not ticking, its OnTimer runs every second. Could it process the flag? Currently OnTimer does telemetry, closeby? No, OnTimer does archive flush, telemetry, Mae, microstructure, scalp events, heartbeat, carry. It does not run trading logic. So the flag would not be processed by OnTimer. Thus a quiet symbol instance won't react to the flag until it gets a tick. So the eviction can be delayed. This is a concrete exploit. We can cite: OnTimer (fxgrind.mq5 ~? ) does not call Grind_RetryMissingExits or check GRIND_SLOT_STARVED. So fleet flag response is tick-driven. On a quiet symbol, no ticks. Therefore D4 step 2 can fail to free a slot within the 3000 ms quarantine window. This is fatal-to-premise because exits can still be refused for capacity.

T-8 Deferred entry vs existing logic. Does any invariant, cap transition, telemetry field or reconstruction step assume an entry is resting whenever it "should" be? The design introduces deferred entries. Current code has no concept of deferred. Invariants check broker state. If an entry is deferred, it's not on broker. The live state may have side.add_pending_ticket = 0 and depth = n. EnsureAddNext will be called next tick, see no add_pending_ticket, and try to place again. If budget blocks, it defers again. That's fine. But what about cap transition? Grind_OnSideCapTransition emits WARN_LAYER_CAP_REACHED when depth >= max_layers. Deferred entries don't affect depth. Telemetry heartbeat gains new fields. No existing logic assumes an entry is resting. However, reconstruction: if an instance is halted and reinit, it rebuilds from broker. Deferred entries are in memory, lost on reinit. That's acceptable (re-evaluated later). But there is a subtle issue: Grind_RetryMissingExits iterates over layers and places exits. It does not care about deferred entries. So no. But what about Grind_EnsureAddNext? It checks side.add_pending_ticket. If deferred, add_pending_ticket remains 0. So it will try to place every tick. That's not zero-send if the budget check is before the send. D3 says "a budget-blocked entry is not sent; it is marked deferred and re-evaluated on later ticks using the existing target logic. Zero sends while deferred." But if it's marked deferred, does EnsureAddNext still run and check budget? Yes. That check is local, no send. So zero sends. But if the budget allows, it sends. So fine. However, there is a problem: Grind_TryPlaceL0 has a check: if side.l0_pending_ticket !=0, it may return false. If L0 is deferred, l0_pending_ticket remains 0. So next tick it will try again. That's fine. But what about ADR-124 recentre? It modifies the opposite side's L0. If the opposite side's L0 is deferred (not placed), then l0_pending_ticket is 0. Grind_TryRecenterOppositeL0 checks if l0_pending_ticket == 0, returns. So it won't recenter a non-existent L0. That's correct. But if the L0 is deferred, the flat side has no resting order. The other side has layers. The recentre logic assumes the flat side has an L0 resting to recenter. If it's deferred, the recentre won't happen. But the L0 will be placed when budget allows, and then recentre can happen later. Is that a problem? The ADR-124 recentre is triggered when the flat side's L0 is stranded. If it's not placed, it can't be stranded. So no. But consider: the flat side's L0 is deferred because account full. The other side has layers. Price moves. The deferred L0 target is recomputed each tick. When budget allows, it places at the new target. That's fine. So T-8 NO-EXPLOIT? But wait, there is a potential invariant issue: If an entry is deferred, the instance may be flat on one side. The invariants don't require an L0 to be present. I7 checks depth > max_layers. I3 checks layers have exits. No check for missing L0. So no invariant violation. So T-8 NO-EXPLOIT.

T-9 Request cost. Is D3 truly zero-send while deferred? Any path (recentre MODIFY, stray cancel) that still sends every tick at a full book? At full book, free=0. D2: ENT sends allowed only when free > reserve. So L0 and adds are blocked. But what about recentre? Grind_TryRecenterOppositeL0 calls Grind_ModifyPendingPrice. D2 says Modify always allowed. So if there is a resting L0 on a flat side, recentre will modify it every tick if the deadband condition is met? Let's check: Grind_TryRecenterOppositeL0 checks if Grind_PriceWithinDeadband(resting, clamped, deadband_pips, _Point) then return. So if the price moves outside deadband, it modifies. This is a MODIFY send. D2 allows it always. So at a full book, if the flat side's L0 is stranded and price moves, it will send a MODIFY every tick (or every time outside deadband). The deadband is 4 pips default. If price moves 1 pip per tick, it may modify every few ticks. So there is still request cost at full book. But the design claims "zero sends while deferred" for entries. The recentre is not an entry; it's a modify. The memo D9 says "Any 10040 on an ENT send marks it deferred instead of retrying next tick." It doesn't address MODIFY. So T-9 EXPLOIT-FOUND: D3 does not stop MODIFY sends. At a full book, if a flat side has a resting L0, recentre can still send MODIFY orders. Also, stray-L0 reconcile can send CANCEL if it detects a stray L0. When does that happen? Grind_ReconcileStrayL0 checks if side depth >0 and l0_pending_ticket !=0. It cancels if it's resting. This can happen if an L0 is resting while the side has layers (stray). At full book, this could happen if an L0 was placed before the side got layers. It will cancel it. That's a send. But it's a one-time cancel, not every tick. Also, Grind_EnsureAddNext has a path: if side.add_pending_ticket !=0, and the resting order's label doesn't match required_index, it cancels it. That's a cancel. Could happen. But the main per-tick send at full book is MODIFY in recentre. Also, CloseBy sends are allowed and may be sent every tick if both legs present. But CloseBy is limited by queue and retries. So T-9: D3 not zero-send; recentre MODIFY can still fire every tick at full book, contributing to request storm. Severity fixable-within-design (make recentre also budget-aware? But modify doesn't consume slot, so it's allowed. The request storm concern is about OrderSend count. MODIFY is an OrderSend. So it counts against the 2000 daily limit. The design's claim "request storms stop" is false. The observed storm was from refused sends retried every tick. MODIFY is not refused for capacity, so it will succeed, but it still consumes request budget. At full book, the fleet might have many stranded L0s, each modifying frequently. That's a lot of requests. So the premise that request storms stop is not fully addressed. But the primary failure was refusals. Still, T-9 EXPLOIT-FOUND.

Now GIVENS CHECK. We need list any of G1-G8 found inaccurate with file and line. Let's go through.

G1: Entry gates check CanPlaceEntryLayer and CapAllowsEntry. Neither checks account capacity. In code: Grind_TryPlaceL0 checks Grind_CanPlaceEntryLayer and Grind_CapAllowsEntry. Grind_EnsureAddNext checks Grind_CanPlaceEntryLayer and Grind_CapAllowsEntry. No capacity check. Accurate.

G2: Grind_HandleSideDealFill (~833): on ENT fill, appends layer and calls Grind_TryPlaceExitForLayer immediately. On EXT fill queues CloseBy. In code: yes, for c_role=="ENT", it appends layer, then finds layer_idx and calls Grind_TryPlaceExitForLayer. For c_role=="EXT" && entry_type==DEAL_ENTRY_IN, it queues CloseBy. Accurate.

G3: Grind_OnTickEngine (~1026) order: ReconcileStrayL0, L0 placement, ADR-124 recentre, cap transition, Grind_RetryMissingExits, then EnsureAddNext. In code: yes, order matches. Accurate.

G4: OnTick (~226): ProcessCloseByQueues; if not halted, invariants + Grind_QuarantineStep; on HALT sets g_grind_halted directly. If quarantined, runs ONLY Grind_RetryMissingExits (when guards allow) and returns. In code: yes. Accurate.

G5: Grind_HaltCritical (~376) is second halt entry point. Neither halt path cancels orders. In code: Grind_HaltCritical sets g_grind_halted and telemetry. It does not cancel. OnTick halt path sets g_grind_halted and telemetry, no cancel. Accurate.

G6: Grind_OrderSendCounted (~76) increments request counter on every OrderSend, including locally refused sends. In code: Grind_OrderSendCounted calls OrderSend, then Grind_ApiCounterIncrement() unconditionally. So yes, increments even if refused. Accurate.

G7: Grind_ReconCheckPendingAddCorrupt (grind_recon.mqh ~370): I8 halt if add_pending_ticket is non-zero and the order is not found as an order. In code: The function checks if add_pending_ticket is non-zero, then searches tickets for that ticket. If not found, returns I8 halt. BUT as I noted, this function is called from Grind_RebuildBookFromTicketsInner with long_out.add_pending_ticket, which was just set from tickets. So it will always be found. Let's re-examine: In Grind_RebuildBookFromTicketsInner, it builds long_scratch and short_scratch, then checks invariants, then calls Grind_ReconCheckPendingAddCorrupt(tickets, ticket_count, long_out.add_pending_ticket, reason_out). long_out is the output state. It is populated at the end? Wait, look at the order in Grind_RebuildBookFromTicketsInner:

- It builds long_scratch, short_scratch from tickets.
- Then checks invariants.
- Then:
   if(!Grind_ReconCheckPendingAddCorrupt(tickets, ticket_count, long_out.add_pending_ticket, reason_out))
      return false;
   if(!Grind_ReconCheckPendingAddCorrupt(tickets, ticket_count, short_out.add_pending_ticket, reason_out))
      return false;

But long_out.add_pending_ticket is set during the scanning loop? Let's look: In the scanning loop, for c_role=="ENT" && kind==ORDER, it does:
   if(c_layer == 0) { ... set long_out.l0_pending_ticket ... }
   else { ... set long_out.add_pending_ticket ... }
Wait, that's inside the loop over tickets! So long_out.add_pending_ticket is set during the scan. Then after the loop, it checks Grind_ReconCheckPendingAddCorrupt with long_out.add_pending_ticket. Since it was just set from tickets[i].ticket, and tickets[i].kind == ORDER, the check will find it. So I8 never fails in this path. Is there any other call to Grind_ReconCheckPendingAddCorrupt? Not in provided code. So G7's description "I8 halt if add_pending_ticket is non-zero and the order is not found as an order" is technically what the function does, but in practice it cannot trigger because the only caller passes a ticket that was just found as an order. So the invariant is effectively dead. However, the GIVENS says "Grind_ReconCheckPendingAddCorrupt ...: I8 halt if add_pending_ticket is non-zero and the order is not found as an order." That is accurate as a description of the function's logic. But the implication that it can halt on a corrupt live tracker is false. The GIVENS doesn't claim it's live. It just says the function does that. But the memo says "I8 ... halts if add_pending_ticket is set but the order is gone. Any eviction must clear the tracker in the same pass as the cancel." So the memo assumes I8 is a live check. That's inaccurate. So we can flag G7 as misleading/inaccurate: the function exists but is not wired to detect live tracker corruption. The caller uses reconstructed state, so it cannot catch a missing order. So G7 is inaccurate in the sense that it suggests I8 will halt on a stale live add_pending_ticket. We can list it.

G8: Quarantine constants: GRIND_QUARANTINE_MIN_MS 3000, MIN_CHECKS 3 (grind_quarantine.mqh ~13-14). I3 naked reasons are quarantinable; I7 is not. In code: defines GRIND_QUARANTINE_MIN_MS 3000 and GRIND_QUARANTINE_MIN_CHECKS 3. Grind_IsQuarantinableReason returns true for I3_LONG_NAKED, I3_SHORT_NAKED, etc. For I7? It returns false for I7_LONG_DEPTH, I7_SHORT_DEPTH. So accurate.

So G7 is the one inaccurate. Also maybe G3 line numbers? Not needed.

Premise verdict: Does an exit-priority slot budget solve the observed failures? The observed failures: exits refused at capacity, halts, untracked fills, request storms, reinit instability. The budget (D1-D9) does not guarantee exits never refused because of race T-1: reserve can be exhausted by simultaneous entries, and D4 local eviction fails when the instance has no resting ENT (e.g., its L0 just filled). Fleet flag relies on peers ticking, which is not guaranteed. So exits can still be refused, leading to naked layers, quarantine, halt. Also halt cancels entries (D6) doesn't prevent untracked fills due to race. Request storms: D3 stops ENT retries but MODIFY recentre still sends. So the premise is not solved. The design is a step in the right direction but the core invariant "exits never refused" is not achieved. So PREMISE VERDICT: NO, an exit-priority slot budget as specified does not solve the observed failures. The race and cross-instance timing defects allow the same failure mode to recur.

OVERRIDE CHECK: Does any finding kill the premise, or are all fixable within it? T-1 is fatal-to-premise. T-7 is fatal-to-premise. T-5 is fixable? The premise of exit-priority is killed by T-1 and T-7 because they demonstrate exits can still be refused. Unless the design is amended to make slots truly reserved (e.g., pre-allocate exit slots by not allowing entries to consume them, which requires atomic accounting or per-instance reservation). That is a fundamental change to the design. So the finding kills the premise as stated. The override check: T-1 and T-7 kill the premise. So final line: premise killed.

Need write in required format. For each threat T-1..T-9:
- VERDICT
- LOAD-BEARING CLAIM: file : function : invariant
- MINIMAL REPRO / MECHANISM
- SEVERITY

Then GIVENS CHECK.
Then PREMISE VERDICT.
Then OVERRIDE CHECK (last line).

We need be concise but thorough. Desired oververbosity 5. We have 118 lines? The user message says line count 118 at bottom? Actually the user message ends with "Line count: 118". That's probably the prompt's line count. Our answer can be longer.

Let's structure.

For each threat, we need file:function:invariant. Use approximate line numbers from code? We can reference function names.

T-1 RACE.
VERDICT: EXPLOIT-FOUND
LOAD-BEARING: grind_engine.mqh : Grind_TryPlaceL0 / Grind_EnsureAddNext : budget check D2 allows ENT sends when free > InpSlotReserve. No atomic reservation. D4 assumes a resting ENT exists.
MECHANISM: 14 instances on same account. All read free = R+1. All pass D2 (free > R). All send L0 (or add). Terminal accepts only R+1 sends; rest 10040 deferred. Now free=0. Price gaps through all filled L0s simultaneously (same tick). Each fill converts an order to a position (count unchanged). Each Grind_HandleSideDealFill calls Grind_TryPlaceExitForLayer. All exit sends see free=0. D4 step 1: each instance checks for own resting ENT furthest from market. The filled L0 was the only ENT; no resting ENT remains. D4 step 2: first sets GRIND_SLOT_STARVED. Peers see flag but they also just filled their only L0; no ENT to withdraw. Flag cannot create a slot. Exits remain refused. I3 naked detected. Quarantine 3000ms/3 checks. Halt.
SEVERITY: fatal-to-premise.

T-2 COUNT CORRECTNESS.
VERDICT: EXPLOIT-FOUND (or DESIGN-UNSAFE)
LOAD-BEARING: grind_engine.mqh : proposed D1 : free = AccountInfoInteger(ACCOUNT_LIMIT_ORDERS) - PositionsTotal() - OrdersTotal(). MT5 docs describe ACCOUNT_LIMIT_ORDERS as pending orders only. No atomicity with in-flight sends.
MECHANISM: (a) If ACCOUNT_LIMIT_ORDERS is pending-only, D1 subtracts positions from a pending limit. When positions high, free understates, blocking exits unnecessarily; when positions low but pending high, free may overstate combined free if server enforces combined cap not exposed. (b) In-flight: instance A reads free=1, sends exit. Instance B in same tick reads free=1 before A's order is visible, sends ENT. Both pass. Terminal may accept both or refuse one. Counts are not a reservation. (c) CloseBy transient: PositionsTotal may not reflect closed legs until terminal processes. D4 says CloseBy always allowed, but its effect on free is not instantaneous.
SEVERITY: fixable-within-design? The semantic mismatch is fixable by reading the correct property or using a server-side check, but if the property is wrong, the budget is miscalibrated. For the premise, it's a correctness flaw. I'd say DESIGN-UNSAFE or fixable-within-design. Let's say DESIGN-UNSAFE because D1 depends on a property whose semantics are documented differently and the observed count (81+113=194) does not cleanly match 200, suggesting the property may not be the enforced cap. But the threat says "Does anything in D1 depend on ACCOUNT_LIMIT_ORDERS semantics that the MT5 docs describe differently (pending only)?" Yes. So EXPLOIT-FOUND. Severity: fixable-within-design? If the property is wrong, the whole budget is wrong. I'll say fixable-within-design if the correct cap can be discovered, but as coded it's unsafe. Maybe fatal-to-premise? Not necessarily, because you could use OrdersTotal only if limit is pending-only. But the observed failures are combined. I'll say DESIGN-UNSAFE.

T-3 EVICTION STATE MACHINE.
VERDICT: EXPLOIT-FOUND
LOAD-BEARING: grind_engine.mqh : Grind_CancelPendingOrder returns false for any retcode != TRADE_RETCODE_DONE; proposed D4 clears tracker unconditionally after cancel.
MECHANISM: D4 evicts an add (ticket 123). Grind_CancelPendingOrder sends TRADE_ACTION_REMOVE. Terminal returns TRADE_RETCODE_TIMEOUT (or REJECT) but order remains live. D4 clears side.add_pending_ticket = 0. Next tick Grind_EnsureAddNext sees add_pending_ticket==0, depth n>0, computes add_target, places a new add for layer n. Now two ENT orders for same layer n rest. Both fill. Grind_HandleSideDealFill appends two layers with same layer_index n. Next invariant check: Grind_ReconLayerIndicesValid detects duplicate indices -> I5_LONG_CORRUPT_LAYER_INDICES. I5 is not quarantinable -> halt. Also cancel racing fill: if cancel fails because order already filled, clearing tracker is safe, but if fill not yet processed, HandleSideDealFill will still process and append layer; the exit may be placed. But the double-place path is concrete.
SEVERITY: fixable-within-design (only clear tracker on confirmed cancel or confirmed gone; handle retcode).

T-4 FLEET FLAG.
VERDICT: EXPLOIT-FOUND
LOAD-BEARING: grind_recon.mqh : Grind_ReconCheckPendingAddCorrupt? No, for flag: proposed D4 step 2, GlobalVariable GRIND_SLOT_STARVED. No existing code. But the mechanism relies on all instances ticking. OnTimer (fxgrind.mq5) does not process the flag.
MECHANISM: Instance A on active symbol sets flag. Instance B on quiet symbol (e.g., NZDCAD during Asian session) does not receive ticks. Its OnTimer runs every second but does not check GRIND_SLOT_STARVED or call any order routine. So B never withdraws its ENT. A's quarantine window (3000ms/3 checks) expires while B is silent. A halts. Also oscillation: when B finally ticks, it sees flag, cancels ENT, free rises above reserve, flag clears. Then A (if not halted) and B re-place entries, free falls, flag sets again. Each cycle costs cancel+place per instance. If many instances, request storm.
SEVERITY: fatal-to-premise (T-7 also covers timing). Here the flag's dependence on peer ticks is a fatal flaw for the "exits never refused" claim.

T-5 HALT CANCELS ENTRIES.
VERDICT: EXPLOIT-FOUND
LOAD-BEARING: fxgrind.mq5 : OnTradeTransactionEngine (grind_engine.mqh) : if(g_grind_halted) return; so fills are ignored when halted. Proposed D6 cancels ENT on halt.
MECHANISM: Halt occurs (invariant or Grind_HaltCritical). D6 sends cancel for own resting ENT. Before cancel processed, an ENT fills. The deal add transaction arrives. Grind_OnTradeTransactionEngine runs: it records archive, then if(g_grind_halted) return; It does NOT call Grind_HandleSideDealFill. The filled ENT becomes an untracked position with no exit. The cancel may then fail (order gone). On reinit, Grind_ReconstructState sees the position as ENT with no EXT -> I3 naked -> halt. Thus D6 does not "remove the untracked-fill failure"; it only narrows the window. Also if halt reason is I5/I8 (tracker corrupt), the set of "own resting ENT" may be misidentified; cancelling based on corrupt tracker can cancel wrong orders or leave some.
SEVERITY: fixable-within-design? To fix, process fills even when halted, or don't cancel if fills in flight. But as specified, it's a flaw. I'll say fixable-within-design but the claim in D6 is false.

T-6 TICK REORDER (D7).
VERDICT: NO-EXPLOIT
LOAD-BEARING: grind_engine.mqh : Grind_OnTickEngine : moving Grind_RetryMissingExits before L0 placement. No invariant or recentre depends on L0 being placed first. Invariants run before engine in OnTick.
MECHANISM: Checked interactions: Grind_ReconcileStrayL0 runs before both; it only cancels stray L0. Grind_TryRecenterOppositeL0 only modifies an existing L0; if L0 placement is after retry, recenter still sees the same L0 state. Cap transition is telemetry only. EnsureAddNext runs after. No double exit. The only effect is exit gets slot before new L0, which is the intent.
SEVERITY: cosmetic (or none). I'll say NO-EXPLOIT.

T-7 QUARANTINE TIMING.
VERDICT: EXPLOIT-FOUND
LOAD-BEARING: grind_quarantine.mqh : Grind_QuarantineStep : halt when elapsed >= 3000 and checks >= 3. fxgrind.mq5 : OnTick : while quarantined, only Grind_RetryMissingExits runs and returns; engine skipped. OnTimer does not run trading logic.
MECHANISM: Instance A on EURUSD (fast ticks) detects I3 naked at T=0. Quarantine enters. A ticks every 100ms. At T=3000ms, checks=30 >=3, elapsed>=3000 -> halt. Meanwhile, A had no local ENT to evict, so it set GRIND_SLOT_STARVED at T=0. Instance B on NZDCAD (quiet, no ticks for 10s) does not tick. Its OnTimer runs but does not check the flag. B does not cancel its ENT. A's exit retry in Grind_RetryMissingExits still sees free=0 and is refused. D4 cannot evict (no local ENT). A halts at T=3000ms. Quantify: if peer tick interval > 3000ms, fleet flag cannot save A. On Asian session, NZDCAD can be tickless for minutes. So D4 eviction across ticks cannot land inside the quarantine window. Severity: fatal-to-premise.

T-8 DEFERRED ENTRY VS EXISTING LOGIC.
VERDICT: NO-EXPLOIT
LOAD-BEARING: grind_recon.mqh : Grind_ReconCheckInvariants : no invariant requires an entry to be resting. Cap transition and telemetry do not assume resting entries.
MECHANISM: Deferred entry leaves side.add_pending_ticket = 0 and l0_pending_ticket = 0. Invariants check only layers (positions) and exits. They do not require an L0 or add order. EnsureAddNext will retry on next tick; budget check is local. Reconstruction from broker will not see deferred entries (they were never sent), which is correct. No double-place or halt path found.
SEVERITY: cosmetic.

T-9 REQUEST COST.
VERDICT: EXPLOIT-FOUND
LOAD-BEARING: grind_engine.mqh : Grind_TryRecenterOppositeL0 : calls Grind_ModifyPendingPrice. Proposed D2 says Modify always allowed. No budget check on modify. grind_engine.mqh : Grind_EnsureAddNext : stale add cancel path can still send cancel.
MECHANISM: At full book (free=0), D2 blocks ENT sends. But if a side is flat and has a resting L0 (l0_pending_ticket !=0), and the opposite side has layers, Grind_TryRecenterOppositeL0 runs every tick. If the L0 is stranded beyond deadband (4 pips), it calls Grind_ModifyPendingPrice, which sends TRADE_ACTION_MODIFY. This is an OrderSend, counted by Grind_OrderSendCounted. It is not refused for capacity (modify doesn't consume a slot). So at a full book, the instance can still send one MODIFY per tick (or per time price moves outside deadband). With 14 instances, this is a request stream. Also Grind_ReconcileStrayL0 can send a cancel if a stray L0 exists; Grind_EnsureAddNext can send a cancel if an add label mismatches. D3's "zero sends while deferred" is false for these paths.
SEVERITY: fixable-within-design (e.g., budget-gate modifies or throttle them, but they don't consume slots; the storm is smaller than refused sends but still nonzero). The memo's claim "request storms stop" is overstated.

GIVENS CHECK:
- G7 inaccurate: grind_recon.mqh : Grind_ReconCheckPendingAddCorrupt : called only from Grind_RebuildBookFromTicketsInner with long_out.add_pending_ticket, which is set from the tickets array during the same scan. Therefore I8 cannot fire for a live stale add_pending_ticket; it only ever sees a ticket that is present as an order. The memo's assumption that I8 will catch a missing order after eviction is false. No live check of g_grind_long.add_pending_ticket against broker exists in provided code.
- All other G1-G6, G8 accurate as described.

Maybe also G3 line number? Not needed.

PREMIERE VERDICT:
The proposed exit-priority slot budget does not solve the observed failures. T-1 shows the reserve can be exhausted by a simultaneous entry burst, and D4 local eviction fails when the instance has no resting ENT (e.g., its L0 just filled). T-7 shows the fleet flag cannot reliably free a slot within the quarantine window on quiet symbols because peer instances only react on ticks, not OnTimer. Therefore exits can still be refused for capacity, producing I3 naked, quarantine, and halt. D6 does not eliminate untracked fills because OnTradeTransactionEngine ignores fills when halted. D9/D3 do not stop MODIFY sends from recentre. The observed halt storm and reinit instability are not fully mitigated.

OVERRIDE CHECK (last line): T-1 and T-7 kill the premise as stated. The design would need a reservation mechanism that does not rely on peer ticks or post-hoc local eviction (e.g., pre-allocating exit slots per instance or an atomic terminal-wide reservation), which is a fundamental change, not a fix within the current design. So premise killed.

Need ensure we don't propose remedies? The negative space says "Do not propose remedies the EA cannot execute". We can mention that the fix requires fundamental change, but not propose implementation. That's okay.

Let's write answer in required format. Use headings. Keep concise. Ensure each threat has the four bullets.

We should also mention T-2 semantic: MT5 docs say ACCOUNT_LIMIT_ORDERS is pending orders only. I'll state that.

Check line count? Not needed.

Let's produce final.

## Final Report

## T-1 RACE
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_TryPlaceL0 / Grind_EnsureAddNext : D2 budget check` allows ENT sends when `free > InpSlotReserve`. No atomic reservation exists. D4 assumes a resting ENT is available to evict.
- **MINIMAL REPRO:** 14 instances all read `free = R+1`. All pass D2 and send L0. Terminal accepts only `R+1`; rest return 10040 and are deferred. Now `free = 0`. Price gaps through all filled L0s in the same tick. Each fill converts an order to a position (count unchanged). Each `Grind_HandleSideDealFill` calls `Grind_TryPlaceExitForLayer`. All exits see `free = 0` and are refused. D4 step 1: each instance looks for its own furthest resting ENT. The filled L0 was the only ENT; none remain. D4 step 2: first instance sets `GRIND_SLOT_STARVED`. Peers see the flag but they also just filled their only L0; no ENT to withdraw. The flag cannot create a slot. Exits remain refused. I3 naked is detected. Quarantine 3000 ms / 3 checks. Halt.
- **SEVERITY:** fatal-to-premise

## T-2 COUNT CORRECTNESS
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `proposed D1 : free = AccountInfoInteger(ACCOUNT_LIMIT_ORDERS) - PositionsTotal() - OrdersTotal()`. MT5 documents `ACCOUNT_LIMIT_ORDERS` as pending orders only, not positions + pending. Counts are not atomic with in-flight sends.
- **MINIMAL REPRO:** (a) If `ACCOUNT_LIMIT_ORDERS` is pending-only, D1 subtracts positions from a pending limit. When positions are high, `free` understates combined capacity and can block exits unnecessarily; when the server enforces a combined cap not exposed by this property, `free` can overstate it. (b) Two instances read the same `free` before either `OrderSend` is reflected in `OrdersTotal()`. Both send. The terminal may accept both and hit 200, or refuse one with 10040. D1 is a read, not a reservation. (c) CloseBy closes two positions; `PositionsTotal()` may not reflect the reduction until the terminal processes the close, so `free` is transiently wrong.
- **SEVERITY:** fixable-within-design (if the correct cap source is found), but as coded the budget is miscalibrated.

## T-3 EVICTION STATE MACHINE
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_CancelPendingOrder` returns `false` for any retcode other than `TRADE_RETCODE_DONE`. Proposed D4 says clear the tracker after cancel.
- **MINIMAL REPRO:** D4 evicts add ticket 123. `Grind_CancelPendingOrder` sends `TRADE_ACTION_REMOVE`. Terminal returns `TRADE_RETCODE_TIMEOUT` (or `REJECT`) but the order remains live. D4 clears `side.add_pending_ticket = 0`. Next tick `Grind_EnsureAddNext` sees `add_pending_ticket == 0`, `depth n > 0`, computes `add_target`, and places a new add for layer `n`. Now two ENT orders for layer `n` rest. Both fill. `Grind_HandleSideDealFill` appends two layers with the same `layer_index`. Next invariant check: `Grind_ReconLayerIndicesValid` detects duplicate indices → `I5_LONG_CORRUPT_LAYER_INDICES`. I5 is not quarantinable → halt.
- **SEVERITY:** fixable-within-design (clear tracker only on confirmed cancel or confirmed gone; handle retcodes).

## T-4 FLEET FLAG
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `proposed D4 step 2 : GlobalVariable GRIND_SLOT_STARVED`. No existing code processes this flag. `fxgrind.mq5 : OnTimer` does not check the flag or run trading logic.
- **MINIMAL REPRO:** Instance A on active symbol sets `GRIND_SLOT_STARVED`. Instance B on a quiet symbol (e.g., NZDCAD during Asian session) receives no ticks. Its `OnTimer` fires every second but only flushes archive, telemetry, Mae, microstructure, scalp events, heartbeat, and carry; it never reads `GRIND_SLOT_STARVED` or calls `Grind_RetryMissingExits`. B never withdraws its ENT. A’s quarantine window (3000 ms / 3 checks) expires while B is silent. A halts. When B finally ticks, it sees the flag, cancels its ENT, `free` rises above reserve, flag clears. Then A (if not halted) and B re-place entries, `free` falls, flag sets again. Each cycle costs cancel + place per instance.
- **SEVERITY:** fatal-to-premise

## T-5 HALT CANCELS ENTRIES
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_OnTradeTransactionEngine` has `if(g_grind_halted) return;` before `Grind_HandleSideDealFill`. Proposed D6 cancels own ENT on halt.
- **MINIMAL REPRO:** Halt occurs (invariant or `Grind_HaltCritical`). D6 sends cancel for own resting ENT. Before the cancel is processed, the ENT fills. The deal add transaction arrives. `Grind_OnTradeTransactionEngine` records archive, then `if(g_grind_halted) return;` — it does **not** call `Grind_HandleSideDealFill`. The filled ENT becomes an untracked position with no exit. The cancel may then fail (order gone). On reinit, `Grind_ReconstructState` sees the position as ENT with no EXT → I3 naked → halt again. D6 narrows the window but does not remove the untracked-fill failure.
- **SEVERITY:** fixable-within-design (process fills on halt, or avoid cancel while fills in flight), but the D6 claim is false.

## T-6 TICK REORDER (D7)
- **VERDICT:** NO-EXPLOIT
- **LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_OnTickEngine` moving `Grind_RetryMissingExits` before L0 placement. Invariants run before the engine in `OnTick`.
- **MINIMAL REPRO:** Checked interactions: `Grind_ReconcileStrayL0` runs before both. `Grind_TryRecenterOppositeL0` only modifies an existing L0; if L0 placement is after retry, recenter still sees the same L0 state. Cap transition is telemetry only. `EnsureAddNext` runs after. No double exit. The only effect is the exit gets the slot before a new L0, which is the intent.
- **SEVERITY:** cosmetic

## T-7 QUARANTINE TIMING
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `grind_quarantine.mqh : Grind_QuarantineStep` halts when `elapsed >= 3000` and `checks >= 3`. `fxgrind.mq5 : OnTick` while quarantined runs only `Grind_RetryMissingExits` and returns; the engine is skipped. `OnTimer` does not process `GRIND_SLOT_STARVED`.
- **MINIMAL REPRO:** Instance A on EURUSD (fast ticks) detects I3 naked at T=0. Quarantine enters. A ticks every 100 ms. At T=3000 ms, checks ≥ 3, elapsed ≥ 3000 → halt. A had no local ENT to evict, so it set `GRIND_SLOT_STARVED` at T=0. Instance B on NZDCAD (quiet, no ticks for 10 s) does not tick. Its `OnTimer` runs but does not check the flag. B does not cancel its ENT. A’s exit retry in `Grind_RetryMissingExits` still sees `free = 0` and is refused. D4 cannot evict (no local ENT). A halts at T=3000 ms. Quantify: if peer tick interval > 3000 ms, the fleet flag cannot save A. On quiet symbols this is common.
- **SEVERITY:** fatal-to-premise

## T-8 DEFERRED ENTRY VS EXISTING LOGIC
- **VERDICT:** NO-EXPLOIT
- **LOAD-BEARING CLAIM:** `grind_recon.mqh : Grind_ReconCheckInvariants` checks only layers (positions) and exits. No invariant requires an entry to be resting. Cap transition and telemetry do not assume resting entries.
- **MINIMAL REPRO:** Deferred entry leaves `side.add_pending_ticket = 0` and `l0_pending_ticket = 0`. Invariants do not require an L0 or add order. `EnsureAddNext` retries on the next tick; the budget check is local. Reconstruction from broker does not see deferred entries (they were never sent), which is correct. No double-place or halt path found.
- **SEVERITY:** cosmetic

## T-9 REQUEST COST
- **VERDICT:** EXPLOIT-FOUND
- **LOAD-BEARING CLAIM:** `grind_engine.mqh : Grind_TryRecenterOppositeL0` calls `Grind_ModifyPendingPrice`. Proposed D2 says Modify is always allowed. No budget check on modify. `Grind_EnsureAddNext` stale-add cancel path can still send cancel.
- **MINIMAL REPRO:** At full book (`free = 0`), D2 blocks ENT sends. But if a side is flat and has a resting L0 (`l0_pending_ticket != 0`), and the opposite side has layers, `Grind_TryRecenterOppositeL0` runs every tick. If the L0 is stranded beyond deadband (4 pips), it calls `Grind_ModifyPendingPrice`, sending `TRADE_ACTION_MODIFY`. This is an `OrderSend`, counted by `Grind_OrderSendCounted`. It is not refused for capacity (modify does not consume a slot). So at a full book, the instance can still send one MODIFY per tick. With 14 instances, this is a request stream. Also `Grind_ReconcileStrayL0` can send a cancel if a stray L0 exists; `Grind_EnsureAddNext` can send a cancel if an add label mismatches. D3’s “zero sends while deferred” is false for these paths.
- **SEVERITY:** fixable-within-design (budget-gate or throttle modifies), but the “request storms stop” claim is overstated.

## GIVENS CHECK
- **G7 inaccurate:** `grind_recon.mqh : Grind_ReconCheckPendingAddCorrupt` is called only from `Grind_RebuildBookFromTicketsInner` with `long_out.add_pending_ticket`, which was set from the `tickets` array during the same scan. It therefore only sees a ticket that is present as an order; I8 cannot fire for a live stale `add_pending_ticket`. The memo’s assumption that I8 will catch a missing order after eviction is false. No live check of `g_grind_long.add_pending_ticket` against broker exists in the provided code.
- All other G1–G6 and G8 accurate as described.

## PREMISE VERDICT
The proposed exit-priority slot budget does **not** solve the observed failures. T-1 shows the reserve can be exhausted by a simultaneous entry burst, and D4 local eviction fails when the instance has no resting ENT (e.g., its L0 just filled). T-7 shows the fleet flag cannot reliably free a slot within the quarantine window on quiet symbols because peer instances only react on ticks, not `OnTimer`. Therefore exits can still be refused for capacity, producing I3 naked, quarantine, and halt. D6 does not eliminate untracked fills because `Grind_OnTradeTransactionEngine` ignores fills when halted. D9/D3 do not stop MODIFY sends from recentre. The observed halt storm and reinit instability are not fully mitigated.

## OVERRIDE CHECK
T-1 and T-7 kill the premise as stated. The design would need a reservation mechanism that does not rely on peer ticks or post-hoc local eviction (e.g., pre-allocating exit slots per instance or an atomic terminal-wide reservation), which is a fundamental change, not a fix within the current design. Premise killed.