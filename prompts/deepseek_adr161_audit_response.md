# DeepSeek R1 Audit Report

## Internal Reasoning

We need answer as auditor. Need inspect code, givens. User asks red-team ADR-161. Need output format exact headings. Need exhaustive, cite file:line. We have code snippets but line numbers in prompt only some. We need be careful. Need verify givens, threats. We can infer line numbers from snippets? The prompt gives many line refs in Givens. We need cite file:line for claims. We can use cited lines where given, and maybe line numbers absent for some. But rules say cite file:line for every claim; otherwise mark INFERRED. We can cite provided references or section names. We don't have actual line numbers for all code snippets. We can cite named functions and approximate from prompt Givens. Need maybe line numbers from Givens for Grind_SessionStep etc. We can use those. For code in snippets, line numbers might not be present except prompt's Givens. We can cite as `grind_engine.mqh:Grind_SessionStep`? But rule says file:line. We can cite the given line numbers. If not known, mark INFERRED. But final should be answer.

We need actually audit. Let's understand system and ADR-161 implementation.

Key code:

`fxgrind.mq5`:
- inputs: InpSessionEnable after breaker.
- OnInit: ... calls `Grind_SessionStep(InpMagic, InpSlot, InpSessionEnable, TimeGMT(), false);` before `EventSetTimer(1);`, after config dump, archive init, carry snapshot. Actually called directly before EventSetTimer. It then Print. This is in OnInit. However G5 says OnInit `:253`, timer flag. Need check: In given code, `Grind_SessionStep` call is before EventSetTimer, after Archive/Carry. Timer not set yet but call with from_tick false. Good.
- OnTimer: calls `Grind_SessionStep(... false)` after ArchiveFlush. Then telemetry block. So every second transition.
- OnTick: calls `Grind_SessionStep(... true)` after CloseBy, invariant/halt cancel, eject, auto eject, before BreakerOnTick and before halted/quarantined returns. Actually code: OnTick:
  - ProcessCloseByQueues
  - if !halted { invariant/quarantine; if halt cancel entries}
  - EjectPollCommand, AutoEjectOnTick
  - Grind_SessionStep(... true)
  - Grind_BreakerOnTick
  - if halted return
  - if quarantined { retry exits; return}
  - OnTickEngine
So session step runs even if halted/quarantined (before returns), and it can cancel entries. Good? It says from OnTick before breaker and before halted/quarantined returns. Yes.

`Grind_SessionStep` in engine:
```
void Grind_SessionStep(const ulong magic, const string slot, const bool enabled,
                       const datetime gmt, const bool from_tick)
{
   g_grind_session_enabled = enabled;
   if(!enabled) {
      g_grind_session_closed = false;
      return;
   }

   const bool was = g_grind_session_closed;
   g_grind_session_closed = !Grind_SessionOpenAt(gmt);
   const string key = Grind_FtmoDayKey(gmt);
   const int off = Grind_TorontoUtcOffset(gmt);

   if(!g_grind_session_closed) {
      if(was) {
         const string detail = "{\"utc_offset\":" + IntegerToString(off) + "}";
         Grind_ArchiveMarker("INFO", "SESSION_OPEN", key, 0, detail);
      }
      return;
   }

   const int resting = Grind_OwnRestingEntryCount(magic, slot);

   if(!was) {
      g_grind_session_closed_since = gmt;
      g_grind_session_stuck_warned = false;
      const string detail =
         "{\"utc_offset\":" + IntegerToString(off) +
         ",\"resting_ent\":" + IntegerToString(resting) + "}";
      Grind_ArchiveMarker("INFO", "SESSION_CLOSE", key, 0, detail);
      if(resting > 0) {
         Grind_CancelOwnEntryOrders(magic, slot);
         g_grind_session_last_cancel = gmt;
      }
      return;
   }

   if(resting > 0 && from_tick
      && gmt - g_grind_session_last_cancel >= GRIND_SESSION_CANCEL_RETRY_SEC) {
      Grind_CancelOwnEntryOrders(magic, slot);
      g_grind_session_last_cancel = gmt;
   }

   const int still = Grind_OwnRestingEntryCount(magic, slot);
   if(!g_grind_session_stuck_warned
      && gmt - g_grind_session_closed_since >= GRIND_SESSION_STUCK_WARN_SEC
      && still > 0) {
      ...
   }
}
```

Potential bugs:
- `g_grind_session_last_cancel` initialized 0. On first transition to closed, if resting>0, sets last_cancel=gmt. If resting==0, last_cancel remains 0. Then if later resting becomes >0 while closed? Can it? Entries blocked; placement blocked by Grind_EntriesBlocked at sites. But fill-time placement? `Grind_TryPlaceAddAtFill` checks Grind_EntriesBlocked. `Grind_OnTradeTransactionEngine` calls `Grind_HandleSideDealFill` and `Grind_TryPlaceAddAtFill`; if closed, blocked. However what about fills of existing entries? At close, if an existing ENT fills, it creates a position and then `Grind_TryPlaceAddAtFill` is blocked. But the pending entry order is removed by fill. No new resting entry. But add? If a straddle L0 fills after close? The cancel should have removed it at close. Race: if fill happens before cancel, then layer exists, no pending entry. `Grind_ExitQManageSide` places EXT, OK. `Grind_EnsureAddNext` not called from fill? `Grind_TryPlaceAddAtFill` checks blocked. So no add. Later OnTickEngine runs but EntriesBlocked prevents entries. So resting should remain 0. But there is possible if cancel failed and retry sets last_cancel. If resting==0 at transition and later a pre-existing ENT order appears? Not likely.

- Critical: On transition to closed, if `resting == 0`, no cancel, `last_cancel` stays 0. Later if a resting ENT appears (e.g., due to an order placed just before close but not yet visible? Actually if placed before close, count should see it if OrdersTotal includes it; but if order is pending in broker asynchronously? MT5 order send returns ticket; it should appear. But if placed by another instance? Own magic/slot filters. Could an ENT be placed after transition? `Grind_EntriesBlocked` prevents all sites. But race between the step that sets closed and a placement in the same tick/OnTick? OnTick calls SessionStep before OnTickEngine, so same tick after close, OnTickEngine sees EntriesBlocked. But OnTradeTransaction can occur asynchronously between SessionStep and OnTickEngine? OnTradeTransaction is a separate event, can run at any time? In MQL5, OnTradeTransaction runs when trade transaction occurs, interleaved with OnTick? It can interrupt? Actually event handlers are serialized; OnTradeTransaction is called when a trade transaction event occurs, possibly between other events. It could call `Grind_HandleSideDealFill` and `Grind_TryPlaceAddAtFill` which checks EntriesBlocked. So blocked if session closed flag set. But what if OnTradeTransaction runs before SessionStep at 16:55:00? The first step at 16:55:00 might be timer at 16:55:00. A tick at exactly 16:54:59.999 might place entry; then at 16:55:00 timer closes and cancels. So bounded. The gap between 16:55:00 and next step: the close transition happens on timer every second at 16:55:00 (assuming timer runs). If timer delayed, a tick might place between actual 16:55:00 and first step. But OnTick calls SessionStep before OnTickEngine, so any OnTick at/after 16:55 first closes then blocks. However if a trade transaction fill occurs before OnTick after 16:55? Fill-time placement checks EntriesBlocked. But EntriesBlocked only true after SessionStep ran. If no OnTimer and no OnTick yet, OnTradeTransaction could run and `Grind_TryPlaceAddAtFill` sees session closed flag still false, so places an add. That's leakage between 16:55:00 and first step. Also `Grind_HandleSideDealFill` may call `Grind_TryPlaceAddAtFill` only for ENT fills; fill-time adds. So an entry can be placed after 16:55 before first session step if a fill occurs in that gap. Bounded? Could be one add per fill? Since while closed, later steps cancel. The design says tick path re-places after block clears; but leakage before first step. We need assess T-2. Also any other path? `Grind_OnTradeTransactionEngine` calls `Grind_HandleSideDealFill` for both sides. For ENT fill, if `g_grind_fill_time_place` true, it may call `Grind_SendNextAddEnt` or `Grind_ApplyEntryHorizon`, both check `Grind_EntriesBlocked`. So if session flag not yet set, leak. But if `g_grind_fill_time_place` false (default? preset opts in maybe), `Grind_TryPlaceAddAtFill` returns immediately. Fleet B presets cycle 3? InpFillTimePlace likely true? The ADR-152 input default false, preset opts in. So on Fleet B maybe true. So leak possible.

- Also `OnInit` calls `Grind_SessionStep` after `Grind_ReconstructState` and `Grind_RetryMissingExits`. If EA starts while closed, it will set session closed and cancel entries. But `Grind_ReconstructState` might set `g_grind_long.l0_pending_ticket` from existing orders; then SessionStep cancels. Good. But what about `Grind_RetryMissingExits` before SessionStep? It only places EXT. Fine.

- `OnInit` calls SessionStep with `from_tick=false`. If closed and resting entries, it cancels. Good. But if enabled and open, no marker (was false? Initially g_grind_session_closed=false. `was=false`, `g_grind_session_closed=false`, open, so no SESSION_OPEN marker. Only if was true. On start open, no marker. That's okay? Design says if now open: if was, marker. So initial open silent. Test SW9 initial open silent.

- `OnTimer` calls SessionStep with `TimeGMT()`. `OnTick` also `TimeGMT()`. Good.

- `Grind_OwnRestingEntryCount` and `Grind_CancelOwnEntryOrders` filters: live path:
  - iterate `OrdersTotal()-1` down
  - `OrderGetTicket(i)`, `OrderSelect(ticket)`
  - `OrderGetString(ORDER_SYMBOL) != _Symbol` skip
  - `Grind_MagicMatches(OrderGetInteger(ORDER_MAGIC), magic)`
  - parse comment
  - `c_role != "ENT" || c_slot != slot` skip
  - cancel.
  Same filters for count. For test path: filters magic, parse, role ENT, slot. Does not filter symbol. Test records don't have symbol. In live, symbol filter. Good.

Potential T-3: Can a live instance own an ENT order that `GrindCommentParse` rejects? Comments are built by `GrindCommentBuild` with max length 31. `GRIND_COMMENT_MAX_LEN 31`. Build: "GRIND|" + slot + "|" + side + "|" + Lnn + "|" + role. Slot default "OPT" (3) => 6+3+1+1+1+3? Let's count: "GRIND|" 6, slot 3 =9, "|" 1=10, side 1=11, "|"=12, "L00"=3 =>15, "|"=16, role "ENT"=3 =>19. So under 31. But slot could be longer? Input InpSlot string. Preset slots likely "OPT", "ALT". If someone sets slot longer, `GrindCommentBuild` prints FATAL if >31 but still returns the too-long comment. Broker may truncate? MQL5 pending order comment max length? Broker can truncate to 31 chars? The code checks. If slot is long, comment >31, broker may truncate or reject. If truncated, parse fails; then CancelOwnEntryOrders misses it and OwnRestingEntryCount misses it, so no WARN. Could a live instance own such an order? Yes if InpSlot makes comment >31. But are slots constrained? Not in code; presets use "OPT"/"ALT". The ADR says eleven instances one per magic; two pairs second slot "ALT". So slot length 3. But user asks T-3: Can a live instance own an ENT order that GrindCommentParse rejects? If broker rewrites/truncates comments. MT5 brokers often modify comments? They can truncate to 31, but if comment exactly 31 fine. If broker appends something? Generally pending order comments may be modified by broker? Some brokers add prefixes/suffixes? The code relies on comment parse. If broker modifies, reconstruction also fails. But in this system, comments are already used for reconstruction and order ownership. If broker rewrites, many things break. ADR-161's cancel uses same parse as reconstruction, so if parse fails, the order is invisible to cancel and count. Could be a live instance owns one? The order was placed by this EA with a valid comment, but broker may truncate/rewrite. For slot length <=31, parse should work. But there's a subtle bug: `GrindCommentParse` uses `StringSplit(comment, '|', parts)`. If comment has extra text after role, it still parses role from parts[4] and ignores extra? Actually role_field = parts[4]; if broker appends, parts[4] may still be "ENT" or "ENT..."? `GrindCommentRoleMatches` checks prefix. If broker appends to role, still matches. If broker truncates, may fail. If broker adds prefix, parts[0] != "GRIND" fail. So if broker rewrites, cancel misses. But is that a new break? ADR-161 depends on existing parse. The threat says "a comment the broker truncates or rewrites, a different slot or symbol, a magic mismatch. Can a live instance own an ENT order that GrindCommentParse rejects?" Yes, if broker modifies comment. But the system already requires parse for reconstruction and exits. However ADR-161's cancel is supposed to cancel every ENT order of this magic and slot. If parse fails, it misses. Smallest fix: also cancel by magic and maybe known ticket? But without comment cannot know slot/role. Could use pending tickets stored in state (`l0_pending_ticket`, `add_pending_ticket`) plus magic, but if state lost? The design uses parse. To fix, maybe on placement store ticket and use state to cancel even if parse fails; or widen filter to cancel any order of this magic that is not an EXT? But EXT role also parse. If parse fails, can't distinguish ENT/EXT. Need not redesign. But likely this is a known limitation of comment-based system, not new. The ADR's T-3 asks can a live instance own an ENT order that GrindCommentParse rejects. I think verdict BREAKS/NEEDS-FIX: if broker truncates/rewrites, cancel misses and no WARN. But is it in scope? The code uses same filters as cancel; no additional robustness. The smallest fix: in `Grind_CancelOwnEntryOrders` and `Grind_OwnRestingEntryCount`, if `GrindCommentParse` fails but `Grind_MagicMatches` and ticket matches a known pending entry ticket (`l0_pending_ticket`/`add_pending_ticket`), treat as ENT for this slot? But that only covers known tickets; if state lost, not. Or use order type and magic? Cannot know role. Maybe acceptable because broker truncation would already break reconstruction. But ADR-161's stated goal "cancel every role-ENT order of this magic and slot" is not met. Need decide.

- T-4 Clock. Need verify G1/G2. `Grind_TorontoUtcOffset`:
```
MqlDateTime dt;
TimeToStruct(gmt, dt);
const datetime dst_start = Grind_NthSundayMonthUtc(dt.year, 3, 2, 7);
const datetime dst_end = Grind_NthSundayMonthUtc(dt.year, 11, 1, 6);
if(gmt >= dst_start && gmt < dst_end) return -4; return -5;
```
`Grind_NthSundayMonthUtc`:
```
dt.year=year; dt.mon=month; dt.day=1; dt.hour=hour_utc; ...
datetime t = StructToTime(dt);
while(true) { TimeToStruct(t, dt); if(dt.day_of_week == 0) break; t += 86400; }
if(n > 1) t += (n-1)*7*86400;
return t;
```
This finds first Sunday at hour_utc, then adds weeks. For March second Sunday 07:00 UTC, November first Sunday 06:00 UTC. Correct for Toronto? DST starts second Sunday in March at 2:00 local (EST) = 07:00 UTC. DST ends first Sunday in November at 2:00 local (EDT) = 06:00 UTC. Correct. For years where month starts on Sunday, day 1 is Sunday, hour set, day_of_week 0, returns day 1. For n=1 November, first Sunday. Good.

`Grind_SessionOpenAt`:
```
const int off = Grind_TorontoUtcOffset(gmt);
const datetime local_stamp = gmt + off * 3600;
MqlDateTime dt; TimeToStruct(local_stamp, dt);
if(dt.day_of_week < 1 || dt.day_of_week > 5) return false;
const long local = (long)gmt + (long)off * 3600L;
const int sec = (int)(local % 86400L);
return (sec >= 25200 && sec < 60900);
```
Check negative offset and `%` in MQL5. `local` is long. `local % 86400L`. If `local` is negative (before 1970?) no, gmt current. `gmt` is datetime (uint?). In MQL5, `datetime` is 64-bit integer, unsigned? Actually `datetime` is 64-bit integer, type for date/time, unsigned? In MQL5, datetime is `long`? I think it's 64-bit integer. `gmt + off*3600` with off=-5 gives positive for modern dates. `local % 86400` positive. For negative gmt maybe but not relevant. However using `%` on `long` with negative could yield negative remainder in C, but local positive. So fine.

But potential bug: `Grind_SessionOpenAt` uses local `day_of_week` from `TimeToStruct(local_stamp)`. Since `local_stamp` is a datetime value, `TimeToStruct` interprets it as UTC? Actually `TimeToStruct` takes a datetime and populates MqlDateTime fields. It does not apply timezone; it just decomposes the integer as if it's a datetime in server? In MQL5, `TimeToStruct` converts a datetime to MqlDateTime; it doesn't consider timezone. So if `local_stamp = gmt + off*3600`, it decomposes that shifted value. That's correct for local calendar. Good.

DST edges: At 2026-03-08 06:59:59, offset -5 => local 01:59:59, Sunday, closed. At 07:00:00, offset -4 => local 03:00:00? Wait 07:00 UTC - 4 = 03:00 local. Session opens at 07:00 local, so still closed. The test SW3? They have `SW3 thu 07:00 open` 2026.09.24 11:00:00 UTC = 07:00 EDT. For DST transition, at 07:00 UTC shift jumps from 02:00 EST to 03:00 EDT. So local 03:00, before 07:00, closed. At 11:00 UTC = 07:00 EDT open. So no issue.

But what about `Grind_TorontoUtcOffset` year boundaries: It uses `dt.year` of gmt. For gmt in January, year correct. DST start/end for that year. Good. For gmt exactly at year boundary? Jan 1, offset -5. Correct.

Potential issue: `Grind_NthSundayMonthUtc` uses `StructToTime(dt)` with `dt` possibly containing uninitialized fields? It sets year, mon, day, hour, min, sec. Other fields (day_of_week, day_of_year, etc.) are not set. `StructToTime` may ignore them? In MQL5, `StructToTime` uses fields year, mon, day, hour, min, sec. Uninitialized day_of_week etc. ignored. But `dt` is not zeroed before setting fields. In MQL5, local struct variables are not automatically zero-initialized? Actually MqlDateTime is a structure; local variables may be uninitialized. `StructToTime` likely only uses the fields. But to be safe, should ZeroMemory(dt). However `Grind_LastSundayMonthUtc` in existing code also doesn't zero. So not new.

Potential issue: `Grind_TorontoUtcOffset` returns -4 if gmt >= dst_start && gmt < dst_end. But `dst_end` is 06:00 UTC first Sunday November. At exactly 06:00 UTC, offset -5. Correct. At exactly dst_start 07:00 UTC, offset -4. Correct.

Potential issue: `Grind_SessionOpenAt` uses `local_stamp = gmt + off * 3600`. `off` is int. `off * 3600` int. If gmt is datetime (ulong?), addition yields datetime? In MQL5, `datetime` is `long`? It is 8-byte integer. Adding int works. `local_stamp` is datetime. Then `TimeToStruct(local_stamp, dt)`. But `local_stamp` may be out of valid datetime range? No.

T-4 Wrong offset or window on any date: Need check month starting on Sunday. `Grind_NthSundayMonthUtc` starts at day 1. If day 1 is Sunday, while loop breaks immediately, returns day 1 at hour_utc. For n=2, adds 7 days. Correct. For November first Sunday, if Nov 1 is Sunday, returns Nov 1 06:00. Correct.

Year boundaries: If gmt is Dec 31 and dst? Offset -5. Correct. If gmt is Jan 1, year = new year, dst_start March new year. Correct.

What would operator see if offset wrong? Print line in OnInit: `GRIND_SESSION enable=... toronto_utc_offset=... open_now=...`. If offset wrong, they'd see -4 in winter or -5 in summer, and open_now mismatch. But the print is only at OnInit, not continuously. The archive markers include utc_offset at SESSION_CLOSE/OPEN. They can see.

Potential issue: `TimeGMT()` under Wine or wrong terminal timezone. In MQL5, `TimeGMT()` returns GMT based on local PC time and timezone settings? Actually `TimeGMT()` returns the current GMT time, calculated from local time and timezone settings of the terminal/PC. If the terminal's timezone is wrong, TimeGMT may be wrong. The ADR says VPS and Linux box clocks are UTC. But if Wine's timezone is not UTC, TimeGMT might be off. The code doesn't use `TimeGMT()` for session open? It does. If wrong, session window shifts. Operator sees print. Not a code bug per se. But could be a break if box not UTC. Given says Linux/Wine box, clock UTC. So assume.

T-5 Restart and reinit. On reinit (compile, reattach, chart change), `OnInit` runs. Globals: `g_grind_session_enabled`, `g_grind_session_closed`, etc. are global variables in engine. In MQL5, global variables are reset on reinit? Actually EA global variables are destroyed and recreated on reinit? When an EA is removed and reattached, global variables are reinitialized to their initial values (false/0). On recompile, the EA is removed and reattached, so yes. But if only parameter change? It may call OnDeinit and OnInit, globals reset? In MQL5, when you change inputs, the EA is reinitialized; global variables are reinitialized to default? I believe global variables are initialized only once at program start; on reinit, they may retain values? Actually MQL5: "When the program is reinitialized, global variables are not reinitialized. They keep their values." Wait, need recall. In MQL5, global variables and static variables are initialized before OnInit. On reinit (e.g., changing timeframe, inputs), the program is not unloaded? Actually it calls OnDeinit then OnInit. Global variables are not reset automatically. I think global variables retain their values on reinit. Let's verify: MQL5 documentation: "Global variables are initialized before OnInit. If the program is reinitialized, global variables are not reinitialized." I think that's true for static? Actually, for EAs, when inputs are changed, the EA is reinitialized; global variables are initialized again? Hmm. In MQL4, global variables are reset on reinit? I recall that in MQL5, global variables are not reset on reinit? Let's think. The code in OnInit sets many globals: `g_grind_halted = false; g_grind_cap_blocked = false;` etc. It doesn't reset `g_grind_session_closed`. So if reinit occurs while closed, `g_grind_session_closed` may remain true from before. Then OnInit calls `Grind_SessionStep` which computes `was = g_grind_session_closed` (true), `g_grind_session_closed = !Grind_SessionOpenAt(gmt)`. If still closed, `was=true`, so it will NOT take the `!was` transition block. It will skip archiving SESSION_CLOSE and skip the initial cancel. Then it goes to retry block: `resting > 0 && from_tick` — from_tick is false in OnInit, so no cancel. Then stuck warn maybe. So on reinit while closed, the first step does NOT archive SESSION_CLOSE and does NOT cancel resting entries. This is a break for T-5. It relies on globals resetting on reinit. If they don't reset, cancel is skipped. Does MQL5 reset globals on reinit? Need be accurate. In MQL5, "Global variables are not reinitialized during the program reinitialization." I think yes, they persist. Let's check memory: MQL5 variables: "If a program is reinitialized (e.g., when the timeframe is changed), global variables are not initialized again. They retain their values." Actually, I recall that global variables are initialized when the program is loaded, and on reinit the program is not unloaded? The terminal calls OnDeinit and OnInit without unloading the program? The global variables are not reinitialized. So this is a real break. The design's tests reset globals manually, so they don't catch. The smallest fix: on OnInit, before calling `Grind_SessionStep`, reset `g_grind_session_closed = false; g_grind_session_closed_since = 0; g_grind_session_last_cancel = 0; g_grind_session_stuck_warned = false;` or add a parameter to force transition. But ADR says globals reset on every reinit reason? The threat asks "Do the globals reset on every reinit reason?" The answer: no, they do not automatically. The code does not reset session globals in OnInit (unlike `g_grind_halted`, `g_grind_cap_blocked`, `g_grind_halt_reason` which are reset). So T-5 BREAKS/NEEDS-FIX. Smallest fix: In OnInit, before `Grind_SessionStep`, set `g_grind_session_closed = false;` (or add an explicit reset function) so `was` is false and transition path runs. But careful: if you set closed=false, then SessionStep computes was=false, if closed, it archives SESSION_CLOSE and cancels. If open, it does nothing (was false). That fixes reinit. But if reinit occurs while closed and session was already closed, you get a duplicate SESSION_CLOSE marker. The threat asks duplicate or missing markers. With reset, duplicate SESSION_CLOSE on every reinit while closed. Maybe acceptable? Or pass a flag to force. The smallest fix could be to reset `g_grind_session_closed=false` in OnInit. But then duplicate markers. Alternatively, in `Grind_SessionStep`, treat `g_grind_session_closed_since == 0` as not initialized? If `closed_since==0`, then even if was true, do transition. But on reinit, globals may persist closed_since. If we reset closed_since=0 in OnInit, then condition `!was || g_grind_session_closed_since == 0` would work. But design says no. We can suggest resetting session state in OnInit before the call. The ADR's D5 says call SessionStep in OnInit; it doesn't reset globals. So break.

But wait: Are global variables reset on reinit? Let's be certain. MQL5 documentation: "Global variables are available only inside the program they are defined in. They are initialized before OnInit()." On reinit, the program is not unloaded? The terminal calls OnDeinit() and then OnInit(). The global variables are not reinitialized. I remember that in MQL5, when you change inputs, the EA is reinitialized, and global variables are reinitialized? Actually, I've seen code that resets globals in OnInit because they are not reset on reinit. For example, `g_grind_halted = false;` in OnInit suggests they expect globals may persist? They explicitly reset some globals. So yes, they know globals persist. The session globals are not reset. So break.

T-6 Retry and budget. Bound cancel calls per instance per day. On transition: 1 cancel if resting>0. Retries: while closed, on ticks every 10s. If cancel fails, each tick after 10s. How many ticks per second? OnTick can be multiple per second? In FX, ticks can be many per second. The retry condition checks `gmt - last_cancel >= 10`. `gmt` is datetime seconds. If multiple ticks within same second, condition false after first cancel (last_cancel set to current second). But if tick flood over many seconds, at most one cancel per 10 seconds. However `gmt` is `TimeGMT()` with second resolution. If ticks occur at 10.1s and 10.2s, `gmt` integer seconds may be same? `TimeGMT()` returns datetime (seconds). So difference is integer seconds. It could allow a cancel at 10.0 and again at 20.0. So max ~1 per 10s while closed. Closed period per day: from 16:55 to 07:00 next day? Actually Monday-Friday 07:00-16:55 open. Closed periods: after 16:55 to next day 07:00 (about 14h05m = 50700s) on Mon-Thu; Friday 16:55 to Monday 07:00 (about 62h05m = 223500s). Plus weekends. If entries remain stuck (cancel fails), retries every 10s on ticks. But ticks only when market open. On Friday after 16:55, market closed, no ticks until Sunday 22:00? Actually FX market opens Sunday 17:00 ET. So no ticks over weekend. The retry loop only runs on ticks. So over weekend, no retries. On Sunday open, ticks resume, but session is closed until Monday 07:00. So it will retry every 10s from Sunday 22:00 to Monday 07:00 (9 hours = 32400s => ~3240 cancels). Per instance per day. That's a lot. But only if cancel keeps failing and entries rest. The API counter may count order send operations. Each cancel is a TRADE_ACTION_REMOVE via `Grind_CancelPendingOrder` -> `Grind_OrderEngineSend` -> `Grind_OrderSendCounted` (API counter). If cancel fails with network error, it may still count? The API counter tracks entry stop? Need check `grind_api_counter.mqh` not provided. But likely counts order sends. 3240 cancels per instance over weekend night could hit API limits. Also the WARN only once. The design says retry only on ticks, every 10s. Bound: For a closed period with market open: maximum ~1 per 10s. For 9 hours, 3240. For 11 instances, ~35k. Could starve or flood. But is this realistic? If cancel fails, it's likely due to market closed at 16:55; on Monday morning it's open, cancel should succeed. But if it fails due to symbol trade mode disabled? Then no ticks? If market open but cancels fail, yes. The threat asks "Can the retry loop or the WARN starve or flood anything (API counter entry stop, archive queue)?" The retry loop does not archive except WARN once. It calls `Grind_CancelOwnEntryOrders` which calls `Grind_CancelPendingOrder` for each resting ENT. Each cancel attempt may go through `Grind_OrderEngineSend` and `Grind_OrderSendCounted`. If the API counter has an entry stop, it might stop entries, but cancels? `Grind_ApiCounterEntryStopped()` blocks entries, not cancels. But the API counter may count all trade requests. Need inspect `grind_api_counter.mqh` not provided. The threat says "API counter entry stop" — maybe `Grind_ApiCounterEntryStopped()` only affects entries. The cancel calls could consume API budget. There is no backoff beyond 10s. Smallest fix: exponential backoff or cap retries per closure, or only retry on new tick after a longer interval. But design ruled "tick-only retries, at most every 10 s". So not to redesign. However if it can flood, it's a break. We can bound: worst case per instance per closed period with continuous ticks: one cancel per 10s. For Friday close to Monday open, no ticks until Sunday. Sunday 22:00 to Monday 07:00 = 9h = 3240 attempts. If each attempt cancels multiple orders, say 2 entries, 6480 order sends. This could be significant. But if cancel succeeds, it stops. If it fails, the WARN at 300s. The design intended retries only until success. The threat "failing cancel at the close and the weekend" — if cancel fails at Friday close, no ticks until Sunday, so no retries over weekend. Then Sunday open, it retries every 10s. That's the worst. Could flood API counter. The smallest fix: increase retry interval or use a backoff, but design ruled constants. Could skip retries when market is not open? The retry only on ticks, so it only retries when market open. But Sunday 22:00 market open, session closed. So it retries. This is intended to cancel stuck entries before Monday open. But 3240 attempts is a lot. Maybe acceptable? The API counter may have limits. Need check `Grind_OrderSendCounted`. Not provided. We can flag NEEDS-FIX: add a maximum retry count per closure or exponential backoff, or rely on Friday close no ticks and Monday open first tick. Actually Monday 07:00 session opens; if entries still resting, they would be cancelled? Wait, session opens at 07:00 Monday. At 07:00, `Grind_SessionOpenAt` true, so `g_grind_session_closed=false`. The transition to open archives SESSION_OPEN, but does NOT cancel entries. The block clears, so new entries can be placed. But the stuck entries from Friday would still be resting! If they weren't cancelled over the weekend, at Monday 07:00 the session opens and they remain. That's a severe break: the design says outside window resting entries cancelled. If cancel fails at Friday close and no ticks over weekend, then at Monday 07:00 the session opens and the stuck entries are still resting. The retry loop only runs while closed. Once open, it stops retrying and does not cancel. The entries are now inside the window? Wait, they were placed before Friday 16:55. They are resting entries. At Monday 07:00, the session opens. The rule: new entries only 07:00-16:55. Resting entries outside are cancelled. But at Monday 07:00, we are inside the window, so resting entries are allowed? The ADR says outside the window this instance's resting entries are cancelled. It doesn't say they are cancelled at open. But if they weren't cancelled over the weekend, they persisted through the closed period. At Monday 07:00, the session is open, so they are no longer outside the window. They could fill during Monday session. That violates the intent: entries should not have rested outside the window. The design's retry only on ticks: if no ticks over weekend, no retry. At Monday 07:00 first tick, `SessionStep` sets open, returns before cancel. So stuck entries survive. This is a major break. Let's trace: Friday 16:55, session closes, tries cancel. Suppose cancel fails (market closed, rollover). `last_cancel = Friday 16:55`. No more ticks until Sunday 22:00. At Sunday 22:00, market opens, session is closed (weekend), so `from_tick=true`, `resting>0`, `gmt - last_cancel >= 10` (yes, days), so it cancels. If cancel succeeds, entries gone. If cancel fails again, it retries every 10s until Monday 07:00. At Monday 07:00, `Grind_SessionOpenAt` becomes true. `was=true` (closed), so it archives SESSION_OPEN and returns. It does NOT cancel. So if all retries from Sunday 22:00 to Monday 07:00 failed, entries remain. That's a window leak: entries rest inside the open window that were supposed to be cancelled. The design says "outside the window this instance's resting entries are cancelled". If they remain into the open window, they are not cancelled. Is that a break? The threat T-6 asks "Bound the cancel calls per instance per day, including a failing cancel at the close and the weekend (no ticks)." This is exactly the issue: no ticks over weekend, but Sunday open gives retries. However if cancel fails continuously, at open it stops. The entries then rest. The smallest fix: on transition to open, if there are resting entries that should have been cancelled? But the design says on open it just archives and returns. Could change to: if now open and was closed, and resting entries exist, cancel them before opening? But that would cancel entries that were placed? Actually if they survived the closed period, they should be cancelled because they were placed while... wait, they were placed before close. The rule says outside window resting entries are cancelled. At Monday 07:00, they are inside window, so maybe they are allowed? The rule says new entries only 07:00-16:55. It doesn't say existing entries are cancelled at open. If they survived the closed period, they are existing entries. The design intended to cancel them at close. If cancel failed, they survive. Is that acceptable? The design says "outside the window this instance's resting entries (L0 AND adds) are cancelled". If they are not cancelled, the rule is violated for the closed period. At Monday 07:00, they are no longer outside, but they did rest outside. The operator wanted no resting entries outside. So it's a break. But the design's retry strategy relies on ticks during closed period. Sunday 22:00 to Monday 07:00 provides ticks. If cancels fail for 9 hours, that's extreme. But possible if market is open but broker rejects cancels? Why would it reject? Maybe because order is frozen? Or API counter? The retry loop would keep trying. If it never succeeds, the WARN is only once. At Monday open, it stops. The smallest fix: on transition to open, if `was` and `resting > 0`, cancel before archiving SESSION_OPEN? But then if entries were re-placed? They can't be placed while closed. So any resting entries at open are stuck from before. Cancelling them is consistent with the rule? Actually at open, they are allowed. But they were supposed to be cancelled. To enforce, cancel on open if they existed at close. But the design doesn't track a flag. Could use `g_grind_session_closed_since > 0` and resting>0 at open -> cancel. However if the EA started while closed and no entries, then open, no cancel. If entries were placed before close and cancel failed, at open cancel them. That would fix leak. But it adds a cancel at open. The ADR says no design re-open. But we can suggest smallest fix. Alternatively, the retry loop could continue while closed even without ticks? But no ticks, no timer? Timer runs every second even when market closed. Wait! OnTimer runs every second regardless of ticks. The design says retries run only from ticks: "a tick proves the market is open, so a cancel that failed at the close (rollover break, Friday close) is not retried into a closed market all weekend. The timer's role is the transition, so a quiet pair still cancels at 16:55:00." So timer does not retry. But OnTimer still runs every second even on weekend. Could use timer to retry? The design explicitly says tick-only retries. So no. The break is that if cancel fails at Friday close, no retries until Sunday open. Then Sunday open to Monday open, retries. If cancel keeps failing, at Monday open it stops and entries rest. The smallest fix: continue retrying on timer when closed? But design ruled tick-only. Alternatively, on transition to open, if `was` and resting>0, cancel. That is a small change within D4. But does it violate "tick-only retries"? It would be a one-time cancel at open, not a retry. Could be acceptable. Need mention.

Also, what about the 300s WARN? It uses `gmt - closed_since >= 300`. If closed_since set at Friday 16:55, then on Sunday 22:00, it's >300, so it will warn then. The WARN is once per closed period. Fine.

T-7 Interactions. With breaker's own cancel latch, ADR-160 gate, halted/quarantined, API entry stop, 07:00 re-placement. Need check ordering.

- Breaker: `Grind_BreakerOnTick` runs after `Grind_SessionStep` in OnTick. Both can cancel entries. Breaker has `g_grind_breaker_cancel_done` per FTMO day. If breaker trips and cancels, it sets done. Session step also cancels if closed. They don't double-cancel because both clear pending tickets. But if breaker cancels first? Order in OnTick: SessionStep then BreakerOnTick. If session closed, SessionStep cancels, clears pending. Then BreakerOnTick may also cancel? Breaker only cancels if `g_grind_breaker_tripped && !g_grind_breaker_cancel_done`. It calls `Grind_CancelOwnEntryOrders`. If entries already gone, it does nothing but sets `g_grind_breaker_cancel_done = true`. That's fine. If breaker trips while session open, breaker cancels; session not closed, no interference. If session closed and breaker trips, session cancels first. No double.

- ADR-160 gate: `Grind_EntriesBlocked` includes API stop and breaker and session. The gate likely uses `Grind_EntriesBlocked`? Not sure. But session block is independent. The 07:00 re-placement: OnTickEngine places L0 and adds. If session opens at 07:00, `Grind_SessionStep` sets closed=false, EntriesBlocked false. Then OnTickEngine places L0 and adds. But what about stale `l0_pending_ticket`/`add_pending_ticket`? At close, `Grind_CancelOwnEntryOrders` clears them if cancel succeeds. If cancel fails, it leaves them. Then `Grind_OwnRestingEntryCount` counts them. On retry, it tries again. If eventually succeeds, clears. If session opens with stuck entries, `OnTickEngine` will run. `Grind_TryPlaceL0` checks `side.l0_pending_ticket != 0` and if `Grind_SelectOurOrder` true, returns false (leave it). So a stuck L0 remains, and no new L0 is placed. That might be okay? It's an existing entry. But it should have been cancelled. If it fills, okay. If it's an add, `Grind_EnsureAddNext` checks `add_pending_ticket` and may cancel if label stale or modify. So stuck add may be modified. Not a double-cancel.

- Halted/quarantined: `OnTick` calls SessionStep before halted/quarantined returns. So even if halted, SessionStep can cancel entries. But if halted, `OnTick` earlier in the `if(!g_grind_halted)` block may already call `Grind_CancelOwnEntryOrders` on invariant fail. Then SessionStep also cancels. Double cancel? If halted due to invariant, `Grind_CancelOwnEntryOrders` called. Then SessionStep runs, if closed, it counts resting (0) and if transition? If closed and was false, it archives SESSION_CLOSE, resting=0, no cancel. If was true, resting=0, no cancel. So no double. If session closed and then halt occurs, SessionStep may cancel first, then halt cancel. If halt cancel first? OnTick order: CloseBy, then invariant block (can cancel), then eject, then SessionStep. So if halt occurs, it cancels entries, then SessionStep. If session was closed and was false, SessionStep sees resting=0, archives SESSION_CLOSE, no cancel. Fine. If session was already closed and halt occurs, SessionStep may retry cancel if resting>0 and from_tick. But halt cancel already cleared. So no double.

- API entry stop: `Grind_EntriesBlocked` includes API stop. SessionStep does not check API stop; it only checks session. So even if API entry stop active, session step will cancel entries when closed. That's fine. But `Grind_CancelOwnEntryOrders` does not check API stop. Good.

- 07:00 re-placement: At session open, `Grind_SessionStep` sets closed=false. Then `OnTickEngine` runs. It will place L0 if flat. But what if `g_grind_ent_sent_this_tick` is true from before? OnTickEngine resets it at start. So fine. What about `g_grind_session_last_cancel`? Not reset. If session opens, then closes again later same day? It will be set on new transition if resting>0. If resting=0, last_cancel remains old. Then if later resting>0 while closed? Not possible. So fine.

- Double-cancel with breaker latch: Breaker cancel done per day. If session closes multiple times per day? The session window is Mon-Fri 07:00-16:55. There is only one close per day (16:55) and one open (07:00). On weekends, closed all day. The transition to closed on Friday 16:55, then stays closed until Monday 07:00. So one transition per week? Actually Monday 07:00 open, Monday 16:55 close, Tuesday 07:00 open, etc. So one close per weekday. The breaker cancel is per FTMO day (Prague?). Session close at 16:55 Toronto may fall in different FTMO day? FTMO day key uses Prague offset. Toronto 16:55 EDT = 20:55 UTC. Prague is UTC+2 in summer, so 22:55 Prague, same day. In winter, Toronto 16:55 EST = 21:55 UTC, Prague UTC+1 = 22:55, same day. So same FTMO day. No conflict.

- Stale pending tickets: If `Grind_CancelOwnEntryOrders` succeeds, it clears. If fails and order still exists, it leaves. On retry, it will try again. If order disappeared (filled/cancelled externally), `Grind_SelectOurOrder` false, then it clears pending. Good.

- Invariant: `Grind_CheckBookInvariants` may consider pending tickets? Not sure.

T-8 Default OFF inert. With `InpSessionEnable=false`, `Grind_SessionStep` sets `g_grind_session_enabled=false`, `g_grind_session_closed=false`, returns. `Grind_SessionBlocksEntries` returns false. `Grind_EntriesBlocked` unchanged. No markers. But wait: `OnInit` still calls `Grind_SessionStep` and then `Print("GRIND_SESSION enable=...")`. That Print is new output even when disabled. Also `OnTimer` and `OnTick` call `Grind_SessionStep` every time, which with disabled does two assignments and returns. Minimal overhead. Order traffic unchanged. But the Print at every OnInit is new log line. Behavior: no order changes. So inert except log print. The ADR says "default OFF is inert" — it changes log output but not order traffic. The question: "With InpSessionEnable = false, show the new code changes nothing in behaviour or order traffic". The Print is a behavior change (log). Also the new input appears in config dump? `Grind_ConfigDumpString` does not include InpSessionEnable. The archive config fields do not include it. So no config dump change. The Print is extra. Could flag NEEDS-FIX? Probably acceptable? The rule says default OFF inert; a log line is not inert. But maybe they mean trading behavior. We can note it's not completely inert: OnInit prints a GRIND_SESSION line even when disabled. Also `Grind_SessionStep` is called in OnTimer/OnTick, so it sets `g_grind_session_enabled=false` and `g_grind_session_closed=false` every call. That could interfere if some other code sets them? Only session code. No. So order traffic unchanged. Verdict: HOLDS for order traffic, but note the extra log line. Could be NEEDS-FIX if strict.

Now Givens check. Need verify G1-G5 with line numbers. The prompt gives line numbers in Givens. We need check if correct based on code.

G1: `Grind_TorontoUtcOffset` (`grind_pure.mqh:479`) returns -4 from 2nd Sunday March 07:00Z up to (not including) 1st Sunday November 06:00Z, else -5, via `Grind_NthSundayMonthUtc` (`:456`). In code, `Grind_NthSundayMonthUtc` is defined after defines. The line numbers might be off. We can say CONFIRMED (function body matches). But need cite line. The prompt says `:456`, `:479`. We can cite those. But we don't have actual line numbers; we can use given. We'll say CONFIRMED (as coded, `grind_pure.mqh:479-487` for Toronto, `:456-477` for Nth). Note: `Grind_TorontoUtcOffset` uses `dt.year` of gmt. Good.

G2: `Grind_SessionOpenAt` (`grind_pure.mqh:491`) uses LOCAL weekday (1..5) and local seconds in [25200, 60900). Code matches. CONFIRMED.

G3: `Grind_EntriesBlocked` (`grind_engine.mqh:859`) ORs `Grind_SessionBlocksEntries` (`:854`, enabled AND closed) with API stop and breaker; session block does not depend on `InpBreakerEnable`. Code matches. CONFIRMED.

G4: Every pending ENT order is placed by `Grind_PlaceLimit` from `Grind_TryPlaceL0` (`:1355`) or `Grind_SendNextAddEnt` (`:1617`), and every path to those passes an `if(Grind_EntriesBlocked()) return` at `:1109`, `:1335`, `:1544`, `:1648` or `:1759`. The other two `Grind_PlaceLimit` calls (`:1410`, `:2425`) place EXT orders. Need verify. In code, `Grind_PlaceLimit` calls:
1. `Grind_TryPlaceL0` calls `Grind_PlaceLimit` for L0 ENT.
2. `Grind_SendNextAddEnt` calls `Grind_PlaceLimit` for add ENT.
3. `Grind_TryPlaceExitForLayer` calls `Grind_PlaceLimit` for EXT.
4. `Grind_ExitQManageSide` calls `Grind_PlaceLimit` for EXT.
Are there other `Grind_PlaceLimit` calls? In `grind_engine.mqh`, I see `Grind_TryPlaceL0`, `Grind_SendNextAddEnt`, `Grind_TryPlaceExitForLayer`, `Grind_ExitQManageSide`. Also maybe in `grind_closeby.mqh`? No. So four calls. The given says other two at `:1410`, `:2425` place EXT. That matches `Grind_TryPlaceExitForLayer` and `Grind_ExitQManageSide`. Good.

Paths to ENT placements:
- `Grind_TryPlaceL0` itself checks `Grind_EntriesBlocked` at line after `g_grind_ent_sent_this_tick`. That is one.
- `Grind_SendNextAddEnt` checks `Grind_EntriesBlocked` at start.
- Callers of `Grind_TryPlaceL0`: only `Grind_OnTickEngine`? Actually `Grind_TryPlaceL0` is called in `Grind_OnTickEngine` directly. It may also be called from tests. In production, only `OnTickEngine`. But `OnTickEngine` is called from `OnTick` after session step. Does `OnTickEngine` itself check EntriesBlocked? No, but `Grind_TryPlaceL0` does. So path passes.
- Callers of `Grind_SendNextAddEnt`: `Grind_ApplyEntryHorizon`, `Grind_TryPlaceAddAtFill`, `Grind_ServiceDueAddFlags`, `Grind_EnsureAddNext`. Each of these checks `Grind_EntriesBlocked` before calling? Let's verify:
  - `Grind_ApplyEntryHorizon`: checks `if(Grind_EntriesBlocked()) return 0;` near start. Yes.
  - `Grind_TryPlaceAddAtFill`: checks `if(Grind_EntriesBlocked()) return;` at start. Yes.
  - `Grind_ServiceDueAddFlags`: calls `Grind_SendNextAddEnt` in else branch. Does it check EntriesBlocked? It does not directly. But it is called from `Grind_OnTickEngine` after `Grind_RetryMissingExits`. `Grind_OnTickEngine` is called from OnTick after SessionStep and after halted/quarantined returns. But `Grind_ServiceDueAddFlags` does not check EntriesBlocked before calling `Grind_SendNextAddEnt` (which does check at its start). So the path passes because `Grind_SendNextAddEnt` itself checks. The given says every path to those passes an `if(Grind_EntriesBlocked()) return` at the listed lines. That includes the checks inside the functions. So G4 is CONFIRMED. However note: `Grind_ServiceDueAddFlags` calls `Grind_EnsureAddNext` if horizon active, which checks. If not horizon active, it calls `Grind_SendNextAddEnt`, which checks. So all ENT placements go through a check. Good.

But wait: `Grind_TryPlaceL0` has `if(side.l0_pending_ticket != 0)` block before `Grind_EntriesBlocked` check. If there is an existing L0 pending ticket, it returns false without checking EntriesBlocked. That's not placing. Fine.

G5: `Grind_SessionStep` (`grind_engine.mqh:1033-1089`) is called from `OnInit` (`fxgrind.mq5:253`, timer flag), `OnTimer` every second (`:279`) and `OnTick` (`:323`, tick flag) BEFORE the breaker and before the halted/quarantined returns. On transition to closed it archives `SESSION_CLOSE` and calls `Grind_CancelOwnEntryOrders` (`:2439`) if own entries rest; while closed it retries only on ticks, at most every 10 s; after 300 s with entries still resting it archives `SESSION_CANCEL_STUCK` once. `Grind_OwnRestingEntryCount` (`:2513`) applies the same filters as the cancel. Need verify line numbers. In code, `Grind_SessionStep` is indeed in engine. OnInit call is before EventSetTimer. OnTimer call after ArchiveFlush. OnTick call after AutoEject and before BreakerOnTick. In OnTick, it is before `if(g_grind_halted) return;` and before quarantined return. So yes. On transition to closed it archives SESSION_CLOSE and cancels if resting>0. While closed retries only on ticks and >=10s. After 300s with entries still resting it archives WARN once. `Grind_OwnRestingEntryCount` same filters. CONFIRMED. But note: OnTimer call is every second, but `from_tick=false`, so no retries. OnTick call `from_tick=true`. Good.

Potential line number mismatch: The given says `Grind_SessionStep` at `:1033-1089`. In code, it appears after `Grind_BreakerOnTick`. The line numbers may be approximate. We'll say CONFIRMED.

Now T-1 Unwind stays open. Need confirm nothing in exits, exit queue, carry, CloseBy, manual/auto ejection consults `Grind_SessionBlocksEntries` or `Grind_EntriesBlocked`, and session code can never cancel or modify EXT order.

- Exits: `Grind_ExitQManageSide` places EXT via `Grind_PlaceLimit`. It does not check `Grind_EntriesBlocked`. `Grind_TryPlaceExitForLayer` does not check. `Grind_RetryMissingExits` calls `Grind_ExitQManageSide`. No session check. Carry: `Grind_CarryOnTimerStep` not shown; likely does not check session. Ejection: `Grind_EjectPollCommand` and `Grind_AutoEjectOnTick` check `engine_blocked` parameter passed as `g_grind_halted || g_grind_quarantined`. Not session. So they run. CloseBy: `Grind_ProcessCloseByQueues` runs at start of OnTick before session step. It can cancel entries? In `Grind_ProcessCloseByQueue`, on exhaustion it calls `Grind_CancelOwnEntryOrders(magic, g_grind_recon_slot)`. That cancels ENT orders, not EXT. It does not check session. That's fine; it's an independent cancel of entries, allowed? The threat says "Confirm nothing in exits, the exit queue, carry, CloseBy (`grind_closeby.mqh:170`, `:233` also cancel entries), manual or automatic ejection consults `Grind_SessionBlocksEntries` or `Grind_EntriesBlocked`". CloseBy calls `Grind_CancelOwnEntryOrders` but does not consult session. It cancels entries even if session is open/closed. That's existing behavior. Does that violate? No, it's not consulting session. It can cancel entries as part of CloseBy failure. The session code can never cancel or modify an EXT order: `Grind_CancelOwnEntryOrders` filters `c_role != "ENT"`, so it only cancels ENT. `Grind_OwnRestingEntryCount` only counts ENT. Session step does not call modify. So T-1 HOLDS. But note: `Grind_CancelOwnEntryOrders` is called by CloseBy and breaker and halt, but session code itself only calls it. It cannot cancel EXT. Good.

T-2 Leakage outside window. Need find any way entry rests or is placed while closed. We already identified fill-time placement race before first step. Also check `Grind_OnTickEngine` state re-armed. `Grind_OnTickEngine` calls `Grind_RetryMissingExits` (EXT), then `Grind_ServiceDueAddFlags`, then `Grind_ReconcileStrayL0`, then L0 placements. All entry placements check `Grind_EntriesBlocked` inside `Grind_TryPlaceL0` and `Grind_SendNextAddEnt`. But `Grind_ReconcileStrayL0` can cancel L0, not place. `Grind_TryRecenterOppositeL0` can modify an existing L0. It does not check `Grind_EntriesBlocked`. If session is closed, `Grind_OnTickEngine` is still called? Wait, OnTick: after SessionStep and Breaker, if not halted/quarantined, it calls `Grind_OnTickEngine`. So even when session closed, OnTickEngine runs. Inside, `Grind_TryRecenterOppositeL0` may modify a resting L0 order if it exists. But if session closed, the L0 should have been cancelled by SessionStep. However if cancel failed or if there is a race, an L0 might still rest. `Grind_TryRecenterOppositeL0` will modify it (re-center) while closed! That is a modification of an ENT order outside the window. The session code is supposed to cancel resting entries outside the window. If it failed, the engine could modify them because `Grind_OnTickEngine` doesn't check `Grind_EntriesBlocked` before calling `Grind_TryRecenterOppositeL0`. Let's trace: OnTick with session closed. SessionStep tries to cancel, fails. Then OnTickEngine runs. If side depth >0 and opposite flat, `Grind_TryRecenterOppositeL0` is called. It checks `opposite_side.l0_pending_ticket != 0` and `Grind_SelectOurOrder`. If the L0 is still resting, it may re-center it. That is a modification of an ENT order while closed. The ADR says outside window resting entries are cancelled. If cancel failed, they shouldn't be modified. But more importantly, `Grind_TryRecenterOppositeL0` does not check `Grind_EntriesBlocked`. However, if session closed, `Grind_EntriesBlocked` is true, but `Grind_TryRecenterOppositeL0` is not an entry placement, it's a modification. The design says "resting entries are cancelled"; it doesn't say they can't be modified. But modifying them keeps them alive, possibly re-centering them to market. That seems bad. But is it a leak? It doesn't place new entries, but it keeps an entry order alive. If cancel failed, the entry is still there. The session retry will try to cancel again on next tick. But between retries, the engine may modify it. That could interfere with cancel? Not directly. But it's outside the window. The threat says "Find ANY way an entry rests or is placed while closed". An entry rests if cancel fails; the engine modifying it doesn't make it rest more, but it could prevent it from being cancelled? No. However, `Grind_TryRecenterOppositeL0` could also be called when session is closed and there is an L0 that was placed before close and not yet cancelled. It modifies it. That's a behavior outside window. The smallest fix: add `if(Grind_EntriesBlocked()) return;` to `Grind_TryRecenterOppositeL0` or skip recenter in OnTickEngine when session closed. But the threat focuses on placement. Still, it's a leak of activity.

More critical: `Grind_OnTickEngine` calls `Grind_EnsureAddNext` for sides with depth or add_pending_ticket. `Grind_EnsureAddNext` checks `Grind_EntriesBlocked` before placing, but it also has a block at the top that handles `side.add_pending_ticket != 0` and may cancel it if label stale. That cancel is not gated by EntriesBlocked. So if session closed and an add is pending (cancel failed), `Grind_EnsureAddNext` can cancel it because label stale. That's a cancel, not a placement. But it could also modify it: if `side.add_pending_ticket != 0`, it checks label, then later if `Grind_EntriesBlocked()` returns true, it returns before modifying. So no modify. The top block can cancel a stale add even when closed. That's a cancel, fine. But it could cancel an ENT order outside the session? It's still ENT. That's okay.

What about `Grind_TryPlaceAddAtFill`? It checks `Grind_EntriesBlocked`. Good.

What about `Grind_ApplyEntryHorizon`? It checks `Grind_EntriesBlocked` early. Good.

What about `Grind_ServiceDueAddFlags`? It does not check `Grind_EntriesBlocked` before calling `Grind_SendNextAddEnt`, but `Grind_SendNextAddEnt` checks. Good.

What about `Grind_TryPlaceL0`? It checks `Grind_EntriesBlocked` before placing. But note: the check is after the `side.l0_pending_ticket != 0` block. If `l0_pending_ticket == 0`, it checks `g_grind_ent_sent_this_tick` then `Grind_EntriesBlocked`. So no placement if blocked.

But there is a path: `Grind_OnTickEngine` calls `Grind_TryPlaceL0` only if `Grind_SideDepth == 0` and marketable. It passes through the check. So no placement while closed, assuming `g_grind_session_closed` is true.

The race: between the actual time 16:55:00 and the first call to `Grind_SessionStep` that sets `g_grind_session_closed = true`. During this gap, `Grind_EntriesBlocked` is false. If an OnTick occurs, it calls SessionStep first, so by the time OnTickEngine runs, closed is set. So no placement via OnTick. But what about OnTradeTransaction? It can occur outside OnTick. It calls `Grind_OnTradeTransactionEngine`. If `g_grind_fill_time_place` is true, `Grind_TryPlaceAddAtFill` checks `Grind_EntriesBlocked`. If the session step hasn't run yet (e.g., timer is delayed and no tick since 16:55), then `g_grind_session_closed` is still false, so `Grind_EntriesBlocked` false, and it can place an add. This is the leakage. Bounded: one add per ENT fill that occurs in that gap. How many fills? Could be multiple if multiple ENT orders fill in the gap. But the gap is between 16:55:00 and the next OnTimer/OnTick. OnTimer is set to 1 second, so at most ~1 second. But if the terminal is busy, timer could be delayed. Worst case: until next event. If no events, no fills? Fills come as trade transactions. So if a fill occurs, it's an event. The fill itself triggers OnTradeTransaction. At that moment, if no SessionStep has run since 16:55, it can place an add. Then the next OnTimer/OnTick will set closed and cancel. So leakage is one add per fill in that gap. If multiple fills occur before any timer, each could place an add. But `g_grind_ent_sent_this_tick` is not reset in OnTradeTransaction? It is reset in OnTickEngine. In OnTradeTransaction, `Grind_HandleSideDealFill` calls `Grind_TryPlaceAddAtFill` which calls `Grind_SendNextAddEnt`. Does `Grind_SendNextAddEnt` check `g_grind_ent_sent_this_tick`? Yes, it checks `if(g_grind_ent_sent_this_tick) return false;` but that flag is only set true after a successful placement. It is not reset in OnTradeTransaction. So if one add is placed, subsequent fills in the same "tick" (same OnTick cycle) will be blocked by `g_grind_ent_sent_this_tick`. But if the flag is false, multiple fills could each place? Actually the first add placement sets `g_grind_ent_sent_this_tick = true`, so subsequent placements in the same event cycle are blocked. But `g_grind_ent_sent_this_tick` is reset at the start of `Grind_OnTickEngine`. If no OnTick occurs, it remains true after first placement. So at most one add per side? Actually `Grind_SendNextAddEnt` sets `g_grind_ent_sent_this_tick = true` after a successful placement. So only one ENT placement per tick cycle. Since OnTradeTransaction can happen between ticks, the flag might already be true from a previous OnTick. In any case, bounded by number of sides (2) per event cycle. So worst case 2 adds in the gap. Then next step cancels. So T-2 BREAKS with small race. The design's D4 says "OnTick every tick before breaker" and OnTimer every second. The gap is at 16:55:00. If timer runs exactly at 16:55:00, gap is zero. But if timer delayed, a fill can leak. The smallest fix: call `Grind_SessionStep` from `OnTradeTransaction` before `Grind_TryPlaceAddAtFill`? Or have `Grind_EntriesBlocked` compute session closed on the fly from `TimeGMT()`? But that would add a time check everywhere. Alternatively, in `Grind_TryPlaceAddAtFill`, check `!Grind_SessionOpenAt(TimeGMT())` if session enabled. But that's a design change. The ADR already has session step in OnTick and OnTimer. The race is small but real. The threat asks "a placement between 16:55:00 and the next step (fill-time placement in OnTradeTransaction before any step runs)". So they want us to find it. Verdict: BREAKS. Smallest fix: in `Grind_TryPlaceAddAtFill`, before placing, if `g_grind_session_enabled && !Grind_SessionOpenAt(TimeGMT())` return. Or call `Grind_SessionStep` at the start of `Grind_OnTradeTransactionEngine`. But that might cancel on every transaction. The smallest fix: add a direct session check in `Grind_TryPlaceAddAtFill` (and maybe `Grind_SendNextAddEnt`? But `Grind_SendNextAddEnt` is called from other paths that already have session step). Actually `Grind_TryPlaceAddAtFill` is the only entry placement path that can be invoked from OnTradeTransaction without a preceding SessionStep. So add `if(g_grind_session_enabled && !Grind_SessionOpenAt(TimeGMT())) return;` at the top of `Grind_TryPlaceAddAtFill` after the `g_grind_fill_time_place` check. That closes the race. But note: `Grind_TryPlaceAddAtFill` already checks `Grind_EntriesBlocked()`, which would be true if session closed flag set. The direct check handles the flag not yet set. That's a small fix.

Another leak: `Grind_OnTradeTransactionEngine` for ENT fill also calls `Grind_HandleSideDealFill`, which calls `Grind_ExitQManageSide` (places EXT) and then `Grind_TryPlaceAddAtFill`. The EXT placement is allowed. The add placement is the leak.

What about `Grind_OnTickEngine` being called from OnTick when session closed? It is called after SessionStep, so `g_grind_session_closed` is true. `Grind_TryRecenterOppositeL0` modifies L0. That's not placement but activity. Could be considered leak. We can mention under T-2 or T-7. The threat says "entry rests or is placed". Modification is not placement. But it keeps an entry alive. If cancel failed, it's already resting. The modification doesn't create a new resting entry. So maybe not a leak in count. But it could re-center the entry to market, making it more likely to fill outside window. That is a violation of the spirit. The smallest fix: in `Grind_OnTickEngine`, skip `Grind_TryRecenterOppositeL0` if `Grind_SessionBlocksEntries()`. Or add check in `Grind_TryRecenterOppositeL0`. We'll note as a secondary leak.

T-3 Cancel completeness. We discussed comment parse. Also slot mismatch: `Grind_CancelOwnEntryOrders(magic, slot)` filters `c_slot != slot`. If an ENT order was placed with a different slot (e.g., "ALT") but same magic? Each instance has its own magic and slot. The given says eleven instances one per magic; two pairs run a second instance with slot ALT. So magic is unique per instance? Actually "one per magic; two pairs run a second instance with slot ALT". So each instance has a unique magic? Or do the two ALT instances share magic with the OPT instance? The fleet-b says "Magics are shared with cycle 3: separate account, separate terminal, separate GlobalVariable store". Wait, "Magics are shared with cycle 3: separate account, separate terminal". That means on Fleet B, the OPT instance has magic e.g., 2226xxxx, and the ALT instance has the same magic? Or different? The text: "Eleven instances (one per magic; two pairs run a second instance with slot `ALT`)". So each instance has a unique magic? Actually "one per magic" suggests each instance has its own magic. But then "two pairs run a second instance with slot ALT" — if they have a different magic, they are just another instance. The phrase "Magics are shared with cycle 3" means Fleet B uses the same magic numbers as cycle 3. But on the same account, magic must be unique per instance? The magic lock prevents duplicate magic. So each instance has a unique magic. The slot is an additional identifier. So `Grind_CancelOwnEntryOrders(magic, slot)` filters both. If an order for this magic has a different slot, it belongs to a different instance? But if magic is unique, it shouldn't exist. However, if the same instance accidentally placed an order with a different slot (e.g., due to a bug or recon), it would be missed. The threat asks "a different slot or symbol, a magic mismatch. Can a live instance own an ENT order that GrindCommentParse rejects?" Different slot: if `side.l0_pending_ticket` points to an order with a different slot, the cancel for this slot won't cancel it. But how could that happen? The instance always builds comments with its own `InpSlot`. Unless recon reconstructs from an order with different slot? `Grind_ReconstructState` uses `g_grind_recon_slot` to filter. So it should only pick its own slot. So different slot not likely. Magic mismatch: if magic matches? The filter uses `Grind_MagicMatches(OrderGetInteger(ORDER_MAGIC), magic)`. If magic is 0? InpMagic must be non-zero. If broker changes magic? Not likely. Symbol mismatch: filter `OrderGetString(ORDER_SYMBOL) != _Symbol`. If the instance is running on a chart of symbol X, but an order for this magic exists on symbol Y? The instance only trades `_Symbol`. So not likely. The main risk is comment parse failure due to broker truncation/rewrite. Also, `GrindCommentParse` requires exactly `parts[0] == "GRIND"`. If broker adds a prefix, fail. Some brokers add "from #12345" or similar? MT5 order comments can be modified by broker. This is a real risk. The smallest fix: in `Grind_CancelOwnEntryOrders` and `Grind_OwnRestingEntryCount`, if comment parse fails, but the ticket equals `g_grind_long.l0_pending_ticket` or `add_pending_ticket` (for this magic/slot), include it. But that only covers tracked tickets. If recon lost them, not. Alternatively, use order type and magic: an ENT order is a limit order with magic and not EXT. But EXT also limit. Without comment, cannot distinguish. Could check if ticket is in `l0_pending_ticket`/`add_pending_ticket` state. That is a small fix. But if state is lost, the order is orphaned. However, if comment parse fails, recon would also fail to reconstruct, so the instance might not know about it. The ADR-161 cancel should still cancel any order of this magic that is an ENT. But without comment, impossible to know slot/role. The broker truncation is a known limitation. I think we should mark NEEDS-FIX. The smallest fix: on placement, store the ticket and a fallback flag; in cancel, if an order's magic matches and comment parse fails, cancel it if its ticket matches a known pending entry ticket. But that doesn't handle unknown. Given the system's design, comments are the contract. If broker rewrites, many things break. The ADR-161 is not worse. But the threat asks "Can a live instance own an ENT order that GrindCommentParse rejects?" Yes, if the broker truncates. So verdict BREAKS? Or NEEDS-FIX? We can say NEEDS-FIX with smallest fix: add a fallback in `Grind_CancelOwnEntryOrders` to cancel orders whose ticket is stored in `l0_pending_ticket`/`add_pending_ticket` even if parse fails. But if the order is not tracked, still missed. Another fix: before placing, check `StringLen(comment) <= 31` and slot length; if too long, refuse to place (fail). That prevents the EA from creating unparseable comments. But broker truncation still possible. The code already prints FATAL if comment >31 but still places. That's a bug: `GrindCommentBuild` prints FATAL but returns the comment. So if slot is long, it places an order with a too-long comment that broker may truncate. The smallest fix: in `GrindCommentBuild`, if length > GRIND_COMMENT_MAX_LEN, return "" or make the caller fail. But that changes existing code. Or in `Grind_TryPlaceL0`/`Grind_SendNextAddEnt`, check comment length before placing. But slot is controlled by preset. For Fleet B, slots are "OPT"/"ALT", so length is fine. So in practice, not an issue for this fleet. But the threat is general. We can note: for the Fleet B slots, parse succeeds; if a future slot makes comment >31, `GrindCommentBuild` prints FATAL but still returns, so the order can be placed and then missed by cancel. Smallest fix: make `GrindCommentBuild` return "" on overflow and have callers handle. But that's outside ADR-161? It touches grind_comment.mqh. However the ADR says no change to existing? The smallest fix within ADR-161: in `Grind_TryPlaceL0` and `Grind_SendNextAddEnt`, after building comment, if `StringLen(comment) > GRIND_COMMENT_MAX_LEN`, return false. That prevents placing unparseable ENT. That's a small fix. We'll mention.

Also, `Grind_OwnRestingEntryCount` and cancel use `OrdersTotal()`. In MQL5, `OrdersTotal()` returns the number of pending orders. Good. They iterate backwards. Good.

T-4 Clock. We discussed. Need also check DST edges and month starting on Sunday. The functions seem correct. But there is a subtle bug: `Grind_NthSundayMonthUtc` uses `StructToTime(dt)` where `dt.day=1` and `dt.hour=hour_utc`. But `StructToTime` expects the MqlDateTime fields to be in local time? No, `StructToTime` converts a MqlDateTime structure to a datetime value. It does not apply timezone. So it treats the fields as the datetime value. That's correct for UTC if we intend UTC. However, `TimeToStruct(t, dt)` decomposes the datetime. It doesn't apply timezone. So the loop checks `dt.day_of_week`. That's the weekday of the datetime value. Since the datetime value is constructed as if UTC, it's UTC weekday. Correct.

But wait: `Grind_LastSundayMonthUtc` in existing code uses `dt.hour = 1`. For Toronto, we need 07:00 UTC and 06:00 UTC. The new function uses `hour_utc`. Good.

Potential bug: For November 1st Sunday at 06:00 UTC, if the first Sunday is November 1, then at 06:00 UTC, the offset changes to -5. But the function `Grind_NthSundayMonthUtc(year, 11, 1, 6)` returns Nov 1 06:00 UTC. Correct. At that exact instant, `gmt >= dst_start`? No, for end, condition is `gmt < dst_end`. So at 06:00 UTC, `gmt < dst_end` is false, so offset -5. Correct. At 05:59:59, offset -4. Correct.

What about March? At 07:00 UTC, `gmt >= dst_start` true, offset -4. At 06:59:59, offset -5. Correct.

Month starting on Sunday: For November 2026, Nov 1 is Sunday? 2026-11-01 is Sunday. `Grind_NthSundayMonthUtc(2026,11,1,6)`: day=1, hour=6, day_of_week=0, returns Nov 1 06:00. Correct. For March 2026, March 1 is Sunday? 2026-03-01 is Sunday. `Grind_NthSundayMonthUtc(2026,3,2,7)`: day=1 is Sunday, then n>1 adds 7 days -> March 8 07:00. Correct. Tests have SW1 mar 2026 == 2026.03.08. Good.

Year boundaries: `Grind_TorontoUtcOffset` uses `dt.year` of gmt. For gmt = 2026-12-31 23:59:59, year=2026, dst_end 2026-11-01, so offset -5. For gmt = 2027-01-01 00:00:00, year=2027, dst_start 2027-03-14, offset -5. Correct.

Datetime arithmetic with negative offset: `gmt + off * 3600`. If `gmt` is a `datetime` (unsigned 64-bit?), adding negative might be tricky. In MQL5, `datetime` is 64-bit integer, signed? Actually it's `long`? The documentation: `datetime` is a 64-bit integer type. It can hold values from 1970-01-01. It is unsigned? I think it's signed. `gmt + off*3600` where off=-5 gives a positive value for modern dates. So fine. `local % 86400L` with positive local gives positive remainder. Good.

TimeGMT under Wine: If terminal timezone wrong, TimeGMT may be wrong. The operator can see the print. Not a code bug.

Verdict T-4: HOLDS, with caveat on TimeGMT environment. The code is correct for UTC. If the box clock or terminal timezone is wrong, the window is wrong. The operator's print shows the offset and open_now, so they can detect. Smallest fix: none in code; ensure Wine TZ=UTC and verify print.

T-5 Restart and reinit. We identified global persistence. Need verify if globals are reset on reinit. Let's be certain. In MQL5, when an EA is reinitialized (e.g., change inputs, timeframe), the following happens: OnDeinit is called, then the EA is unloaded? Actually, when you change inputs, the EA is removed and a new instance is created? I think the terminal calls OnDeinit, then unloads the EA, then loads it again with new inputs, calling OnInit. During unload, global variables are destroyed. On load, they are initialized again. Wait, I need recall correctly. In MQL5, there is a concept of "reinitialization" when the EA is not unloaded? Let's check memory: In MQL5, when you change the timeframe of a chart, the EA is not reinitialized? Actually, changing timeframe does not reinitialize the EA. Changing inputs does. When you change inputs, the EA is reinitialized: OnDeinit is called, then OnInit is called. The global variables are not reinitialized? I remember reading: "When you change inputs, the EA is reinitialized. Global variables are not reinitialized; they keep their values." Actually, I think in MQL5, global variables are initialized only once when the program is loaded. If the program is reinitialized, global variables are not reinitialized. For example, `static` variables are not reinitialized. Global variables are like static. So they persist. Let's confirm with known behavior: In MQL5, if you have a global counter and change the EA's inputs, does the counter reset? I believe it does NOT reset. Many EAs use `OnInit` to reset flags because they know globals persist. The code here resets `g_grind_halted = false;` in OnInit, which suggests they expect it might persist. So yes, session globals persist. Therefore T-5 BREAKS.

Let's trace reinit while closed:
- Before reinit: `g_grind_session_enabled = true`, `g_grind_session_closed = true`, `g_grind_session_closed_since = some time`, `g_grind_session_last_cancel = some time`, `g_grind_session_stuck_warned = true`.
- OnDeinit called.
- OnInit called. It does not reset these. It calls `Grind_SessionStep(... enabled=true, gmt=TimeGMT(), from_tick=false)`.
- Inside: `g_grind_session_enabled = true`. `was = g_grind_session_closed` (true). `g_grind_session_closed = !Grind_SessionOpenAt(gmt)`. If still closed, `g_grind_session_closed = true`.
- `if(!g_grind_session_closed)` false.
- `resting = Grind_OwnRestingEntryCount(...)`.
- `if(!was)` false because was=true. So it skips the transition block: no SESSION_CLOSE marker, no cancel even if resting>0.
- Then retry block: `resting > 0 && from_tick` — from_tick false, so skip.
- Stuck warn: `!g_grind_session_stuck_warned` — if it was already true, skip. If it was false, and `gmt - closed_since >= 300` and still>0, it may warn. But no cancel.
- Result: entries are not cancelled on reinit while closed. They will only be cancelled if the next OnTick occurs (from_tick=true) and the retry condition `gmt - last_cancel >= 10` is met. If last_cancel is old, it will cancel on first tick. But if the market is closed (e.g., weekend), there are no ticks. So the entries remain until Monday ticks. That's a window leak: on reinit during weekend, entries rest until Sunday/Monday. The design intended reinit to cancel. The threat says "A compile, reattach or chart change while closed: does the first step archive SESSION_CLOSE and cancel what rests?" Answer: NO, if globals persist. Smallest fix: In OnInit before calling `Grind_SessionStep`, reset `g_grind_session_closed = false;` (and maybe `g_grind_session_closed_since = 0; g_grind_session_last_cancel = 0; g_grind_session_stuck_warned = false;`). Then the first step will see `was=false`, and if closed, archive SESSION_CLOSE and cancel. This may duplicate SESSION_CLOSE markers on every reinit while closed. To avoid duplicates, could add a parameter `force_transition` to `Grind_SessionStep` or check `g_grind_session_closed_since == 0`. But smallest fix: reset `g_grind_session_closed = false` in OnInit. The duplicate marker is acceptable? The threat asks "Any duplicate or missing markers that would mislead a reader of the archive?" With reset, you get duplicate SESSION_CLOSE on each reinit while closed. Without reset, you get missing SESSION_CLOSE and missing cancel. The missing cancel is worse. The duplicate marker could be misleading. A better fix: pass a new parameter `from_init` to force the transition block. But ADR says no redesign. The smallest fix within D4: in `Grind_SessionStep`, change condition `if(!was)` to `if(!was || g_grind_session_closed_since == 0)`. Then on reinit, if `g_grind_session_closed_since` persisted non-zero, it still won't. So reset `g_grind_session_closed_since = 0` in OnInit. Then condition triggers. But that still duplicates marker. Actually, if you reset `closed_since=0`, then `!was` is false, but `g_grind_session_closed_since == 0` true, so it enters transition. It sets `closed_since = gmt` and archives SESSION_CLOSE. So duplicate marker on every reinit. To avoid duplicate, you could track a boolean `g_grind_session_initialized` that is not reset? But on reinit, it would persist. Hmm. The simplest fix to ensure cancel: in OnInit, explicitly call `Grind_CancelOwnEntryOrders` if session closed and enabled? But that bypasses the marker logic. The threat asks for smallest fix. We can suggest: in `OnInit`, before `Grind_SessionStep`, reset `g_grind_session_closed = false;` so the transition path runs. The duplicate SESSION_CLOSE marker is acceptable for audit? The archive will show a SESSION_CLOSE at each reinit while closed, which is actually informative (it shows the EA restarted while closed). But it might be confused with the real close. Could include a detail `"reinit":true`? Not in design. We'll note.

Also, `g_grind_session_enabled` is set by the call. If reinit with `InpSessionEnable` changed from true to false, `Grind_SessionStep` sets `g_grind_session_enabled=false`, `g_grind_session_closed=false`, returns. No cancel. That's correct: disabling session should not cancel? The design says if disabled, no marker, no cancel. So entries that were cancelled? Actually if you disable, the block clears, so entries can be placed. Existing entries remain. That's fine.

T-6 Retry and budget. We discussed. Need bound cancel calls per instance per day. Let's compute worst case. The retry only on ticks while closed and `resting>0` and `gmt - last_cancel >= 10`. Suppose an ENT is stuck (cancel fails). At Friday 16:55 close, one cancel attempt. Then no ticks until Sunday 22:00. At Sunday 22:00, first tick: `gmt - last_cancel` huge, so cancel attempt. If fails, last_cancel = Sunday 22:00. Then every 10s on ticks until Monday 07:00. That's 9 hours = 32400 seconds. Max attempts = 3240 per instance. Each attempt calls `Grind_CancelOwnEntryOrders`, which iterates all orders and calls `Grind_CancelPendingOrder` for each own ENT. If 2 entries, 2 order sends per attempt = 6480 order sends per instance. With 11 instances, ~71,280 order sends over 9 hours. If they all fail, API counter may be hit. The API counter entry stop? `Grind_ApiCounterEntryStopped()` likely stops entries after a number of order sends per day. If it counts these cancels, it might stop entries. But if it stops entries, the session step still cancels. The retry loop continues regardless of API entry stop? `Grind_CancelOwnEntryOrders` does not check `Grind_ApiCounterEntryStopped`. So it will keep trying. This could flood the API counter and cause a critical stop. The WARN only once. The archive queue is not flooded because no archive per retry. So the main risk is API counter. The smallest fix: add a maximum retry interval backoff, or stop retrying after N attempts and rely on next open? But design says retries every 10s. To bound, we could increase `GRIND_SESSION_CANCEL_RETRY_SEC` or cap retries per closure. Since constants were ruled, maybe not. But the threat asks to bound. We can say worst case ~3240 attempts per instance for a weekend stuck cancel. If the broker is open but rejects cancels, this is a flood. The smallest fix: exponential backoff (10s, 20s, 40s...) or a max retry count (e.g., 30) then WARN. But that changes constants. Alternatively, only retry if `Grind_OwnRestingEntryCount` changed? No. We'll flag NEEDS-FIX: introduce a retry counter and cap, or backoff. Also, the 300s WARN uses `gmt - closed_since >= 300`. If closed_since is set at Friday 16:55, the WARN will fire on Sunday 22:00:05? Actually `gmt - closed_since` is large, so it will fire on the first tick after close if >300s. If the first tick is Sunday 22:00, it will fire then, not exactly at 300s. That's fine. It warns once.

What about the break at Monday 07:00? As discussed, if retries failed, entries remain and session opens. The smallest fix: on transition to open, if `was` and `resting > 0`, cancel. This also bounds the leak. We'll mention under T-6 or T-2.

T-7 Interactions. Need check ordering that double-cancels, stale tickets, invariants.

- Double-cancel: SessionStep and BreakerOnTick both call `Grind_CancelOwnEntryOrders`. In OnTick order, SessionStep runs before BreakerOnTick. If session closed and resting>0, SessionStep cancels and clears pending. Then BreakerOnTick runs. If breaker is enabled and tripped, it will call `Grind_CancelOwnEntryOrders` again. But since pending cleared, it will iterate orders, find none, and set `g_grind_breaker_cancel_done = true`. So no double cancel on same order. However, if SessionStep's cancel fails, it leaves orders. Then BreakerOnTick's cancel may succeed. That's fine. If Breaker's cancel runs first? It doesn't; order is fixed. So no double-cancel issue.

- Stale `l0_pending_ticket`/`add_pending_ticket`: If SessionStep cancel fails, it leaves tickets. Then OnTickEngine runs. `Grind_TryPlaceL0` sees `l0_pending_ticket != 0` and if order still exists, returns false. So no new L0. `Grind_EnsureAddNext` sees `add_pending_ticket != 0` and may cancel if label stale or modify. If order exists and label ok, and `Grind_EntriesBlocked` is true, it returns before modifying. So it leaves it. On next retry, SessionStep tries again. If eventually cancel succeeds, clears. If order fills, `Grind_HandleSideDealFill` clears ticket. So no stale ticket after successful cancel. If cancel never succeeds, ticket remains, but order remains. That's consistent.

- Invariant: `Grind_CheckBookInvariants` may check that pending tickets correspond to orders? Not sure. If cancel fails, pending ticket still valid. If order disappears externally, `Grind_SelectOurOrder` false, and `Grind_CancelOwnEntryOrders` clears pending on next call? Actually in `Grind_CancelOwnEntryOrders`, if cancel fails but `!Grind_SelectOurOrder`, it clears pending. So if order disappeared, it clears. Good.

- ADR-160 gate: The gate likely uses `Grind_EntriesBlocked`? Not sure. Session block adds to EntriesBlocked, so gate may consider it. That's intended. No ordering issue.

- Halted/quarantined: SessionStep runs before halted return, so it can cancel even if halted. If halted, OnTick earlier might have called `Grind_CancelOwnEntryOrders` on invariant fail. Then SessionStep runs. If session closed and was false, it counts resting (0) and archives SESSION_CLOSE. No double cancel. If session closed and was true, it may retry cancel if resting>0. But halt already cancelled, so resting=0. No issue.

- 07:00 re-placement: At open, SessionStep sets closed=false. OnTickEngine runs. It will place L0 if flat. But what if an L0 was stuck from before? It exists, so `Grind_TryPlaceL0` returns false. So no new L0. The stuck L0 remains. If it's an add, `Grind_EnsureAddNext` will handle. No double placement.

Potential ordering issue: `Grind_SessionStep` is called in `OnTick` after `Grind_AutoEjectOnTick` and `Grind_EjectPollCommand`. Ejection can modify EXT orders. Session step then cancels ENT. No interaction. If ejection modifies an EXT, session step doesn't touch. Good.

T-8 Default OFF inert. As noted, `OnInit` prints a `GRIND_SESSION` line even when disabled. Also `Grind_SessionStep` is called in OnTimer/OnTick, setting `g_grind_session_enabled=false` and `g_grind_session_closed=false`. This could theoretically clear `g_grind_session_closed` if someone set it manually? No. It does not affect order traffic. The extra print is a behavior change. The question says "show the new code changes nothing in behaviour or order traffic". The print is a behavior change. Also the new input appears in the EA's input list, which is a UI change. The config dump does not include it, so archive doesn't record it. But the input is visible. Is that "inert"? Probably acceptable. We can say HOLDS for order traffic, but note the extra log line and input. If strict, NEEDS-FIX: remove the Print when disabled. But the ADR explicitly requires the Print in OnInit to confirm clock. It doesn't say only when enabled. It says `Print("GRIND_SESSION enable=", InpSessionEnable, ...)`. So even disabled prints. That's by design. So not a break.

Now, need also check Givens line numbers. We don't have exact line numbers but can use given. We'll format.

We need output exactly:
```
GIVENS CHECK     -- G1..G5: CONFIRMED / WRONG (with line)
T-1 .. T-8       -- verdict, evidence, smallest fix
PREMISE VERDICT  -- safe to merge? one paragraph
TEST GAPS        -- tests missing, with expected values
```

We need be exhaustive but within line count? The prompt says line count at bottom 106 for the message. Our output can be longer. Desired oververbosity 5. We need provide detailed but not insane.

Let's structure answer.

First GIVENS CHECK:
- G1: CONFIRMED. `grind_pure.mqh:479` `Grind_TorontoUtcOffset` returns -4 if `gmt >= Grind_NthSundayMonthUtc(y,3,2,7) && gmt < Grind_NthSundayMonthUtc(y,11,1,6)`, else -5. `Grind_NthSundayMonthUtc` at `:456` matches.
- G2: CONFIRMED. `Grind_SessionOpenAt` `:491` uses local weekday and `sec >= 25200 && sec < 60900`.
- G3: CONFIRMED. `Grind_EntriesBlocked` `grind_engine.mqh:859` ORs `Grind_ApiCounterEntryStopped() || Grind_BreakerBlocksEntries() || Grind_SessionBlocksEntries()`. `Grind_SessionBlocksEntries` `:854` returns `g_grind_session_enabled && g_grind_session_closed`. No `InpBreakerEnable`.
- G4: CONFIRMED. Only ENT placements are `Grind_TryPlaceL0` and `Grind_SendNextAddEnt`; the other `Grind_PlaceLimit` calls are EXT. Each ENT placement path passes through a `Grind_EntriesBlocked` check. Cite lines as given.
- G5: CONFIRMED. `Grind_SessionStep` called in OnInit, OnTimer, OnTick as described; transition archives SESSION_CLOSE and cancels if resting>0; retries on ticks >=10s; WARN after 300s once; `Grind_OwnRestingEntryCount` same filters. Note line numbers are approximate from prompt but code matches.

Now T-1 to T-8.

T-1: HOLDS. Exits/exit queue/carry/CloseBy/eject do not consult session. `Grind_ExitQManageSide` and `Grind_TryPlaceExitForLayer` place EXT without `Grind_EntriesBlocked`. CloseBy calls `Grind_CancelOwnEntryOrders` on its own failure, but that only cancels ENT (`c_role != "ENT"` filter). Session code cannot cancel/modify EXT: `Grind_CancelOwnEntryOrders` filters role ENT; `Grind_OwnRestingEntryCount` counts ENT only; `Grind_SessionStep` never calls modify.

T-2: BREAKS. Leakage race: `Grind_TryPlaceAddAtFill` (called from `Grind_OnTradeTransactionEngine` on ENT fill) checks `Grind_EntriesBlocked()`, but if the first `Grind_SessionStep` after 16:55:00 has not run yet, `g_grind_session_closed` is still false. A fill in that gap can place an add. Bounded: at most one ENT placement per side per event cycle (`g_grind_ent_sent_this_tick`), so worst case 2 adds per instance; they will be cancelled by the next timer/tick step, so worst-case lifetime is until next step (≤ ~1s if timer healthy, longer if delayed). Also `Grind_TryRecenterOppositeL0` in `Grind_OnTickEngine` can modify a resting L0 while closed (no session check), keeping an entry alive outside window. Smallest fix: in `Grind_TryPlaceAddAtFill`, after `g_grind_fill_time_place` check, add `if(g_grind_session_enabled && !Grind_SessionOpenAt(TimeGMT())) return;`. For recenter, add `if(Grind_SessionBlocksEntries()) return;` at top of `Grind_TryRecenterOppositeL0` or skip in `Grind_OnTickEngine` when `Grind_SessionBlocksEntries()`.

T-3: NEEDS-FIX. Live cancel misses ENT if `GrindCommentParse` fails on broker-truncated/rewritten comment, or if comment >31. `GrindCommentBuild` (`grind_comment.mqh`) prints FATAL but still returns the overlong comment; `Grind_TryPlaceL0`/`Grind_SendNextAddEnt` place it. For Fleet B slots "OPT"/"ALT" comment is ~19 chars, so parse works. But a future longer slot or broker rewrite means the order is never counted or cancelled, and no WARN. Smallest fix: in `Grind_TryPlaceL0` and `Grind_SendNextAddEnt`, after `GrindCommentBuild`, if `StringLen(comment) > GRIND_COMMENT_MAX_LEN` return false. For broker rewrite, add fallback in `Grind_CancelOwnEntryOrders`/`Grind_OwnRestingEntryCount`: if parse fails but ticket matches `l0_pending_ticket`/`add_pending_ticket` for this magic/slot, treat as ENT for counting/cancel. (Mark fallback as INFERRED if not exact.)

T-4: HOLDS. Clock logic correct for UTC: DST starts 2nd Sun Mar 07:00Z, ends 1st Sun Nov 06:00Z; month-start Sunday handled by day_of_week==0 break; year boundaries use gmt year; local weekday and seconds use shifted datetime. `TimeGMT()` under Wine/wrong terminal TZ is environment risk; operator sees `GRIND_SESSION` print on init and `utc_offset` in SESSION_CLOSE/OPEN markers to detect. No code fix; verify Wine TZ=UTC and `open_now` at install.

T-5: BREAKS. On reinit (compile/reattach/chart change) while closed, global `g_grind_session_closed` persists (MQL5 globals are not reinitialized on reinit; OnInit explicitly resets other globals like `g_grind_halted`). First `Grind_SessionStep` sees `was=true`, skips `!was` transition block: no `SESSION_CLOSE` marker, no initial cancel. With no ticks (weekend) entries rest until next tick; at Monday 07:00 open, step returns before cancel, so stuck entries survive into open window. Smallest fix: in `OnInit` immediately before `Grind_SessionStep`, reset session state: `g_grind_session_closed = false; g_grind_session_closed_since = 0; g_grind_session_last_cancel = 0; g_grind_session_stuck_warned = false;` so `was=false` and transition runs. Accept duplicate SESSION_CLOSE on reinit or add a reinit detail.

T-6: NEEDS-FIX. Worst-case retry flood: if cancel fails at Friday 16:55, no ticks over weekend; Sunday market open ~22:00 to Monday 07:00 gives ~9h with ticks. Retry every 10s => ~3240 `Grind_CancelOwnEntryOrders` calls per instance; with 2 resting entries ~6480 order-remove sends per instance; 11 instances ~71k sends. This can stress API counter/entry stop. Worse: if all fail, at Monday 07:00 `Grind_SessionStep` sees open, archives SESSION_OPEN, returns without cancel; stuck ENT orders survive into open window. WARN only once, no archive flood. Smallest fix: on transition to open, if `was && Grind_OwnRestingEntryCount(magic,slot) > 0`, cancel before returning (or track `g_grind_session_stuck` and cancel). For budget, add exponential backoff or cap retries per closure (e.g., 30 attempts) then WARN and stop until next transition; this is a small change to D4 but bounds API sends. Cite `grind_engine.mqh:Grind_SessionStep`.

T-7: NEEDS-FIX / mostly HOLDS. Double-cancel: SessionStep runs before BreakerOnTick in OnTick; if session cancels and clears pending, breaker's later cancel finds none but sets `g_grind_breaker_cancel_done`; no double on same order. If session cancel fails, breaker may succeed. Stale pending tickets: if session cancel fails, `Grind_TryPlaceL0` sees existing ticket and returns false; `Grind_EnsureAddNext` sees existing add, and if `Grind_EntriesBlocked` true returns before modify; retries clear on success or on order disappearance. Halted/quarantined: SessionStep runs before halted/quarantined returns, so it can cancel; earlier halt cancel may already have cleared, no double. ADR-160 gate/API stop: `Grind_EntriesBlocked` includes them; session step only adds. 07:00 re-placement: if stuck entries survive to open (T-6), `Grind_TryPlaceL0` will not place a new L0 because `l0_pending_ticket != 0`; `Grind_EnsureAddNext` may modify/cancel stale add. So no double-place, but stuck entries persist. Smallest fix: same as T-6 open-transition cancel; also ensure `g_grind_session_last_cancel` is reset on open? Not necessary.

T-8: HOLDS for order traffic; NEEDS-FIX if strict on inert. With `InpSessionEnable=false`, `Grind_SessionStep` sets `g_grind_session_enabled=false`, `g_grind_session_closed=false`, returns; `Grind_SessionBlocksEntries` false; `Grind_EntriesBlocked` unchanged. No entry/exit order traffic changes. However, `OnInit` prints a new `GRIND_SESSION` line even when disabled, and the new input appears in the EA input list. If "nothing" includes logs/inputs, that's a small behavior change. No fix needed unless strict; could gate Print on `InpSessionEnable` or accept as diagnostic.

PREMISE VERDICT: Not safe to merge as-is. Multiple breaks: T-2 race, T-5 reinit state persistence, T-6 stuck entries survive to open and retry flood, T-3 comment fallback. The design is close but implementation has gaps tests don't cover. Needs fixes before merge.

TEST GAPS: Tests missing:
- Reinit while closed: simulate persistent `g_grind_session_closed=true`, call `Grind_SessionStep` with `from_tick=false`; expect SESSION_CLOSE marker and cancel. Current code fails.
- Race fill-time placement: set `g_grind_session_enabled=true`, `g_grind_session_closed=false` (stale), `TimeGMT` after 16:55, call `Grind_TryPlaceAddAtFill`; expect no place. Current code places.
- Stuck cancel across open: set `g_grind_session_closed=true`, resting ENT, `send_ok=false`, step at Monday 07:00 (open); expect cancel before/at open or no stuck ENT. Current code opens and leaves ENT.
- Comment overflow: slot length such that comment >31; expect placement refused or cancel still works. Current code places and misses.
- Retry budget: simulate many ticks over closed period with failing cancel; expected bound (e.g., max N attempts) or backoff. Current code unbounded.
- Recenter while closed: set session closed, L0 resting, call OnTickEngine; expect no modify. Current code may modify.
- Default OFF: assert no `GRIND_SESSION` Print when disabled (if desired).

Need include expected values? The prompt says "TEST GAPS -- tests missing, with expected values". We can provide expected outcomes.

We must cite file:line. We'll use the given line numbers where possible. For functions in snippets without line numbers, we can cite as `grind_engine.mqh:Grind_SessionStep` or `fxgrind.mq5 OnTick`. The rule says cite file:line for every claim; otherwise mark INFERRED. We can mark some as INFERRED. We'll try to use line numbers from Givens: `grind_engine.mqh:1033-1089` for SessionStep, `:2439` for CancelOwnEntryOrders, `:2513` for OwnRestingEntryCount, `:1355` TryPlaceL0, `:1617` SendNextAddEnt, `:1109`, `:1335`, `:1544`, `:1648`, `:1759` for EntriesBlocked checks. For `Grind_TryPlaceAddAtFill`, no line given; mark INFERRED or use `grind_engine.mqh` without line. We can say `grind_engine.mqh:Grind_TryPlaceAddAtFill` (line not in prompt; INFERRED). For `fxgrind.mq5` OnInit call, given `:253`; OnTimer `:279`; OnTick `:323`. Use those.

Let's write final in exact headings. Keep it concise but complete.

Need ensure no markdown code block? The output format is plain text. We can use the headings.

Let's draft.

GIVENS CHECK:
```
G1 CONFIRMED -- grind_pure.mqh:479 Grind_TorontoUtcOffset ...
G2 CONFIRMED -- grind_pure.mqh:491 ...
...
```

T-1 etc.

For T-2, mention bounded worst case: at most 2 orders, until next step. If timer healthy, <1s; if delayed, longer. But if no step, could be longer. Actually if no step and no ticks, a fill triggers OnTradeTransaction, places add. Then next event might be another fill. But `g_grind_ent_sent_this_tick` prevents multiple per tick. If no OnTick, it remains true, so only one per side. So max 2.

For T-5, mention MQL5 globals persist on reinit. Is that true? I should be confident. If not, the break might be false. Let's double-check. In MQL5, "Global variables are initialized before OnInit(). If the program is reinitialized, global variables are not reinitialized." I think it's correct. For example, if you change timeframe, OnDeinit and OnInit are called, but global variables are not reset? Actually changing timeframe does not reinitialize the EA in MQL5? I think changing timeframe calls OnDeinit and OnInit? No, changing timeframe does not reinitialize the EA; it just changes the chart. Reinitialization occurs when you change inputs, change symbol, change period? Actually MQL5: "The EA is reinitialized when: the EA is loaded; the EA is unloaded; the timeframe or symbol is changed; inputs are changed." I think when reinitialized, global variables are not reset. Let's check memory: In MQL5, global variables are stored in a special area. When the program is reinitialized, the global variables are not destroyed. Yes. So T-5 holds.

What about `OnInit` resetting `g_grind_session_closed`? Not present. So break.

For T-6, the open-transition cancel: at Monday 07:00, `Grind_SessionStep` sees `g_grind_session_closed` true, `was=true`, `g_grind_session_closed = !Grind_SessionOpenAt(gmt)` false. Then `if(!g_grind_session_closed)` true, `if(was)` true, archives SESSION_OPEN, returns. It does not check resting. So stuck entries remain. Correct.

T-3: `GrindCommentBuild` prints FATAL but returns overlong. In `Grind_TryPlaceL0`, it calls `GrindCommentBuild(slot, side_letter, 0, "ENT")` and passes to `Grind_PlaceLimit`. No length check. So yes.

T-7: Double-cancel with breaker: If session step cancels and clears pending, breaker cancel later calls `Grind_CancelOwnEntryOrders` again. It will iterate orders, find none, and if breaker tripped, sets `g_grind_breaker_cancel_done = true`. No double. But if session step cancel succeeds, it doesn't set breaker's latch. So breaker may cancel again on next tick if it thinks not done? Wait, breaker's cancel is gated by `g_grind_breaker_cancel_done`. If session step cancels, breaker's `g_grind_breaker_cancel_done` remains false until breaker itself runs and executes cancel. So on the same tick, session step cancels, then breaker runs, sees tripped and `!cancel_done`, calls cancel (no orders), then sets `cancel_done=true`. So one extra no-op cancel. Not a double-cancel on an order. Fine.

What about session step canceling while breaker is tripped? Breaker's cancel latch is per FTMO day. If session closes multiple times? Only one close per day. So no.

T-8: Print in OnInit even when disabled. Cite `fxgrind.mq5:253`? The print is right after. Given says OnInit call at `:253`, timer flag. The print is next. We can say `fxgrind.mq5:OnInit` print. Mark INFERRED line.

Now, final answer. Ensure headings exactly. We'll use the exact heading names.

Let's produce.

## Final Report

GIVENS CHECK     -- G1..G5: CONFIRMED / WRONG (with line)
- G1 CONFIRMED. `grind_pure.mqh:479` `Grind_TorontoUtcOffset` returns -4 when `gmt >= Grind_NthSundayMonthUtc(y,3,2,7) && gmt < Grind_NthSundayMonthUtc(y,11,1,6)`, else -5. `Grind_NthSundayMonthUtc` at `grind_pure.mqh:456` builds day 1 at `hour_utc`, advances to `day_of_week==0`, then adds `(n-1)*7*86400`.
- G2 CONFIRMED. `grind_pure.mqh:491` `Grind_SessionOpenAt` uses the shifted local `day_of_week` 1..5 and local seconds `>= 25200 && < 60900`.
- G3 CONFIRMED. `grind_engine.mqh:859` `Grind_EntriesBlocked` ORs `Grind_ApiCounterEntryStopped() || Grind_BreakerBlocksEntries() || Grind_SessionBlocksEntries()`. `grind_engine.mqh:854` `Grind_SessionBlocksEntries` is `g_grind_session_enabled && g_grind_session_closed`; no `InpBreakerEnable`.
- G4 CONFIRMED. ENT orders are placed only by `Grind_TryPlaceL0` (`grind_engine.mqh:1355`) and `Grind_SendNextAddEnt` (`grind_engine.mqh:1617`). The other `Grind_PlaceLimit` calls (`grind_engine.mqh:1410`, `:2425`) are EXT. Every ENT path passes an `if(Grind_EntriesBlocked()) return` at `grind_engine.mqh:1109`, `:1335`, `:1544`, `:1648`, `:1759`.
- G5 CONFIRMED. `Grind_SessionStep` (`grind_engine.mqh:1033-1089`) is called from `OnInit` (`fxgrind.mq5:253`), `OnTimer` (`fxgrind.mq5:279`) and `OnTick` (`fxgrind.mq5:323`) before the breaker and before halted/quarantined returns. Transition archives `SESSION_CLOSE` and calls `Grind_CancelOwnEntryOrders` (`grind_engine.mqh:2439`) if own entries rest; while closed retries only on ticks, at most every 10 s; after 300 s archives `SESSION_CANCEL_STUCK` once. `Grind_OwnRestingEntryCount` (`grind_engine.mqh:2513`) uses the same filters.

T-1 .. T-8       -- verdict, evidence, smallest fix
- T-1 HOLDS. Exits (`Grind_ExitQManageSide`, `Grind_TryPlaceExitForLayer`), carry, CloseBy (`grind_closeby.mqh:170`, `:233`) and ejection (`Grind_EjectPollCommand`, `Grind_AutoEjectOnTick`) never consult `Grind_SessionBlocksEntries` or `Grind_EntriesBlocked`. CloseBy calls `Grind_CancelOwnEntryOrders` on its own failures, but that only cancels role `ENT` (`grind_engine.mqh:2439` onward filters `c_role != "ENT"`). Session code cannot cancel or modify EXT: `Grind_CancelOwnEntryOrders` filters `ENT`; `Grind_OwnRestingEntryCount` counts `ENT`; `Grind_SessionStep` never calls modify.
- T-2 BREAKS. Fill-time race: `Grind_TryPlaceAddAtFill` (called from `Grind_OnTradeTransactionEngine` on an ENT fill) checks `Grind_EntriesBlocked()`, but if the first `Grind_SessionStep` after 16:55:00 has not run yet, `g_grind_session_closed` is still false. A fill in that gap can place an add. Bound: `g_grind_ent_sent_this_tick` limits this to at most one ENT placement per side per event cycle, so worst case 2 adds per instance; they are cancelled by the next timer/tick step, so lifetime is until the next step (≤ ~1 s if the timer is healthy, longer if delayed). Secondary leak: `Grind_TryRecenterOppositeL0` in `Grind_OnTickEngine` can modify a resting L0 while closed; it has no session check. Smallest fix: in `Grind_TryPlaceAddAtFill`, after `g_grind_fill_time_place`, add `if(g_grind_session_enabled && !Grind_SessionOpenAt(TimeGMT())) return;`. For recenter, add `if(Grind_SessionBlocksEntries()) return;` at the top of `Grind_TryRecenterOppositeL0` or skip the call in `Grind_OnTickEngine` when `Grind_SessionBlocksEntries()`.
- T-3 NEEDS-FIX. A live instance can own an ENT that `GrindCommentParse` rejects if the broker truncates or rewrites the comment. `GrindCommentBuild` (`grind_comment.mqh`) prints FATAL when `StringLen(comment) > GRIND_COMMENT_MAX_LEN` but still returns the overlong comment; `Grind_TryPlaceL0`/`Grind_SendNextAddEnt` then place it. For Fleet B slots `OPT`/`ALT` the comment is ~19 chars, so parse works, but a future longer slot or broker rewrite makes the order invisible to both cancel and count, so no WARN. Smallest fix: in `Grind_TryPlaceL0` and `Grind_SendNextAddEnt`, after building the comment, if `StringLen(comment) > GRIND_COMMENT_MAX_LEN` return false. For broker rewrite, add a fallback in `Grind_CancelOwnEntryOrders`/`Grind_OwnRestingEntryCount`: if parse fails but the ticket equals `l0_pending_ticket` or `add_pending_ticket` for this magic/slot, treat it as ENT. (Fallback is INFERRED because exact lines are not in the prompt.)
- T-4 HOLDS. The clock logic is correct for UTC: DST starts 2nd Sun Mar 07:00Z and ends 1st Sun Nov 06:00Z; a month starting on Sunday is handled by the `day_of_week==0` break on day 1; year boundaries use the gmt year; local weekday/seconds use the shifted datetime. `TimeGMT()` under Wine or a wrong terminal timezone is an environment risk; the operator can see `GRIND_SESSION enable=... toronto_utc_offset=... open_now=...` on init and `utc_offset` in `SESSION_CLOSE`/`SESSION_OPEN` markers. No code fix; verify Wine TZ=UTC and the init print.
- T-5 BREAKS. On reinit (compile, reattach, chart change) while closed, `g_grind_session_closed` persists. MQL5 globals are not reinitialized on reinit; `OnInit` explicitly resets other globals (`g_grind_halted`, `g_grind_cap_blocked`, etc.) but not the session globals. The first `Grind_SessionStep` then sees `was=true`, skips the `!was` transition block, so it does not archive `SESSION_CLOSE` and does not do the initial cancel. With no ticks (weekend), entries rest until the next tick; at Monday 07:00 the step sees open and returns before cancel, so stuck entries survive into the open window. Smallest fix: in `OnInit` immediately before `Grind_SessionStep`, reset `g_grind_session_closed = false; g_grind_session_closed_since = 0; g_grind_session_last_cancel = 0; g_grind_session_stuck_warned = false;` so `was=false` and the transition path runs. This may duplicate `SESSION_CLOSE` on each reinit while closed; if that is unacceptable, add a reinit detail field or a force-transition argument.
- T-6 NEEDS-FIX. Worst-case retry flood: if cancel fails at Friday 16:55, there are no ticks over the weekend; from Sunday market open (~22:00) to Monday 07:00 is ~9 h with ticks. Retrying every 10 s gives ~3240 `Grind_CancelOwnEntryOrders` calls per instance; with 2 resting entries that is ~6480 order-remove sends per instance, ~71k across 11 instances. This can stress the API counter/entry stop. Worse: if all fail, at Monday 07:00 `Grind_SessionStep` sees open, archives `SESSION_OPEN`, returns without cancel, and stuck ENT orders survive into the open window. The WARN is once, so no archive flood. Smallest fix: on transition to open, if `was && Grind_OwnRestingEntryCount(magic,slot) > 0`, cancel before returning. To bound API use, add exponential backoff or a retry cap per closure (e.g., 30 attempts) then WARN and stop until the next transition.
- T-7 NEEDS-FIX (mostly ordering-safe, but stuck-entry issue remains). Double-cancel: `Grind_SessionStep` runs before `Grind_BreakerOnTick` in `OnTick`; if session cancels and clears pending, the breaker’s later cancel finds no orders but sets `g_grind_breaker_cancel_done`; if session cancel fails, the breaker may succeed. Stale tickets: if session cancel fails, `Grind_TryPlaceL0` sees the existing ticket and returns false; `Grind_EnsureAddNext` sees the existing add and, because `Grind_EntriesBlocked` is true, returns before modify; retries clear on success or on order disappearance. Halted/quarantined: `Grind_SessionStep` runs before the halted/quarantined returns, so it can cancel; any earlier halt cancel has already cleared. ADR-160 gate/API stop: both are already inside `Grind_EntriesBlocked`. 07:00 re-placement: if stuck entries survive to open (T-6), `Grind_TryPlaceL0` will not place a new L0 because `l0_pending_ticket != 0`; `Grind_EnsureAddNext` may modify/cancel a stale add. No double-place, but the stuck entry persists. Smallest fix: same open-transition cancel as T-6; ensure `g_grind_session_last_cancel` is reset on open only if you also change retry semantics.
- T-8 HOLDS for order traffic; NEEDS-FIX only if “inert” includes logs/inputs. With `InpSessionEnable=false`, `Grind_SessionStep` sets `g_grind_session_enabled=false`, `g_grind_session_closed=false` and returns; `Grind_SessionBlocksEntries` is false; `Grind_EntriesBlocked` is unchanged; no entry/exit traffic changes. However, `OnInit` still prints `GRIND_SESSION enable=false ...` and the new input appears in the EA input list. That is a diagnostic change, not an order-traffic change. If strict inertness is required, gate the `Print` on `InpSessionEnable`; otherwise accept it as the operator’s clock check.

PREMISE VERDICT  -- safe to merge? one paragraph
Not safe to merge as-is. The clock and block plumbing are correct, exits/carry/CloseBy/ejection are untouched, and default OFF does not change order traffic, but the implementation has several gaps the current SW tests cannot see: a fill-time placement race before the first close step (T-2), reinit while closed skipping the cancel and SESSION_CLOSE marker because session globals persist (T-5), a weekend stuck cancel that can leave ENT orders resting into Monday’s open and can generate thousands of retry sends (T-6), and comment-parse brittleness that can make an ENT invisible to the cancel/count filters (T-3). The smallest fixes are localized: a direct session-open check in `Grind_TryPlaceAddAtFill`, a session-state reset in `OnInit`, an open-transition cancel if resting entries remain, and a comment-length guard or fallback ticket check. Until then, do not merge to `main` or deploy to Fleet B.

TEST GAPS        -- tests missing, with expected values
- Reinit while closed: set `g_grind_session_enabled=true`, `g_grind_session_closed=true`, `g_grind_session_closed_since` old, resting ENT, then call `Grind_SessionStep(..., from_tick=false)` with a closed time. Expected: one `SESSION_CLOSE` marker and `remove_calls == 1`. Current code: no marker, no cancel because `was=true`.
- Fill-time race: set `g_grind_session_enabled=true`, `g_grind_session_closed=false` (stale), `g_grind_fill_time_place=true`, time 16:55:00, and call `Grind_TryPlaceAddAtFill` via a simulated ENT fill. Expected: `place_calls == 0`. Current code: may place one add.
- Stuck cancel across open: set `g_grind_session_closed=true`, `send_ok=false`, one resting ENT, then call `Grind_SessionStep` with Monday 07:00:00 Toronto (open). Expected: cancel attempt before/at open, or no resting ENT remains. Current code: archives `SESSION_OPEN`, returns, leaves ENT.
- Comment overflow: set a slot long enough that `GrindCommentBuild` exceeds `GRIND_COMMENT_MAX_LEN`; call `Grind_TryPlaceL0`. Expected: `place_calls == 0` or the order is still cancellable. Current code: places an overlong comment and later misses it in cancel/count.
- Retry budget: simulate a closed period with many ticks and `send_ok=false`. Expected: a bounded number of `remove_calls` (e.g., ≤ cap) or increasing backoff. Current code: one attempt per 10 s indefinitely while ticks continue.
- Recenter while closed: set session closed, one resting L0, `modify_calls` reset, then run `Grind_OnTickEngine`. Expected: `modify_calls == 0`. Current code: `Grind_TryRecenterOppositeL0` may modify the L0.
- Default OFF marker/log: with `InpSessionEnable=false`, assert no `GRIND_SESSION` archive marker and, if strict, no `GRIND_SESSION` print. Current code: no marker, but prints on `OnInit`.

<!-- finish_reason=stop -->
