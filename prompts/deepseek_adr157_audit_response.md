# DeepSeek R1 Audit Report

## Internal Reasoning

We need answer audit. Need follow requested output format exact headings. Need line count? User says message has line count at bottom, maybe irrelevant. Need audit ADR-157 implementation. Need verify givens citing file:line. But provided snippets don't have line numbers except some references. Need cite by function maybe approximate? Prompt requires cite file:line. We have no line numbers in attached code except ADR references. We can cite code by function/line? We can use given givens references with :379 etc. For our own, need cite file:line. But we don't have actual line numbers for attached code. We can cite function names and maybe line numbers from code? The snippets lack numbering. We can say INFERRED for line? But rules: cite file:line otherwise mark INFERRED. Need produce claims. We can cite by section/function and note line numbers unavailable in transcript? But should comply. We can use the line numbers referenced in prompt for some. For our claims, maybe cite function and quote. But user demands file:line. We need approximate lines? The code snippets can be counted manually. We can derive line numbers for key functions by counting from beginning of each file? This is possible but long. We can provide function:line if we count? Let's count relevant.

Need audit threats T-1..T-8. Need not re-open design. Need find implementation bugs.

Let's analyze code deeply.

Key functions:

- Grind_AutoEjectOnTick in grind_engine.mqh around after command poll, called fxgrind.mq5. G3 says called after command poll, before halt return at fxgrind.mq5:308. In code OnTick: after invariant check, Grind_EjectPollCommand then Grind_AutoEjectOnTick. Then if halted return. Actually AutoEject called with blocked = g_grind_halted || g_grind_quarantined. If halted, AutoEject returns early because enabled && blocked -> return. So before halt return but does nothing if halted. OK.

- Grind_AutoEjectTrySide:
  * if !enabled || blocked return -1.
  * depth = side depth; if depth < max_layers return -1.
  * computes ranks, finds idx where rank == depth-1. This is deepest by exit queue rank, not necessarily array index. Good.
  * rank = depth -1; exit_order_ticket from idx.
  * code = Grind_EjectValidate(blocked, enabled, true, depth, rank, exit_order_ticket != 0). Note: passes found=true unconditionally; OK since idx found. But also passes blocked and enabled again; first checks redundant.
  * Then stability, spread.
  * position_ticket = side.layers[idx].position_ticket.
  * if Grind_EjectIsEjected(position_ticket): compares resting = layer.exit_target, min_dist, new_target = Grind_EjectTargetPrice(is_long). If !targetWorse return -1.
  * calls Grind_EjectAcceptLayer.

Potential issues:

T-1 Retry storm: accept path modifies, if fails returns MODIFY_FAILED. No backoff. It will retry every tick. Need quantify. On busy tick stream maybe 10-20 ticks/sec? MT5 OnTick on FX maybe up to tens per second, especially volatile. Per side per tick request if modify failing. If both sides, two. At 10 ticks/s = 600/min per side, 1200/min both. API budget soft warn 1,800 daily? Wait shared daily API budget soft warn 1,800. If API counter counts order requests? The code has Grind_ApiCounter maybe. Each modify via Grind_OrderEngineSend -> Grind_OrderSendCounted? Need inspect API counter? Not attached. But likely counts per order operation. Even at modest 5 ticks/s, 300/min, blows 1800 in 6 min. FTMO hyperactivity. Smallest fix: in-memory per-side backoff W minutes after MODIFY_FAILED. But trigger stateless? Could use static/global var not persisted. Need mention.

Also if modify fails due to freeze level/invalid price, same target will persist until market moves; retry every tick. If it fails because no change? Actually if target equals resting, Grind_ModifyPendingPrice may fail? Broker may return "no changes" error? But the trigger only calls if not already ejected OR target worse by min distance. If not already ejected and not ejected offset, it will fire even if target equals current resting? Wait not already ejected means Grind_EjectIsEjected returns false (offset zero). It doesn't compare target to resting unless ejected. So if at cap stable spread fine and not ejected, it calls accept. Accept modifies exit to target = best passive price (ask+min for long? Actually Grind_EjectTargetPrice for long: Grind_CarryClampLongExit(bid, bid, ask,...). For long exit: theoretical=bid. ClampLongExit: out=bid; if theoretical <= ask + min_dist? bid <= ask+min_dist true, out=ask+min_dist. So target=ask+min_dist. For short: theoretical=ask; clamp short: if ask >= bid - min_dist true, out=bid - min_dist. So target best passive. If exit already at that price? If not ejected, offset is zero, but resting exit may already equal target due to formula/carry? Then modify to same price may fail (TRADE_RETCODE_NO_CHANGES? Actually MQL5 returns TRADE_RETCODE_NO_CHANGES if price same). Then EJECT_REFUSED every tick. But does it become ejected? No, because modify failed before offset set. So infinite retry even though no action needed. That's T-1 plus maybe bug: if target equals current exit price, should mark ejected? But ejection offset would be zero? Actually if target == raw+accrued, offset zero, Grind_EjectIsEjected false forever. But if target same as formula, then no need eject? The logic expects ejection offset nonzero. But if market at formula, target might be different. But could happen.

T-2 Stale exit_target. carry pass modifies resting exits but never updates layer.exit_target. Let's verify. In Grind_CarryExitShiftLayer, after modify, it updates GV shift/accrued, but does not update g_grind_long.layers[idx].exit_target. Indeed no reference to exit_target in grind_carry.mqh. So layer.exit_target remains old. Orphan test uses layer.exit_target as resting. After carry move, actual order price changed (e.g., moved by swap). The orphan test compares new_target vs stale resting. Can wrongly re-eject or hold.

Let's analyze. Orphan test: if already ejected (offset != 0), compute resting = layer.exit_target. new_target = best passive. If new_target worse than resting by >= min_dist, then accept (re-eject). If not, return -1 (hold). Since layer.exit_target was set at ejection accept time to target. Carry pass later modifies the exit order to formula+accrued+offset? Wait carry pass uses formula = raw + eject_offset (since begin includes eject offset), then applies accrued shift: theoretical = formula - direction*accrued_pips*pip_size? Actually Grind_CarryShiftedExitPrice(formula_exit, direction, accrued_pips, pip_size) = formula_exit - direction * accrued_pips * pip_size. For long direction=1, subtract accrued. That moves exit down by accrued (funding cost). So actual resting price becomes raw + eject_offset - accrued_pips. Then accrued GV set to accrued_price = theoretical - formula_exit = -accrued_pips*pip_size. So actual exit price = formula + accrued = raw+eject_offset+accrued. I6 matches. But layer.exit_target remains old ejected target (raw+eject_offset). After carry, actual resting is lower for long (worse? For long exit, lower is worse for us? A long exit sells; lower sell price is worse. But for orphan test, "new target worse than resting" for long means new_target < resting - min_dist. If actual resting moved lower, stale resting is higher. Suppose new_target is between actual resting and stale resting. Stale test: new_target < stale resting - min_dist? Maybe false if new_target not far below stale, so holds, but actual resting is lower, so new_target may actually be better (higher) than actual? Wait for long exit, better price is higher (sell higher). The orphan rule: if new target is worse than resting by >= min_dist, trail down. If actual resting moved lower (worse), new_target might be better than actual, but stale resting is higher. Condition new_target < stale - min_dist. If actual resting = stale - debt, and new_target = actual + small (better), then new_target < stale - min_dist? Only if debt + small > min_dist. If debt large, yes maybe re-eject even though new_target is better than actual? Wait if new_target is better (higher) than actual, you don't want to move exit lower to new_target because that would be worse than actual? Actually moving exit to best passive price: for long exit, best passive price is ask+min. That's the lowest sell price allowed? Wait for a long exit (sell limit), to be passive you must place above ask? Actually sell limit must be > bid? Let's recall: For a long position, exit is sell limit. To be passive, price must be above current ask? Actually a sell limit order is filled when market bid >= price? It rests above market. The "best passive price" for a sell limit is ask + min distance? That is the lowest price you can place that is not marketable? For sell limit, if price <= bid, it's marketable. To be passive, price > ask? Usually for sell limit, price must be above current ask (or stop level). The code Grind_CarryClampLongExit: theoretical=bid. If theoretical <= ask + min_dist then out=ask+min_dist. So target=ask+min_dist. That's above ask. For long exit, higher price is better (more profit), but best passive is the closest to market (lowest) above ask. So "new target worse" for long: new_target lower than resting. If market moves down, ask+min drops, new target becomes lower (worse). The orphan test trails it down if new target is worse by min_dist. So actual resting after carry moved lower due to accrued (funding cost), which is also worse. Stale resting is higher (better). If new_target is lower than stale by min_dist, it re-ejects to new_target. But actual resting might already be lower than new_target? Let's set numbers.

At ejection time: target = 100 (stale resting). Actual = 100.
Carry moves actual down to 95 (accrued -5). stale resting still 100.
Market moves down: new_target = 94. Condition new_target < stale - min_dist (e.g., min_dist=1): 94 < 99 true -> re-eject to 94. That's correct? Actual was 95, new target 94 is worse than actual by 1, so trailing to 94 is appropriate (move down by 1). But condition used stale 100, so it re-ejects even if new_target = 98.5: 98.5 < 99 true -> re-eject to 98.5. But actual resting is 95. new_target 98.5 is better (higher) than actual. Re-ejecting to 98.5 would move exit UP (better) but also resets offset? Actually accept path modifies to target 98.5, sets offset = target - raw - accrued. But accrued is -5, raw = 100? Wait raw formula = entry+exit. offset originally target - raw - accrued. After carry, accrued changed to -5. Eject offset remains original (100 - raw - 0). So current actual = raw + offset + accrued = raw + (100-raw) + (-5) = 95. If re-eject to 98.5, new offset = 98.5 - raw - (-5) = 103.5 - raw. Then actual = raw + offset + accrued = raw + 103.5 - raw -5 = 98.5. So it moves exit up to 98.5. But is that allowed? The orphan test is supposed to only trail worse, not improve. Condition used stale resting 100, so it thought new target 98.5 is worse than resting by 1.5, but actual resting is 95, so new target is actually better. It would move exit up, possibly making it closer to market? Wait for long exit, higher price is further from market? Actually sell limit above ask. Best passive target is ask+min. If market moved down, ask+min is lower. So new_target 98.5 means market ask is around 97.5. Actual resting 95 is below ask+min? But a sell limit at 95 would be below ask, which is marketable? Wait if ask=97.5, a sell limit at 95 would be below bid? Actually sell limit below market would execute immediately? In MT5, a sell limit must be above current price. If actual resting 95 and ask 97.5, that's invalid; it would have been filled or rejected. But carry moved it to 95 when market was higher? Let's re-evaluate. For long exit, sell limit price must be above bid? Actually sell limit order can be placed above current market. If market ask=100, target=101. Carry moves down to 95 because of swap? But if market ask=100, a sell limit at 95 is below bid? That would be marketable and would fill immediately. But carry only happens at rollover, and it modifies pending order. It can move it down into market? The clamp functions: Grind_CarryClampLongExit(theoretical, bid, ask,...): if theoretical <= ask + min_dist, out=ask+min_dist. So it clamps to at least ask+min_dist. So actual resting cannot be below ask+min_dist. At rollover, if market ask=100, actual resting becomes max(theoretical, 101). So if accrued is -5, theoretical=95, clamp sets to 101. So actual resting = 101, not 95. So after carry, actual may be clamped to market. Thus actual resting could be higher than stale? Let's examine.

In carry shift, new_exit = theoretical, then clamped if is_long and theoretical <= ask+min_dist. So actual = max(theoretical, ask+min_dist). If theoretical is lower, clamped to ask+min_dist. So actual is not simply stale + accrued; it's clamped. The accrued GV is set to accrued_price = theoretical - formula_exit (unclamped). The shift GV records applied_shift = new_exit - intended. So I6 formula includes accrued + shift. The layer.exit_target is not updated. Actual order price = new_exit. Orphan test uses layer.exit_target (old). If actual was clamped to market, it could be much higher than old target? For long, if market moved up, ask+min_dist could be above old target. Then actual resting is higher (better) than stale. Orphan test compares new_target to stale. If new_target is worse than stale by min_dist, it re-ejects. But actual resting might be even higher, so new_target is worse than actual too, so re-ejecting down is correct? If actual is higher, and new_target lower, moving down to new_target is trailing down, which is intended when market moves down. But if actual was clamped up due to market moving up, and then market moves down a bit, new_target might be lower than stale but still higher than actual? Wait actual clamped up means actual > stale. new_target could be between stale and actual. Condition new_target < stale - min_dist: if new_target is lower than stale, it re-ejects to new_target, which is lower than actual. That is trailing down. But if new_target is higher than actual? Impossible if new_target < stale < actual. So re-eject down to new_target is correct (it's worse than actual). The problem is if actual is lower than stale (e.g., carry moved down without clamp) and new_target is higher than actual but lower than stale. Then condition triggers but new_target is better than actual, so re-eject would move exit up (improve). Is that allowed? The orphan rule says "new target is WORSE than its resting price by at least min passive distance". If actual resting is lower, new_target higher is not worse. So it would wrongly re-eject. But can actual be lower than stale? For a long, carry moves exit down (theoretical = formula - accrued_pips). If not clamped, actual = old target + accrued + shift? Actually old target = raw+eject_offset. After carry, actual = raw+eject_offset + accrued. accrued is negative (funding cost) for long? Let's see: swap for long usually negative (you pay). swap_points = SYMBOL_SWAP_LONG. Grind_CarryShiftPips(swap_points, mult, digits) = swap_points * mult / pips_div. If swap_points is negative (cost), shift pips negative. accrued_pips = ledger + pending. For long, theoretical = formula_exit - direction*accrued_pips*pip_size = formula - accrued_pips*pip_size. If accrued_pips is negative, theoretical = formula + |accrued|? Wait direction=1. If swap is negative, accrued_pips negative. theoretical = formula - (negative) = formula + positive. That moves exit UP (better). Let's check swap sign. SYMBOL_SWAP_LONG is swap for buy positions. If you pay swap, it's negative. Grind_CarryShiftPips returns swap_points * multiplier / pips_div. So negative. accrued_price = theoretical - formula = - accrued_pips*pip_size = positive. So accrued GV positive. Then formula + accrued = theoretical. So for long, if you pay swap, exit moves UP? That seems wrong: paying funding should make you need to sell higher to cover cost. Actually yes, to cover negative swap, you need higher exit price. So exit moves up (better). For short, you pay swap, exit moves down. So carry moves exit in direction that improves price to cover cost. So actual resting can be higher than stale for long. Then if market moves down, new_target lower. Condition new_target < stale - min_dist. If actual is higher than stale, and new_target is lower than stale, it's definitely lower than actual, so trailing down is correct. If actual is lower than stale? That would happen if swap is positive (you earn), so exit moves down. Then new_target could be higher than actual but lower than stale. Condition triggers, re-eject to higher, which is improving (moving away from market? Actually for long exit, higher is further from market, less likely to fill. But best passive price is lowest above ask. Moving higher would make it less likely to fill, which is bad for ejection? But ejection wants to fill to free slot. If you earn carry, you might move exit down to make it more likely to fill? Wait if you earn positive swap, you want to keep position? The carry pass moves exit to cover funding. If swap positive, you receive funding, so you can lower your exit price and still break even. That makes it more likely to fill, which is good for ejection. So actual resting is lower (better for fill). Then new_target (best passive) might be higher than actual? But new_target is always best passive = ask+min. If market ask is above actual, then actual is below market? But a sell limit below market would be marketable. The clamp ensures actual >= ask+min. So actual cannot be lower than best passive. Therefore new_target <= actual always for long? Wait new_target = ask+min. Actual is clamped to at least ask+min. So actual >= new_target. So new_target is never better than actual; it's always worse or equal. So the orphan test using stale resting: if stale > actual, new_target could be > actual but < stale. Then condition new_target < stale - min_dist might be true, causing re-eject to new_target. But new_target is worse than actual (since actual >= new_target). So moving down to new_target is trailing down, which is correct? But if actual is already lower than new_target? Wait actual >= new_target, so actual is higher (better) than new_target. Moving down to new_target is worse, which is what orphan rule does when market moves down. But why would you want to move down if actual is already higher? Because new_target is the best passive price, which is lower. If market moved down, the best passive price is lower. But if actual is still higher than new_target, it means actual is not at the market; it's stale from before. The orphan rule wants to trail it down to new_target to keep it passive and closer to market. So moving down to new_target is correct. So even if stale is wrong, the condition might still trigger correctly? Let's find a case where stale causes wrong hold or wrong re-eject.

Wrong hold: actual resting is worse (lower for long) than new_target? But actual >= new_target always due to clamp? Actually if actual was set earlier when market was higher, actual could be > new_target. So actual is better than new_target. The orphan rule wants to move down to new_target if new_target is worse than actual by min_dist. But condition uses stale. If stale is higher than actual, then new_target < stale - min_dist might be false even though new_target < actual - min_dist is true. Example: actual = 100, stale = 105, min_dist=1, new_target=99.5. Condition: 99.5 < 105 -1 = 104 -> true, so re-ejects. No wrong hold. If new_target=104.5: condition 104.5 < 104 false -> holds. But actual=100, new_target=104.5 is better than actual, so holding is correct. If new_target=99: condition true, re-ejects. Actual=100, new_target=99 is worse by 1, so should re-eject. Condition true. So wrong hold? Need case where actual=100, new_target=98.5 (worse by 1.5, should re-eject), stale=97 (lower than actual). Condition: new_target < stale - min_dist -> 98.5 < 96 false -> holds. So wrong hold. Can stale be lower than actual? For long, stale = old target. Actual after carry could be higher if swap cost (negative) moves up. So actual > stale. Then new_target might be between stale and actual. If new_target is worse than actual by >= min_dist, it should re-eject, but condition uses stale (lower) so new_target < stale - min_dist is false. So it holds, leaving exit orphaned (too high) when market moved down. That's a real bug: after carry moves exit up (to cover funding), stale target is lower. When market moves down, new_target is lower than actual but higher than stale. The orphan test fails to trail it down because it compares to stale lower value. So it will not re-eject, exit stays too high and may not fill. That's a NEEDS-FIX.

Wrong re-eject: actual lower than stale (if swap positive moves down). new_target is always <= actual? Wait for long, new_target = ask+min. actual >= ask+min = new_target. So actual >= new_target. So new_target is never better than actual. So moving to new_target is always down (worse). If actual < stale, then new_target <= actual < stale. Condition new_target < stale - min_dist could be true even if new_target is not worse than actual by min_dist (e.g., new_target = actual - 0.5). Then it would re-eject to new_target, moving down by 0.5, which is less than min_dist. The rule says only trail if worse by >= min_dist to avoid churn. So it would re-eject too aggressively, causing extra modifies and possibly violating min distance? But the accept path will set target to new_target. That's a minor churn. So both directions possible depending on sign of swap and clamp. So T-2 BREAKS. Smallest fix: compare against actual order price via Grind_OrderGetPriceOpen(exit_order_ticket) instead of layer.exit_target.

Also note: after accept path, it sets layer.exit_target = target. But carry doesn't update it. So stale.

T-3 Bar series correctness. CopyRates without ArraySetAsSeries. In MQL5, CopyRates copies data with oldest first? Actually MQL5 docs: CopyRates returns data in chronological order? Let's recall. For CopyRates, the elements are ordered from present to past by default? Wait, MQL5: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." This is for CopyRates? Actually MQL5 time series are indexed from present to past by default. But CopyRates copies data into array; if the array is not set as series, the indexing is from past to present? Let's check. MQL5 documentation: "When copying to the rates array, the elements are ordered from past to present, i.e., the oldest element is at index 0." Hmm I need be precise. I have knowledge: In MQL5, CopyRates copies history data to an array. The array is populated in the order from the start_date to the end_date, i.e., oldest first. But if you use ArraySetAsSeries, you can reverse. Actually MQL5 docs: "The order of the copied data: if the array is a timeseries, the elements are ordered from present to past; otherwise, from past to present." Let me recall: For CopyBuffer, if you don't set as series, index 0 is the oldest. For CopyRates, the returned array is not a timeseries unless set. By default, arrays are not series. So CopyRates with start=0 count=60? The start parameter: 0 means current bar. The count is number of bars. The function copies from start (0 = current) backwards? Wait start is the index of the first element to copy. In MQL5, for CopyRates, the direction of copying is from past to present? Actually the documentation says: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." That is for CopyRates? Let's check memory: For CopyRates, "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." If so, then rates[0] is current bar, rates[1] is previous, etc. But if ArraySetAsSeries is not used, the array is not a timeseries, but the copied data is still stored in the array in that order? Actually the function fills the array in the order of copying. The start parameter determines the first bar. If start=0, it copies the current bar first? Wait, MQL5 CopyRates signature: int CopyRates(string symbol, ENUM_TIMEFRAMES timeframe, int start_pos, int count, MqlRates rates[]). start_pos: "The index of the first bar to copy." If start_pos=0, it's the current bar. The function copies from start_pos towards past? Or towards future? The documentation: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." That implies rates[0] is the current bar, rates[1] is the previous, etc. So oldest is at the end. But I've seen code that uses ArraySetAsSeries(rates,true) to make index 0 current. Actually if the array is not set as series, the data is stored in chronological order? Let's think: In MQL5, the timeseries are accessed with index 0 as current. When you copy to an array, if you don't set as series, the array is a normal array, and the data is copied in the order from the start_pos to the past? The start_pos is an index in the timeseries. The timeseries index increases towards past. So start_pos=0 is current, start_pos=1 is one bar ago. The function copies count bars starting from start_pos. If it copies in that order, rates[0] would be current, rates[1] one bar ago, etc. But many examples: 
```
MqlRates rates[];
ArraySetAsSeries(rates,true);
CopyRates(_Symbol, PERIOD_CURRENT, 0, 100, rates);
// rates[0] is current
```
Without ArraySetAsSeries, rates[0] would be the oldest? Actually I recall that if you don't set as series, the array is filled in chronological order: rates[0] = oldest, rates[count-1] = newest. Let's verify with known MQL5: The function CopyTime: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." So if you copy 3 bars, the array will have [0]=current, [1]=previous, [2]=older. But if you set ArraySetAsSeries, it reverses? Wait, ArraySetAsSeries sets the array to be accessed like a timeseries (index 0 = last element). The CopyRates function always copies data in the order from present to past? Actually the documentation says: "If you want to copy data in the order from past to present, set the array as a timeseries using ArraySetAsSeries." Hmm.

Let's look at MQL5 docs for CopyRates: 
"int CopyRates(
   string           symbol_name,       // symbol name
   ENUM_TIMEFRAMES  timeframe,         // period
   int              start_pos,         // start position
   int              count,             // data count to copy
   MqlRates         rates[]            // target array to copy
   );
...
The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar."
So rates[0] is current bar. That means the array is filled with current at index 0. But if the array is not set as series, index 0 is the first element. So the oldest is at the end? Wait, if it copies from present to past, the first copied element is current, then previous, etc. So rates[0] = current, rates[1] = previous. So the array is ordered from newest to oldest. That is the same as a timeseries. But the documentation also says: "If you need to copy data in the order from past to present, use ArraySetAsSeries." Actually I think I'm confusing with CopyBuffer. Let's check memory of MQL5: For CopyRates, the default is that the array is not a timeseries, but the data is copied in the order from past to present? No, the documentation explicitly says: "The elements ordering of the copied data is from present to past". That means index 0 is current. So without ArraySetAsSeries, the array is still ordered present to past? But then what does ArraySetAsSeries do? It changes the indexing direction of the array. If you set as series, index 0 is the last element (oldest? Actually timeseries index 0 is current). Wait, a timeseries array in MQL5 has index 0 as the most recent. So ArraySetAsSeries makes index 0 the most recent. If CopyRates already puts most recent at index 0, then setting as series would reverse it? That doesn't make sense.

Let's recall correctly: In MQL5, the timeseries (like Close[], Time[]) are indexed from 0 (current) to increasing (past). When you copy data to a dynamic array, the array is a normal array, and the data is copied in the order from oldest to newest. To make it behave like a timeseries, you call ArraySetAsSeries(array, true), which reverses the indexing so that index 0 is the newest. The CopyRates function copies data from the start_pos to the past? Actually start_pos is an index in the timeseries. If start_pos=0, it's the current bar. The function copies count bars starting from start_pos. If it copies chronologically from oldest to newest, then the oldest among the copied bars would be at index 0. The oldest among the copied bars is start_pos + count - 1 (since start_pos=0 is current, increasing index is past). So the oldest is at the end. If it copies from oldest to newest, then rates[0] would be the oldest, rates[count-1] = current. That matches the common pattern: without ArraySetAsSeries, rates[0] is the oldest. I've used this: 
```
MqlRates rates[];
int copied = CopyRates(_Symbol, PERIOD_CURRENT, 0, 100, rates);
// rates[0] is the oldest of the 100 bars
```
Yes, that's correct. The documentation statement "from present to past" might mean the start_pos is from present to past (i.e., start_pos=0 is current, and increasing start_pos goes back in time). But the copied data in the array is ordered from past to present? Let's check the exact wording: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." This could be interpreted as: the elements are ordered such that the first element corresponds to the present? Actually "from present to past" means the first element is present, then past. So rates[0] = present. But then why do people use ArraySetAsSeries? Let's test memory: In many MQL5 examples, they do:
```
MqlRates rates[];
ArraySetAsSeries(rates, true);
CopyRates(_Symbol, PERIOD_CURRENT, 0, 100, rates);
```
Then rates[0] is current. If CopyRates already puts current at 0 without ArraySetAsSeries, then ArraySetAsSeries would reverse it, making rates[0] the oldest. That would be wrong. So I think the documentation means: the start_pos parameter is indexed from present to past. The copied data in the array is ordered from past to present. That is, the array is filled chronologically. Let me confirm from MQL5 reference: For CopyRates: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." Actually I just found a mental note: In MQL5, CopyRates copies data in the order from past to present. The documentation says: "The elements ordering of the copied data is from present to past" for CopyBuffer? No, for CopyBuffer it says: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." Wait, for CopyBuffer, if you don't set as series, index 0 is the oldest? I'm confused.

Let's look at the code's usage. In Grind_AutoEjectOnTick:
```
const int ns = CopyRates(_Symbol, PERIOD_M1, 0, 60, spread_rates);
...
for(int i = 0; i < ns; i++) spreads[i] = spread_rates[i].spread;
...
const int copied = CopyRates(_Symbol, PERIOD_M1, 0, bar_count, rates);
...
for(int i = 0; i < copied; i++) {
   times[i] = rates[i].time + 60;
   vals_long[i] = rates[i].low;
   vals_short[i] = rates[i].high + rates[i].spread * _Point;
}
```
Then they pass times and vals to Grind_AutoEjectStable, which calls Grind_ExtremeIndexMostRecent(vals, n, want_min). That function finds the index of the extreme, with ties most recent (higher index). So it assumes the array is ordered oldest to newest (chronological), because "most recent tie wins" means later index = more recent. If the array were ordered newest to oldest, then index 0 is most recent, and "most recent tie wins" would require smaller index. The function uses `i > best` for ties, so it picks the highest index as most recent. Therefore the code assumes rates[0] is oldest, rates[copied-1] is newest (current bar). So if CopyRates actually returns newest first, this is a bug. Which is correct? Let's verify MQL5 behavior. I recall that CopyRates without ArraySetAsSeries returns data in chronological order (oldest first). For example, the standard MQL5 documentation for CopyRates: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." Wait, I just checked an online memory: Actually for CopyRates, the function copies data from the start_pos to the past. The first element of the array is the oldest? Let's think about the start_pos parameter. If start_pos = 0, it means the current bar. The function copies count bars starting from start_pos. The bars are indexed from current (0) to past (1,2,...). If it copies in the order of the timeseries, the first copied bar would be start_pos (current), then start_pos+1 (previous), etc. So the array would be ordered from current to past. That would mean rates[0] is current, rates[1] is previous. Then the code's assumption is wrong. But I have written MQL5 code before. The typical pattern to get historical bars in chronological order is:
```
MqlRates rates[];
int copied = CopyRates(_Symbol, PERIOD_CURRENT, 0, 100, rates);
// rates[0] is the oldest, rates[99] is the current
```
Actually I remember that without ArraySetAsSeries, the array is filled from oldest to newest. I've used `rates[0]` as the oldest. Let me search my memory: In MQL5, the `CopyRates` function, if the target array is not a timeseries, the data is copied in the order from past to present. The documentation says: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." This is confusing because "present to past" could mean the start position is present, but the array is filled past to present? Actually, I recall that for CopyBuffer, the default is that the array is not a timeseries, and the data is copied in the order from present to past? Let's check the official MQL5 documentation for CopyBuffer: 
"int CopyBuffer(...);
The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar."
And then it says: "If you need to copy data in the order from past to present, set the array as a timeseries using ArraySetAsSeries."
So without ArraySetAsSeries, the data is ordered from present to past. That means index 0 is the most recent. With ArraySetAsSeries, it's reversed to past to present? Wait, if you set as series, index 0 becomes the most recent. That's the same as present to past. So that doesn't make sense.

Let's look at actual MQL5 documentation for ArraySetAsSeries: "The function sets the AS_SERIES flag for the specified array, so that the elements of the array are indexed from the end to the beginning (from the most recent data to the oldest)." So a timeseries array has index 0 as the most recent. Without AS_SERIES, a normal array has index 0 as the first element. When CopyRates copies data, it fills the array. The order of filling is determined by the start_pos and count. The start_pos is an index in the timeseries. If start_pos=0, it's the current bar. The function copies count bars. The question is: does it copy them in the order of timeseries indices (0,1,2,...) or in chronological order? If it copies in timeseries index order, then rates[0] = current, rates[1] = previous. That would be "present to past". If it copies chronologically, rates[0] = oldest, rates[count-1] = current. The documentation says: "The elements ordering of the copied data is from present to past". So it copies in the order of timeseries indices: 0,1,2,... So rates[0] is current, rates[1] is one bar ago, etc. That means the array is ordered newest to oldest. Then why do people use ArraySetAsSeries? Because if you use ArraySetAsSeries(rates, true), the array is still filled with current at index 0? Actually ArraySetAsSeries just changes the indexing direction of the array itself. If the array is a timeseries, index 0 is the last element. CopyRates fills the array starting from index 0? I'm getting tangled.

Let's test with a simple example from memory. In MQL5, to iterate over historical bars in chronological order:
```
MqlRates rates[];
ArraySetAsSeries(rates, false);
int copied = CopyRates(_Symbol, PERIOD_CURRENT, 0, 100, rates);
for(int i = 0; i < copied; i++) {
   // rates[i] is from oldest to newest?
}
```
Actually, I've seen code that does:
```
MqlRates rates[];
ArraySetAsSeries(rates, true);
CopyRates(_Symbol, PERIOD_CURRENT, 0, 100, rates);
for(int i = 0; i < 100; i++) {
   // rates[i] is from current to past
}
```
And to get chronological, they reverse the loop. So if you don't set as series, the array is filled in chronological order? Let's check the MQL5 documentation for CopyRates. I can recall the exact text: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." This is for CopyRates? Actually, I think this text is for CopyTime, CopyOpen, etc. Let me think: The function CopyRates has a note: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." So rates[0] is the current bar. That means the array is ordered newest to oldest. So the code's assumption that rates[0] is oldest is wrong. But wait, the code uses `times[i] = rates[i].time + 60;`. If rates[0] is current bar, its time is the open time of the current bar. Adding 60 gives its close time. For a forming bar, close time is future. The stability check uses `now - times[idx] >= window_sec`. If the current bar is at index 0, and it's the extreme, then times[0] = current bar open + 60. now is TimeCurrent, which is within the current bar, so now - times[0] is negative (since times[0] is in the future). So it would not count as stable. That's fine. But the tie rule: most recent tie wins. If array is newest first, then index 0 is most recent. The function Grind_ExtremeIndexMostRecent uses `i > best` to pick later index as more recent. That would pick older bars as more recent, which is wrong. So there is a potential bug if CopyRates returns newest first. Let's verify definitively. I can reason from MQL5: The start_pos parameter is the index of the first bar to copy. Index 0 is the current bar. The function copies count bars. The order of copying: according to MQL5 documentation, "The elements ordering of the copied data is from present to past". So it copies from present (index 0) to past (index 1, 2, ...). So rates[0] = current, rates[1] = previous. That means the array is ordered from newest to oldest. Therefore, the code's assumption that index increases with time is wrong. But wait, I've written code like:
```
MqlRates rates[];
int copied = CopyRates(_Symbol, PERIOD_CURRENT, 0, 100, rates);
// rates[0] is the oldest
```
Is that a false memory? Let's check a known MQL5 code snippet from official examples. For example, in the MQL5 documentation for CopyRates, there is an example:
```
void OnStart()
  {
   MqlRates rates[];
   ArraySetAsSeries(rates,true);
   int copied=CopyRates(_Symbol,PERIOD_CURRENT,0,100,rates);
   if(copied>0)
     {
      Print("Copied ", copied, " rates");
      for(int i=0; i<copied; i++)
         Print(i, " time=", rates[i].time, " open=", rates[i].open);
     }
  }
```
They set ArraySetAsSeries(rates,true) before copying. So with AS_SERIES, index 0 is the most recent. Without AS_SERIES, index 0 is the oldest. That is the standard. So the code does NOT set ArraySetAsSeries. Therefore, the array is filled in chronological order: rates[0] = oldest, rates[copied-1] = newest (current bar). This matches the code's assumption. The documentation statement "from present to past" refers to the start_pos parameter, not the array order. Actually, the documentation says: "The elements ordering of the copied data is from present to past, i.e., starting position of 0 means the current bar." This is ambiguous, but the example uses ArraySetAsSeries to get present-to-past order. Without it, the array is past-to-present. So the code is correct. I think T-3 is likely HOLDS. But need check forming bar: CopyRates with start=0 includes the current forming bar. The code copies bar_count = 2W bars including current. It dates bars at open + 60 (close). For the forming bar, its close time is in the future. The stability check computes `now - times[idx]`. If the extreme is the forming bar, times[idx] = open + 60 > now, so now - times[idx] is negative, which is < window_sec, so not stable. So forming bar cannot count as stable. Good.

Weekend/market-open gap: If there are missing bars (e.g., weekend), CopyRates returns only available bars? If fewer than 2W bars exist, copied < bar_count, function returns early (no fire). That's safe but disables. After weekend, there may be fewer than 2W bars if history not loaded? Actually M1 bars are historical, so there should be plenty. But if history not yet loaded, CopyRates may return fewer bars. That's T-5.

T-4 Spread units. Current spread = (ask - bid) / _Point. Baseline = MqlRates.spread. In MQL5, MqlRates.spread is the spread in points? Yes, it's the difference in points. But what is a point? For 5-digit and 3-digit (JPY) symbols, point is the last digit. For 5-digit EURUSD, point = 0.00001. Spread in points is integer number of points. So (ask-bid)/_Point gives the same units. For 3-digit JPY, point = 0.001, spread in points is integer. So same units. However, there is a catch: MqlRates.spread is of type int. It represents the spread in points. The code does `spreads[i] = spread_rates[i].spread;` (int to double). Then `current = (ask - bid) / _Point;`. Both are in points. So units match. But what about 5-digit vs 3-digit? Both use point as the smallest increment. So same units. T-4 HOLDS. But wait: In MQL5, MqlRates.spread is in points, but the point size might be different. The code uses `_Point` for current spread. `_Point` is the symbol's point. So it's consistent. One subtlety: the baseline mean is computed from bars including the current forming bar? The code copies 60 bars from 0, including current bar. The current bar's spread is the spread at the moment of the bar? MqlRates.spread for the forming bar is the current spread? Actually, for the current bar, the spread field might be the current spread or the spread at open? It updates? Not sure. But units are same. So HOLDS.

T-5 Per-tick cost. CopyRates every tick while enabled, even if no side capped. In Grind_AutoEjectOnTick, it first checks `if(!enabled || blocked) return; if(max_layers < 1 || stable_minutes < 1) return;` Then it copies rates unconditionally, before checking if any side is at cap. So even if both sides are below cap, it does two CopyRates calls (60 and 2W) every tick. That's CPU and potentially history access. Risk: CopyRates can return fewer bars if history not loaded, causing early return (silently disabling). Also if history is not yet loaded, it may return -1 or 0, and the function returns. That might be fine but it won't fire until history loads. The bigger issue: CopyRates on every tick may cause terminal to load history, but once loaded it's cached. CPU cost is small but non-zero. Also, if the symbol has no M1 history? It would return 0 and never fire. That's a NEEDS-FIX? The threat asks "Any risk beyond CPU (e.g., history not yet loaded returning fewer bars and silently disabling)?" Yes, if history is not yet loaded, CopyRates may return fewer than bar_count, and the function returns without firing. That's a silent disable until history loads. But typically history loads quickly. Also, if there are fewer than 2W bars in the symbol's history (e.g., new symbol), it will never fire. That's a limitation. But the implementation as written does the CopyRates before the cap check. So it's a per-tick cost even when no side capped. Smallest fix: move the cap check before CopyRates. In Grind_AutoEjectOnTick, we can check depths first, and only if either side >= max_layers, then do CopyRates. That's a clear optimization. Also, the 60-bar copy is done even if the 2W copy fails? It does 60 first, then 2W. If no side capped, it still does both. So T-5 BREAKS (or NEEDS-FIX). The threat says "Any risk beyond CPU". I'd say NEEDS-FIX: unnecessary per-tick history access; move cap check before CopyRates. Also, silent disable if history not loaded: if copied < bar_count return; that means no auto ejection until enough history. That's acceptable but should be logged. But the main fix is reorder.

T-6 Same-tick interactions. Command poll then auto trigger in one tick. The carry pass at 23:50 (OnTimer). The exit queue's own re-pricing afterwards (Grind_OnTickEngine). Can any combination double-modify or leave I6 inconsistent?

Let's trace OnTick:
1. Process close-by queues.
2. Check invariants. If halt, set halted, cancel entries.
3. Grind_EjectPollCommand(...) -> if command, may accept and modify exit, set offset, set layer.exit_target.
4. Grind_AutoEjectOnTick(...) -> may also accept and modify exit for the same layer? If command just ejected, then Grind_EjectIsEjected is true. Auto trigger will check orphan test. It might re-eject if new target worse. But command just ejected to current best passive price. So new_target = same target. Orphan test: resting = layer.exit_target (just set to target). new_target = same target. targetWorse? For long, resting - new_target = 0 >= min_dist? No. So returns -1. So no double modify. Good.
5. If halted return.
6. If quarantined, retry missing exits, return.
7. Grind_OnTickEngine -> calls Grind_RetryMissingExits at start, which calls Grind_ExitQManageSide for both sides. This function may re-price exits? Let's see: Grind_ExitQManageSide: it first holds/cancels exits for layers not required/allowed. Then for required layers without exit order, it places new exit. It does NOT modify existing exit orders. So it won't re-price an already resting exit. However, if the ejection modified the exit, it's still there, so no action. So no double modify. But what about the exit queue's own re-pricing? Actually the comment in threat says "the exit queue's own re-pricing afterwards". Looking at Grind_ExitQManageSide, it only places missing exits; it doesn't modify existing ones. Wait, there is also Grind_ExitQClampPassive and Grind_PlaceLimit. It doesn't call Grind_ModifyPendingPrice. So no re-pricing of existing exits. The only re-pricing is in carry pass and auto eject. So T-6 might be safe. But consider carry pass at 23:50. OnTimer runs every second. At 23:50, carry pass begins. It modifies exits in chunks of 2 per timer call. This can happen concurrently with OnTick auto eject? MQL5 events are sequential? OnTimer and OnTick are not executed concurrently; they are queued. So no race. But within the same tick? OnTimer is separate. So no double modify in same tick. However, if carry pass is active and auto eject fires, it might modify an exit that carry is about to modify? But events are sequential, so one completes before the other. The carry pass uses g_grind_carry_exit_work arrays to track work; if auto eject modifies an exit and sets offset, carry pass later might use stale formula? Let's check: Carry pass begins at 23:50 with Grind_CarryExitPassBegin, which builds work items from current state including eject offset. If auto eject happens after begin but before carry processes that layer, the work item has old formula (without new eject offset). Then carry modifies the exit using old formula + accrued, overwriting the ejection? That's a serious interaction! Let's analyze.

Timeline:
- 23:50: OnTimer calls Grind_CarryOnTimerStep -> Grind_CarryExitPassStep. If gate due, Grind_CarryExitPassBegin builds work items from current layers. For an ejected layer, formula = raw + eject_offset (current). Then carry step processes up to 2 layers per timer call. It may take several seconds to process all layers (chunk 2, timer 1s). During this window, OnTick may fire auto eject. Auto eject could re-eject a layer (modify exit, update offset, update layer.exit_target). But the carry work item for that layer was already built with the old offset. When carry eventually processes that layer, it will use the old formula (which doesn't include the new eject offset) and modify the exit to old_formula + accrued, potentially overwriting the new ejection. This would break I6? Let's see. After auto eject, offset_new is set based on new target. Carry then modifies exit to old_formula + accrued + applied_shift? Actually carry uses formula_exit from work item (old). It computes theoretical = formula_exit - direction*accrued_pips. It modifies exit to new_exit. It sets accrued GV to accrued_price, and shift GV to applied_shift = new_exit - (formula_exit + accrued_price). But the eject offset GV is still the new one. The I6 formula is raw + accrued + eject_offset + shift. The actual exit price is new_exit. The sum raw + accrued + eject_offset_new + shift should equal new_exit? Let's compute: carry set new_exit = theoretical (clamped). theoretical = old_formula + accrued_price? Actually Grind_CarryShiftedExitPrice(formula_exit, direction, accrued_pips, pip_size) = formula_exit - direction*accrued_pips*pip_size. Then accrued_price = theoretical - formula_exit. So theoretical = formula_exit + accrued_price. So new_exit (if not clamped) = formula_exit + accrued_price. Then applied_shift = new_exit - (formula_exit + accrued_price) = 0. But formula_exit is old raw + old_eject_offset. So new_exit = old_raw + old_eject_offset + accrued. But actual I6 sum = raw + accrued + eject_offset_new + shift. That would be raw + accrued + eject_offset_new. These are not equal unless old_eject_offset = eject_offset_new. So I6 would break! The exit would be at old_formula + accrued, but I6 expects raw + accrued + new_eject_offset. This is a HALTs. So T-6 is a BREAKS. This is a critical race between carry pass and auto eject. The carry pass is active only in the 23:50 window and processes in chunks. Auto eject can fire any tick. If it fires while carry pass is active, it can modify an exit that carry has already queued work for. The smallest fix: Prevent auto eject while carry pass is active. Or make carry pass re-read the eject offset before modifying? Or update the work item? The simplest: in Grind_AutoEjectTrySide, check if carry pass is active (g_grind_carry_exit_pass_active) and return -1. Since carry pass only runs in a narrow window, this avoids the race. Also command eject? The operator command could also race. But ADR-155 probably didn't consider. The threat is about ADR-157. So fix: add `if(g_grind_carry_exit_pass_active) return -1;` in auto eject try side. That's smallest.

Also, what about the exit queue's own re-pricing? As noted, Grind_ExitQManageSide doesn't modify existing orders. So no.

Also, command poll and auto trigger in same tick: If command poll accepts, it sets offset and layer.exit_target. Auto trigger then sees ejected. It might re-eject if new target worse. But since command just ejected to current best passive, new target equals current best. So no. But if market moved between the two calls within the same tick? The functions are called sequentially in the same OnTick. Grind_EjectPollCommand calls Grind_EjectAcceptLayer, which calls Grind_EjectTargetPrice. Then Grind_AutoEjectOnTick calls Grind_EjectTargetPrice again. Between them, no new tick, but market might have moved if the first modify took time? The ask/bid are read from Grind_MarketAsk/Bid, which query the terminal. They could change between calls if a new tick arrives? In MQL5, OnTick is not reentrant; the market data might update during the function execution? Usually not, but possible. If new_target differs slightly, orphan test might trigger a second modify in the same tick. That would be a double modify. The orphan test requires new target worse by >= min_dist. Since min_dist is at least one point, a small market move within the same tick could trigger it. That's a potential double modify. But the command just ejected to best passive. If market moves down (for long), new best passive is lower, so new target worse by maybe a few points. If >= min_dist, auto trigger will re-eject immediately. That's actually intended? It's a re-ejection if the market moved enough. But doing it in the same tick is a bit aggressive. However, it's not a race; it's sequential. It could cause two modifies in one tick. Is that a problem? It costs API and might violate "at most one modify per side per stable window" (ADR says negligible). But it's allowed by the orphan rule. So not a bug per se. T-6 main issue is carry pass race.

T-7 Both sides / blocked flags. Both sides firing on one tick: The code calls TrySide for long, then for short. They are independent. Could both fire? Yes, if both sides capped and stable. They use separate layers, separate orders. So safe. A side becoming blocked (quarantine) mid-sequence: The `blocked` parameter is passed by value from OnTick. It's `g_grind_halted || g_grind_quarantined` at the time of call. Within Grind_AutoEjectOnTick, it doesn't change. So if quarantine occurs during the tick? Quarantine is checked at start of OnTick. It won't change during the same tick unless something inside modifies it. Grind_EjectAcceptLayer doesn't modify quarantine. So safe. But what if the first side's ejection causes an invariant failure? The accept path modifies order, sets offset. The invariant check is not run until next tick. So it could leave I6 inconsistent until next tick. But that's normal. If a modify fails, it returns MODIFY_FAILED and continues to the other side. That's fine. T-7 HOLDS.

T-8 Restart. The trigger is stateless. After restart with an ejected, unfilled layer, does the orphan test behave correctly? On restart, Grind_ReconstructState rebuilds layers. It reads eject offset from GVs. It sets layer.exit_target? Let's check Grind_ReconstructState (not attached). It probably sets exit_target from formula + accrued + eject_offset? Or from actual order price? If it sets exit_target to actual resting price, then orphan test works. But if it sets to formula target without offset, then stale. Need infer. The code in Grind_EjectAcceptLayer sets layer.exit_target = target. On restart, reconstruction likely uses Grind_ExitQFormulaTarget which includes eject_offset, and sets exit_target to that? Or it reads order price? We don't have grind_recon.mqh. But given ADR-155, reconstruction passes I6. The layer.exit_target is probably set to the actual exit price from the order or from formula. If it's set from formula + accrued + eject_offset, then it matches actual. But carry pass doesn't update exit_target, so after restart, if carry pass had modified the exit, reconstruction would compute formula + accrued + eject_offset, which is exactly the actual resting price (since carry sets accrued and shift). So on restart, exit_target would be correct! The bug T-2 is only in-memory during runtime. After restart, exit_target is recomputed correctly. So T-8 likely HOLDS. But need check if restart happens while carry pass active? Not relevant. So T-8 HOLDS.

Now T-1 Retry storm: Need quantify. On busy tick stream, what's typical? EURUSD during news can have 100+ ticks/sec. At 50 ticks/sec, 3000/min per side. API budget soft warn 1800 per day? Wait "shared daily API budget (soft warn 1,800)". That's 1800 requests per day? If so, a retry storm would blow it in seconds. Even at 1 tick/sec, 60/min, 1800 in 30 min. FTMO hyperactivity: FTMO has rules against high-frequency trading? They might flag excessive order modifications. Smallest fix: in-memory per-side backoff of W minutes after MODIFY_FAILED. But the trigger is stateless. Could use a static datetime per side? Since ADR says stateless computed from bars, but a backoff timer is state. The candidate fix is given. That's acceptable. So T-1 BREAKS.

T-2 BREAKS. Smallest fix: compare against order's actual price via Grind_OrderGetPriceOpen(exit_order_ticket). That function exists. In Grind_AutoEjectTrySide, replace:
```
const double resting = is_long ? g_grind_long.layers[idx].exit_target : g_grind_short.layers[idx].exit_target;
```
with
```
const double resting = Grind_OrderGetPriceOpen(exit_order_ticket);
```
If order not found, maybe return -1.

T-3 HOLDS? Need check forming bar and weekend. The code copies 2W bars including current. It dates bars at open+60. For forming bar, close time is future. Stability check: now - times[idx] >= window_sec. If idx is forming bar, now < times[idx], so now - times[idx] < 0 < window_sec, so not stable. Good. For weekend gap: if fewer than 2W bars exist, copied < bar_count, returns early. So no fire. That's safe. But what about the extreme being set at a bar that is more than 2W minutes ago? The window is last 2W minutes. The code copies exactly 2W bars (including current). The oldest bar's close time is at most 2W minutes ago? If bars are missing, the time span could be longer than 2W minutes. For example, if there's a weekend gap, the 2W bars might span more than 2W minutes. Then an extreme set in that older bar could be considered within the window even though it's more than 2W minutes ago in wall-clock time. The stability check uses `now - times[idx] >= window_sec`. If the bar is older than 2W minutes, now - times[idx] > window_sec, so it would be considered stable (since >= window_sec). But the ADR says "over the last 2W minutes". If the series includes bars older than 2W minutes due to gaps, the extreme could be from before the window, and it would incorrectly satisfy stability. For example, if there was a gap, the 2W bars cover 3W minutes. An extreme set at the oldest bar (3W minutes ago) would be considered within the window? Actually the code doesn't check that times[idx] >= now - window_sec. It only checks that the extreme is at least W minutes ago. It doesn't verify that the extreme is within the last 2W minutes. The series is supposed to be the last 2W minutes. If bars are missing, the series is not exactly the last 2W minutes. The code copies 2W bars, not 2W minutes. So if there are gaps, the window is larger. This could cause firing when the extreme is actually older than 2W minutes. Is that a bug? The ADR says "over the last 2W minutes the extreme ... was set at least W minutes ago". If the extreme is older than 2W minutes, it shouldn't count because it's not in the last 2W minutes. The code would still count it because it's in the copied bars. So T-3 could be NEEDS-FIX. But how common are gaps? M1 bars on FX have gaps on weekends and holidays. After a weekend, there's a gap. If the EA is running and a side is capped, the 2W bars will include the gap. The extreme might be from before the gap. Then stability check might fire immediately after market open if the extreme is old. But wait, the extreme is the lowest bid or highest ask. After a weekend, the market opens with a gap. The lowest bid might be from before the weekend. The bar series includes bars before the gap. The stability check would see that the lowest bid was set more than W minutes ago (it was set days ago). So it would fire immediately at market open, ejecting into the gap. That violates the spirit of "no new extreme for W minutes" because there hasn't been W minutes of trading; the market just opened. Actually, the ADR says "stability over the last 2W minutes". If the market was closed, there are no bars. The last 2W minutes of trading bars might span a longer wall-clock time. The code uses bar count, not wall-clock. So it could fire immediately at open. That's a potential bug. The threat asks: "Behaviour right after a weekend/market-open gap, or when fewer than 2W bars exist?" So we should flag: after a weekend gap, the 2W-bar window includes pre-gap bars, so an old extreme is considered stable, and it may fire at open. The smallest fix: require the oldest bar in the series to be within `now - 2W*60` seconds? Or filter out bars older than 2W minutes by time. Actually, the pure function Grind_AutoEjectStable only checks the extreme's age. It doesn't check the window bounds. The code could pass only bars with time >= now - 2*W*60. But it copies by count. So fix: in Grind_AutoEjectOnTick, after building times, skip bars older than now - 2*stable_minutes*60? Or better, check that the extreme's time is >= now - 2*stable_minutes*60. But the ADR says the extreme must be within the last 2W minutes. So add a check in Grind_AutoEjectStable: if (now - times[idx] > 2*window_sec) return false? Wait, the window_sec passed is stable_minutes*60, which is W. The function is called with window_sec = stable_minutes * 60. That's W. The stability condition is that the extreme was set at least W minutes ago. The ADR says over the last 2W minutes. So the series is supposed to be last 2W minutes. The function doesn't know about 2W. It just checks age >= W. It assumes the series is within 2W. If the series includes older bars, the extreme could be older than 2W, and age >= W is still true. To fix, we could verify that the oldest time in the series is >= now - 2*W*60. Or simply require that the extreme's time is >= now - 2*W*60 (i.e., not older than 2W). But the condition is that it was set at least W minutes ago. So it should be between W and 2W minutes ago. The code only checks >= W. It doesn't check <= 2W. So if the extreme is older than 2W, it still passes. That's a bug. Smallest fix: in Grind_AutoEjectStable, add a check that `now - times[idx] <= 2*window_sec`? But the function doesn't receive 2W. It receives window_sec = W. So we could pass 2W as well, or check in caller. Actually, the caller knows stable_minutes. It could check that the oldest bar in times is within 2W. Or in Grind_AutoEjectStable, we can check that the extreme index is not older than the series intended? But the series is supposed to be exactly 2W. The bug is that the series may not be exactly 2W due to gaps. So the fix is to ensure the series covers exactly the last 2W minutes. In Grind_AutoEjectOnTick, after CopyRates, we can check that `rates[copied-1].time + 60 >= now - 2*stable_minutes*60`? Actually the oldest bar's close time should be within 2W minutes. If it's older, we have a gap. So we could return early if the oldest bar is too old. That would prevent firing after a weekend gap until enough new bars are formed. That seems safe. So T-3 NEEDS-FIX.

Also, T-3: "Can the forming bar ever count as stable?" As analyzed, no, because its close time is in the future, so age negative < W. But wait: The code copies bars including the current forming bar. For the current bar, `rates[0].time` is the open time of the current bar. `times[0] = time + 60`. `now` is TimeCurrent. If the current bar just opened, now might be equal to open time. Then times[0] = open + 60, which is in the future. So now - times[0] is negative. So not stable. If the bar is about to close, now is open+60 - epsilon. Then now - times[0] = -epsilon, still negative. So never stable. Good.

T-4 HOLDS. But wait: MqlRates.spread is in points, but for some symbols, the spread might be in pips? No, it's points. The code uses `_Point`. So consistent. However, note: The baseline is computed from `spread_rates[i].spread`. The current spread is `(ask - bid) / _Point`. For 5-digit, ask-bid might be 0.00012, _Point=0.00001, current=12 points. MqlRates.spread for that bar would be 12. So match. For 3-digit JPY, ask-bid=0.012, _Point=0.001, current=12 points. MqlRates.spread=12. So match. T-4 HOLDS.

T-5: Per-tick cost. As analyzed, it does CopyRates before checking cap. So even with no side capped, it does two CopyRates per tick. That's a performance issue and can cause silent disable if history not loaded. Smallest fix: Move the cap check before CopyRates. In Grind_AutoEjectOnTick, after the enabled/blocked checks, check if either side depth >= max_layers. If neither, return. Then do CopyRates. That avoids unnecessary work. Also, if history not loaded, it returns early. But with the reorder, it only happens when a side is capped, which is less frequent. Still a risk: if a side is capped and history not loaded, it will silently not fire. That's acceptable but should be logged. The threat asks "Any risk beyond CPU (e.g., history not yet loaded returning fewer bars and silently disabling)?" Yes, the silent disable. So NEEDS-FIX: reorder and log when copied < bar_count.

T-6: Carry pass race. BREAKS. Smallest fix: In Grind_AutoEjectTrySide, add a guard: `if(g_grind_carry_exit_pass_active) return -1;` This prevents auto eject from firing while carry pass is mid-flight. Also, command eject could race? The command poll runs before auto eject. If carry pass is active, command eject might also race. But the threat is about ADR-157. The fix for auto eject is to skip during carry pass. Should we also skip command? That's ADR-155. But to be safe, we could mention that the same race applies to command eject, but out of scope. The smallest fix for T-6 is the guard.

T-7: HOLDS. Both sides independent; blocked flags passed by value. No issue.

T-8: HOLDS. Restart reconstructs from GVs. The orphan test uses layer.exit_target, which on restart is likely set correctly from reconstruction. But wait: T-2 says layer.exit_target is stale in memory after carry. On restart, reconstruction sets it. So after restart, it's correct. So T-8 HOLDS.

Now GIVENS CHECK:
G1: Grind_EjectAcceptLayer (grind_engine.mqh:379) is former command accept branch, unchanged except source field. In code, we see Grind_EjectAcceptLayer at around line? The prompt says :379. The function signature has `const string source`. It is called by Grind_EjectPollCommand with "command" at :433. In the code, Grind_EjectPollCommand calls `Grind_EjectAcceptLayer(is_long, idx, magic, exit_pips, "command")`. So CONFIRMED.
G2: Grind_AutoEjectTrySide (:515) checks enabled/blocked, cap, deepest, validation, stability, spread, then orphan test, then calls accept path with "auto" (:595). In code, function starts with `if(!enabled || blocked) return -1;` then depth check, then ranks, validation, stability, spread, orphan test, then `return Grind_EjectAcceptLayer(is_long, idx, magic, exit_pips, "auto");`. So CONFIRMED.
G3: Grind_AutoEjectOnTick (:599) builds series from CopyRates(PERIOD_M1), dating bars at open + 60 s, long = bar low, short = bar high + bar spread; and is called after command poll, before halt return (fxgrind.mq5:308). In code, Grind_AutoEjectOnTick does exactly that. And in fxgrind.mq5 OnTick, it's called after Grind_EjectPollCommand and before `if(g_grind_halted) return;`. So CONFIRMED.
G4: Pure helpers (grind_pure.mqh:331-385) implement most-recent-tie extreme, stability, spread gate and orphan test as ADR-157 states. In code, Grind_ExtremeIndexMostRecent, Grind_AutoEjectStable, Grind_AutoEjectSpreadOk, Grind_AutoEjectTargetWorse are there. They match. So CONFIRMED.

Now PREMISE VERDICT: safe to merge with switch default off? Given T-1, T-2, T-3, T-5, T-6 are issues. T-2 and T-6 can cause I6 halt or wrong behavior. Since default off, it's safe in the sense that it won't run unless enabled. But if enabled, it has bugs. So not safe to merge if it might be enabled without fixes. The question: "safe to merge (switch default off)?" Probably: MERGE BLOCKER? Since it's behind default false, it can merge but must not be enabled until fixes. But the prompt asks "PREMISE VERDICT -- safe to merge (switch default off)? one paragraph". I'd say: Behind default-off, it poses no immediate risk to production, but the implementation contains at least two correctness breaks (T-2 stale exit_target causing wrong orphan decisions; T-6 carry-pass race causing I6 halt) and one operational risk (T-1 retry storm). Therefore it should not be merged without fixing T-2 and T-6 at minimum, or at least the merge must be conditional on never enabling InpAutoEject. The safe statement: It is safe to merge only as dead code with the switch guaranteed off; enabling it in current form is unsafe.

TEST GAPS: Tests missing, with expected values. Need list specific tests that would catch these. For T-1: test that after a MODIFY_FAILED, a second tick does not issue another modify for same side until W minutes have passed. Expected: modify_calls increments by 1, then no increase on subsequent ticks. Also test EJECT_REFUSED event count. For T-2: test carry pass modifies exit, then auto eject orphan test compares against actual order price, not stale layer.exit_target. Expected: after carry moves exit to P_actual, and new_target = P_actual - min_dist - 1, auto eject fires; if new_target = P_actual - min_dist + 1, it does not. Also test that layer.exit_target is updated after carry? Or test uses order price. For T-3: test weekend gap: series with bars older than 2W minutes; extreme at oldest bar; expect stable returns false (no fire). Expected: if oldest bar close time < now - 2W*60, then no fire. Also test forming bar: extreme on current bar, expect stable false. For T-4: test 5-digit and 3-digit symbols: baseline spread from MqlRates and current spread from ask-bid/_Point produce same units. Expected: current_spread_points == (ask-bid)/_Point, baseline mean computed from rates.spread. For T-5: test no side capped: CopyRates not called. Expected: ns and copied remain 0 or function returns before CopyRates. Also test history not loaded: copied < bar_count, no fire, and maybe a log/telemetry. For T-6: test auto eject during active carry pass: expect no auto eject modify. Or test carry pass work item built before auto eject, then auto eject fires, then carry modifies: I6 should still hold. Expected: after both, I6 passes. For T-7: test both sides capped and stable: both modify calls occur, one per side. Test quarantine mid-tick: if blocked true, no modify. For T-8: test restart with ejected, unfilled layer, then carry pass moves it, restart again, I6 passes and orphan test uses correct resting. Expected: layer.exit_target equals actual order price after reconstruction.

Also GIVENS check: need cite lines. We can use the line numbers provided in prompt for G1-G4. For our own claims, we need cite file:line. Since we don't have exact line numbers, we can cite by function name and approximate line numbers? We can count from the code snippets. Let's try to assign line numbers to key functions in grind_engine.mqh. The prompt gives some: Grind_EjectAcceptLayer at :379, Grind_EjectPollCommand at :433, Grind_AutoEjectTrySide at :515, Grind_AutoEjectOnTick at :599. These are from the prompt's GIVENS. So we can use those. For other claims, we can cite function names and say line numbers as per snippet? The snippet doesn't have line numbers. We can say "grind_engine.mqh: Grind_AutoEjectTrySide (G2 line 515)" for T-2. For T-6, cite grind_carry.mqh: Grind_CarryExitPassBegin and Grind_AutoEjectTrySide. We can use the function names and the given line numbers where available. The rules say cite file:line otherwise mark INFERRED. We can mark some as INFERRED if we can't give exact line. But we can give line numbers by counting. Let's count for grind_carry.mqh? The prompt says Grind_CarryExitPassBegin at :796, :804 in ADR-155. So we can use those. For grind_pure.mqh, the prompt says pure helpers at :331-385. So we can cite those. For grind_engine.mqh, we have the given line numbers for the new functions. For other functions like Grind_ModifyPendingPrice, we can count. Grind_ModifyPendingPrice is after Grind_AutoEjectOnTick. In the snippet, after Grind_AutoEjectOnTick ends, there is Grind_ModifyPendingPrice. The prompt says Grind_AutoEjectOnTick starts at :599. Let's estimate lines: The function Grind_AutoEjectOnTick is about 45 lines. So Grind_ModifyPendingPrice starts around :645. We can just cite the function name and say "grind_engine.mqh: Grind_ModifyPendingPrice" without line? The rule says cite file:line. We can use the line numbers from the prompt's GIVENS and for others, we can count from the provided code if we want. But this is a text output; we can approximate. I'll use the line numbers provided in the prompt for the new functions, and for other functions, I'll cite by function name and say "grind_engine.mqh: Grind_ModifyPendingPrice (no line number in transcript, INFERRED)". That satisfies marking INFERRED.

Let's structure output exactly:

```
GIVENS CHECK     -- G1..G4: CONFIRMED / WRONG (with line)
T-1 .. T-8       -- verdict, evidence, smallest fix
PREMISE VERDICT  -- safe to merge (switch default off)? one paragraph
TEST GAPS        -- tests missing, with expected values
```

Need be exhaustive but concise. Let's draft.

GIVENS CHECK:
G1: CONFIRMED. grind_engine.mqh:379 Grind_EjectAcceptLayer signature includes `const string source`; called from Grind_EjectPollCommand at :433 with "command".
G2: CONFIRMED. grind_engine.mqh:515 Grind_AutoEjectTrySide does the checks in order and calls Grind_EjectAcceptLayer at :595 with "auto".
G3: CONFIRMED. grind_engine.mqh:599 Grind_AutoEjectOnTick uses CopyRates(PERIOD_M1), times = time+60, vals_long = low, vals_short = high + spread*_Point; called from fxgrind.mq5:308 after Grind_EjectPollCommand and before halt return.
G4: CONFIRMED. grind_pure.mqh:331-385 implements Grind_ExtremeIndexMostRecent, Grind_AutoEjectStable, Grind_AutoEjectSpreadOk, Grind_AutoEjectTargetWorse as described.

T-1: BREAKS. Evidence: Grind_EjectAcceptLayer (grind_engine.mqh:379) on modify failure returns GRIND_EJECT_MODIFY_FAILED without setting any backoff. Grind_AutoEjectOnTick (grind_engine.mqh:599) calls Grind_AutoEjectTrySide every tick; if still capped/stable/spread ok and not ejected, it calls accept again. No state change. Quantify: FX majors can produce 10–100 ticks/sec. At 10 ticks/s, one side = 600 modify attempts/min; both = 1200/min. Soft warn 1800/day is exceeded in <2 min. At 50 ticks/s, 3000/min per side. FTMO hyperactivity flags likely. Smallest fix: add per-side in-memory backoff (e.g., `datetime g_auto_eject_backoff_until_long/short`) set to now + W*60 on MODIFY_FAILED; check at top of Grind_AutoEjectTrySide.

T-2: BREAKS. Evidence: Grind_AutoEjectTrySide (grind_engine.mqh:515) orphan test uses `g_grind_long.layers[idx].exit_target` (or short) at lines ~570. Grind_CarryExitShiftLayer (grind_carry.mqh:~? per ADR :796) modifies the actual pending order and updates accrued/shift GVs but never updates `layer.exit_target` (no reference in grind_carry.mqh). After carry, layer.exit_target is stale. Correct behavior: compare against actual resting price. Example: long exit ejected at 100, carry moves actual to 102 (funding cost), stale remains 100. Market drops, new_target=101.5, min_dist=1. Condition new_target < stale - min_dist => 101.5 < 99? false, so holds. But actual is 102, new_target 101.5 is worse by 0.5 (< min_dist), so hold is correct? Wait, if actual=102, new_target=101.5, difference 0.5 < min_dist, so hold is correct. Let's find a case where it wrongly holds. We need actual > stale, and new_target < actual - min_dist but new_target >= stale - min_dist. Example: actual=102, stale=100, min_dist=1. new_target=100.5. Condition: 100.5 < 100 - 1 = 99? false -> holds. But actual=102, new_target=100.5, difference 1.5 >= min_dist, so should re-eject. Wrong hold. Conversely, if actual < stale, new_target could trigger re-eject without min_dist. So BREAKS. Smallest fix: in Grind_AutoEjectTrySide, replace `resting = layer.exit_target` with `resting = Grind_OrderGetPriceOpen(exit_order_ticket)` (grind_engine.mqh:Grind_OrderGetPriceOpen) and return -1 if 0.

T-3: NEEDS-FIX. Evidence: Grind_AutoEjectOnTick (grind_engine.mqh:599) copies exactly `bar_count = 2*stable_minutes` M1 bars by count, not by wall-clock window. It dates bars at open+60 and passes to Grind_AutoEjectStable (grind_pure.mqh:331-385) which only checks `now - times[idx] >= W*60`. It does not check that the extreme is within the last 2W minutes. After a weekend gap, the 2W bars can span more than 2W wall-clock minutes, so an extreme from before the gap (e.g., Friday) is considered stable and the EA can fire immediately at Monday open. Also, if fewer than 2W bars exist, `copied < bar_count` returns early (silent disable). Forming bar: times = open+60 > now, so age negative, cannot be stable (correct). Smallest fix: in Grind_AutoEjectOnTick, after CopyRates, verify the oldest bar's close time is within 2W minutes: `if(now - (rates[0].time + 60) > 2*stable_minutes*60) return;` (or filter bars older than now - 2W*60). This prevents pre-gap extremes from counting.

T-4: HOLDS. Evidence: Grind_AutoEjectOnTick (grind_engine.mqh:599) computes `current = (Grind_MarketAsk() - Grind_MarketBid()) / _Point` and baseline from `MqlRates.spread` (points). MQL5 `MqlRates.spread` is in points of the symbol, and `_Point` is the symbol point. For 5-digit and 3-digit symbols both are integer point counts. Same units. No fix.

T-5: NEEDS-FIX. Evidence: Grind_AutoEjectOnTick (grind_engine.mqh:599) performs `CopyRates` for 60 and 2W bars before checking whether any side is at cap. The cap check is inside Grind_AutoEjectTrySide (grind_engine.mqh:515). So every tick while enabled (even with no side capped) does two history copies. Risk: CPU, and if history is not yet loaded, `copied < bar_count` returns early and silently disables auto-eject. Smallest fix: move the cap check to the top of Grind_AutoEjectOnTick: `if(Grind_SideDepth(g_grind_long) < max_layers && Grind_SideDepth(g_grind_short) < max_layers) return;` before any CopyRates. Also log when copied < bar_count.

T-6: BREAKS. Evidence: Grind_CarryExitPassBegin (grind_carry.mqh:796 per ADR) builds work items with `formula = raw + Grind_EjectOffsetGet(...)`. If an auto eject (Grind_AutoEjectTrySide -> Grind_EjectAcceptLayer, grind_engine.mqh:379/515) fires after the carry pass has begun but before it processes that layer, the work item still contains the old eject offset. When carry later modifies the exit (Grind_CarryExitShiftLayer, grind_carry.mqh:796), it uses the old formula, overwriting the new ejection. I6 then expects `raw + accrued + new_eject_offset + shift` but the order sits at `old_raw + old_eject_offset + accrued`, so I6 fails and HALTS. Smallest fix: in Grind_AutoEjectTrySide, add `if(g_grind_carry_exit_pass_active) return -1;` at the top (after enabled/blocked). This prevents auto eject during the carry pass window.

T-7: HOLDS. Evidence: Grind_AutoEjectOnTick (grind_engine.mqh:599) calls TrySide for long then short sequentially with independent state. The `blocked` flag is passed by value from OnTick (fxgrind.mq5:308) and is not mutated inside the eject path. A side becoming blocked mid-sequence would only affect the next tick. No shared state or double-modify.

T-8: HOLDS. Evidence: The trigger is stateless, but restart uses Grind_ReconstructState (not shown, INFERRED) to rebuild layers and read `GRIND_EJECT_OFFSET_` GVs. The orphan test uses `layer.exit_target`, which on restart is set from the reconstructed formula (including eject offset) or actual order price. Since the carry pass bug (T-2) is in-memory only, after restart `layer.exit_target` is recomputed correctly and the orphan test behaves. No fix for T-8 itself.

PREMISE VERDICT: Not safe to enable. Behind default-off, the code is dead and poses no production risk, so merging as disabled is acceptable only if the team commits to fixing T-2 and T-6 before enabling. T-2 causes wrong orphan decisions (holds or re-ejects incorrectly) leading to stuck or over-trailed exits. T-6 is a race that can break I6 and halt the instance. T-1 and T-5 are operational/performance risks. T-3 can fire immediately after a weekend gap. Therefore, merge only as disabled scaffolding; do not enable `InpAutoEject` until T-2 and T-6 are fixed and T-1/T-3/T-5 are addressed.

TEST GAPS:
- T-1: Retry storm. Expected: after a MODIFY_FAILED, next tick does not issue another modify for same side until W minutes elapsed. Simulate 10 ticks, assert modify_calls == 1 and EJECT_REFUSED == 1.
- T-2: Stale exit_target. After carry pass moves exit to P_actual, set layer.exit_target to P_stale (different). Set new_target = P_actual - min_dist - 1 (long). Expect auto eject fires. Set new_target = P_actual - min_dist + 1, expect no fire. Also assert layer.exit_target is not used (or is updated).
- T-3: Weekend gap. Build times/vals where oldest bar is >2W minutes old and is the extreme. Expect Grind_AutoEjectStable returns false. Also test forming bar extreme: times[idx] > now, expect false. Also test fewer than 2W bars: expect no fire.
- T-4: Spread units. For a 5-digit and a 3-digit symbol, seed MqlRates.spread and ask/bid such that current spread in points equals baseline mean. Expect Grind_AutoEjectSpreadOk true at k=1.0, false at k=0.9.
- T-5: No side capped. Call Grind_AutoEjectOnTick with depth < max_layers. Expect CopyRates not called (mock history). Also test history not loaded: copied < bar_count, expect no fire and a log/telemetry.
- T-6: Carry pass race. Start carry pass (Grind_CarryExitPassBegin), then before processing layer, fire auto eject on that layer. Then let carry process. Assert I6 passes and final exit price equals raw + accrued + eject_offset + shift.
- T-7: Both sides. Set both sides capped/stable. Expect two modifies, one per side, both with source "auto". Set quarantine mid-tick: expect no modifies.
- T-8: Restart after ejection + carry. Eject layer, run carry pass, restart (Grind_ReconstructState), then run auto eject orphan test. Expect resting price used is actual order price and I6 passes.

Need ensure format exact. Also include line count? The user says "This message has a line count at the bottom" and "Line count: 96" at bottom of their message. Probably not needed in output. We can ignore.

We should also mention GIVENS check with lines. For G2, the line :515 and :595 are given. We'll use those.

Let's write final answer with exact headings. Use markdown. Keep it concise but complete.

## Final Report

GIVENS CHECK     -- G1..G4: CONFIRMED / WRONG (with line)

- **G1: CONFIRMED.** `grind_engine.mqh:379` `Grind_EjectAcceptLayer(..., const string source)` is the accept path. `Grind_EjectPollCommand` at `grind_engine.mqh:433` calls it with `"command"`.
- **G2: CONFIRMED.** `grind_engine.mqh:515` `Grind_AutoEjectTrySide` checks enabled/blocked, cap, deepest, validation, stability, spread, orphan test, then calls `Grind_EjectAcceptLayer` at `grind_engine.mqh:595` with `"auto"`.
- **G3: CONFIRMED.** `grind_engine.mqh:599` `Grind_AutoEjectOnTick` builds series from `CopyRates(PERIOD_M1)`, dates bars at `time + 60`, long = `low`, short = `high + spread * _Point`; called from `fxgrind.mq5:308` after the command poll and before the halt return.
- **G4: CONFIRMED.** `grind_pure.mqh:331-385` implements `Grind_ExtremeIndexMostRecent`, `Grind_AutoEjectStable`, `Grind_AutoEjectSpreadOk`, `Grind_AutoEjectTargetWorse` as ADR-157 states.

T-1 .. T-8       -- verdict, evidence, smallest fix

- **T-1: BREAKS.** Evidence: `Grind_EjectAcceptLayer` (`grind_engine.mqh:379`) returns `GRIND_EJECT_MODIFY_FAILED` without setting any backoff. `Grind_AutoEjectOnTick` (`grind_engine.mqh:599`) calls `Grind_AutoEjectTrySide` every tick; if still capped/stable/spread ok and not ejected, it retries. No state changes. Quantify: FX majors can do 10–100 ticks/sec. At 10 ticks/s, one side = 600 modifies/min; both = 1200/min. Soft warn 1,800/day is exceeded in <2 min. At 50 ticks/s, 3000/min per side. FTMO hyperactivity likely. **Smallest fix:** per-side in-memory backoff (e.g., `datetime g_auto_eject_backoff_long/short`) set to `now + W*60` on `MODIFY_FAILED`; check at top of `Grind_AutoEjectTrySide`.

- **T-2: BREAKS.** Evidence: `Grind_AutoEjectTrySide` (`grind_engine.mqh:515`) orphan test uses `g_grind_long.layers[idx].exit_target` / `g_grind_short.layers[idx].exit_target` (~:570). `Grind_CarryExitShiftLayer` (`grind_carry.mqh:796` per ADR) modifies the actual pending order and updates accrued/shift GVs but never updates `layer.exit_target` (no `exit_target` reference in `grind_carry.mqh`). After carry, `layer.exit_target` is stale. Example (long): ejected at 100, carry moves actual to 102, stale stays 100. Market drops, new_target=100.5, min_dist=1. Condition `new_target < stale - min_dist` → `100.5 < 99` false → holds. But actual=102, new_target=100.5 is worse by 1.5 ≥ min_dist, so it should re-eject. Wrong hold. Opposite sign can trigger sub-min re-ejects. **Smallest fix:** in `Grind_AutoEjectTrySide`, replace `resting = layer.exit_target` with `resting = Grind_OrderGetPriceOpen(exit_order_ticket)`; return -1 if 0.

- **T-3: NEEDS-FIX.** Evidence: `Grind_AutoEjectOnTick` (`grind_engine.mqh:599`) copies exactly `2*stable_minutes` M1 bars by count, not by wall-clock window. `Grind_AutoEjectStable` (`grind_pure.mqh:331`) only checks `now - times[idx] >= W*60`; it does not verify the extreme is within the last 2W minutes. After a weekend gap, the 2W bars can span >2W wall-clock minutes, so an extreme from Friday can count as stable and fire at Monday open. Fewer than 2W bars → `copied < bar_count` returns early (silent disable). Forming bar: `times = open + 60 > now`, so age negative, cannot be stable (correct). **Smallest fix:** in `Grind_AutoEjectOnTick`, after `CopyRates`, reject if the oldest bar’s close is older than `now - 2*stable_minutes*60`: `if(now - (rates[0].time + 60) > 2*stable_minutes*60) return;` (or filter bars older than that).

- **T-4: HOLDS.** Evidence: `Grind_AutoEjectOnTick` (`grind_engine.mqh:599`) computes `current = (Grind_MarketAsk() - Grind_MarketBid()) / _Point`; baseline is `MqlRates.spread` (points). MQL5 `MqlRates.spread` is in symbol points and `_Point` is the symbol point. For 5-digit and 3-digit (JPY) both are integer point counts. Same units. No fix.

- **T-5: NEEDS-FIX.** Evidence: `Grind_AutoEjectOnTick` (`grind_engine.mqh:599`) performs `CopyRates` for 60 and 2W bars before checking whether any side is at cap. The cap check is inside `Grind_AutoEjectTrySide` (`grind_engine.mqh:515`). So every tick while enabled (even with no side capped) does two history copies. Risk: CPU, and if history is not yet loaded, `copied < bar_count` returns early and silently disables auto-eject. **Smallest fix:** move the cap check to the top of `Grind_AutoEjectOnTick`: `if(Grind_SideDepth(g_grind_long) < max_layers && Grind_SideDepth(g_grind_short) < max_layers) return;` before any `CopyRates`. Also log when `copied < bar_count`.

- **T-6: BREAKS.** Evidence: `Grind_CarryExitPassBegin` (`grind_carry.mqh:796` per ADR) builds work items with `formula = raw + Grind_EjectOffsetGet(...)`. If auto eject (`Grind_AutoEjectTrySide` → `Grind_EjectAcceptLayer`, `grind_engine.mqh:515/379`) fires after the carry pass has begun but before it processes that layer, the work item still holds the old eject offset. When carry later modifies the exit (`Grind_CarryExitShiftLayer`, `grind_carry.mqh:796`), it uses the old formula, overwriting the new ejection. I6 expects `raw + accrued + new_eject_offset + shift`, but the order sits at `old_raw + old_eject_offset + accrued`, so I6 fails and HALTS. **Smallest fix:** in `Grind_AutoEjectTrySide`, add `if(g_grind_carry_exit_pass_active) return -1;` after the enabled/blocked check.

- **T-7: HOLDS.** Evidence: `Grind_AutoEjectOnTick` (`grind_engine.mqh:599`) calls `Grind_AutoEjectTrySide` for long then short sequentially with independent state. The `blocked` flag is passed by value from `fxgrind.mq5:308` and is not mutated inside the eject path. A side becoming blocked mid-sequence only affects the next tick. No shared state or double-modify.

- **T-8: HOLDS.** Evidence: trigger is stateless, but restart uses `Grind_ReconstructState` (not shown; INFERRED) to rebuild layers and read `GRIND_EJECT_OFFSET_` GVs. The orphan test uses `layer.exit_target`, which on restart is set from the reconstructed formula (including eject offset) or actual order price. The T-2 bug is in-memory only; after restart `layer.exit_target` is recomputed correctly. No fix for T-8 itself.

PREMISE VERDICT  -- safe to merge (switch default off)? one paragraph

Not safe to enable. Behind default-off, the code is dead and poses no production risk, so merging as disabled scaffolding is acceptable only if the team commits to fixing T-2 and T-6 before enabling. T-2 causes wrong orphan decisions (stuck or over-trailed exits). T-6 is a race that can break I6 and halt the instance. T-1 and T-5 are operational/performance risks. T-3 can fire immediately after a weekend gap. Therefore: merge only as disabled; do not enable `InpAutoEject` until T-2 and T-6 are fixed and T-1/T-3/T-5 are addressed.

TEST GAPS        -- tests missing, with expected values

- **T-1 retry storm:** after a `MODIFY_FAILED`, next tick must not issue another modify for the same side until W minutes elapsed. Expected: simulate 10 ticks, `modify_calls == 1`, `EJECT_REFUSED == 1`.
- **T-2 stale `exit_target`:** after carry moves exit to `P_actual`, set `layer.exit_target = P_stale`. Long: set `new_target = P_actual - min_dist - 1`, expect auto eject fires. Set `new_target = P_actual - min_dist + 1`, expect no fire. Also assert the orphan test does not use `layer.exit_target`.
- **T-3 weekend gap:** build `times/vals` where the oldest bar is older than `now - 2W*60` and is the extreme. Expect `Grind_AutoEjectStable` returns false. Forming bar extreme: `times[idx] > now`, expect false. Fewer than 2W bars: expect no fire.
- **T-4 spread units:** for a 5-digit and a 3-digit symbol, seed `MqlRates.spread` and ask/bid so current spread in points equals baseline mean. Expected: `Grind_AutoEjectSpreadOk` true at k=1.0, false at k=0.9.
- **T-5 per-tick cost:** call `Grind_AutoEjectOnTick` with both sides below cap. Expected: no `CopyRates` calls. History not loaded: `copied < bar_count`, expect no fire and a log/telemetry.
- **T-6 carry-pass race:** start carry pass (`Grind_CarryExitPassBegin`), then before processing a layer fire auto eject on that layer. Then let carry process. Expected: I6 passes and final exit price equals `raw + accrued + eject_offset + shift`.
- **T-7 both sides / blocked:** both sides capped/stable. Expected: two modifies, one per side, both `source:"auto"`. Quarantine mid-tick: expect no modifies.
- **T-8 restart:** eject layer, run carry pass, restart (`Grind_ReconstructState`), then run auto-eject orphan test. Expected: resting price used is actual order price and I6 passes.

<!-- finish_reason=stop -->
